---
phase: quick-260805-v1f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx
  - src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx
  - src/frontend/components/UI/Sidebar/components/SidebarTour.tsx
  - public/locales/en/translation.json
autonomous: true
requirements: [QUICK-260805-v1f]

must_haves:
  truths:
    - "The left sidebar no longer shows a Discord entry"
    - "The left sidebar no longer shows a GitHub Sponsors entry"
    - "The Ko-fi entry still renders in the same community group"
    - "The Settings > Log 'Join our Discord' button still works (unchanged)"
    - "The sidebar tour's community step no longer claims the group links to Discord"
  artifacts:
    - path: "src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx"
      provides: "Sidebar link list without Discord / GitHub Sponsors items"
      contains: "Ko-fi"
    - path: "src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx"
      provides: "Regression assertions that the two labels are absent and Ko-fi is present"
  key_links:
    - from: "src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx"
      to: "frontend/helpers"
      via: "import removed — helper still exported for LogSettings"
      pattern: "openDiscordLink"
---

<objective>
Remove the Discord and GitHub Sponsors entries from the left sidebar menu, leaving Ko-fi and everything else intact.

Purpose: These two external links are inherited from upstream Heroic and point at Heroic's community/funding, not GameLib's. They do not belong in this fork's sidebar.

Output: `SidebarLinks` renders without the two items, its now-dead imports are gone, a regression test locks the removal in, and the sidebar tour copy stops promising a Discord link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx
@src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx
@src/frontend/components/UI/Sidebar/components/SidebarTour.tsx

<interfaces>
<!-- Extracted from the codebase. Do not re-explore to confirm these. -->

Current shape of the community group (SidebarLinks/index.tsx, ~lines 304-325):
a single `<div data-tour="sidebar-community">` wrapping three `<SidebarItem elementType="button" ...>`
entries in order: Discord (`icon={faDiscord}`, `label={t('userselector.discord', 'Discord')}`,
`onClick={() => handleExternalLink(openDiscordLink)}`), Ko-fi (`icon={faCoffee}`, `label="Ko-fi"`,
`onClick={() => handleExternalLink(window.api.openKofiPage)}`), and GitHub Sponsors
(`icon={faGithub}`, `label="GitHub Sponsors"`,
`onClick={() => handleExternalLink(window.api.openGithubSponsorsPage)}`).

Import facts that decide what becomes dead in this file:
- line 19: `import { faDiscord, faGithub } from '@fortawesome/free-brands-svg-icons'` — BOTH symbols
  are used only by the two removed items, so the whole import line dies.
- line 20: `import { openDiscordLink } from 'frontend/helpers'` — used only by the removed Discord
  item in this file, so this import line dies.
- `faCoffee` (line 7, from `free-solid-svg-icons`) is used by Ko-fi and MUST stay.

Cross-file facts (verified — do NOT widen scope to these):
- `openDiscordLink` is still consumed by `src/frontend/screens/Settings/sections/LogSettings/index.tsx`
  (line 10 import, line 259 onClick), so `frontend/helpers`' export stays.
- The `openDiscordLink` / `openGithubSponsorsPage` IPC listeners exist in `src/backend/main.ts`,
  `src/backend/sidecar/shellFilesFlowRegistration.ts`, `src/common/types/ipc.ts`, and
  `src/preload/api/{helpers,misc}.ts`, and are covered by `src/backend/sidecar/__tests__/shellFilesFlows.test.ts`.
  Leave the entire IPC surface alone.
- The `userselector.discord` translation key stays in `public/locales/**` (upstream-owned; other
  screens and translation tooling reference it).

Test harness facts (`__tests__/index.test.tsx`):
- No jsdom. `SidebarLinks()` is called as a plain function; `SidebarItem` is mocked to return
  `{ type: 'mock-sidebar-item', props }`.
- `useTranslation` is mocked as `t: (_key, defaultValue) => defaultValue`, so a rendered item's
  `props.label` equals the literal default string (`'Discord'`, `'Ko-fi'`, `'GitHub Sponsors'`).
- A `labelsOf(tree)` helper already exists inside the `describe('SidebarLinks account item')` block
  (built on the file-level `collectElements`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop the Discord and GitHub Sponsors sidebar entries</name>
  <files>src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx, src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx</files>
  <behavior>
    - Rendered sidebar labels do NOT contain 'Discord'
    - Rendered sidebar labels do NOT contain 'GitHub Sponsors'
    - Rendered sidebar labels DO still contain 'Ko-fi'
    - Existing assertions still hold: labels[0] === 'GameLib'; 'Manage Accounts' sits immediately before 'Accessibility'
  </behavior>
  <action>
Write the failing assertions first, then delete the two entries.

Test file: promote the existing `labelsOf` helper so it is reachable from a new top-level
`describe('SidebarLinks community links')` block (either lift `labelsOf` to module scope next to
`collectElements`, or duplicate the three-line helper in the new block — pick whichever keeps the
diff smallest). Add one test that builds a logged-out tree via `makeContextValue()` and asserts
`labels` does not contain `'Discord'`, does not contain `'GitHub Sponsors'`, and does contain
`'Ko-fi'`. Run the suite and confirm this new test FAILS before touching the component.

Component file: inside the `<div data-tour="sidebar-community">` block, delete the Discord
`SidebarItem` and the GitHub Sponsors `SidebarItem`. Keep the wrapping div, its `data-tour`
attribute, and the Ko-fi item exactly as they are — this is a two-entry deletion, not a
restructure. Then delete the two now-dead import lines: the whole
`@fortawesome/free-brands-svg-icons` import and the `frontend/helpers` import. Do not touch
`faCoffee` or any other import.

Test file cleanup: with `frontend/helpers` no longer imported by the component, the
`jest.mock('frontend/helpers', ...)` block and the docstring sentence that justifies it (the
"'frontend/helpers' is stubbed because its module body reads `window.api.openDiscordLink` at import
time" clause) are obsolete — remove both. If removing the mock makes the suite fail, restore the
mock only (not the docstring claim) and note the reason in the summary.

Do not modify `LogSettings`, `frontend/helpers`, the preload/IPC layer, or any locale file in this
task.
  </action>
  <verify>
    <automated>pnpm exec jest src/frontend/components/UI/Sidebar/components/SidebarLinks && pnpm exec eslint src/frontend/components/UI/Sidebar/components/SidebarLinks && pnpm codecheck</automated>
    <automated>grep -c "faDiscord\|faGithub\|GitHub Sponsors\|openDiscordLink" src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx | grep -qx 0 && grep -q "Ko-fi" src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx</automated>
  </verify>
  <done>The SidebarLinks suite passes including the new community-links test; eslint reports no unused-import errors for the file; `pnpm codecheck` is clean; `index.tsx` contains `Ko-fi` and none of `faDiscord`, `faGithub`, `GitHub Sponsors`, `openDiscordLink`.</done>
</task>

<task type="auto">
  <name>Task 2: Stop the sidebar tour from advertising the removed Discord link</name>
  <files>src/frontend/components/UI/Sidebar/components/SidebarTour.tsx, public/locales/en/translation.json</files>
  <action>
The tour step targeting `[data-tour="sidebar-community"]` currently reads "Join our community on
Discord and support GameLib's development." That group now contains only Ko-fi, so the copy is
false.

In `SidebarTour.tsx`, change the default string on the `tour.sidebar.community` `t()` call to
`"Support GameLib's development."` Keep the `t('tour.sidebar.community', ...)` call shape — the
string stays behind i18n, per the project's standing localisation requirement. Do not rename the
key and do not touch the `element` selector or any other tour step.

In `public/locales/en/translation.json`, update the matching `community` value (nested under the
sidebar tour keys, currently the same sentence) to the identical new text so the en bundle and the
inline default agree.

Do not edit non-en locale files. Translated bundles will carry the old sentence until the next
translation sync — record that as a known, accepted follow-up in the summary rather than
hand-editing 30 locales.
  </action>
  <verify>
    <automated>grep -riq "discord" src/frontend/components/UI/Sidebar/components/SidebarTour.tsx; test $? -eq 1</automated>
    <automated>grep -q "Support GameLib's development." public/locales/en/translation.json && node -e "JSON.parse(require('fs').readFileSync('public/locales/en/translation.json','utf8'))" && pnpm exec eslint src/frontend/components/UI/Sidebar/components/SidebarTour.tsx</automated>
  </verify>
  <done>`SidebarTour.tsx` contains no occurrence of "Discord"; the en `tour.sidebar.community` value matches the new inline default; `translation.json` still parses as valid JSON; eslint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→shell (`openUrlOrFile`) | Outbound external-URL opening; this change only removes two call sites, adds none |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v1f-01 | Tampering | `public/locales/en/translation.json` edit | mitigate | Verify the file still parses as JSON (`node -e "JSON.parse(...)"`) before commit — a broken bundle silently blanks UI copy |
| T-v1f-02 | Denial of Service | Dead IPC listeners `openGithubSponsorsPage` left registered | accept | Handler is inert without a caller, opens a fixed constant URL, and removing it would break `shellFilesFlows.test.ts` and the ported IPC inventory. Out of scope for this quick task |
| T-v1f-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages |
</threat_model>

<verification>
1. `pnpm exec jest src/frontend/components/UI/Sidebar` — SidebarLinks suite green, including the new community-links test.
2. `pnpm codecheck` — no TypeScript errors from removed imports.
3. `pnpm exec eslint src/frontend/components/UI/Sidebar` — no unused-import or lint errors.
4. Manual sanity (optional, not blocking): launch the app, confirm the sidebar's bottom group shows only Ko-fi, and that Settings > Log still offers "Join our Discord".
</verification>

<success_criteria>
- Discord and GitHub Sponsors entries are gone from `SidebarLinks`.
- Ko-fi entry and its `data-tour="sidebar-community"` wrapper are unchanged.
- No unused imports remain in `SidebarLinks/index.tsx`.
- A test asserts the two labels are absent and Ko-fi is present.
- Settings > Log Discord button, `frontend/helpers`' `openDiscordLink` export, and the whole IPC/preload surface are untouched.
- Sidebar tour community copy no longer references Discord (en + inline default).
- Full `pnpm exec jest src/frontend`, `pnpm codecheck`, and `pnpm exec eslint` on touched paths are clean.
</success_criteria>

<output>
Create `.planning/quick/260805-v1f-remove-discord-and-github-sponsors-from-/260805-v1f-SUMMARY.md` when done.
</output>
