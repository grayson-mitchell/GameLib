---
phase: quick-260908-k3x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/sidecar/bootstrap.ts
  - src/backend/sidecar/__tests__/rosettaBootWiring.test.ts
  - src/backend/sidecar/__tests__/rosettaPlatformGate.test.ts
  - .planning/todos/pending/2026-09-06-checkrosettainstall-never-runs-under-tauri.md
  - .planning/todos/completed/2026-09-06-checkrosettainstall-never-runs-under-tauri.md
  - .planning/STATE.md
autonomous: true
requirements: [K3X-01, K3X-02, K3X-03]
must_haves:
  truths:
    - "On an Apple Silicon Mac without Rosetta, the GameLib sidecar shows the 'Rosetta not found' dialog during boot instead of failing opaquely."
    - "The Rosetta probe does not run at all on Linux or Windows."
    - "The Rosetta warning cannot crash, delay, or fail sidecar boot."
    - "A future edit that deletes the call from init() turns a test RED (the function's own test suite stays green, which is exactly how this regression survived two phases)."
  artifacts:
    - path: "src/backend/sidecar/bootstrap.ts"
      provides: "Block F -- checkRosettaWhenMac() plus its once-guarded call site inside init()"
      contains: "checkRosettaWhenMac"
    - path: "src/backend/sidecar/__tests__/rosettaBootWiring.test.ts"
      provides: "init()-driven proof that the probe and the dialog fire from the sidecar boot path, and that a second init() does not re-probe"
    - path: "src/backend/sidecar/__tests__/rosettaPlatformGate.test.ts"
      provides: "Proof that the macOS gate suppresses the probe on non-mac"
  key_links:
    - from: "src/backend/sidecar/bootstrap.ts"
      to: "src/backend/utils.ts checkRosettaInstall"
      via: "static import + floated call chained off the i18next init promise"
      pattern: "checkRosettaInstall"
    - from: "src/backend/utils.ts checkRosettaInstall"
      to: "RUST_DIALOG_MESSAGE"
      via: "electronStub dialog.showMessageBox forward (already wired, Phase 33 Plan 03)"
      pattern: "dialog\\.showMessageBox"
---

<objective>
Port `checkRosettaInstall()` into the Tauri sidecar boot path.

Purpose: Apple Silicon is the only supported Mac target and every Steam title GameLib runs is a
Windows binary under Wine/GPTK, all of which need Rosetta. The boot-time probe and its
"install Rosetta" guidance dialog were lost when `src/backend/main.ts` was deleted by Phase
35-14, so a machine without Rosetta now fails opaquely at launch. `checkRosettaInstall()` is
still defined at `src/backend/utils.ts:1402` and is referenced by **nothing** except its own
test file — a green function test that proved nothing for two phases.

Output: a once-guarded, macOS-gated, floated call in `init()`; two test suites that fail if the
call site disappears; the todo closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/todos/pending/2026-09-06-checkrosettainstall-never-runs-under-tauri.md

Source files (read the cited ranges only — do not re-read a range twice):
- `src/backend/utils.ts` lines ~1402-1432 — `checkRosettaInstall()` itself. **Do not modify it.**
- `src/backend/sidecar/bootstrap.ts` — `init()` starts at line 415; the module-scope guard flags
  are at lines 154-199; the i18next block is at lines 605-680; Blocks A/B/E and the
  `READY_SENTINEL` write are at lines ~730-866.
- `src/backend/sidecar/__tests__/migrationsWiring.test.ts` — **the model for Task 2.** Same class
  of gap (an Electron-only startup side effect the sidecar never ran), same shape of fix, and its
  header records the mocking hygiene this codebase has already paid for.
- `src/backend/sidecar/__tests__/bootstrapWirings.test.ts` lines 1-200 — the source of the
  `jest.unmock('i18next')` line, the `online_monitor` / `axios` / `backend/constants/environment`
  / `backend/config` mock shapes, and the `waitFor` poller.

<interfaces>
Already true in the tree — verified at planning time, do not re-derive:

`src/backend/utils.ts`
```typescript
export async function checkRosettaInstall(): Promise<void>
// Shells `arch -x86_64 /usr/sbin/sysctl sysctl.proc_translated` via
// `const execAsync = promisify(exec)` (utils.ts:97, module scope — so a
// jest.mock of `child_process.exec` reaches it).
// On failure: dialog.showMessageBox({ title: t('box.warning.rosetta.title', ...),
//                                     message: t('box.warning.rosetta.message', ...),
//                                     buttons: ['OK'], icon: windowIcon })
// It has NO platform guard of its own. The caller must gate it.
```

`src/backend/sidecar/bootstrap.ts` (existing)
```typescript
import { isMac } from '../constants/environment'   // line 125 — ALREADY IMPORTED
export function init(input?: Readable, output?: Writable): void  // SYNCHRONOUS by contract
export function reconcileStoreUsersWhenOnline(): void            // the house shape for Block F
```
There is currently **no** `from '../utils'` import in `bootstrap.ts`. Adding one introduces no new
module to the sidecar bundle — `./handlers` (Step 2 of this file's own import order) already pulls
`utils.ts` in transitively, so this is a second binding onto an already-resident module, the same
situation the `playtimeSyncQueue` import comment records.

`src/backend/platform/index.ts` — `dialog.showMessageBox` **is** wired to `RUST_DIALOG_MESSAGE`
(Phase 33 Plan 03). It is a total method: it never rejects, resolving `{ response: safeIndex }` on
transport error. So the dialog genuinely renders under Tauri, and it can never crash boot.
Its forward passes only `message` / `title` / `kind` / `buttons` — **`icon` is silently
dropped.** That is acceptable and is not to be fixed here.

`public/locales/en/translation.json` lines 176-179 — `box.warning.rosetta.title` = "Rosetta not
found" and `box.warning.rosetta.message` both exist in the `translation` namespace.
</interfaces>

<gotchas>
Standing findings this plan is built on. Violating any of them produces a green test that proves
nothing, or a red CI.

1. **`src/backend/__mocks__/i18next.ts` is applied automatically to every backend test file** —
   it is `{ t: (key) => key }` with no `.init`/`.use`. A suite that wants the real i18next must
   call `jest.unmock('i18next')` (not `jest.mock`).
2. **`resetMocks: true` strips factory-supplied implementations before every test.** Any
   `jest.fn()` created in a `jest.mock()` factory must be re-implemented in `beforeEach`.
3. **A leaked `requestRustInvoke` timer crashes node after a green summary** (the live
   `pnpm test:ci` red at `sidecarRpc.ts:339`). Task 2 must never let a test reach the REAL
   `dialog.showMessageBox` forward — stub it.
4. **`jest.setupContainment.ts` already redirects `os.homedir()` project-wide** for the backend
   jest project, so neither new suite needs its own `jest.mock('os')` (which is inert per-suite
   anyway in some configurations). Do not add one.
5. **`git add` fails atomically on a nonexistent pathspec after a `git mv`** — Task 3's todo move
   must stage the destination path, and the committed blob must be line-count checked.
</gotchas>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the macOS Rosetta check into the sidecar boot path (Block F)</name>
  <files>src/backend/sidecar/bootstrap.ts</files>
  <action>
Add a new startup side effect to `bootstrap.ts`, following the exact shape of the existing
`reconcileStoreUsersWhenOnline()` (Block E) — an exported named function holding the work, plus a
once-guarded call from `init()`.

Four edits, in this order:

(a) Add `import { checkRosettaInstall } from '../utils'` to the import block. Before doing so,
confirm there is no import cycle: grep `src/backend/utils.ts` for any `from './sidecar/` or
`from 'backend/sidecar/` import and report the result in the SUMMARY. `isMac` is already imported
at line 125 — do not duplicate it.

(b) Capture the i18next readiness promise. The i18next block (line ~618) currently floats
`i18next.use(Backend).init({...}).catch(...)` with nothing holding the result. Introduce a
module-scope `let i18nReady: Promise<void> = Promise.resolve()` next to the other module-scope
guard flags (lines 154-199) and assign the **caught** promise to it:

  i18nReady = i18next.use(Backend).init({...}).then(() => undefined).catch((error) => { <the
  existing logWarning, byte-identical> })

Assigning the caught promise — not the raw one — is load-bearing and must be commented as such:
a failed i18n init must still let the Rosetta check run (with i18next's own fallback strings),
and must not leave a second unhandled rejection. If the outer `try` around
`GlobalConfig.get().getSettings()` throws, `i18nReady` correctly stays `Promise.resolve()`.

(c) Add the exported function. Name it `checkRosettaWhenMac`, place it beside
`reconcileStoreUsersWhenOnline` (above `init()`), and give it a header comment covering the
five decisions below:

  export function checkRosettaWhenMac(): void {
    if (!isMac) return
    try {
      i18nReady
        .then(() => checkRosettaInstall())
        .catch((error: unknown) => { logWarning(`[bootstrap] checkRosettaInstall() failed: ${String(error)}`, LogPrefix.Backend) })
    } catch (error) {
      logWarning(`[bootstrap] checkRosettaWhenMac() could not be started: ${String(error)}`, LogPrefix.Backend)
    }
  }

  - **Why the mac gate lives HERE and not in `checkRosettaInstall()`**: the probe shells
    `arch -x86_64 /usr/sbin/sysctl`, which is meaningless off macOS. `checkRosettaInstall()` has
    no guard of its own and is left unmodified so its existing test suite stays valid.
  - **Why floated, not awaited**: `init()` is synchronous by contract — `src/sidecar/index.ts`
    and ~10 test suites call it as such. Same constraint the `applyMigrations()` comment records.
  - **Why chained off `i18nReady` rather than relying on statement order**: the dialog's title and
    message come from `i18next.t()`, and `i18next.use(Backend).init()` is asynchronous
    (installed i18next is 22.5.1; `i18next-fs-backend` reads catalogs off disk). Statement order
    alone would only guarantee that `init()` had been *called*, not that the catalog had *loaded*
    — so a non-English user would get the inline English defaults. Chaining makes the ordering
    exact rather than probable. This is the only backend `t()` consumer whose call is scheduled by
    `init()` itself, which is why it can be made exact at all.
  - **Why it cannot fail boot**: `dialog.showMessageBox` is a total method under the sidecar
    (never rejects), the promise is caught, and the whole thing is inside a try/catch.
  - **`icon: windowIcon` is inert** under the Rust dialog forward (which passes only
    message/title/kind/buttons). Named so a reader does not go looking for a missing icon.

(d) Add the call site in `init()` as **Block F**, immediately BEFORE the
`output.write(`${READY_SENTINEL}\n`)` line (i.e. after Block E's
`storeUserReconcileInitialized` block), guarded by a new module-scope `rosettaCheckInitialized`
flag declared alongside the others:

  if (!rosettaCheckInitialized) { rosettaCheckInitialized = true; checkRosettaWhenMac() }

Comment the placement, because it is constrained from both sides:
  - Must be AFTER the i18next block (line ~618) so `i18nReady` holds the real promise rather than
    the `Promise.resolve()` initial value.
  - Must be AFTER `startRpcServer(input, output)` and `electronStub.bindTransport(...)`, because
    the dialog reaches the shell via `requestRustInvoke`, which needs a live write stream. Do not
    rely on the i18n await pushing it into a later macrotask — make the placement carry it.
  - Runs before `READY_SENTINEL`, but deliberately does NOT delay READY: boot is not blocked on a
    warning dialog.
  - The guard flag lives at the CALL SITE (matching Blocks A/B/E), not inside
    `checkRosettaWhenMac()`. Re-running the function is harmless — the probe is read-only — so the
    flag exists only to prevent a duplicate dialog, and keeping it out of the function is what
    lets `rosettaPlatformGate.test.ts` call it directly.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | tail -5; npx jest --selectProjects Backend --testPathPattern "sidecar/__tests__/(bootstrap|bootstrapWirings|electronReachLedger|externalDynamicImportGate|migrationsWiring)" 2>&1 | tail -20</automated>
  </verify>
  <done>`bootstrap.ts` exports `checkRosettaWhenMac`, `init()` calls it once behind `rosettaCheckInitialized`, `i18nReady` holds the caught i18next init promise, `tsc` is clean, and the existing sidecar bootstrap/gate suites are green (any pre-existing red is named with the sha it was measured at, per the "pre-existing is a claim about a chosen baseline" finding).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the wiring in both directions — RED first</name>
  <files>src/backend/sidecar/__tests__/rosettaBootWiring.test.ts, src/backend/sidecar/__tests__/rosettaPlatformGate.test.ts</files>
  <behavior>
`src/backend/sidecar/__tests__/rosettaBootWiring.test.ts` — the positive direction, driven through
`init()`. Model it on `migrationsWiring.test.ts` for mocking hygiene, and copy from
`bootstrapWirings.test.ts` lines 1-200: `jest.unmock('i18next')`, the `online_monitor`
full-surface mock, an `axios` mock (`head` -> `{status:200}`, `create` -> an instance whose `get`
resolves benign data), the `backend/config` `GlobalConfig` stub, and the `waitFor` poller. Pin
`jest.mock('backend/constants/environment', () => ({ isWindows: false, isMac: true, isLinux: false,
isSteamDeckGameMode: false, isFlatpak: false }))` — pinned, not host-derived, so the suite is
deterministic on a Linux runner too. Add `jest.mock('child_process', () => ({
...jest.requireActual('child_process'), exec: jest.fn() }))`, re-implemented in `beforeEach`
(gotcha 2). Do NOT add a `jest.mock('os')` (gotcha 4). Do NOT use `jest.isolateModules` — the two
tests must share `bootstrap.ts`'s module state, the way `migrationsWiring.test.ts`'s pair does.

  - Test 1 — **the point of this fix**: with `exec` invoking its callback with an
    `Error('arch: posix_spawnp: /usr/sbin/sysctl: Bad CPU type in executable')`, and
    `dialog.showMessageBox` stubbed (see below), call `init(new PassThrough(), new PassThrough())`
    and `waitFor` three assertions:
      (a) `exec` was called with a command containing both `arch -x86_64` and
          `sysctl.proc_translated` — the probe ran, from boot;
      (b) the `showMessageBox` stub was called exactly once, with `title` === "Rosetta not found";
      (c) **the ordering discriminator**: capture, INSIDE the stub at the moment it is called,
          the value of `i18next.t('box.warning.rosetta.title')` called with **no default
          argument**, and assert it is not the raw key. Loaded catalog -> "Rosetta not found";
          unloaded -> the key echoed back. This is what distinguishes Task 1(b)'s chained design
          from bare statement order; a default-carrying `t()` call cannot distinguish them,
          because the en catalog string and the inline default are byte-identical. (Same
          technique, and same reason, as `gamelibNamespaceLoad.test.ts`.)
  - Test 2 — idempotence: a second and third `init()` in the same registry does not re-probe;
    the `arch` call count stays at 1. State plainly in a comment that this test is meaningful
    only in sequence with Test 1 and must never be read as evidence of wiring on its own.

`src/backend/sidecar/__tests__/rosettaPlatformGate.test.ts` — the negative direction, and it is a
deliberate direct call rather than an `init()` drive. Mock `backend/constants/environment` with
`isMac: false, isLinux: true`, mock `child_process.exec` the same way, import
`{ checkRosettaWhenMac }` from `../bootstrap`, call it, flush, and assert `exec` was never called.
The composition to state in the header: Test 1 of the sibling suite proves `init()` calls
`checkRosettaWhenMac()` unconditionally; this suite proves the gate inside it suppresses the probe
off macOS. Neither half alone is sufficient — a direct-call test with no wiring assertion is
exactly how `shutdownBridgeHelper()` stayed green with zero production callers for a milestone.
  </behavior>
  <action>
Write both suites, then **RED-prove them before the fix is in place**. The RED proof is the
deliverable, not a formality: this regression survived two phases behind a green
`checkRosettaInstall.test.ts`, so a suite that cannot fail against a call-less `bootstrap.ts`
reproduces the exact defect.

Procedure — hold the commit constant and vary the tree (`git show <sha>:<path> >` into the tree;
never `git checkout --`, which fires this repo's post-checkout hook):
  1. Restore Task 1's `bootstrap.ts` to its pre-fix content.
  2. Run both suites. Record the exact failure messages. Test 1 must fail with a genuine
     assertion failure naming the missing call (e.g. "Received number of calls: 0"), NOT a
     module-resolution error — that distinction is what makes it non-vacuous.
  3. Note honestly which tests are green in BOTH directions (Test 2 and the platform-gate suite
     both will be — they pass against a bootstrap with no wiring at all). Record that as known
     vacuity in each file's own header, in the words `migrationsWiring.test.ts` uses. Never let a
     green there read as evidence of wiring.
  4. Restore Task 1's `bootstrap.ts` byte-identically (`git diff` must be empty against the
     post-Task-1 state) and re-run to GREEN.

`dialog.showMessageBox` MUST be stubbed in the positive suite (gotcha 3). Use
`jest.spyOn(<the electronStub module object>, 'showMessageBox').mockResolvedValue({ response: 0,
checkboxChecked: false })` — the module's `dialog` is a plain exported const object, and
`utils.ts` imports that same object reference, so the spy is observed. Re-install it in
`beforeEach` (gotcha 2). Letting the real forward run would arm a 60-second `requestRustInvoke`
timer against a PassThrough that answers nothing — the live `pnpm test:ci` red.

If the broad `child_process` mock destabilizes the boot graph, the documented fallback is a
partial mock of `../../utils` (`{ ...jest.requireActual('../../utils'), checkRosettaInstall:
jest.fn() }`) asserting the call happened. Take it only if forced, and record in the SUMMARY that
it proves the call site exists and says nothing about whether the probe or dialog can actually do
their work — the weaker claim the `migrationsWiring.test.ts` header rejects for its own case.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "sidecar/__tests__/rosetta" 2>&1 | tail -25</automated>
  </verify>
  <done>Both suites green post-fix; Test 1 RED-proven against a pre-fix `bootstrap.ts` with its verbatim failure message recorded in the SUMMARY; known-vacuous tests labelled as such in their own file headers; `bootstrap.ts` restored byte-identical after the RED run.</done>
</task>

<task type="auto">
  <name>Task 3: Close the todo and record the limits of what was proven</name>
  <files>.planning/todos/pending/2026-09-06-checkrosettainstall-never-runs-under-tauri.md, .planning/todos/completed/2026-09-06-checkrosettainstall-never-runs-under-tauri.md, .planning/STATE.md</files>
  <action>
`git mv` the todo from `pending/` to `completed/` and rewrite its `status:` field from `OPEN` to a
RESOLVED narrative, following the convention of the most recent closure
(`.planning/todos/completed/2026-09-05-egs-sync-and-unsync-dialogs-are-indistinguishable-at-a-glance.md`,
landed by commit `d627fd223`): the status string names the date, the quick-task id, what actually
changed, and any claim in the original todo that turned out to be wrong or narrower than written.

Things this narrative must carry, because they are the durable findings:
  - The todo's framing ("the old caller was `main.ts:241`") is not actionable as written —
    `src/backend/main.ts` no longer exists (deleted by Phase 35-14). This was a port into the
    sidecar's own boot path, not a re-wire of an existing call site.
  - The ordering decision and why: chained off the caught i18next init promise, because
    `i18next.use(Backend).init()` is async and statement order alone would have shipped English
    defaults to every non-English user. Name the discriminating assertion that proves it.
  - `icon: windowIcon` is dropped by the Rust dialog forward. Not fixed, deliberately.
  - **What was NOT proven, stated plainly rather than implied**: no test here, and no test that
    can run on this machine, observes the dialog actually rendering on an Apple Silicon Mac that
    lacks Rosetta. This Mac HAS Rosetta, so the true failing condition is unreproducible locally;
    the probe's failure is simulated by a mocked `exec` rejection. What is proven is that the
    boot path invokes the probe, that a failed probe reaches `dialog.showMessageBox`, and that
    the forward to `RUST_DIALOG_MESSAGE` behind it is already live and total (Phase 33 Plan 03).
    The unproven residue is one hop: that the Rust shell paints that particular dialog.

Then decide, and say which you chose: EITHER close the todo outright on the reasoning above, OR
close it and file ONE new pending todo scoping a live gate — "on a Rosetta-less Apple Silicon
Mac (or with the `arch` binary made to fail), boot the packaged app and confirm the dialog
paints" — carrying `severity: minor`, `platform: macos`, `ready: live-gate` in exactly that key
order (CLAUDE.md's Todo triage frontmatter section; `pnpm planning-gates` reds without them).
Prefer filing it: the one hop above is precisely the shape of gap this project's records keep
losing.

Finally add a `last_activity` entry to `.planning/STATE.md`. **Do not invoke any `gsd-sdk`
`state.*` verb** — it has corrupted `STATE.md` and `ROADMAP.md` on every recorded attempt (9/9).
Edit the file directly.
  </action>
  <verify>
    <automated>pnpm planning-gates 2>&1 | tail -15; test -f .planning/todos/completed/2026-09-06-checkrosettainstall-never-runs-under-tauri.md && test ! -f .planning/todos/pending/2026-09-06-checkrosettainstall-never-runs-under-tauri.md && echo TODO_MOVED</automated>
  </verify>
  <done>Todo lives in `completed/` with a RESOLVED status narrative naming the unproven live hop; any new pending todo carries `severity:`/`platform:`/`ready:` in order; `pnpm planning-gates` exits 0; `STATE.md` updated by hand. After committing, verify the committed blob is not an empty rename: `git show HEAD --stat` must show the moved file with a nonzero line delta (the `git mv` + `git add` atomic-failure trap).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar -> `arch -x86_64 /usr/sbin/sysctl` | A fixed literal command with no user-controlled interpolation. Read-only probe. |
| sidecar -> Rust shell (`RUST_DIALOG_MESSAGE`) | Existing, already-audited channel. This change adds one more caller, no new channel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-K3X-01 | Denial of Service | `init()` boot path | mitigate | Call is floated, never awaited; `init()` stays synchronous by contract. `READY_SENTINEL` is not delayed on it. |
| T-K3X-02 | Denial of Service | unhandled rejection crashing the sidecar | mitigate | Both the chained promise and the synchronous body are caught and logged. `dialog.showMessageBox` is itself a total method (never rejects). |
| T-K3X-03 | Information Disclosure | probe output in `gamelib.log` | accept | `checkRosettaInstall()` logs only "Rosetta is available/not available" — no paths, no user data. |
| T-K3X-04 | Tampering | command injection into the probe | accept | The command is a compile-time string literal with no interpolation; unchanged from the code that has shipped since Heroic. |
| T-K3X-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages. No `package.json` change is in `files_modified`; if one becomes necessary, STOP and run the Package Legitimacy Gate first. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exit 0.
2. `npx jest --selectProjects Backend --testPathPattern "sidecar/__tests__/rosetta"` — both new suites green.
3. `npx jest --selectProjects Backend --testPathPattern "sidecar/__tests__"` — no sidecar suite regressed. Any red must be named against the sha it was measured at and shown to pre-date this work; "pre-existing" without a sha is not a finding.
4. `npx jest --selectProjects Backend --testPathPattern "checkRosettaInstall"` — the pre-existing function suite still green and still untouched (`git diff` shows zero changes to `src/backend/utils.ts`).
5. `pnpm codecheck` exit 0; prettier clean on the three touched source files (dry-run — the repo-wide prettier gate is red, so never run a repo-wide write).
6. `pnpm planning-gates` exit 0.

**Explicitly NOT verified, and not claimable:** that the dialog paints on an Apple Silicon Mac
without Rosetta. This machine has Rosetta, so the failing condition cannot be produced here; the
probe failure is simulated via a mocked `exec` rejection. A live gate would prove the last hop
(Rust shell renders `RUST_DIALOG_MESSAGE` for this caller) and is scoped in Task 3.
</verification>

<success_criteria>
- `checkRosettaInstall` has a production call site again — `grep -rn "checkRosettaInstall" src/ --include="*.ts" | grep -v __tests__` returns both the definition in `utils.ts` and the import + call in `bootstrap.ts`.
- Deleting that call site turns `rosettaBootWiring.test.ts` Test 1 RED with a genuine assertion failure — demonstrated, not asserted.
- The probe never runs off macOS.
- Nothing on the boot path can be delayed or crashed by the warning.
- `src/backend/utils.ts` is byte-unchanged.
- The todo is in `completed/` with a status narrative that names the one hop still unproven.
</success_criteria>

<output>
Create `.planning/quick/260908-k3x-port-checkrosettainstall-into-tauri-side/260908-k3x-SUMMARY.md` when done.
</output>
