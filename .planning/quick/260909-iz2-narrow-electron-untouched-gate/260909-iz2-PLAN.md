---
phase: quick-260909-iz2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/sidecar/__tests__/electronUntouched.test.ts
  - .planning/todos/pending/2026-09-08-electronuntouched-gate-red-at-head-bans-any-configstore.md
  - .planning/todos/completed/2026-09-08-electronuntouched-gate-red-at-head-bans-any-configstore.md
autonomous: true
requirements: [REQ-28-02, REQ-28-04, D-04]

must_haves:
  truths:
    - "`npx jest --selectProjects Backend src/backend/sidecar/__tests__/electronUntouched.test.ts` is GREEN, with the by-construction gate still present and still asserting."
    - "The full `npx jest --selectProjects Backend` run has ZERO failures (baseline at HEAD is 1 failed / 4744 passed / 2 skipped, and this gate is the single failure)."
    - "A synthetic `import { configStore } from '../storeManagers/steam/electronStores'` still TRIPS the gate."
    - "A synthetic `import { configStore as steamStore } from './electronStores'` still TRIPS the gate (aliasing does not evade)."
    - "A synthetic `configStore.set(TOKEN_STORE_KEY, x)` body still TRIPS the gate."
    - "`bootstrap.ts`'s real `import { configStore } from '../constants/key_value_stores'` does NOT trip the gate."
    - "`bootstrap.ts` and `keyringTokenStore.ts` are byte-unchanged by this task."
    - "The todo is in `.planning/todos/completed/`, recording that reading 1 (the gate is over-broad) was taken and why reading 2 was refuted."
  artifacts:
    - path: "src/backend/sidecar/__tests__/electronUntouched.test.ts"
      provides: "Named source-TEXT helper encoding D-04's binding-level constraint, the rewired by-construction gate, and the guard tests that prove the narrowing"
      contains: "findSteamTokenSurfaceViolations"
    - path: ".planning/todos/completed/2026-09-08-electronuntouched-gate-red-at-head-bans-any-configstore.md"
      provides: "Closed todo with the disposition recorded in its `status:` field"
  key_links:
    - from: "the by-construction gate test"
      to: "findSteamTokenSurfaceViolations"
      via: "the gate calls the helper on each file's source text instead of running its own inline regex"
      pattern: "findSteamTokenSurfaceViolations\\("
    - from: "the guard tests"
      to: "findSteamTokenSurfaceViolations"
      via: "the same helper, driven against synthetic source strings (text in, not a path)"
      pattern: "findSteamTokenSurfaceViolations\\("
---

<objective>
Narrow `electronUntouched.test.ts`'s by-construction gate from a bare `configStore` substring ban
to a binding-level encoding of D-04, and PROVE the narrowing did not open a blind spot.

Purpose: the Backend suite is deterministically RED at HEAD on this one gate. `204025b39` added
`import { configStore } from '../constants/key_value_stores'` to `bootstrap.ts` — a DIFFERENT
store (Epic/GOG), whose only use is `configStore.delete('userInfo')` at `bootstrap.ts:431`, an
Epic user record and not a token. D-04's actual wording is narrower than the gate:
"HARD CONSTRAINT: the sidecar must never write `TOKEN_STORE_KEY` into the shared `configStore`" —
the STEAM store from `storeManagers/steam/electronStores`. The gate convicts correct code.

Output: one modified test file, one closed todo. `bootstrap.ts` and `keyringTokenStore.ts` are
NOT touched — the source is correct and the gate is wrong.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/2026-09-08-electronuntouched-gate-red-at-head-bans-any-configstore.md
@src/backend/sidecar/__tests__/electronUntouched.test.ts
@src/backend/testUtils/stripSourceComments.ts

<decision_locked>
The decision the todo left open is ALREADY MADE. Do not re-litigate it, do not present
alternatives, do not add a "decide between reading 1 and reading 2" step.

**Reading 1 is taken: the gate is over-broad.** Evidence:
- D-04, verbatim (`.planning/phases/28-tauri-keyring-real-safestorage-via-the-keyring-crate/28-CONTEXT.md:57`
  and `28-RESEARCH.md:55`): "HARD CONSTRAINT: the sidecar must never write `TOKEN_STORE_KEY` into
  the shared `configStore`" — the Steam store from `storeManagers/steam/electronStores`.
- `bootstrap.ts:100` imports a DIFFERENT `configStore` from `../constants/key_value_stores`; its
  only use is `configStore.delete('userInfo')` at `:431`, an Epic user record, not a token.

**Reading 2 is REFUTED: the sidecar is NOT meant to be free of all key-value stores.** Both gated
files already legitimately import from the Steam token surface:
- `keyringTokenStore.ts:6` — `import type { TokenStore, TokenReadOutcome, TokenUnreadableReason }
  from 'backend/storeManagers/steam/tokenStore'`
- `bootstrap.ts:73` — `import { setTokenStore as installTokenStore } from
  '../storeManagers/steam/tokenStore'`, which IS D-04's seam.
A specifier-level (module-level) ban would therefore be RED ON ARRIVAL. The ban must be at the
BINDING level: which names a steam-token-surface module is allowed to bind into these files.
</decision_locked>

<interfaces>
Already imported by the test file (line ~40):

```ts
import { stripSourceComments as stripComments } from 'backend/testUtils/stripSourceComments'
```

Also available from the same module, NOT yet imported — import it if the import-line scan needs
trailing-comment stripping (it does; see `<gotchas>`):

```ts
export function stripSourceComments(source: string): string
export function stripTrailingLineCommentTs(line: string): string
```

`stripSourceComments` strips block comments (`/* ... */`) and drops WHOLE lines that begin with a
comment marker. It does NOT strip a trailing `// ...` on a code line — that is deliberate (the
WR-08 regression class). `stripTrailingLineCommentTs` is the string-literal-aware single-line TS
scanner (tracks `'`, `"`, backtick, honours `\` escapes) that safely removes a trailing comment.

The relevant constants live ONLY here — `src/backend/storeManagers/steam/constants.ts:15-16`:

```ts
export const TOKEN_STORE_KEY = 'refreshToken'
export const TOKEN_PREFIX = 'steam:v1:'
```

And the actual writer — `src/backend/storeManagers/steam/tokenStore.ts:188`:

```ts
async setToken(token: string): Promise<void> {
  configStore.set(TOKEN_STORE_KEY, this.encryptToken(token))
}
```

That method belongs to `ElectronTokenStore`, which is why `ElectronTokenStore` must be one of the
banned bindings: installing it inside the sidecar would violate D-04 directly.

The gate being replaced, currently at `electronUntouched.test.ts:298-307`:

```ts
it('by-construction gate: keyringTokenStore.ts and bootstrap.ts never reference configStore/TOKEN_STORE_KEY/TOKEN_PREFIX (comments stripped)', () => {
  const files = [
    join(__dirname, '../keyringTokenStore.ts'),
    join(__dirname, '../bootstrap.ts')
  ]
  for (const file of files) {
    const stripped = stripComments(readFileSync(file, 'utf-8'))
    expect(stripped).not.toMatch(/TOKEN_STORE_KEY|TOKEN_PREFIX|configStore/)
  }
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the binding-level helper and its guard tests</name>
  <files>src/backend/sidecar/__tests__/electronUntouched.test.ts</files>

  <behavior>
  Add a named, exported-within-file helper that takes source TEXT (a string, NOT a path) so guard
  tests can drive it against synthetic sources:

  ```ts
  function findSteamTokenSurfaceViolations(source: string, label: string): string[]
  ```

  It returns a list of human-readable violation strings; an empty array means clean.

  Rule A — bare identifier ban (unchanged in spirit, kept faithful to D-04): after comment
  stripping, any occurrence of `\bTOKEN_STORE_KEY\b` or `\bTOKEN_PREFIX\b` anywhere in the source
  is a violation. These two names are unique to the Steam token surface, so a bare ban is exact.
  Note that `configStore` is deliberately NOT in this rule any more — that is the whole point.

  Rule B — binding-level ban (this is the narrowing): no import / `require(...)` / dynamic
  `import(...)` whose SPECIFIER names the Steam token surface may bind any of
  `configStore`, `TOKEN_STORE_KEY`, `TOKEN_PREFIX`, `ElectronTokenStore`.

  Steam token surface specifiers, and ONLY these:
  - `(^|/)storeManagers/steam/(electronStores|tokenStore|constants)` with an optional
    `.ts`/`.js`/`.mts`/`.cts` suffix and end-of-string — covers the
    `backend/storeManagers/steam/...` alias form and every `../`/`../../` relative form.
  - `^\./(electronStores|tokenStore|constants)` with the same optional suffix and end-of-string —
    the sibling form.
  Everything else is out of scope. In particular `../constants/key_value_stores` must NOT match
  (it is a `../constants/` path, not `./constants` and not `storeManagers/steam/constants`), and
  `../storeManagers/gog/electronStores` (real, at `bootstrap.ts:49`) must NOT match.

  Match on the IMPORTED name — the identifier LEFT of `as` in an import clause, and LEFT of `:` in
  a `require`/dynamic-import destructuring — so aliasing cannot evade.

  Whole-module bindings from a steam token surface (`import * as X from ...`, `import X from ...`,
  `const X = require(...)` with no destructuring) are ALSO violations, because they bind every
  export including the banned four.

  Expected behaviours the guard tests must pin — all four are MANDATORY; without (a)–(c) the
  narrowing is unproven and this task has failed:
  - (a) `import { configStore } from '../storeManagers/steam/electronStores'` → TRIPS
  - (b) `import { configStore as steamStore } from './electronStores'` → TRIPS (alias no evade)
  - (c) a synthetic body containing `configStore.set(TOKEN_STORE_KEY, x)` → TRIPS
  - (d) `import { configStore } from '../constants/key_value_stores'` → does NOT trip
        (this is `bootstrap.ts`'s real, correct shape)
  And, if they fit in the task budget, also pin:
  - (e) `const { configStore } = require('../storeManagers/steam/electronStores')` → TRIPS
  - (f) `const { configStore } = await import('./electronStores')` → TRIPS
  - (g) `import { setTokenStore as installTokenStore } from '../storeManagers/steam/tokenStore'`
        → does NOT trip (this is D-04's own seam, real at `bootstrap.ts:73`)
  - (h) `import type { TokenStore } from 'backend/storeManagers/steam/tokenStore'` → does NOT trip
        (real at `keyringTokenStore.ts:6`)
  - (i) `import { playtimeSyncQueue } from '../storeManagers/gog/electronStores'` → does NOT trip
  - (j) a trailing `// import { configStore } from './electronStores'` appended to a real code
        line → does NOT trip (proves the trailing-comment strip is wired)
  </behavior>

  <action>
  Implement `findSteamTokenSurfaceViolations(source, label)` in
  `src/backend/sidecar/__tests__/electronUntouched.test.ts`, placed near the bottom of the file
  beside the existing comment-stripping note, or immediately above the `describe` — the executor
  picks, but it must be a single named function taking TEXT.

  Normalisation, in this order: `stripComments(source)`, then split on `\n`, map each line through
  `stripTrailingLineCommentTs`, then rejoin with `\n`. Add `stripTrailingLineCommentTs` to the
  existing `backend/testUtils/stripSourceComments` import. Do NOT hand-roll a `/\/\/.*$/` replace —
  that is the documented WR-08 regression class and this repo has a stripper-integrity guard for it.

  Scan the rejoined text with three regexes, each capturing (binding clause, specifier), and each
  allowing NEWLINES inside the clause because `bootstrap.ts` has multi-line brace imports:
  static `import ... from '<spec>'` (allow an optional `type` keyword), `= require('<spec>')`, and
  `= [await] import('<spec>')`. Use a non-greedy `[\s\S]*?` for the clause.

  From each clause, derive the imported names: if the clause is `* as X`, or is a bare identifier
  with no braces, record the whole-module marker; for a `{ ... }` clause, split on `,` and take the
  segment LEFT of `\s+as\s+` or `\s*:\s*`, trimmed. Flag a violation when the specifier is a steam
  token surface AND either the whole-module marker is present or an imported name is in
  `['configStore', 'TOKEN_STORE_KEY', 'TOKEN_PREFIX', 'ElectronTokenStore']`.

  Put the banned-name list in a named module-level `const` with a code comment stating WHY each
  entry is there, and state explicitly — do not leave it implicit — that `ElectronTokenStore` is
  banned because it is the actual writer of `TOKEN_STORE_KEY`
  (`src/backend/storeManagers/steam/tokenStore.ts:188` does `configStore.set(TOKEN_STORE_KEY, ...)`),
  so installing it inside the sidecar would violate D-04 directly.

  Add a NEW top-level `describe` for the guard tests, named exactly:
  `steam token surface binding gate helper` — no parentheses, brackets or other regex
  metacharacters in that name or in the individual `it` titles, because Jest's `-t` filter is a
  REGEX and a `(` in a name makes the filter match zero tests while still exiting 0.

  Write the guard tests as pure string-in / array-out assertions against the helper. They must NOT
  read any file from disk, must NOT touch the real store, and must NOT add any
  `.set()`/`.delete()`/`.clear()` call anywhere — this suite is STRICTLY READ-ONLY against the
  developer's real `~/Library/Application Support/GameLib/steam_store/config.json` (a previous
  version of it permanently destroyed a real developer's Steam session; the file header explains).

  Assert on the SHAPE of the result, not just truthiness: a tripping case must produce a non-empty
  array whose joined text names the offending binding, and a clean case must produce exactly `[]`.

  Do NOT modify the existing by-construction gate assertion in this task — it stays red, and
  Task 1's verify filters it out by test name. Do NOT edit `bootstrap.ts` or `keyringTokenStore.ts`.
  Do NOT touch any other test file.
  </action>

  <verify>
    <automated>npx jest --selectProjects Backend src/backend/sidecar/__tests__/electronUntouched.test.ts -t 'steam token surface binding gate helper'</automated>
  </verify>

  <done>
  The guard-test describe runs with a NON-ZERO test count (a zero-count run is a false green — the
  `-t` regex matched nothing) and every guard test passes. Cases (a), (b), (c) and (d) are all
  present. `git diff --stat` shows exactly one changed file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire the by-construction gate onto the helper and correct the module docstring</name>
  <files>src/backend/sidecar/__tests__/electronUntouched.test.ts</files>

  <action>
  Replace the body of the existing `it('by-construction gate: keyringTokenStore.ts and bootstrap.ts
  never reference configStore/TOKEN_STORE_KEY/TOKEN_PREFIX (comments stripped)', ...)` so that,
  for each of the two files, it reads the source with `readFileSync` and calls
  `findSteamTokenSurfaceViolations(source, file)`, asserting the result is `[]`. Assert on the
  ARRAY (e.g. `expect(violations).toEqual([])`) so a failure prints which binding offended and in
  which file, rather than a bare pattern-mismatch.

  Rename the `it` title to say what it now encodes — it no longer bans the bare identifier
  `configStore`, so the old title would be a false record. Keep it free of regex metacharacters
  where practical. Suggested: `by-construction gate: keyringTokenStore.ts and bootstrap.ts never
  bind the Steam token surface -- D-04, comments stripped`.

  Do NOT delete the gate, do NOT weaken it to a warning, and do NOT drop the two gated files from
  the list. The `electronStub.ts safeStorage` gate immediately below it is unrelated — leave it
  exactly as it is.

  Then update the test file's MODULE DOCSTRING. Its final paragraph currently reads "The final two
  tests are a by-construction source gate (T-28-01/T-28-09): they read the sidecar's own source
  files with comments stripped and assert the forbidden identifiers/lie never reappear...".
  Rewrite that paragraph to state:
  - what the gate now encodes: D-04's BINDING-level constraint — the two sidecar files may not bind
    `configStore`/`TOKEN_STORE_KEY`/`TOKEN_PREFIX`/`ElectronTokenStore` from the Steam token
    surface (`storeManagers/steam/{electronStores,tokenStore,constants}` and its sibling form), and
    may not name `TOKEN_STORE_KEY`/`TOKEN_PREFIX` at all;
  - why the previous bare-substring form was wrong: it banned the identifier `configStore`
    regardless of which module it came from, so `bootstrap.ts:100`'s unrelated Epic/GOG
    `import { configStore } from '../constants/key_value_stores'` — used only for
    `configStore.delete('userInfo')` at `:431`, an Epic user record and not a token — convicted
    correct code and held the whole Backend suite red (landed by `204025b39`);
  - that the narrowing is proven, not asserted: the `steam token surface binding gate helper`
    describe drives the same helper against synthetic sources and pins that real violations still
    trip. Name that guard block explicitly so a future reader narrowing this gate again knows the
    proof exists and must be extended, per the recorded lesson that narrowing a negative gate by
    region leaves a widening blind spot.

  Also correct the docstring's "The final two tests" phrasing if the guard describe changes what
  "final" means — say what is true of the file as it now stands, not what was true before.

  Still do NOT edit `bootstrap.ts` or `keyringTokenStore.ts`, and do NOT touch any other test.
  </action>

  <verify>
    <automated>npx jest --selectProjects Backend src/backend/sidecar/__tests__/electronUntouched.test.ts</automated>
  </verify>

  <done>
  The whole file is GREEN with zero failures and zero skips, the by-construction gate test is
  present in the run output under its new name, and `git diff --name-only` lists exactly
  `src/backend/sidecar/__tests__/electronUntouched.test.ts`. `git diff --stat HEAD --
  src/backend/sidecar/bootstrap.ts src/backend/sidecar/keyringTokenStore.ts` is EMPTY.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify against the full Backend suite, check formatting, and close the todo</name>
  <files>.planning/todos/pending/2026-09-08-electronuntouched-gate-red-at-head-bans-any-configstore.md, .planning/todos/completed/2026-09-08-electronuntouched-gate-red-at-head-bans-any-configstore.md</files>

  <action>
  Run the FULL Backend suite: `npx jest --selectProjects Backend`. The verified baseline at HEAD is
  1 failed / 4744 passed / 2 skipped across 211 suites, and the single failure is this gate. The
  post-change run must report ZERO failures. Record the actual counts in the SUMMARY — do not write
  "passed" without the numbers. `--selectProjects` is case-sensitive and exits 0 on an unmatched
  project name, so confirm the run reports ~211 suites; a suite count near zero is a false green.

  Check formatting on the changed file ONLY, from the REPO ROOT (a `cd`-ed invocation resolves a
  different prettier config and can report CLEAN against nothing):
  `npx prettier --check src/backend/sidecar/__tests__/electronUntouched.test.ts`
  If it reports the file as unformatted, run `npx prettier --write` on that same single path and
  re-run the Backend file test. Prettier is red repo-wide in this project, so do NOT run a repo-wide
  check and do NOT reformat any other file.

  Close the todo with `git mv`, preserving the date-prefixed filename (this is the repo's convention
  — 88 of 108 completed todos keep their `YYYY-MM-DD-` prefix):

      git mv .planning/todos/pending/2026-09-08-electronuntouched-gate-red-at-head-bans-any-configstore.md \
             .planning/todos/completed/2026-09-08-electronuntouched-gate-red-at-head-bans-any-configstore.md

  Then edit the moved file's frontmatter to match the convention used by recently-completed todos:
  set `status:` to a quoted, single-line resolution string and add `resolved_by: quick-260909-iz2`
  and `resolves_phase: null`. The `status:` string must record:
  - WHICH reading was taken — reading 1, the gate is over-broad;
  - WHY reading 2 was refuted — both gated files already legitimately import from the Steam token
    surface (`keyringTokenStore.ts:6` binds the `TokenStore` type, `bootstrap.ts:73` binds
    `setTokenStore`, which IS D-04's seam), so a specifier-level ban would have been red on arrival;
  - WHAT shipped — the bare `configStore` substring ban became a binding-level check
    (`findSteamTokenSurfaceViolations`), `TOKEN_STORE_KEY`/`TOKEN_PREFIX` stayed bare-banned, and
    guard tests pin that real violations including the aliased form still trip;
  - the measured Backend result, with counts, before and after.
  Do NOT drop or reword the existing `severity`, `platform`, `ready`, `area`, `created`, `title`,
  or `files` keys — `pnpm planning-gates` enforces `severity`/`platform`/`ready` on `pending/` and
  the completed file should stay well-formed regardless.

  Run `pnpm planning-gates` to confirm the todo frontmatter gate is still satisfied.
  </action>

  <verify>
    <automated>npx jest --selectProjects Backend 2>&1 | tail -6; npx prettier --check src/backend/sidecar/__tests__/electronUntouched.test.ts; test -f .planning/todos/completed/2026-09-08-electronuntouched-gate-red-at-head-bans-any-configstore.md && ! test -f .planning/todos/pending/2026-09-08-electronuntouched-gate-red-at-head-bans-any-configstore.md && echo TODO_CLOSED</automated>
  </verify>

  <done>
  Full Backend run reports 0 failed (vs the 1-failed baseline) across ~211 suites with the pass
  count at or above 4745; prettier reports the changed file formatted; `TODO_CLOSED` prints; the
  completed todo's `status:` names reading 1, the refutation of reading 2, and the measured counts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> developer's real Steam store | This suite drives the REAL, unmocked `configStore` and reads `~/Library/Application Support/GameLib/steam_store/config.json`. A write here destroys live user credentials. |
| sidecar source -> Steam token surface | D-04's constraint: the sidecar must never bind or write the Electron-side Steam refresh token. This gate is the by-construction half of that proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-iz2-01 | Tampering | `electronUntouched.test.ts` guard tests | mitigate | Guard tests are pure string-in/array-out against the helper; the plan forbids any `.set()`/`.delete()`/`.clear()` call and any new disk write, preserving the suite's STRICTLY-READ-ONLY contract that a prior version violated by destroying a real developer's Steam session. |
| T-iz2-02 | Elevation of Privilege | narrowed by-construction gate | mitigate | The narrowing is proven, not asserted: mandatory guard cases (a)/(b)/(c) pin that a real Steam-surface `configStore` import, an ALIASED one, and a `configStore.set(TOKEN_STORE_KEY, ...)` body all still trip. Matching on the imported name LEFT of `as`/`:` closes the aliasing evasion; whole-module (`* as X`, bare `require`) bindings from the steam surface are also flagged. |
| T-iz2-03 | Repudiation | test module docstring | mitigate | Task 2 rewrites the docstring's final paragraph so the file's own record states what the gate encodes and why the bare-substring form was wrong — a docstring left describing the old ban would be a false record of a shipped constraint. |
| T-iz2-SC | Tampering | npm/pip/cargo installs | n/a | No package installs. This plan modifies one existing test file and moves one markdown file. |
</threat_model>

<verification>
Both of these must be run and their real numbers recorded:

1. `npx jest --selectProjects Backend src/backend/sidecar/__tests__/electronUntouched.test.ts` — GREEN,
   0 failures, and the run must include BOTH the renamed by-construction gate and the
   `steam token surface binding gate helper` guard block with a non-zero test count.
2. `npx jest --selectProjects Backend` — 0 failures across ~211 suites, against the verified HEAD
   baseline of 1 failed / 4744 passed / 2 skipped whose single failure was this gate.

Plus:
3. `npx prettier --check src/backend/sidecar/__tests__/electronUntouched.test.ts` from the repo root — clean.
4. `git diff --stat HEAD -- src/backend/sidecar/bootstrap.ts src/backend/sidecar/keyringTokenStore.ts` — EMPTY.
   These two files are correct; the gate was wrong.
5. `git status --short .planning/todos/` shows the rename into `completed/`.
</verification>

<success_criteria>
- The by-construction gate still exists, still asserts, and is GREEN.
- It no longer bans the bare identifier `configStore`; it bans binding `configStore`,
  `TOKEN_STORE_KEY`, `TOKEN_PREFIX` or `ElectronTokenStore` from the Steam token surface, matched on
  the imported name so aliasing cannot evade.
- `TOKEN_STORE_KEY` and `TOKEN_PREFIX` remain bare-banned anywhere in the two gated files.
- Guard cases (a), (b), (c) prove the narrowed gate still trips on real violations; case (d) proves
  it no longer convicts `bootstrap.ts`'s Epic/GOG `constants/key_value_stores` import.
- The full Backend suite has 0 failures.
- Only `src/backend/sidecar/__tests__/electronUntouched.test.ts` changed under `src/`.
- The todo is closed in `.planning/todos/completed/` with the disposition and the measured counts
  recorded in its `status:` field.
</success_criteria>

<output>
Create `.planning/quick/260909-iz2-narrow-electron-untouched-gate/260909-iz2-SUMMARY.md` when done.
</output>
