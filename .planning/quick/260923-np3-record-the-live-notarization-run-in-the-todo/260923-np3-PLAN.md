---
phase: quick-260923-np3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md
autonomous: true
requirements:
  - NP3-01
  - NP3-02
  - NP3-03
  - NP3-04
must_haves:
  truths:
    - 'A reader of the todo can see that the live gate RAN on 2026-09-23 and was inconclusive on its headline question.'
    - 'A reader can see WHICH of the four `NOTHING HERE IS VERIFIED` hazards moved (1) and which did not (2, 3, 4).'
    - 'A reader following `### The only real verification` is warned that pushing a throwaway tag writes the real `v0.7.0` draft release BEFORE they are told to push one.'
    - 'A reader can see that the 60-minute bound shipped by 260923-mrx is itself unproven live.'
    - 'Whoever picks up the Linux sibling todo learns it looks stale.'
    - 'Every frontmatter key is byte-identical to HEAD and the todo is still OPEN in pending/.'
  artifacts:
    - path: '.planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md'
      provides: 'The amended todo: new np3 status sub-section, corrected hazard list, warned recipe, stale-sibling pointer'
      contains: '### STATUS 2026-09-23 (quick-260923-np3)'
  key_links:
    - from: '### NOTHING HERE IS VERIFIED item 1'
      to: '### STATUS 2026-09-23 (quick-260923-np3)'
      via: 'item 1 cites run 35808881023 as the evidence that it is now observed'
      pattern: 'OBSERVED WORKING'
    - from: '### The only real verification'
      to: 'the v0.7.0 draft release side effect'
      via: 'a warning placed above the "Push a fresh throwaway tag" instruction'
      pattern: 'v__VERSION__'
---

<objective>
On 2026-09-23 the live gate for the macOS notarization todo RAN for the first time (run
35808881023). It was INCONCLUSIVE on its headline question — Apple never returned a verdict — but
DECISIVE on several sub-questions. The todo currently reads as though nothing has ever been
observed, and one of the four items under `### NOTHING HERE IS VERIFIED` is now false.

Purpose: make the todo tell the truth about what has and has not been observed, so the next person
to pick it up does not re-run a gate whose keychain hazard is already settled, and does not push a
throwaway tag without knowing it writes a real release.

Output: one amended file. DOCUMENTATION-ONLY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md

The `260923-ihw` and `260923-mrx` rows in `.planning/STATE.md` carry this same story from the other
two angles and are the cross-check for anything below. Do not edit STATE.md in this plan.
</context>

<constraints>
Locked by the operator. Do not revisit, do not plan alternatives, do not ask.

- The todo stays **OPEN** and stays in `.planning/todos/pending/`. Do not move it.
- **All frontmatter keys stay byte-identical**: `severity: critical`, `platform: macos`,
  `ready: live-gate`, `needs: retag-and-confirm-notarization-accepted`, `status: OPEN`. The gate
  ran but was INCONCLUSIVE, so `needs:` is still literally true. Do not "advance" it. Do not touch
  `created`, `title`, `area`, `found_by`, `source` or `files` either.
- **Doc-only, ONE file.** Do not edit any source file, workflow, script or test. In particular do
  NOT touch `.github/workflows/release-tauri.yml` or `meta/signMachOResources.ts`.
- Do NOT create, push or delete any git tag. Do NOT trigger, cancel or re-run any workflow. Do NOT
  touch any GitHub release, asset or `latest.json`.
- **Do NOT rewrite existing history in the todo.** `## STATUS 2026-09-17 (quick-260917-uik)` and
  `### STATUS 2026-09-23 (quick-260923-ihw)` stay as the record of what was believed then. ADD a
  new dated sub-section; do not revise those two.
- Today's date is **2026-09-23**. Use it for every new date stamp.
- Do not leave a stray closing envelope tag anywhere in the todo body —
  `.planning/planning-envelope-tag-gate.py` is one of the 12 planning gates and watches
  `.planning/**/*.md`.
</constraints>

<measured_evidence>
All of this is MEASURED. Do not re-derive it, do not re-run anything to confirm it, and do not
soften it. Run 35808881023, workflow `release-tauri.yml`, annotated tag `v0.7.0-notarize-test2` at
commit 77f3b4388, pushed by the operator by hand. Cancelled by hand at 2h14m. macOS job id
107015694390. All timestamps UTC, verbatim from the downloaded job log.

1. **The helper-signing step RAN and PASSED — its first live execution.** Step 18, "Sign every
   Mach-O in the macOS helper tree before bundling": `02:07:49` -> `02:08:20`, conclusion
   **success**, **31 seconds** for the whole tree.

2. **Hazard 1 is now OBSERVED WORKING — the keychain does NOT collide.** After step 18 created and
   tore down its throwaway keychain, Tauri's own signing pass succeeded. Verbatim: `02:13:01`
   `found cert "***" with organization "grayson mitchell"`; then `Signing` of
   `Contents/MacOS/gamelib-sidecar`, `Contents/MacOS/gamelib-shell`, `GameLib.app` and (at
   `02:13:17`) `GameLib.zip` — each `replacing existing signature`. No `security import` failure,
   no `errSecInternalComponent`, no collision. The trap-based teardown left clean state.

3. **Hazards 2, 3 and 4 remain UNOBSERVED.** Hazard 2 (signature surviving Tauri's
   `bundle.macOS.files` copy into `Contents/Resources`): still unobserved — the `.app` was bundled,
   but **no artifact was ever published or downloaded**, so nothing was inspected. Do NOT write
   "the bundle step succeeded" as evidence for it. Hazard 3 (notarization returns `Accepted`):
   still unobserved, see 4. Hazard 4 (a helper crashing at runtime under the hardened runtime with
   no entitlements): still unobserved, untouched, still requires step 6 of the recipe.

4. **Notarization was submitted and Apple never answered.** `02:13:17` `Notarizing .../GameLib.app`,
   then 2h05m37s of complete silence, then `04:18:54` `##[error]The operation was canceled.` and
   `04:18:55` `Terminate orphan process: pid (39170) (notarytool)`. Not `Invalid`, not `Accepted` —
   **no verdict at all**. Contrast the previous failed run 35223308954 (2026-09-17), where Apple
   returned `Invalid` in **62 seconds**. The build was NOT the slow part: `Built application` at
   `02:13:00`, i.e. 4m40s from step start on a warm `swatinem/rust-cache`; the Linux leg's whole
   `tauri-action` step took 4m53s.

5. **Cause of the unbounded wait, and the fix already shipped.** `release-tauri.yml` had NO
   `timeout-minutes` anywhere, so it ran against GitHub's 360-minute default. `tauri-bundler`
   hardcodes `--wait` on its `xcrun notarytool submit` call and exposes neither a timeout flag nor
   an env var (`xcrun notarytool submit --timeout` exists, but we do not own that argv; upstream PR
   `tauri-apps/tauri#13521` added `--no-wait`, NOT a timeout). Quick task **260923-mrx** (commits
   `f83b237cc`, `dad059ef2`, `f17d46cff`) shipped `timeout-minutes: 60` on the `tauri-action` step
   plus a macOS-only `if: failure()` step running `xcrun notarytool history`, so a future timeout is
   self-explaining inside the run. **That bound is itself UNPROVEN LIVE — no run has hit it.**

6. **NEW HAZARD in this todo's own verification procedure.** The todo says to "push a fresh
   throwaway tag". A throwaway tag is NOT side-effect-free. `tauri-action` is configured
   `tagName: v__VERSION__`, which resolves from `tauri.conf.json`'s version to `v0.7.0` — NOT from
   the tag that triggered the run. So every test tag writes into the one REAL `v0.7.0` draft
   release (id `378785323`). Measured after this run: `GameLib_0.7.0_aarch64.dmg` and
   `GameLib_0.7.0_x64.dmg` untouched at 2026-08-28 (the x64 one stale — Intel dropped in
   `7d84fc0d1`), while `GameLib_0.7.0_amd64.AppImage`, its `.sig` and `latest.json` were all
   overwritten at 2026-09-23T02:10:24-26Z by the Linux leg, which SUCCEEDED at 02:10:27.
   `latest.json` now reads `platforms: ['linux-x86_64', 'linux-x86_64-appimage']` — **macOS is
   gone**, replaced by a Linux-only manifest from a run whose macOS leg never finished. Bound this
   honestly: nothing is live, because the release is still a DRAFT and `promote-updater-feed.yml`
   fires only on `release: types: [published]`. But publishing that draft as-is would promote a feed
   with no macOS entry. Also worth recording: a draft is NOT findable via `releases/tags/<tag>`
   (that 404s) — you must list `repos/<owner>/<repo>/releases` and match `.draft==true`.

7. **Cleanup still owed.** The tag `v0.7.0-notarize-test2` is STILL on origin as of this writing
   (re-checked with `git ls-remote --tags origin`), unlike `v0.7.0-notarize-test1`, which this
   todo's own `found_by` records as deleted after its run.

8. **The decisive open question.** `xcrun notarytool history` has NOT been run. No Apple
   credentials exist on this Mac — `security find-generic-password -s "com.apple.gke.notary.tool"`
   returns "The specified item could not be found in the keychain", and no `APPLE_*` env vars are
   set; the credentials live only as GitHub Actions secrets, whose values cannot be read back. So
   whether the 2026-09-23T02:13 submission ended `Accepted`, `In Progress` or `Invalid` is UNKNOWN.
   That single fact is what would tell us whether the 253-binary signing fix actually worked.
</measured_evidence>

<verify_block_notes>
Read this before running any `<verify>` below. Both traps are measured, not theoretical.

- **`npx prettier --check` over this path is a GREEN CHECK PROVING NOTHING.** `.prettierignore`
  carries a bare `.planning` entry, so the file is never checked; a deliberately mis-formatted probe
  returned exit 0. The CLAUDE.md convention still requires the check to be emitted over explicit
  paths, so it IS emitted below — paired with `grep -qx '.planning' .prettierignore`, which explains
  the green and self-invalidates the pairing if the ignore entry is ever removed. Do not read a
  green prettier here as evidence the file is formatted.
- **Anchors were chosen by CONFIRMED ABSENCE at plan time.** Every string asserted below returns
  zero matches against the unedited todo (verified: `35808881023`, `v0.7.0-notarize-test2`,
  `77f3b4388`, `107015694390`, `02:13:17`, `notarytool history`, `260923-mrx`, `latest.json`,
  `378785323`, `promote-updater-feed`, `timeout-minutes`, `OBSERVED WORKING`, `v__VERSION__`,
  `draft release`, `STALE`). The obvious numbers `253`, `530`, `277` and `506` already appear in the
  file and asserting them would be VACUOUS — do not add them. `35223308954` also already appears
  once; do not assert it either.
- **Both verify blocks were controlled in both directions at plan time**: each FAILS against the
  unedited todo (`FAIL: np3 section absent` / `FAIL: item 1 not marked OBSERVED WORKING`) and PASSES
  against a simulated edit. If a block passes before you have made the edit, something is wrong with
  the block, not with your work.
- Baseline at HEAD: `pnpm planning-gates` is **12/12 green**.
</verify_block_notes>

<tasks>

<task type="auto">
  <name>Task 1: Add the dated np3 status sub-section recording run 35808881023</name>
  <files>.planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md</files>
  <action>
Insert a new sub-section titled exactly `### STATUS 2026-09-23 (quick-260923-np3)` plus a short
dash-suffixed summary of your choosing. Place it AFTER the existing
`### STATUS 2026-09-23 (quick-260923-ihw)` section (which currently ends with its numbered item 6)
and BEFORE the `## Related` heading. Use `###`, matching the ihw sibling's level.

Open it the way the ihw section opens: state that it does NOT revise the two sections above it,
which record what was believed then and are left intact.

The section must carry, from `<measured_evidence>` above: the run identity (run 35808881023, tag
`v0.7.0-notarize-test2`, commit 77f3b4388, macOS job 107015694390, cancelled by hand at 2h14m); that
the helper-signing step ran and passed in 31 seconds, its first live execution; that hazard 1 is now
OBSERVED WORKING with the `02:13:01` cert line and the four `replacing existing signature` artifacts
as the evidence; the silent notarization submit at `02:13:17`, the 2h05m37s of nothing, the
cancellation and the orphaned `notarytool` pid, contrasted against the 62-second `Invalid` of the
earlier run; that the build was NOT the slow part (`Built application` at `02:13:00`, 4m40s warm);
the missing `timeout-minutes` as the cause, `tauri-bundler`'s hardcoded `--wait`, why
`--timeout` was not available to us, and that 260923-mrx shipped `timeout-minutes: 60` plus the
`xcrun notarytool history` failure step; that the mrx bound is itself UNPROVEN LIVE; the
`tagName: v__VERSION__` side effect with the `v0.7.0` draft release id `378785323`, the overwritten
`latest.json` and its now macOS-less `platforms` list, the honest bound that nothing is live because
`promote-updater-feed.yml` fires only on `release: types: [published]`, and the draft-lookup gotcha;
the still-undeleted `v0.7.0-notarize-test2` tag as cleanup owed; and the decisive open question —
`xcrun notarytool history` unrun because no Apple credentials exist on this Mac.

Two things must be stated and must NOT be softened. First: the run was INCONCLUSIVE on the headline
question. No verdict is not a pass. Second: hazards 2, 3 and 4 are still unobserved, and
specifically, do NOT let "the bundle step succeeded" stand as evidence for hazard 2 — no artifact
was ever published or downloaded, so nothing was inspected.

Close the section the way the ihw one closes: the todo stays OPEN, stays in `pending/`, and
`ready: live-gate` / `needs: retag-and-confirm-notarization-accepted` are unchanged, because the
gate ran but did not answer.

Do not edit the `### NOTHING HERE IS VERIFIED` list, the recipe, or `## Related` in this task —
those are Task 2.
  </action>
  <verify>
    <automated>
F=.planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md
S=$(sed -n '/^### STATUS 2026-09-23 (quick-260923-np3)/,/^## Related/p' "$F")
[ -n "$S" ] || { echo "FAIL: np3 section absent"; exit 1; }
for a in 35808881023 'v0.7.0-notarize-test2' 77f3b4388 107015694390 '02:13:17' 'notarytool history' 260923-mrx 'latest.json' 378785323 promote-updater-feed timeout-minutes 'OBSERVED WORKING'; do
  printf '%s\n' "$S" | grep -qF -- "$a" || { echo "FAIL: missing anchor $a"; exit 1; }
done
grep -q '^### STATUS 2026-09-23 (quick-260923-ihw)' "$F" || { echo "FAIL: ihw section was disturbed"; exit 1; }
grep -q '^## STATUS 2026-09-17 (quick-260917-uik)' "$F" || { echo "FAIL: uik section was disturbed"; exit 1; }
echo "OK task1"
    </automated>
  </verify>
  <done>
The new `### STATUS 2026-09-23 (quick-260923-np3)` sub-section sits between the ihw section and
`## Related`, carries every anchor above, states the run was inconclusive, and both prior STATUS
sections are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the hazard list in place, warn the recipe, and flag the stale sibling</name>
  <files>.planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md</files>
  <action>
Three in-place edits to existing sections of the same file.

**(a) `### NOTHING HERE IS VERIFIED` — edit in place, do NOT delete or renumber the list.** A reader
must be able to see which of the four moved and which did not, so the list keeps exactly four
numbered items in their existing order. Rewrite the tail of item 1 (the keychain-collision hazard)
to mark it **OBSERVED WORKING** with the date 2026-09-23 and run 35808881023 as its evidence, and
point at the new np3 sub-section for the detail. Leave items 2, 3 and 4 explicitly still
UNOBSERVED — each must still carry the word `UNOBSERVED`. While you are in item 2, add the
correction from `<measured_evidence>` item 3: the `.app` was bundled but no artifact was ever
published or downloaded, so the bundle step succeeding is NOT evidence for it.

Note the section's own framing sentence still holds and should not be weakened — a green local run
and a green CI build still prove nothing about this defect.

**(b) `### The only real verification` — warn BEFORE the push.** The warning goes into the recipe
itself, not only into the new status section, because someone following the recipe may never read
the status sections. Place it ABOVE the existing `Push a fresh throwaway tag. Then, in order:` line
so it is read before a tag is pushed, not after. It must state that a throwaway tag is NOT
side-effect-free, name `tagName: v__VERSION__` resolving from `tauri.conf.json` rather than from the
pushed tag, and name the consequence: every test tag writes into the one real `v0.7.0` draft release
(id `378785323`), which is how run 35808881023 came to overwrite `latest.json` with a
macOS-less manifest. Keep the honest bound — nothing is live while the release is a draft, because
`promote-updater-feed.yml` fires only on `release: types: [published]` — and mention that a draft
404s via `releases/tags/<tag>` so it must be found by listing releases and matching `.draft==true`.
Do not renumber or otherwise disturb the existing steps 1-6, and do not weaken the existing
"Step 6 is not optional" paragraph.

**(c) `## Related` — one line only.** Add a note that the sibling
`2026-09-17-linux-release-leg-fails-to-compile-get-window-missing-on-apphandle.md` looks STALE: the
Linux leg SUCCEEDED on run 35808881023 at 02:10:27. This is a pointer for whoever picks it up and
nothing more. **Do not open, edit, move or close that sibling todo.** Leave the other two Related
bullets alone.

Frontmatter stays byte-identical — no key added, removed, reordered or revalued.
  </action>
  <verify>
    <automated>
F=.planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md
B=$(sed -n '/^### NOTHING HERE IS VERIFIED/,/^### The only real verification/p' "$F")
[ "$(printf '%s\n' "$B" | grep -cE '^[0-9]+\. ')" -eq 4 ] || { echo "FAIL: list is not 4 numbered items"; exit 1; }
printf '%s\n' "$B" | grep -qF 'OBSERVED WORKING' || { echo "FAIL: item 1 not marked OBSERVED WORKING"; exit 1; }
[ "$(printf '%s\n' "$B" | sed -n '/^2\. /,$p' | grep -cF 'UNOBSERVED')" -eq 3 ] || { echo "FAIL: items 2-4 are not all still UNOBSERVED"; exit 1; }
R=$(sed -n '/^### The only real verification/,/^### STATUS 2026-09-23 (quick-260923-ihw)/p' "$F")
printf '%s\n' "$R" | grep -qF 'v__VERSION__' || { echo "FAIL: recipe lacks the tag side-effect warning"; exit 1; }
printf '%s\n' "$R" | grep -qF 'draft release' || { echo "FAIL: recipe lacks the draft-release consequence"; exit 1; }
w=$(printf '%s\n' "$R" | grep -nF 'v__VERSION__' | head -1 | cut -d: -f1)
p=$(printf '%s\n' "$R" | grep -nF 'Push a fresh throwaway tag' | head -1 | cut -d: -f1)
[ -n "$w" ] && [ -n "$p" ] && [ "$w" -lt "$p" ] || { echo "FAIL: warning does not precede the push instruction"; exit 1; }
REL=$(sed -n '/^## Related/,$p' "$F")
printf '%s\n' "$REL" | grep -qF 'STALE' && printf '%s\n' "$REL" | grep -qF '35808881023' || { echo "FAIL: Related lacks the stale-sibling note"; exit 1; }
git diff --exit-code -- .planning/todos/pending/2026-09-17-linux-release-leg-fails-to-compile-get-window-missing-on-apphandle.md || { echo "FAIL: sibling todo modified"; exit 1; }
for k in 'severity: critical' 'platform: macos' 'ready: live-gate' 'needs: retag-and-confirm-notarization-accepted' 'status: OPEN'; do
  sed -n '1,15p' "$F" | grep -qxF "$k" || { echo "FAIL: frontmatter key drifted: $k"; exit 1; }
done
echo "OK task2"
    </automated>
  </verify>
  <done>
Item 1 of the hazard list reads OBSERVED WORKING while items 2-4 still read UNOBSERVED and the list
is still four items; the recipe warns about the `v0.7.0` draft release above the push instruction;
`## Related` carries the stale-sibling pointer; the sibling todo and all five frontmatter keys are
untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan -> repo working tree | The only write is to one `.planning/` markdown file. No executable surface is produced or modified. |
| this task -> GitHub releases / tags / Actions | Explicitly NOT crossed. The plan records a side effect that already occurred; it must not cause another. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-np3-01 | Tampering | the two prior STATUS sections | mitigate | Task 1 verify asserts both prior STATUS headings still exist; the action forbids revising them. |
| T-np3-02 | Tampering | the sibling Linux todo | mitigate | Task 2 verify runs `git diff --exit-code` against that exact path. |
| T-np3-03 | Repudiation | the four-item hazard list | mitigate | Verify pins the list at 4 numbered items with items 2-4 still UNOBSERVED, so hazard 1 cannot be advanced by deleting or renumbering the list. |
| T-np3-04 | Elevation of Privilege | git tags / GitHub releases / workflow runs | accept | No task invokes `git tag`, `git push`, `gh release` or a workflow dispatch. Recorded as a prohibition in `<constraints>`, not enforced by a gate. |
| T-np3-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. No `<verify>` below runs a package manager install. |
</threat_model>

<verification>
Run once, after both tasks:

```
npx prettier --check .planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md
grep -qx '.planning' .prettierignore && echo 'NOTE: prettier check above is VACUOUS -- .planning is ignored'
pnpm planning-gates
git status --porcelain
```

`pnpm planning-gates` must report **12/12**, matching the HEAD baseline — this file lives in
`.planning/todos/pending/`, which the todo-frontmatter gate and the envelope-tag gate both watch.

`git status --porcelain` must list **exactly one** modified file. If it lists two, the sibling todo
or a source file was touched and the change is out of scope.

The `grep -qx` line is not decorative: it is what explains the prettier green. If it ever fails, the
`.planning` ignore entry has been removed and the prettier check above it has become real.
</verification>

<success_criteria>
- `git status --porcelain` shows exactly one modified file: the notarization todo.
- `pnpm planning-gates` reports 12/12.
- The todo is still at `.planning/todos/pending/` with all five frontmatter keys byte-identical.
- `### STATUS 2026-09-23 (quick-260923-np3)` exists between the ihw section and `## Related`, and
  states the run was INCONCLUSIVE.
- `### NOTHING HERE IS VERIFIED` still has four numbered items; item 1 is OBSERVED WORKING, items 2,
  3 and 4 still say UNOBSERVED.
- The throwaway-tag side effect is warned ABOUT inside `### The only real verification`, above the
  push instruction.
- `## Related` carries the one-line stale-sibling pointer and the sibling file is unmodified.
- No git tag created, pushed or deleted; no workflow triggered; no release or asset touched.
</success_criteria>

<output>
Create `.planning/quick/260923-np3-record-the-live-notarization-run-in-the-todo/260923-np3-SUMMARY.md` when done.
</output>
