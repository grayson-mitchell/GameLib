---
phase: quick-260909-s8x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.mjs
  - package.json
  - .gitignore
  - meta/lintScoped.cjs
  - .planning/quick/260909-s8x-split-the-lint-ratchet-into-separate-pro/260909-s8x-LINT-BASELINE.md
  - .planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-LINT-BASELINE.md
autonomous: true
requirements: [QUICK-260909-s8x]

must_haves:
  truths:
    - "The five `no-unsafe-*` rules resolve to OFF for a `.ts` file under `__tests__`/`__mocks__` and stay at WARN for a production file — provable with `eslint --print-config`, not by reading the config."
    - "`pnpm lint` exits 0 on the unmodified tree at HEAD, and exits 1 if EITHER the production ceiling or the test ceiling is exceeded — proven by mutation in both directions, on each half independently."
    - "`pnpm lint` runs BOTH halves unconditionally; a failing production half does not prevent the test half from reporting."
    - "The production ceiling and the test ceiling are independent: a test-file warning can never be absorbed by production headroom, and vice versa."
    - "The two scopes partition exactly what `eslint .` covered: zero files counted twice, zero files uncovered, and the two warning counts sum to the single-scope total — proven by set operations on three `-f json` dumps, not by reasoning about globs."
    - "Each ceiling sits at its measured count with zero padding; `N-1` is RED and `N` is GREEN for both."
    - "A scope that silently matches nothing FAILS rather than passing — the runner enforces a linted-file floor per half."
    - "`.husky/pre-push` and `.github/workflows/lint.yml` still invoke bare `pnpm lint` and are unmodified; both paths are re-run and green."
    - "The new per-half cache files are gitignored — a `pnpm lint` run leaves `git status` clean."
    - "`git diff` against `src/` is empty: not one individual lint warning was fixed or suppressed."
    - "The production warning count is IDENTICAL before and after the config edit — proving the test override did not leak into production."
    - "39-LINT-BASELINE.md's `--max-warnings 4157` ratchet is marked SUPERSEDED in place, so the stale ceiling cannot be cited as live."
  artifacts:
    - path: "meta/lintScoped.cjs"
      provides: "Single source of truth for both lint scopes, both ceilings, both cache locations, both file-count floors, and the exit-code join"
      contains: "SRC_CEILING"
    - path: "package.json"
      provides: "`lint`, `lint:src`, `lint:tests` scripts all routed through meta/lintScoped.cjs"
      contains: "lint:src"
    - path: "eslint.config.mjs"
      provides: "Test-file override that disables the five no-unsafe-* rules alongside the existing no-explicit-any: off"
      contains: "no-unsafe-member-access"
    - path: ".gitignore"
      provides: "Ignore entries for the two per-half eslint cache files"
      contains: ".eslintcache-src"
    - path: ".planning/quick/260909-s8x-split-the-lint-ratchet-into-separate-pro/260909-s8x-LINT-BASELINE.md"
      provides: "Verbatim measurements, scope-symmetry proof, six-case mutation proof matrix, and the recorded decisions"
      min_lines: 80
  key_links:
    - from: "package.json `lint` script"
      to: "meta/lintScoped.cjs"
      via: "node invocation with no selector flag (runs both halves)"
      pattern: "\"lint\": \"node meta/lintScoped\\.cjs\""
    - from: "meta/lintScoped.cjs"
      to: "eslint Node API"
      via: "require('eslint').ESLint — no shell, no spawn, no .bin shim"
      pattern: "require\\('eslint'\\)"
    - from: ".husky/pre-push"
      to: "package.json `lint` script"
      via: "bare `pnpm lint` — UNMODIFIED, must keep binding"
      pattern: "pnpm lint"
    - from: ".github/workflows/lint.yml"
      to: "package.json `lint` script"
      via: "bare `pnpm lint` — UNMODIFIED, must keep binding"
      pattern: "run: pnpm lint"
---

<objective>
`pnpm lint` is `eslint --cache --max-warnings 4157 .` and has been RED since 2026-09-06 on
**volume, not severity**: 0 errors, 4253 warnings. Because `.husky/pre-push` and
`.github/workflows/lint.yml` both call bare `pnpm lint`, this blocks every push.

Two conceptually distinct changes, in that order:

1. **Resolve a policy contradiction.** `eslint.config.mjs:85-91` already sets
   `@typescript-eslint/no-explicit-any: 'off'` for test files — `any` is EXPLICITLY SANCTIONED
   there. But the five `no-unsafe-*` rules, which fire precisely on dereferencing/assigning/
   returning/passing an `any`, remain on globally. They emit thousands of warnings in tests for
   using a construct the same config permits. Those warnings are unactionable by the project's own
   policy. Disable those five rules **in the test override only**.

2. **Split the single ceiling into two independent ratchets** so a production regression can never
   again hide under test-file headroom.

Purpose: restore a green push gate WITHOUT lowering the bar on production code, and make the
production number impossible to contaminate by test-file churn.

Output: a two-ratchet lint gate, a single-source-of-truth runner, and a baseline document carrying
verbatim measurements and a mutation proof matrix — the same discipline
`39-LINT-BASELINE.md` set when the current ceiling was established.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@eslint.config.mjs
@package.json
@.husky/pre-push
@.github/workflows/lint.yml
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-LINT-BASELINE.md

<planner_measurements>
Measured at HEAD `b72e9a365` by the planner with `npx eslint --no-cache -f json`. These are
RECORDED, not estimated — but they are a snapshot and the tree moves. **You MUST re-measure every
number before you hard-code it.** They are given here so you have a reconciliation target and can
tell a legitimate drift from a broken scope.

Baseline partition, PRE-change (already proven to be a clean partition — overlap 0, uncovered 0,
outside 0):

| scope | files linted | errors | warnings |
|---|---|---|---|
| `eslint .` (whole tree) | 1192 | 0 | 4253 |
| production (`.` minus `__tests__`/`__mocks__`) | 751 | 0 | 1123 |
| tests (`__tests__`/`__mocks__`, `.ts` + `.tsx`) | 441 | 0 | 3130 |

751 + 441 = 1192. 1123 + 3130 = 4253. Exact.

**PREDICTION for the post-change test count: 635, plus 1-2.** The five `no-unsafe-*` rules account
for 2538 warnings in the test scope, but only 2495 of those are in `.ts` files. 3130 - 2495 = 635.
The "plus 1-2" is explained in the trap below.

Predicted post-change production count: **1123, unchanged.** The override touches test files only.
If production moves, the override leaked and you must stop and diagnose.
</planner_measurements>

<planner_traps>
Five things the planner verified that will otherwise waste your time or produce a false green:

1. **The existing override glob does NOT cover `.tsx`.** `eslint.config.mjs:85` is
   `['**/__tests__/**/*.ts', '**/__mocks__/**/*.ts']`, and `*.ts` does not match `foo.tsx`.
   Confirmed against the resolved config: for
   `src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx`, `no-explicit-any` resolves to
   `[2]` (ERROR) and `unbound-method` to `[2]`, while `no-unsafe-member-access` is `[1]`. There
   are **66** such test `.tsx` files carrying **60** warnings, of which **43** are `no-unsafe-*`.
   **Do NOT widen the glob to `.tsx`.** Doing so would turn `no-explicit-any` OFF for 66 files that
   currently pass with it ON — a policy loosening nobody asked for, and explicitly outside scope.
   The consequence is a deliberate, recordable asymmetry: those 66 files are RATCHETED under the
   test ceiling but LINTED under production rules. Record it; do not "fix" it.

2. **`src/preload/__tests__/childWindows.test.ts:127`** carries
   `// eslint-disable-next-line @typescript-eslint/no-unsafe-member-access, @typescript-eslint/no-unsafe-call`.
   Once those rules are OFF for that file, the directive becomes UNUSED, and this repo's flat
   config already reports unused directives as warnings (there are 65 such `ruleId: null` findings
   in the test scope today). So the post-change test count will be 635 **plus one or two** newly
   unused-directive warnings from that single line. **Do NOT delete the directive** — that would
   be fixing an individual warning, which is out of scope. Count it, and reconcile.
   (`src/frontend/screens/Login/components/Runner/__tests__/logoutFailureSurface.test.tsx:76` has
   a similar directive but is a `.tsx` file, so per trap 1 it is unaffected.)

3. **`require.resolve('eslint/bin/eslint.js')` THROWS** `ERR_PACKAGE_PATH_NOT_EXPORTED` on eslint
   9.29.0. Do not build the runner around spawning the eslint binary. Use the Node API
   (`require('eslint').ESLint`) — no shell, no `spawnSync`, no `.bin/*.cmd` shim, and therefore
   none of this repo's recorded spawn/exit-code failure modes.

4. **There are `__tests__` directories outside `src/`** (e.g. `meta/__tests__/`). Every scope
   pattern must be repo-root-relative, never `src/`-anchored.

5. **There are zero `.test.ts`/`.spec.ts` files outside a `__tests__` directory** (verified with
   `git ls-files`). The directory-based partition is therefore complete today — but the runner's
   file-count floor (Task 2) is what keeps it honest if that changes.
</planner_traps>

<hard_constraints>
- **Do NOT fix any individual lint warning.** This changes lint POLICY and its measurement, not
  code. `git diff -- src/` MUST be empty at the end. Same for `meta/` except the one new file.
- **Do NOT touch the global severities** in the inherited `// FIXME: All of these rules should be
  errors instead` block at `eslint.config.mjs:37`. Promoting those to errors is a separate,
  much larger decision.
- **Do NOT disable the five rules globally.** Production's `no-unsafe-*` warnings are real debt and
  MUST stay visible and ratcheted.
- **Do NOT disable anything else in tests.** `unbound-method` (364) and `require-await` (114) stay
  as warnings there.
- **Do NOT lower the production ceiling** by suppressing production warnings. It is set at its
  measured value, whatever that is.
- **Do NOT run any `gsd-sdk state.*` or `roadmap.*` verb.** The orchestrator owns STATE.md and
  ROADMAP.md. `gsd-sdk` state writes are recorded as corrupting both files.
- **Commit by explicit path only.** The working tree carries unrelated untracked paths
  (`.claude/skills/archify/`, `skills-lock.json`, a `.gitkeep`) belonging to other sessions.
  No `git add -A`, no `git add .`, no `gsd-sdk query commit`. Name every path.
- **Do not run `pnpm prettier`** — the repo-wide prettier gate is independently red, and running
  the fixer would re-redden unrelated files. Run `npx prettier --check` against ONLY the files you
  create or edit.
</hard_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the policy contradiction and measure both populations</name>
  <files>eslint.config.mjs</files>
  <action>
First, capture the PRE-change ground truth so the post-change delta is attributable. Run three
independent `npx eslint --no-cache -f json` dumps into a scratch directory:
(a) whole tree: `.`;
(b) production: `.` plus `--ignore-pattern '**/__tests__/**' --ignore-pattern '**/__mocks__/**'`;
(c) tests: the four positional globs `'**/__tests__/**/*.ts'`, `'**/__tests__/**/*.tsx'`,
`'**/__mocks__/**/*.ts'`, `'**/__mocks__/**/*.tsx'` (single-quote them so the shell does not
expand them).
Parse each dump with a short node one-liner: dump length is the linted-file count,
`severity === 2` is an error, `severity === 1` is a warning. Record files/errors/warnings for all
three. Also record the current `pnpm lint` output verbatim and its exit code.

Then prove the partition by SET OPERATIONS on the `filePath` fields, not by reasoning about the
globs: assert (i) the production and test path sets are disjoint, (ii) their union equals the
whole-tree path set exactly, (iii) production warnings + test warnings equals whole-tree warnings.
All three must hold with zero slack. If any does not, STOP — the scope definition is wrong and
nothing downstream is trustworthy.

Now make the ONE config edit. In the existing test-file override block at `eslint.config.mjs:85-91`
— the block whose `files` is `['**/__tests__/**/*.ts', '**/__mocks__/**/*.ts']` and which already
carries `'@typescript-eslint/no-explicit-any': 'off'` and
`'@typescript-eslint/unbound-method': 'warn'` — add exactly five more entries, each set to `'off'`:
`@typescript-eslint/no-unsafe-argument`, `@typescript-eslint/no-unsafe-assignment`,
`@typescript-eslint/no-unsafe-call`, `@typescript-eslint/no-unsafe-member-access`,
`@typescript-eslint/no-unsafe-return`. Above them, add a short comment stating WHY: the same block
sanctions `any` in tests, so rules that fire on consuming an `any` are unactionable there by the
project's own policy; they remain WARN in production, where the debt is real. Do not change the
`files` glob. Do not change anything else in the file.

Re-run all three dumps post-change and reconcile against the planner's predictions:
production MUST be unchanged (any movement means the override leaked into production — stop and
diagnose); tests should land at 635 plus 1-2 unused-directive warnings from
`src/preload/__tests__/childWindows.test.ts:127`. Confirm that specific file is the source of the
`+1-2` by locating the new `ruleId: null` findings in the post-change test dump. If the reconciled
numbers differ from the predictions for any other reason, find the reason before continuing.

Keep every raw dump and every parsed table — Task 3 transcribes them verbatim.

Do NOT commit yet; the config edit and the ratchet must land as separate commits so a future
reader can see which change moved which number.
  </action>
  <verify>
    <automated>npx eslint --print-config src/preload/__tests__/childWindows.test.ts | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s).rules;const off=['argument','assignment','call','member-access','return'].every(k=>JSON.stringify(r['@typescript-eslint/no-unsafe-'+k])==='[0]');const any=JSON.stringify(r['@typescript-eslint/no-explicit-any'])==='[0]';const ub=JSON.stringify(r['@typescript-eslint/unbound-method'])==='[1]';const ra=JSON.stringify(r['@typescript-eslint/require-await'])==='[1]';if(off&&any&&ub&&ra){console.log('TEST SCOPE OK')}else{console.error('TEST SCOPE WRONG',JSON.stringify(r['@typescript-eslint/no-unsafe-call']),JSON.stringify(r['@typescript-eslint/unbound-method']));process.exit(1)}})" && npx eslint --print-config src/backend/utils.ts | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s).rules;const warn=['argument','assignment','call','member-access','return'].every(k=>JSON.stringify(r['@typescript-eslint/no-unsafe-'+k])==='[1]');if(warn){console.log('PROD SCOPE OK')}else{console.error('PROD SCOPE LEAKED');process.exit(1)}})"</automated>
  </verify>
  <done>
The five `no-unsafe-*` rules resolve to `[0]` for a test `.ts` file and `[1]` for a production file.
`no-explicit-any` is still `[0]` and `unbound-method` still `[1]` in tests (nothing else loosened).
The pre-change partition proof passed with zero slack. The post-change production warning count is
identical to pre-change. The post-change test count is reconciled and its every-warning delta is
attributed. `git diff --stat` shows `eslint.config.mjs` and nothing else.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install two independent ratchets behind a single-source-of-truth runner</name>
  <files>meta/lintScoped.cjs, package.json, .gitignore</files>
  <action>
Create `meta/lintScoped.cjs` — a plain CommonJS file. It is the ONLY place that defines the scopes,
the ceilings, the cache locations and the file-count floors, so the two scripts can never drift
apart. (`eslint.config.mjs:93` ignores `**/*.cjs`, so this file is not itself linted; it must still
be prettier-clean.)

It must use the ESLint Node API — `const { ESLint } = require('eslint')` — and NOT spawn a binary.
`require.resolve('eslint/bin/eslint.js')` throws on eslint 9.29.0 (planner trap 3), and this repo's
memory is a catalogue of shell/exit-code fail-open shapes (`esbuild | node -` exiting 0;
`spawnSync` blocking signals; `prettier --check` on a copy resolving a different config and
reporting CLEAN). The API removes the entire class.

Structure it as two scope descriptors, `src` and `tests`, each an object carrying: a human label; the
`ESLint` constructor options; the patterns passed to `lintFiles`; a `maxWarnings` ceiling; and a
`minFiles` floor.
- `src`: patterns `['.']`, constructor option `ignorePatterns: ['**/__tests__/**', '**/__mocks__/**']`,
  `cache: true`, `cacheLocation: '.eslintcache-src'`.
- `tests`: patterns `['**/__tests__/**/*.ts', '**/__tests__/**/*.tsx', '**/__mocks__/**/*.ts', '**/__mocks__/**/*.tsx']`,
  `cache: true`, `cacheLocation: '.eslintcache-tests'`, and `errorOnUnmatchedPattern: false` so a
  future glob that matches nothing does not abort the run — the `minFiles` floor is what catches
  that instead.

Set `maxWarnings` for each scope to the EXACT count Task 1 measured post-change. No padding — a
ratchet with headroom is not a ratchet. Put the measurement date and the commit sha they were taken
against in a comment directly above the constants, and point at the baseline document Task 3 writes.

Set `minFiles` for each scope to 50% of the Task-1-measured linted-file count, rounded down. State
in a comment that this floor exists to catch SCOPE COLLAPSE (a pattern that silently matches
nothing, which would otherwise sail under a zero-headroom ceiling and pass forever), and that it is
deliberately NOT a drift ratchet — this repo deletes files routinely.

A `runScope` function should: construct `ESLint`, `await lintFiles(patterns)`, print the `stylish`
formatter output, then evaluate three conditions in order and report ALL that fail rather than
short-circuiting — (a) any `severity === 2` error, (b) `results.length < minFiles`, (c) warning
count `> maxWarnings`. For the ceiling failure emit a message matching ESLint's own wording,
`ESLint found too many warnings (maximum: N).`, so existing greps and expectations still bind. For
the floor failure emit a message naming the scope, the observed file count and the floor. Return a
boolean.

The entry point reads `process.argv`: `--src` runs only the src scope, `--tests` only the tests
scope, and NO selector runs BOTH — unconditionally, one after the other, never short-circuiting.
Collect both booleans, print a two-line summary naming each half and its verdict, and
`process.exit(ok ? 0 : 1)`. Wrap the whole thing so that any thrown exception or rejected promise
exits NON-ZERO with the error printed; a runner that crashes must never look like a pass.

**Decision to record (the aggregate-script question):** `lint` runs both halves unconditionally and
ORs the exit codes, rather than being `pnpm lint:src && pnpm lint:tests`. The `&&` form is
exit-code-correct but reports only half the picture — a production failure means the developer
never sees the test half and needs a second round trip. Since the entire point of this change is
that the two populations are independently visible, chaining defeats it. Write this reasoning into
a comment in the runner and into the Task 3 document.

In `package.json`, replace the single `lint` line with three, keeping `lint` in the same position:
`"lint": "node meta/lintScoped.cjs"`, `"lint:src": "node meta/lintScoped.cjs --src"`,
`"lint:tests": "node meta/lintScoped.cjs --tests"`. Leave `lint-fix` alone.

**Decision to record (the pre-push and CI question):** `.husky/pre-push` and
`.github/workflows/lint.yml` both call bare `pnpm lint`, and `pnpm lint` still runs everything and
still fails on either half — so BOTH FILES STAY UNMODIFIED. This is the deliberate choice, not an
oversight: keeping one aggregate entry point means there is exactly one thing to invoke and no
chance of a caller updated to run only one half. Do not edit either file. Prove the binding by
running the exact command each invokes.

In `.gitignore`, next to the existing `.eslintcache` entry at line 25, add `.eslintcache-src` and
`.eslintcache-tests` as two explicit entries (no wildcard — explicit names cannot silently widen).
Leave the existing `.eslintcache` entry in place.

**Run the mutation proof matrix.** Six cases, each recorded verbatim with its exit code. Mutate ONLY
the constant in `meta/lintScoped.cjs`, take a byte-identical backup first (`cp`), and restore from
the backup after each case, confirming restoration with `shasum` — never by retyping.
1. `pnpm lint:src` with `SRC_CEILING` at N_src-1 -> exit 1, message names maximum N_src-1.
2. `pnpm lint:src` restored to N_src -> exit 0.
3. `pnpm lint:tests` with `TESTS_CEILING` at N_tests-1 -> exit 1.
4. `pnpm lint:tests` restored to N_tests -> exit 0.
5. `pnpm lint` with ONLY `SRC_CEILING` lowered -> exit 1, AND the tests half's output is present in
   the same run. This case carries double duty: it proves propagation from the FIRST half and
   proves the aggregate does not short-circuit.
6. `pnpm lint` with ONLY `TESTS_CEILING` lowered -> exit 1. Proves propagation from the SECOND half.
Then a seventh, targeting the fail-open shape directly: temporarily point the `tests` patterns at a
directory that does not exist and confirm `pnpm lint:tests` exits NON-ZERO naming the `minFiles`
floor — a scope that matches nothing must FAIL, not pass. Restore and re-verify by `shasum`.

Finally, re-run the Task 1 partition proof against the NEW scripts (not raw eslint invocations) to
confirm the two scripts together still cover exactly what `eslint .` covered.
  </action>
  <verify>
    <automated>git check-ignore -q .eslintcache-src && git check-ignore -q .eslintcache-tests && rm -f .eslintcache-src .eslintcache-tests && pnpm lint; echo "lint exit=$?" && test -z "$(git status --porcelain)" -o -n "$(git status --porcelain | grep -v -E 'eslint.config.mjs|package.json|.gitignore|meta/lintScoped.cjs|.claude/skills/archify|skills-lock.json|.gitkeep|.planning/quick/260909-s8x')" && pnpm lint:src; echo "src exit=$?" && pnpm lint:tests; echo "tests exit=$?" && test -z "$(git diff --name-only -- src/)" && echo "SRC UNTOUCHED"</automated>
  </verify>
  <done>
`pnpm lint` exits 0 and prints both halves. `pnpm lint:src` and `pnpm lint:tests` each exit 0 at
their exact measured ceilings. All seven mutation cases behaved as specified and their transcripts
are captured. `git check-ignore` passes for both new cache files and a full lint run leaves no new
tracked-or-untracked cache artifacts. `.husky/pre-push` and `.github/workflows/lint.yml` are
byte-unchanged (`git diff --name-only` does not list them). `git diff --name-only -- src/` is empty.
`meta/lintScoped.cjs` is restored byte-identical after every mutation, confirmed by `shasum`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the baseline record, supersede the old ceiling, and commit by explicit path</name>
  <files>.planning/quick/260909-s8x-split-the-lint-ratchet-into-separate-pro/260909-s8x-LINT-BASELINE.md, .planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-LINT-BASELINE.md</files>
  <action>
Write `260909-s8x-LINT-BASELINE.md` in this quick directory as a SIBLING to
`39-LINT-BASELINE.md`, following that document's discipline exactly: verbatim command transcripts
with exit codes, two independent measurement paths where available, a plan-vs-measured
reconciliation table, and a mutation proof shown in both directions. State the commit sha every
number was measured against.

Sections, at minimum:
- **The contradiction, stated and resolved.** What `no-explicit-any: 'off'` in the test override
  already sanctioned, and why the `no-unsafe-*` family firing in the same files was unactionable by
  the project's own policy. Name the five rules and the count each contributed.
- **Pre-change and post-change measurement tables** for all three scopes (whole tree, production,
  tests): files linted, errors, warnings, and the top rules by count for each of the two new
  populations.
- **The scope-symmetry proof.** The set operations and their results — disjointness, exact union,
  and the warning-count reconciliation — shown as the actual output, with the explicit statement
  that this was proven by measurement rather than by reading the globs.
- **The plan-vs-measured reconciliation table.** The planner predicted: production 1123 unchanged;
  tests 635 plus 1-2 unused-directive warnings. Record each prediction against what you measured
  and mark each Match / Off-by-N. Where a number is off, say why. This repo's own recorded lesson
  is that five consecutive plans found their census wrong against ground truth; the table is the
  guard.
- **The two ceilings**, each with its exact value, the sha it was measured against, and an explicit
  statement that neither carries padding.
- **The `minFiles` floor and what it is for.** State the failure mode it closes: a zero-headroom
  ceiling cannot detect a scope that matches nothing, because nothing produces zero warnings and
  zero warnings passes. Include the seventh mutation case's transcript as the proof it bites.
- **The mutation proof matrix**, all seven cases, verbatim, with exit codes, and the note that
  every mutation was restored from a `cp` backup and confirmed by `shasum` rather than retyped.
- **Two recorded decisions with their reasoning**: (a) `lint` runs both halves unconditionally and
  ORs exit codes rather than chaining with `&&`, and why; (b) `.husky/pre-push` and
  `.github/workflows/lint.yml` are deliberately UNMODIFIED because bare `pnpm lint` still covers
  everything, and why one aggregate entry point is preferable to updating each caller.
- **The recorded asymmetry.** 66 test `.tsx` files (60 warnings, 43 of them `no-unsafe-*`) are
  ratcheted under the TEST ceiling but linted under PRODUCTION rules, because the override glob is
  `*.ts` and widening it would turn `no-explicit-any` OFF for 66 files that currently pass with it
  ON. State that this was a deliberate non-change, so a future reader does not "fix" it by
  accident. Include the `eslint --print-config` evidence.
- **The surviving unused directive.** `src/preload/__tests__/childWindows.test.ts:127` names two
  now-disabled rules and its directive is now unused; it was deliberately NOT deleted because
  fixing individual warnings is out of scope, and its warning is inside the test ceiling.
- **How each ceiling is legitimately changed** — mirroring 39-LINT-BASELINE.md's own section: a
  deliberate commit citing a freshly measured number and the sha it was taken against, never drift.
- **What was NOT done.** No individual warning fixed or suppressed; the inherited
  `// FIXME: ... should be errors instead` block's global severities untouched; `git diff -- src/`
  empty.

Then edit `39-LINT-BASELINE.md` in place: at its "Task 3: the `--max-warnings` ratchet" section,
add a short, prominent SUPERSEDED note stating that the single `--max-warnings 4157` ceiling was
replaced on 2026-09-09 by two independent ceilings, that `4157` must not be cited as a live number,
and linking to this document. Do not rewrite or delete the original record — that file's own
convention is to mark superseded numbers in place, and its history of stale figures being cited as
live is exactly what the note prevents. Change nothing else in it.

Then verify the whole gate chain end to end, in the order pre-push runs it: `pnpm codecheck`,
then `pnpm lint`. Do NOT run `pnpm prettier` — the repo-wide prettier gate is independently red;
instead run `npx prettier --check` naming ONLY the files this plan created or edited. Fix any
prettier complaint on those files only. Also run `pnpm planning-gates`, since this plan adds a
document under `.planning/` and those gates run in CI.

Finally, commit. **Explicit paths only** — the working tree carries unrelated untracked paths from
other sessions (`.claude/skills/archify/`, `skills-lock.json`, and a `.gitkeep` under
`.planning/phases/43-*/`). No `git add -A`, no `git add .`, no `gsd-sdk query commit` (it stages
the entire tree). Three commits, each naming its files:
1. `fix(quick-260909-s8x): stop flagging sanctioned any in test files` — `eslint.config.mjs`
2. `fix(quick-260909-s8x): split the lint ratchet into production and test ceilings` —
   `meta/lintScoped.cjs`, `package.json`, `.gitignore`
3. `docs(quick-260909-s8x): record the two-ceiling lint baseline and mutation proofs` — this
   directory's baseline document, this plan file, and `39-LINT-BASELINE.md`
After the last commit, run `git status --porcelain` and confirm the only remaining entries are the
three unrelated paths listed above — nothing of this plan's was left behind, and nothing of another
session's was swept in.

Do not invoke any `gsd-sdk state.*` or `roadmap.*` verb. The orchestrator owns STATE.md and
ROADMAP.md.
  </action>
  <verify>
    <automated>test -s .planning/quick/260909-s8x-split-the-lint-ratchet-into-separate-pro/260909-s8x-LINT-BASELINE.md && grep -qi "SUPERSEDED" .planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-LINT-BASELINE.md && pnpm codecheck && pnpm lint && pnpm planning-gates && npx prettier --check eslint.config.mjs package.json meta/lintScoped.cjs .planning/quick/260909-s8x-split-the-lint-ratchet-into-separate-pro/260909-s8x-LINT-BASELINE.md && git diff --name-only HEAD~3 HEAD | sort | tee /dev/stderr | grep -qv -E '^(\.claude/|skills-lock\.json)' && test -z "$(git status --porcelain | grep -v -E '\.claude/skills/archify|skills-lock\.json|\.gitkeep')"</automated>
  </verify>
  <done>
The baseline document exists and carries all its required sections with verbatim transcripts.
`39-LINT-BASELINE.md` carries an in-place SUPERSEDED note on its `4157` ratchet and is otherwise
unchanged. `pnpm codecheck`, `pnpm lint` and `pnpm planning-gates` all exit 0. Prettier is clean on
the touched files only. Three commits landed, each naming explicit paths; `git diff --name-only
HEAD~3 HEAD` lists only this plan's files and none of the three unrelated session paths.
`git status --porcelain` shows only those three unrelated paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / pre-push gate -> developer's belief about code health | A gate that cannot fail is worse than no gate; this is the boundary the whole plan defends |
| npm dependency tree -> build machine | No new package is installed by this plan; the runner uses `eslint`, already a direct devDependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-s8x-01 | Repudiation | `meta/lintScoped.cjs` exit-code join | mitigate | Seven-case mutation matrix in Task 2 proves non-zero propagates from either half, and that the aggregate does not short-circuit |
| T-s8x-02 | Denial of Service (of signal) | scope patterns silently matching nothing | mitigate | `minFiles` floor per scope, itself mutation-proven by pointing the test patterns at a nonexistent directory and asserting a non-zero exit |
| T-s8x-03 | Elevation of Privilege (of test policy into production) | test override block leaking into production files | mitigate | `eslint --print-config` asserts `[1]` for the five rules on a production file, and the production warning count must be byte-identical pre/post change |
| T-s8x-04 | Tampering | uncommitted work from concurrent sessions swept into a commit | mitigate | Explicit-path commits only; `git add -A`/`git add .`/`gsd-sdk query commit` forbidden; post-commit `git status --porcelain` census |
| T-s8x-SC | Tampering | npm/pnpm installs | accept | This plan installs no package. `eslint` 9.29.0 is an existing direct devDependency; no legitimacy gate is triggered |
</threat_model>

<verification>
1. `npx eslint --print-config` on a test `.ts` file: five `no-unsafe-*` rules are `[0]`,
   `no-explicit-any` is `[0]`, `unbound-method` is `[1]`, `require-await` is `[1]`.
2. `npx eslint --print-config` on a production file: five `no-unsafe-*` rules are `[1]`.
3. Production warning count identical pre- and post-config-change.
4. Scope partition: disjoint, exact union, warning counts reconcile — by set operations on
   `-f json` dumps.
5. `pnpm lint`, `pnpm lint:src`, `pnpm lint:tests` all exit 0 at HEAD.
6. Mutation matrix: 7/7 cases behaved as specified; the runner restored byte-identical each time.
7. `.husky/pre-push` and `.github/workflows/lint.yml` unmodified and their bare `pnpm lint` still
   binds.
8. `git check-ignore` passes for both new cache files; a lint run leaves `git status` clean.
9. `git diff -- src/` empty.
10. `pnpm codecheck` and `pnpm planning-gates` exit 0; prettier clean on touched files.
11. `39-LINT-BASELINE.md` marks `4157` SUPERSEDED in place.
</verification>

<success_criteria>
- `pnpm lint` exits 0 on the unmodified tree, and `.husky/pre-push` / `.github/workflows/lint.yml`
  are green without either file being edited.
- Two independent ceilings, each at its exact measured value with zero padding, each proven RED at
  `N-1` and GREEN at `N`.
- A production regression cannot be absorbed by test-file headroom, and vice versa.
- A scope that matches nothing FAILS rather than passing.
- The two scopes partition exactly what `eslint .` covered, proven by measurement.
- Zero individual lint warnings fixed or suppressed; `git diff -- src/` empty.
- The measurement, the decisions, the asymmetry and the mutation proofs are all recorded in a
  baseline document, and the superseded `4157` ceiling is marked as such where it lives.
</success_criteria>

<output>
Create `.planning/quick/260909-s8x-split-the-lint-ratchet-into-separate-pro/260909-s8x-SUMMARY.md`
when done. It must record: both final ceilings and the sha they were measured against, the
plan-vs-measured reconciliation outcome, the 7/7 mutation matrix result, and the two recorded
decisions (unconditional both-halves aggregate; pre-push/CI deliberately unmodified).
</output>
