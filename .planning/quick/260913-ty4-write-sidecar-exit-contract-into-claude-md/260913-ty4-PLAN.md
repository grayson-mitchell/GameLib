---
quick_id: 260913-ty4
date: 2026-09-13
description: Write the sidecar's stdin-owned exit contract into CLAUDE.md as a convention
source_todo: .planning/todos/completed/2026-09-13-sidecar-stdin-owned-exit-contract-is-undocumented-centrally-and-gated-only-by-smoke-sidecar.md
scope: item 1 of that todo only (item 2, "consider a gate", is a deliberate separate decision)
files:
  - CLAUDE.md
---

# Quick 260913-ty4: write the sidecar exit contract into CLAUDE.md

## Goal

Close item 1 of the sidecar-exit-contract todo: the invariant is load-bearing, has broken three
times in three weeks, and is written only as three in-situ comments. Put it in CLAUDE.md once,
centrally, alongside the two-profile rule.

## Placement decision

Into the `<!-- GSD:conventions-start source:CONVENTIONS.md -->` region of `CLAUDE.md`, immediately
after the fake-HOME two-profile rule and before `<!-- GSD:conventions-end -->`.

The `source:CONVENTIONS.md` marker is **nominal, not live**. Verified two ways:

- `.planning/spikes/CONVENTIONS.md` is a 144-line "Spike Conventions" file whose headings are
  Stack / Structure / Patterns / Tools & Libraries. It contains **zero** of the text currently in
  CLAUDE.md's conventions region (`grep -c 'two-profile\|Todo triage\|severity: medium'` → 0).
- `d7d021a05 feat(260913-arr): add fake-HOME profile helper and the two-profile convention`
  hand-wrote the neighbouring convention straight into this region.

So a hand edit here follows established precedent and is not at risk of being regenerated away.

## Content requirements

Derived from the in-situ comment at `installedJsonWatcher.ts:133-149`, not composed fresh. Must
state **both halves**, because the second is what `260913-m9c` proved `unref()` cannot fix:

1. `unref()` every handle you create.
2. Do not leave unbounded in-flight work at boot — in-flight requests are referenced by
   definition, and `unref()` is the wrong tool for them.

Must also carry: the correction that the missing `'end'` handler is by design and the Rust shell
does reap the child; the three-break evidence table; an honest statement of what enforces this
(`smoke:sidecar` alone, blind to slow exit); and the `.unref?.()` grep trap.

## Facts, re-measured at this tree (do not take from the todo verbatim)

| claim | todo said | measured | action |
| ----- | --------- | -------- | ------ |
| `shutdown_child()` location | `main.rs:1158` | `:1158` is the doc comment; `fn` is at **`:1182`**, called at `:9631` under `RunEvent::Exit` (`:9629`) | cite 1182 / 9631 |
| unref call sites | 13 | **13** (8 optional-call, 5 plain) | confirmed |
| optional-call dominance | 8 of 13 | **8 of 13** | confirmed |
| `\.unref()` undercount | returns 5 | returns **5** real sites | confirmed |
| `STARTUP_TIMEOUT_MS` | 30s | `30_000` at `sidecarStartupSmoke.cjs:78` | confirmed |
| CI wiring | `test.yml:32` | `run: pnpm smoke:sidecar` at `test.yml:32` | confirmed |
| break SHAs | `ef77e4a1e`, `9e8e1b224` | both exist, dates/subjects match | confirmed |

Note the census trap fired live while writing this plan: a bare `grep -rn 'unref'` counts comment
prose, and `grep '\.unref()'` misses the dominant optional-call spelling. The 13 was reached by
matching both spellings and stripping comment lines.

## Tasks

1. Insert the new `### The sidecar's exit contract (stdin owns its lifetime)` section into
   CLAUDE.md's conventions region.
   - **verify:** section present; `prettier --check CLAUDE.md` clean; `pnpm planning-gates` green;
     `git diff --stat` shows CLAUDE.md only.
   - **done:** the invariant, both halves, the three-break table, the enforcement-honesty
     paragraph, the no-reflexive-gate paragraph, and the grep trap are all present.

## Out of scope

- **No gate.** Item 2 of the todo is an explicit "decide deliberately; do not add one
  reflexively" — a gate blind to the in-flight class would be worse than the honest sentence.
- No source changes. No `'end'` handler on `startRpcServer()`, no weakening of
  `installUncaughtExceptionGuard()`, no raising `STARTUP_TIMEOUT_MS` — the todo fences all three.
- The todo stays in `pending/`: item 2 remains open after this lands.
