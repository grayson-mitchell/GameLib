---
phase: quick-260923-89z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md
  - .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md
  - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [QUICK-260923-89z]

must_haves:
  truths:
    - "`gsd-sdk query audit-uat` reports by_phase['38'] == 25 and total_items == 50 -- a DROP of exactly 9, proving both that the nine entries left the array AND that the array still parses"
    - "`gsd-sdk query audit-uat` still reports by_phase['34.13'] == 7 -- the receipt edit did not disturb 34.13's own visible items"
    - "None of the nine retired items' `test:` prose is emitted by audit-uat any longer; all 25 survivors still are"
    - "Each of the nine retired entries survives verbatim under a new `human_verification_retired:` array, each carrying a retirement date of 2026-09-23, the quick id `260923-89z`, and a stated reason"
    - "`status:` in 38-VERIFICATION.md is still exactly `human_needed`"
    - "All nine `human_verification_relocated` receipts in 34.13-UAT.md that name a retired item carry a return-half `outcome:` recording the retirement -- no receipt points at a retired item while still reading 'open'"
    - "38-S16 carries a note stating its scope is UNCHANGED and it still needs BOTH the Windows and the Linux machine, so S15's retirement cannot be read as making S16 Windows-only"
    - "38-C08's scope_note no longer says a controller discharges 'all eight' and no longer instructs running C07 and C08 back to back"
    - "`python meta/runPlanningGates.py` reports 12/12 planning gates passed"
  artifacts:
    - path: ".planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md"
      provides: "the 25-item ledger plus a 9-entry human_verification_retired array"
      contains: "human_verification_retired"
    - path: ".planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md"
      provides: "return-half receipts for the nine retired items"
      contains: "260923-89z"
  key_links:
    - from: "38-VERIFICATION.md human_verification_retired"
      to: "34.13-UAT.md human_verification_relocated"
      via: "the 38-SNN / 38-C07 item id appearing in both, with the retirement recorded on both sides"
      pattern: "38-S15"
---

<objective>
Retire the nine Phase 38 UAT items that name the **electron runtime**. Phase 35 removed the
Electron build; there is no runtime on any host that can execute them. They are not deferred,
they are unrunnable, and leaving them in `human_verification` inflates the project's open-UAT
backlog with nine rows nobody can ever run.

Purpose: keep the deferred-hardware ledger a list of things that are actually schedulable.
Output: `38-VERIFICATION.md` with 25 open items and a 9-entry `human_verification_retired`
array; two-way retirement receipts in `34.13-UAT.md`; count text corrected in `ROADMAP.md`
and a narrative note in `38-HUMAN-UAT.md`.

**This ships no code.** Every file touched is a planning document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md
</context>

<premises_verified_at_plan_time>

These were re-derived at planning time, not taken on trust. Do not re-litigate them; DO
re-confirm the two marked RE-CONFIRM before editing, since they are what the change rests on.

1. **RE-CONFIRM — Electron is gone from `package.json`.** `grep -i electron package.json`
   returns NO MATCH (zero occurrences: no `dependencies` entry, no `devDependencies` entry, no
   script). The dev scripts are `tauri:dev`, `tauri:dev:packaged`, `tauri:dev:run`,
   `tauri:dev:keyring`, `tauri:dev:vault`. There is no `pnpm start`, and `34.13-UAT.md`'s
   electron rows were all run under `pnpm start`.

2. **RE-CONFIRM — the electron-runtime set is exactly nine, derived from `test:` prose.**
   Eight `test:` strings contain the literal phrase `electron runtime`:
   `38-S01`, `38-S03`, `38-S05`, `38-S07`, `38-S09`, `38-S11`, `38-S13`, `38-S15`.
   One more, `38-C07`, reads `...controller-reachable, on ELECTRON.` That is nine.
   Confirm with, from the repo root:
   `grep -n 'id: "38-\|electron runtime\|on ELECTRON' <the 38-VERIFICATION.md path>`
   No other item in the array names Electron in its `test:`. (`38-C08` mentions Electron only
   in its `not_covered_by_c07` prose — it is the TAURI half and SURVIVES. `sweep_notes.
   why_the_module_is_high_risk` mentions Electron historically; it is not an item.)
   **If the set you derive differs from these nine, STOP and report the difference rather than
   proceeding against this plan's numbers.**

3. **CORRECTION TO THE BRIEF — all nine originate in Phase 34.13, not 34.1.** The task brief
   said the S-series came from 34.13 and `38-C07` from 34.1. That is wrong: `38-C07` carries
   `origin_phase: "34.13"` / `origin_item: "G-GAMEPAD-CARET / electron (34.13-UAT.md, PART C)"`.
   **There is no 34.1 receipt to touch.** All nine receipts live in one file:
   `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md`.

4. **`38-C07` predicted this exact decision.** It already carries an `electron_cutover_risk`
   field reading: *"Phase 35 removes the Electron build. If 35 lands before a controller is
   available, this item becomes unobservable BY CONSTRUCTION... It would then be retired as
   unscoreable, NOT recorded as a pass and NOT silently dropped."* This change is the return
   half of that forward-looking note. Cite it in C07's retirement reason.

5. **Measured baseline of `gsd-sdk query audit-uat` (run at plan time):**
   `total_files: 8`, `total_items: 59`, `by_phase: {27:2, 30:2, 32:2, 33:3, 34:2, 35:7,
   38:34, 34.13:7}`.
   **Expected after this change:** `by_phase["38"] == 25`, `total_items == 50`,
   `by_phase["34.13"] == 7` (UNCHANGED), everything else unchanged.

6. **Baseline `python meta/runPlanningGates.py` → `12/12 planning gates passed`.**

7. **`pnpm planning-gates` WILL FAIL ON THIS HOST for an unrelated reason.** The script is
   `python3 meta/runPlanningGates.py`, and `python3` here resolves to the Microsoft Store
   alias stub at `/c/Users/grays/AppData/Local/Microsoft/WindowsApps/python3`, which prints
   *"Python was not found; run without arguments to install from the Microsoft Store"* and
   exits non-zero. **Use the fallback `python meta/runPlanningGates.py` (Python 3.12.10, works).**
   Do NOT "fix" `package.json` to say `python` — that would break the gate on CI/macOS.

8. **No gate ratchets on this file's count.** `.planning/uat-visibility-gate.py` carries a
   module-level assert (`assert not [k for k in LEDGER if "VERIFICATION" in k.upper()]`) that
   structurally forbids a VERIFICATION file from entering its LEDGER — 38-VERIFICATION.md is
   not ledgered and cannot be. `34.13-UAT.md` is ALSO absent from the LEDGER, which means it
   currently measures **zero** invisible items; the gate's "direction 2" fires on *a file
   absent from the ledger carrying ANY invisible item*, so an edit that broke an item's shape
   there would turn CI red. `.planning/planning-frontmatter-gate.py` targets `.planning/STATE.md`
   only. `.planning/planning-envelope-tag-gate.py` only looks for trailing orphan tool-call
   tags. **Do not widen or edit any gate. If a gate goes red, fix the document.**

9. **No local YAML parser is available.** `python` on this host has no `pyyaml`. The parse
   proof is therefore the audit count itself, exactly as the file's own `score:` line already
   argues for the insert direction: a count that does NOT move by 9 means either the edit did
   not register or the array stopped parsing.

</premises_verified_at_plan_time>

<hazards>

Restated from the file's own `audit_tool_note` and `relocation_rules` so they cannot be missed.

**H1 — `status:` MUST stay exactly `human_needed`.** `gsd-sdk query audit-uat` admits a
VERIFICATION.md when status is `human_needed` OR `gaps_found`, but only EMITS items when status
is `human_needed`. Setting `gaps_found` is admitted and then yields ZERO items — the phase
disappears from the audit entirely and the project's whole deferred backlog vanishes with
nothing turning red. **Do not touch `status:`.**

**H2 — annotating does not retire. Moving retires.** The audit counts EVERY entry in
`human_verification` regardless of any `result:`/`retired:` field. An item annotated in place is
still counted. Each of the nine must be **physically removed** from `human_verification` and
**re-inserted** under the new sibling array.

**H3 — NEVER cross-reference by position.** The `id:` field does not survive into audit output;
items are emitted with a POSITIONAL integer and the `test:` prose as `name`. Positions are
ARRIVAL order, not id order — audit position 1 is `38-W02` and position 2 is `38-W01`, off by one
in a way that silently reverses. **Match items by `test:` prose only.** Removing nine items
renumbers every position after them, so any position noted before the edit is worthless after it.

**H4 — YAML fragility, with no local parser.** The frontmatter uses a mix of unquoted plain
scalars, `"double"` and `'single'` (with `''` escaping) styles. When moving an entry, move its
lines **verbatim, byte for byte**, changing only the indentation context (which does not change
— both arrays sit at the same level). When authoring the new `score:` value, keep it a single
unquoted plain scalar on one line and mirror the punctuation shapes already proven to parse in
that field (it already contains `: `, `(`, `)`, backticks and `->`). If the audit returns 0 items
for phase 38, or drops phase 38 entirely, the YAML broke — **revert and retry, do not patch
blindly.**

**H5 — do not disturb `34.13-UAT.md`'s item shapes.** Per CLAUDE.md's "UAT item shape" rule and
premise 8: touch only the `human_verification_relocated` frontmatter block, the `relocation_note`
scalar, and the third/fourth columns of nine markdown table rows. Do **not** touch any
`### N.` heading, any `expected:` line, or any `result:` line anywhere in that file, and never
introduce `expected: |`.

**H6 — this is a RETIREMENT, not a discharge and not a pass.** Nothing was observed. Do not move
anything into `human_verification_discharged`. Every piece of text this plan adds must say so.

</hazards>

<tasks>

<task type="auto">
  <name>Task 1: Retire the nine electron-runtime items in the Phase 38 ledger</name>
  <files>.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md</files>
  <action>
First re-confirm premises 1 and 2 (grep `package.json` for `electron`; grep the ledger for
`id: "38-`, `electron runtime` and `on ELECTRON`). Report what you found. Proceed only if the
set is exactly the nine named in premise 2; otherwise stop and report.

Then make four edits to this one file.

(a) MOVE the nine entries. Cut the complete YAML list entries for `38-S01`, `38-S03`, `38-S05`,
`38-S07`, `38-S09`, `38-S11`, `38-S13`, `38-S15` and `38-C07` out of `human_verification` and
paste them, VERBATIM and in the same relative order, into a NEW top-level frontmatter key
`human_verification_retired:`. Place that key immediately BEFORE the existing
`human_verification_discharged: []` line, as its sibling (same indentation, zero). Do not trim
any field — carry `why_human`, `blocked_by`, `platform_gate`, `prior_state`, `surface_note`,
`unmount_note` and `electron_cutover_risk` across untouched. A trimmed entry is a less auditable
entry, and the whole point of the new array is that the text survives.
`human_verification` must end with exactly 25 entries (verify by
`grep -c '^  - id: "38-'`). `human_verification_retired` must hold exactly 9.

(b) APPEND three fields to each of the nine moved entries, at the end of the entry:
  `retired: 2026-09-23`
  `retired_by: "quick 260923-89z"`
  `retired_reason: '<reason>'`
The reason is the same for all nine except C07's, and must state, in the file's own register:
Phase 35 removed the Electron build; `package.json` carries no `electron` dependency,
devDependency or script (re-verified 2026-09-23), so NO host can execute an electron-runtime row
— this item is UNRUNNABLE EVERYWHERE, not merely deferred. It was NEVER RUN and NO OBSERVATION IS
CLAIMED: this is a retirement, NOT a pass and NOT a discharge, which is why it is not in
`human_verification_discharged`. Its surviving TAURI sibling (name it: S02, S04, S06, S08, S10,
S12, S14, S16 or C08 respectively) is UNAFFECTED and remains the load-bearing observation.
For `38-C07` additionally: this is the return half of its own `electron_cutover_risk` field,
which predicted exactly this disposition — "retired as unscoreable, NOT recorded as a pass and
NOT silently dropped."

(c) REWRITE the `score:` line. It currently begins
`score: N/A — collection phase, no must-haves. 34 relocated items, 0 discharged. (Was 30 until
2026-09-04, ...` and continues with a dated changelog. Change the headline count to
`25 relocated items, 0 discharged, 9 retired.` and insert a NEW dated entry as the FIRST
parenthetical clause, in the same style as the existing ones, preserving the existing
`Was 30 until 2026-09-04...` and `Was 29 until 2026-09-01...` clauses verbatim after it. The new
clause must carry: `Was 34 until 2026-09-23, when quick 260923-89z RETIRED the nine
electron-runtime items` — naming all nine ids — the reason (Phase 35 removed the Electron build;
no `electron` in `package.json`), the explicit statement that this is NOT a discharge and NOT a
pass, and the tool confirmation in the same shape the existing clauses use:
`audit-uat moved 34 -> 25 and 59 -> 50, which is the check that the array still parses — a FLAT
count after a removal would mean the edit did not register or the array failed to parse.`
Keep it a single-line unquoted plain scalar (see H4). **Do not touch `status:`** (H1).

(d) STOP TWO SURVIVORS FROM GOING STALE:
  - `38-S16` (the TAURI half of the G-D20-Q6-COPY pair): add a `pair_note:` field stating that
    `38-S15`, the ELECTRON half, was retired on 2026-09-23 by quick `260923-89z`; that THIS
    item's scope is UNCHANGED — it is still scored on BOTH matrix row 5 (Windows) AND row 7
    (Linux) and therefore still needs BOTH machines; and that S15's retirement does NOT make
    this item Windows-only, because the Linux/row-7 half was never S15's to carry. Leave S16's
    existing `blocked_by` ("machine switch x2 ... BOTH the Windows and the Linux machine")
    exactly as it is — it is still correct and is the field that proves the point.
  - `38-C08`: its `scope_note` currently reads "Run in the same sitting as 38-C01..C06 — one
    controller discharges all eight. Run C07 and C08 back to back on the same game so the two
    runtimes' focus orders are compared under identical library state." Both halves are now
    false. Rewrite it to say a controller discharges all **seven** surviving controller items
    (38-C01..C06 plus this one), and that the C07/C08 back-to-back runtime comparison is no
    longer possible because C07 was retired on 2026-09-23 — so this item now stands alone as the
    ONLY observation of the caret's controller reachability. Leave C08's `not_covered_by_c07`
    field in place: it remains the record of WHY the two were ever split.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md && test "$(grep -c '^  - id: \"38-' "$F")" = 34 && test "$(sed -n '/^human_verification:/,/^human_verification_retired:/p' "$F" | grep -c '^  - id: \"38-')" = 25 && grep -q '^status: human_needed$' "$F" && grep -q '^human_verification_retired:' "$F" && test "$(grep -c 'retired_by: \"quick 260923-89z\"' "$F")" = 9 && echo OK</automated>
  </verify>
  <done>
`human_verification` holds 25 entries and none of their `test:` strings contains `electron
runtime` or `on ELECTRON`; `human_verification_retired` holds the 9 verbatim entries, each with
`retired`, `retired_by` and `retired_reason`; `score:` reads 25/0/9 with a dated 2026-09-23
clause; `status:` is still `human_needed`; `38-S16` carries the pair note and `38-C08`'s
scope_note no longer says "all eight".
  </done>
</task>

<task type="auto">
  <name>Task 2: Close the two-way receipts in 34.13-UAT.md</name>
  <files>.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md</files>
  <action>
Phase 38's `relocation_rules` (3) makes relocation two-way, and its "How to close an item" step 4
says the origin's receipt must be walked back so it does not rot. All nine retired items have a
receipt in THIS file, in the frontmatter `human_verification_relocated:` array (17 entries in
total: `38-C07`, `38-C08`, `38-S01`..`38-S16`). Nine of them currently read
`outcome: "open — not yet run in phase 38"` while pointing at an item that no longer exists in
`human_verification`. That is exactly the rot the rule exists to prevent.

Three edits, all in this one file. **Obey H5: touch nothing else.**

(a) For the NINE receipts whose `to_item:` is `38-S01`, `38-S03`, `38-S05`, `38-S07`, `38-S09`,
`38-S11`, `38-S13`, `38-S15` or `38-C07`, replace the `outcome:` value with a return half that
records: RETIRED 2026-09-23 by quick 260923-89z; never run and never observed; now unrunnable on
every host because Phase 35 removed the Electron build (no `electron` in `package.json`); moved
out of `38-VERIFICATION.md`'s `human_verification` into its `human_verification_retired` array;
and explicitly NOT a pass and NOT a discharge. Keep the existing double-quoted YAML scalar style
and do not introduce a raw `"` inside the value.
**Leave the other eight receipts (`38-C08`, `38-S02`, `38-S04`, `38-S06`, `38-S08`, `38-S10`,
`38-S12`, `38-S14`, `38-S16`) reading `open` — they are still open, and flipping them would be
the same false-closure this rule guards against.**

(b) Extend the `relocation_note:` scalar. It currently directs a reader to
`38-VERIFICATION.md`'s `human_verification_discharged` array "which is where a discharged item is
MOVED to". Add a sentence naming `human_verification_retired` as the OTHER destination an item
can leave `human_verification` for, and stating the difference plainly: discharged means it was
run and observed; retired means it can never be run at all and NOTHING was observed.

(c) The markdown result tables further down carry nine rows whose status column reads
`RELOCATED` and whose fourth column says "Moved to Phase 38 as `38-SNN`/`38-C07`...". Those are
the rows for `G-QUICK-WIN / electron`, `G-QUICK-LINUX / electron`, `G-ROW-5 / electron`,
`G-ROW-6 / electron`, `G-ROW-7 / electron`, `G-ROW-8 / electron`, `G-D20-CONTENTLIGHT /
electron`, `G-D20-Q6-COPY / electron` and `G-GAMEPAD-CARET / electron` — find them by their
`| <ROW-ID> | electron | RELOCATED |` prefix, not by line number. For each:
  - change the third column from `RELOCATED` to `RELOCATED → RETIRED 2026-09-23`
  - append to the END of the fourth column's existing text (before the trailing ` | |`), leaving
    the existing text intact, one short bolded sentence recording the retirement: the Electron
    build was removed in Phase 35 so this row is unrunnable on every host; see
    `38-VERIFICATION.md`'s `human_verification_retired`; not a pass.
Do not touch the `tauri` sibling row of any pair — those remain `RELOCATED` and open.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md && test "$(grep -c '260923-89z' "$F")" -ge 10 && test "$(grep -c 'outcome: "open — not yet run in phase 38"' "$F")" = 8 && test "$(grep -c 'RELOCATED → RETIRED 2026-09-23' "$F")" = 9 && test "$(grep -c '| tauri | RELOCATED |' "$F")" = 9 && grep -q 'human_verification_retired' "$F" && echo OK</automated>
  </verify>
  <done>
Nine receipts carry a dated retirement return half naming quick `260923-89z`; the other eight
still read `open`; `relocation_note` distinguishes retired from discharged; nine electron table
rows read `RELOCATED → RETIRED 2026-09-23` and their tauri siblings are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the count text, then prove the numbers moved and commit</name>
  <files>.planning/ROADMAP.md, .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md</files>
  <action>
(a) `.planning/ROADMAP.md`, in the `### Phase 38:` section, carries a line beginning
`**Items: 34 as of 2026-09-04.**` (around line 4648). It is the only live count citation outside
`38-VERIFICATION.md` — the other count line in that section, `**Items: 29 as of 2026-09-01**`, is
already explicitly marked "(historical, superseded by the 2026-09-04 count above)" and must be
left alone. Update the live line to `**Items: 25 as of 2026-09-23.**`, add a short dated
paragraph in the section's existing voice recording the retirement of the nine electron-runtime
items by quick `260923-89z`, naming all nine ids, giving the reason (Phase 35 removed the
Electron build; `package.json` carries no `electron` dependency or script), stating it is NOT a
pass and NOT a discharge, pointing at `38-VERIFICATION.md`'s `human_verification_retired`, and
demoting the 2026-09-04 count to historical the same way the 2026-09-01 one was. Include the
S15/S16 consequence in one clause: `38-S16` survives and is still scored on BOTH Windows row 5
and Linux row 7, so it still needs BOTH machines. Do not touch the `38-E03`/`38-E04` return-half
block or the `⚠ PREMISE CORRECTED` block.

(b) `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md`
— DECISION, stated rather than assumed: this file DOES need a small edit, but not a count fix.
Its `## Scope` section names only `38-W01` and `38-C01`..`38-C05`, so none of the nine retired
items appears anywhere in it; its counts are not derived from the retired set. Add a short
`## Retired` note after `## Scope` recording: nine electron-runtime items retired 2026-09-23 by
quick `260923-89z`; Phase 35 removed the Electron build so no host can run them; a Windows or
Linux operator planning a sitting should NOT look for electron rows; `38-S16` still needs BOTH
machines; nothing was observed, so this is not a pass.
**Explicitly do NOT fix the `## Current Test` line reading "6 items seeded, 0 discharged".** It
was already stale before this change (the ledger held 34, not 6) for an unrelated reason, this
change does not make it more or less wrong, and silently correcting it inside a retirement commit
would bury a pre-existing defect. Instead, state in the new `## Retired` note, in one sentence,
that the "6 items seeded" line is known-stale and that `38-VERIFICATION.md` is the authoritative
count — which the file's own blockquote already says.

(c) VERIFY THE NUMBERS MOVED, then commit. Run, from the repo root:
  - `python meta/runPlanningGates.py` — expect `12/12 planning gates passed`.
    (`pnpm planning-gates` will fail on this host: its `python3` is the Microsoft Store stub.
    Use `python`. Do NOT edit `package.json`, and do NOT widen or edit any gate — CLAUDE.md is
    explicit. A red gate means fix the document.)
  - `gsd-sdk query audit-uat` and read the `summary` block.
    **Required, and a FLAT count is a FAILURE, not a pass:**
      `by_phase["38"]`     34 → **25**
      `total_items`        59 → **50**
      `by_phase["34.13"]`   7 → **7** (unchanged)
      `total_files`         8 → **8** (unchanged; phase 38 must still appear)
    If `by_phase["38"]` is still 34, the entries did not leave the array. If phase 38 is ABSENT
    from `by_phase`, or reports 0, the YAML broke or `status:` was disturbed — revert and retry
    (H1/H4). If `by_phase["34.13"]` moved, Task 2 damaged a UAT item shape (H5).
  - Confirm by prose, never by position (H3): none of the emitted phase-38 `name` strings
    contains `electron runtime` or `on ELECTRON`, and the eight surviving tauri `test:` strings
    (`38-S02`, `S04`, `S06`, `S08`, `S10`, `S12`, `S14`, `S16`) plus `38-C08` are all still
    present.
Then commit all four files in ONE atomic commit:
`docs(quick-260923-89z): retire the nine Phase 38 electron-runtime UAT items`
with a body recording the 34 → 25 / 59 → 50 measurement and that this is a retirement, not a
discharge. End the message with the attribution line
`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python meta/runPlanningGates.py 2>&1 | tail -2 | grep -q '12/12 planning gates passed' && gsd-sdk query audit-uat 2>/dev/null | python -c "import json,sys; d=json.load(sys.stdin); s=d['summary']; assert s['by_phase']['38']==25, s['by_phase']; assert s['total_items']==50, s['total_items']; assert s['by_phase']['34.13']==7, s['by_phase']; p=[r for r in d['results'] if r['phase']=='38'][0]; bad=[i['name'] for i in p['items'] if 'electron runtime' in i['name'] or 'on ELECTRON' in i['name']]; assert not bad, bad; print('OK 38=25 total=50 34.13=7 no-electron-rows')"</automated>
  </verify>
  <done>
`12/12 planning gates passed` with `python meta/runPlanningGates.py`; audit-uat reports
`by_phase["38"] == 25`, `total_items == 50`, `by_phase["34.13"] == 7`; no emitted phase-38 item
names an electron runtime; ROADMAP's live count reads 25 as of 2026-09-23; `38-HUMAN-UAT.md`
carries the retirement note; all four files committed in one commit.
  </done>
</task>

</tasks>

<verification>

The load-bearing check is the audit delta, because there is no local YAML parser (premise 9) and
the file's own `score:` line already establishes that a count which fails to move is the signal
that an edit silently did nothing:

| measurement | before | after | what a flat/wrong value means |
|---|---|---|---|
| `by_phase["38"]` | 34 | **25** | still 34 → entries never left the array (H2). 0 or absent → YAML broke or `status:` disturbed (H1/H4). |
| `total_items` | 59 | **50** | any other value → a different file's items changed too |
| `by_phase["34.13"]` | 7 | **7** | moved → Task 2 damaged a UAT item shape (H5) |
| `total_files` | 8 | **8** | 7 → phase 38 dropped out of the audit entirely |
| `python meta/runPlanningGates.py` | 12/12 | **12/12** | red → fix the document, never the gate |

Plus, by prose and never by position (H3): the nine retired `test:` strings must be absent from
audit output, and all 25 survivors present.

</verification>

<success_criteria>

- `38-VERIFICATION.md`: `human_verification` holds 25 entries, `human_verification_retired` holds
  the 9 verbatim entries each with `retired: 2026-09-23`, `retired_by: "quick 260923-89z"` and a
  `retired_reason`, `status:` is untouched at `human_needed`, and `score:` reads 25/0/9 with a
  dated 2026-09-23 changelog clause carrying the 34 → 25 / 59 → 50 confirmation.
- `38-S16` records that its scope is unchanged and it still needs BOTH machines; `38-C08`'s
  scope_note no longer claims a controller discharges eight items.
- `34.13-UAT.md`: nine receipts walked back with a dated return half; eight still open; nine
  table rows read `RELOCATED → RETIRED 2026-09-23`; no UAT item shape touched.
- `ROADMAP.md` live count reads 25 as of 2026-09-23, with the old 34 demoted to historical.
- `38-HUMAN-UAT.md` carries a `## Retired` note, and the pre-existing "6 items seeded" staleness
  is named rather than silently fixed.
- `audit-uat` and `python meta/runPlanningGates.py` report the numbers in `<verification>`.
- One atomic commit across all four files.

</success_criteria>

<output>
Create `.planning/quick/260923-89z-retire-phase-38-electron-runtime-items/260923-89z-SUMMARY.md`
when done. Record in it: the re-derived electron set (and whether it matched the nine), the
before/after audit numbers actually observed, the 34.1→34.13 origin correction, and the
`38-HUMAN-UAT.md` "6 items seeded" staleness left deliberately unfixed.
</output>
