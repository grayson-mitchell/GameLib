---
phase: quick-260820-kq0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/screens/Login/components/SteamLogin/index.tsx
  - src/frontend/screens/Login/components/SteamLogin/index.scss
  - src/frontend/screens/Login/components/SteamLogin/__tests__/steamLoginWindowChrome.test.ts
  - src/frontend/components/UI/Dialog/components/Dialog.tsx
  - src/frontend/components/UI/Dialog/__tests__/dialogWindowChrome.test.ts
autonomous: false
requirements: [QUICK-260820-KQ0]

must_haves:
  truths:
    - "Clicking the Steam tile on the Login screen presents the Steam sign-in surface as a floating modal window centred over the app, with a dimmed backdrop over the whole app, exactly as the Humble/GOG/Amazon sign-in window presents over the app — NOT as a bare 480px box pinned to the top-left of the content area."
    - "The Steam sign-in window carries a title (`Sign in to Steam`) and a close control in its own header, and is dismissible by that close control, by Escape, and by backdrop click."
    - "Dismissing the window returns the user to `/login` with every tile intact; the Steam tile shows `Connected` after a successful sign-in, unchanged from today."
    - "All THREE of SteamLogin's top-level render branches (`not-installed`, `checking`, and the main tabs view) render inside the SAME single window shell — the window does not appear, disappear, or replay its entrance transition as `step` changes."
    - "`window.api.steamStartQR`, `steamPollCredential`, `steamStartCredentials`, `steamSubmitGuard`, `getSteamUserInfo` and `steam.login(...)` are called from exactly the same places and with exactly the same arguments as at HEAD — no auth, token, or credential-storage behaviour changes."
    - "`src/frontend/screens/Login/index.tsx`, `src/frontend/screens/Login/components/Runner/index.tsx` and `src/frontend/App.tsx` are byte-for-byte identical to HEAD, so `loginInFlightUiReachability.test.tsx`'s five source gates and `navTabs.test.ts`'s `/loginweb/steam` -> `accounts` mapping stay green untouched."
  artifacts:
    - path: "src/frontend/screens/Login/components/SteamLogin/index.tsx"
      provides: "Steam sign-in rendered inside the shared Dialog window shell, single top-level return"
      contains: "from 'frontend/components/UI/Dialog'"
    - path: "src/frontend/screens/Login/components/SteamLogin/index.scss"
      provides: "Panel styling adjusted for life inside the dialog paper (no duplicated chrome)"
    - path: "src/frontend/screens/Login/components/SteamLogin/__tests__/steamLoginWindowChrome.test.ts"
      provides: "Comment-stripped source gate pinning the single-Dialog window shell + the three-branch consolidation"
      contains: "stripSourceComments"
  key_links:
    - from: "src/frontend/screens/Login/components/SteamLogin/index.tsx"
      to: "src/frontend/components/UI/Dialog/components/Dialog.tsx"
      via: "named import of Dialog/DialogHeader from the UI barrel"
      pattern: "import \\{[^}]*Dialog[^}]*\\} from 'frontend/components/UI/Dialog'"
    - from: "src/frontend/screens/Login/components/SteamLogin/index.tsx"
      to: "react-router-dom navigate('/login')"
      via: "closeWindow dismissal handler passed to Dialog onClose"
      pattern: "onClose=\\{closeWindow\\}"
---

<objective>
Give the Steam sign-in surface the same windowed presentation the other stores' sign-in
surfaces have: a floating panel centred over the app with a dimmed backdrop, a titled header,
and a close control — instead of today's bare `.steamLoginPanel` box that renders flush in the
content area.

Purpose: the user's report is that Steam's login does not present like Humble's. Humble (and
every other runner) opens a real sign-in WINDOW over the app; Steam renders an unadorned page.
This closes the presentation gap using the app's own shared modal primitive.
Output: `SteamLogin` renders inside `frontend/components/UI/Dialog`, plus a source gate pinning
that shape, plus a human visual confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/frontend/screens/Login/components/SteamLogin/index.tsx
@src/frontend/screens/Login/components/SteamLogin/index.scss
@src/frontend/components/UI/Dialog/components/Dialog.tsx
@src/frontend/components/UI/Dialog/components/DialogHeader.tsx
@src/frontend/screens/WineManager/components/WineManagerSettingsModal.tsx
@src/frontend/screens/Login/__tests__/loginInFlightUiReachability.test.tsx

<findings>
<!-- Verified by direct source read during planning. Executor should NOT re-derive these. -->

**1. Steam has NO native login window, and cannot have one.**
`grep -n "steam" src/frontend/screens/WebView/useTauriOAuthLogin.ts src/frontend/screens/WebView/loginRoutes.ts`
returns nothing, and `src/backend/sidecar/steamAuthFlowRegistration.ts` registers no window arm.
Steam authenticates entirely in-process via `steam-session` behind `window.api.steamStartQR` /
`steamStartCredentials` / `steamSubmitGuard` / `steamPollCredential` — there is no URL to load
into a webview.

Routing Steam through Humble's native mechanism (`"humble_login_open"` arm,
`src-tauri/src/main.rs:3534`) is STRUCTURALLY BLOCKED, not merely inconvenient: login windows
are deliberately excluded from the Tauri capability scope. See `src-tauri/src/main.rs:967`
("never join `capabilities/default.json`'s `windows: ["main"]` array") and `:5624`. A login
window therefore has NO `window.api` / `invoke` access at all, so the Steam form could not make
a single one of its five backend calls from inside one. Widening that scope to accommodate a
presentation change would be a security regression and is explicitly out of scope here.

This plan therefore delivers the in-app equivalent — the shared modal window primitive — which
is the closest achievable presentation parity.

**2. Do NOT convert the Steam tile into an overlay on the Login screen.**
`src/frontend/screens/Login/__tests__/loginInFlightUiReachability.test.tsx` is a threat-pinned
source gate (T-34.4.2-39 / T-34.4.2-41, mutation-proven) whose documented mechanism is: clicking
a login tile NAVIGATES AWAY from the Login screen, unmounting all six tiles at once. Giving the
Steam tile a `primaryLoginAction` that opens an in-place overlay would falsify that gate's own
prose and weaken the pin. Keep the existing `loginUrl={steamLoginPath}` navigation exactly as it
is. `src/frontend/screens/Login/index.tsx`, `Runner/index.tsx` and `App.tsx` are NOT touched by
this plan.

Because the `/loginweb/steam` route renders inside `Root`'s `<Outlet/>` while the app shell
(nav/titlebar) stays mounted, and because MUI's `Dialog` portals to `document.body`, the dialog
floats over the entire app with a full-viewport backdrop — visually a sign-in window over the
app, which is the requested outcome.

**3. Dialog primitive contract (read, not assumed).**
`src/frontend/components/UI/Dialog/components/Dialog.tsx` exports
`Dialog({ className?, children, showCloseButton, onClose })`. It renders a MUI `Dialog`
(portal + backdrop + Escape + focus trap), an absolutely-positioned close `IconButton` gated by
`showCloseButton`, and wraps `children` in MUI's own `DialogContent`. `DialogHeader({ children })`
renders a MUI `DialogTitle`. Backdrop-click close is already honoured unless
`disableDialogBackdropClose` is set in `ContextProvider`.

Import shape used by existing callers (`WineManager/components/WineManagerSettingsModal.tsx:3-6`,
`WebView/index.tsx:34-37`):
`import { Dialog, DialogHeader } from 'frontend/components/UI/Dialog'`

`Dialog` holds its own `open` state initialised to `true` and mounts with a 500ms entrance
transition plus a `window.api.gamepadAction({ action: 'tab' })` focus hack. It must therefore be
mounted ONCE and kept mounted — see Task 1's single-return requirement.

**4. SteamLogin's three top-level render branches (line numbers at HEAD).**
- `:272` `if (step === 'not-installed')` -> returns `<div className="steamLoginPanel">`
- `:321` `if (step === 'checking')` -> returns `<div className="steamLoginPanel">`
- `:575` main tabs view -> returns `<div className="steamLoginPanel">`
`renderQRContent` (`:330`) and `renderCredentialsContent` (`:407`) are inner helpers, not
top-level returns. All five `navigate('/login')` call sites are at `:94`, `:140`, `:226`, `:253`,
`:310`, `:581`.

**5. Timer teardown already exists.** The mount effect with `[]` deps owns interval/timer
cleanup on unmount (stated in the comment at `:196-205`), and `navigate('/login')` unmounts the
route component. A new dismissal route (Escape / backdrop / header close) therefore needs NO
extra teardown beyond navigating — do not add speculative clears.

**6. Test environment has NO DOM.** `src/frontend/jest.config.js` is `testEnvironment: 'node'`;
jsdom and react-test-renderer are not installed. The established convention in this area is a
comment-stripped SOURCE GATE via `stripSourceComments` from `backend/testUtils/stripSourceComments`
(see `loginInFlightUiReachability.test.tsx`). A render test is not available — which is why
Task 3 is a human visual gate, not a substitute assertion.

**7. Localisation.** `SteamLogin/index.tsx` contains NO `t()` calls at all today; every string is
a raw English literal. This plan MOVES the existing `Sign in to Steam` literal from the `<h1>` at
`:582-593` into the dialog header. It introduces ZERO new user-facing strings and therefore adds
no new i18n keys. Do not opportunistically localise this file here — that is its own task.

**8. Pre-existing, out of scope.** `.tabsWrapper` (`SteamLogin/index.tsx:600`) has no matching
CSS rule reachable from this component (`.tabsWrapper` is styled only inside
`src/frontend/screens/WineManager/index.css:57`). That is already true at HEAD. Do not fix it
here; just confirm in Task 3 that the tabs remain legible inside the dialog.
</findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render SteamLogin inside the shared Dialog window shell</name>
  <files>src/frontend/screens/Login/components/SteamLogin/index.tsx, src/frontend/screens/Login/components/SteamLogin/index.scss</files>
  <action>
Restructure `SteamLogin` so it has exactly ONE top-level `return`, which renders the shared
`Dialog` window shell, with the current three branches selected INSIDE it.

Concretely:

(a) Add `import { Dialog, DialogHeader } from 'frontend/components/UI/Dialog'` alongside the
existing imports, matching the import path used by `WineManagerSettingsModal.tsx`.

(b) Add a single dismissal handler in the component body:
`const closeWindow = () => navigate('/login')`.
Route EVERY dismissal through it. Replace the `navigate('/login')` at the "Back to Login"
button (`:581`) and at the not-installed "Return to Login" button (`:310`) with `closeWindow`.
Leave the four success-path `navigate('/login')` calls (`:94`, `:140`, `:226`, `:253`) exactly
as they are — those are sign-in completions, not dismissals, and rewriting them would blur the
two. Do NOT add timer clears to `closeWindow`; finding 5 explains why the mount effect already
owns teardown.

(c) Convert the two early returns into a body-selecting helper. Introduce
`function renderWindowBody()` that returns the `not-installed` tree when `step === 'not-installed'`,
the spinner tree when `step === 'checking'`, and otherwise the current main tabs content
(the `<Tabs>` block plus the two `<TabPanel>`s). Strip the outer `<div className="steamLoginPanel">`
wrapper from each of the three branches — the dialog paper is now the panel. Delete the `<h1>`
"Sign in to Steam" heading (`:582-593`) and the "Back to Login" button (`:576-586`) from the main
branch: the header title and the close control replace both, and leaving them would give the
window two competing titles and two competing close affordances.

(d) The single top-level return becomes:
`<Dialog showCloseButton={true} onClose={closeWindow} className="steamLoginDialog">` containing
`<DialogHeader onClose={closeWindow}>Sign in to Steam</DialogHeader>` followed by
`<div className="steamLoginBody">{renderWindowBody()}</div>`.
Mount `Dialog` unconditionally — it must NOT be re-created when `step` changes, or the 500ms
entrance transition and the `gamepadAction` focus hack replay on every step transition
(finding 3).

(e) In `index.scss`, retire the `.steamLoginPanel` rule (the dialog paper now supplies the
background, radius and padding — keeping it would double the chrome) and replace it with a
`.steamLoginBody` rule carrying only what is still needed: `display: flex`,
`flex-direction: column`, `gap: var(--space-md)`, `width: min(480px, 95%)`. Leave
`.steamNotFound`, `.steamError`, `.steamQrContainer` and `.sid-input` untouched.

Do not touch `Login/index.tsx`, `Runner/index.tsx`, or `App.tsx` (finding 2). Do not change any
`window.api.steam*` call, argument, or ordering.
  </action>
  <verify>
    <automated>pnpm codecheck &amp;&amp; pnpm exec eslint src/frontend/screens/Login/components/SteamLogin</automated>
  </verify>
  <done>`tsc --noEmit` and eslint are clean; `SteamLogin/index.tsx` has exactly one top-level `return` and exactly one `<Dialog` occurrence; `grep -c steamLoginPanel` over `index.tsx` and `index.scss` returns 0 for both.</done>
</task>

<task type="auto">
  <name>Task 2: Source gate pinning the single-window shell, RED-proven</name>
  <files>src/frontend/screens/Login/components/SteamLogin/__tests__/steamLoginWindowChrome.test.ts</files>
  <action>
Create a comment-stripped source gate in the established style of
`src/frontend/screens/Login/__tests__/loginInFlightUiReachability.test.tsx`. Read the file with
`readFileSync` and pass it through `stripSourceComments` from `backend/testUtils/stripSourceComments`
— never assert against raw source, and never count occurrences over unstripped text (a comment
mentioning `steamLoginPanel` would otherwise self-invalidate the gate).

State in the file's own header docblock that this is a SOURCE GATE, not a render test, and that
`src/frontend/jest.config.js` is `testEnvironment: 'node'` so no DOM assertion is available
(finding 6).

Assertions, each labelled PRESENCE or ABSENCE:

1. PRESENCE — the component imports the shared window primitive:
   the stripped source matches `/import \{[^}]*\bDialog\b[^}]*\} from 'frontend\/components\/UI\/Dialog'/`
   and also contains `DialogHeader`.
2. PRESENCE + uniqueness — the window is mounted exactly once:
   `(source.match(/<Dialog[\s>]/g) ?? []).length === 1`. This is the assertion that fails if a
   future edit re-splits the component into per-`step` dialogs and reintroduces the
   entrance-transition replay.
3. PRESENCE — the dialog is dismissible and its dismissal is the single `closeWindow` handler:
   source matches `/showCloseButton=\{true\}/`, matches `/onClose=\{closeWindow\}/`, and
   `const closeWindow = () => navigate('/login')` is present.
4. ABSENCE — the old bare panel root is gone: `source` contains zero occurrences of
   `steamLoginPanel`. Read `index.scss` too and assert zero there as well.
5. PRESENCE — all three branches funnel through one body renderer:
   `function renderWindowBody()` is present, and the stripped source contains exactly one
   top-level `<Dialog` return by asserting `source.indexOf('<Dialog') > source.indexOf('function renderWindowBody()')`
   is FALSE-safe — instead assert simply that `renderWindowBody(` appears exactly twice
   (declaration + single call site).

FALSIFIABILITY (mandatory, and recorded in the SUMMARY): before committing, prove each assertion
RED by temporarily mutating `SteamLogin/index.tsx` — for assertion 2 duplicate the `<Dialog`
element; for assertion 4 rename `.steamLoginBody` back to `.steamLoginPanel`; for assertion 3
replace `onClose={closeWindow}` with an inline arrow. Confirm the specific test FAILS for each
mutation, then `git diff --quiet src/frontend/screens/Login/components/SteamLogin/index.tsx`
before moving to the next mutation. A gate that has not been shown to fail against a known-bad
input is not a gate.

Additionally assert the FILLED-specimen guard: the RAW (unstripped) `index.tsx` must contain
`Dialog` — so a broken comment stripper turns this gate red rather than vacuous.
  </action>
  <verify>
    <automated>pnpm test src/frontend/screens/Login/components/SteamLogin/__tests__/steamLoginWindowChrome.test.ts &amp;&amp; pnpm test src/frontend/screens/Login/__tests__/loginInFlightUiReachability.test.tsx src/frontend/components/UI/NavShell/__tests__/navTabs.test.ts</automated>
  </verify>
  <done>The new gate passes; every assertion has a recorded RED proof; `loginInFlightUiReachability.test.tsx` (5 gates) and `navTabs.test.ts` still pass unmodified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual confirmation of the windowed presentation</name>
  <action>Blocking human gate. No DOM test environment exists in this repo (finding 6), so the appearance IS the deliverable and only a human can confirm it. Follow the steps in `<how-to-verify>` and report against each named property.</action>
  <what-built>
The Steam sign-in surface now renders inside the app's shared modal window primitive
(`frontend/components/UI/Dialog`) — centred, backdropped, titled, with a close control — instead
of the bare `.steamLoginPanel` box. Routing, tile behaviour and every Steam auth call are
unchanged. A source gate pins the single-window shape; it cannot see what actually renders,
which is why this step exists.
  </what-built>
  <how-to-verify>
Run `pnpm tauri:dev` (NOT `tauri dev` — that serves a stale static bundle).

1. Navigate to the Login screen. Click the **Humble Bundle Login** tile and note how its sign-in
   window presents: where it sits relative to the app, whether the app behind it is dimmed.
   Dismiss it and return to `/login`.
2. Click the **Steam Login** tile. CONFIRM, naming each property directly rather than by
   landmark:
   a. The Steam panel is HORIZONTALLY AND VERTICALLY CENTRED in the app window (not flush to the
      top-left of the content area as before).
   b. The rest of the app behind it is DIMMED by a backdrop that covers the full viewport.
   c. A header reading `Sign in to Steam` is present, with a close (X) control at its top-right.
   d. There is exactly ONE title and exactly ONE close affordance — no leftover `<h1>` and no
      leftover "Back to Login" button.
3. Switch between the **QR Code** and **Username & Password** tabs. CONFIRM the window does NOT
   flash, re-slide-in, or re-centre on the tab switch, and that the QR code and both form fields
   are fully visible and legible inside the panel (not clipped by the dialog's `max-height: 95vh`
   / `overflow: auto`).
4. Dismiss the window three ways in turn, re-opening from the Steam tile each time: the header X,
   the Escape key, and a click on the dimmed backdrop. CONFIRM each returns you to the Login
   screen with all six tiles present.
5. Complete a real Steam sign-in (QR or credentials). CONFIRM the window closes on success and
   the Steam tile shows `Connected`.

If the Steam tile already shows `Connected`, log out from it first so the sign-in path is
actually reachable.
  </how-to-verify>
  <resume-signal>Type "approved", or describe which of 2a-2d / 3 / 4 / 5 failed and what you saw instead.</resume-signal>
</task>

<!--
ROUND 2 RESULT (recorded, not rewritten): Task 3 above was RUN and returned a FAIL —
"steam window has sharp corners, humble rounded" and "steam window opens instantly" (no
transition). Root cause: `.Dialog__element` (Dialog/index.css) intended a 10px radius and a
500ms entrance transition for every Dialog, but is dead CSS -- no element in the MUI-based
Dialog primitive ever carries that class, so every dialog in the app fell back to MUI's
default 4px radius and default ~225ms Fade. A round-2 attempt scoped the radius fix to
SteamLogin only (a `.MuiDialog-paper.steamLoginDialog` compound selector); the operator then
overrode that scope decision ("fix the primitive properly for all of them"), so that
Steam-only rule was removed and Task 4 below fixes the primitive itself instead. Task 3's
steps above are SUPERSEDED by Task 5's expanded multi-dialog gate -- do not re-run Task 3
verbatim; use Task 5.
-->

<task type="auto">
  <name>Task 4: Fix the corner radius and entrance transition in the shared Dialog primitive (not just SteamLogin)</name>
  <files>src/frontend/components/UI/Dialog/components/Dialog.tsx, src/frontend/components/UI/Dialog/__tests__/dialogWindowChrome.test.ts</files>
  <action>
Fix the dead-CSS defect (see the ROUND 2 RESULT note above) at its source, `Dialog.tsx`, so
all 25 consumers of `frontend/components/UI/Dialog` inherit rounded corners and a perceptible
entrance transition, not just `SteamLogin`.

(a) Add `borderRadius: '10px'` to `StyledPaper` (the existing `styled(Paper)(...)` override
already used for `backgroundColor` — the same MUI-supported component-level override
mechanism, not a competing external stylesheet racing MUI's own emotion-injected paper class).

(b) Add a directional entrance transition to `MuiDialog`: a `forwardRef` wrapper
(`SlideUpTransition`) rendering `<Slide direction="up" ref={ref} {...props} />` (MUI's own
`Slide`, imported from `@mui/material`), wired via `TransitionComponent={SlideUpTransition}`
and `transitionDuration={500}` — matching the dead rule's translateY-from-below intent and
500ms duration. Do NOT apply `.Dialog__element` directly to the Paper/root as a className: its
visibility in `index.css` is gated on `:popover-open`/`[open]`, pseudo-states a rendered
element can never match, which would make the dialog permanently invisible. Do NOT add a
`BackdropComponent`/`BackdropProps` — MUI already renders exactly one `.MuiBackdrop-root`;
adding another would double the dimming layer.

(c) The existing dead `sx={{ '& .Dialog__element': { maxWidth, paddingTop } }}` prop on
`MuiDialog` targets a class nothing in this tree carries — it is unreachable. DROP it (do not
revive it): `maxWidth="md"` on `MuiDialog` and `StyledPaper`'s own `maxWidth: '100%'` already
constrain width, and reviving unreviewed sizing here would be an undiscussed side effect
across all 25 consumers, out of scope for a corners/animation fix. Document this drop decision
inline as a comment.

(d) Preserve the gamepad navigation hooks: `.MuiDialog-paper` (`gamepad.ts:316`) and
`.MuiDialog-root` (`gamepad.ts:370`, `:377`) must still be the classes MUI renders (unaffected
by a `PaperComponent`/`TransitionComponent` swap — confirm, don't assume). The mount-time
`window.api.gamepadAction({ action: 'tab' })` hack (`Dialog.tsx:46-53` at HEAD) fires in a
`useEffect` on React mount, independent of the transition component's own enter timing — it
already fired before the transition's `onEntered` callback under the old default `Fade`, and
continues to under `Slide`; do not attempt to gate it on transition completion.

(e) Confirm (read, don't assume) that `themes.scss:388`'s per-theme
`.MuiDialog-paper { background-color: var(--background-light) }` rule still applies to the
themed paper — it targets the same class name regardless of which `PaperComponent`/transition
is used, so it is unaffected by this change.

Extend `src/frontend/components/UI/Dialog/__tests__/dialogWindowChrome.test.ts` (new file) with
a comment-stripped SOURCE GATE, in the same style and with the same limitations as
`steamLoginWindowChrome.test.ts` (a `testEnvironment: 'node'` project — this can only prove the
radius/transition wiring exists in source text, never that the cascade or a render outcome
occurs). State that limitation in the file's own docblock. RED-prove every assertion by
temporary mutation + restore before committing (recorded in the SUMMARY, with the mutated file
confirmed byte-identical restored, not merely `git diff --quiet`-clean, since this file may
carry other uncommitted work at mutation time).

Remove the now-obsolete round-2 SCSS-scoped assertion from `steamLoginWindowChrome.test.ts`
(and the round-2 `.MuiDialog-paper.steamLoginDialog` rule from `SteamLogin/index.scss`, if
still present) — the fix moved to the primitive, so a Steam-only compound selector is no
longer part of the shape being pinned.
  </action>
  <verify>
    <automated>pnpm codecheck &amp;&amp; pnpm exec eslint src/frontend/components/UI/Dialog &amp;&amp; pnpm exec jest --config src/frontend/jest.config.js</automated>
  </verify>
  <done>`tsc --noEmit` and eslint are clean (0 errors); the full frontend jest suite passes (all suites, not just the two touched by this plan — the primitive change reaches 25 consumers); `dialogWindowChrome.test.ts`'s assertions are each independently RED-proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human visual confirmation of the primitive fix, across multiple dialogs (SUPERSEDES Task 3)</name>
  <action>Blocking human gate. Task 3 above already ran once and FAILED (sharp corners, instant
open) against a since-abandoned Steam-only fix. This gate re-verifies the SAME properties, but
now against the primitive-level fix, and widens the check beyond Steam's login window to at
least two other dialogs, since the fix now reaches all 25 consumers. Follow
`<how-to-verify>` and report against each named property. Do NOT self-certify this gate.</action>
  <what-built>
The shared `Dialog` primitive (`frontend/components/UI/Dialog/components/Dialog.tsx`) now sets
a 10px paper corner radius and a 500ms upward `Slide` entrance transition, replacing dead CSS
that never applied to any real element. This reaches every one of the 25 files that import from
`frontend/components/UI/Dialog` (see SUMMARY.md for the full census), not just `SteamLogin`.
  </what-built>
  <how-to-verify>
Run `pnpm tauri:dev` (NOT `tauri dev` — that serves a stale static bundle).

1. Navigate to the Login screen. Click the **Humble Bundle Login** tile as a reference point
   for corner radius, backdrop darkness, and entrance feel. Dismiss it.
2. Click the **Steam Login** tile. CONFIRM, naming each property directly:
   a. The dialog's corners are ROUNDED (not sharp) — comparable to Humble's.
   b. Opening the dialog shows a PERCEPTIBLE entrance animation (it slides/animates in — it does
      NOT snap open instantly).
   c. The rest of the app is dimmed by exactly ONE backdrop, at the expected darkness (not a
      double-dimmed or missing backdrop).
   d. The dialog is fully visible — not invisible, not clipped, not stuck off-screen.
   e. Focus lands correctly on open (Tab/gamepad navigation reaches the dialog's controls; the
      close button is reachable).
   Dismiss it.
3. Open an **install-flow dialog** (e.g. click Install on any game in the Library to open the
   Install modal, or a nested dialog inside it such as the branch/platform selector). Repeat the
   same checks (a-e) against it. Dismiss it.
4. Open the **Settings dialog** (e.g. from a game's settings, or the app-wide Settings screen —
   whichever is reachable). Repeat the same checks (a-e) against it. Dismiss it.
5. CONFIRM none of the three dialogs regressed in size/scroll behaviour — content that was
   visible before (form fields, tabs, buttons) is still visible and not clipped.

If any of steps 2-4 fail on ANY named property (a-e), report exactly which dialog, which
property, and what you saw instead — do not average across dialogs.
  </how-to-verify>
  <resume-signal>Type "approved", or describe which dialog and which of a-e failed and what you saw instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> Steam sign-in form | Steam username, password and Steam Guard code are typed into this surface |
| renderer -> sidecar (`window.api.steam*`) | Credentials cross into the Node sidecar and on to Steam's CM network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-KQ0-01 | Spoofing | `SteamLogin` window presentation | mitigate | The window is rendered by the app's OWN `Dialog` primitive inside the main webview — it is NOT a separate OS window and cannot be confused with one from another origin. The plan explicitly refuses to widen `capabilities/default.json`'s `windows: ["main"]` scope (finding 1), so no new IPC-capable surface is created. |
| T-KQ0-02 | Information disclosure | password / Steam Guard inputs | accept | `type="password"` and the existing input handling are untouched; no field is moved into a new process or a new logging path. No `window.api.logInfo` call is added to this component by this plan. |
| T-KQ0-03 | Elevation of privilege | login-in-flight tile reachability (T-34.4.2-39 / -41 UI pin) | mitigate | The plan deliberately does NOT give the Steam tile a `primaryLoginAction` and does not touch `Login/index.tsx`, `Runner/index.tsx` or `App.tsx`, so the navigate-away-unmounts-all-tiles mechanism the pin documents is preserved verbatim. Task 2 re-runs `loginInFlightUiReachability.test.tsx` as a regression gate. |
| T-KQ0-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependency is introduced — `@mui/material` and `frontend/components/UI/Dialog` are already in the tree and already used by this screen's sibling components. No package-legitimacy checkpoint is required because no install task exists. |
</threat_model>

<verification>
- `pnpm codecheck` clean.
- `pnpm exec eslint src/frontend/screens/Login/components/SteamLogin` clean.
- `pnpm test src/frontend/screens/Login src/frontend/components/UI/NavShell` — new gate green, the
  five pre-existing `loginInFlightUiReachability` gates green, `navTabs` green.
- `git diff --stat` shows changes confined to the three files in `files_modified`. In particular
  `src/frontend/screens/Login/index.tsx`, `src/frontend/screens/Login/components/Runner/index.tsx`
  and `src/frontend/App.tsx` must appear NOWHERE in the diff.
- Human visual gate (Task 3) returned "approved".
- (Round 3) `pnpm exec jest --config src/frontend/jest.config.js` — full suite green (all suites,
  not just the two this plan directly touches), since Task 4 changes the shared primitive.
- (Round 3) `dialogWindowChrome.test.ts`'s assertions are each independently RED-proven, and
  `git diff` on `Dialog.tsx` shows only the radius/transition change plus the deliberate drop of
  the dead `sx` block — no unrelated behaviour change to the 25 consumers.
- (Round 3) Human visual gate (Task 5, superseding Task 3) returned "approved" across all three
  dialogs checked (Steam login, an install-flow dialog, the Settings dialog).
</verification>

<success_criteria>
- The Steam sign-in surface presents as a centred, backdropped, titled, dismissible window over
  the app — confirmed by a human against the named properties 2a-2d, not inferred from a passing
  test.
- The window is mounted once and survives every `step` and tab transition without replaying its
  entrance.
- Zero change to Steam authentication, token handling or credential storage — provable from the
  diff.
- Zero change to login routing or to the login-in-flight tile-reachability threat pin.
- (Round 3) The corner radius and entrance transition are fixed at the shared `Dialog`
  primitive, so all 25 consumers inherit the fix — not a Steam-only patch.
- (Round 3) No double backdrop, no invisible/clipped dialog, and gamepad/keyboard focus still
  lands correctly on open, across all three dialogs checked in Task 5.
</success_criteria>

<output>
Create `.planning/quick/260820-kq0-steam-login-should-have-the-same-windowi/260820-kq0-SUMMARY.md` when done.
Record in it: (1) the RED-proof mutation text and observed Jest failure for each Task 2
assertion, (2) the human gate verdict verbatim, (3) an explicit note that the native-login-window
route was investigated and rejected on the `windows: ["main"]` capability-scope finding, so a
future reader does not re-litigate it.

(Round 3 additions, same SUMMARY.md, rewritten not appended-only where correcting a prior false
claim): (4) Task 3's FAIL verdict, verbatim; (5) the corrected rationale for single-mounting
`Dialog` in `SteamLogin` — it is NOT "preserving a one-time entrance transition" (no such
transition existed at runtime at HEAD; that was a false claim in the original SUMMARY), it is
avoiding a Paper remount and a replayed `gamepadAction` tab-hack; (6) the full 25-file consumer
census of `frontend/components/UI/Dialog` importers; (7) the RED-proof mutation text and
observed Jest failure for each `dialogWindowChrome.test.ts` assertion, restored byte-identical
(not merely `git diff --quiet`-clean); (8) the deliberate decision to DROP (not realize) the
dead `sx` block's `maxWidth`/`paddingTop` intent, and why; (9) Task 5's verdict verbatim, still
OPEN until the operator runs it — never self-certified.
</output>
