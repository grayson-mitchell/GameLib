---
phase: quick-260926-8j9
plan: 01
subsystem: planning-ledger
tags: [uat, verification-ledger, windows, tray, login-window, gamepad-adjacent, audit-uat]
requires:
  - 'Phase 38 human_verification array (38-VERIFICATION.md)'
  - '34.1-VERIFICATION.md human_verification_relocated receipts'
  - '34.4.1-VERIFICATION.md human_verification_relocated receipts'
provides:
  - '38-W01 discharged PASS (window buttons, first live confirmation)'
  - '38-W02 discharged PASS (tray dark/light swap, first live confirmation darkTrayIcon does anything)'
  - '38-W03 discharged FAIL, accepted by operator decision (login window title reset)'
  - 'Two pending todos: login-title on_page_load reset; tray-glyph auto-selection'
  - 'outcome: receipts on 34.1 (two, plus closing a pre-existing relocation-rule-(3) ID gap) and 34.4.1 (one)'
affects:
  - '.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/'
  - '.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/'
  - '.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/'
tech-stack:
  added: []
  patterns:
    - 'discharge-by-move, never by in-place result: (audit-uat counts array membership)'
    - 'FAIL discharges an item exactly as legitimately as a PASS'
    - 'specification correction at discharge, disclosed and labelled, not a silent re-score'
key-files:
  created:
    - .planning/todos/pending/2026-09-26-login-window-on-page-load-overwrites-the-composed-title.md
    - .planning/todos/pending/2026-09-26-tray-glyph-variant-selection-is-manual-and-theme-blind.md
  modified:
    - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md
    - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-VERIFICATION.md
    - .planning/STATE.md
decisions:
  - 'Operator accepted 38-W03 as FAIL (will not fix) against WR-07''s letter: the origin is the trustworthy, shell-resolved half and it survives; the lost document title costs usability, not security'
  - 'A FAIL discharges an item exactly as legitimately as a PASS -- moved to human_verification_discharged, not left open, not recorded as a gap'
metrics:
  tasks: 7
  commits: 5
  audit_phase_38_before: 16
  audit_phase_38_after: 13
  audit_grand_total_before: 41
  audit_grand_total_after: 38
  completed: 2026-09-26
---

# Quick 260926-8j9: Phase 38 Windows sitting close-out — Summary

Closed the 2026-09-26 Phase 38 Windows sitting: `38-W01` (custom window buttons) and `38-W02`
(tray dark/light glyph swap) discharged PASS as first live confirmations of previously
static-only-gated Windows behaviour, and `38-W03` (login window provisional title) discharged
FAIL, accepted by operator decision against WR-07's letter, with its `expected:` corrected in
place as a specification correction (the document title is APPENDED after the origin, never
substituted for it).

## What was built

**Task 1 — discharge `38-W01` and `38-W02` as PASS** (`7fefb28e2`, folded with Task 2's commit
after a concurrent-session staging collision was corrected — see Deviations). Both entries moved
from `human_verification` to `human_verification_discharged` in `38-VERIFICATION.md`, with every
pre-existing field preserved verbatim and a new `result:` added as the first key after `id:`.
`38-W01`'s result frames the pass as the return half of its own `prior_state` ("STATICALLY FIXED,
NEVER LIVE-CONFIRMED... never observed in five sessions"). `38-W02`'s result records all three
legs (immediate swap, black-on-light legible, white-on-dark legible), names both operator-observed
traps (the correct-but-alarming "dark glyph hard to read on a dark taskbar", and the taskbar-button
icon vs. the actual notification-area tray glyph), and records the first live proof `darkTrayIcon`
does anything at all.

**Task 2 — discharge `38-W03` as FAIL-accepted, correct its `expected:`, update `score:`**
(same commit `7fefb28e2`). `38-W03` moved to `human_verification_discharged` with a `result:`
carrying all fourteen disclosure tokens the plan's honesty gate requires: `INFERRED, NOT OBSERVED`
(the `on_page_load` `Started`/`Finished` event ordering is reached by elimination across four
title-setting sites, never measured — that closure's only `eprintln!` is macOS-gated),
`CORROBORATION ONLY, NOT PROOF` (the `len=22` match to "Humble Bundle - Log In" corroborates the
hook fired but the title string itself is never logged, T-34.4.1-106), `SPECIFICATION CORRECTION`
/ `NOT A RE-SCORE`, the operator-decision framing, and `DELIBERATE DEVIATION`. `expected:` was
rewritten from "REPLACED by the document's own title" to the correct APPEND contract, citing
`main.rs:2060-2068`'s doc comment and T-34.5-G6-23. `score:` (line 5) gained a new prepended
parenthetical recording the sitting, matching the file's existing `(Was N until ...)` chain rather
than being folded into the main sentence. `sweep_notes.windows_linux_dependency` gained a dated
amendment distinguishing `38-W01`'s dev-build discharge from `38-W04`/`38-W05`'s still-open need
for a CI-produced installer artifact.

**Task 3 — commit the ledger discharges** (`7fefb28e2`). See Deviations: the first commit attempt
swept in an unrelated concurrent session's staged rename and was un-committed via `git reset
HEAD~1` (mixed, non-destructive) and re-committed clean.

**Task 4 — file two pending todos** (`0736ec037`). TODO A documents the `on_page_load` origin-only
reset defect (accepted, will not fix) with a precise one-line-guard fix proposal. TODO B documents
tray-glyph auto-selection: the Windows registry trap (`SystemUsesLightTheme`, which governs the
taskbar, is a *sibling* of `AppsUseLightTheme`, which is what tao 0.35.3 actually reads and governs
the *app* theme — independently settable via Windows 11's "Custom" Colors option), the Linux
no-reliable-signal case, the theme-agnostic-artwork third option, and the stale
`UseDarkTrayIcon.tsx:49` "(needs restart)" fallback fold-in. Both todos carry `severity: minor`,
`platform: any`, `ready: code` in that exact bare-lowercase order.

**Task 5 — add the Sitting 4 block to `38-HUMAN-UAT.md`** (`a710fe9cc`). Appended after Sitting 3
(sittings 1-3 left byte-unchanged, confirmed by diff), reproducing the two verbatim `[shell]`
terminal lines as an indented code block, recording the observation trap, and reconciling
frontmatter `updated:`/`sessions:` and the `## Current Test` paragraph to four sittings / 13 open /
13 discharged / 10 retired.

**Task 6 — walk the receipts back to both origin phases** (`b8b2d3f10`). Added `outcome:` to
34.1's `38-W01` and `38-W02` entries and 34.4.1's `38-W03` entry in their existing
`human_verification_relocated` array entries — no parallel key created. Also closed a genuine
pre-existing relocation-rule-(3) gap on 34.1's tray receipt: it named `moved_to: "Phase 38"` but
carried neither `moved_as` nor `was_uat_item`, unlike its sibling window-buttons entry. Added
`moved_as: "38-W02"` and `was_uat_item: "6d / Gap G3"` alongside the new `outcome:`.

**Task 7 — STATE.md row and final verify gate** (`2df381844`). Appended a 5-cell row (matching
the table's current convention, not the 4-cell shape used by rows before mid-2026) after the
concurrent session's `260926-8vk` row.

## Corrections against the plan's own measured baseline

The plan's `<output>` spec requires recording four line-reference corrections it made against its
own briefing — these were asserted, not re-derived, and held:

- `UseDarkTrayIcon.tsx:49`, not `:50`, for the stale `"(needs restart)"` fallback.
- `trayIconAssets.test.ts:125` and `:131` for the `isUniformFill` assertions, not the bare `:119`
  (which is the `it(...)` title).
- `main.rs:6358` for the `set_title` call that applies the composed title (the composition itself
  is at `:6357`), and `main.rs:6282-6288` for the WR-07 "actual requirement" comment block (the
  sentence itself spans `:6284-6285`), not a bare `:6283`.

All four were used correctly throughout this plan's edits, per the plan's own instruction to
assert rather than re-derive them.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Concurrent-session staging collision on Task 3's commit**
- **Found during:** Task 3 (commit the ledger discharges).
- **Issue:** `git add` staged only `38-VERIFICATION.md` and `git diff --cached --name-only`
  confirmed it, but by the time `git commit` ran (a separate tool call), a concurrent session had
  staged its own unrelated rename (`.planning/todos/pending/2026-09-25-vite-dev-watcher-...md` ->
  `completed/`) in the shared working tree, and that rename was swept into the commit — exactly
  the hazard the plan's non-negotiable #8 warned about.
- **Fix:** `git reset HEAD~1` (mixed reset — non-destructive, does not touch the working tree),
  re-staged only `38-VERIFICATION.md`, re-confirmed via `git diff --cached --name-only`, and
  re-committed. The other session's file returned to its pre-commit state, untouched by this plan.
- **Files modified:** none beyond the intended `38-VERIFICATION.md`.
- **Verification:** `git show --name-only HEAD` after the re-commit shows exactly one file.
- **Committed in:** `7fefb28e2` (the corrected commit; the original `f0bc6e344` was un-committed
  and does not appear in the final `git log`).

**2. [Rule 2 - Missing critical / documentation] Logged, did not fix, two out-of-scope pre-existing
issues**
- **Found during:** Task 4 verify (`pnpm planning-gates`) and Task 6 verify (the outcome-count
  grep).
- **Issue A:** `.planning/planning-envelope-tag-gate.py` (added 2026-09-22, predates this session)
  fails against three already-committed, unrelated files from quick `260925-uok` (commit
  `20ffb98e7`) carrying trailing orphan envelope-closing-tag lines.
- **Issue B:** Task 6's own automated verify script counts `outcome:` keys with an unscoped
  `grep -c "^    outcome: "` over the whole file, which also matches pre-existing, unrelated
  `outcome:` keys under `live_uat_sessions:` (34.1) and a live-gate-summary array (34.4.1) that
  predate this plan.
- **Fix:** Neither was fixed — both are out of scope per the SCOPE BOUNDARY rule (pre-existing,
  unrelated files/scripts). Both are logged in detail in
  `.planning/quick/260926-8j9-phase-38-windows-sitting-close-out/deferred-items.md`, including the
  scoped re-count that confirms this plan's own work is correct (3 `outcome:` keys within 34.1's
  `human_verification_relocated` array, 1 within 34.4.1's).
- **Files modified:** none (this quick task's `deferred-items.md` only).
- **Verification:** `pnpm planning-gates` reports 12/13 (the 1 failure is Issue A, independently
  corroborated by concurrent quick task `260926-8vk`'s own side finding in its SUMMARY). The scoped
  `awk`/`grep` re-count for Issue B confirms 3 and 1 respectively, matching the plan's `<done>`
  criteria exactly.

---

**Total deviations:** 2 (1 auto-fixed blocking issue, 1 logged-not-fixed pair of out-of-scope
pre-existing issues).
**Impact on plan:** No scope creep. The concurrent-session collision was corrected without loss —
the other session's rename was restored to its pre-commit state. Both logged issues are
independently verified not to affect this plan's own correctness.

## Issues Encountered

None beyond the two documented above.

## Formatter check

Not run and not claimed. `.prettierignore` lists `.planning`, and this plan touched only
`.planning/` files, so `npx prettier --check` over them would be vacuous — verified at plan time
with a deliberately malformed probe file passing at exit 0.

## Known Stubs

None. This is a documentation-only ledger-editing plan; no UI, API, or data-flow surface was
touched.

## Commits

| Task | Commit | Message |
|---|---|---|
| 1, 2 | `7fefb28e2` | `docs(quick-260926-8j9): discharge 38-W01, 38-W02 and 38-W03 from the Windows sitting` |
| 4 | `0736ec037` | `docs(quick-260926-8j9): file the login-title reset and tray-glyph-selection todos` |
| 5 | `a710fe9cc` | `docs(quick-260926-8j9): add the sitting-4 session block to 38-HUMAN-UAT.md` |
| 6 | `b8b2d3f10` | `docs(quick-260926-8j9): record sitting-4 outcomes on the 34.1 and 34.4.1 relocation receipts` |
| 7 | `2df381844` | `docs(quick-260926-8j9): record the sitting-4 close-out in STATE.md` |

## Final verify gate (actual tool output)

`npx gsd-sdk query audit-uat`: phase 38 `status: human_needed`, **13** open items (was 16), grand
total **38** (was 41). `human_verification_discharged` gained exactly `38-W01`, `38-W02`,
`38-W03`; `human_verification` holds exactly 13 `- id:` entries. `pnpm planning-gates`: **12/13**
(the 1 failure is the pre-existing, unrelated `planning-envelope-tag-gate.py` issue documented
above).

## Self-Check: PASSED

All five modified/created files under `.planning/` exist on disk. All five commit hashes resolve
in `git log`. `human_verification_discharged` in `38-VERIFICATION.md` contains `38-W01`, `38-W02`
and `38-W03` at line numbers greater than the `human_verification_discharged:` key's own line.
Both pending todos exist with `severity`/`platform`/`ready` in order. `34.1-VERIFICATION.md` and
`34.4.1-VERIFICATION.md` each carry their new `outcome:` keys within the correct array bounds.
