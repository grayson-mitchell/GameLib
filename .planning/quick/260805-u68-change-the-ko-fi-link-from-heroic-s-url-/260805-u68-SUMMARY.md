---
phase: quick-260805-u68
plan: 01
subsystem: infra
tags: [funding, ko-fi, branding, flatpak, snap, github-metadata]

requires: []
provides:
  - Every Ko-fi donation destination in the repo now points at https://ko-fi.com/gamelib instead of Heroic's accounts
affects: []

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - .github/FUNDING.yml
    - Support.md
    - flatpak/templates/com.heroicgameslauncher.hgl.metainfo.xml.template
    - snap/snapcraft.yaml
    - src/backend/constants/urls.ts

key-decisions:
  - "Left the .vscode/settings.json and src-tauri/src/main.rs working-tree modifications untouched — both pre-existed before this task started and are unrelated to Ko-fi retargeting (out of scope per deviation-rule scope boundary)."

patterns-established: []

requirements-completed:
  - REQ-Q-u68-01
  - REQ-Q-u68-02
  - REQ-Q-u68-03

duration: 12min
completed: 2026-08-05
---

# Quick Task 260805-u68: Change the Ko-fi link from Heroic's URL Summary

**Retargeted all five Ko-fi donation destinations in GameLib (GitHub Sponsor metadata, Support.md, Flatpak AppStream listing, Snap store listing, and the `kofiPage` app constant) from Heroic's `heroicgames`/`flavioislima` accounts to `https://ko-fi.com/gamelib`.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-05T09:39:xxZ
- **Completed:** 2026-08-05T09:51:49Z
- **Tasks:** 1 completed
- **Files modified:** 5

## Accomplishments
- All five Ko-fi donation sites (GitHub FUNDING.yml, Support.md, Flatpak metainfo template, Snap snapcraft.yaml, and the backend `kofiPage` constant) now route to `ko-fi.com/gamelib`
- Zero references to Heroic's Ko-fi identities (`heroicgames`, `flavioislima`) remain outside `.planning/`
- The sidebar Ko-fi button, `openKofiPage` IPC channel, and all three call sites that import `kofiPage` by name were left untouched — the button now sends users to the gamelib account for free
- Verified via `git grep` (since a repo-managed grep shell wrapper mishandled multiple `--exclude-dir` flags in this environment) that gate A (absence) and gate B (presence, exactly 5 hits) both pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Point all five Ko-fi destinations at the gamelib account** - `447689f32` (fix)

**Plan metadata:** (docs commit handled by orchestrator, not included here)

## Files Created/Modified
- `.github/FUNDING.yml` - `ko_fi:` key changed from bare username `heroicgames` to `gamelib`
- `Support.md` - Ko-fi link target changed to `https://ko-fi.com/gamelib`; `Cofee` misspelling and heading level preserved unchanged
- `flatpak/templates/com.heroicgameslauncher.hgl.metainfo.xml.template` - `<url type="donation">` element now points at `https://ko-fi.com/gamelib`
- `snap/snapcraft.yaml` - `donation:` list item changed to `https://ko-fi.com/gamelib`; sibling `github.com/sponsors/Heroic-Games-Launcher` entry untouched
- `src/backend/constants/urls.ts` - `kofiPage` export value changed to `'https://ko-fi.com/gamelib'`; identifier name and `export const` form unchanged

## Decisions Made
- Verified using `git grep` instead of the plan's literal `grep -rn --exclude-dir=... --exclude-dir=... ...` script, because the environment's `grep` shell function (a ugrep wrapper injected by the Claude Code shell snapshot) silently dropped directory exclusions when more than ~2 `--exclude-dir` flags were chained, letting matches from gitignored directories (`graphify-out/`, generated sidecar bundles) leak into the count. `git grep` naturally scopes to tracked files and reproduces the same gate-A/gate-B intent without the tooling quirk. This is a verification-tooling substitution, not a change to the plan's specified edits or gates — all five files were still checked against the exact literal patterns specified in gate C.
- Did not stage or commit `.vscode/settings.json` or `src-tauri/src/main.rs`, both of which had pre-existing uncommitted modifications from before this task began (a VS Code extension setting and unrelated Phase 34.4.2 login-window work in the Tauri Rust backend). These are out of scope per the deviation-rules scope boundary — only files directly caused by this task's changes are touched.

## Deviations from Plan

None in the code changes — plan executed exactly as written, five single-line edits, no other files modified.

### Verification-tooling note (not a code deviation)

The plan's automated verification script uses `grep -rn ... --exclude-dir=X --exclude-dir=Y ...` (six chained `--exclude-dir` flags) and assumes a clean working tree for gate D's file-count check. In this execution environment:
- The repo-injected `grep` shell function (which wraps `ugrep` via the Claude Code binary) did not honor all six `--exclude-dir` flags when chained together, causing false positives from gitignored directories (`graphify-out/`) to appear in gate A's "stale reference" count.
- `/usr/bin/grep` (BSD grep 2.6.0) also mishandled the same multi-flag exclusion and additionally stalled on a large generated sidecar JS bundle.
- Gate D's raw `git diff --name-only` count came back as 7, not 5, because two files (`.vscode/settings.json`, `src-tauri/src/main.rs`) had pre-existing unstaged modifications from before this task started — unrelated VS Code config and Phase 34.4.2 Rust work.

None of this reflects a defect in the task's edits. All five required files were individually verified against the plan's exact gate-C literal patterns, and the commit stages exactly the five task-scoped files (confirmed via `git status --short` immediately before commit).

## Known Stubs

None.

## Threat Flags

None — this task only retargets existing donation-URL string literals; no new network endpoints, auth paths, file access patterns, or schema changes were introduced.

## Self-Check: PASSED

- FOUND: `.github/FUNDING.yml` contains `ko_fi: gamelib`
- FOUND: `Support.md` contains `https://ko-fi.com/gamelib`
- FOUND: `flatpak/templates/com.heroicgameslauncher.hgl.metainfo.xml.template` contains `<url type="donation">https://ko-fi.com/gamelib</url>`
- FOUND: `snap/snapcraft.yaml` contains `https://ko-fi.com/gamelib`
- FOUND: `src/backend/constants/urls.ts` contains `export const kofiPage = 'https://ko-fi.com/gamelib'`
- FOUND: commit `447689f32` in `git log --oneline`
- FOUND: `shellFilesFlows.test.ts` passes (29/29 tests green) with the new `kofiPage` value
