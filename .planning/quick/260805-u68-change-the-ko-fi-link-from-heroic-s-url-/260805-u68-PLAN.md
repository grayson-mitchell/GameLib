---
phase: quick-260805-u68
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/FUNDING.yml
  - Support.md
  - flatpak/templates/com.heroicgameslauncher.hgl.metainfo.xml.template
  - snap/snapcraft.yaml
  - src/backend/constants/urls.ts
autonomous: true
requirements:
  - REQ-Q-u68-01   # Every Ko-fi destination in the repo points at https://ko-fi.com/gamelib
  - REQ-Q-u68-02   # No Heroic Ko-fi identity (heroicgames, flavioislima) survives anywhere outside .planning/
  - REQ-Q-u68-03   # The Ko-fi sidebar button and its openKofiPage channel keep working unchanged — only the URL value moves

must_haves:
  truths:
    - "Clicking the sidebar's Ko-fi button opens https://ko-fi.com/gamelib, not Heroic's page"
    - "The GitHub repo's Sponsor button resolves to the gamelib Ko-fi account"
    - "The Flatpak and Snap store listings advertise gamelib as the donation target"
    - "Support.md's Ko-fi link points at gamelib"
    - "A repo grep for `heroicgames`/`flavioislima` in a ko-fi context (outside .planning/) returns zero matches"
    - "The existing sidecar channel test still passes — the openKofiPage channel and its shell.openExternal dispatch are untouched"
  artifacts:
    - path: "src/backend/constants/urls.ts"
      provides: "kofiPage constant consumed by main.ts, shellFilesFlowRegistration.ts and the sidebar button"
      contains: "https://ko-fi.com/gamelib"
      absent: "ko-fi.com/heroicgames"
    - path: ".github/FUNDING.yml"
      provides: "GitHub Sponsor-button funding metadata"
      contains: "ko_fi: gamelib"
    - path: "flatpak/templates/com.heroicgameslauncher.hgl.metainfo.xml.template"
      provides: "AppStream donation URL for the Flatpak listing"
      contains: "https://ko-fi.com/gamelib"
    - path: "snap/snapcraft.yaml"
      provides: "Snap store donation link list"
      contains: "https://ko-fi.com/gamelib"
    - path: "Support.md"
      provides: "User-facing support/donation doc"
      contains: "https://ko-fi.com/gamelib"
  key_links:
    - from: "src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx"
      to: "src/backend/constants/urls.ts"
      via: "openKofiPage IPC channel -> openUrlOrFile(kofiPage); the button is NOT edited, it inherits the new value"
      pattern: "kofiPage"
    - from: "src/backend/sidecar/__tests__/shellFilesFlows.test.ts"
      to: "src/backend/constants/urls.ts"
      via: "imports the real kofiPage constant and asserts the dispatched URL contains `ko-fi.com` — stays green across the handle change"
      pattern: "ko-fi\\.com"
---

<objective>
Retarget every Ko-fi donation destination in GameLib from Heroic's accounts to the
fork's own: `https://ko-fi.com/gamelib`.

Purpose: GameLib is a public fork. Five files still route donations to upstream
Heroic's Ko-fi identities (`heroicgames` in four places, the original maintainer's
personal `flavioislima` in one). Money and attribution currently flow to the wrong
account.

Output: Five single-line edits. No channel plumbing, no UI, no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

The repo-wide grep has already been run. These are the complete five sites — do
NOT go hunting for more:

| # | File | Line | Current value |
|---|------|------|---------------|
| 1 | `.github/FUNDING.yml` | 5 | `ko_fi: heroicgames` |
| 2 | `Support.md` | 3 | `### Buy a Cofee: [Ko-fi](https://ko-fi.com/flavioislima)` |
| 3 | `flatpak/templates/com.heroicgameslauncher.hgl.metainfo.xml.template` | 15 | `  <url type="donation">https://ko-fi.com/heroicgames</url>` |
| 4 | `snap/snapcraft.yaml` | 23 | `  - https://ko-fi.com/heroicgames` |
| 5 | `src/backend/constants/urls.ts` | 15 | `export const kofiPage = 'https://ko-fi.com/heroicgames'` |

Note site 1 is a bare **username** (`gamelib`), not a URL. The other four are full URLs.

**Explicitly out of scope — do not touch:**
- The sidebar Ko-fi button, `openKofiPage` channel, preload binding, `ipc.ts` type,
  `main.ts` listener, `shellFilesFlowRegistration.ts`. The button stays; only the
  constant's value changes, and every consumer inherits it for free.
- `shellFilesFlows.test.ts:363` pins the substring `ko-fi.com`, which survives the
  handle change. Do not edit the test.
- Upstream's "Cofee" typo on `Support.md:3` — **leave it misspelled.** Fixing it is a
  separate concern and widens the fork's diff against upstream for no functional gain.
  Change only the URL inside the parentheses.
- The neighbouring `github: Heroic-Games-Launcher`, `ethereum:`, ETH/BTC and
  `sponsors/Heroic-Games-Launcher` entries in these same files. This task is Ko-fi only;
  broader fork-identity rebranding is not authorised here.
- The just-completed quick task 260805-t0s (Patreon removal). Do not re-litigate its edits.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point all five Ko-fi destinations at the gamelib account</name>
  <files>.github/FUNDING.yml, Support.md, flatpak/templates/com.heroicgameslauncher.hgl.metainfo.xml.template, snap/snapcraft.yaml, src/backend/constants/urls.ts</files>
  <action>
Make exactly five single-line edits, using Edit (never sed, never a scripted
find-and-replace across the tree — a blind replace would also rewrite `.planning/`
history documents, which must stay as written).

1. `.github/FUNDING.yml` line 5 — change the value of the `ko_fi:` key from
   `heroicgames` to `gamelib`. This field takes a bare username, NOT a URL; writing a
   URL here silently breaks GitHub's Sponsor button. Leave the `github:`,
   `open_collective:`, `ethereum:` and every commented placeholder line untouched.

2. `Support.md` line 3 — inside the existing markdown link, replace the target
   `https://ko-fi.com/flavioislima` with `https://ko-fi.com/gamelib`. Preserve the rest
   of the line byte-for-byte, including the `Cofee` misspelling and the `### ` heading
   level. The link text stays `Ko-fi`.

3. `flatpak/templates/com.heroicgameslauncher.hgl.metainfo.xml.template` line 15 —
   in the `<url type="donation">` element, replace `https://ko-fi.com/heroicgames`
   with `https://ko-fi.com/gamelib`. Keep the two-space indent and the element name.
   Do not touch the sibling `homepage`, `bugtracker`, `translate`, `help` or
   `vcs-browser` URLs.

4. `snap/snapcraft.yaml` line 23 — in the `donation:` list, replace the
   `https://ko-fi.com/heroicgames` item with `https://ko-fi.com/gamelib`. Preserve the
   `  - ` list-item indentation. Leave the sibling
   `https://github.com/sponsors/Heroic-Games-Launcher` entry and the `contact:`,
   `issues:`, `source-code:`, `website:` keys alone.

5. `src/backend/constants/urls.ts` line 15 — change the `kofiPage` export's value to
   `'https://ko-fi.com/gamelib'`. Keep the single quotes, the `export const` form and
   the identifier name `kofiPage` exactly as-is: `main.ts:155`,
   `shellFilesFlowRegistration.ts:119` and `shellFilesFlows.test.ts:234` all import
   this binding by name, so renaming it would break three call sites for no reason.

No other file in the repository is modified.
  </action>
  <verify>
    <automated>
set -o pipefail
cd "$(git rev-parse --show-toplevel)"

EXCL="--exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning --exclude-dir=graphify-out --exclude-dir=dist --exclude-dir=build"

# Gate A — absence: no Heroic Ko-fi identity survives. Was 5 before this task, must be 0.
OLD=$(grep -rn -I $EXCL -e 'ko-fi\.com/heroicgames' -e 'ko-fi\.com/flavioislima' -e 'ko_fi: heroicgames' . | wc -l | tr -d ' ')
test "$OLD" = "0" || { echo "FAIL gate A: $OLD stale Ko-fi identity reference(s) remain"; grep -rn -I $EXCL -e 'ko-fi\.com/heroicgames' -e 'ko-fi\.com/flavioislima' -e 'ko_fi: heroicgames' .; exit 1; }

# Gate B — presence: exactly the 5 expected sites now carry the gamelib identity. Was 0 before, must be 5.
NEW=$(grep -rn -I $EXCL -e 'ko-fi\.com/gamelib' -e 'ko_fi: gamelib' . | wc -l | tr -d ' ')
test "$NEW" = "5" || { echo "FAIL gate B: expected 5 gamelib Ko-fi references, found $NEW"; grep -rn -I $EXCL -e 'ko-fi\.com/gamelib' -e 'ko_fi: gamelib' .; exit 1; }

# Gate C — per-site pinning: each of the 5 files carries it, so gate B's count cannot be
# satisfied by 5 hits clustered in one file.
grep -q '^ko_fi: gamelib$' .github/FUNDING.yml || { echo "FAIL: FUNDING.yml ko_fi key not set to bare username 'gamelib'"; exit 1; }
grep -q 'https://ko-fi\.com/gamelib' Support.md || { echo "FAIL: Support.md"; exit 1; }
grep -q '<url type="donation">https://ko-fi\.com/gamelib</url>' flatpak/templates/com.heroicgameslauncher.hgl.metainfo.xml.template || { echo "FAIL: flatpak metainfo template"; exit 1; }
grep -q 'https://ko-fi\.com/gamelib' snap/snapcraft.yaml || { echo "FAIL: snapcraft.yaml"; exit 1; }
grep -q "^export const kofiPage = 'https://ko-fi\.com/gamelib'$" src/backend/constants/urls.ts || { echo "FAIL: urls.ts kofiPage constant"; exit 1; }

# Gate D — blast radius: exactly 5 files changed, no more.
CHANGED=$(git diff --name-only | sort)
test "$(echo "$CHANGED" | wc -l | tr -d ' ')" = "5" || { echo "FAIL gate D: expected 5 changed files, got:"; echo "$CHANGED"; exit 1; }

# Gate E — the openKofiPage channel still dispatches its constant to shell.openExternal.
npx jest src/backend/sidecar/__tests__/shellFilesFlows.test.ts --silent || exit 1

echo "PASS: all Ko-fi destinations retargeted to gamelib"
    </automated>
  </verify>
  <done>
`grep -rn 'ko-fi.com/heroicgames|ko-fi.com/flavioislima|ko_fi: heroicgames'` outside
`.planning/` returns nothing; each of the five files carries the gamelib value in its
correct native form (bare username in FUNDING.yml, full URL in the other four); exactly
five files show in `git diff --name-only`; `shellFilesFlows.test.ts` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app/docs → external donation site | A URL constant the app hands to `shell.openExternal`, and store/GitHub metadata that routes real user money to a third-party account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u68-01 | Tampering | `kofiPage` + 4 metadata donation targets | mitigate | A mistyped handle silently routes donations to a stranger's Ko-fi page. Gate C pins the exact literal `https://ko-fi.com/gamelib` per file with an anchored grep, so a typo fails the task rather than shipping. |
| T-u68-02 | Spoofing | the `gamelib` Ko-fi handle itself | accept | Whether `ko-fi.com/gamelib` is actually controlled by the project owner is outside this repo's control; the handle was supplied directly by the developer in the task request. No code-side mitigation possible. |
| T-u68-03 | Elevation of Privilege | blind repo-wide `sed` replace | mitigate | A scripted replace would rewrite `.planning/` history docs and the Patreon-removal task's records. Action mandates per-file Edit; gate D caps the change at exactly 5 files. |
| T-u68-SC | Tampering | package-manager installs | n/a | This plan installs no packages — no supply-chain surface. |
</threat_model>

<verification>
Run the task's automated gate. It is self-contained and takes under a minute.

Manual spot-check (optional, not blocking): launch the app, click the sidebar Ko-fi
button, confirm the browser lands on `ko-fi.com/gamelib`.
</verification>

<success_criteria>
- All five Ko-fi destinations resolve to `https://ko-fi.com/gamelib`
- Zero Heroic Ko-fi identity references remain outside `.planning/`
- The Ko-fi button, its IPC channel and every consumer of `kofiPage` are functionally unchanged
- Exactly five files modified
- `shellFilesFlows.test.ts` green
</success_criteria>

<output>
Create `.planning/quick/260805-u68-change-the-ko-fi-link-from-heroic-s-url-/260805-u68-SUMMARY.md` when done
</output>
