---
quick_id: 260727-c42
type: quick
plan: 01
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/config.json
  - .planning/graphs/.last-build-snapshot.json
  - .planning/graphs/GRAPH_REPORT.md
  - .planning/graphs/graph.html
  - .planning/graphs/graph.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Neither graph directory can be accidentally committed — git ignores both"
    - "The gsd-graphify skill no longer rebuilds the duplicate graph"
    - "The stale 13MB .planning/graphs/ directory is gone from the repo and working tree"
    - "graphify-out/ remains on disk, untouched, as the single live graph"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rules for graphify-out/ and .planning/graphs/"
      contains: "graphify-out/"
    - path: ".planning/config.json"
      provides: "graphify.enabled: false"
      contains: "\"graphify\""
  key_links:
    - from: ".planning/config.json"
      to: "gsd-graphify skill"
      via: "graphify.enabled gate"
      pattern: "\"enabled\": false"
---

<objective>
Consolidate on the standalone `graphify` CLI graph (`graphify-out/`) as the project's single knowledge graph, and retire the duplicate GSD-built graph (`.planning/graphs/`).

Purpose: The project accumulated two competing graphs. `graphify-out/` (97MB, fresh at HEAD 11cb6f9c, 5,888 nodes) is the one CLAUDE.md and the `.claude/settings.json` PreToolUse hooks reference. `.planning/graphs/` (13MB) is stale since 2026-07-20, is tracked in git, and is rebuilt by a skill nothing in this project consults. Worse, `graphify-out/` is currently neither tracked nor ignored — one careless `git add -A` commits 97MB.

Output: gitignore rules covering both graph dirs, `graphify.enabled: false` in GSD config, and the tracked `.planning/graphs/` files removed from the repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.gitignore
@.planning/config.json

<facts>
<!-- Investigation is COMPLETE. Do not re-investigate these; they are verified. -->

Current state (verified 2026-07-27):
- `git check-ignore graphify-out/ .planning/graphs/` exits 1 — NEITHER path is ignored today.
- `git ls-files .planning/graphs` returns exactly 4 tracked files:
    .planning/graphs/.last-build-snapshot.json
    .planning/graphs/GRAPH_REPORT.md
    .planning/graphs/graph.html
    .planning/graphs/graph.json
- `.planning/graphs/` contains ONLY those 4 files — no untracked strays. So a plain
  `git rm -r` empties the directory and git removes the now-empty dir from the working tree.
- `git status` shows `?? graphify-out/` (untracked, unignored).

Consumers of `.planning/graphs/`: only `~/.claude/skills/gsd-graphify/SKILL.md`,
`~/.claude/get-shit-done/bin/lib/graphify.cjs`, and two GSD workflow docs — all outside
this repo. Nothing in the GameLib app build, test suite, or CI reads it. Removal is safe,
and the content is recoverable from git history if ever needed.

`.gitignore` today ends at line 50 with the `/.build-tools/` block (Phase 24 Plan 07).
`.planning/config.json` today ends with a `"graphify": { "enabled": true }` object
(lines 53-55), two-space indented, no trailing comma.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ignore both graph directories</name>
  <files>.gitignore</files>
  <action>
    Append a new trailing section to `.gitignore` (after the existing `/.build-tools/`
    block) that ignores BOTH knowledge-graph output directories: `graphify-out/` and
    `.planning/graphs/`.

    Include a short comment explaining why, in the same explanatory style as the existing
    Phase 24 comments in this file: `graphify-out/` is the live standalone-CLI graph
    (large, regenerable via `graphify update .`, never committed); `.planning/graphs/` is
    the retired GSD-skill duplicate, ignored so it cannot come back.

    Use directory-form patterns with a trailing slash (`graphify-out/`, not `/graphify-out`)
    so they match at any depth and are unambiguously directories. Do not reorder, reword,
    or delete any existing rule in the file.
  </action>
  <verify>
    <automated>git check-ignore -v graphify-out/ .planning/graphs/ && test "$(git check-ignore graphify-out/ .planning/graphs/ | wc -l | tr -d ' ')" = "2"</automated>
  </verify>
  <done>`git check-ignore` exits 0 and reports both `graphify-out/` and `.planning/graphs/` as ignored, each attributed to the new `.gitignore` rules. All pre-existing ignore rules are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Disable the duplicate GSD graph build</name>
  <files>.planning/config.json</files>
  <action>
    Flip `graphify.enabled` from `true` to `false` in `.planning/config.json` so the
    `gsd-graphify` skill stops rebuilding the duplicate graph.

    This is a one-token edit: change the boolean on the `"enabled"` line inside the
    `"graphify"` object. Do NOT reformat, re-key, re-order, or re-serialize the rest of
    the file — every other key, the two-space indentation, and the key ordering must be
    byte-identical afterward. Use Edit (targeted string replace), not a JSON
    parse-and-rewrite, to guarantee this.

    Note there is also an `"intel": { "enabled": false }` object earlier in the file — make
    sure the edit targets the `"enabled"` line inside `"graphify"`, not that one. Anchor the
    replacement on enough surrounding context (the `"graphify": {` line) to disambiguate.
  </action>
  <verify>
    <automated>node -e "const c=require('./.planning/config.json'); if(c.graphify.enabled!==false) throw new Error('graphify.enabled not false'); if(c.intel.enabled!==false) throw new Error('intel.enabled corrupted'); if(c.workflow.code_review!==true||c.mode!=='yolo') throw new Error('unrelated keys mutated'); console.log('ok')" && test "$(git diff --numstat .planning/config.json | cut -f1)" = "1"</automated>
  </verify>
  <done>`.planning/config.json` parses as valid JSON, `graphify.enabled === false`, unrelated keys (`intel.enabled`, `workflow.code_review`, `mode`) are unchanged, and `git diff --numstat` shows exactly 1 line added / 1 removed.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the tracked stale graph from the repo</name>
  <files>.planning/graphs/.last-build-snapshot.json, .planning/graphs/GRAPH_REPORT.md, .planning/graphs/graph.html, .planning/graphs/graph.json</files>
  <action>
    Remove the retired GSD graph from version control AND the working tree with a single
    `git rm -r .planning/graphs`.

    Use `git rm -r`, NOT `git rm -r --cached`. `--cached` would untrack the files but leave
    13MB of stale artifacts sitting on disk where they can still be read as if current —
    the explicit intent here is that they leave the working tree too. The content stays
    recoverable from git history (last committed at 2026-07-20) if ever needed.

    The directory holds only the 4 tracked files (verified — no untracked strays), so git
    will drop the now-empty `.planning/graphs/` directory itself. If `git rm` unexpectedly
    reports untracked or modified files blocking the removal, STOP and report rather than
    reaching for `-f`.

    Leave `graphify-out/` completely alone — it is the live graph and must remain on disk.
  </action>
  <verify>
    <automated>test -z "$(git ls-files .planning/graphs)" && test ! -d .planning/graphs && test -f graphify-out/graph.json</automated>
  </verify>
  <done>`git ls-files .planning/graphs` returns empty, the `.planning/graphs/` directory no longer exists on disk, and `graphify-out/graph.json` is still present and untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git remote | Repo is a public fork (`gamelib`); anything committed is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-c42-01 | Information Disclosure | `graphify-out/` accidentally committed to the public fork | mitigate | Task 1 adds the gitignore rule; verified by `git check-ignore` exiting 0 |
| T-c42-02 | Denial of Service | 97MB blob bloating public fork clone size permanently | mitigate | Same gitignore rule — prevention only; nothing is committed yet, so no history rewrite is needed |
| T-c42-03 | Tampering | JSON rewrite of `.planning/config.json` silently mutating unrelated GSD settings | mitigate | Task 2 mandates targeted Edit over parse-and-rewrite; verified by numstat == 1 line and explicit unrelated-key assertions |
| T-c42-04 | Repudiation | Stale 2026-07-20 graph read as if current, yielding wrong architectural answers | mitigate | Task 3 removes it from the working tree entirely, not just the index |
| T-c42-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this task — zero dependency changes |
</threat_model>

<verification>
Run all three from the repo root after execution:

1. `git check-ignore graphify-out/ .planning/graphs/` — exits 0, prints both paths.
2. `node -e "const c=require('./.planning/config.json'); console.log(c.graphify.enabled)"` — prints `false`.
3. `git ls-files .planning/graphs` — prints nothing; `test ! -d .planning/graphs` succeeds.
4. `git status --short` — shows the 4 `.planning/graphs/` deletions staged, `.gitignore` and `.planning/config.json` modified, and NO `?? graphify-out/` line (it is now ignored).

No application source changed, so no build or test run is required.
</verification>

<success_criteria>
- Both `graphify-out/` and `.planning/graphs/` are git-ignored.
- `.planning/config.json` is valid JSON with `graphify.enabled: false` and no other key mutated.
- `.planning/graphs/` is untracked and absent from the working tree.
- `graphify-out/` is still present on disk and functional.
- `CLAUDE.md` is unmodified (its `## graphify` section correctly describes the surviving graph).
</success_criteria>

<output>
Create `.planning/quick/260727-c42-consolidate-on-the-standalone-graphify-g/260727-c42-SUMMARY.md` when done.
</output>
