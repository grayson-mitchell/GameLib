---
phase: quick-260926-c07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/__tests__/fakeHomeIsolation.test.ts
  - CLAUDE.md
  - .planning/todos/pending/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md
  - .planning/todos/completed/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md
autonomous: true
requirements:
  - TODO-2026-09-25-fakehomeisolation-windows-paths

must_haves:
  truths:
    - "`npx jest src/backend/__tests__/fakeHomeIsolation.test.ts` is green on this Windows 11 machine (it was 1 failed / 4 passed before)"
    - "Every repo-relative path the gate compares is `/`-separated on every OS, produced by one `toRepoRel()` helper"
    - "The enforcing scan no longer skips `meta/sidecarStartupSmoke.cjs`, so adding a hand-rolled HOME env block to it turns the gate RED on every OS"
    - "A new test asserts each exempt file contains a spawn call and assigns none of the eight keys, which makes its 'passes NO env key' contract executable"
    - "A new test asserts every ALLOWED_TO_ASSIGN entry and every EXEMPTIONS[].file appears in scannedRelPaths, so a skip or exempt path that can no longer match goes RED on any OS"
    - "Both negative controls were run, went RED, were reverted, and their output is recorded verbatim in SUMMARY; `git diff` holds no injection"
  artifacts:
    - path: "src/backend/__tests__/fakeHomeIsolation.test.ts"
      provides: "Normalized, non-skipping fake-HOME isolation gate with exemption-contract and skip-list-reachability tests"
      contains: "function toRepoRel"
    - path: "CLAUDE.md"
      provides: "Fake-HOME section states the exemption is from the convention, not the detector"
    - path: ".planning/todos/completed/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md"
      provides: "Closed todo with resolution note"
  key_links:
    - from: "src/backend/__tests__/fakeHomeIsolation.test.ts scannedRelPaths and the enforcing loop's rel"
      to: "toRepoRel()"
      via: "the only call site of relative()"
      pattern: "split\\(sep\\)\\.join\\('/'\\)"
    - from: "enforcing loop skip condition"
      to: "ALLOWED_TO_ASSIGN only"
      via: "exemptFiles no longer consulted in the skip"
      pattern: "ALLOWED_TO_ASSIGN\\.includes\\(rel\\)"
---

<objective>
Fix `src/backend/__tests__/fakeHomeIsolation.test.ts` so it passes on Windows, and settle what the
`meta/sidecarStartupSmoke.cjs` exemption means. Investigation found the exemption is dead on every
OS, and that it masks the regression it warns about. All decisions come from
`260926-c07-CONTEXT.md` and are LOCKED.

Purpose: today the gate is red on Windows. On macOS/Linux it is green, but its exemption silently
skips the one file whose header says an `env` key there would blind the smoke gate. Normalizing
paths alone would have made Windows as blind as macOS. This plan normalizes the paths AND stops the
skip, so the gate sees that regression on every OS.

Output: the updated gate, a one-to-two-sentence CLAUDE.md amendment, the todo moved to
`completed/`, and a SUMMARY recording both negative-control outputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/quick/260926-c07-fakehomeisolation-gate-normalize-repo-re/260926-c07-CONTEXT.md
@.planning/todos/pending/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md
@src/backend/__tests__/fakeHomeIsolation.test.ts

<interfaces>
Facts the executor needs. They were extracted from source and do not need re-exploring.

src/backend/__tests__/fakeHomeIsolation.test.ts (current, 210 lines):
- line 29: `import { join, relative } from 'node:path'`. Add `sep`.
- line 34: `REPO_ROOT = join(__dirname, '..', '..', '..')`
- line 55: `HELPER_REL_PATH = 'src/backend/testUtils/fakeHomeProfile.ts'`
- lines 68-71: `ALLOWED_TO_ASSIGN` = [HELPER_REL_PATH, 'src/backend/jest.setupContainment.ts']
- lines 73-85: EXEMPTIONS docstring (rewrite this). Lines 86-106: `Exemption` interface plus the single entry for 'meta/sidecarStartupSmoke.cjs' with `reason` and two `anchors` (KEEP unchanged).
- lines 109-110: `SPAWN_CALL_RE`. Lines 118-120: `keyAssignmentRe(key)` = `(?:^|[{,(\s])${key}\s*:` (multiline).
- line 143: `scannedRelPaths = scannedFiles.map((f) => relative(REPO_ROOT, f))`, which is BUG SITE 1
- line 144: `exemptFiles = new Set(EXEMPTIONS.map((e) => e.file))`
- line 151: `const rel = relative(REPO_ROOT, absolute)`, which is BUG SITE 2
- line 152: `if (ALLOWED_TO_ASSIGN.includes(rel) || exemptFiles.has(rel)) continue`, the skip to narrow
- tests at 168-176 (exists on disk) and 178-196 (anchor still in raw file): KEEP as-is
- test at 198-202 ("scanned a real population"): currently red on Windows, and green after normalization

src/backend/testUtils/stripSourceComments.ts: `export function stripSourceComments(source: string): string`
src/backend/testUtils/fakeHomeProfile.ts: exports `FAKE_HOME_ENV_KEYS` (8 keys)

meta/sidecarStartupSmoke.cjs:
- line 42: header "The `spawnSync` below passes NO `env` key". Line 62: "anyone adding an `env` key here is removing this gate's ability to see..."
- lines 105-108: `const build = spawnSync('pnpm', ['build:sidecar'], { cwd: REPO_ROOT, encoding: 'utf-8' })`
- lines 122-126: `const run = spawnSync(process.execPath, [BUNDLE], { cwd: REPO_ROOT, encoding: 'utf-8', timeout: STARTUP_TIMEOUT_MS })`

CLAUDE.md lines 183-188 (Fake-HOME section, "What is enforced, and what is not."), ending
"...The one exemption, `meta/sidecarStartupSmoke.cjs`, is declared in that gate's `EXEMPTIONS`
table with its reason, and the gate re-reads the exempt file to prove the reason is still written
there."

package.json: `"codecheck": "tsc --noEmit && tsc -p tsconfig.meta.json --noEmit"`,
`"planning-gates": "python3 meta/runPlanningGates.py"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize repo-relative paths, stop skipping exempt files, add exemption-contract and skip-list-reachability tests</name>
  <files>src/backend/__tests__/fakeHomeIsolation.test.ts</files>
  <behavior>
    - "skip list is reachable" (new): every entry of ALLOWED_TO_ASSIGN and every EXEMPTIONS[].file is contained in scannedRelPaths. It is RED on Windows before normalization and green after. Report the unreachable entries as an array compared with toEqual([]) so the failure names them.
    - "exempt file honours its contract" (new): for each EXEMPTIONS entry that exists on disk, the comment-stripped source matches SPAWN_CALL_RE, and FAKE_HOME_ENV_KEYS.filter(key => keyAssignmentRe(key).test(source)) is empty. Collect violations as strings and compare them with toEqual([]).
    - The enforcing test no longer skips exempt files. With the smoke file unmodified it stays green, because that file assigns no keys (CONTEXT finding 1).
    - The existing "scanned a real population" test goes green on Windows. The "exists on disk", "reason still written" and "key list length 8" tests are unchanged and green.
  </behavior>
  <action>
All of this is per the CONTEXT.md locked decisions.

(1) Path normalization, per "Normalize once at the source": add `sep` to the node:path import. Add a top-level `function toRepoRel(abs: string): string` that returns `relative(REPO_ROOT, abs).split(sep).join('/')`, and give it a short docstring. The docstring must say that `path.relative` emits `\` on Windows while every literal in this file is `/`-separated, and that this helper is the ONLY permitted caller of `relative()` in this file. Use it for `scannedRelPaths` (line 143) and for the enforcing loop's `rel` (line 151). After the edit, `relative(` must appear exactly once in comment-stripped code, inside toRepoRel. Keep the `relative` import, since toRepoRel uses it.

(2) Stop skipping, per "Exemption semantics: stop skipping, keep the drift check": change line 152's skip to `if (ALLOWED_TO_ASSIGN.includes(rel)) continue`. Delete the `exemptFiles` Set, because nothing uses it any more, and an unused const would fail lint/tsc noUnusedLocals if enabled. Keep the EXEMPTIONS entry, its `reason` and both `anchors` byte-for-byte. Rewrite the EXEMPTIONS docstring (lines 73-85) so it states plainly that:
- an exemption is from the CONVENTION (this file must inherit the operator's real profile), not from THIS DETECTOR;
- the detector flags hand-rolled home/config/state env blocks, and an exempt file carrying one would violate its own stated reason, so exempt files are still scanned;
- the table was previously used as a skip list, which was dead on every OS (the file passes no `env` key) and on macOS/Linux hid the exact regression the file's header warns against (cite quick 260926-c07).
Keep the existing paragraph about anchors sitting on one line. ALLOWED_TO_ASSIGN keeps its skip semantics and its docstring, per CONTEXT. Optionally add one clause there noting that the helper entry currently suppresses nothing either, because neither file has a spawn call (finding 3).

(3) Add the two new `it(...)` tests from <behavior> inside the existing describe. Place the reachability test next to "scanned a real population", and the contract test after the anchor test. The contract test reads the exempt file with `stripSourceComments(readFileSync(path, 'utf-8'))`, which is the same stripping idiom as the enforcing scan. Give each a brief comment naming what it catches:
- reachability: "a skip or exempt path written in a form the walk can never produce (the Windows separator bug that 260926-c07 fixed) silently stops matching; this catches it on every OS".
- contract: "the smoke gate's header promises NO env key; this makes that executable".

IMPORTANT, self-scan hazard: this test file is itself under src/ and gets scanned. Do not write any string or template literal in executable code that could match keyAssignmentRe (e.g. `'HOME: x'`) or SPAWN_CALL_RE (e.g. `spawn(`). Comments are safe because they are stripped. Test names and docstring wording are Claude's discretion.

TDD order: first add the reachability test only and run jest. Confirm it is RED on this Windows machine (record the output for SUMMARY; this doubles as negative control (b)'s baseline). Then apply the normalization and the rest, and confirm GREEN.
  </action>
  <verify>
    <automated>npx jest src/backend/__tests__/fakeHomeIsolation.test.ts && pnpm codecheck && npx eslint src/backend/__tests__/fakeHomeIsolation.test.ts && npx prettier --check src/backend/__tests__/fakeHomeIsolation.test.ts && node -e "const s=require('fs').readFileSync('src/backend/__tests__/fakeHomeIsolation.test.ts','utf8').split('\n').filter(l=>!/^\s*(\*|\/\/|\/\*)/.test(l)).join('\n');const n=(s.match(/\brelative\(/g)||[]).length;if(n!==1){console.error('relative( call count',n);process.exit(1)}if(/exemptFiles/.test(s)){console.error('exemptFiles still present');process.exit(1)}"</automated>
  </verify>
  <done>Jest reports 7 passed / 0 failed on Windows. codecheck, eslint and prettier are clean on the file. relative( appears once in code. The exemptFiles skip is gone. The EXEMPTIONS entry, reason and anchors are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Run both negative controls, record verbatim, revert (nothing committed)</name>
  <files>meta/sidecarStartupSmoke.cjs (temporary injection only, reverted), src/backend/__tests__/fakeHomeIsolation.test.ts (temporary revert only, restored)</files>
  <action>
These are the proof steps from CONTEXT "Negative controls". Both are TEMPORARY. They must be reverted and must never be committed. Commit Task 1 BEFORE starting this task, so `git checkout -- <file>` restores the committed state cleanly.

(a) In `meta/sidecarStartupSmoke.cjs`, add `env: { ...process.env, HOME: '/tmp/x' },` as a property of the options object of the `run` spawnSync (lines 122-126, after `encoding: 'utf-8',`). Do NOT run the smoke script itself. Run `npx jest src/backend/__tests__/fakeHomeIsolation.test.ts`. Expected: RED, with the enforcing test's offenders listing `meta/sidecarStartupSmoke.cjs (assigns HOME)` and the new exemption-contract test also failing. Capture the relevant failure output. Then run `git checkout -- meta/sidecarStartupSmoke.cjs`.

(b) In `src/backend/__tests__/fakeHomeIsolation.test.ts`, temporarily change toRepoRel's body to return just `relative(REPO_ROOT, abs)` (no split/join). Run the jest file. Expected: RED on this Windows machine, with the new reachability test listing all three entries as unreachable, and the "scanned a real population" test failing too. Capture the output. Then run `git checkout -- src/backend/__tests__/fakeHomeIsolation.test.ts`.

If either control does NOT go red, STOP and report it as a finding in SUMMARY. Do not adjust the gate to force a red. That would be the green-check-proving-nothing pattern.

Afterwards, re-run the jest file (green) and confirm `git status --porcelain meta/sidecarStartupSmoke.cjs src/backend/__tests__/fakeHomeIsolation.test.ts` prints nothing. Record both captured outputs verbatim in SUMMARY under "Negative controls", together with the Task 1 pre-normalization RED output. No file is written by this task that persists, so there is no prettier target. The verify block confirms the tree is clean instead.
  </action>
  <verify>
    <automated>git diff --exit-code -- meta/sidecarStartupSmoke.cjs src/backend/__tests__/fakeHomeIsolation.test.ts && npx jest src/backend/__tests__/fakeHomeIsolation.test.ts</automated>
  </verify>
  <done>Controls (a) and (b) each observed RED, with named offenders or unreachable entries. Both were reverted. The working tree is clean for both files, jest is green again, and the outputs are held for SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Amend CLAUDE.md Fake-HOME section and close the todo</name>
  <files>CLAUDE.md, .planning/todos/pending/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md, .planning/todos/completed/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md</files>
  <action>
(1) CLAUDE.md, per CONTEXT "Docs": in the "What is enforced, and what is not." paragraph of the Fake-HOME section, extend the sentence ending "...to prove the reason is still written there." by 1-2 sentences, matching the section's tone. The added text says that the exemption is from the convention, not the detector. The file is still scanned, and the gate asserts it assigns none of the eight variables, so adding an `env` block there turns the gate red. Edit only inside that paragraph. Keep the file's existing line-wrap width (~100 cols, as the surrounding prose uses).

(2) Todo: `git mv` the todo from `.planning/todos/pending/` to `.planning/todos/completed/` (same filename). Append a `## Resolution (quick 260926-c07, 2026-09-26)` section of about 5-8 lines. It must answer the todo's question directly: the enforcing test was green on Windows because the exemption is dead on every OS, since `sidecarStartupSmoke.cjs`'s spawnSync calls pass no `env` key and can never match the detector. That also means the todo's proof step ("drop the exemption and confirm RED") could not succeed. State what was done instead: toRepoRel normalization, exempt files are no longer skipped, the new contract and reachability tests, and negative controls (a)/(b) went RED (see the SUMMARY). Leave the existing frontmatter unchanged. `completed/` is exempt from the frontmatter gate, and the triage keys are already present anyway.
  </action>
  <verify>
    <automated>npx prettier --check CLAUDE.md .planning/todos/completed/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md && test ! -e .planning/todos/pending/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md && grep -q "not the detector\|not from the detector\|not from this detector" CLAUDE.md && (pnpm planning-gates || python meta/runPlanningGates.py)</automated>
  </verify>
  <done>CLAUDE.md states the convention-vs-detector distinction in 1-2 added sentences. The todo is in completed/ with a resolution note answering its question, and none remains in pending/. Prettier is clean on both written files, and planning-gates passes. If `python3` is not on PATH on Windows, the fallback `python` invocation is acceptable; record in SUMMARY which one ran.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → repo source | Gate reads source files only. No spawn, no network, no profile access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-c07-01 | Tampering | negative-control injections in meta/sidecarStartupSmoke.cjs and the gate | mitigate | Task 1 committed first; injections reverted with `git checkout --`; Task 2 verify runs `git diff --exit-code` on both files |
| T-c07-02 | Information disclosure | running the smoke script with the injected env | mitigate | Task 2 forbids running the smoke script; only jest (static source read) is run, so no sidecar starts and no profile data is captured |
| T-c07-03 | Repudiation (gate proving less than claimed) | exemption skip | mitigate | skip removed; contract test plus reachability test make the exemption and skip list executable, proven RED by controls (a)/(b) |
</threat_model>

<verification>
- `npx jest src/backend/__tests__/fakeHomeIsolation.test.ts` passes on Windows (7 tests).
- `pnpm codecheck` passes.
- `npx prettier --check` is clean over exactly the files written (the gate, CLAUDE.md, the completed todo).
- `pnpm planning-gates` passes.
- `git status` shows no residue of either negative control.
</verification>

<success_criteria>
- The gate is green on Windows, and path comparison is OS-independent through a single toRepoRel().
- An `env` block with any of the eight keys added to `meta/sidecarStartupSmoke.cjs` turns the gate RED on every OS (proven by control (a)).
- A skip/exempt path that cannot match turns the gate RED on every OS (proven by control (b) on Windows).
- The EXEMPTIONS entry, reason, anchors, and drift tests are preserved.
- CLAUDE.md and the todo are updated, and SUMMARY records both control outputs verbatim.
</success_criteria>

<output>
Create `.planning/quick/260926-c07-fakehomeisolation-gate-normalize-repo-re/260926-c07-SUMMARY.md` when done. Include a "Negative controls" section with the Task 1 pre-normalization RED output, control (a) output, and control (b) output, all verbatim.
</output>
