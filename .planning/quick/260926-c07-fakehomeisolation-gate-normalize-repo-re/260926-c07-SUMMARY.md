---
phase: quick-260926-c07
plan: 01
status: complete
subsystem: testing
tags: [jest, source-gate, windows-compat, ci-gate]

requires: []
provides:
  - "fakeHomeIsolation gate green on Windows via a single toRepoRel() normalization point"
  - "exempt files (meta/sidecarStartupSmoke.cjs) no longer skipped by the enforcing scan"
  - "exempt-file-honours-its-contract test making the smoke gate's 'passes NO env key' header claim executable"
  - "skip-list-reachability test catching any allow/exempt path that can no longer match, on every OS"
affects: [fake-home-isolation, sidecar-startup-smoke, ci]

tech-stack:
  added: []
  patterns:
    - "toRepoRel(): normalize path.relative() output once at the source, rather than at every comparison site"
    - "exemption-from-convention vs exemption-from-detector: an EXEMPTIONS entry documents why a file inherits the real profile, but the file is still scanned for the shape the exemption warns against"

key-files:
  created: []
  modified:
    - src/backend/__tests__/fakeHomeIsolation.test.ts
    - CLAUDE.md
    - .planning/todos/completed/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md

key-decisions:
  - "Normalize once via toRepoRel(), the sole permitted relative() call site in the file"
  - "Stop skipping exempt files in the enforcing scan; the EXEMPTIONS table documents a convention exemption, not a detector exemption"
  - "Add an exemption-contract test and a skip-list-reachability test rather than widening any allow/exempt list"

patterns-established:
  - "Repo-source-gate path comparisons must go through one normalization helper, never compare raw relative() output to /-separated literals"

requirements-completed:
  - TODO-2026-09-25-fakehomeisolation-windows-paths

duration: 5min
completed: 2026-09-26
---

# Quick Task 260926-c07: fakeHomeIsolation gate — normalize paths, settle the exemption Summary

**Normalized `fakeHomeIsolation.test.ts`'s path comparisons through a single `toRepoRel()` helper, stopped the (dead-on-every-OS) exemption skip, and added two new tests that make the exemption's contract and the skip-list's reachability executable — both proven by negative controls that went RED as predicted.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-09-25T20:41:41Z
- **Completed:** 2026-09-25T20:46:33Z
- **Tasks:** 3 completed (Task 1 TDD: 2 commits; Task 2: 0 commits, verification-only; Task 3: 1 commit)
- **Files modified:** 3 (`src/backend/__tests__/fakeHomeIsolation.test.ts`, `CLAUDE.md`, the moved/edited todo)

## Accomplishments

- `npx jest src/backend/__tests__/fakeHomeIsolation.test.ts` is now green on this Windows machine: 7 passed / 0 failed (was 1 failed / 4 passed before this task; now 7 tests total, up from 5, because of the two new tests)
- All repo-relative path comparisons in the gate go through one `toRepoRel()` helper; `relative(` appears exactly once in comment-stripped code
- The enforcing scan no longer skips `EXEMPTIONS`-listed files; a new "exempt file honours its contract" test asserts each exempt file still contains a spawn call and assigns none of the eight fake-HOME keys
- A new "skip list is reachable" test asserts every `ALLOWED_TO_ASSIGN` and `EXEMPTIONS[].file` entry appears in `scannedRelPaths`, catching a Windows-style path-separator bug (or any other unreachable skip/exempt path) on every OS
- Both negative controls (injecting an `env` key into the exempt smoke file; reverting the normalization) went RED exactly as predicted, then were cleanly reverted (`git diff --exit-code` confirmed clean)
- CLAUDE.md's Fake-HOME section now states the exemption is from the convention, not the detector
- The originating todo is closed with a resolution note answering its central question

## Task Commits

Task 1 followed the TDD RED/GREEN cycle as instructed by the plan's `<action>` block:

1. **Task 1 (RED): add failing skip-list-reachability test** - `f5238e385` (test)
2. **Task 1 (GREEN): normalize paths via toRepoRel(), stop skipping exempt files, add exemption-contract test** - `fa2cb3f04` (feat)
3. **Task 2: run both negative controls** - no commit (both injections reverted before any commit; verified via `git diff --exit-code`)
4. **Task 3: amend CLAUDE.md, close the todo** - `6a0ab2596` (docs)

No separate plan-metadata commit was made by the executor — the orchestrator handles the docs commit per this task's constraints (SUMMARY.md/STATE.md are not committed by this agent).

## Files Created/Modified

- `src/backend/__tests__/fakeHomeIsolation.test.ts` - Added `toRepoRel()` (sole `relative()` caller), used it for `scannedRelPaths` and the enforcing loop's `rel`; removed the dead `exemptFiles` skip; rewrote the `EXEMPTIONS` docstring (convention-vs-detector); added a one-clause note to `ALLOWED_TO_ASSIGN`'s docstring (finding 3: neither entry currently suppresses a live hit); added "skip list is reachable" and "exempt file honours its contract" tests
- `CLAUDE.md` - Fake-HOME section, "What is enforced, and what is not." paragraph extended by two sentences: the exemption is from the convention, not the detector; the exempt file is still scanned and asserted to assign none of the eight variables
- `.planning/todos/completed/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md` - Moved from `pending/`; appended a `## Resolution (quick 260926-c07, 2026-09-26)` section

## Decisions Made

- Followed all CONTEXT.md locked decisions exactly: `toRepoRel()` normalization point, exemption-from-convention-not-detector semantics, both new tests, both negative controls, CLAUDE.md wording, todo resolution — no deviation from the locked plan.

## Deviations from Plan

None - plan executed exactly as written. No Rule 1-4 auto-fixes were needed; the implementation matched the plan's `<action>` blocks and CONTEXT.md decisions directly.

One out-of-scope discovery was logged (not fixed) per the scope-boundary rule: `pnpm planning-gates` fails on `planning-envelope-tag-gate.py` due to stray orphan envelope tags in three files from an unrelated, earlier quick task (`260925-uok`, committed before this session in `20ffb98e7`). None of this task's own files triggered any gate — `todo-frontmatter-gate.py` (the gate scoped to the todo move) passed independently. See `.planning/quick/260926-c07-fakehomeisolation-gate-normalize-repo-re/deferred-items.md` for detail.

## Issues Encountered

None beyond the pre-existing, unrelated `planning-envelope-tag-gate.py` failure noted above.

## Negative Controls

### Task 1 pre-normalization RED (also negative control (b)'s baseline)

Ran immediately after adding the "skip list is reachable" test, before any normalization or skip-condition changes (still using raw `relative()`):

```
FAIL Backend src/backend/__tests__/fakeHomeIsolation.test.ts
  fake-HOME isolation (quick-260913-arr item 6, CLAUDE.md two-profile rule)
    √ no in-repo file spawns a child with a hand-rolled home/config/state env block (110 ms)
    √ every declared exemption still exists on disk (1 ms)
    √ every exemption reason is still written in the exempt file itself
    × scanned a real population, including the helper it exists to enforce (3 ms)
    × skip list is reachable: every allowed and exempt path can match (2 ms)
    √ imports its key list from the helper, so the two cannot disagree

  ● fake-HOME isolation (quick-260913-arr item 6, CLAUDE.md two-profile rule) › scanned a real population, including the helper it exists to enforce

    expect(received).toContain(expected) // indexOf

    Expected value: "src/backend/testUtils/fakeHomeProfile.ts"
    Received array: ["src\\backend\\anticheat\\ipc_handler.ts", "src\\backend\\anticheat\\utils.ts", "src\\backend\\appshell\\language.ts", "src\\backend\\appshell\\releases.ts", "src\\backend\\appshell\\themes.ts", "src\\backend\\appshell\\__tests__\\appshellModules.test.ts", "src\\backend\\backend_events.ts", "src\\backend\\cache.ts", "src\\backend\\config.ts", "src\\backend\\constants\\environment.ts", …]

  ● fake-HOME isolation (quick-260913-arr item 6, CLAUDE.md two-profile rule) › skip list is reachable: every allowed and exempt path can match

    expect(received).toEqual(expected) // deep equality

    - Expected  - 1
    + Received  + 5

    - Array []
    + Array [
    +   "src/backend/testUtils/fakeHomeProfile.ts",
    +   "src/backend/jest.setupContainment.ts",
    +   "meta/sidecarStartupSmoke.cjs",
    + ]

Test Suites: 1 failed, 1 total
Tests:       2 failed, 4 passed, 6 total
```

### Control (a): inject `env: { ...process.env, HOME: '/tmp/x' }` into the exempt smoke file's `run` spawnSync

Temporarily added `env: { ...process.env, HOME: '/tmp/x' },` to `meta/sidecarStartupSmoke.cjs`'s `run` spawnSync options (after `encoding: 'utf-8',`), on top of the Task 1 GREEN commit. Did NOT run the smoke script itself; only ran jest (static source read):

```
FAIL Backend src/backend/__tests__/fakeHomeIsolation.test.ts
  fake-HOME isolation (quick-260913-arr item 6, CLAUDE.md two-profile rule)
    × no in-repo file spawns a child with a hand-rolled home/config/state env block (112 ms)
    √ every declared exemption still exists on disk (1 ms)
    √ every exemption reason is still written in the exempt file itself
    × exempt file honours its contract: spawns, but assigns none of the eight keys (1 ms)
    √ scanned a real population, including the helper it exists to enforce
    √ skip list is reachable: every allowed and exempt path can match (1 ms)
    √ imports its key list from the helper, so the two cannot disagree

  ● fake-HOME isolation (quick-260913-arr item 6, CLAUDE.md two-profile rule) › no in-repo file spawns a child with a hand-rolled home/config/state env block

    expect(received).toEqual(expected) // deep equality

    - Expected  - 1
    + Received  + 3

    - Array []
    + Array [
    +   "meta/sidecarStartupSmoke.cjs (assigns HOME)",
    + ]

  ● fake-HOME isolation (quick-260913-arr item 6, CLAUDE.md two-profile rule) › exempt file honours its contract: spawns, but assigns none of the eight keys

    expect(received).toEqual(expected) // deep equality

    - Expected  - 1
    + Received  + 3

    - Array []
    + Array [
    +   "meta/sidecarStartupSmoke.cjs (assigns HOME)",
    + ]

Test Suites: 1 failed, 1 total
Tests:       2 failed, 5 passed, 7 total
```

Reverted with `git checkout -- meta/sidecarStartupSmoke.cjs`. Confirmed clean via `git diff --exit-code -- meta/sidecarStartupSmoke.cjs` (exit 0, no output).

### Control (b): revert `toRepoRel()` to drop the `.split(sep).join('/')` normalization

Temporarily changed `toRepoRel()`'s body to `return relative(REPO_ROOT, abs)` (no split/join), on top of the Task 1 GREEN commit:

```
FAIL Backend src/backend/__tests__/fakeHomeIsolation.test.ts
  fake-HOME isolation (quick-260913-arr item 6, CLAUDE.md two-profile rule)
    √ no in-repo file spawns a child with a hand-rolled home/config/state env block (106 ms)
    √ every declared exemption still exists on disk (1 ms)
    √ every exemption reason is still written in the exempt file itself
    √ exempt file honours its contract: spawns, but assigns none of the eight keys
    × scanned a real population, including the helper it exists to enforce (2 ms)
    × skip list is reachable: every allowed and exempt path can match (1 ms)
    √ imports its key list from the helper, so the two cannot disagree

  ● fake-HOME isolation (quick-260913-arr item 6, CLAUDE.md two-profile rule) › scanned a real population, including the helper it exists to enforce

    expect(received).toContain(expected) // indexOf

    Expected value: "src/backend/testUtils/fakeHomeProfile.ts"
    Received array: ["src\\backend\\anticheat\\ipc_handler.ts", ...]

  ● fake-HOME isolation (quick-260913-arr item 6, CLAUDE.md two-profile rule) › skip list is reachable: every allowed and exempt path can match

    expect(received).toEqual(expected) // deep equality

    - Expected  - 1
    + Received  + 5

    - Array []
    + Array [
    +   "src/backend/testUtils/fakeHomeProfile.ts",
    +   "src/backend/jest.setupContainment.ts",
    +   "meta/sidecarStartupSmoke.cjs",
    + ]

Test Suites: 1 failed, 1 total
Tests:       2 failed, 5 passed, 7 total
```

Reverted with `git checkout -- src/backend/__tests__/fakeHomeIsolation.test.ts`. Confirmed clean via `git diff --exit-code -- meta/sidecarStartupSmoke.cjs src/backend/__tests__/fakeHomeIsolation.test.ts` (exit 0, no output). Final re-run after both controls: 7 passed / 0 failed.

Neither control failed to go red; no adjustment to the gate was needed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The gate is green on Windows and OS-independent going forward through `toRepoRel()`.
- An `env` block with any of the eight keys added to `meta/sidecarStartupSmoke.cjs` will now turn the gate RED on every OS.
- A skip/exempt path written in a form the walk can never produce will now turn the gate RED on every OS.
- One pre-existing, unrelated `planning-gates` failure remains open (see Deviations above and `deferred-items.md`); recommend a follow-up todo to strip the stray envelope tags from the three `260925-uok` files.

---
*Quick task: 260926-c07*
*Completed: 2026-09-26*

## Self-Check: PASSED

- FOUND: `src/backend/__tests__/fakeHomeIsolation.test.ts`
- FOUND: `CLAUDE.md`
- FOUND: `.planning/todos/completed/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md`
- CONFIRMED ABSENT: `.planning/todos/pending/2026-09-25-fakehomeisolation-gate-compares-posix-paths-and-fails-on-windows.md`
- FOUND: `.planning/quick/260926-c07-fakehomeisolation-gate-normalize-repo-re/deferred-items.md`
- FOUND commit: `f5238e385` (test)
- FOUND commit: `fa2cb3f04` (feat)
- FOUND commit: `6a0ab2596` (docs)
