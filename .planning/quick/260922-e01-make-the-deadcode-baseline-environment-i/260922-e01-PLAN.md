---
phase: quick-260922-e01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - meta/findDeadcode.cjs
  - meta/deadcode-baseline-unreachable.txt
  - meta/deadcode-baseline-used-in-module.txt
  - meta/__tests__/findDeadcode.test.ts
autonomous: true
requirements:
  - FIX-260922-9um-env-dependent-baseline

must_haves:
  truths:
    - '`pnpm find-deadcode` reports `unreachable: 47 OK | used-in-module: 203 OK` on this Mac'
    - 'The six ts-prune parse artifacts are excluded from the MEASURED POPULATION, not merely deleted from the ledgers'
    - 'Deleting the six from the ledgers alone would turn macOS red with six NEW findings — the exclusion is what prevents that'
    - 'The exclusion is keyed on full identity (path + name), so a real export named `Record` or `Parameters` elsewhere is still caught'
    - 'meta/__tests__/findDeadcode.test.ts CLI half exits 0 again'
  artifacts:
    - path: 'meta/findDeadcode.cjs'
      provides: 'KNOWN_PARSE_ARTIFACTS identity exclusion applied to the measured population'
    - path: 'meta/deadcode-baseline-unreachable.txt'
      provides: '47 identities (was 52)'
    - path: 'meta/deadcode-baseline-used-in-module.txt'
      provides: '203 identities (was 204)'
---

<objective>
`260922-9um` shipped a dead-code ratchet whose baseline was generated on macOS only. It is
**environment-dependent**, so CI is red in TWO jobs at `f12a8bfc4` — `Lint` (the `Find dead code`
step) and `Test` (the one CLI-half test). This makes the ratchet environment-independent.
</objective>

<context>
@meta/findDeadcode.cjs
@meta/deadcode-baseline-unreachable.txt
@meta/deadcode-baseline-used-in-module.txt
@meta/__tests__/findDeadcode.test.ts

## Measured from the CI logs (run 35648042450, ubuntu-latest, Node v24.20.0)

```
unreachable: 5 baseline entr(ies) ... no longer found:
  - meta/releaseTags.ts - Record
  - meta/releaseTags.ts - satisfies
  - src/frontend/components/UI/NavShell/components/FilterFacetGroup/selectionCount.ts - readonly
  - src/frontend/components/UI/NavShell/components/FilterFacetGroup/selectionCount.ts - satisfies
  - src/frontend/state/InstallProgress.ts - Parameters
used-in-module: 1 baseline entr(ies) ... no longer found:
  - src/frontend/components/UI/NavShell/components/FilterFacetGroup/selectionCount.ts - DescriptorKind
unreachable: 47 FAIL | used-in-module: 203 FAIL
```

**ZERO new findings.** Linux's population (250) is a strict SUBSET of this Mac's (256). Local is
Node v26.2.0; CI is Node v24.20.0.

## Each of the six, verified against source — all are ts-prune mis-parses

| identity | causing source |
| --- | --- |
| `meta/releaseTags.ts - satisfies` / `- Record` | `} as const satisfies Record<DownloadedBinary, string>` (`releaseTags.ts:35`) |
| `selectionCount.ts - satisfies` / `- readonly` / `- DescriptorKind` | `] as const satisfies readonly DescriptorKind[]` (`selectionCount.ts:46`) |
| `src/frontend/state/InstallProgress.ts - Parameters` | `Parameters<typeof useShallow<StoreType, T>>[0]` (`InstallProgress.ts:16`) |

`DescriptorKind` is declared `type DescriptorKind = ActiveFilterDescriptor['kind']` at
`selectionCount.ts:26` with **no `export`** — so it is an artifact too, not a real export. None of
the six is a real exported symbol. The source todo for `260922-9um` already classified this class:
*"There is nothing to delete; the tool is wrong."*
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude the six parse artifacts from the measured population</name>
  <files>meta/findDeadcode.cjs, meta/deadcode-baseline-unreachable.txt, meta/deadcode-baseline-used-in-module.txt</files>
  <action>
Add to `meta/findDeadcode.cjs` a `KNOWN_PARSE_ARTIFACTS` constant: a list of the six full
identities above, each with an inline comment naming the causing source line and why it is not a
real export. Apply it as a filter to the collected findings **before** partitioning into the two
populations.

**Key on the FULL IDENTITY (`path - name`), never on the bare name.** `Record` and `Parameters`
are legal TypeScript identifiers and a name-only filter would silently hide a real dead export
named either. `satisfies` and `readonly` are only *contextually* reserved — `const readonly = 1`
is legal TS — so they get no special treatment either. Say this in the comment.

Comment the mechanism honestly, in the style of the rest of the file:

- ts-prune mis-parses `satisfies` expressions and the `Parameters<>` type operator, emitting
  keywords and type-reference names as if they were exported identifiers.
- **WHICH artifacts it emits is environment-dependent** — measured Node v26.2.0 (macOS) emits all
  six, Node v24.20.0 (ubuntu-latest CI) emits none. That is why `260922-9um` was green locally and
  red in CI, and why the ledger could not simply be regenerated per-platform.
- The exclusion is therefore what makes the population **the same on every machine**. This is not
  widening the gate: these are not exports, and excluding them removes noise the tool itself is
  wrong about. A real dead export is still caught everywhere.
- If a seventh artifact ever appears, the gate goes RED (a NEW finding) rather than silently
  absorbing it. Adding to this list must stay a deliberate, commented act — the same discipline as
  the ledgers.

Then delete those six lines from the two ledgers: 5 from
`meta/deadcode-baseline-unreachable.txt`, 1 (`- DescriptorKind`) from
`meta/deadcode-baseline-used-in-module.txt`. Update each ledger's header count
(`52 -> 47`, `204 -> 203`) **and** any `#` prose that states a count.

**Check the `#` annotations for numbers that move.** The used-in-module ledger carries a cluster
note about `meta/` entries with a measured `36 / 15` split and a "51 meta/ entries" figure — none
of the six removed identities is a `meta/` cluster member except `meta/releaseTags.ts - Record`
and `- satisfies`, which are in the UNREACHABLE ledger, not that cluster. Re-derive rather than
assume: after editing, count the `meta/` lines in the used-in-module ledger and confirm the note's
figures still hold; if they moved, correct them.

Also check the unreachable ledger's own "MIXED GROUP -- two of the next three lines are PARSE
ARTIFACTS" note near its `releaseTags.ts` entries — removing those entries may leave that note
describing lines that are gone. Repair or remove it as the content requires.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && pnpm find-deadcode > /tmp/e01.txt 2>&1; echo "EXIT=$?"; tail -2 /tmp/e01.txt; grep -vc '^#\|^$' meta/deadcode-baseline-unreachable.txt; grep -vc '^#\|^$' meta/deadcode-baseline-used-in-module.txt</automated>
  </verify>
  <done>
`pnpm find-deadcode` exits 0 reporting `unreachable: 47 OK | used-in-module: 203 OK`. The two
ledgers hold 47 and 203 non-comment identities. No `#` annotation states a count that is now false.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the exclusion, and prove it is not a blanket name filter</name>
  <files>meta/__tests__/findDeadcode.test.ts</files>
  <action>
Update the suite for the new counts and add assertions that did not exist:

1. **The exclusion excludes.** Given a synthetic finding list containing one of the six identities,
   the artifact filter drops it.
2. **The exclusion is identity-scoped, NOT name-scoped** — this is the assertion that protects the
   real property. A finding named `Record` or `Parameters` at a DIFFERENT path must SURVIVE the
   filter. Without this, a future refactor to a bare-name filter would pass the suite while
   silently blinding the gate.
3. **Anti-vacuity:** assert the artifact list is non-empty and that every entry in it is absent
   from both committed ledgers — an identity cannot be both excluded and ledgered, and if one ever
   is, the ledger has a line that can never match.
4. The CLI-half test (`exits 0 against real repo`) must pass again.

Export whatever helper the filter lives behind so the pure half can reach it, and keep the
`require.main === module` guard intact — the suite's existing comment depends on it.

Keep the file warning-free: `pnpm lint`'s tests ceiling is **638 with ZERO headroom**.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx jest meta/__tests__/findDeadcode.test.ts 2>&1 | tail -6</automated>
  </verify>
  <done>
Every test in the suite passes, including the CLI-half spawn and the new identity-vs-name
assertion. The name-scoped near-miss is proven to survive the filter.
  </done>
</task>

</tasks>

<verification>
1. `pnpm find-deadcode` exit 0, `unreachable: 47 OK | used-in-module: 203 OK` — captured to a file
   and read from `$?`, never through a pipe.
2. Ratchet still bites: delete a live identity from a ledger -> RED with a `+`; restore by `cp`
   from a scratchpad copy (NOT `git checkout --`, which fires this repo's `post-checkout` hook);
   confirm md5 restored.
3. `npx jest meta/__tests__/findDeadcode.test.ts` green.
4. `pnpm codecheck`, `pnpm lint` (production <=1124, tests <=638), `pnpm prettier` green.
5. `git diff --stat` lists ONLY the four files in `files_modified`. In particular
   `.github/workflows/*` must be ABSENT — task `260922-avw` owns those and its PLAN.md is already
   written and untracked.
</verification>

<success_criteria>
- Both platforms measure the same population. Locally provable only for the macOS half; the Linux
  half is confirmed by the next CI run, and that residual is stated rather than claimed.
- The six are excluded at the source of measurement, not hidden by a ledger edit.
- A real export named `Record`/`Parameters` at another path is still caught — proven by test.
</success_criteria>

<residual>
**This cannot be fully verified on this machine.** The macOS half is provable here; that Linux now
agrees is a prediction until the next CI run confirms it. Do not write a summary claiming
cross-platform parity is proven. The prediction is specific and falsifiable: CI should report
`unreachable: 47 OK | used-in-module: 203 OK`.
</residual>
