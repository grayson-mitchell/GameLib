---
phase: 260917-8hr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md
  - .planning/todos/pending/2026-09-17-linux-release-leg-fails-to-compile-get-window-missing-on-apphandle.md
  - .planning/todos/pending/2026-09-17-windows-release-leg-dies-in-install-deps-tar-reads-c-as-a-remote-host.md
  - .planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md
autonomous: true
requirements:
  - QUICK-260917-8hr-01
  - QUICK-260917-8hr-02
  - QUICK-260917-8hr-03
  - QUICK-260917-8hr-04

must_haves:
  truths:
    - "Three new pending todos exist, one per failed matrix leg of run 35223308954"
    - "Every measured figure in those todos is byte-identical to the figures in <measured_facts> below"
    - "`python3 .planning/todos/todo-frontmatter-gate.py` passes over the enlarged pending/ corpus"
    - "The existing macOS signing todo records that notarization RAN and was returned Invalid, and is still OPEN"
    - "Nothing outside `.planning/` is modified"
  artifacts:
    - path: ".planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md"
      provides: "FINDING 1 — macOS notarization Invalid, 253 unsigned paths under Contents/Resources"
      contains: "severity: critical"
    - path: ".planning/todos/pending/2026-09-17-linux-release-leg-fails-to-compile-get-window-missing-on-apphandle.md"
      provides: "FINDING 2 — E0599 get_window on &AppHandle, Linux only"
      contains: "severity: major"
    - path: ".planning/todos/pending/2026-09-17-windows-release-leg-dies-in-install-deps-tar-reads-c-as-a-remote-host.md"
      provides: "FINDING 3 — tar -tzf exit 2, Cannot connect to C"
      contains: "severity: major"
    - path: ".planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md"
      provides: "STATUS 2026-09-17 section; still status: OPEN, ready: live-gate"
      contains: "STATUS 2026-09-17"
  key_links:
    - from: ".planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md"
      to: ".planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md"
      via: "cross-link by filename in the STATUS 2026-09-17 section"
      pattern: "2026-09-17-notarization-rejects-253-unsigned-binaries"
---

<objective>
File the three failures from GitHub Actions run 35223308954 — the FIRST tag-push run
`release-tauri.yml` has ever completed — as three pending todos, and update the existing macOS
signing todo to record that notarization has now actually run and been rejected.

Purpose: all three failures were invisible until a real tag push happened. They are pre-existing,
not introduced by the tag or by commit `cc2d66248`. Recording them with measured figures is the
whole deliverable.

Output: 3 new files in `.planning/todos/pending/`, 1 edited file in the same directory.

**THIS IS A DOCS-ONLY TASK.** These todos RECORD defects; they do not fix them. Zero edits under
`src/`, `src-tauri/`, `.github/`, or `meta/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md
@.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md
</context>

<fidelity_contract>
**Every number and quoted string in `<measured_facts>` was measured by the orchestrator from the
live run. Copy them VERBATIM. Do NOT re-derive them by calling `gh`, `git`, or anything else, and
do NOT round, re-count, paraphrase, or tidy any figure.**

This repo has a documented history of fabricated and mis-derived counts (memory
`records-census-audit-index`, `zero-occurrence-grep-blames-the-module-not-the-dropped-arg`). If you
believe a figure is wrong, SAY SO in the SUMMARY — do not silently change it.

You have no access to run 35223308954; the tag `v0.7.0-notarize-test1` was deleted from origin and
locally after the run. `<measured_facts>` is the only source. Treat any urge to "verify" a figure
against the live repo as out of scope.
</fidelity_contract>

<frontmatter_contract>
`.planning/todos/pending/` frontmatter is CI-enforced by `.planning/todos/todo-frontmatter-gate.py`
(run via `pnpm planning-gates`). Scope is `pending/` only.

Three keys are REQUIRED on every file, matched **bare, lowercase, exact, unquoted**, at column 0:

    severity: critical | major | medium | minor
    platform: macos | windows | linux | any
    ready:    code | live-gate | human | blocked

Order: `severity:`, then `platform:`, then `ready:`, contiguous. Never `severity: "critical"`,
never `severity: Critical`, never a value outside those lists. A duplicate key is also a failure
(the gate reports it separately from a bad value).

The `/gsd-add-todo` frontmatter template does NOT emit these three — you are adding them by hand.

Use `.planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md`
as the structural template: `created` / `title` / `area` / `severity` / `platform` / `ready` /
`needs` / `status` / `found_by` / `source` / `files`, then `## Problem`, `## Current behaviour` or
`## Why it matters`, `## Direction`, `## Verification`, `## Related`.
</frontmatter_contract>

<measured_facts>

SOURCE OF ALL FACTS: GitHub Actions run 35223308954, repo `grayson-mitchell/GameLib`,
workflow `release-tauri.yml`, triggered by throwaway annotated tag `v0.7.0-notarize-test1`
at commit `cc2d66248` on 2026-09-17. The tag was deleted from origin and locally after the run.

CONTEXT THAT MATTERS FOR ALL THREE TODOS: this was the FIRST tag push this pipeline has ever
completed. `release-tauri.yml`'s own header comment says "UNPROVEN LIVE: this pipeline has
never completed a real tag-push run". That is why all three failures below were invisible
until now — they are pre-existing, not introduced by the tag or by commit cc2d66248.
All three matrix legs failed, for three UNRELATED reasons.

--- FINDING 1: macOS notarization REJECTED (severity critical, platform macos, ready code) ---

The macOS leg built and signed successfully, then FAILED at notarization.

Verbatim error line:

```
failed to bundle project: failed codesign application: failed to notarize app: Finished with status Invalid for id b55513c6-5b60-42bd-b69b-6e0dda7bab23 (Processing complete)
"status": "Invalid",
"statusSummary": "Archive contains critical validation errors",
```

Tauri's signing pass signed EXACTLY these, in this order (verbatim from the log):

```
Signing .../bundle/macos/GameLib.app/Contents/MacOS/gamelib-sidecar
Signing .../bundle/macos/GameLib.app/Contents/MacOS/gamelib-shell
Signing .../bundle/macos/GameLib.app
Signing /var/folders/.../GameLib.zip
Notarizing .../bundle/macos/GameLib.app
```

It does NOT recurse into `Contents/Resources/`. That is the defect.

253 distinct rejected paths. 100% of them are under
`GameLib.zip/GameLib.app/Contents/Resources/build/bin/arm64/darwin/`.
ZERO rejected paths outside `Contents/Resources`. (The orchestrator explicitly grepped for
paths not under Contents/Resources and got an empty result.)

Error-message instance counts (these count ISSUE INSTANCES, not distinct binaries):

```
506  "The signature does not include a secure timestamp."
500  "The binary is not signed with a valid Developer ID certificate."
 10  "The executable does not have the hardened runtime enabled."
  6  "The signature of the binary is invalid."
```

Issue instances grouped by top-level bundled helper:

```
394  legendary
390  nile      (includes its embedded Python 3.12 tree, e.g.
                nile/_internal/python3.12/lib-dynload/*.cpython-312-darwin.so —
                zlib, unicodedata, termios, syslog, select, resource, readline,
                pyexpat, mmap, math, grp, fcntl, binascii, array, _uuid, _struct,
                _statistics, _ssl, _socket, _sha3, _sha2, and more)
226  gogdl
  6  comet
  6  steam-bridge-helper
```

IMPORTANT FRAMING — do not overstate the prior fix:
The Node SEA sidecar (`Contents/MacOS/gamelib-sidecar`) drew ZERO notarization complaints.
The `com.apple.security.cs.allow-jit` entitlement added in quick-260914-vbw HOLDS UP under a
real notarization attempt. It was NECESSARY and is NOT SUFFICIENT. The orchestrator had
predicted the failure would be in the sidecar's nested signing; that prediction was WRONG,
and the todo should record that the defect is in the Heroic-inherited runner binaries instead.

Relevant config: `src-tauri/tauri.macos.conf.json` maps

```
"Resources/build/bin/arm64/darwin": "../build/bin/arm64/darwin"
```

which is what places these helpers inside `Contents/Resources/`.

DIRECTION for this todo (record as direction, do not implement): every Mach-O under
`Contents/Resources/` must be signed with the Developer ID identity, `--options runtime`,
and `--timestamp`, INNERMOST-FIRST, BEFORE Tauri signs the outer `.app` — because signing the
outer bundle first invalidates when inner contents change. Tauri v2 has no built-in hook that
recurses Resources, so this likely means a signing step in `release-tauri.yml` between the
sidecar build and `tauri-action`, or a `beforeBundleCommand`. Note the nested `.so` files in
nile's embedded Python are also Mach-O and need the same treatment. Flag as an OPEN QUESTION
whether these third-party binaries can be re-signed with our Developer ID at all, and whether
any need their own entitlements.

--- FINDING 2: Linux leg fails to COMPILE (severity major, platform linux, ready code) ---

Verbatim:

```
error[E0599]: no method named `get_window` found for reference `&AppHandle` in the current scope
For more information about this error, try `rustc --explain E0599`.
error: could not compile `gamelib-shell` (bin "gamelib-shell") due to 1 previous error
failed to build app: failed to build app
##[error]Command "pnpm ["tauri","build"]" failed with exit code 1
```

Call sites in `src-tauri/src/main.rs`:

```
:5203   let window = app.get_window(MAIN_WINDOW_LABEL).ok_or_else(|| {
:6915   None => match app.get_window(label) {
```

(also a doc-comment mention at :3534 and a comment at :6908 — not code)

KEY OBSERVATION: the SAME commit compiled fine on macOS (that leg reached notarization).
So this is NOT simply "the code is wrong". Suspect per-platform Cargo feature unification —
Tauri v2 gates `Manager::get_window` behind the `unstable` feature, and a platform-specific
dependency may be enabling it on macOS but not Linux. The planner should record this as a
HYPOTHESIS to verify, not as an established cause. Note that `get_webview_window` is the
Tauri v2 rename candidate, but do NOT assert that as the fix without verification.

--- FINDING 3: Windows leg dies in install-deps (severity major, platform windows, ready code) ---

Failed step: `Run ./.github/actions/install-deps`. Job duration ~1m47s. Every subsequent step
was skipped, so the Windows leg NEVER REACHED signing — meaning this run tells us nothing
either way about the Windows signing path.

Verbatim:

```
Error: tar -tzf failed (exit 2): tar (child): Cannot connect to C: resolve failed
tar: Error is not recoverable: exiting now
 ELIFECYCLE  Command failed with exit code 1.
##[error]Process completed with exit code 1.
```

Cause hypothesis: GNU tar parses a Windows absolute path `C:\...` as a REMOTE HOST spec
(`host:path`), so it tries to "connect" to host `C`. Usual remedy is `--force-local` or a
tar that understands drive letters. Record as hypothesis.

Also present in that job, NOT the cause, worth one line only:

```
##[warning]Node.js 20 is deprecated. The following actions target Node.js 20 but are being
forced to run on Node.js 24: pnpm/action-setup@v4
```

--- ITEM 4: UPDATE the existing macOS signing todo ---

File: `.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md`

Add a `## STATUS 2026-09-17 — notarization has now RUN, and FAILED` section near the top
(after the existing 2026-09-14 STATUS section). It must record:
  - Notarization is no longer untested: it ran, and Apple returned `Invalid`.
  - The blocker is now a NEW, LARGER one: 253 unsigned binaries in `Contents/Resources/`.
  - The `allow-jit` entitlement fix is VINDICATED (sidecar drew zero complaints).
  - Cross-link to the new macOS Resources-signing todo by filename.
  - The Windows leg never reached signing, so the sibling Windows todo gained no information.

DO NOT close this todo. DO NOT change its `status: OPEN`.
Its `ready: live-gate` is still correct (closing it still needs a published artifact plus a
browser download). Leave `severity`/`platform` as they are.

</measured_facts>

<tasks>

<task type="auto">
  <name>Task 1: File the macOS notarization-rejection todo (FINDING 1)</name>
  <files>.planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md</files>
  <action>
Create that file. Use the Write tool — never a heredoc.

Frontmatter, in this key order, with `severity` / `platform` / `ready` contiguous and bare:
`created: 2026-09-17T00:00:00.000Z`; `title` stating the measured defect in house style — it must
carry the number 253, name `Contents/Resources`, and be honest that this is a notarization
REJECTION, not an untested path; `area: build`; `severity: critical`; `platform: macos`;
`ready: code`; `needs: resign-resources-then-retag`; `status: OPEN`; a `found_by:` naming GitHub
Actions run 35223308954 on `grayson-mitchell/GameLib`, the throwaway annotated tag
`v0.7.0-notarize-test1` at commit cc2d66248, and the fact that the tag was deleted from origin and
locally afterwards; `source:` pointing at
`.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md`; and a `files:`
list of `.github/workflows/release-tauri.yml` and `src-tauri/tauri.macos.conf.json` — the two files
a fix would plausibly touch. Do NOT list files under `src/`.

`severity: critical` is correct and should be justified in the body rather than left bare: this is
a shipped claim that is false — the pipeline reports a signing path it does not actually complete,
and no macOS release can be published at all until it is fixed.

Body, copying every figure VERBATIM from `<measured_facts>` FINDING 1:

- `## Problem` — the macOS leg built and signed successfully then failed at notarization. Quote
  the three verbatim lines (submission id b55513c6-5b60-42bd-b69b-6e0dda7bab23, `"status":
  "Invalid"`, `"statusSummary": "Archive contains critical validation errors"`) in a fenced block.
- `## What Tauri actually signed` — reproduce the five `Signing`/`Notarizing` lines verbatim, in
  order, in a fenced block, and state plainly that the pass does not recurse into
  `Contents/Resources/`. That is the defect.
- `## The measured population` — 253 distinct rejected paths; 100% under
  `GameLib.zip/GameLib.app/Contents/Resources/build/bin/arm64/darwin/`; ZERO rejected paths outside
  `Contents/Resources`, and note explicitly that this zero is a measured grep result with an empty
  output, not an assumption. Then reproduce BOTH count tables verbatim (506/500/10/6 by message,
  and 394/390/226/6/6 by helper, with the nile Python-3.12 parenthetical intact). Label both tables
  as counting ISSUE INSTANCES, not distinct binaries — these numbers do not sum to 253 and are not
  supposed to.
- `## The allow-jit fix is VINDICATED, and was NOT the failure` — record that
  `Contents/MacOS/gamelib-sidecar` drew ZERO notarization complaints, that the
  `com.apple.security.cs.allow-jit` entitlement from quick-260914-vbw holds up under a real
  notarization attempt, and that it was NECESSARY but NOT SUFFICIENT. Record the wrong prediction
  honestly: the failure was predicted to be in the sidecar's nested signing; it was not. The defect
  is in the Heroic-inherited runner binaries. Do not overstate the prior fix in either direction.
- `## Why this was invisible until now` — this was the FIRST tag push the pipeline has ever
  completed; quote `release-tauri.yml`'s own header comment "UNPROVEN LIVE: this pipeline has never
  completed a real tag-push run". The defect is pre-existing, not introduced by the tag or by
  cc2d66248.
- `## Direction` — reproduce the DIRECTION paragraph from `<measured_facts>`: every Mach-O under
  `Contents/Resources/` signed with the Developer ID identity, `--options runtime`, `--timestamp`,
  INNERMOST-FIRST, BEFORE Tauri signs the outer `.app`, because signing the outer bundle first
  invalidates when inner contents change; Tauri v2 has no built-in hook that recurses Resources, so
  this likely means a signing step in `release-tauri.yml` between the sidecar build and
  `tauri-action`, or a `beforeBundleCommand`; the nested `.so` files in nile's embedded Python are
  also Mach-O and need the same treatment. Include the
  `"Resources/build/bin/arm64/darwin": "../build/bin/arm64/darwin"` mapping from
  `src-tauri/tauri.macos.conf.json` as the reason these helpers land in `Contents/Resources/`.
- `## Open questions` — as an explicit section, not buried: (a) can these third-party binaries be
  re-signed with our Developer ID at all; (b) do any of them need their own entitlements. Both are
  unanswered.
- `## Verification` — a green build proves nothing here; the failing leg is the only thing that has
  ever exercised this path. Verification is a fresh tag push reaching a notarization result of
  `Accepted`, plus `codesign -dv --verbose=4` / `spctl -a -vvv -t install` /
  `xcrun stapler validate` on the published artifact.
- `## Related` — cross-link `2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md` by filename
  and the two sibling todos filed in Tasks 2 and 3 by filename.

Do not invent figures not present in `<measured_facts>`. Where something is a hypothesis, label it.
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md &amp;&amp; for s in 'b55513c6-5b60-42bd-b69b-6e0dda7bab23' '253 distinct rejected paths' '506' '500' '394' '390' '226' 'steam-bridge-helper' '35223308954' 'v0.7.0-notarize-test1' 'cc2d66248' 'com.apple.security.cs.allow-jit' 'Resources/build/bin/arm64/darwin'; do grep -qF "$s" .planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md || { echo "MISSING VERBATIM: $s"; exit 1; }; done &amp;&amp; grep -qx 'severity: critical' .planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md &amp;&amp; grep -qx 'platform: macos' .planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md &amp;&amp; grep -qx 'ready: code' .planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md &amp;&amp; echo OK</automated>
  </verify>
  <done>File exists; carries `severity: critical` / `platform: macos` / `ready: code` bare at column 0 in that order; every measured figure from FINDING 1 appears verbatim; the allow-jit vindication and the wrong prediction are both recorded; open questions are a named section.</done>
</task>

<task type="auto">
  <name>Task 2: File the Linux compile-failure and Windows install-deps todos (FINDINGS 2 and 3)</name>
  <files>.planning/todos/pending/2026-09-17-linux-release-leg-fails-to-compile-get-window-missing-on-apphandle.md, .planning/todos/pending/2026-09-17-windows-release-leg-dies-in-install-deps-tar-reads-c-as-a-remote-host.md</files>
  <action>
Create both files with the Write tool. Same frontmatter contract and same `found_by:` run/tag
provenance as Task 1 (run 35223308954, tag `v0.7.0-notarize-test1`, commit cc2d66248, tag since
deleted). Same house style. Both are `area: build`, `status: OPEN`, `ready: code`.

FILE A — the Linux todo. `severity: major`, `platform: linux`, `needs: identify-feature-drift-then-fix`,
`files:` `src-tauri/src/main.rs` and `src-tauri/Cargo.toml`.

`## Problem` quotes the five verbatim lines (E0599 `no method named \`get_window\` found for
reference \`&amp;AppHandle\``, the `rustc --explain E0599` line, the `could not compile
\`gamelib-shell\` (bin "gamelib-shell") due to 1 previous error` line, `failed to build app: failed
to build app`, and the `##[error]Command "pnpm ["tauri","build"]" failed with exit code 1` line) in
a fenced block, then lists the two call sites verbatim — `:5203 let window =
app.get_window(MAIN_WINDOW_LABEL).ok_or_else(|| {` and `:6915 None =&gt; match app.get_window(label)
{` — and notes that :3534 is a doc-comment mention and :6908 a comment, NOT code. Get that
distinction into the file; a later session counting call sites by grep will otherwise find four.

`## The observation that makes this interesting` — the SAME commit compiled fine on macOS; that leg
reached notarization. So this is NOT simply "the code is wrong", and anyone who reads it that way
will fix the wrong thing.

`## Hypothesis — NOT established` — as its own clearly labelled section: suspect per-platform Cargo
feature unification; Tauri v2 gates `Manager::get_window` behind the `unstable` feature, and a
platform-specific dependency may enable it on macOS but not Linux. State in the file that this is
unverified. Mention `get_webview_window` as the Tauri v2 rename CANDIDATE and state explicitly that
it must NOT be asserted as the fix without verification.

`## Verification` — `cargo check` for the Linux target (or a tag push reaching a green Linux leg) is
the only proof; a green macOS leg proves nothing about this, which is the whole point of the
section above.

FILE B — the Windows todo. `severity: major`, `platform: windows`,
`needs: fix-tar-invocation-then-retag`, `files:` `.github/actions/install-deps` (the failed step's
path as reported) and `.github/workflows/release-tauri.yml`.

`## Problem` names the failed step `Run ./.github/actions/install-deps`, job duration ~1m47s, and
quotes the four verbatim lines (`Error: tar -tzf failed (exit 2): tar (child): Cannot connect to C:
resolve failed`, `tar: Error is not recoverable: exiting now`, ` ELIFECYCLE  Command failed with
exit code 1.`, `##[error]Process completed with exit code 1.`) in a fenced block.

`## What this run does NOT tell us` — its own section, because it is the load-bearing negative
result: every subsequent step was skipped, so the Windows leg NEVER REACHED signing. This run gives
`2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md` no information either way.
Cross-link that file by filename.

`## Hypothesis — NOT established` — GNU tar parses a Windows absolute path `C:\...` as a REMOTE HOST
spec (`host:path`) and tries to "connect" to host `C`. Usual remedy is `--force-local` or a tar that
understands drive letters. Labelled as a hypothesis.

`## Noise, not the cause` — exactly one line: the job also emitted `##[warning]Node.js 20 is
deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24:
pnpm/action-setup@v4`. Say it is not the cause and do not expand on it.

Both files get a `## Related` cross-linking the other two 2026-09-17 todos and noting the shared
provenance: this was the FIRST tag push `release-tauri.yml` has ever completed — its header comment
says "UNPROVEN LIVE: this pipeline has never completed a real tag-push run" — all three matrix legs
failed, for three UNRELATED reasons, and all three defects are pre-existing.
  </action>
  <verify>
    <automated>L=.planning/todos/pending/2026-09-17-linux-release-leg-fails-to-compile-get-window-missing-on-apphandle.md; W=.planning/todos/pending/2026-09-17-windows-release-leg-dies-in-install-deps-tar-reads-c-as-a-remote-host.md; test -f "$L" &amp;&amp; test -f "$W" &amp;&amp; for s in 'E0599' 'get_window' '5203' '6915' '35223308954' 'v0.7.0-notarize-test1' 'get_webview_window'; do grep -qF "$s" "$L" || { echo "MISSING IN LINUX: $s"; exit 1; }; done &amp;&amp; for s in 'Cannot connect to C: resolve failed' 'exit 2' '1m47s' 'install-deps' '35223308954' 'pnpm/action-setup@v4' '2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md'; do grep -qF "$s" "$W" || { echo "MISSING IN WINDOWS: $s"; exit 1; }; done &amp;&amp; grep -qx 'severity: major' "$L" &amp;&amp; grep -qx 'platform: linux' "$L" &amp;&amp; grep -qx 'ready: code' "$L" &amp;&amp; grep -qx 'severity: major' "$W" &amp;&amp; grep -qx 'platform: windows' "$W" &amp;&amp; grep -qx 'ready: code' "$W" &amp;&amp; echo OK</automated>
  </verify>
  <done>Both files exist with correct bare triage keys; both quote their verbatim log lines; both label their cause as a HYPOTHESIS; the Linux file records that macOS compiled the same commit and that :3534/:6908 are not code; the Windows file records that signing was never reached.</done>
</task>

<task type="auto">
  <name>Task 3: Update the existing macOS signing todo, then run the frontmatter gate</name>
  <files>.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md</files>
  <action>
Edit — do not rewrite — `2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md`. Insert a new
section headed `## STATUS 2026-09-17 — notarization has now RUN, and FAILED` immediately AFTER the
existing `## STATUS 2026-09-14` section (which ends just before `## Problem`) and BEFORE `## Problem`.
Leave the 2026-09-14 section intact.

The new section must record, per `<measured_facts>` ITEM 4:
- Notarization is no longer untested. It ran in GitHub Actions run 35223308954 (throwaway annotated
  tag `v0.7.0-notarize-test1`, commit cc2d66248, tag since deleted) and Apple returned `Invalid` —
  submission id b55513c6-5b60-42bd-b69b-6e0dda7bab23, `"statusSummary": "Archive contains critical
  validation errors"`.
- The blocker is now a NEW and LARGER one: 253 unsigned binaries under `Contents/Resources/`, none
  of which Tauri's signing pass touches. Say plainly that this todo's remaining work grew rather
  than shrank.
- The `allow-jit` entitlement fix from quick-260914-vbw is VINDICATED — the sidecar
  (`Contents/MacOS/gamelib-sidecar`) drew zero notarization complaints.
- Cross-link the new macOS todo BY FILENAME:
  `2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md`.
- The Windows leg never reached signing (it died in install-deps), so
  `2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md` gained NO information from
  this run. Name that file too, so a reader of either does not mistake a failed run for evidence.

HARD CONSTRAINTS on this edit:
- DO NOT close this todo. `status: OPEN` stays exactly as it is.
- DO NOT change `ready: live-gate` — closing it still needs a published artifact plus a browser
  download, so `live-gate` is still correct even though a new `code`-ready sibling now exists.
- DO NOT change `severity` or `platform`.
- Do not delete or reword the existing 2026-09-14 STATUS section, the `## Problem` section, or the
  Verification section.

Then run the CI gate over the whole enlarged corpus and confirm the docs-only boundary held.
  </action>
  <verify>
    <automated>M=.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md; grep -qF 'STATUS 2026-09-17' "$M" &amp;&amp; grep -qF 'STATUS 2026-09-14' "$M" &amp;&amp; grep -qF '2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md' "$M" &amp;&amp; grep -qF '2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md' "$M" &amp;&amp; grep -qF '35223308954' "$M" &amp;&amp; grep -qx 'status: OPEN' "$M" &amp;&amp; grep -qx 'ready: live-gate' "$M" &amp;&amp; grep -qx 'severity: major' "$M" &amp;&amp; grep -qx 'platform: macos' "$M" &amp;&amp; python3 .planning/todos/todo-frontmatter-gate.py &amp;&amp; test -z "$(git status --porcelain -- src src-tauri .github meta)" &amp;&amp; echo ALL-OK</automated>
  </verify>
  <done>The 2026-09-04 todo carries a STATUS 2026-09-17 section naming the run, the Invalid result, the 253 binaries, the vindicated allow-jit fix, and both cross-links; it is still `status: OPEN` with `ready: live-gate`; `todo-frontmatter-gate.py` passes over all pending todos; `git status --porcelain -- src src-tauri .github meta` is empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Docs-only change under `.planning/`. No code path, no network input, no runtime surface is added or modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260917-8hr-01 | Information disclosure | Todo bodies quoting CI logs | mitigate | Only the log excerpts in `<measured_facts>` may be copied. They contain no secret values — the Apple submission id b55513c6-5b60-42bd-b69b-6e0dda7bab23 is a submission identifier, not a credential. Do not paste any additional log region, and never a `::add-mask::` line or a secret name with a value beside it. |
| T-260917-8hr-02 | Tampering | `.planning/todos/todo-frontmatter-gate.py` | mitigate | The gate is READ-ONLY input for this plan. If it goes red, fix the todo's frontmatter. Never widen `VOCABULARY` or drop a key from `REQUIRED_KEYS` — CLAUDE.md and the gate's own docstring both forbid it. |
| T-260917-8hr-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. Nothing is added to any manifest; no legitimacy gate is required. |
</threat_model>

<verification>
1. `python3 .planning/todos/todo-frontmatter-gate.py` exits 0 (self-test then live scan). Its final
   line names a pending-todo count 3 higher than before this plan ran.
2. `ls .planning/todos/pending/2026-09-17-*.md | wc -l` is 3.
3. `git status --porcelain -- src src-tauri .github meta` is EMPTY. Baseline for this check: at plan
   time those four paths were clean — the only dirty entries in the tree were under `.planning/`,
   plus untracked `.claude/skills/archify/` and `skills-lock.json`, none of which this plan touches.
4. Figure fidelity — spot-check by eye against `<measured_facts>`, not by re-derivation: 253, 506,
   500, 10, 6, 394, 390, 226, 6, 6, run 35223308954, tag `v0.7.0-notarize-test1`, commit cc2d66248,
   submission id b55513c6-5b60-42bd-b69b-6e0dda7bab23, job duration ~1m47s, call sites :5203 and
   :6915.
5. The 2026-09-04 todo is still `status: OPEN` and still `ready: live-gate`.
</verification>

<success_criteria>
- 3 new files under `.planning/todos/pending/` named `2026-09-17-*.md`, one per failed matrix leg.
- 1 edited file: `2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md`, still OPEN.
- All 4 files pass `todo-frontmatter-gate.py`.
- Every measured figure byte-identical to `<measured_facts>`.
- Each cause that is a hypothesis is labelled as one (Linux feature unification, Windows tar host
  parsing). The macOS cause — Tauri not recursing into `Contents/Resources/` — is measured, not a
  hypothesis, and may be stated flatly.
- Zero changes under `src/`, `src-tauri/`, `.github/`, `meta/`.
</success_criteria>

<output>
Create `.planning/quick/260917-8hr-file-three-release-run-failures-as-todos/260917-8hr-SUMMARY.md` when done.

In the SUMMARY, state explicitly whether any figure in `<measured_facts>` looked wrong to you and
was copied anyway. Copying it anyway is the correct behaviour; silently omitting or "correcting" it
is not.
</output>
