---
phase: quick-260710-l27
plan: 01
subsystem: ui
tags: [react, i18n, electron-store, settings, gamepage]

# Dependency graph
requires: []
provides:
  - Two always-visible Extra-info rows (Crossover rating, Wine rating) reading AppleGamingWikiInfo directly via ratingTier
  - Removal of the cover-art AppleRatingOverlay pill and its CSS
  - Full removal of the appleRatingSource setting (component, state, types, electron_store schema, i18n keys)
affects: [gamepage-extra-info, settings-general]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "AppleGamingWiki compatibility data shown as two parallel rows instead of a single user-selectable row, since the source setting became redundant"

key-files:
  created: []
  modified:
    - src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx
    - src/frontend/screens/Game/GamePage/components/appleRating.ts
    - public/locales/en/gamepage.json
    - src/frontend/screens/Game/GamePage/components/index.tsx
    - src/frontend/screens/Game/GamePage/index.tsx
    - src/frontend/screens/Game/GamePage/index.css
    - src/frontend/screens/Settings/components/index.ts
    - src/frontend/screens/Settings/sections/GeneralSettings/index.tsx
    - src/frontend/state/GlobalState.tsx
    - src/frontend/state/ContextProvider.tsx
    - src/frontend/types.ts
    - src/common/types/electron_store.ts
    - src/common/types.ts
    - public/locales/en/translation.json

key-decisions:
  - "Kept ratingTier and its 'Unrated' fallback for empty ratings; removed pickRating entirely since both ratings are now shown unconditionally (locked decision B from the plan)."
  - "Deletion of AppleRatingOverlay.tsx and its dependent pickRating removal landed together in the Task 1 commit (git rm staged the file before Task 1's `git add` ran); Task 2's commit covers the remaining barrel-export/usage/CSS removal. Functionally equivalent to the plan's task boundaries, just a one-commit-early file deletion."

patterns-established: []

requirements-completed: [L27]

# Metrics
duration: 25min
completed: 2026-07-10
---

# Phase quick-260710-l27 Plan 01: Extra-info Crossover/Wine rating rows Summary

**Split the single AppleGamingWiki rating row into two always-visible Crossover/Wine rows, deleted the cover-art rating pill, and fully removed the now-redundant `appleRatingSource` setting from every layer (UI, state, types, electron_store, i18n).**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-07-10T02:53:00Z (approx)
- **Completed:** 2026-07-10T03:18:05Z
- **Tasks:** 3 code tasks completed + 1 non-blocking human-verify checkpoint deferred
- **Files modified:** 14 (1 deleted outright: `AppleRatingOverlay.tsx`; `AppleRatingSource.tsx` also deleted)

## Accomplishments
- `AppleWikiInfo.tsx` now renders two always-visible rows ("Crossover rating" and "Wine rating"), each reading `applegamingwiki.crossoverRating` / `wineRating` through `ratingTier`, falling back to "Unrated" when empty
- `pickRating` removed from `appleRating.ts`; `ratingTier` and `capitalize` retained unchanged
- `AppleRatingOverlay.tsx` (cover-art pill) deleted along with its barrel export, GamePage usage, and `.appleRatingPill` CSS rules
- `appleRatingSource` setting fully removed: `Settings/components/AppleRatingSource.tsx` deleted, its render call removed from `GeneralSettings`, and all wiring stripped from `GlobalState.tsx`, `ContextProvider.tsx`, `frontend/types.ts`, `common/types/electron_store.ts`, and `common/types.ts` (the `AppleRatingSource` type itself removed last)
- i18n: `info.crossover-rating` / `info.wine-rating` added to `gamepage.json` (replacing `info.apple-gaming-wiki`); `apple_rating_source*` keys removed from `en/translation.json`
- `pnpm codecheck` (tsc --noEmit) passes with zero errors
- Grep gate confirms zero remaining references to `appleRatingSource`, `AppleRatingSource`, `AppleRatingOverlay`, `pickRating`, or `appleRatingPill` in `src/`

## Task Commits

1. **Task 1: Split the Extra-info rating row into two rows + drop pickRating** - `c6377f5c` (feat) — also carried the `AppleRatingOverlay.tsx` file deletion (see Deviations below)
2. **Task 2: Delete the cover-art rating pill (AppleRatingOverlay)** - `258e230f` (feat) — barrel export, GamePage usage, and CSS removal
3. **Task 3: Remove the appleRatingSource setting across UI, state, and types** - split across two commits: `7efc5840` (feat, `AppleRatingSource.tsx` component deletion) and `0f8f2fa6` (feat, remaining state/types/i18n wiring removal)

**Plan metadata:** pending (this SUMMARY commit)

## Files Created/Modified
- `src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx` - Two always-visible rating rows, shared onClick handler
- `src/frontend/screens/Game/GamePage/components/appleRating.ts` - `pickRating` removed, `ratingTier`/`capitalize` kept
- `public/locales/en/gamepage.json` - `info.crossover-rating` / `info.wine-rating` added, `info.apple-gaming-wiki` removed
- `src/frontend/screens/Game/GamePage/components/AppleRatingOverlay.tsx` - deleted
- `src/frontend/screens/Game/GamePage/components/index.tsx` - `AppleRatingOverlay` barrel export removed
- `src/frontend/screens/Game/GamePage/index.tsx` - `AppleRatingOverlay` import/usage/comment removed
- `src/frontend/screens/Game/GamePage/index.css` - `.appleRatingPill` rules + DETAIL-02 comment removed
- `src/frontend/screens/Settings/components/AppleRatingSource.tsx` - deleted
- `src/frontend/screens/Settings/components/index.ts` - barrel export removed
- `src/frontend/screens/Settings/sections/GeneralSettings/index.tsx` - import + render removed
- `src/frontend/state/GlobalState.tsx` - state field, initializer, setter, provider value removed
- `src/frontend/state/ContextProvider.tsx` - default context values removed
- `src/frontend/types.ts` - `GameContextType` members removed
- `src/common/types/electron_store.ts` - `StoreStructure.configStore.appleRatingSource` field removed
- `src/common/types.ts` - `AppleRatingSource` type removed (last)
- `public/locales/en/translation.json` - `apple_rating_source*` keys removed

## Decisions Made
- Followed the plan's locked decision B: rows are always visible (never hidden per-rating); `ratingTier('')` already yields "Unrated" so no extra empty-string handling was needed.
- The `AppleGamingWikiInfo` import was dropped entirely from `appleRating.ts` (rather than kept-but-unused) since after removing `pickRating` nothing in that file referenced it — satisfies the plan's "drop unused names to satisfy the linter" note.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Deleted `AppleRatingOverlay.tsx` before starting Task 2**
- **Found during:** Task 1 verification (`pnpm codecheck`)
- **Issue:** `AppleRatingOverlay.tsx` imported `pickRating` from `appleRating.ts`; removing `pickRating` per Task 1 broke `tsc` because the overlay component (scheduled for deletion in Task 2) still referenced it. Task 1's codecheck could not go green in isolation.
- **Fix:** Performed the file deletion and its barrel-export/GamePage-usage/CSS removal (Task 2's scope) immediately so `tsc` had a consistent tree to check, then ran `pnpm codecheck` once across both tasks' combined changes (passed clean).
- **Files modified:** Same files as Task 2 (`AppleRatingOverlay.tsx` deleted, `components/index.tsx`, `GamePage/index.tsx`, `index.css`)
- **Verification:** `pnpm codecheck` passed after both tasks' edits were applied together.
- **Committed in:** `c6377f5c` (Task 1 commit unintentionally included the `AppleRatingOverlay.tsx` deletion because it was already `git rm`-staged before the Task 1 `git add` ran); `258e230f` (Task 2 commit) contains the remaining barrel-export/usage/CSS removal.

**2. [Process note] Task 3 split into two commits instead of one**
- **Found during:** Task 3 commit step
- **Issue:** A single `git add` invocation listing all Task 3 files failed entirely (pathspec error) because one of the files had already been `git rm`-staged by a prior step, so the shell command errored and none of the intended files were staged by that call — only the pre-staged deletion got committed.
- **Fix:** Re-ran `git add` for the remaining Task 3 files and created a second commit covering the rest of the wiring removal. No functional impact — all Task 3 changes are captured across the two commits, and the combined diff matches the plan's Task 3 scope exactly.
- **Files modified:** None beyond what Task 3 already specified.
- **Verification:** `git status --short` clean after both commits; `pnpm codecheck` and the grep gate both pass.
- **Committed in:** `7efc5840`, `0f8f2fa6`

---

**Total deviations:** 2 (1 blocking-fix, 1 commit-sequencing process note)
**Impact on plan:** No scope creep — both deviations are about commit granularity/ordering, not code content. All planned file changes landed exactly as specified; final diff matches Task 1/2/3 scope.

## Issues Encountered
None beyond the commit-sequencing notes above.

## User Setup Required
None - no external service configuration required.

## Human Verification (Non-blocking, Deferred)

The plan's final task is `checkpoint:human-verify` for visual/functional UAT in a running Electron app. This environment cannot launch the GUI, so verification is deferred to a real dev-build run. Expected outcomes (per plan `<how-to-verify>`):

1. Open a macOS-relevant game's Extra-info tab → two rows render: "Crossover rating" and "Wine rating"; an unrated value shows "Unrated".
2. Cover art has no rating pill overlaid.
3. Settings → General has no "Mac compatibility rating source" dropdown.
4. Clicking either rating row opens the CrossOver/CodeWeavers compatibility page.

This is **non-blocking** — code changes are complete and verified via `pnpm codecheck` + grep gate; only the visual confirmation is pending.

## Next Phase Readiness
Plan complete at the code level. Recommend running the app in dev mode to confirm the four UAT points above before considering L27 fully closed.

---
*Phase: quick-260710-l27*
*Completed: 2026-07-10*

## Self-Check: PASSED

- FOUND: commit c6377f5c (Task 1 + AppleRatingOverlay deletion)
- FOUND: commit 258e230f (Task 2 remaining removal)
- FOUND: commit 7efc5840 (Task 3 component deletion)
- FOUND: commit 0f8f2fa6 (Task 3 remaining wiring removal)
- FOUND: commit 2d4b1a3d (SUMMARY commit)
- FOUND: src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx
- CONFIRMED DELETED: src/frontend/screens/Game/GamePage/components/AppleRatingOverlay.tsx
- CONFIRMED DELETED: src/frontend/screens/Settings/components/AppleRatingSource.tsx
- FOUND: .planning/quick/260710-l27-extra-info-crossover-wine-rating-rows-re/260710-l27-SUMMARY.md
