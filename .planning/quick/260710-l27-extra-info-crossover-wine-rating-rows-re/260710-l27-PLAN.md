---
phase: quick-260710-l27
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx
  - src/frontend/screens/Game/GamePage/components/appleRating.ts
  - public/locales/en/gamepage.json
  - src/frontend/screens/Game/GamePage/components/AppleRatingOverlay.tsx
  - src/frontend/screens/Game/GamePage/components/index.tsx
  - src/frontend/screens/Game/GamePage/index.tsx
  - src/frontend/screens/Game/GamePage/index.css
  - src/frontend/screens/Settings/components/AppleRatingSource.tsx
  - src/frontend/screens/Settings/components/index.ts
  - src/frontend/screens/Settings/sections/GeneralSettings/index.tsx
  - src/frontend/state/GlobalState.tsx
  - src/frontend/state/ContextProvider.tsx
  - src/frontend/types.ts
  - src/common/types/electron_store.ts
  - src/common/types.ts
  - public/locales/en/translation.json
autonomous: false
requirements: [L27]

must_haves:
  truths:
    - "The Extra-info tab shows a 'Crossover rating' row and a 'Wine rating' row, always visible when applegamingwiki data exists."
    - "Each row shows its tier label; an empty rating renders as 'Unrated'."
    - "No rating pill is overlaid on the cover art anymore."
    - "The 'Mac compatibility rating source' setting no longer appears in Settings."
    - "The app compiles clean: `pnpm codecheck` passes."
  artifacts:
    - path: "src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx"
      provides: "Two always-visible rating rows (Crossover + Wine)"
      contains: "info.crossover-rating"
    - path: "src/frontend/screens/Game/GamePage/components/appleRating.ts"
      provides: "ratingTier + capitalize only (pickRating removed)"
      contains: "export const ratingTier"
    - path: "public/locales/en/gamepage.json"
      provides: "i18n keys for the two new rows"
      contains: "crossover-rating"
  key_links:
    - from: "AppleWikiInfo.tsx"
      to: "appleRating.ts ratingTier"
      via: "import { ratingTier }"
      pattern: "ratingTier\\(applegamingwiki\\.(crossover|wine)Rating\\)"
---

<objective>
Split the single AppleGamingWiki rating row in the game Extra-info tab into two
always-visible rows ("Crossover rating" + "Wine rating"), delete the cover-art
rating pill, and fully remove the now-redundant "Mac compatibility rating source"
(`appleRatingSource`) setting and all of its wiring.

Purpose: The rating-source setting only existed to pick which single rating to
display. Showing both ratings directly makes the setting redundant and gives the
user more information at a glance.

Output: Two-row Extra-info rating display, no cover-art pill, and a codebase with
zero `appleRatingSource` / `AppleRatingSource` / `AppleRatingOverlay` / `pickRating`
references remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Contracts the executor needs — no codebase exploration required. -->

From src/common/types.ts (AppleGamingWikiInfo, ~line 732):
```typescript
interface AppleGamingWikiInfo {
  crossoverRating: string
  wineRating: string
  crossoverLink: string
}
// AppleRatingSource (line 742) — to be REMOVED last:
export type AppleRatingSource = 'crossover' | 'wine'
```

From src/frontend/screens/Game/GamePage/components/appleRating.ts:
```typescript
// KEEP: ratingTier(rating) -> { label, colorVar }.
//   ratingTier('') => { label: 'Unrated', colorVar: '--status-default' }
//   otherwise capitalized label.
// KEEP: capitalize (private helper).
// REMOVE: pickRating(info, source) and the AppleRatingSource import.
```

GameContext exposes `wikiInfo`; `wikiInfo.applegamingwiki` may be null.
`createNewWindow(url)` from `frontend/helpers` opens an external window.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split the Extra-info rating row into two rows + drop pickRating</name>
  <files>
    src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx,
    src/frontend/screens/Game/GamePage/components/appleRating.ts,
    public/locales/en/gamepage.json
  </files>
  <action>
    In `appleRating.ts`: remove the `pickRating` export and remove `AppleRatingSource`
    from the `import { AppleGamingWikiInfo, AppleRatingSource } from 'common/types'`
    line (import becomes `import { AppleGamingWikiInfo } from 'common/types'`). Keep
    `ratingTier`, the `RatingTier` interface, and the private `capitalize`. Note:
    `AppleGamingWikiInfo` is still referenced by the `ratingTier` doc/usage context —
    keep the import if still used, otherwise drop unused names to satisfy the linter
    (tsc/eslint is the safety net).

    In `AppleWikiInfo.tsx`: rewrite to render TWO always-visible rows. Remove the
    `pickRating` import (import only `{ ratingTier }`), remove the
    `import ContextProvider` line and the `const { appleRatingSource } =
    useContext(ContextProvider)` line, and remove the `pickRating(...)` "hide when
    empty" early return. KEEP the two guards: `if (!wikiInfo) return null` and
    `if (!applegamingwiki) return null`. Define one shared `onClick` handler (extract
    the existing crossover/codeweavers logic: if `applegamingwiki.crossoverLink` open
    the crossover URL, else open the codeweavers search URL with `gameInfo.title`) and
    reuse it for both rows. Render two `<a role="button" className="iconWithText">`
    anchors reusing the existing `<WineBar />` icon + `title={t('info.clickToOpen',
    'Click to open')}` markup:
      Row 1: `<b>{t('info.crossover-rating', 'Crossover rating')}:</b>` value
             `{ratingTier(applegamingwiki.crossoverRating).label}`
      Row 2: `<b>{t('info.wine-rating', 'Wine rating')}:</b>` value
             `{ratingTier(applegamingwiki.wineRating).label}`
    Wrap the two anchors in a React fragment. Because `ratingTier('')` yields
    "Unrated", empty ratings render as "Unrated" — rows are never hidden per-rating
    (locked decision B).

    In `public/locales/en/gamepage.json` under the `"info"` object: remove the
    `"apple-gaming-wiki"` key and add `"crossover-rating": "Crossover rating"` and
    `"wine-rating": "Wine rating"` (keep the object's existing alphabetical ordering:
    `crossover-rating` after `clickToOpen`, `wine-rating` near the end). Leave the
    stale `apple-gaming-wiki` keys in the other locale files untouched (harmless).
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
  </verify>
  <done>
    `AppleWikiInfo.tsx` renders two rows (Crossover + Wine) reading `crossoverRating`
    and `wineRating` via `ratingTier`; `pickRating` no longer exists in
    `appleRating.ts`; gamepage.json has `info.crossover-rating` and `info.wine-rating`;
    `pnpm codecheck` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the cover-art rating pill (AppleRatingOverlay)</name>
  <files>
    src/frontend/screens/Game/GamePage/components/AppleRatingOverlay.tsx,
    src/frontend/screens/Game/GamePage/components/index.tsx,
    src/frontend/screens/Game/GamePage/index.tsx,
    src/frontend/screens/Game/GamePage/index.css
  </files>
  <action>
    Delete the file `AppleRatingOverlay.tsx` entirely.

    In `GamePage/components/index.tsx`: remove line 17
    `export { default as AppleRatingOverlay } from './AppleRatingOverlay'`.

    In `GamePage/index.tsx`: remove `AppleRatingOverlay,` from the component import
    block (~line 51), and remove the DETAIL-02 pill comment block (~lines 446-456)
    together with the render guard `{platform === 'darwin' && !gameInfo.is_mac_native
    && (<AppleRatingOverlay gameInfo={gameInfo} />)}` (~lines 457-459). Leave the
    `<GamePicture />` and `.store-icon` markup intact.

    In `GamePage/index.css`: remove the DETAIL-02 comment (~lines 170-173) and both
    `.appleRatingPill` and `.appleRatingPill:hover` rules (~lines 174-191). Leave the
    surrounding `.gamePicture` rule intact.
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
  </verify>
  <done>
    `AppleRatingOverlay.tsx` deleted; no import/export/usage of `AppleRatingOverlay`
    remains; `.appleRatingPill` CSS removed; `pnpm codecheck` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove the appleRatingSource setting across UI, state, and types</name>
  <files>
    src/frontend/screens/Settings/components/AppleRatingSource.tsx,
    src/frontend/screens/Settings/components/index.ts,
    src/frontend/screens/Settings/sections/GeneralSettings/index.tsx,
    src/frontend/state/GlobalState.tsx,
    src/frontend/state/ContextProvider.tsx,
    src/frontend/types.ts,
    src/common/types/electron_store.ts,
    src/common/types.ts,
    public/locales/en/translation.json
  </files>
  <action>
    Do the whole removal in ONE commit so tsc never sees a half-removed type (partial
    removal fails mid-way). Remove the `AppleRatingSource` type LAST-in-source-order
    reasoning: delete every consumer first, then the type definition.

    - Delete `Settings/components/AppleRatingSource.tsx` entirely.
    - `Settings/components/index.ts`: remove line 8
      `export { default as AppleRatingSource } from './AppleRatingSource'`.
    - `Settings/sections/GeneralSettings/index.tsx`: remove `AppleRatingSource,` from
      the import (line 5) and remove the `<AppleRatingSource />` render (line 40).
    - `state/GlobalState.tsx`: remove `AppleRatingSource` from the type import (~line
      15), the `appleRatingSource: AppleRatingSource` state field (~line 117), the
      init line `appleRatingSource: configStore.get('appleRatingSource', 'crossover')`
      (~line 293), the `setAppleRatingSource` setter method (~lines 378-380), and the
      `setAppleRatingSource: this.setAppleRatingSource,` provider value (~line 1445).
      Also remove `appleRatingSource: this.state.appleRatingSource,` from the provider
      value if present (search the provider `value={{ ... }}` object).
    - `state/ContextProvider.tsx`: remove the `appleRatingSource: 'crossover',` and
      `setAppleRatingSource: () => null,` defaults (~lines 94-95).
    - `frontend/types.ts`: remove `AppleRatingSource` from the import (~line 18) and
      the `appleRatingSource` + `setAppleRatingSource` context members (~lines 124-125).
    - `common/types/electron_store.ts`: remove `AppleRatingSource` from the import
      (~line 18) and the `appleRatingSource: AppleRatingSource` schema field (~line 42).
    - `common/types.ts`: remove `export type AppleRatingSource = 'crossover' | 'wine'`
      (~line 742) — this is the LAST removal.
    - `public/locales/en/translation.json`: remove the `apple_rating_source`,
      `apple_rating_source_crossover`, and `apple_rating_source_wine` keys (~lines
      876-878). Leave other-locale copies untouched (harmless).

    tsc is the safety net: any missed reference fails codecheck. Run the grep gate
    below to confirm zero remaining references in `src/`.
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
    <automated>test $(grep -rEl 'appleRatingSource|AppleRatingSource|AppleRatingOverlay|pickRating|appleRatingPill' src/ | wc -l | tr -d ' ') = 0</automated>
  </verify>
  <done>
    Zero references to `appleRatingSource`, `AppleRatingSource`, `AppleRatingOverlay`,
    `pickRating`, or `appleRatingPill` in `src/`; the setting no longer renders in
    Settings; `apple_rating_source*` i18n keys removed from en/translation.json;
    `pnpm codecheck` passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <what-built>
    Extra-info tab now shows two always-visible rating rows (Crossover + Wine), the
    cover-art pill is gone, and the "Mac compatibility rating source" setting is
    removed.
  </what-built>
  <how-to-verify>
    1. Run the app (`pnpm start` / dev build) — GUI cannot be launched in this
       environment, so this is a manual check.
    2. Open a macOS-relevant game's page → Extra-info tab. Confirm TWO rows appear:
       "Crossover rating" and "Wine rating". Confirm an unrated value shows "Unrated".
    3. Confirm the cover art has NO rating pill overlaid.
    4. Open Settings → General. Confirm there is no "Mac compatibility rating source"
       dropdown.
    5. Click each rating row → confirm it opens the CrossOver/CodeWeavers compatibility
       page.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues. Non-blocking — may be deferred to real-device UAT.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This is a frontend display/config refactor: it renders
already-fetched `applegamingwiki` data and removes a persisted config field. No new
network calls, no new user input parsing. The reused `onClick` opens the same
external CodeWeavers URLs that already existed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l27-01 | Information Disclosure | external URL open via createNewWindow | accept | Reuses pre-existing handler and URL construction; no change in exposure surface. |
| T-l27-02 | Tampering | removed electron_store `appleRatingSource` field | accept | Removing a config field; stale persisted values are ignored, not read. No migration needed. |
</threat_model>

<verification>
- `pnpm codecheck` passes after each task (tsc + lint are the primary safety net).
- Grep gate: zero remaining `appleRatingSource` / `AppleRatingSource` /
  `AppleRatingOverlay` / `pickRating` / `appleRatingPill` references in `src/`.
- Human visual UAT (non-blocking) confirms two rows render, no pill, no setting.
</verification>

<success_criteria>
- Extra-info tab shows "Crossover rating" and "Wine rating" rows, always visible,
  "Unrated" for empty values.
- Cover-art rating pill removed.
- "Mac compatibility rating source" setting fully removed with no dangling references.
- `pnpm codecheck` green.
</success_criteria>

<output>
Create `.planning/quick/260710-l27-extra-info-crossover-wine-rating-rows-re/260710-l27-SUMMARY.md` when done.
</output>
