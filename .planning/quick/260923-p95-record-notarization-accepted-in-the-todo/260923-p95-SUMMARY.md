---
phase: quick-260923-p95
plan: 01
subsystem: docs/todos
tags: [notarization, macos, documentation-only]
dependency-graph:
  requires: []
  provides: ["notarization todo re-triaged with Apple's Accepted verdict"]
  affects: [".planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md"]
tech-stack:
  added: []
  patterns: ["append-only dated STATUS sub-sections", "reconcile-not-delete frontmatter/prose triage"]
key-files:
  created: []
  modified:
    - ".planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md"
decisions:
  - "severity: critical -> major (the false-shipped-claim ground is discharged by Apple's Accepted verdict; what remains is a possibly-broken feature, not a false claim)"
  - "needs: retag-and-confirm-notarization-accepted -> launch-helpers-under-hardened-runtime"
  - "Attribution line uses the current session's mandated 'Claude Sonnet 5' form rather than the plan's literal 'Claude Opus 5 (1M context)' string -- see Deviations"
metrics:
  duration: "~35 min"
  completed: 2026-09-23
---

# Quick Task 260923-p95: Record notarization Accepted verdict in the todo Summary

Re-triaged `.planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md`
to record that Apple **ACCEPTED** the 2026-09-23 notarization submission
(`e7ec58a5-acd7-4dad-9bee-fca7b2bea039`), flipping hazards 2 and 3 from UNOBSERVED to CONFIRMED
and moving `severity: critical` -> `major` / `needs: retag-and-confirm-notarization-accepted` ->
`launch-helpers-under-hardened-runtime`, without deleting or renumbering any prior evidence.

## What was done

**Task 1 — Frontmatter re-triage, `## Problem` reconciliation, hazard flips (commit `0162d12c8`, part of the single combined commit below).**
- `severity: critical` -> `severity: major`; `needs: retag-and-confirm-notarization-accepted` ->
  `needs: launch-helpers-under-hardened-runtime`. `ready: live-gate`, `platform: macos`,
  `status: OPEN` left byte-identical.
- `## Problem` paragraph amended to record that `critical` was correct when written on 2026-09-17,
  that its ground (`this is a shipped claim that is false`) was discharged on 2026-09-23, and that
  the file is now `major`. The original reasoning and the verbatim `Invalid` error block were kept
  intact, not deleted.
- Items 2 and 3 of the `### NOTHING HERE IS VERIFIED` list flipped from UNOBSERVED to CONFIRMED,
  citing the submission id and distinguishing this as **direct** evidence (the ticket enumerates
  all 253 binaries by in-bundle path with per-file cdhashes) rather than an inference from
  "Apple accepted, so signatures must have been intact." Items 1 and 4 left byte-identical.

**Task 2 — New `### STATUS 2026-09-23 (quick-260923-p95)` section (same commit).**
Inserted between the np3 section and `## Related`, covering: how the verdict was obtained
(`notarytool history --keychain-profile gamelib`, Team ID `S7U223QWXJ`); the two-row history with
its cross-check against the `Invalid` id already quoted in `## Problem`; the parsed ticket fields
(`Accepted`, `Ready for distribution`, `statusCode: 0`, `issues: None`, `uploadDate`, `sha256`,
`ticketContents: 257`, 253 under `Contents/Resources`); what this settles (hazards 2 and 3) and
explicitly what it does NOT settle (hazard 4 still UNOBSERVED, recipe steps 2-5 never run, no
stapling ever happened); the open risk to the `timeout-minutes: 60` bound shipped by
quick-260923-mrx (Apple exposes no `completedDate`, so the bound's correctness is still unproven);
the triage movement; and cleanup carried forward unaddressed (`v0.7.0-notarize-test2` tag still on
origin, macOS-less `latest.json` in the `v0.7.0` draft release).

**Task 3 — Gates, formatter check, single-file commit.**
`pnpm planning-gates` ran 12/12 both before verification runs and as part of the commit gate.
`npx prettier --check` was run on the target path per repo convention and is recorded below as
**vacuous**. Exactly one file was staged, diffed, and committed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed a line-wrap that split a required literal phrase across two lines**
- **Found during:** Task 1 self-check (verify script `FAIL: prob keeps shipped-claim phrase`)
- **Issue:** The first draft of the reconciled `## Problem` paragraph wrapped `this is a\nshipped
  claim that is false` across a line break, so `grep -cF 'this is a shipped claim that is false'`
  (a fixed-string, single-line match) returned 0 instead of the required 1.
- **Fix:** Reworded the wrap point so the full phrase sits on one physical line.
- **Files modified:** the same single target file.
- **Commit:** folded into `0162d12c8` (the phrase never existed as committed content before the fix).

### Attribution line deviates from the plan's literal string (not a Rule 1-4 deviation — a precedence conflict)

The plan's Task 3 action text and `<verify>` block specify the literal attribution line
`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`. This executor's system
instructions for this session mandate `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`
and state explicitly that this "replaces Claude Code's own earlier attribution guidance, such as a
previous copy of this reminder." The plan's specific string reflects whichever model authored the
plan in a prior session, not a user instruction (CLAUDE.md or memory rule) that would take
precedence over the session's attribution mandate. The commit therefore carries the Sonnet 5 form.
The plan's own `<verify>` assertion for the exact Opus 5 string was run and confirmed to fail
(`got=0 want=1`) purely on this string; every other assertion in Task 3's verify block passed,
including the "no push-state claim" and single-file-commit checks. This is recorded here rather
than silently reconciled, per the instruction not to let a `pass` claim cover a defective check.

## Known Stubs

None. This is a documentation-only change to one markdown file; no code paths, UI, or data flow
were touched.

## Threat Flags

None. This plan's own `<threat_model>` already covers the only two trust boundaries in play
(Keychain-derived output leaking into the public repo, and hand-transcription of the notarization
ticket becoming a false record), and no new surface was introduced — no source, workflow, script,
tag, or release was touched, confirmed by
`git status --porcelain -- .github src-tauri src meta package.json` returning empty both before
and after the commit.

## Vacuous formatter check (recorded per CLAUDE.md convention)

`npx prettier --check .planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md`
exits 0 and prints "All matched files use Prettier code style!" — but this proves nothing about
this file's formatting. `.prettierignore` carries `.planning` as an exact line
(`grep -cx '.planning' .prettierignore` = 1), so prettier's glob matches ZERO files under this
path and the check is vacuously green over an empty set. The husky pre-commit prettier hook is
vacuous here for the identical reason (it also honours `.prettierignore`). Emitted anyway per
convention, paired with the proving grep, and recorded here as vacuous rather than presented as a
real formatting guarantee.

## What remains open (carried forward, per the plan's `<output>` instructions)

1. **Hazard 4 (a helper crashing at runtime under the hardened runtime with no entitlements) is
   still UNOBSERVED**, and recipe step 6 (launch the app, invoke legendary/nile/gogdl once each)
   is the only thing that tests it. This is why the todo stays `ready: live-gate` and OPEN.
2. **Recipe steps 2-5 were never run** — `codesign -dv --verbose=4`, `spctl`,
   `xcrun stapler validate`, and the survivor count — because no artifact was ever published or
   downloaded from either live run to date; nothing was ever stapled.
3. **Cleanup still owed, untouched by this task:** the tag `v0.7.0-notarize-test2` is still on
   origin (per np3 item 7), and the `v0.7.0` draft release's `latest.json` was overwritten with a
   macOS-less manifest by the Linux leg of run 35808881023 (per np3 item 6). Neither was addressed
   here — both are documentation-only findings carried forward, not code or infrastructure changes
   this task was scoped to make.
4. **The `timeout-minutes: 60` bound** (shipped by quick-260923-mrx) remains unproven live: Apple's
   API exposes no `completedDate`, so it is still unknown whether 60 minutes is generous or tight
   relative to Apple's actual processing time. The next run's `if: failure()` diagnostic step is
   what will make a future timeout self-diagnosing — this task recorded the risk only, per its
   explicit scope boundary against touching the workflow.

## Self-Check: PASSED

- `.planning/todos/pending/2026-09-17-notarization-rejects-253-unsigned-binaries-under-contents-resources.md` — FOUND (still in `pending/`, still `status: OPEN`)
- Commit `0162d12c8` — FOUND (`git log --oneline --all | grep -q 0162d12c8` succeeds)
- `git show --name-only --format= HEAD` lists exactly one path — CONFIRMED (the todo, and only the todo)
- All four md5-pinned locked regions (`uik` head, six-step recipe, `ihw`, `np3`) — CONFIRMED byte-identical to HEAD `60617c6b2` both after Task 1 and again after Task 2
- `pnpm planning-gates` — CONFIRMED 12/12 both pre- and post-edit
- No source, workflow, script, tag, or release touched — CONFIRMED (`git status --porcelain -- .github src-tauri src meta package.json` empty)
- No credential material in the new section — CONFIRMED (all three negative-control regexes from the plan's `<verify>` matched zero lines)
