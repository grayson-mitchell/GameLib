---
quick_id: 260912-873
type: execute
plan: 01
autonomous: true
files_modified:
  - src/frontend/assets/gog-logo.svg
  - src/frontend/screens/Game/GamePage/index.css
  - .planning/todos/pending/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md
  - .planning/todos/completed/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md
  - .planning/STATE.md
source_todo: .planning/todos/pending/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md

must_haves:
  truths:
    - 'gog-logo.svg is a single flat <svg><path/> with no <use>/<symbol> indirection'
    - 'The path `d` attribute is byte-identical to the pre-edit file (sha256 match)'
    - 'The root <svg> still carries class="gogIcon"'
    - 'Re-rendered at 192x192 the ink bbox is 192 wide x 175 tall with top gap ~8 and bottom gap ~8'
    - 'GamePage/index.css no longer contains an `&.gogIcon` padding override'
    - 'The owed live re-measure is recorded as NOT done, not claimed as a PASS'
  artifacts:
    - path: 'src/frontend/assets/gog-logo.svg'
      provides: 'Flat, centred GOG mark at its true 34:31 bounds'
      contains: 'class="gogIcon"'
    - path: 'src/frontend/screens/Game/GamePage/index.css'
      provides: 'Store icons all sharing the symmetric var(--space-2xs) padding'
    - path: '.planning/todos/completed/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md'
      provides: 'Closed todo recording that the headline "make it square" remedy was REJECTED on measurement'
  key_links:
    - from: 'src/frontend/assets/gog-logo.svg'
      to: 'src/frontend/screens/Humble/Keys/index.css:447'
      via: 'root class="gogIcon" matched by the scoped .humbleKeyRowStoreLogo .gogIcon fill escape'
      pattern: 'class="gogIcon"'
---

<objective>
Flatten `src/frontend/assets/gog-logo.svg` to a single `<svg><path/>` at its true `0 0 34 31`
bounds, dropping the `<use>`/`<symbol>` self-nesting, the JSX-ism `className`, and the
`xMidYMax` bottom-alignment that is the actual rendering defect. In the same change, delete the
`&.gogIcon` padding override in `GamePage/index.css` that exists only to hand-correct that
bottom-flush render.

Purpose: the GOG mark currently sits ~0.85 CSS px lower than the Steam mark beside it in the
Humble Keys row, and the GamePage compensates for that with an asymmetric 9px-wide override that
sizes GOG differently from every other store icon. Both fall out of one root cause.

Output: two source files changed, the source todo closed with the reason its own suggested
remedy was rejected, an honest record of the live gate still owed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260912-873-fix-gog-logo-svg-malformed-structure-and/260912-873-CONTEXT.md
@.planning/todos/pending/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md

CONTEXT.md is **locked and measured**. Do not re-derive it and do not re-litigate its §1
rejection of the todo's headline remedy.
</context>

<measured_baseline>
Verified by the planner at plan time. If any of these does not match what you see, STOP and
report the discrepancy rather than adapting silently.

**`src/frontend/assets/gog-logo.svg`** — 1271 bytes, 8 lines, tab-indented, ends with a
trailing newline. Current shape:

```
1  <svg class="gogIcon" viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg">
2  → <use href="#icon-logo-gog">
3  → → <symbol preserveAspectRatio="xMidYMax meet" viewBox="0 0 34 31" id="icon-logo-gog">
4  → → → <path className="cls-1" d="M31,31H3a3,3,…Z">
5  → → → </path>
6  → → </symbol>
7  → </use>
8  </svg>
```

The `d` attribute value is **1008 characters**, occupying bytes 222–1230 of the file.

**`src/frontend/screens/Game/GamePage/index.css`** — the block to delete is lines **618–621**
(a blank line plus the 3-line rule), inside `.store-icon > & > svg`:

```
615        height: 46px;
616        padding: var(--space-2xs);
617        border-radius: 10px;
618
619        &.gogIcon {
620          padding: 0px var(--space-3xs) var(--space-3xs);
621        }
622      }
623    }
```

After deletion, line 617 is followed directly by the closing `}`.

**Tooling present:** `rsvg-convert 2.62.3` at `/opt/homebrew/bin/rsvg-convert`. Python3 has
`zlib` but **no PIL/Pillow**; there is no `ffmpeg`, no ImageMagick. A pure-python PNG scanline
decode is the route (a `upng-js` transitive dep also exists in `node_modules` if you prefer JS,
but it is not a direct dependency — prefer the dependency-free python).

**Prettier baseline:** `src/frontend/screens/Game/GamePage/index.css` is **clean** at HEAD
(`npx prettier --check` on it alone → "All matched files use Prettier code style!").
</measured_baseline>

<traps>
Read these before starting. Each one has bitten this repo before.

1. **Prettier has no parser for `.svg`.** Naming `gog-logo.svg` explicitly on a prettier
   command line exits non-zero with `[error] No parser could be inferred`. That is not a
   formatting failure and it is **not** something to fix by adding a parser or an override.
   Prettier's repo-wide glob simply skips the file. Run the prettier check against the **CSS
   file only**.

2. **`pnpm prettier` is `prettier --check .` — repo-wide, and red in places at HEAD.** Do not
   run it as your gate and do not report its exit code as a regression you caused. Scope to the
   touched file and say so plainly.

3. **`themeTokens.test.ts` names `src/frontend/screens/Game/GamePage/index.css`** in its
   `NAVBAR_ACTIVE_CONSUMERS` list, and its doc comment cites GamePage lines `590, 619, 646`
   (34.11-era) → `642/671/698` (current). Line 619 is coincidentally the `&.gogIcon` line, and
   deleting those 4 lines shifts 642/671/698 up by 4. **The gate is content-regex on
   `--navbar-active`, not line numbers**, and the `&.gogIcon` block contains no such token — so
   it should pass unchanged. The comment *explicitly states* its line numbers are stale and
   "left as written so this comment stays a faithful record of what that review saw."
   **Do not helpfully update those numbers.** Prove the gate passes by running the test.

4. **`humbleKeysStylesheet.test.ts` counts `.gogIcon {` rules** — but only inside
   `src/frontend/screens/Humble/Keys/index.css`, which you are not touching. Do not introduce
   any new `.gogIcon` CSS rule anywhere as part of this change.

5. **`class="gogIcon"` on the root `<svg>` is load-bearing.** Dropping it re-opens the closed
   light-theme colour defect (CONTEXT.md §4, commit `c690a117a`). Nothing else in the file is.

6. **Do not retype or copy-paste the `d` string.** 1008 characters of coordinates; a slip is
   invisible at 19px and would ship a silently-wrong brand mark. Extract it with a script.

7. **`git mv` + a large content append defeats rename detection.** When closing the todo:
   edit the file contents **first**, commit-stage nothing, then run the `git mv` **alone**.

8. **`gsd-sdk query commit` stages the entire working tree.** Run `git status` before any
   commit and confirm nothing unrelated is swept in (there are untracked
   `.claude/skills/archify/` and `skills-lock.json` entries at session start).
</traps>

<tasks>

<task type="auto">
  <name>Task 1: Flatten gog-logo.svg with a programmatically-extracted `d`, and re-measure geometry</name>
  <files>src/frontend/assets/gog-logo.svg</files>
  <action>
Write a throwaway script (put it in the session scratchpad, not the repo) that does the whole
edit programmatically so the `d` value is never retyped:

  1. Read the ORIGINAL `src/frontend/assets/gog-logo.svg` and extract the `d` attribute value
     with a regex on `\sd="([^"]*)"`. Record `len(d)` and `sha256(d)`.
  2. Assert `len(d) == 1008` before proceeding. If it is not, stop — the file is not what the
     plan measured.
  3. Copy the ORIGINAL to the scratchpad as `gog-logo.svg.orig` so the byte-identity check in
     step 6 compares against a pre-edit artefact rather than against your own memory.
  4. Emit the new file by string composition, interpolating the extracted `d` verbatim:

     - Root element: `<svg class="gogIcon" viewBox="0 0 34 31" xmlns="http://www.w3.org/2000/svg">`
     - One child, indented with a single **tab**: `<path class="cls-1" d="{d}"/>`
     - Closing `</svg>` and a single trailing newline.

     Changes encoded here, all from CONTEXT.md §7: root viewBox `0 0 32 32` → `0 0 34 31` (the
     path's true bounds); `<use>`/`<symbol>` indirection deleted entirely; `className` →
     `class`; `preserveAspectRatio="xMidYMax meet"` **dropped**, so the default `xMidYMid meet`
     centres the glyph. `class="gogIcon"` survives on the root — see trap 5.

     Self-closing `<path …/>` is fine (the original used a redundant `></path>` pair). Tab
     indentation matches the rest of `src/frontend/assets/`.
  5. Write it.
  6. **Byte-identity gate:** re-extract `d` from the NEW file and assert its sha256 equals the
     sha256 captured from `gog-logo.svg.orig` in step 1. Print both hashes. If they differ,
     restore from the `.orig` copy and stop.

Then measure. Render both the original and the new file and compare ink bounding boxes:

     rsvg-convert -w 192 -h 192 -b white <svg> -o <png>

Decode each PNG in pure python (`zlib` + `struct`; handle the per-scanline filter bytes 0–4 —
rsvg emits RGBA8). Ink = any pixel that is not pure white. Report min/max ink column and row,
plus derived width, height, top gap and bottom gap, for BOTH renders side by side.

**Expected (from CONTEXT.md §8):**

| | before | after |
|---|---|---|
| ink width | 192 | 192 |
| ink height | 175 | 175 |
| top gap | 17 | ≈8 |
| bottom gap | 0 | ≈8 |

Height staying at **175** is the proof the 34:31 brand aspect was not distorted. Top and bottom
gaps equalising is the proof the bottom-flush alignment defect is fixed. A height that is not
175 means you stretched the mark — revert.

Paste the raw measured numbers into your notes for the SUMMARY. Do not round them into "looks
centred"; this repo's rule for this UI is measure, do not eyeball.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/frontend/assets/gog-logo.svg','utf8'); const fail=m=>{console.error('FAIL: '+m);process.exit(1)}; if(!/class=\"gogIcon\"/.test(s))fail('root class=gogIcon missing'); if(/className/.test(s))fail('className survives'); if(/&lt;use|&lt;symbol|<use|<symbol/.test(s))fail('use/symbol survives'); if(/preserveAspectRatio/.test(s))fail('preserveAspectRatio survives'); if(!/viewBox=\"0 0 34 31\"/.test(s))fail('root viewBox not 0 0 34 31'); const d=/\sd=\"([^\"]*)\"/.exec(s); if(!d||d[1].length!==1008)fail('d attr length is '+(d?d[1].length:'absent')+', expected 1008'); console.log('OK flat svg, d len '+d[1].length)"</automated>
  </verify>
  <done>
`gog-logo.svg` is a flat `<svg><path/>`; the `d` sha256 matches the pre-edit value; the
192×192 re-render measures width 192, height 175, top gap ≈8, bottom gap ≈8, with the raw
numbers recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the compensating `&amp;.gogIcon` override in GamePage/index.css</name>
  <files>src/frontend/screens/Game/GamePage/index.css</files>
  <action>
Delete lines **618–621** of `src/frontend/screens/Game/GamePage/index.css` — the blank line
plus the whole `&.gogIcon { padding: 0px var(--space-3xs) var(--space-3xs); }` rule — so line
617 (`border-radius: 10px;`) is followed directly by the closing `}` of the `& > svg` block.

Rationale (CONTEXT.md §6): that asymmetric override — no top padding, bottom padding only —
exists solely to lift the bottom-flush glyph off the floor. Once the asset centres itself the
override becomes a ~9px over-correction that pushes GOG up and sizes it differently from the
Epic/Steam/Amazon icons beside it, which all take the base symmetric `var(--space-2xs)`.
Leaving it in converts this fix into a new GamePage defect. GOG now inherits the base rule.

Confirm afterwards that `grep -n "gogIcon" src/frontend/screens/Game/GamePage/index.css`
returns **nothing**, and that `grep -rn "gogIcon" src/frontend --include='*.css'
--include='*.scss'` still shows the Humble Keys scoped escape and the global `_colors.scss`
rule — you must not have disturbed either (trap 4, trap 5).

Then run the gates:

  - `pnpm codecheck` (`tsc --noEmit`)
  - `pnpm lint` (`node meta/lintScoped.cjs`)
  - `npx prettier --check src/frontend/screens/Game/GamePage/index.css` — **this file only**.
    Baseline at HEAD was clean, so this must stay clean. Do NOT add the `.svg` to that command
    (trap 1) and do NOT run repo-wide `pnpm prettier` as the gate (trap 2).
  - The two stylesheet gates that name files in this blast radius:
    `npx jest --selectProjects frontend -t "themeTokens" --passWithNoTests` — or run the two
    suites by path, whichever the repo's jest config makes reliable:
    `src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts` and
    `src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts`.
    (`--selectProjects` is case-sensitive and exits 0 on no-match, so `--passWithNoTests` plus
    a check that the suite count is non-zero is mandatory — a green run over zero suites proves
    nothing. `-t` is a regex.)

Report each gate's result. If `pnpm lint` or `pnpm codecheck` is red at HEAD for reasons
unrelated to these two files, say so and name the baseline sha — do not assert "pre-existing"
without one.
  </action>
  <verify>
    <automated>grep -c "gogIcon" src/frontend/screens/Game/GamePage/index.css; test "$(grep -c 'gogIcon' src/frontend/screens/Game/GamePage/index.css)" = "0" &amp;&amp; npx prettier --check src/frontend/screens/Game/GamePage/index.css</automated>
  </verify>
  <done>
The `&.gogIcon` block is gone, the CSS is prettier-clean, `codecheck` and `lint` are reported
with an honest baseline, and both stylesheet suites ran with a non-zero suite count and passed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the todo on the rejected-remedy record, and file the owed live re-measure</name>
  <files>.planning/todos/pending/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md, .planning/todos/completed/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md, .planning/STATE.md</files>
  <action>
**Step A — edit the todo in place, while it is still in `pending/`** (trap 7: content edit
first, `git mv` alone afterwards, or rename detection is lost).

  - Frontmatter: `status: OPEN` → `status: RESOLVED`. Leave `severity: minor`,
    `platform: any`, `ready: code` exactly as they are — bare, lowercase, unquoted (CLAUDE.md's
    enforced triple). Add `src/frontend/screens/Game/GamePage/index.css` to the `files:` list,
    since that file shipped as part of the closure and the current list names only the SVG.
  - Append a `## Resolution` section that records, in this order:
    1. **The headline remedy was REJECTED on measurement.** The todo asked to "correct the
       `<symbol>`'s viewBox to a square aspect … so the glyph renders 19.0 × 19.0". It was not
       done, and a future reader must not re-file it. State why, with CONTEXT.md §1's numbers:
       the artwork is the gog.com wordmark in a rounded rect whose own path spans 0..34 × 0..31,
       so 34:31 is the **brand aspect, not a defect**; a square viewBox with `meet` is a no-op
       (ink stays 34:31, just letterboxed, and re-measures 19.0 × 17.5 again), and forcing a
       true square needs `preserveAspectRatio="… none"` — a ~9.7% vertical stretch that turns
       the rounded corners into ellipses. Note that `steam-logo.svg` (`0 0 496 512`) and
       `amazon-logo.svg` (`0 0 448 512`) are also non-square, so "square" was never the house
       convention.
    2. **What the same measurement DID expose, and what shipped.** `xMidYMax` = bottom-aligned,
       giving top gap 17 / bottom gap 0 in a 192 box, i.e. the mark sitting ~0.85 CSS px lower
       than a centred glyph next to the vertically-centred Steam logo. Fixed by flattening to
       a root `viewBox="0 0 34 31"` with the default `xMidYMid meet`. Quote the before/after
       ink bbox table from Task 1 with the real measured numbers.
    3. The two structural malformations the todo also recorded (`<use>`/`<symbol>` self-nesting,
       `className` → `class`) — both fixed in the same edit.
    4. The GamePage `&.gogIcon` override deletion and why it had to ship in the same change.
    5. **Verification, stated honestly.** Desk measurements + which gates ran and their scope.
       Say explicitly that the prettier check was scoped to the CSS file because `.svg` has no
       prettier parser, and that repo-wide prettier is red at HEAD independent of this change.
  - **Step B — the live gate is OWED and was NOT run.** State plainly, in the Resolution
    section, that no live app run was taken: the Humble Keys row and the GamePage store-icon
    row both need a re-measure in a live render (both themes) to confirm the centring lands and
    that GOG now matches its neighbours' size on the GamePage. Do **not** write "PASS", do not
    write "verified live", do not imply it. This todo carried `ready: code`, so the live gate is
    a follow-up, not a blocker.

    Then file that follow-up as a new todo in `.planning/todos/pending/` carrying all three
    CLAUDE.md-mandated keys in order — `severity:` then `platform:` then `ready:`, bare and
    lowercase:

        severity: minor
        platform: macos
        ready: live-gate

    plus `created`, `title`, `area`, `files`, `resolves_phase: null`. Its body names the two
    surfaces to re-measure and the expected numbers (Humble Keys row: GOG ink 19.2 × 17.5 with
    ~0.85px above and ~0.85px below inside the 19.2px box, vertically centred against the
    neighbouring Steam mark; GamePage: GOG the same rendered size as Epic/Steam/Amazon, all on
    symmetric `var(--space-2xs)`).

**Step C — move the todo.** `git mv .planning/todos/pending/<file> .planning/todos/completed/`
as its own operation, with no other staged content.

**Step D — `pnpm planning-gates`** must pass (it enforces the pending-todo frontmatter triple
on your new follow-up todo; `completed/` is exempt).

**Step E — SUMMARY + STATE row.** Write the quick-task SUMMARY into
`.planning/quick/260912-873-fix-gog-logo-svg-malformed-structure-and/` and add the row to the
`### Quick Tasks Completed` table in `.planning/STATE.md` (the table starts at line 5548;
append after the most recent row, currently `260912-7o4`). The SUMMARY must carry the same
honest live-gate statement — measured desk numbers, live re-measure NOT taken — and must record
that the todo's own suggested fix was rejected.

Before committing anything: `git status`, and confirm the untracked `.claude/skills/archify/`
and `skills-lock.json` are not swept in (trap 8).
  </action>
  <verify>
    <automated>test -f .planning/todos/completed/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md &amp;&amp; test ! -f .planning/todos/pending/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md &amp;&amp; grep -q "^status: RESOLVED" .planning/todos/completed/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md &amp;&amp; pnpm planning-gates</automated>
  </verify>
  <done>
The todo sits in `completed/` with `status: RESOLVED` and a Resolution section that names the
rejected remedy and its measurements; a new `ready: live-gate` todo carrying the
severity/platform/ready triple sits in `pending/`; `pnpm planning-gates` passes; SUMMARY written
and the STATE.md quick-task row added.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Static asset + stylesheet edit. No input parsing, no network, no new dependency, no package install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-873-01 | Tampering | `d` attribute of `gog-logo.svg` | mitigate | Extract programmatically and gate on a sha256 match against a pre-edit copy (Task 1 step 6). A silent coordinate slip is invisible at 19px and would ship a corrupted brand mark. |
| T-873-02 | Tampering | root `class="gogIcon"` | mitigate | Automated verify greps for `class="gogIcon"`; losing it re-opens the closed light-theme fill defect (commit `c690a117a`). |
| T-873-SC | Tampering | package installs | n/a | No package-manager install in this task. No legitimacy gate needed. |
</threat_model>

<verification>
1. `gog-logo.svg` is flat, `d` sha256 unchanged, root `class="gogIcon"` and `viewBox="0 0 34 31"` present, no `className`/`<use>`/`<symbol>`/`preserveAspectRatio`.
2. 192×192 re-render: ink width 192, height **175**, top gap ≈8, bottom gap ≈8 (was 17/0).
3. `grep -n "gogIcon" src/frontend/screens/Game/GamePage/index.css` → no matches.
4. `npx prettier --check src/frontend/screens/Game/GamePage/index.css` → clean.
5. `pnpm codecheck`, `pnpm lint` → reported with an honest baseline sha if red for unrelated reasons.
6. `themeTokens.test.ts` and `humbleKeysStylesheet.test.ts` pass with a **non-zero suite count**.
7. `pnpm planning-gates` passes.
8. Live re-measure recorded as OWED and NOT taken, in both the SUMMARY and a `ready: live-gate` follow-up todo.
</verification>

<success_criteria>
- The GOG mark renders centred at its true 34:31 aspect, proven by measurement, not by eye.
- GamePage store icons all share the symmetric `var(--space-2xs)` padding.
- The closed todo makes it impossible for a future reader to re-file "make it square" without
  first reading why it was rejected.
- No claim of a live PASS appears anywhere in the artefacts.
</success_criteria>

<output>
Create `.planning/quick/260912-873-fix-gog-logo-svg-malformed-structure-and/260912-873-SUMMARY.md` when done.
</output>
