---
quick_id: 260827-t9c
mode: quick-full
type: execute
title: Fix Phase 34.11's five correctness residual review warnings (WR-01, WR-05, WR-06, WR-09, WR-12) and reconcile both ledgers
phase: 34.11-library-filtering-search-views-collections-and-cross-store-f
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [WR-01, WR-05, WR-06, WR-09, WR-12]
files_modified:
  - src/frontend/components/UI/NavShell/components/FilterRunnabilityFacet/index.tsx
  - src/frontend/components/UI/NavShell/__tests__/FilterRunnabilityFacet.test.tsx
  - src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts
  - src/frontend/screens/Library/components/FilterZeroResult/index.tsx
  - src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx
  - src/frontend/screens/Library/index.tsx
  - src/frontend/screens/Library/engineWiring.ts
  - src/frontend/screens/Library/__tests__/engineWiring.test.ts
  - src/frontend/screens/Library/__tests__/libraryPipeline.test.ts
  - src/frontend/screens/Library/__tests__/libraryHookStaleness.test.ts
  - .planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md
  - .planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md

must_haves:
  truths:
    - "A drift between FilterRunnabilityFacet's literal switch and RUNNABILITY_LABELS produces a NAMED test failure, not a renderer-killing throw at module import (WR-06)."
    - "The libraryUnion memo recomputes when a login/logout changes only a username or user_id — every gate makeLibrary reads is in its dependency chain (WR-01)."
    - "eslint on src/frontend/screens/Library/index.tsx no longer reports `react-hooks/exhaustive-deps` for the libraryUnion memo, and no eslint-disable was added anywhere (WR-01)."
    - "Launching a game during a session updates recentAppNames without remounting the Library, driven by the same recentGamesChanged signal RecentlyPlayed/index.tsx already uses (WR-05)."
    - "A currentCollection naming a renamed or deleted category is cleared instead of filtering the entire library away (WR-09)."
    - "A descriptor whose chipLabelSpec is null cannot inflate activeFilterCount, and the zero-result body can never render an empty filter list (WR-12)."
    - "The shared-union invariant is intact: buildGridPipeline is still called exactly once, and the grid and both facet counts still read the same unfiltered union (CR-01 not regressed)."
    - "34.11-REVIEW-FIX.md and the pending todo state the SAME open count (5), with status partial / pending respectively."
  artifacts:
    - path: "src/frontend/screens/Library/__tests__/libraryHookStaleness.test.ts"
      provides: "Dependency-completeness gate for the libraryUnion callback (WR-01) and the currentCollection validation wiring gate (WR-09), each with an explicit non-vacuity proof"
      contains: "libraryUnion"
    - path: "src/frontend/screens/Library/engineWiring.ts"
      provides: "collectionIsStale() — pure, exported, behaviourally testable (WR-09)"
      exports: ["collectionIsStale"]
    - path: "src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts"
      provides: "renderableActiveFilters() — the single chipLabelSpec survivor filter shared by index.tsx, FilterChipRow and FilterZeroResult (WR-12)"
      exports: ["renderableActiveFilters"]
    - path: ".planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md"
      provides: "Reconciled fix ledger: fixed 17 / open 5 / invalid 1 / total 23, status partial"
      contains: "260827-t9c"
  key_links:
    - from: "src/frontend/screens/Library/index.tsx"
      to: "makeLibrary login gates"
      via: "useCallback dependency array containing every gate identifier makeLibrary reads"
      pattern: "makeLibrary = useCallback"
    - from: "src/frontend/screens/Library/index.tsx"
      to: "window.api.handleRecentGamesChanged"
      via: "useEffect subscription that re-reads configStore games.recent"
      pattern: "handleRecentGamesChanged"
    - from: "src/frontend/screens/Library/index.tsx"
      to: "collectionIsStale"
      via: "useEffect calling setCurrentCollectionPersisted(null)"
      pattern: "collectionIsStale\\("
    - from: "src/frontend/screens/Library/index.tsx"
      to: "renderableActiveFilters"
      via: "activeFilterDescriptors memo, before activeFilterCount is derived from .length"
      pattern: "renderableActiveFilters\\("
---

<objective>
Close the five **correctness** residual Warning findings from `34.11-REVIEW.md` — WR-01,
WR-05, WR-06, WR-09, WR-12 — each with a regression test that fails against the current
code, then reconcile `34.11-REVIEW-FIX.md` and the pending residual todo so both state the
same open count.

Purpose: these five are the only residual findings that are behavioural defects with a
known fix. The other five open findings are a UX judgement (WR-08), a test-quality debt
(WR-16), two decisions frozen by D-08 (WR-10/WR-11) and a gate-design decision (WR-18) —
all out of scope here and all still open afterwards.

Output: five source fixes, four test files (three extended, one new), two reconciled
ledgers.

**Expected end state, stated up front so a drift is detectable:** `34.11-REVIEW-FIX.md`
frontmatter `fixed: 17`, `open: 5`, `invalid: 1`, `total: 23`, `criticals_open: 0`,
`status: partial`. The pending todo keeps `status: pending` and carries exactly five
bullets: WR-18, WR-08, WR-16, WR-10, WR-11. **Both documents must say five.**
</objective>

<context>
@.planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW.md
@src/frontend/screens/Library/index.tsx
@src/frontend/screens/Library/engineWiring.ts
@src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts
@src/frontend/screens/Library/components/FilterZeroResult/index.tsx
@src/frontend/components/UI/NavShell/components/FilterRunnabilityFacet/index.tsx
@src/frontend/screens/Library/__tests__/libraryPipeline.test.ts
@src/frontend/screens/Library/__tests__/connectedStoresParity.test.ts
@src/frontend/components/UI/NavShell/__tests__/FilterRunnabilityFacet.test.tsx

<measured_baseline>
<!-- Taken during planning, 2026-08-27, against the working tree. These are the numbers
     the verify steps compare against. Re-locate every landmark BY SYMBOL — quick task
     260827-s8z deleted ~90 lines from Library/index.tsx and every line number printed in
     34.11-REVIEW.md and in the todo is STALE. -->

`pnpm exec eslint src/frontend/screens/Library/index.tsx` -> **23 warnings, 0 errors**.
Among them, WR-01's own tripwire, live and confirmed:

    578:5  warning  React Hook useMemo has a missing dependency: 'makeLibrary'.
                    Either include it or remove the dependency array   react-hooks/exhaustive-deps

Also live at `651:5`: "unnecessary dependency: 'reconcileTick'". That one is **deliberate**
— the `engineDeps` memo comment documents `reconcileTick` as a pure invalidation token for
the 2026-08-21 steam-library-22-games-missing fix. **Do not touch it.** Expected post-fix
count is **22 warnings, 0 errors**, differing from the baseline by exactly the `:578` line.

Current symbol landmarks (line numbers will move as you edit):
- `const [currentCollection, setCurrentCollection_] = useState<string | null>(` ~`:165`
- `setCurrentCollectionPersisted` ~`:168`
- `const recentAppNames = useMemo(` ~`:191`, deps `[]`
- `const connectedStores: StoreFacetValue[] = useMemo(` ~`:208` (WR-03's fix — reads `amazon.user_id`)
- `const makeLibrary = () => {` ~`:543`
- `const libraryUnion = useMemo(` ~`:576`, deps are the five `.library` arrays + `sideloadedLibrary`
- `const activeFilterDescriptors = useMemo(` ~`:794`; `const activeFilterCount = activeFilterDescriptors.length` ~`:798`
- `customCategories` destructured ~`:100`; `customCategories.list` already passed into `engineDeps` ~`:646`
</measured_baseline>

<environment_facts>
- **No jsdom, no react-test-renderer in this project.** `src/frontend/jest.config.js` says so
  explicitly. Frontend component tests invoke the component as a plain function and inspect
  the returned element graph, or read source text. Plan every regression test for that
  world — do not propose a render/hook test.
- Jest project displayName is **`Frontend`** (capital F). `--selectProjects` is
  case-sensitive and exits 0 on a miss, so **run jest by test PATH**, never by project name.
- `libraryPipeline.test.ts`'s `functionRegion()` **throws** when its needle is missing
  (`throw new Error(\`${startNeedle} not found\`)`), so changing `makeLibrary`'s declaration
  fails that suite LOUDLY rather than fail-open. Its needle is the literal string
  `'const makeLibrary = () => {'` and **must be updated in the same task**.
- `connectedStoresParity.test.ts` reads `makeLibrary`'s gates with
  `/const\s+show([A-Z][a-zA-Z]*)\s*=\s*(.+)/g` over the WHOLE file. Wrapping `makeLibrary`
  in `useCallback` leaves the `const showEpic = ...` lines untouched, so that parser is
  unaffected — but re-run the suite to prove it, do not assume.
- `chipLabelSpec` returns `null` for: a `view` value outside
  `installed|recentlyPlayed|favourites` (including a forged `all`), a `store` value outside
  `RunnerToStore`, a `runnability` tier outside `RUNNABILITY_LABELS`, or a `kind` outside
  the union. A retired/forged `libraryView` in `localStorage` is the reachable production
  path for WR-12.
- `RecentlyPlayed/index.tsx` refreshes itself via
  `window.api.handleRecentGamesChanged(onRecentGamesUpdated)`
  (`src/preload/api/library.ts:16`, `frontendListenerSlot('recentGamesChanged')`), with the
  returned function used as the effect cleanup.
</environment_facts>

<hazards>
1. **Line numbers are stale.** Re-locate by symbol. Never `sed -n` a number out of the review.
2. **WR-01 has blast radius.** `libraryUnion` feeds BOTH the grid and the facet counts — that
   shared-union property IS CR-01's fix, pinned by `engineWiring.test.ts`
   (`countForStore('steam') === 3` with `gog` selected) and by `libraryPipeline.test.ts`
   (`buildGridPipeline(` appears exactly once). Widening a dependency array must not touch
   the single-call structure.
3. **`react-hooks/exhaustive-deps` is a WARNING here, not an error.** An `eslint-disable` is
   not a fix, and on this project a disable naming a dead rule suppresses nothing. **Add no
   suppression of any kind.**
4. **A test that cannot fail on the bug it guards is this phase's recurring defect.** Every
   new assertion below carries a stated failing assertion against current code plus a
   non-vacuity proof.
5. **`pnpm codecheck` says nothing about CI lint.** Run `pnpm exec eslint` on changed files
   separately.
6. **Never run a full `pnpm test`** — it manufactures a different failure set under load. Run
   jest by test path only.
7. **Jest can exit 0 having run nothing.** Every jest step must assert a non-zero test count.
8. **A meta gate is ALREADY RED and is NOT ours:** `meta/__tests__/genI18nGateScope.test.ts`
   (~`:415`) fails on `i18nForkTouchedFiles.json` drift from Phase 34.17 (`29ccffa4b`) and
   quick `260824-u8b`. Do not fix it, do not run `pnpm gen-i18n-gate-scope`, do not
   regenerate `meta/i18nGateScope.json`. No task here needs the meta project at all.
9. **No `git stash`, no `git reset`, no `git checkout -- <file>`** (post-checkout hook fires
   and breaks helper binaries). Revert via `git show HEAD:<path> > <path>`. A CONCURRENT
   session is committing 34.13 work to this same branch — commit only your own paths, never
   `git add -A`.
10. **Do not push.** `.husky/pre-push` is red repo-wide.
11. New user-facing strings go in `gamelib.json`, NEVER `translation.json`.
    **As specified below, none of these five fixes adds a user-facing string** — WR-12 is
    closed by suppressing an unrenderable sentence, not by writing a new one. If an executor
    finds itself needing one, that is a signal the design drifted; stop and re-read WR-12.
</hazards>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-06 (module-scope throw) and WR-12 (unrenderable descriptors inflate the count)</name>
  <files>
src/frontend/components/UI/NavShell/components/FilterRunnabilityFacet/index.tsx,
src/frontend/components/UI/NavShell/__tests__/FilterRunnabilityFacet.test.tsx,
src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts,
src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx,
src/frontend/screens/Library/components/FilterZeroResult/index.tsx,
src/frontend/screens/Library/index.tsx
  </files>

  <behavior>
WR-06, in `FilterRunnabilityFacet.test.tsx`:
  - Test A (new, `it.each` over `Object.keys(RUNNABILITY_LABELS)`): for every tier,
    `runnabilityLabel(tier, echoDefaultValue) === RUNNABILITY_LABELS[tier][1]`. Fails today
    only in the sense that the symbols are not exported; after the move it is the drift
    alarm the module-scope `throw` used to be, and it names the tier that drifted.
  - Test B (non-vacuity): assert the `it.each` table has exactly 4 entries, so deleting
    `RUNNABILITY_LABELS` members cannot silently empty the table.
  - Test C (new, added to the existing `medal/derivation source gate` describe, which
    already holds `source = readFileSync(SOURCE_PATH)`): assert the component source
    contains **no** `throw` — matched as `/\bthrow\b/`, **not** `/^throw/`. The `^`-anchored
    form is precisely how an earlier pass wrongly reported this finding closed: the `throw`
    is indented inside `if (process.env.NODE_ENV !== 'production')`. **Failing assertion
    against current code: `expect(source).not.toMatch(/\bthrow\b/)` is RED today.**
    Pair it with the file's existing non-vacuity idiom (`const knownBad = source + '\nthrow'`
    must match) so the gate is proven able to fail.

WR-12, in `FilterChipRow/__tests__/index.test.tsx` (or a sibling `__tests__` file under
`FilterChipRow/` if that file is already crowded — it must live under a `__tests__/`
directory to be collected):
  - Test D: `renderableActiveFilters(describeActiveFilters({ ...DEFAULT_FILTER_ENGINE_STATE,
    view: 'retiredView' as LibraryView }, ''))` returns `[]`. **Failing assertion against
    current code: today `describeActiveFilters` returns one descriptor and nothing filters
    it, so `activeFilterCount` would be 1 with zero renderable labels.** Assert both halves:
    `describeActiveFilters(...)` has length 1 (the input really does produce a descriptor —
    non-vacuity) AND `renderableActiveFilters(...)` has length 0.
  - Test E: a control — a state with a REAL filter (e.g. `view: 'installed'`) survives
    `renderableActiveFilters` unchanged, so the helper is not a blanket `[]`.
  - Test F: `renderableActiveFilters` is referentially the same filter both consumers apply
    — assert `renderableActiveFilters(list)` equals
    `list.filter(d => chipLabelSpec(d) !== null)` for a mixed list of one renderable and one
    unrenderable descriptor.
  </behavior>

  <action>
**WR-06 — move the assertion into the test suite.**
In `FilterRunnabilityFacet/index.tsx`: add `export` to `runnabilityLabel` and to
`echoDefaultValue`, and DELETE the entire `if (process.env.NODE_ENV !== 'production') { ... }`
block including its `throw`. Rewrite the block's justification into the header comment above
`runnabilityLabel` so the reason the switch holds literal copy survives the deletion —
replace the sentence beginning "The development-only check beneath the switch throws if..."
with one naming the test that now owns the drift alarm. Keep `echoDefaultValue`'s own comment
(it explains why no second copy of the English text lives in this file, which is a
hardcoded-string-gate concern independent of WR-06).
In `FilterRunnabilityFacet.test.tsx`: import `{ runnabilityLabel, echoDefaultValue }` as named
imports alongside the existing default import, import `RUNNABILITY_LABELS` from
`frontend/screens/Library/facetLabels`, and add Tests A/B in a new
`describe('literal default text matches facetLabels.ts (WR-06)')`. Add Test C to the existing
source-gate describe. **Add no English string literals to the test file** — read every
expected text out of `RUNNABILITY_LABELS`; that file is inside the hardcoded-string gate's
scan scope.

**WR-12 — derive the count from the renderable survivors.**
In `chipLabels.ts` (which must stay React-free — no `react`/`react-i18next` import), export:

    export function renderableActiveFilters(
      descriptors: ActiveFilterDescriptor[]
    ): ActiveFilterDescriptor[]

implemented as `descriptors.filter((d) => chipLabelSpec(d) !== null)`. Give it a header
comment citing WR-12 and stating the invariant it buys: `activeFilterCount` counts only
descriptors that can be named, so a count greater than zero always implies at least one
label. This is the review's PRIMARY recommendation ("filter descriptors through
`chipLabelSpec` once in `Library/index.tsx` before publishing them, and derive
`activeFilterCount` from the survivor list").

In `Library/index.tsx`, wrap the existing descriptor memo:

    const activeFilterDescriptors = useMemo(
      () => renderableActiveFilters(
        filterEngine.describeActiveFilters(engineState, filterText)
      ),
      [engineState, filterText]
    )

`activeFilterCount` stays `activeFilterDescriptors.length` — D-26's "one call, the count IS
that list's length" property is preserved, and is now true of a list every consumer can
render. Extend the D-26 comment above the memo to say so. Import `renderableActiveFilters`
from `./components/FilterChipRow/chipLabels`.

In `FilterZeroResult/index.tsx`: keep the existing `chipLabelSpec` null-filter (it predates
the review, `726b96f93`, and is now redundant but harmless — leaving it makes the component
correct when mounted against a hand-built context in a test). ADD the review's second
recommendation immediately after `labels` is computed:

    if (labels.length === 0) {
      return null
    }

with a comment naming WR-12 and stating it as defence in depth: `activeFilterCount` can no
longer be positive with empty labels, and this guard makes `"No games match ."`
unrepresentable at the render site regardless of what a caller supplies. Note that
`FilterChipRow` needs no change — a bare `Clear all` chip with no chips is the same defect
and is closed by the same upstream filter.

**Do not** add a `gamelib.json` string: the fix suppresses an unrenderable sentence rather
than authoring a new one.
  </action>

  <verify>
    <automated>
pnpm exec jest --config jest.config.js src/frontend/components/UI/NavShell/__tests__/FilterRunnabilityFacet.test.tsx src/frontend/screens/Library/components/FilterChipRow/__tests__ --ci 2>&1 | tee /tmp/t9c-task1.log; grep -Eq 'Tests:[^0-9]*[1-9][0-9]* passed' /tmp/t9c-task1.log && ! grep -q 'failed' /tmp/t9c-task1.log
    </automated>
    <automated>
# WR-06 gate is non-vacuous and correctly anchored: the indented throw must be gone.
grep -c 'throw' src/frontend/components/UI/NavShell/components/FilterRunnabilityFacet/index.tsx | grep -qx 0
    </automated>
    <automated>
# WR-12 wiring is real, not just present in chipLabels.ts.
grep -q 'renderableActiveFilters(' src/frontend/screens/Library/index.tsx && grep -q 'labels.length === 0' src/frontend/screens/Library/components/FilterZeroResult/index.tsx
    </automated>
    <automated>
# chipLabels.ts stays React-free (WR-07's landed invariant).
! grep -Eq "from 'react(-i18next)?'" src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts
    </automated>
    <automated>
pnpm exec eslint src/frontend/components/UI/NavShell/components/FilterRunnabilityFacet/index.tsx src/frontend/components/UI/NavShell/__tests__/FilterRunnabilityFacet.test.tsx src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts src/frontend/screens/Library/components/FilterZeroResult/index.tsx 2>&1 | tee /tmp/t9c-lint1.log; ! grep -q ' error ' /tmp/t9c-lint1.log
    </automated>
  </verify>

  <done>
`FilterRunnabilityFacet/index.tsx` contains zero occurrences of `throw`; `runnabilityLabel`
and `echoDefaultValue` are exported and the drift alarm is an `it.each` producing a
tier-named failure. `renderableActiveFilters` exists in `chipLabels.ts`, is applied in
`Library/index.tsx` before `activeFilterCount` is derived, and `FilterZeroResult` returns
`null` on empty labels. Test D fails against the pre-fix code by construction (one
descriptor in, zero labels out) and passes now. No eslint errors; no new English string
literal in any of the four files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 (memo dependency completeness), WR-05 (recentAppNames frozen at mount), WR-09 (stale currentCollection)</name>
  <files>
src/frontend/screens/Library/index.tsx,
src/frontend/screens/Library/engineWiring.ts,
src/frontend/screens/Library/__tests__/engineWiring.test.ts,
src/frontend/screens/Library/__tests__/libraryPipeline.test.ts,
src/frontend/screens/Library/__tests__/libraryHookStaleness.test.ts
  </files>

  <behavior>
New file `src/frontend/screens/Library/__tests__/libraryHookStaleness.test.ts`. It follows
`connectedStoresParity.test.ts`'s established idiom exactly: `readFileSync` +
`stripSourceComments` over `Library/index.tsx`, brace-counted region extraction, and a
NON-VACUITY assertion before every parity assertion. There is no jsdom in this project, so a
source-derived gate is the only instrument available for a dependency-array defect — but it
is a gate that fails on the real bug, because the bug IS "the set of identifiers read inside
the callback is not a subset of the set listed in its dependency array".

  - Test G (WR-01, the failing one): extract `makeLibrary`'s body; collect every
    `<object>.<prop>` and bare-identifier read that comes from component props/state —
    concretely the twelve: `epic.username`, `epic.library`, `gog.username`, `gog.library`,
    `amazon.user_id`, `amazon.library`, `zoom.enabled`, `zoom.username`, `zoom.library`,
    `steam?.username`, `steam?.library`, `sideloadedLibrary`. Extract the dependency array of
    the `useCallback` that wraps `makeLibrary`. Assert the read set is a SUBSET of the
    declared dependency set (normalising `?.` to `.` and stripping whitespace, the same
    `normalise()` shape `connectedStoresParity.test.ts` uses).
    **Failing assertion against current code:** today there is no `useCallback` at all and
    the `libraryUnion` memo lists only six of the twelve, so this test is RED — it reports
    the six missing login-gate identifiers by name.
  - Test H (WR-01 non-vacuity): assert the extracted read set has exactly 12 members and
    contains `epic.username`. Without this, deleting `makeLibrary`'s body would make Test G
    vacuously green (`[].every(...)` is `true`) — the exact fail-open shape this project has
    hit before.
  - Test I (WR-01 known-bad): run the same subset predicate against a synthetic dependency
    list with `epic.username` removed and assert it returns FALSE. This proves the predicate
    can fail independently of the file's current contents.
  - Test J (WR-05): assert `Library/index.tsx` subscribes to `handleRecentGamesChanged` and
    that the `recentAppNames` state is not a `useMemo` with an empty dependency array —
    matched as the absence of `/recentAppNames[\s\S]{0,200}?configStore\.get\([\s\S]{0,120}?\[\]\s*\)/`
    only if you can write it unambiguously; otherwise assert positively: the file contains
    `handleRecentGamesChanged` AND `setRecentAppNames`. Prove non-vacuity with a known-bad
    string that lacks both.
  - Test K (WR-09, behavioural, in `engineWiring.test.ts`): `collectionIsStale` truth table —
    `(null, {...})` false; `('preset_uncategorized', {})` false; `('Shooters', { Shooters: [] })`
    false; `('Shooters', { Shooters2: [] })` **true**; `('Shooters', {})` **true**. The
    `preset_uncategorized` row is the one that matters: a naive `!(x in list)` implementation
    would clear the sentinel and silently retire the Uncategorized view.
  - Test L (WR-09, wiring, in `libraryHookStaleness.test.ts`): assert `Library/index.tsx`
    contains a `collectionIsStale(` call and a `setCurrentCollectionPersisted(null)` call, and
    prove non-vacuity against a known-bad copy with both stripped. A behavioural unit test of
    a brand-new helper passes trivially and proves nothing about the effect being mounted;
    this pairs with it.
  </behavior>

  <action>
**WR-01 — complete the dependency chain without suppressing the rule.**
Take the review's FIRST option (extract into a `useCallback` with the complete dependency
list and depend on that), not its second (add six values to the memo's array). Rationale to
record in the code comment: the second option leaves
`react-hooks/exhaustive-deps: missing dependency 'makeLibrary'` live at the memo, and that
warning is the finding's own tripwire — a fix that leaves the tripwire tripped has not closed
the finding.

In `Library/index.tsx`:
- Change `const makeLibrary = () => {` to `const makeLibrary = useCallback(() => {` and close
  it with `}, [ ...twelve identifiers... ])`, listing every gate and library it reads:
  `epic.username, epic.library, gog.username, gog.library, amazon.user_id, amazon.library,
  zoom.enabled, zoom.username, zoom.library, steam?.username, steam?.library,
  sideloadedLibrary`. Use `amazon.user_id` — **not** `amazon.username`; WR-03's fix
  (`5472fb015`) aligned `connectedStores` and `makeLibrary` on `user_id` and
  `connectedStoresParity.test.ts` fails if they split again.
- Change the `libraryUnion` memo to `useMemo(() => makeLibrary(), [makeLibrary])`.
- REWRITE the comment above `libraryUnion` that currently says the dependency list is carried
  over verbatim "including its known incompleteness ... a separate, still-open review finding
  (WR-01) ... the warning ... must stay visible". That text is now false. Replace it with the
  closure: WR-01 closed by quick `260827-t9c`; the memo depends on the callback, the callback
  declares every gate it reads, no suppression was added.
- Add `useCallback` to the existing `react` import if absent.
- **Do not** change the `buildGridPipeline` call, the `gridPipeline.games` read, or the
  `{ countForStore, countForRunnability } = gridPipeline` destructure. The shared-union
  invariant is structural and must survive byte-identical.

In `libraryPipeline.test.ts`, update the ONE needle
`functionRegion(libraryTsx, 'const makeLibrary = () => {')` to
`'const makeLibrary = useCallback(() => {'`. `functionRegion` throws on a missing needle, so
skipping this turns that suite red, not silently green — but update it deliberately rather
than discovering it. Add a one-line comment recording why the needle changed.

**WR-05 — refresh on the same signal `RecentlyPlayed` uses.**
Replace `const recentAppNames = useMemo(() => configStore.get('games.recent', []).map(...), [])`
with `useState` seeded from the same read plus a `useEffect` that re-reads on
`window.api.handleRecentGamesChanged`, using the returned unsubscribe function as the effect
cleanup — the identical pattern at `RecentlyPlayed/index.tsx` (`handleRecentGamesChanged` ->
`loadRecentGames`, cleanup calls the returned remover). Keep the existing comment's claim
("mirrors RecentlyPlayed/index.tsx's own read") true by mirroring its refresh too.

**REJECT the review's fallback for WR-05 and say so in the comment.** The review offered "at
minimum add `libraryStatus` to the dependency array". `libraryStatus` is the install/download
status array — it changes on every progress tick, so depending on it would recompute
`recentAppNames` (and, through `engineDeps`, invalidate downstream memos) continuously
during any download: a re-render storm in exchange for a value that only changes when a game
is launched or stopped. `handleRecentGamesChanged` is the review's own primary
recommendation and fires exactly on the real event. Record this two-sentence rationale in the
code comment so the rejection is on the record rather than looking like an oversight.

**WR-09 — validate `currentCollection` on read and on category-list change.**
In `engineWiring.ts`, add and export:

    export function collectionIsStale(
      collection: string | null,
      customCategories: Record<string, string[]>
    ): boolean

returning `false` for `null`, `false` for `PRESET_UNCATEGORIZED` (imported by identifier from
`./filterEngine` — CR-04's fix exports it once and a test asserts it is imported, never
re-declared; check the exact `customCategories` value type against `buildEngineDeps`'s
`EngineDepsInputs` and match it rather than inventing one), and `!(collection in
customCategories)` otherwise.
In `Library/index.tsx`, add the effect below `setCurrentCollectionPersisted`:

    useEffect(() => {
      if (collectionIsStale(currentCollection, customCategories.list)) {
        setCurrentCollectionPersisted(null)
      }
    }, [currentCollection, customCategories.list])

Comment it with WR-09 and the user-visible failure it prevents: a renamed or deleted category
leaves `passesCollection` false for every game, so the grid is empty, the zero-result sentence
names a collection that no longer exists, and the Collections list has no row to click to
clear it — an unrecoverable state reachable by ordinary use of `CategoriesManager`.
`setCurrentCollectionPersisted` is a plain function redefined each render; do not add it to the
dependency array (that would loop). If eslint asks for it, extract it with `useCallback`
rather than suppressing — an eslint-disable is out of bounds for this whole task.
  </action>

  <verify>
    <automated>
pnpm exec jest --config jest.config.js src/frontend/screens/Library/__tests__ --ci 2>&1 | tee /tmp/t9c-task2.log; grep -Eq 'Tests:[^0-9]*[1-9][0-9]* passed' /tmp/t9c-task2.log && ! grep -q 'failed' /tmp/t9c-task2.log
    </automated>
    <automated>
# CR-01's shared-union invariant is intact: exactly one buildGridPipeline call.
test "$(grep -c 'buildGridPipeline(' src/frontend/screens/Library/index.tsx)" = "1"
    </automated>
    <automated>
# WR-01's tripwire is gone and NOTHING was suppressed. Baseline was 23 warnings / 0 errors.
pnpm exec eslint src/frontend/screens/Library/index.tsx 2>&1 | tee /tmp/t9c-lint2.log; ! grep -q "missing dependency: 'makeLibrary'" /tmp/t9c-lint2.log && grep -Eq '\(0 errors' /tmp/t9c-lint2.log && N=$(grep -Eo '[0-9]+ warnings' /tmp/t9c-lint2.log | grep -Eo '[0-9]+') && [ "$N" -le 23 ] && echo "warnings now $N (baseline 23, expected 22)"
# Expected 22. 23 or fewer with the makeLibrary line gone means the WR-05/WR-09 edits
# introduced one new warning -- ACCEPTABLE ONLY IF NAMED in the SUMMARY with its rule and
# line. More than 23, or any error, is a regression: fix it, do not suppress it.
    </automated>
    <automated>
# No suppression of any kind was introduced in any file this task touched.
! git diff -U0 -- src/frontend/screens/Library/ | grep -E '^\+' | grep -q 'eslint-disable'
    </automated>
    <automated>
pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | tee /tmp/t9c-tsc.log; ! grep -q 'error TS' /tmp/t9c-tsc.log
    </automated>
  </verify>

  <done>
`makeLibrary` is a `useCallback` declaring all twelve identifiers it reads; `libraryUnion`
depends on it; eslint reports 22 warnings / 0 errors with the `makeLibrary` line gone and
every other warning — including the deliberate `reconcileTick` one — unchanged. `recentAppNames`
is state refreshed by `handleRecentGamesChanged`, with the `libraryStatus` fallback explicitly
rejected in a code comment. `collectionIsStale` is exported, unit-tested through all five rows
(including the `preset_uncategorized` false row), and wired into an effect that clears the
persisted value. `libraryPipeline.test.ts`'s needle is updated. `buildGridPipeline` is still
called exactly once. No eslint-disable anywhere in the diff. `engineWiring.test.ts` and
`connectedStoresParity.test.ts` are green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile both ledgers to the same open count</name>
  <files>
.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md,
.planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md
  </files>

  <action>
Do this task LAST, after Tasks 1 and 2 are committed, so every row can cite a real hash.
Capture the hashes first: `git log --oneline -3` and map each finding to the commit that
closed it.

**`34.11-REVIEW-FIX.md`:**
- Frontmatter: add `re-swept-2: 2026-08-27` / `re-swept-2_by: quick 260827-t9c` (or extend the
  existing `re-swept` keys with a second dated entry — keep whatever shape the file already
  uses parseable). Update `dispositions:` to `fixed: 17`, `open: 5`, `invalid: 1`,
  `total: 23`. `criticals_open: 0` unchanged. **`status:` STAYS `partial`** — WR-08, WR-10,
  WR-11, WR-16 and WR-18 remain open, so `all_fixed` would be the precise false claim the
  file's own "Verdict" section warns about. Do not edit `34.11-REVIEW.md`; the file says
  explicitly that the review records what was FOUND and is never rewritten when fixes land.
- Flip five Warnings rows to **FIXED**, each citing its commit hash and the specific evidence,
  in the same voice as the existing FIXED rows (which cite a landmark, not a SUMMARY):
  - WR-01 — the `useCallback` with twelve declared identifiers, the memo depending on it, the
    eslint count moving 23 -> 22 with the `makeLibrary` line gone, and the note that no
    suppression was added. Mention the `libraryPipeline.test.ts` needle update, since a reader
    diffing that file will otherwise wonder why a gate changed.
  - WR-05 — subscribes to `handleRecentGamesChanged`, and state plainly that the review's
    `libraryStatus` fallback was **deliberately not taken** (progress-tick re-render storm),
    with the primary recommendation taken instead. This is a partial-divergence from the
    review's text and must be visible in the ledger, not buried in a code comment.
  - WR-06 — the indented `throw` is gone (`grep -c throw` is 0 — record that the earlier
    false-closure was a `^throw` anchor missing an indented statement, so the next reader does
    not repeat it), and the drift alarm is now an `it.each` in
    `NavShell/__tests__/FilterRunnabilityFacet.test.tsx`.
  - WR-09 — `collectionIsStale` in `engineWiring.ts`, its five-row truth table including the
    `preset_uncategorized` row, and the effect that clears the persisted value.
  - WR-12 — `renderableActiveFilters` applied in `index.tsx` before `activeFilterCount` is
    derived, plus the `labels.length === 0` guard. Keep the existing row's honest note that the
    `chipLabelSpec` null-filter at `FilterZeroResult` predates the review (`726b96f93`) and was
    never the residual.
- Add a re-sweep paragraph for quick `260827-t9c` alongside the existing `260825-vy5` and
  `260827-s8z` paragraphs, with the arithmetic stated and checked: 17 + 5 + 1 = 23.
- Update the **Residual** section: "ten open Warnings" becomes five, and the sentence naming
  WR-10/WR-11 as the D-08 pair stays. The **Badge attribution** table is unaffected —
  `status: partial` still maps to `inprogress` — leave it alone.

**`.planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md`:**
- `status:` STAYS `pending`. Keep `resolves_phase: "34.11"`.
- Update the title: it currently says "10 open Warning-level review findings". Make it five.
- Add a dated update paragraph for quick `260827-t9c` in the same shape as the existing
  `260827-s8z` one: WR-01, WR-05, WR-06, WR-09 and WR-12 are now FIXED and removed from the
  groups below; the remaining five were not re-swept by this task and are carried unchanged.
- DELETE the five closed bullets. The "Correctness — small but real" group loses all five of
  its bullets and therefore loses its heading too — delete the empty heading rather than
  leaving it dangling. What survives: WR-18 (i18n gate scope), WR-08 (UX), WR-16 (test
  quality), WR-10/WR-11 (blocked on D-08).
- Prune the `files:` list to the files the surviving five actually name — `FilterChipRow/index.tsx`
  and `FilterZeroResult/index.tsx` are no longer referenced by any remaining bullet; keep
  `FilterChipRow/__tests__/index.test.tsx` (WR-16), `FilterCollectionList/index.tsx` (WR-08)
  and `meta/i18nGateScope.json` (WR-18). `Library/index.tsx` stays for WR-10/WR-11.
- Update the body sentence "Ten Warnings were confirmed still present" to five, and the
  discharged list in the paragraph above it to include the five newly closed IDs.

**Cross-document consistency is the deliverable.** Before finishing, grep both files for a
count word and confirm they agree: a count that differs between the two is a failure mode this
project has hit before (a status doc lagging its source, undetected).

Commit with `git add` naming ONLY the two paths — a concurrent session is committing 34.13
work to this branch, so never `git add -A` and never `git commit -a`. Do not push.
  </action>

  <verify>
    <automated>
# Frontmatter arithmetic: 17 + 5 + 1 = 23, status partial, criticals_open 0.
F=.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md; grep -q 'fixed: 17' $F && grep -q 'open: 5' $F && grep -q 'invalid: 1' $F && grep -q 'total: 23' $F && grep -q 'status: partial' $F && grep -q 'criticals_open: 0' $F
    </automated>
    <automated>
# All five findings are marked FIXED in the ledger, none still OPEN.
F=.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md; for w in WR-01 WR-05 WR-06 WR-09 WR-12; do grep "^| $w " $F | grep -q '\*\*FIXED\*\*' || exit 1; done; for w in WR-08 WR-10 WR-11 WR-16 WR-18; do grep "^| $w " $F | grep -q 'OPEN' || exit 1; done
    </automated>
    <automated>
# The todo no longer carries any of the five closed IDs as an open bullet, and still carries the five that remain.
T=.planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md; for w in WR-01 WR-05 WR-06 WR-09 WR-12; do ! grep -q "^- \*\*$w\*\*" $T || exit 1; done; for w in WR-08 WR-10 WR-11 WR-16 WR-18; do grep -q "$w" $T || exit 1; done; grep -q 'status: pending' $T
    </automated>
    <automated>
# This quick task is named in both re-sweep trails.
grep -q '260827-t9c' .planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md && grep -q '260827-t9c' .planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md
    </automated>
    <automated>
# No stale "ten"/"10 open" count survives in either document.
! grep -Eqi 'ten (open|Warnings)|10 open Warning' .planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md .planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md
    </automated>
  </verify>

  <done>
Both documents state five open findings — WR-08, WR-10, WR-11, WR-16, WR-18 — and neither
references the closed five as open. `34.11-REVIEW-FIX.md` reads `fixed: 17 / open: 5 /
invalid: 1 / total: 23`, `status: partial`, with every flipped row citing a commit hash and
a landmark rather than a SUMMARY, and with WR-05's divergence from the review's fallback
recorded explicitly. The todo keeps `status: pending`, has its title and `files:` list
pruned, and carries a dated `260827-t9c` update paragraph.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` -> Library render | `libraryCollection`, `libraryView`, `storeFacet` are user-writable strings read straight into filter state |
| `configStore` (`games.recent`) -> Library render | mutable on-disk store read into `recentAppNames` |
| dev/test import -> renderer bootstrap | module-evaluation-time code in a component file runs before React mounts, outside any error boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t9c-01 | Denial of Service | `FilterRunnabilityFacet/index.tsx` module scope | mitigate | Delete the module-scope `throw`; the drift check becomes a named test failure (Task 1, WR-06). Verified by `grep -c throw == 0` on the component, an assertion deliberately NOT anchored to `^`. |
| T-t9c-02 | Denial of Service | `currentCollection` from `localStorage` | mitigate | `collectionIsStale` clears a collection absent from `customCategories.list`, so a forged or orphaned key can no longer filter the whole library away with no UI affordance to recover (Task 2, WR-09). |
| T-t9c-03 | Tampering | `libraryView` from `localStorage` -> zero-result sentence | mitigate | `renderableActiveFilters` drops descriptors `chipLabelSpec` cannot name, and `FilterZeroResult` returns `null` on empty labels, so a forged view value can neither inflate `activeFilterCount` nor render `"No games match ."` (Task 1, WR-12). |
| T-t9c-04 | Information Disclosure | stale library grid after logout | mitigate | The `libraryUnion` callback declares every login gate it reads, so a logout that changes only `username`/`user_id` recomputes the union instead of leaving the previous account's titles on screen (Task 2, WR-01). |
| T-t9c-SC | Tampering | npm/pip/cargo installs | mitigate | **No package installs in this task.** No `package.json` change is in `files_modified`; the frontend jest project has no jsdom by design and none is to be added. If any task appears to need a dependency, stop — that is a design drift, not a fix. |
</threat_model>

<verification>
Run after all three tasks, from the repo root:

1. `pnpm exec jest --config jest.config.js src/frontend/screens/Library src/frontend/components/UI/NavShell/__tests__ --ci` — assert a non-zero passed count and zero failures. **Run by PATH.** Do not use `--selectProjects`; it is case-sensitive and exits 0 on a miss.
2. `pnpm exec eslint src/frontend/screens/Library/index.tsx` — expect `22 problems (0 errors, 22 warnings)`, exactly one fewer than the measured 23-warning baseline, with the missing line being `react-hooks/exhaustive-deps ... 'makeLibrary'`. Any other delta means an unrelated warning was introduced or silenced.
3. `pnpm exec tsc --noEmit` clean.
4. `git diff --stat` touches only the twelve paths in `files_modified`. A concurrent session owns 34.13 work on this branch — anything else in the diff is not yours.
5. **Do NOT run the `meta` jest project.** `meta/__tests__/genI18nGateScope.test.ts` is known-red from Phase 34.17 (`29ccffa4b`) and quick `260824-u8b`, is not caused by this task, and must not be "fixed" here. If a broader run is unavoidable, it must tolerate exactly that one failure and fail on any other.
6. **Do not push.**
</verification>

<success_criteria>
- WR-01, WR-05, WR-06, WR-09, WR-12 each fixed, each with a regression test whose failing assertion against the pre-fix code is named in this plan, and each with a non-vacuity proof.
- `react-hooks/exhaustive-deps` for `makeLibrary` is gone from `Library/index.tsx` and **no eslint-disable was added anywhere in the diff**.
- CR-01's shared-union invariant survives: `buildGridPipeline(` appears exactly once in `Library/index.tsx`, `engineWiring.test.ts` (`countForStore('steam') === 3` with `gog` selected) and `connectedStoresParity.test.ts` are both green.
- `34.11-REVIEW-FIX.md`: `fixed: 17 / open: 5 / invalid: 1 / total: 23`, `status: partial`, five rows flipped with commit-hash evidence, `260827-t9c` in the re-sweep trail.
- The pending todo: `status: pending`, five surviving bullets (WR-18, WR-08, WR-16, WR-10, WR-11), the five closed ones struck.
- **Both documents state the same open count: five.**
- WR-05's rejection of the review's `libraryStatus` fallback is recorded in BOTH the code comment and the ledger row.
</success_criteria>

<output>
Write `.planning/quick/260827-t9c-fix-phase-34-11-correctness-residual-rev/260827-t9c-SUMMARY.md` when done.
</output>
