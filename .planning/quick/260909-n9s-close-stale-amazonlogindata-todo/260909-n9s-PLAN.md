---
quick_id: 260909-n9s
title: "Close stale todo: WebView amazonLoginData is permanently null"
date: 2026-09-09
type: docs-only
---

# Quick Task 260909-n9s

## Description

Close `.planning/todos/pending/2026-09-01-webview-amazonlogindata-is-permanently-null.md`.
Its premise was refuted by Phase 40 Plan 01 (commit `157409206`), which **deleted** the
`amazonLoginData` state and all six read sites from `src/frontend/screens/WebView/index.tsx`
rather than repairing them.

## Why this is docs-only

The todo asked for a code fix. That fix is unnecessary — and its *prescribed remedy* was
superseded, not merely already-applied:

> Todo's remedy: "read Amazon's login data from `oauthLoginState` instead of the dead local
> `amazonLoginData` state".

`index.tsx` needs **no** Amazon login data at all. `useTauriOAuthLogin.ts` fetches it
internally and never hands it back to the component.

## Evidence

| Todo claim | Current state |
|---|---|
| `useState<NileLoginData \| null>(null)` holds dead state | Gone. `index.tsx` no longer imports `NileLoginData`; its only `useState` is `showLoginWarningFor` |
| L~203 `'/loginweb/nile': amazonLoginData ? ... : ''` | Now the literal `''` — and unreachable (below) |
| L~242 `if (!amazonLoginData)` always takes no-data branch | Deleted with `handleAmazonLogin` |
| L~253-256 `.client_id` / `.code_verifier` / `.serial` | Deleted |
| L~375 dep-array entry never changes | Deleted |

Reachability of the `''` url — three independent gates, all closed:

1. `isLoginPathname(pathname)` returns `<TauriLoginPanel runner state={oauthLoginState}/>`
   (`index.tsx:422`) **before** `startUrl` is read by the store/wiki arm.
2. `/loginweb/nile` is in `LOGIN_PATHNAMES` (`loginRoutes.ts:24`), so
   `isStoreRoute = !LOGIN_PATHNAMES.includes(pathname) && runner !== 'humble'` is `false`.
3. With `isStoreRoute` false, `useStoreEmbedHost` no-ops and never navigates to the empty url.

The real fetch is live and gated: `useTauriOAuthLogin.ts:220` owns the single
`window.api.getAmazonLoginData()` call, protected by
`__tests__/WebViewAmazonLoginDataSpawn.test.ts` — a single-call-site gate that carries its own
negative control (asserts it FAILS against a synthetic source with a duplicated call site).

Suites run: `WebViewAmazonLoginDataSpawn.test.ts` + `useTauriOAuthLogin.test.tsx`
→ **2 passed, 78 tests**.

## Tasks

1. Move the todo `pending/` → `completed/`, rewriting `status:` to `RESOLVED` with the
   superseded-remedy note and `resolved_by: quick-260909-n9s`.
2. Update `.planning/STATE.md` (Quick Tasks Completed + Last activity).

## Deliberately NOT done

No change to `src/frontend/screens/WebView/index.tsx`. The comments at L165-171 and L276-291
already record why no fetch belongs there (the ~12.8s-per-call double-spawn cost measured by
quick task 260806-teb). Re-adding one would reintroduce that regression.
