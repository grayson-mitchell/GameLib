---
phase: quick-260901-b8z
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [TODO-2026-08-28-FIX-2]
files_modified:
  - meta/assembleRendererDist.ts
  - meta/__tests__/assembleRendererDist.test.ts
  - vite.config.ts
  - meta/__tests__/viteRendererConfig.test.ts
  - src-tauri/tauri.conf.json
  - .github/workflows/release-tauri.yml
  - src/backend/__tests__/releaseWorkflow.test.ts
  - src/frontend/index.tsx
  - src-tauri/build.rs
  - src-tauri/src/main.rs
  - .planning/quick/260901-b8z-repoint-frontenddist-at-renderer-only-dir/260901-b8z-MEASUREMENTS.md
  - .planning/todos/pending/2026-08-28-tauri-macos-bundle-is-786mb-frontenddist-embeds-build-bin-in.md

must_haves:
  truths:
    - "The packaged shell binary's `__TEXT,__const` is below 30,000,000 B (was 223,766,872 B)"
    - "`strings <gamelib-shell> | grep -c 'bin/x64/win32/gogdl.exe'` returns 0"
    - "The packaged app renders a non-English locale — proves `locales/` resolves from the new frontendDist root"
    - "The packaged app's About window opens AND its icon renders — proves `about.html` and `icon.png` resolve from the new frontendDist root"
    - "Shipped `Resources/build/bin` is still ~233.8 MiB with executable helpers — fix (1)'s saving is not regressed"
    - "A missing, empty or partial `build/renderer` fails the build LOUDLY instead of shipping a white screen"
    - "Stale files in `build/assets` are not copied into `build/renderer` — the copy is driven by rollup's emitted-file list, not by the directory"
  artifacts:
    - path: "meta/assembleRendererDist.ts"
      provides: "generateBundle-driven renderer-dir assembly with fail-loud post-conditions"
      exports: ["assembleRendererDist", "assembleRendererDistPlugin", "STATIC_RENDERER_FILES", "STATIC_RENDERER_DIRS"]
    - path: "meta/__tests__/assembleRendererDist.test.ts"
      provides: "Both-direction coverage: every bundle key present AND seeded stale file absent; every post-condition throws"
    - path: ".planning/quick/260901-b8z-repoint-frontenddist-at-renderer-only-dir/260901-b8z-MEASUREMENTS.md"
      provides: "Old-shipped vs new-shipped apparent-byte record with derivations"
  key_links:
    - from: "vite.config.ts"
      to: "meta/assembleRendererDist"
      via: "import + registered last in plugins array"
      pattern: "assembleRendererDistPlugin\\(\\)"
    - from: "src-tauri/tauri.conf.json"
      to: "build/renderer"
      via: "frontendDist"
      pattern: "\"frontendDist\": \"\\.\\./build/renderer\""
    - from: ".github/workflows/release-tauri.yml"
      to: "build/renderer/index.html"
      via: "prune-step hard guard"
      pattern: "test -f build/renderer/index\\.html"
    - from: "assembleRendererDist generateBundle"
      to: "closeBundle"
      via: "captured rollup bundle keys"
      pattern: "Object\\.keys\\(bundle\\)"
---

<objective>
Repoint Tauri's `frontendDist` from `../build` (420,056,993 B, of which 10,195,363 B is
webview-reachable) to a renderer-only `../build/renderer`, so `tauri-codegen` stops
brotli-embedding ~410 MB of helper binaries, the dead Electron main bundle and the SEA prep
blob into `gamelib-shell`.

This is **fix (2)** of `.planning/todos/pending/2026-08-28-tauri-macos-bundle-is-786mb-frontenddist-embeds-build-bin-in.md`.
Fix (1) (`bundle.resources` per-platform narrowing) and the stale-`build/bin` prune are DONE.

Purpose: `__TEXT,__const` 223,766,872 B → predicted ~4.9 MB. Removes the second, dead copy of
every helper binary from the shipped artifact.
Output: a new `meta/assembleRendererDist.ts` vite plugin, a repointed `frontendDist`, an
additive release-workflow guard, and a MEASURED release artifact proving all four criteria.

**Design is Option B+, settled by `260901-b8z-RESEARCH.md` — do not relitigate.** `outDir`
stays `build`, `publicDir` stays default, `emptyOutDir` stays `false`, `bundle.resources` is
untouched, `paths.ts` is untouched, and all five pinned assertions stay green unchanged. The
renderer dir is assembled from **rollup's own emitted-file list**, not from a directory glob —
which is also why 19,540,372 B of stale `build/assets` stops being embedded, for free.
Option A is rejected: a symlink-preserving hand-written copy would make
`preserveRunnerSymlinksPlugin` log a cheerful `restored 0 symlink(s)`, a vacuous green sitting
over F-34.9-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260901-b8z-repoint-frontenddist-at-renderer-only-dir/260901-b8z-RESEARCH.md
@.planning/todos/pending/2026-08-28-tauri-macos-bundle-is-786mb-frontenddist-embeds-build-bin-in.md
@.planning/quick/260901-8rm-shrink-tauri-macos-bundle-narrow-bundle-/260901-8rm-MEASUREMENTS.md
@.planning/quick/260901-a2w-stop-shipping-stale-build-bin-helper-fil/260901-a2w-MEASUREMENTS.md
@vite.config.ts
@meta/preserveRunnerSymlinks.ts
@meta/pruneStaleHelperBinaries.ts

<verified_citations>
Every line number below was RE-VERIFIED by grep in the planning session, 2026-09-01.
FIVE citations in the research/todo had DRIFTED and are corrected here. Trust this block over
the research and the todo wherever they disagree.

CORRECTED (the research/todo values are stale — do NOT copy them into any record):
  vite.config.ts:105                             outDir: 'build'                     (todo said :104)
  vite.config.ts:149                             preserveRunnerSymlinksPlugin()      (todo said :126)
  meta/__tests__/viteRendererConfig.test.ts:121  expect(...).toContain('gamelib-preserve-runner-symlinks')      (research said :119)
  meta/__tests__/viteRendererConfig.test.ts:128  expect(...).toContain('gamelib-prune-stale-helper-binaries')   (research said :126)
  src/preload/api/tauriChildWindows.ts:153       const ABOUT_VERSION_TIMEOUT_MS = 1000   (research AND brief said :154)

CONFIRMED EXACT:
  src-tauri/tauri.conf.json:7          "frontendDist": "../build"
  src-tauri/tauri.conf.json build      beforeBuildCommand is "" — `tauri build` does NOT run vite; the caller must
  vite.config.ts:55-63                 imports; :62 preserveRunnerSymlinks, :63 pruneStaleHelperBinaries
  vite.config.ts:101-102               rollupOptions.input = path.resolve('index.html')
  vite.config.ts:106-111               the emptyOutDir MUST-stay-false comment
  vite.config.ts:112                   emptyOutDir: false
  vite.config.ts:140                   pruneStaleHelperBinariesPlugin()
  meta/preserveRunnerSymlinks.ts:221-222   sourceDir/destDir defaults (__dirname-relative)
  meta/preserveRunnerSymlinks.ts:225       name: 'gamelib-preserve-runner-symlinks'
  meta/pruneStaleHelperBinaries.ts:404-405 buildBinDir/publicBinDir defaults
  meta/pruneStaleHelperBinaries.ts:408     name: 'gamelib-prune-stale-helper-binaries'
  meta/checkBuildBinMirror.ts:202-203      process.cwd()-relative roots
  meta/__tests__/viteRendererConfig.test.ts:87-88   outDir 'build' / emptyOutDir false
  meta/__tests__/viteRendererConfig.test.ts:59      const pluginNames = (config) => string[]
  src/backend/__tests__/packagingConfig.test.ts:133 const fullSource = `../build/bin/${relPath}`
  src/backend/__tests__/packagingConfig.test.ts:388 describe('vite.config.ts registers the runner-symlink preservation plugin (F-34.9-01)')
  src/backend/__tests__/packagingConfig.test.ts:393,400  the two tests in that describe
  src/backend/__tests__/releaseWorkflow.test.ts:433 const PRUNE_STEP_NAME = 'Prune non-frontend build intermediates before bundling'
  src/backend/__tests__/releaseWorkflow.test.ts:442 function seedBuildTree(root, { withBridge })
  src/backend/__tests__/releaseWorkflow.test.ts:446 touch(root, join('build','index.html'), '<!doctype html>')
  src/backend/__tests__/releaseWorkflow.test.ts:514 the "kept" assertion listing build/index.html
  src/backend/__tests__/releaseWorkflow.test.ts:526 rmSync(join(workdir,'build','index.html')) — the existing fail test
  src/backend/__tests__/releaseWorkflow.test.ts     tests use `test(` at :479, :488, :506, :524, :533, :541 (six)
  .github/workflows/release-tauri.yml:418-428       the prune step; :425 is `test -f build/index.html`
  src/frontend/index.tsx:114-117       HttpApi; :115 addPath (DEAD), :116 loadPath (LIVE)
  src/preload/api/tauriChildWindows.ts:177  url: 'about.html?v=' + encodeURIComponent(version)
  public/about.html:50                 <img src="./icon.png" alt="GameLib" />
  src-tauri/src/main.rs:634            MenuItemBuilder::with_id("about", "About GameLib")

MEASURED THIS SESSION (apparent bytes, sum of lstat().st_size; `du` NOT used):
  build/locales    = 4,072,387 B       build/icon.png   = 667,342 B
  build/about.html =     1,909 B       build/index.html =     707 B
  build/assets ALL = 24,993,390 B across 263 files (only 78 / 5,453,018 B reachable)
  Required renderer payload = 10,195,363 B
  build/renderer does NOT exist yet
</verified_citations>

<baseline_artifact_hazard>
A release DMG already exists:
  src-tauri/target/release/bundle/dmg/GameLib_0.7.0_aarch64.dmg  (388,901,574 B, mtime 09-01 07:01)

It was built at 07:01. The a2w prune commits are 681fa1344 (07:35) and 90bb5a08d (07:38).
**This artifact therefore PREDATES the a2w prune** and still embeds the 48,906,861 B of stale
`build/bin`. Two consequences the executor MUST honour:

1. Task 3's release build OVERWRITES this file. Copy it aside FIRST or the baseline is
   destroyed.
2. Its `__const` is the pre-a2w figure. Roughly 23 MB of any delta measured against it belongs
   to quick-260901-a2w, NOT to this task. **Do not claim the full delta as this task's saving.**
   Criteria 1 and 2 are ABSOLUTE thresholds (`< 30,000,000`, `== 0`), so they are provable
   without a re-baseline build — which is why this plan budgets ONE release build, not two.
</baseline_artifact_hazard>

<scoped_out>
Explicitly NOT in this task (the todo's Non-goals — do not fold in):
  - `steam_api.pdb` / `steam_api_shim.lib` removal (~2.7 MB) — a buildSteamBridgeShims.ts
    compile-flag change, different mechanism
  - Cause 3, Tauri's symlink dereferencing on resource copy (~45 MB)
  - The Node sidecar (162 MB) — 138 MB of it is the Node 26 runtime, structural
The 19.5 MB of stale `build/assets` is in scope ONLY as a free side effect of copying from
rollup's emitted-file list. Do NOT add a separate assets-prune mechanism and do NOT flip
`emptyOutDir`.
Logged by the research, each deserving its own todo, NOT fixed here: `crossover-index.json.gz`
is read from `publicDir` but exists nowhere and is carried by no resource entry;
`check:build-bin-mirror` is invoked by nothing (not CI, not husky) so it must not be leaned on
as a regression guard; `build/` root carries 15 stale `icon-dark*`/`icon-light*` files.
</scoped_out>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe rollup's generateBundle key set before building anything on it</name>
  <files>vite.config.ts (temporary, reverted)</files>
  <action>
De-risk the single unverified assumption in the B+ design (RESEARCH.md §8 unknown 2): that
rollup's `generateBundle` bundle keys are exactly the set this build emits, and exclude
everything vite's publicDir copy writes. Building the real plugin on an unverified key set is
how this repo produces a green check that proves nothing.

1. `cp vite.config.ts /tmp/b8z-vite.config.ts.bak` — this is the restore path.
   **NEVER `git checkout -- vite.config.ts`**: a post-checkout hook fires a helper-binary
   download that can THROW. Restore with `cp` only. Never `git stash`, never `git reset`
   (both strand concurrent sessions).
2. Append a throwaway plugin as the LAST entry of the plugins array (after
   `preserveRunnerSymlinksPlugin()` at line 149), with `apply: 'build'`, `enforce: 'post'`,
   whose `generateBundle(_options, bundle)` writes `Object.keys(bundle).sort()` as JSON to
   `/tmp/b8z-bundle-keys.json` and logs the count.
3. Run `pnpm exec vite build`.
4. Restore: `cp /tmp/b8z-vite.config.ts.bak vite.config.ts`, then confirm
   `git diff --stat vite.config.ts` is EMPTY.
5. Classify the captured keys.

**The decision rule, stated before the data is seen:**
- PASS if every key is either `index.html` or matches `^assets/`, AND the assets count is in
  the ~78 band, AND NO key matches `^bin/`, `^main/`, `^preload/`, `^locales/`, `about.html`,
  `icon.png`, `manifest.json`, `robots.txt`, `sidecar-prep.blob` or `.DS_Store`.
- If any non-`assets/` non-`index.html` key appears, or if `assets/` keys number ~263 rather
  than ~78, the B+ mechanism does not do what the research claims. **STOP. Do not proceed to
  Task 2.** Report the actual key set and halt for replanning.

Record the exact count and the classification in the task notes — Task 2's post-conditions and
Task 3's stale-drop claim both rest on this.
  </action>
  <verify>
    <automated>set -o pipefail; test -s /tmp/b8z-bundle-keys.json && python3 -c "
import json,re
k=json.load(open('/tmp/b8z-bundle-keys.json'))
bad=[x for x in k if x!='index.html' and not re.match(r'^assets/',x)]
assert 'index.html' in k, 'index.html MISSING from bundle keys'
assert not bad, f'unexpected non-renderer keys: {bad[:10]}'
a=[x for x in k if x.startswith('assets/')]
assert 60 <= len(a) <= 120, f'assets key count {len(a)} outside the expected ~78 band'
print(f'PASS keys={len(k)} assets={len(a)}')" && git diff --quiet vite.config.ts && echo "probe reverted"</automated>
    <mutation-proof>Add a fake key such as `bin/x64/win32/gogdl.exe` to the JSON → the `bad` assertion fires. Delete `index.html` from the JSON → the first assertion fires. An assets count of 263 (a directory glob rather than the emitted set) fails the band check. The gate CANNOT pass on a missing file: `test -s` fails on absent OR empty, so a probe that never ran is a hard FAIL, not a silent pass. Leaving the probe in vite.config.ts fails the final `git diff --quiet vite.config.ts` check. That check is deliberately a PIPE-FREE exit-status test, NOT the `[ "$(git diff --stat ... | wc -l)" = "0" ]` form: `set -o pipefail` does NOT govern a pipeline inside `$( ... )` — the substitution's exit status is discarded and only its captured stdout reaches the enclosing `[ ]`. So a `git diff` that failed to execute would emit empty stdout, `wc -l` would print `0`, and the check would announce `probe reverted` having verified nothing. Reproduced empirically 2026-09-01: `bash -c 'set -o pipefail; [ "$(false | wc -l | tr -d " ")" = "0" ] &amp;&amp; echo PASS'` prints PASS. `git diff --quiet` instead exits 0 only on a genuinely clean path, 1 on any difference, and 128 if git itself fails (measured: a bogus path gives 128) — so a surviving probe AND a broken git are both hard FAILs.</mutation-proof>
  </verify>
  <done>`/tmp/b8z-bundle-keys.json` holds only `index.html` + `assets/*`, assets count in the ~78 band. `git diff --stat vite.config.ts` is empty (probe fully reverted). If the classification failed, the plan is HALTED and no further task ran.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build meta/assembleRendererDist.ts with fail-loud post-conditions, wire it into vite</name>
  <files>meta/assembleRendererDist.ts, meta/__tests__/assembleRendererDist.test.ts, vite.config.ts, meta/__tests__/viteRendererConfig.test.ts</files>
  <behavior>
Write these tests FIRST, against a synthetic `outDir` fixture in a `mkdtempSync` tmpdir. The
pure function must be exercised directly — do not test only through a real vite build.

Copy correctness, BOTH directions (a one-direction check is how this repo has produced false
greens; assert presence AND absence):
  - Test 1: every captured bundle key lands at `renderer/<key>` with byte-identical content.
  - Test 2: a file seeded into `outDir/assets/` that is NOT in the bundle-key list is ABSENT
    from `renderer/assets/`. This is the 19,540,372 B stale-asset drop, asserted directly.
  - Test 3: `about.html`, `icon.png` and the whole `locales/` tree are copied byte-for-byte
    from `outDir`, though they appear in no bundle key.
  - Test 4: `manifest.json` and `robots.txt` seeded in `outDir` are ABSENT from `renderer/`
    (proven vestigial — RESEARCH.md §2 sweeps 4 and 7; excluded deliberately, not by oversight).
  - Test 5: a pre-existing `renderer/` containing a stale file is fully replaced — the stale
    file is gone after assembly.

Fail-loud post-conditions — each must THROW, and each gets its own test:
  - Test 6: bundle-key list is empty → throws. (Anti-vacuity: guards the case where
    `generateBundle` never fired. Without this, an assembly that copied only the static set
    could look plausible.)
  - Test 7: `index.html` missing from the assembled tree → throws.
  - Test 8: `renderer/assets` has zero files → throws.
  - Test 9: `about.html` missing from `outDir` → throws.
  - Test 10: `icon.png` missing from `outDir` → throws.
  - Test 11: `locales/` absent, or present but containing no `*.json` → throws. **Both cases
    must be tested separately.** A post-condition that passes on a MISSING directory is the
    `collectEntries`-returns-empty-Map shape (`meta/pruneStaleHelperBinaries.ts:404-421`) —
    the exact silent-no-op this repo already owns once.
  </behavior>
  <action>
Create `meta/assembleRendererDist.ts` following the established shape of its two sibling
plugins — export a PURE function plus a thin plugin wrapper, exactly as
`meta/pruneStaleHelperBinaries.ts` and `meta/preserveRunnerSymlinks.ts` do. That is what makes
it unit-testable without a real build.

Exports:
  - `STATIC_RENDERER_FILES = ['about.html', 'icon.png']`
  - `STATIC_RENDERER_DIRS  = ['locales']`
  - `assembleRendererDist(outDir: string, rendererDir: string, bundleKeys: string[])`
    — the pure function: `rm -rf rendererDir`, copy each bundle key `outDir/<key>` →
    `rendererDir/<key>` (creating parent dirs), then copy the static files and dirs, then run
    every post-condition and THROW on the first failure with a message naming the missing path.
  - `assembleRendererDistPlugin(options?: { outDir?: string; rendererDir?: string }): Plugin`
    — `name: 'gamelib-assemble-renderer-dist'`, `apply: 'build'`, `enforce: 'post'`.
    `generateBundle(_options, bundle)` captures `Object.keys(bundle)` into module-local state;
    `closeBundle()` calls the pure function. Defaults `__dirname`-relative
    (`join(__dirname,'..','build')` / `join(__dirname,'..','build','renderer')`), matching
    both siblings — NOT derived from `config.build.outDir`.

Header comment must record, so a future reader does not "fix" a non-problem:
  - Why the copy is driven by rollup's emitted-file list rather than a directory glob: it is
    exact, and it drops the 19,540,372 B of stale `build/assets` that `emptyOutDir: false`
    accumulates — without touching `emptyOutDir`, whose MUST-stay-false rationale at
    `vite.config.ts:106-111` still holds verbatim under this design.
  - Why `manifest.json` + `robots.txt` are excluded: vestigial CRA/PWA scaffold, no
    `<link rel="manifest">` in `index.html`, no service worker, nothing references them.
  - Why the array-position dependency on `preserveRunnerSymlinksPlugin` is a NON-issue: both
    use `closeBundle`/`enforce:'post'` so vite runs them in array order, but the symlink plugin
    touches only `build/bin` and nothing under `bin/` is ever copied into `build/renderer`.
    The two cannot interact.
  - That `locales/` and `icon.png` are STRUCTURALLY duplicated by design — the renderer reads
    them over `tauri://` from `frontendDist`, while the sidecar's i18next-fs-backend and
    `paths.ts`'s `windowIcon` read them from `Contents/Resources/build` via `bundle.resources`
    targets. Deduplicating either breaks backend-side translated strings or the window icon.
  - That the ~9.72 MiB on-disk duplication is accepted: `/build` is gitignored
    (`.gitignore:12`), and jest, prettier and eslint all ignore it already.

Wire into `vite.config.ts`: add the import alongside lines 62-63, and register
`assembleRendererDistPlugin()` as the LAST element of the plugins array, after
`preserveRunnerSymlinksPlugin()` (line 149). Change nothing else in this file — `outDir` (105),
`emptyOutDir` (112) and `rollupOptions.input` (101-102) all stay exactly as they are.

Add to `meta/__tests__/viteRendererConfig.test.ts`, mirroring the two existing plugin-name
tests at lines 120-124 and 127-131:
`expect(pluginNames(config)).toContain('gamelib-assemble-renderer-dist')`.
  </action>
  <verify>
    <automated>set -o pipefail; cd /Users/graysonmitchell/Projects/GameLib && pnpm exec jest meta/__tests__/assembleRendererDist.test.ts meta/__tests__/viteRendererConfig.test.ts src/backend/__tests__/packagingConfig.test.ts 2>&1 | tail -20 && pnpm codecheck 2>&1 | tail -5 && rm -rf build/renderer && rm -f /tmp/b8z-vite-build.log && for d in build/bin public/bin; do [ -n "$(find "$d" -type f -print -quit 2>/dev/null)" ] || { echo "PRE-BUILD FAIL: $d missing/empty — the prune plugin would evaluate an empty root at buildStart and log a vacuous 'nothing to prune'"; exit 1; }; done && pnpm exec vite build 2>&1 | tee /tmp/b8z-vite-build.log | tail -5 && python3 -c "
import os,re
def n(p):
    return sum(len(f) for _,_,f in os.walk(p))
def b(p):
    t=0
    for r,_,fs in os.walk(p):
        for f in fs:
            q=os.path.join(r,f)
            if not os.path.islink(q): t+=os.lstat(q).st_size
    return t
for p in ['build/renderer/index.html','build/renderer/about.html','build/renderer/icon.png']:
    assert os.path.exists(p), f'MISSING {p}'
assert n('build/renderer/assets')>0, 'renderer assets empty'
assert n('build/renderer/locales')>0, 'renderer locales empty'
ra,ba=n('build/renderer/assets'), n('build/assets')
assert ra<ba, f'stale assets NOT dropped: renderer={ra} build={ba}'
tot=b('build/renderer')
assert 8_000_000<tot<14_000_000, f'renderer payload {tot} outside the ~10,195,363 B band'
print(f'PASS renderer={tot}B assets {ra}/{ba}')
log=open('/tmp/b8z-vite-build.log', encoding='utf-8', errors='replace').read()
sm=re.search(r'\[preserve-runner-symlinks\] restored (\d+) symlink\(s\), skipped (\d+), rejected (\d+)',log)
assert sm, 'preserve-runner-symlinks line ABSENT from the build log (plugin dropped, or its format at meta/preserveRunnerSymlinks.ts:234 changed)'
assert (int(sm.group(1)),int(sm.group(2)),int(sm.group(3)))==(12,0,0), f'symlink restore REGRESSED: {sm.group(0)}'
pm=re.search(r'\[prune-stale-helper-binaries\] (nothing to prune|pruned \d+ entr(?:y|ies), \d+ bytes freed)',log)
assert pm, 'prune-stale-helper-binaries line ABSENT from the build log (plugin dropped, or its format at meta/pruneStaleHelperBinaries.ts:417/419-421 changed)'
print('sibling plugins PASS: '+sm.group(0)+' | '+pm.group(0))"</automated>
    <mutation-proof>Replace the bundle-key-driven copy with a wholesale `cp -R outDir/assets` → Test 2 goes red AND the live `ra &lt; ba` assertion goes red (263 vs 263). Delete any single post-condition throw → its dedicated test (6-11) goes red. Drop `assembleRendererDistPlugin()` from the plugins array → the new viteRendererConfig assertion goes red AND the live build produces no `build/renderer`, failing the `os.path.exists` checks. Skip the `rm -rf` → Test 5 goes red. The `rm -rf build/renderer` before the build means a stale tree from a prior run cannot satisfy this gate. `packagingConfig.test.ts` is run to PROVE pins 2 and 6 (F-34.9-01 import/call at :388-404, and the `../build/bin/` resource-source prefix at :133) are still green, not to assume it.

`set -o pipefail` is load-bearing on EVERY `&amp;&amp;` link above: without it, `cmd 2>&amp;1 | tail -N` returns tail's status (0), so a fully red jest run or a failing `pnpm codecheck` would flow straight into the file-existence checks and the gate would report PASS. Empirically confirmed: `bash -c 'false | tail -5 &amp;&amp; echo CONTINUED'` prints CONTINUED. This is the same class as the `esbuild ... | node -` exit-0 defect this repo already owns. With pipefail, every one of the eleven behaviours and `tsc --noEmit` can fail the gate — which is what makes the mutation-proofs above real.

Symlink signal: drop `preserveRunnerSymlinksPlugin()` from the plugins array, or move it after the assembly plugin such that it never runs, and the `[preserve-runner-symlinks]` regex finds nothing → the ABSENT assertion fires. Delete one symlink from `public/bin/arm64/darwin/*/_internal/` and it reports `restored 11` → the `(12,0,0)` tuple assertion fires. Break the destination tree so a record's parent is missing and it reports `skipped 1` → same assertion fires (and the plugin itself throws). A vacuous `restored 0 symlink(s)` — the exact Option-A failure this design was chosen to avoid — fails the tuple check. The DMG `shippedBin` byte band in Task 3 does NOT cover this: per RESEARCH.md §1b Tauri's `bundle.resources` copier dereferences symlinks on its own copy, so the shipped byte count is insensitive to whether the intermediate repo-tree `build/bin` symlinks were restored.

Prune signal: drop `pruneStaleHelperBinariesPlugin()` from the array → no `[prune-stale-helper-binaries]` line at all → the ABSENT assertion fires. Change either `console.log` format string → the regex, which is transcribed verbatim from `meta/pruneStaleHelperBinaries.ts:417` and `:419-421`, stops matching. `rm -rf build/bin` (or `rm -rf public/bin`) before the build → the PRE-BUILD roots loop finds no file under that root and the gate exits 1 BEFORE `pnpm exec vite build` is ever invoked. That loop MUST sit ahead of the build, and the earlier post-build form of it was decorative. Measured 2026-09-01: `rm -rf build/bin &amp;&amp; pnpm exec vite build` prints the vacuous `[prune-stale-helper-binaries] nothing to prune` — `buildStart` ran over an absent root, where `collectEntries` returns an empty Map — and yet `find build/bin -type f | wc -l` is 307 immediately afterwards, because vite's `prepareOutDir()` copies `publicDir` → `outDir` AFTER `buildStart` fires. The plugin's own header states that ordering (`meta/pruneStaleHelperBinaries.ts:386-387`: "`buildStart` fires at rollup input resolution -- strictly BEFORE vite's publicDir copy"). A post-build reading of `build/bin` is therefore repopulated by a mechanism wholly independent of the plugin and can never separate "nothing stale" from "nothing to look at". The post-build check was REMOVED, not kept alongside: for `build/bin` it was decorative, and for `public/bin` it was strictly redundant with the pre-build reading — `pruneStaleHelperBinaries` only ever calls `rmSync` on `resolveDestPath(buildBinDir, ...)` (`meta/pruneStaleHelperBinaries.ts:352-354`) and vite never writes into `publicDir`, so `public/bin` is identical before and after the build. A gate that looks like protection but cannot fire is worse than none. Note the gate deliberately ACCEPTS `nothing to prune` as a passing form: `build/bin` and `public/bin` are currently byte-for-byte identical in path set (0 entries in either direction, measured 2026-09-01), so `nothing to prune` is the CORRECT output on a healthy tree. Asserting against that string would be a gate that fails for the wrong reason.</mutation-proof>
  </verify>
  <done>All eleven behaviours pass (with `set -o pipefail`, so a red test actually fails the gate). `pnpm codecheck` clean. The vite build log carries `[preserve-runner-symlinks] restored 12 symlink(s), skipped 0, rejected 0` and a `[prune-stale-helper-binaries]` line, with `build/bin` and `public/bin` proven non-empty BEFORE the build (a post-build reading of `build/bin` proves nothing — vite repopulates it after `buildStart`). `viteRendererConfig.test.ts` asserts all three plugin names. `packagingConfig.test.ts` green (pins 1, 2, 6, 8 unmoved). A real `pnpm exec vite build` produces `build/renderer/` containing index.html, assets/ (~78 files), about.html, icon.png and locales/, totalling ~10,195,363 B — with strictly fewer asset files than `build/assets`.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint frontendDist, close the fail-open workflow guard, build and measure</name>
  <files>src-tauri/tauri.conf.json, .github/workflows/release-tauri.yml, src/backend/__tests__/releaseWorkflow.test.ts, src/frontend/index.tsx, src-tauri/build.rs, src-tauri/src/main.rs, .planning/quick/260901-b8z-repoint-frontenddist-at-renderer-only-dir/260901-b8z-MEASUREMENTS.md</files>
  <action>
**3a — Repoint.** `src-tauri/tauri.conf.json:7`: `"frontendDist": "../build"` →
`"frontendDist": "../build/renderer"`. Leave `bundle.resources` and all three platform overlay
files completely untouched — their sources stay `../build/...` and their targets stay
`build/...` (repointing sources at `../public/` would break the pin at
`packagingConfig.test.ts:133`). `src/backend/constants/paths.ts` needs NO change: packaged
`publicDir` resolves via `GAMELIB_APP_ROOT` → `resource_dir()` → `Contents/Resources/build`,
populated entirely by `bundle.resources` TARGET paths. `frontendDist` never participates.

**3b — Close the fail-open sibling.** `.github/workflows/release-tauri.yml:425` is
`test -f build/index.html`. After the repoint that guards a directory which is no longer the
embed root: it keeps passing while `build/renderer` is empty. **ADD** a line after 425:
`test -f build/renderer/index.html`. Do NOT replace line 425 — `build/index.html` must still
exist (it is what the assembly copies FROM, and the existing test at :506/:514 pins it). This
is additive; it is the only change to this file.

For the record: the prune step deletes `build/main build/preload build/node-dist`,
`build/sea-config.json`, `build/sidecar-prep.blob` — none of which `build/renderer` needs, so
the step cannot eat the new tree.

**3c — Test the new guard**, in `src/backend/__tests__/releaseWorkflow.test.ts`. An untested
guard could be a no-op typo:
  - Extend `seedBuildTree` (:442) to also create `build/renderer/index.html` and
    `build/renderer/assets/index-abc123.js`.
  - Add `join('build','renderer','index.html')` to the survives-the-prune assertion at :514.
  - Add a SEVENTH `test(` after the existing one at :541: remove
    `build/renderer/index.html` from the seeded tree, run the step, assert non-zero exit.
    Mirror the shape of the existing test at :524-531 that does exactly this for
    `build/index.html`.

**3d — Documentation accuracy** (each is now doubly wrong — wrong path AND "electron-vite"):
  - `src-tauri/build.rs:3` and `src-tauri/src/main.rs:4`: update the `../build (the
    electron-vite renderer output)` prose to name `../build/renderer` and drop "electron-vite".
  - `src/frontend/index.tsx:115`: delete the dead `addPath: 'build/locales/{{lng}}/{{ns}}'`
    line, replacing it with a one-line comment recording WHY it is gone — `addPath` is reached
    only via `backendConnector.saveMissing`, and `saveMissing` is nowhere set in this repo, so
    it never executed. Its `build/...` prefix would be actively wrong under the new layout and
    is a trap sitting one line above the `loadPath` this whole task is about.
    **`loadPath` at :116 is LIVE and must not be touched.**

**3e — Measure the OLD artifact BEFORE building (it is about to be overwritten).**
```bash
cp src-tauri/target/release/bundle/dmg/GameLib_0.7.0_aarch64.dmg /tmp/b8z-baseline-prea2w.dmg
hdiutil attach -nobrowse -readonly /tmp/b8z-baseline-prea2w.dmg
size -m /Volumes/GameLib/GameLib.app/Contents/MacOS/gamelib-shell | grep -A2 '__TEXT'
strings /Volumes/GameLib/GameLib.app/Contents/MacOS/gamelib-shell | grep -c 'bin/x64/win32/gogdl.exe'
python3 -c "
import os
t=0
for r,d,f in os.walk('/Volumes/GameLib/GameLib.app/Contents/Resources/build/bin'):
    for n in f:
        p=os.path.join(r,n)
        if not os.path.islink(p): t+=os.lstat(p).st_size
print('shipped bin apparent bytes:',t)"
hdiutil detach /Volumes/GameLib
```
Apparent bytes only — **never `du`**: APFS block allocation produced a ~7,916 KiB phantom delta
on byte-identical trees in quick-260901-a2w. Label this in MEASUREMENTS.md as the **PRE-a2w**
baseline and state plainly that ~23 MB of any `__const` delta against it is attributable to
quick-260901-a2w, not to this task.

**3f — Build.** No `tauri:build` script exists. `createUpdaterArtifacts: true` at
`tauri.conf.json:47` FAILS (`TAURI_SIGNING_PRIVATE_KEY` unset; the key file is
password-protected and the password is unavailable), so it MUST be overridden on the command
line — **never by editing the repo**:
```bash
set -o pipefail
rm -rf build/renderer
rm -f /tmp/b8z-vite-build-release.log
# PRE-BUILD roots check. It MUST run here, before the build, not in the gate after it:
# the prune plugin evaluates build/bin at `buildStart`, which fires strictly before
# vite's publicDir copy (meta/pruneStaleHelperBinaries.ts:386-387), so a post-build
# reading of build/bin is one vite has already repopulated from public/bin.
for d in build/bin public/bin; do
  [ -n "$(find "$d" -type f -print -quit 2>/dev/null)" ] || { echo "PRE-BUILD FAIL: $d missing/empty"; exit 1; }
  echo "[b8z] PRE-BUILD ROOT OK: $d" >> /tmp/b8z-vite-build-release.log
done
pnpm exec vite build 2>&1 | tee -a /tmp/b8z-vite-build-release.log
pnpm build:sidecar-sea
pnpm exec tauri build --config '{"bundle":{"createUpdaterArtifacts":false}}'
```
`set -o pipefail` is REQUIRED on the `tee` line: without it a failing `vite build` returns tee's
exit 0 and the sidecar/tauri steps would run over a broken renderer tree. `tee -a` (append) is
also required, not cosmetic: the two `PRE-BUILD ROOT OK` markers are written to the log before
the build, and a plain `tee` would truncate them away. The log file is not optional — this
task's automated gate asserts on both the markers and the build output.
Estimate ~7-14 min (LOW confidence, not timed on this machine). The codegen embed step should
get materially faster — brotli over ~10 MB instead of ~420 MB. If it does NOT, treat that as
evidence the assembly did not take effect.

**3g — Measure the NEW artifact** with the identical procedure (mount the freshly built DMG;
Tauri deletes the intermediate `.app` when only `dmg` is targeted). Capture: `__TEXT,__const`
(**NOT** `__DATA_CONST,__const`, which is ~944,424 B and is a different section), `__TEXT,__text`,
the gogdl `strings` count, shipped `Resources/build/bin` apparent bytes, DMG size, and the
`tauri-codegen-assets` staging dir (newest-mtime
`src-tauri/target/release/build/gamelib-shell-*/out/tauri-codegen-assets`, expected ~250 MB →
~10 MB).

Also confirm the two no-regression signals from the release vite build, which is why 3f tees its
output to `/tmp/b8z-vite-build-release.log`. Both are ASSERTED by this task's automated gate —
do NOT eyeball them:

  - `[preserve-runner-symlinks] restored 12 symlink(s), skipped 0, rejected 0`. The format
    string is verbatim from `meta/preserveRunnerSymlinks.ts:234`; the count 12 is
    `find public -type l | wc -l` = 12 and `find build -type l | wc -l` = 12, both measured
    2026-09-01. A vacuous `restored 0` is the Option-A failure this design exists to avoid and
    must fail the gate.
  - a `[prune-stale-helper-binaries]` line in one of its two source-verified forms:
    `nothing to prune` (`meta/pruneStaleHelperBinaries.ts:417`) or
    `pruned N entr{y|ies}, B bytes freed` (`:419-421`).

    **`nothing to prune` is the CORRECT and EXPECTED output here — do not treat it as a
    failure.** `build/bin` and `public/bin` are currently identical in path set (0 entries in
    either direction, measured 2026-09-01), because quick-260901-a2w already removed the stale
    files. There is nothing left to prune, so a gate asserting "not nothing to prune" would be
    red on a healthy tree: a gate failing for the wrong reason.

    The real hazard is different: `nothing to prune` is ALSO what a MISSING root prints, since
    `collectEntries` returns an empty Map for an absent directory. The discriminator is
    therefore not the log text but the roots — read BEFORE the build, in 3f, never after. The
    plugin evaluates `build/bin` at `buildStart`, which fires strictly before vite's publicDir
    copy (`meta/pruneStaleHelperBinaries.ts:386-387`), so a post-build reading of `build/bin`
    is one vite has already repopulated from `public/bin` and proves nothing about what the
    plugin saw. The automated gate therefore asserts that the prune line is present in one of
    its two exact forms AND that 3f's `[b8z] PRE-BUILD ROOT OK:` marker is in the log for both
    roots — so "nothing stale" cannot be confused with "nothing to look at".

Write `260901-b8z-MEASUREMENTS.md` recording every number with its derivation, old-shipped vs
new-shipped ONLY — never `repo tree − shipped tree`, the wrong pair that already produced a
phantom shortfall and two retracted explanations in this task series.
  </action>
  <verify>
    <automated>set -o pipefail; cd /Users/graysonmitchell/Projects/GameLib && pnpm exec jest src/backend/__tests__/releaseWorkflow.test.ts src/backend/__tests__/packagingConfig.test.ts 2>&1 | tail -15 && python3 -c "
import json,re,subprocess,glob,os
w=open('.github/workflows/release-tauri.yml').read()
assert 'test -f build/renderer/index.html' in w, 'new guard MISSING'
assert 'test -f build/index.html' in w, 'original guard was REPLACED, must be additive'
d=json.load(open('src-tauri/tauri.conf.json'))
assert d['build']['frontendDist']=='../build/renderer', d['build']['frontendDist']
src=[l for l in open('src/frontend/index.tsx') if not l.strip().startswith('//')]
assert not any('addPath' in l for l in src), 'dead addPath still present in CODE'
assert any(\"loadPath: 'locales/{{lng}}/{{ns}}.json'\" in l for l in src), 'LIVE loadPath was damaged'
log=open('/tmp/b8z-vite-build-release.log', encoding='utf-8', errors='replace').read()
sm=re.search(r'\[preserve-runner-symlinks\] restored (\d+) symlink\(s\), skipped (\d+), rejected (\d+)',log)
assert sm, 'RELEASE build log has NO preserve-runner-symlinks line — 3f was not teed, or the plugin was dropped'
assert (int(sm.group(1)),int(sm.group(2)),int(sm.group(3)))==(12,0,0), f'symlink restore REGRESSED in the release build: {sm.group(0)}'
pm=re.search(r'\[prune-stale-helper-binaries\] (nothing to prune|pruned \d+ entr(?:y|ies), \d+ bytes freed)',log)
assert pm, 'RELEASE build log has NO prune-stale-helper-binaries line'
for root in ['build/bin','public/bin']:
    assert f'[b8z] PRE-BUILD ROOT OK: {root}' in log, f'no PRE-BUILD ROOT OK marker for {root} — 3f skipped its pre-build roots check, so a vacuous `nothing to prune` over an absent root is indistinguishable from the healthy one'
print('config + release-build-log gates PASS: '+sm.group(0)+' | '+pm.group(0))" && hdiutil attach -nobrowse -readonly src-tauri/target/release/bundle/dmg/GameLib_0.7.0_aarch64.dmg >/dev/null && python3 -c "
import subprocess,os,sys
S='/Volumes/GameLib/GameLib.app/Contents/MacOS/gamelib-shell'
assert os.path.isfile(S), 'shell binary MISSING from the mounted DMG'
o=subprocess.run(['size','-m',S],capture_output=True,text=True).stdout
seg=None;const=None
for line in o.splitlines():
    if line.strip().startswith('Segment'): seg=line.split()[1].rstrip(':')
    if '__const' in line and seg=='__TEXT': const=int(line.split()[-1])
st=subprocess.run(['strings',S],capture_output=True,text=True)
assert st.returncode==0 and st.stdout, 'strings FAILED or produced no output — criterion 2 is unscoreable, not passing'
g=sum(1 for L in st.stdout.splitlines() if 'bin/x64/win32/gogdl.exe' in L)
t=0
R='/Volumes/GameLib/GameLib.app/Contents/Resources/build/bin'
assert os.path.isdir(R), 'shipped build/bin MISSING — fix (1) regressed'
for r,d,f in os.walk(R):
    for n in f:
        p=os.path.join(r,n)
        if not os.path.islink(p): t+=os.lstat(p).st_size
HELPERS=['arm64/darwin/legendary/legendary','arm64/darwin/gogdl/gogdl','arm64/darwin/nile/nile','arm64/darwin/comet','arm64/darwin/steam-bridge-helper']
for h in HELPERS:
    hp=os.path.join(R,h)
    assert os.path.isfile(hp), f'shipped helper MISSING: {h}'
    assert os.access(hp,os.X_OK), f'shipped helper NOT executable: {h}'
print(f'__TEXT,__const={const}  gogdl={g}  shippedBin={t}  helpers x-bit OK={len(HELPERS)}')
assert const is not None and const < 30_000_000, f'CRITERION 1 FAIL: __const={const}'
assert g==0, f'CRITERION 2 FAIL: gogdl strings count={g}'
assert 230_000_000 < t < 260_000_000, f'NON-REGRESSION FAIL: shipped bin {t} not ~233.8 MiB'
print('CRITERIA 1+2 and non-regression PASS')"; R=$?; hdiutil detach /Volumes/GameLib >/dev/null; exit $R</automated>
    <mutation-proof>Revert `frontendDist` to `../build` → `__const` returns to ~220 MB and criterion 1 fails; the gogdl count returns to 1 and criterion 2 fails. Remove the added workflow line → the `new guard MISSING` assertion fires AND the seventh releaseWorkflow test goes red. REPLACE line 425 instead of appending → the `must be additive` assertion fires (this is the direction a one-sided grep would miss). Delete the LIVE `loadPath` while removing `addPath` → the `loadPath was damaged` assertion fires. Comment-stripping the `index.tsx` read means a header comment mentioning `addPath` cannot satisfy the gate — the self-invalidating-prose trap. `os.path.isdir(R)` makes a MISSING shipped `build/bin` a hard fail rather than a vacuous zero-byte pass. The `__const` parser is anchored to `Segment __TEXT`, so it cannot silently read the ~944,424 B `__DATA_CONST,__const` and report a false pass.

`set -o pipefail` is load-bearing: without it `pnpm exec jest ... 2>&amp;1 | tail -15` returns tail's exit 0, so the new seventh `releaseWorkflow` test and the `packagingConfig` pins 2/6/8 could all be RED while the gate walked on to mount the DMG and report PASS. Confirmed empirically: `bash -c 'false | tail -5 &amp;&amp; echo CONTINUED'` prints CONTINUED.

The criterion-2 measurement no longer runs through a shell pipe. `strings ... | grep -c` fails OPEN: if `strings` cannot read the binary, `grep -c` still prints `0` and the old `g=='0'` comparison PASSED on a binary that was never examined. It now runs `strings` directly, asserts a zero return code AND non-empty stdout, and counts matching lines in Python — so an unreadable binary is a hard FAIL, not a vacuous criterion-2 pass.

Executability (must_haves truth 5): `chmod -x` any one of the five named shipped helpers, or have Tauri's resource copy drop the mode bits, and the `NOT executable` assertion fires naming the file. Removing a helper from `bundle.resources` fires the `MISSING` assertion. This closes the gap where a shipped `build/bin` in the 230M-260M byte band could consist entirely of non-executable payload and still pass.

Release-build-log assertions: skip the `tee` in 3f and `/tmp/b8z-vite-build-release.log` does not exist → the `open()` raises and the gate fails (it cannot pass on an absent log). Drop either sibling plugin from the vite array → its regex finds nothing. A vacuous `restored 0 symlink(s)` fails the `(12,0,0)` tuple check. `rm -rf build/bin` (or `public/bin`) before 3f → 3f's PRE-BUILD roots loop exits 1 before `vite build` runs, so no release log is produced at all and this gate's `open()` raises. Delete the pre-build loop from 3f while still building → the `[b8z] PRE-BUILD ROOT OK:` markers are absent from the log and the marker assertion fires. Replace 3f's `tee -a` with `tee` → the markers are truncated and the same assertion fires. The check had to move ahead of the build because a post-build one cannot fire: measured 2026-09-01, `rm -rf build/bin &amp;&amp; pnpm exec vite build` prints the vacuous `nothing to prune` yet leaves `build/bin` at 307 files, since vite's publicDir copy runs strictly after the plugin's `buildStart` (`meta/pruneStaleHelperBinaries.ts:386-387`).

The symlink assertion does NOT share this ordering flaw — verified, not assumed. `preserveRunnerSymlinksPlugin` runs at `closeBundle` (`meta/preserveRunnerSymlinks.ts:228`), i.e. AFTER vite's publicDir copy has dereferenced the symlinks, so its restore is doing real work; and the gate asserts the plugin's OWN reported `(restored, skipped, rejected)` tuple from `meta/preserveRunnerSymlinks.ts:234`, not a filesystem state that some later step could repopulate. There is no mechanism that can make `restored 12` true other than the plugin finding and recreating 12 symlinks: an empty or symlink-free `public/` yields `restored 0` and fails the tuple. The prune check was flawed precisely because it measured a directory rather than the plugin's output; the symlink check already measures the output.</mutation-proof>
  </verify>
  <done>`frontendDist` is `../build/renderer`. The workflow carries BOTH guards. `releaseWorkflow.test.ts` has seven tests, all green; `packagingConfig.test.ts` green (both under `set -o pipefail`, so a red test fails the gate). `__TEXT,__const` &lt; 30,000,000 B and the gogdl count is 0, both measured on a mounted release DMG. Shipped `Resources/build/bin` is ~233.8 MiB and all five named arm64/darwin helpers are present with the executable bit set. The release vite build log carries `restored 12 symlink(s), skipped 0, rejected 0` and a `[prune-stale-helper-binaries]` line over non-empty roots. `260901-b8z-MEASUREMENTS.md` records old-shipped vs new-shipped with the PRE-a2w attribution caveat stated explicitly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: LIVE gate — non-English locale and About window + icon on the packaged artifact</name>
  <files>.planning/quick/260901-b8z-repoint-frontenddist-at-renderer-only-dir/260901-b8z-MEASUREMENTS.md, .planning/todos/pending/2026-08-28-tauri-macos-bundle-is-786mb-frontenddist-embeds-build-bin-in.md</files>
  <action>Present both gestures below to the human EXACTLY as written, wait for a per-item verdict, then record the outcome. Do NOT self-approve: `workflow.auto_advance` does not apply — criteria 3 and 4 are unreachable by any automated check available in this repo, which is precisely why they are a blocking human gate. After the verdict, append the live results to `260901-b8z-MEASUREMENTS.md` and update the todo per the recording rules at the end of this task.</action>
  <what-built>
Criteria 3 and 4 CANNOT be met at config level and CANNOT be met by `pnpm tauri:dev` — it
serves over `devUrl` (`http://localhost:5173`) and resolves NO bundled static asset, so it is
structurally blind to every failure this task can cause. That is the R-34.5-G1-PKG defect
shape, already paid for twice. These two gestures require the human and a real packaged
artifact.

Both consumers being proven here resolve RELATIVE to `frontendDist`, so both break silently
and only in a packaged build if the assembly is wrong:
  - `src/frontend/index.tsx:116` — i18next `loadPath: 'locales/{{lng}}/{{ns}}.json'`
  - `src/preload/api/tauriChildWindows.ts:177` — `url: 'about.html?v=' + version`
  - `public/about.html:50` — `<img src="./icon.png">`, the consumer the original todo MISSED

Claude has already produced and mounted the release DMG in Task 3.
  </what-built>
  <how-to-verify>
**GESTURE A — a non-English locale renders (criterion 3).**

1. Quit any running GameLib.
2. Back up the store:
   `cp ~/Library/Application\ Support/gamelib/store/config.json /tmp/gamelib-config.bak`
3. Set the language to French:
   `python3 -c "import json,pathlib; p=pathlib.Path.home()/'Library/Application Support/gamelib/store/config.json'; d=json.loads(p.read_text()); d['language']='fr'; p.write_text(json.dumps(d,indent=2))"`
4. Mount the DMG and launch `GameLib.app` (from the mounted volume or after dragging to
   `/Applications`). Open the Web Inspector Network tab BEFORE the app finishes loading if you
   want the corroborating signal.
5. **PASS =** the sidebar / library chrome renders in **FRENCH**.
   **FAIL =** it renders in English, or shows raw key names such as
   `sidebar.library` / `Login.message`.
   Corroborating (optional but decisive): Network tab shows
   `tauri://localhost/locales/fr/translation.json` returning **200**, not 404.
   `public/locales/fr/` really does exist and holds `gamelib.json`, `gamelib.mt.json`,
   `gamepage.json`, `login.json`, `translation.json`.
   **ANTI-VACUITY — read this before scoring.** "The app opened" proves NOTHING. A missing
   `locales/` does not blank the UI: i18next falls back to `fallbackLng: 'en'` or to the raw
   key. The gate is specifically *"French text appears where English was"*.
6. Restore:
   `cp /tmp/gamelib-config.bak ~/Library/Application\ Support/gamelib/store/config.json`

**GESTURE B — the About window opens AND its icon renders (criterion 4).**

There is no renderer UI button for this — `grep -rn "showAboutWindow" src/frontend` returns
zero. The ONLY trigger is the tray menu (`src-tauri/src/main.rs:634`).

1. With the packaged app running, click the **menu-bar tray icon** → **"About GameLib"**.
2. Score these **separately** — a window that opens is not by itself a pass:
   - **PASS/FAIL 1:** a 420x380 window titled "About GameLib" appears. (A 404 yields an empty
     or error webview, not the absence of a window — so judge the CONTENT, not the window.)
   - **PASS/FAIL 2:** the **GameLib icon renders at the top of that window**. This is the
     highest-value observation in the entire gate — `icon.png` is the consumer the original
     todo missed, and it appears in ZERO built JS chunks, so nothing else in this plan can
     prove it. A broken-image placeholder or blank space is a **FAIL**.
   - **INFORMATIONAL ONLY, MUST NOT FAIL THE GATE:** the version line. `Version: unknown` is a
     1-second sidecar race — `resolveAboutVersion` races `getHeroicVersion()` against
     `ABOUT_VERSION_TIMEOUT_MS = 1000` at `src/preload/api/tauriChildWindows.ts:153` and
     resolves `'unknown'` on timeout. It indicates a slow sidecar, NOT a frontendDist failure.
     Do not let it fail this gate.
3. Corroborating (optional): in the **about window's** Network tab,
   `tauri://localhost/about.html?v=...` → 200 and `tauri://localhost/icon.png` → 200.

**Console note:** neither gesture requires the DevTools console. The Tauri DevTools console
cannot be pasted into, and a wedged Web Inspector console executes nothing — both gestures are
driven from the filesystem and the tray, deliberately.

**After the verdict, Claude must record results** (do not skip — a plan that stops at a green
build is exactly the failure mode this repo catalogues):
  - Append the live results to `260901-b8z-MEASUREMENTS.md`.
  - Update the todo: mark **fix (2) DONE** with the real measured `__const`, gogdl count, DMG
    size and staging-dir numbers, and record the PRE-a2w attribution caveat.
  - **Leave the todo in `.planning/todos/pending/`.** It must NOT be moved to done: Cause 3
    (Tauri symlink dereferencing, ~45 MB) and the `steam_api.pdb`/`steam_api_shim.lib` item are
    both still OPEN within that same file and are explicitly out of scope here. Re-scope those
    two clearly as the remaining work.
  </how-to-verify>
  <verify>
    <human-check>Gesture A verdict, Gesture B item 1 verdict, Gesture B item 2 (icon) verdict — each scored separately by the human.</human-check>
    <automated>python3 -c "
import os,glob
m=glob.glob('.planning/quick/260901-b8z-*/260901-b8z-MEASUREMENTS.md')
assert m, 'MEASUREMENTS.md missing'
t=open(m[0]).read()
for k in ['__const','gogdl','Gesture A','Gesture B']:
    assert k in t, f'MEASUREMENTS.md never records {k}'
td=glob.glob('.planning/todos/pending/2026-08-28-tauri-macos-bundle-is-786mb-*.md')
assert td, 'todo was MOVED out of pending — Cause 3 and steam_api.pdb are still OPEN, it must stay'
b=open(td[0]).read()
assert 'Fix (2)' in b and 'NOT YET PLANNED' not in b, 'todo still says fix (2) is unplanned'
print('records gate PASS')"</automated>
    <mutation-proof>Moving the todo to `.planning/todos/done/` fails the `still in pending` assertion — this is the specific wrong-close this task must not perform, since Cause 3 and the `steam_api.pdb` item remain open in that same file. Leaving the todo's `NOT YET PLANNED` heading in place fails the status assertion. Writing a MEASUREMENTS.md that records only the build numbers and omits the live verdicts fails the `Gesture A`/`Gesture B` key checks, so a plan that stops at a green build cannot satisfy this gate.</mutation-proof>
  </verify>
  <done>Gesture A PASS (French chrome renders, not English and not raw keys) AND Gesture B item 1 PASS (About window renders content) AND Gesture B item 2 PASS (icon renders, not a broken-image placeholder). `Version: unknown` is informational and does NOT fail this gate. Live results appended to 260901-b8z-MEASUREMENTS.md; the todo records fix (2) DONE with real numbers and REMAINS in .planning/todos/pending/ with Cause 3 and the steam_api.pdb item re-scoped as the open remainder.</done>
  <resume-signal>Report per-item: Gesture A pass/fail, Gesture B item 1 pass/fail, Gesture B item 2 (icon) pass/fail. Type "approved" only if A, B1 and B2 all pass. Otherwise describe exactly what rendered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `tauri://localhost/` → embedded asset store | The only new boundary: the set of files servable to the webview shrinks from all of `build/` to the renderer subset. Strictly narrowing. |
| repo `build/` → shipped artifact | Unchanged — `bundle.resources` sources and targets are untouched by this task. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-b8z-01 | Information disclosure | `frontendDist` embed | mitigate | This task REMOVES ~410 MB of helper binaries, the Electron main bundle and the SEA prep blob from the webview-servable embed. Net reduction in exposed surface; verified by criterion 2 (`gogdl.exe` strings count → 0). |
| T-b8z-02 | Denial of service | assembly step | mitigate | A silently empty/partial `build/renderer` ships a white screen. Mitigated by six fail-loud post-conditions in `assembleRendererDist`, each independently unit-tested (Tests 6-11), plus the additive `test -f build/renderer/index.html` guard in the release workflow and its dedicated seventh executing test. |
| T-b8z-03 | Tampering | npm/pip/cargo installs | n/a | **No package installs in this task.** No dependency is added, removed or upgraded; `meta/assembleRendererDist.ts` uses only `node:fs`/`node:path` and vite's existing `Plugin` type. The Package Legitimacy Gate does not apply. |
| T-b8z-04 | Tampering | updater signing | accept | `createUpdaterArtifacts` is overridden on the COMMAND LINE only, never by repo edit, so the shipped config still requires signing in CI. Local artifacts are unsigned/ad-hoc (0 codesigning identities on this Mac) and are used for measurement only, never distributed. |
</threat_model>

<verification>
Config-level (Task 2 + 3 automated gates):
- `meta/__tests__/assembleRendererDist.test.ts` — 11 behaviours, both copy directions
- `meta/__tests__/viteRendererConfig.test.ts` — pins 1 and 5 unchanged; three plugin names asserted
- `src/backend/__tests__/packagingConfig.test.ts` — pins 2, 6, 8 proven green, not assumed
- `src/backend/__tests__/releaseWorkflow.test.ts` — pin 3 plus the new seventh test
- `pnpm codecheck` clean

Artifact-level (Task 3): `__TEXT,__const` &lt; 30,000,000 B; gogdl strings count 0 (measured
without a shell pipe, so an unreadable binary FAILS rather than reporting 0); shipped
`Resources/build/bin` ~233.8 MiB with `os.access(X_OK)` proven on
`arm64/darwin/{legendary/legendary, gogdl/gogdl, nile/nile, comet, steam-bridge-helper}`.

Build-log-level (asserted in BOTH Task 2 and Task 3, never eyeballed — the release build is
teed to `/tmp/b8z-vite-build-release.log`): `[preserve-runner-symlinks] restored 12 symlink(s),
skipped 0, rejected 0` (format verbatim from `meta/preserveRunnerSymlinks.ts:234`; 12 =
`find public -type l | wc -l`, measured 2026-09-01), and a `[prune-stale-helper-binaries]` line
in one of its two source-verified forms (`meta/pruneStaleHelperBinaries.ts:417` /
`:419-421`) over `build/bin` and `public/bin` roots proven non-empty BEFORE the build — inline
in Task 2's gate, and in Task 3 via 3f's `[b8z] PRE-BUILD ROOT OK:` log markers. `nothing to
prune` is an ACCEPTED form — a2w already removed everything stale, so it is the correct output;
the PRE-build roots check is what separates it from the missing-directory reading. It has to be
pre-build: `buildStart` precedes vite's publicDir copy
(`meta/pruneStaleHelperBinaries.ts:386-387`), so a post-build `build/bin` reading is repopulated
to 307 files regardless of what the plugin did — measured.

The symlink assertion does NOT share that flaw. `preserveRunnerSymlinksPlugin` runs at
`closeBundle` (`meta/preserveRunnerSymlinks.ts:228`), after the publicDir copy has dereferenced
the symlinks, and the gate asserts the plugin's OWN `(restored, skipped, rejected)` tuple rather
than a directory state a later step could restore — so `restored 12` is load-bearing.

Every `&amp;&amp;` chain in every automated gate runs under `set -o pipefail`. Without it,
`cmd 2>&amp;1 | tail -N` returns tail's exit 0 and a wholly red test run flows into the
downstream file checks as a PASS — the `esbuild ... | node -` defect class this repo already
owns.

Live (Task 4, human): French locale renders; About window opens with its icon.

Not relied upon: `check:build-bin-mirror` is invoked by nothing (not CI, not husky), so it is
NOT a regression guard for this task. `.husky/pre-push` has red gates repo-wide; nothing here
needs pushing.
</verification>

<success_criteria>
1. `__TEXT,__const` &lt; 30,000,000 B on a mounted release DMG (from 223,766,872 B) — MEASURED
2. `strings <gamelib-shell> | grep -c 'bin/x64/win32/gogdl.exe'` == 0 — MEASURED
3. LIVE: the packaged app renders a non-English locale — HUMAN, Gesture A
4. LIVE: the packaged app opens the About window AND its icon renders — HUMAN, Gesture B items 1 and 2
5. Non-regression: shipped `Resources/build/bin` still ~233.8 MiB, helpers executable, symlink
   plugin still restores 12
6. `260901-b8z-MEASUREMENTS.md` records old-shipped vs new-shipped in apparent bytes, with the
   PRE-a2w baseline caveat stated
7. The todo records fix (2) DONE with real numbers and REMAINS pending, with Cause 3 and the
   `steam_api.pdb` item re-scoped as the open remainder

Criteria 3 and 4 cannot be satisfied by config-level evidence, and the plan must not claim
completion without them.
</success_criteria>

<output>
Create `.planning/quick/260901-b8z-repoint-frontenddist-at-renderer-only-dir/260901-b8z-SUMMARY.md`
and `260901-b8z-MEASUREMENTS.md` when done. Update the todo file in place; do NOT move it out
of `.planning/todos/pending/`.

Stay on branch `fix/steam-native-install-stability`. Do not create or switch branches. Never
`git stash`, `git reset`, or `git checkout -- <file>` (the post-checkout hook triggers a helper
download that can throw) — restore files with `cp`.
</output>
