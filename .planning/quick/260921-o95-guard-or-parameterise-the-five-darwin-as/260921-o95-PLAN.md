---
phase: quick-260921-o95
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/sidecar/__tests__/nativeImageShim.test.ts
  - src/backend/sidecar/__tests__/shortcutsFlows.test.ts
  - meta/__tests__/verifyRunnerBundle.test.ts
  - src/backend/shortcuts/__tests__/shortcutsExistsFallback.test.ts
  - src/backend/__tests__/utils.test.ts
  - .planning/todos/pending/2026-09-15-darwin-asserting-suites-fail-on-the-linux-ci-runner.md
  - .planning/todos/completed/2026-09-15-darwin-asserting-suites-fail-on-the-linux-ci-runner.md
  - .planning/todos/pending/2026-09-21-confirm-the-five-guarded-suites-run-green-on-the-linux-ci-runner.md
autonomous: true
requirements: [QUICK-260921-O95]

must_haves:
  truths:
    - "On this macOS arm64 machine all five suites still run their FULL pre-change test counts with ZERO skipped: shortcutsExistsFallback 6, shortcutsFlows 31, nativeImageShim 12, utils 22, verifyRunnerBundle 36. A guard that skips on macOS is deletion with extra steps and fails this plan."
    - "`meta/__tests__/verifyRunnerBundle.test.ts` and `src/backend/shortcuts/__tests__/shortcutsExistsFallback.test.ts` contain NO skip of any kind — no `describe.skip`, no `it.skip`, no conditional describe. Only the darwin-specific EXPECTATION inside one test in each file is scoped; both files run every test on every host."
    - "`src/backend/__tests__/utils.test.ts` contains NO platform guard added by this task. The x64 fallback case is fixed by pinning `process.arch`, because the measurement shows the discriminator is `process.arch` (utils.ts:544-554), not `process.platform` — a platform guard there would be a green check proving nothing on every Intel Mac and would stop testing box64 on arm64 Linux."
    - "The two `sips`-dependent suites gate at MODULE scope on the host platform captured before any `overrideProcessPlatform` call, so the `/usr/bin/sips` spawn is never reached on a non-darwin host. Proven locally by temporarily forcing the constant false and observing the suites PASS with the expected skip counts and no ENOENT."
    - "Nothing platform-independent is gated away: `shortcutsFlows` keeps 27 of its 31 tests running on a non-darwin host (only the 4-test darwin `.app` describe is gated) and `nativeImageShim` keeps 3 (the two no-converter factory tests plus the fast-path test that explicitly claims steamhelper.ts:121 works without sips)."
    - "No CI workflow file, no jest config and no production source file is modified. `git diff --name-only` lists exactly the five test files plus `.planning/` paths."
    - "`meta/__tests__/lintTranslations.test.ts` is byte-identical to HEAD — the sixth CI failure is a separately-closed defect and is explicitly out of scope."
    - "`pnpm lint` exits 0 with BOTH warning counts unmoved from the step-0 baseline and neither `SRC_CEILING` (1124) nor `TESTS_CEILING` (638) raised; all five edited files are prettier-clean."
    - "The parent todo lives in `.planning/todos/completed/` and its content AT THE DESTINATION records (a) the per-suite remedy actually applied, (b) the research's arch correction for `utils.test.ts`, and (c) exactly which claims were NOT measured — the Linux run and the x64 run."
    - "A new pending todo carries the one thing this task cannot measure — that the five suites are green on the Linux runner — with `severity`/`platform`/`ready` bare, lowercase, in that order, so `pnpm planning-gates` stays green."
  artifacts:
    - path: "src/backend/sidecar/__tests__/nativeImageShim.test.ts"
      provides: "darwin-gated sips fixture + 3 tests hoisted into a host-independent describe"
      contains: "describeOnDarwin"
    - path: "src/backend/sidecar/__tests__/shortcutsFlows.test.ts"
      provides: "darwin-gated sips fixture and darwin-gated .app describe; other 6 describes unguarded"
      contains: "describeOnDarwin"
    - path: "meta/__tests__/verifyRunnerBundle.test.ts"
      provides: "per-host signature expectation; ok-stays-true claim still asserted on every host"
      contains: "unknown:"
    - path: "src/backend/shortcuts/__tests__/shortcutsExistsFallback.test.ts"
      provides: "per-host path-pair expectation; non-empty/no-throw claim still asserted on every host"
      contains: "not.toEqual(menuFile)"
    - path: "src/backend/__tests__/utils.test.ts"
      provides: "arch-pinned x64-fallback fixture, constructible on every host arch"
      contains: "setArch('arm64')"
    - path: ".planning/todos/pending/2026-09-21-confirm-the-five-guarded-suites-run-green-on-the-linux-ci-runner.md"
      provides: "The single unmeasurable claim, carried forward rather than asserted"
      contains: "ready: blocked"
    - path: ".planning/todos/completed/2026-09-15-darwin-asserting-suites-fail-on-the-linux-ci-runner.md"
      provides: "Parent todo closed with the per-suite decision and the arch correction on disk"
      contains: "260921-o95"
  key_links:
    - from: "src/backend/__tests__/utils.test.ts"
      to: "src/backend/utils.ts"
      via: "archSpecificBinary reads process.arch at call time (utils.ts:547), so the test's pinned arch reaches it"
      pattern: "setArch\\('arm64'\\)"
    - from: "meta/__tests__/verifyRunnerBundle.test.ts"
      to: "meta/verifyRunnerBundle.ts"
      via: "getSignatureState's result.error branch returns unknown:<message> when codesign is absent"
      pattern: "\\^unknown:"
    - from: ".planning/todos/completed/2026-09-15-darwin-asserting-suites-fail-on-the-linux-ci-runner.md"
      to: ".planning/todos/pending/2026-09-21-confirm-the-five-guarded-suites-run-green-on-the-linux-ci-runner.md"
      via: "closing-note cross-reference by filename"
      pattern: "confirm-the-five-guarded-suites-run-green-on-the-linux-ci-runner.md"
---

<objective>
Action the pending todo `2026-09-15-darwin-asserting-suites-fail-on-the-linux-ci-runner.md`: stop the
five host-dependent suites from failing on the Linux CI runner, per-suite, using the remedy the
measurement supports — without skipping a single test on macOS and without gating away the
platform-independent coverage any of these files carry.

Purpose: `ci` is permanently red on PR branches, and a permanently-red gate stops being read. The
real defect found in the same run (816 unlocalised keys) was sitting in the same failure list. This
task removes the noise; it does not remove the signal.

Output: five test files edited (no production source, no CI workflow, no jest config), the parent
todo closed with its decisions recorded, and one new pending todo carrying the single claim that
cannot be measured on this machine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

Two CLAUDE.md conventions bind here. **Todo triage frontmatter** (Task 3): `/gsd-add-todo`'s
template does NOT emit `severity`/`platform`/`ready`, and omitting any of them turns CI red.
**Heroic is not upstream**: never frame any of these suites, or their assertions, as an
upstream deviation or an inherited defect.

@.planning/quick/260921-o95-guard-or-parameterise-the-five-darwin-as/260921-o95-RESEARCH.md
@.planning/todos/pending/2026-09-15-darwin-asserting-suites-fail-on-the-linux-ci-runner.md

<measured_at_head>
Everything below was measured at HEAD `8b767e95c` before this plan was written, by reading the five
test files and the three production files they drive. The research supplied the CAUSES (from the
real failing CI log); this section supplies the STRUCTURE, which is what decides where each guard
goes. Re-confirm the counts in Task 1 step 0; do not re-derive the structure.

**Baseline on this machine (macOS arm64), each suite run alone, `npx jest --runInBand --silent`:**

| suite | tests at HEAD |
| --- | --- |
| `src/backend/shortcuts/__tests__/shortcutsExistsFallback.test.ts` | 6 passed |
| `src/backend/sidecar/__tests__/shortcutsFlows.test.ts` | 31 passed |
| `src/backend/sidecar/__tests__/nativeImageShim.test.ts` | 12 passed |
| `src/backend/__tests__/utils.test.ts` | 22 passed |
| `meta/__tests__/verifyRunnerBundle.test.ts` | 36 passed |

All five must still report these exact numbers, with zero skipped, after every task.

**`nativeImageShim.test.ts` — the sips dependency is 9 of 12 tests, not 12.** The research's table
says "nothing meaningful survives without sips". Measured against the file, that is not quite
right, and the correction is the reason this file gets split rather than gated whole:

- `REAL_PNG` (`:81-97`) is a module-scope IIFE that shells out to `/usr/bin/sips`. This is the frame
  the CI log's `:97:3` points at — the suite fails to RUN, so a guard inside any `test()` body is
  unreachable. It must be gated at module scope.
- Nine tests read `REAL_PNG`/`REAL_ICO` (`:113 :123 :134 :145 :156 :162 :171 :200 :204`) — all
  darwin-only. `:204` is darwin-only too despite asserting the *linux* decline: it builds its input
  from `REAL_PNG`.
- **Three need no converter at all** and are genuine non-darwin coverage: `:183`
  (`createFromPath(nonexistent)` does not throw, `isEmpty()` true), `:196` (`createEmpty()`), and
  `:213` (the fast path, whose own name claims *"steamhelper.ts:121 works even without sips"* and
  which builds its own 5-byte JPEG inline). `isEmpty()` is `sourceBytes().length === 0`
  (`nativeImageShim.ts:240-242`) — pure byte length, no platform branch.

**`shortcutsFlows.test.ts` — the sips dependency is 4 of 31 tests.** `execFileSync('/usr/bin/sips',
…)` at `:214` is a bare module-scope statement building `FIXTURE_ICON_PATH`. `FIXTURE_ICON_PATH` has
exactly two other references: the `mockGetIcon.mockResolvedValue(...)` in the file-wide `beforeEach`
(`:241`) and nothing else. The only describe that actually drives `getIcon` → `convertPngToICNS` →
the real sips chain is **Describe 5, `addShortcut's darwin GAMELIB_SHELL_EXE pin` (`:528`), 4 tests**
(CONTROL, injection, UNSET, EMPTY). The other six describes — registration kind, `shortcutsExists`
fallback, containment, send-body safety, `processShortcut`, `addToSteam` pin, the RPC-frame
discriminator — never reach sips. 27 tests must keep running on a non-darwin host.

**`shortcutFiles` branches on `process.platform`, NOT on the mocked `isMac`** (`shortcuts.ts:159-176`):
darwin collapses both entries onto one `join(userHome, 'Applications', '<name>.app')`; linux returns
`${app.getPath('desktop')}/<name>.desktop` and `${userHome}/.local/share/applications/<name>.desktop`,
which necessarily differ. `shortcutsExistsFallback.test.ts` mocks `backend/constants/environment`
with `isMac: false` — that mock is irrelevant to this assertion and must not be "fixed".

**`shortcutsExists` consults BOTH entries** — `ipc_handler.ts:55` is
`existsSync(desktopFile ?? '') || existsSync(menuFile ?? '')` — so both files' no-regression tests
(which create only the `desktopFile` entry) still discriminate on linux.

**`verifyRunnerBundle.test.ts` — exactly ONE assertion is host-specific.** `getSignatureState`
(`verifyRunnerBundle.ts:533-558`) returns `unknown:${result.error.message.slice(0,200)}` when the
spawn itself fails, which on Linux is the CI log's verbatim `unknown:spawnSync codesign ENOENT`.
Only `:340` `expect(m.signature).toBe('unsigned')` fails. The file's other signature assertion,
`:583` `expect(...codesignDisplay).not.toMatch(/^signed:/)`, passes on both hosts (an `unknown:`
string does not start with `signed:`) — **do not touch it**. That single-failure reading is
corroborated by the run totals: `Tests: 5 failed` across `shortcutsExistsFallback` (1), `utils` (1),
`verifyRunnerBundle` (1) and `lintTranslations` (2); the other two suites failed at suite scope and
contributed zero failing tests.

**`utils.test.ts` — the discriminator is arch, and `archSpecificBinary` reads it at call time.**
`utils.ts:544-554` builds `archSpecificPath` from `process.arch` and `x64Path` from the literal
`'x64'`, both inside the function body. On an x64 host those two strings are identical, the test's
mock (`:141-144`, `!path.includes(join('bin', process.arch))`) denies both, and the production code
correctly throws. The sibling describe at `:170-232` already proves `process.arch` can be pinned
with `Object.defineProperty` and that `archSpecificBinary` honours it. The other two tests in the
G-1 block pass on any arch: `:132` reads `process.arch` symmetrically, and `:152` asserts two
substrings that merely collapse into one on x64 (weaker there, but passing) — **leave both alone**.

**Existing in-repo precedent for the guard shape** (use it; do not invent a new idiom):
`tauriConf.test.ts:359`, `releaseWorkflow.test.ts:401`, `meta/__tests__/updaterSigningKey.test.ts:28`
all read `const describeOnPosix = process.platform === 'win32' ? describe.skip : describe`.

**No gate polices skipped tests.** `meta/*.cjs` contains no `numPendingTests`/"skipped" logic, so a
conditional describe does not trip anything. The protection against a silent drop to zero is this
plan's exact-count verifies, nothing else.
</measured_at_head>

<traps>
**TRAP 1 — capture the host platform at MODULE LOAD, before any override.** Both sips suites call
`overrideProcessPlatform(...)` inside tests (`nativeImageShim.test.ts:26-30`,
`shortcutsFlows.test.ts:153-157`). A gate that reads `process.platform` lazily — inside a `beforeEach`,
or inside a helper called from a test — can observe a test's *faked* platform. Assign
`const HOST_IS_DARWIN = process.platform === 'darwin'` once, at module scope, above the fixture, and
read only that constant afterwards.

**TRAP 2 — a guard inside a `test()` body cannot save `nativeImageShim`.** The failure is at suite
scope, from the `:97` frame. The `REAL_PNG` IIFE itself must be gated, or the suite keeps failing to
run before any test-level guard is evaluated.

**TRAP 3 — do NOT gate `verifyRunnerBundle` or `shortcutsExistsFallback` at describe or file level.**
Each carries a platform-independent claim that the todo explicitly wants green on Linux:
*"signature reporting is not enforcement, `ok` stays true"* and *"returns a real non-empty pair and
does not throw"*. Scope the darwin-specific expectation only. After this task neither file contains
a skip of any kind.

**TRAP 4 — `setArch('x64')` already exists legitimately at `utils.test.ts:228`**, inside the 34.9
onedir describe. A bare repo-wide or file-wide grep for it will false-fire and convict correct code.
Task 2's verify scopes its check to the G-1 describe's line range with `awk`. If you write your own
check, scope it the same way.

**TRAP 5 — do not touch `meta/__tests__/lintTranslations.test.ts`.** It is the sixth failure in the
same CI run, is a genuine defect (816 unlocalised `humbleKeys.*` keys), and is already filed and
closed separately. Its byte-identity is asserted in Task 2's verify.

**TRAP 6 — no CI-environment or workflow change is warranted, for any of the five.** The research
refuted the todo's option 3 from the log itself: the runner binaries are present and readable; it is
`codesign`, the signing tool, that is absent, and there is no `codesign` to install on Linux.

**TRAP 7 — this task cannot prove CI goes green, and must not claim it.** Three things are
unmeasurable on this machine: the real Linux run of any suite, the x64-host arm of the `utils` case
(this Mac is arm64), and the behaviour on Linux of the 27 + 3 tests that have *never once executed
there* — both sips suites have only ever failed at import time, so the log says nothing about them.
Say this plainly in the SUMMARY and in the todo closing note. Do not write "ci is now green".
</traps>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate the two macOS-binary (sips) dependencies at module scope, keeping 30 of their 43 tests running on every host</name>
  <files>src/backend/sidecar/__tests__/nativeImageShim.test.ts, src/backend/sidecar/__tests__/shortcutsFlows.test.ts</files>
  <action>
**Step 0 — baselines, before touching anything.** "Pre-existing" is a claim about a chosen baseline,
so measure it:

1. `git rev-parse --short HEAD` — record it; this plan was written against `8b767e95c`.
2. Run each of the five suites alone with `npx jest --runInBand --silent <path>` and record the
   `Tests:` line. Expected: 6 / 31 / 12 / 22 / 36, all passed, none skipped. If any number differs
   from `<measured_at_head>`, STOP and report — the structure this plan rests on has moved.
3. `pnpm lint` — record BOTH warning counts. `SRC_CEILING = 1124` and `TESTS_CEILING = 638` in
   `meta/lintScoped.cjs`; the tests ceiling has zero slack and this task edits five test files.

**1a — `nativeImageShim.test.ts`.**

Directly below the `import { nativeImage } from '../nativeImageShim'` line (`:24`), add the host
constant and the gate, with a comment recording why it is read here and not later:

```
const HOST_IS_DARWIN = process.platform === 'darwin'
const describeOnDarwin = HOST_IS_DARWIN ? describe : describe.skip
```

The comment must state: `/usr/bin/sips` is macOS-only and its absence on Linux failed this suite at
IMPORT time (the whole suite, zero tests, from the `:97` frame); and that `HOST_IS_DARWIN` is read
once at module load specifically because `overrideProcessPlatform` mutates `process.platform` during
tests, so a lazily-read gate could observe a faked platform.

Then make the `REAL_PNG` fixture conditional, keeping the existing IIFE body byte-identical:

```
const REAL_PNG: Buffer = HOST_IS_DARWIN ? (() => { …existing body, unchanged… })() : Buffer.alloc(0)
```

Add a comment saying the empty-buffer branch is never READ: every test that dereferences
`REAL_PNG`/`REAL_ICO` lives in the darwin-gated describe below. `REAL_ICO = wrapPngAsIco(REAL_PNG)`
stays as-is — it is pure Buffer arithmetic and wrapping an empty buffer spawns nothing.

Rename the existing describe's callee to `describeOnDarwin(...)`, leaving its title, hooks and all
its remaining tests byte-identical.

**Then MOVE — verbatim, not rewritten — these three tests out of it into a new describe placed
immediately after it:**

- `'createFromPath(nonexistent path) does not throw at construction, and isEmpty() is true'` (`:183`)
- `'createEmpty() has isEmpty() === true'` (`:196`)
- `'the fast path returns the source bytes unchanged when no ops are pending and the format already matches (steamhelper.ts:121 works even without sips)'` (`:213`)

Title the new describe so its scope is legible, e.g.
`nativeImageShim — no converter required (runs on every host, sips or not)`. It needs its own
platform save/restore, because the one it used to rely on lives in the describe it just left: copy
the `let originalPlatform` + `beforeEach` (capture) + `afterEach` (`overrideProcessPlatform` restore
and `jest.restoreAllMocks()`) shape verbatim from the darwin describe. The third test calls
`overrideProcessPlatform('linux')`, so the restore is load-bearing — a following suite in the same
jest worker must not inherit a faked platform.

Do not change a single assertion in any of the three. Do not delete or weaken anything.

**1b — `shortcutsFlows.test.ts`.**

Above the `FIXTURE_ICON_PATH` block (`:210`), add the same pair:

```
const HOST_IS_DARWIN = process.platform === 'darwin'
const describeOnDarwin = HOST_IS_DARWIN ? describe : describe.skip
```

Wrap ONLY the `execFileSync('/usr/bin/sips', […])` statement (`:214-221`) in
`if (HOST_IS_DARWIN) { … }`. Keep the `FIXTURE_ICON_PATH` `join(...)` itself unconditional — the
file-wide `beforeEach` at `:241` resolves `mockGetIcon` to it on every host, and on a non-darwin host
that path is simply never read, because the only describe that drives `getIcon` is the one being
gated. The existing 12-line docstring above the constant explains the fixture's provenance; keep it,
and extend it with one sentence naming the gate and why (sips is macOS-only; its ENOENT at this
line failed the suite at import on the Linux runner).

Rename Describe 5's callee — `addShortcut's darwin GAMELIB_SHELL_EXE pin (T-34.5-65/66, D-10 rejects
process.execPath)` at `:528` — to `describeOnDarwin(...)`, title and body unchanged. Add a short
comment above it recording that this describe drives the REAL darwin `.app`/`run.sh`/`.icns` write
path through the real sips-backed shim, that `generateMacOsApp` (`shortcuts.ts:181`) is darwin-only
production code so the surface it pins does not exist on Linux, and that the other six describes are
deliberately left ungated.

**Gate NOTHING else in this file.** Describes 1, 1b, 2, 3, 4, 6 and 7 stay exactly as they are.
Describe 1b's no-regression test works on linux paths too (`ipc_handler.ts:55` checks both entries)
— leave it alone.

**1c — prove the non-darwin arm locally, then revert the proof.** This is the only local evidence
available that the guard does not crash at module scope, and it must be run, not assumed:

1. In BOTH files, temporarily change the constant to `const HOST_IS_DARWIN = false`.
2. Run both suites. Expected, and all four numbers must be checked:
   - `nativeImageShim`: suite PASSES, `Tests: 9 skipped, 3 passed, 12 total`, and no
     `sips ENOENT` anywhere in the output.
   - `shortcutsFlows`: suite PASSES, `Tests: 4 skipped, 27 passed, 31 total`.
   If a count differs, STOP and report the actual numbers — do not adjust the expectation to match
   what you got.
3. Record the two verbatim `Tests:` lines for the SUMMARY.
4. Revert both constants to `process.platform === 'darwin'` and re-run to confirm 12 and 31, zero
   skipped.

Be honest about what step 1c does and does not prove: it proves the sips spawn is unreachable and
that the surviving tests pass when the darwin arm is off. It does NOT prove Linux behaviour — on
this machine `shortcutFiles` still returns darwin paths, so the 27 surviving `shortcutsFlows` tests
are running against macOS path layout. Those 27 have never executed on Linux at all (the suite has
only ever failed at import there), so the next CI run is their first measurement and may surface
genuine new findings. Say exactly this in the SUMMARY.
  </action>
  <verify>
  <automated>
cd /Users/graysonmitchell/Projects/GameLib

NI=src/backend/sidecar/__tests__/nativeImageShim.test.ts
SF=src/backend/sidecar/__tests__/shortcutsFlows.test.ts

# The gate exists in both files and reads the REAL host platform (Trap 1).
for f in "$NI" "$SF"; do
  grep -qF "const HOST_IS_DARWIN = process.platform === 'darwin'" "$f" \
    || { echo "FAIL: $f does not capture the host platform at module scope"; exit 1; }
  grep -qF "const describeOnDarwin = HOST_IS_DARWIN ? describe : describe.skip" "$f" \
    || { echo "FAIL: $f does not define describeOnDarwin"; exit 1; }
  grep -qF 'describeOnDarwin(' "$f" \
    || { echo "FAIL: $f defines the gate but never applies it"; exit 1; }
done

# 1c was reverted: neither file is pinned to a literal.
if grep -rn 'HOST_IS_DARWIN = false\|HOST_IS_DARWIN = true' "$NI" "$SF"; then
  echo "FAIL: the temporary 1c proof constant was left in place"; exit 1
fi

# The sips spawn in shortcutsFlows is inside a HOST_IS_DARWIN branch. Comment
# lines are stripped first: this repo has shipped a raw-source gate satisfied by
# the prose that named the thing it checked.
grep -v '^[[:space:]]*[/*]' "$SF" | grep -A3 'if (HOST_IS_DARWIN)' | grep -qF "execFileSync('/usr/bin/sips'" \
  || { echo "FAIL: the sips call in shortcutsFlows is not inside the darwin branch"; exit 1; }

# nativeImageShim's fixture IIFE is conditional.
grep -v '^[[:space:]]*[/*]' "$NI" | grep -qF 'HOST_IS_DARWIN' \
  || { echo "FAIL: no executable HOST_IS_DARWIN use in nativeImageShim"; exit 1; }
grep -qF 'Buffer.alloc(0)' "$NI" \
  || { echo "FAIL: nativeImageShim has no non-darwin REAL_PNG branch"; exit 1; }

# The three hoisted tests survive VERBATIM and are no longer inside the gated describe.
for t in 'createFromPath(nonexistent path) does not throw at construction' \
         'createEmpty() has isEmpty() === true' \
         'the fast path returns the source bytes unchanged'; do
  grep -qF -- "$t" "$NI" || { echo "FAIL: hoisted test lost: $t"; exit 1; }
done

# Describe 5 in shortcutsFlows is the ONLY gated describe there (one applied gate).
APPLIED=$(grep -c 'describeOnDarwin(' "$SF")
[ "$APPLIED" = "1" ] || { echo "FAIL: expected exactly 1 gated describe in shortcutsFlows, got $APPLIED"; exit 1; }
grep -qF "describeOnDarwin(\"addShortcut's darwin GAMELIB_SHELL_EXE pin" "$SF" \
  || { echo "FAIL: the gated describe in shortcutsFlows is not Describe 5"; exit 1; }

# THE ANTI-DELETION CHECK: on this macOS arm64 host, full counts, nothing skipped.
npx jest --runInBand --silent "$NI" 2>&1 | tee /tmp/o95-ni.txt | grep -E '^Tests:'
grep -qE '^Tests: +12 passed, 12 total$' /tmp/o95-ni.txt \
  || { echo "FAIL: nativeImageShim is not 12 passed / 12 total on this Mac"; exit 1; }

npx jest --runInBand --silent "$SF" 2>&1 | tee /tmp/o95-sf.txt | grep -E '^Tests:'
grep -qE '^Tests: +31 passed, 31 total$' /tmp/o95-sf.txt \
  || { echo "FAIL: shortcutsFlows is not 31 passed / 31 total on this Mac"; exit 1; }

npx prettier --check "$NI" "$SF"
  </automated>
  </verify>
  <done>
Both suites gate the `/usr/bin/sips` spawn on a module-scope host constant, so neither can fail at
import on a non-darwin host. On this Mac both still run every test they ran at HEAD — 12 and 31,
zero skipped. The forced non-darwin arm was measured (9 skipped / 3 passed and 4 skipped / 27 passed)
and reverted. `nativeImageShim`'s three converter-free tests and `shortcutsFlows`'s six ungated
describes keep running on every host. Prettier clean on both files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Parameterise the three host-dependent expectations — one signature literal, one path-pair collapse, one arch-degenerate fixture. No skips in any of these files.</name>
  <files>meta/__tests__/verifyRunnerBundle.test.ts, src/backend/shortcuts/__tests__/shortcutsExistsFallback.test.ts, src/backend/__tests__/utils.test.ts</files>
  <action>
These three are grouped because they share a remedy class the previous task does not: **nothing here
is skipped anywhere**. Each file keeps running all of its tests on every host; only a host-dependent
expectation becomes explicit.

**2a — `verifyRunnerBundle.test.ts:329-343`, the `"unsigned"` literal.**

Add a module-scope constant near `const ARCH = 'arm64'` (`:36`):

```
const HOST_HAS_CODESIGN = process.platform === 'darwin'
```

with a comment recording the measured mechanism: `codesign` ships with macOS and exists nowhere
else; `getSignatureState` (`verifyRunnerBundle.ts:533-540`) hits its `result.error` branch when the
spawn fails and returns `unknown:spawnSync codesign ENOENT` — the verbatim string from the
2026-09-15 Linux CI log. That degraded report is arguably the correct Linux answer, so it is asserted,
not skipped.

Inside the test, leave `expect(summary.ok).toBe(true)` and `expect(r.machoCount).toBeGreaterThan(0)`
**unconditional** — that is the named claim ("signature reporting is not enforcement, `ok` stays
true") and it is what has to stay green on Linux. Scope only the per-file literal:

```
for (const m of r.machoFiles) {
  if (HOST_HAS_CODESIGN) {
    expect(m.signature).toBe('unsigned')
  } else {
    expect(m.signature).toMatch(/^unknown:/)
  }
}
```

Do NOT touch `:582-583` (`codesignDisplay` / `not.toMatch(/^signed:/)`) — measured to pass on both
hosts already. Do not add a skip anywhere in this file.

**Optional supporting measurement (cheap, take it):** prove the Linux string shape locally without a
Linux box, by making the spawn fail the same way:

```
node -e "const {spawnSync}=require('child_process'); const r=spawnSync('codesign',['-dv','/etc/hosts'],{encoding:'utf-8',env:{PATH:'/var/empty'}}); console.log(JSON.stringify(r.error && r.error.message))"
```

If that prints `spawnSync codesign ENOENT`, the `unknown:` prefix the new branch expects is confirmed
against the real `result.error` path rather than inferred. Record the output in the SUMMARY. If it
prints something else, record what it printed and keep the `/^unknown:/` match — it is deliberately
prefix-scoped, not an exact-string match, for exactly this reason.

**2b — `shortcutsExistsFallback.test.ts:168-175`, the path-pair collapse.**

`expect(desktopFile).toBeTruthy()` and `expect(menuFile).toBeTruthy()` stay unconditional — they are
the claim the test is named for (`shortcutFiles('')` returns a real non-empty pair, does not throw).
Replace only the final `expect(desktopFile).toEqual(menuFile)` with a two-armed assertion:

```
if (process.platform === 'darwin') {
  expect(desktopFile).toEqual(menuFile)
} else {
  expect(desktopFile).not.toEqual(menuFile)
}
```

Rewrite the existing two-line comment above it to record the measured mechanism instead of only the
darwin half: `shortcuts.ts:159-176` switches on `process.platform`; darwin collapses both entries onto
one `~/Applications/<name>.app`, while linux returns `<desktop>/<name>.desktop` and
`~/.local/share/applications/<name>.desktop`, which must differ — the exact pair the CI log's diff
printed. Keep the `260905-mv5 Task 1 / plan evidence item 5` provenance in the comment; that is why
this test exists.

The non-darwin arm is a real assertion, not a bypass: it would go red if `shortcutFiles` ever
collapsed the pair on linux. Do not weaken it to `toBeDefined()`, and do not add a skip anywhere in
this file. Leave the `jest.mock('backend/constants/environment', {isMac: false})` at `:38-40` alone —
it is irrelevant to this assertion.

**2c — `utils.test.ts:140-150`, the arch-degenerate fixture. Reproduce the failure locally FIRST.**

This is the suite the research corrects: the discriminator is `process.arch`, not `process.platform`.
Do this in three ordered steps so the fix is measured, not asserted.

*Step i — add the arch controls and reproduce CI's failure on this arm64 Mac.* Inside the
`archSpecificBinary — existence-checked x64 fallback (Phase 34.5 G-1, plan 34.5-17)` describe
(`:112`), above the first `it`, add:

```
const originalArch = process.arch
const setArch = (arch: string) =>
  Object.defineProperty(process, 'arch', { value: arch, configurable: true })

afterEach(() => setArch(originalArch))
```

(Same shape as the sibling describe at `:178-197`. These are sibling block scopes, so the duplicate
names are not a redeclaration and not a shadow.) Then TEMPORARILY add `setArch('x64')` as the first
statement of the `falls back to the x64 path…` test (`:140`) and run the suite. **It must FAIL**,
with the two byte-identical paths in the message — the local reproduction of the CI failure. Record
the verbatim message for the SUMMARY. If it PASSES, stop: the premise of this whole remedy is wrong
and must be re-measured before anything is changed.

*Step ii — apply the fix.* Replace that temporary `setArch('x64')` with `setArch('arm64')` and run
again: the test must PASS. The comment above it must record, in this order: that the premise
("arch-native missing, x64 present") is UNCONSTRUCTIBLE on an x64 host because `utils.ts:544-554`
builds the arch-native candidate from `process.arch` and the fallback from the literal `'x64'`, so
on x64 they are the same path and the mock below denies both; that the CI runner is x64 and the log
printed both paths byte-identical; and that the fix pins a non-x64 arch so the two candidates are
distinct on every host — **explicitly noting that a `process.platform` guard here would be wrong**,
because it would be a green check proving nothing on every Intel Mac and would stop testing the
documented box64 fallback on arm64 Linux, the exact platform that case is about.

*Step iii — confirm nothing else moved.* Leave `:132` and `:152` alone: both pass on any arch. Run
the whole suite: 22 passed, zero skipped, exactly as at HEAD.

Do NOT add any `process.platform` guard to this file, and do NOT skip anything in it.
  </action>
  <verify>
  <automated>
cd /Users/graysonmitchell/Projects/GameLib

VRB=meta/__tests__/verifyRunnerBundle.test.ts
SEF=src/backend/shortcuts/__tests__/shortcutsExistsFallback.test.ts
UT=src/backend/__tests__/utils.test.ts

# TRAP 3: none of these three files may contain a skip of any kind.
if grep -rn 'describe\.skip\|it\.skip\|test\.skip\|describeOnDarwin' "$VRB" "$SEF" "$UT"; then
  echo "FAIL: a skip/gate was introduced into a file that must run everywhere"; exit 1
fi

# 2a: the host-scoped signature branch exists; the unconditional claims survive.
grep -qF "const HOST_HAS_CODESIGN = process.platform === 'darwin'" "$VRB" || exit 1
grep -qF "expect(m.signature).toMatch(/^unknown:/)" "$VRB" \
  || { echo "FAIL: no non-darwin signature expectation"; exit 1; }
grep -qF "expect(m.signature).toBe('unsigned')" "$VRB" \
  || { echo "FAIL: the darwin signature literal was dropped instead of scoped"; exit 1; }
grep -qF "expect(r.machoCount).toBeGreaterThan(0)" "$VRB" || exit 1
# The untouched sibling assertion at :583 is intact.
grep -qF "not.toMatch(/^signed:/)" "$VRB" || { echo "FAIL: :583 was disturbed"; exit 1; }

# 2b: both arms present, the truthy claims unconditional.
grep -qF 'expect(desktopFile).toEqual(menuFile)' "$SEF" || exit 1
grep -qF 'expect(desktopFile).not.toEqual(menuFile)' "$SEF" \
  || { echo "FAIL: no non-darwin path-pair expectation"; exit 1; }
grep -qF 'expect(desktopFile).toBeTruthy()' "$SEF" || exit 1
grep -qF 'expect(menuFile).toBeTruthy()' "$SEF" || exit 1

# 2c: the fix is arch-based, and the temporary x64 reproduction line is GONE from
# the G-1 block. Scoped with awk (TRAP 4): setArch('x64') at :228 in the 34.9
# describe is LEGITIMATE and a file-wide grep would convict correct code.
G1=$(awk '/archSpecificBinary — existence-checked x64 fallback/,/archSpecificBinary — macOS onedir layout/' "$UT")
printf '%s' "$G1" | grep -qF "setArch('arm64')" \
  || { echo "FAIL: the G-1 fallback test does not pin a non-x64 arch"; exit 1; }
if printf '%s' "$G1" | grep -qF "setArch('x64')"; then
  echo "FAIL: the temporary step-i reproduction line survives in the G-1 block"; exit 1
fi
printf '%s' "$G1" | grep -qF 'afterEach(() => setArch(originalArch))' \
  || { echo "FAIL: the G-1 block does not restore process.arch"; exit 1; }
# The legitimate 34.9 x64 case is untouched.
grep -qF "setArch('x64')" "$UT" || { echo "FAIL: the 34.9 linux+x64 case was removed"; exit 1; }
if grep -n "process.platform === 'darwin'" "$UT"; then
  echo "FAIL: a platform guard was added to utils.test.ts (the discriminator is arch)"; exit 1
fi

# TRAP 5: the sixth CI failure is out of scope and untouched.
git diff --quiet -- meta/__tests__/lintTranslations.test.ts \
  || { echo "FAIL: lintTranslations.test.ts was modified"; exit 1; }

# No production source, no workflow, no jest config was touched by this task set.
if git diff --name-only | grep -vE '^(src/backend/sidecar/__tests__/(nativeImageShim|shortcutsFlows)\.test\.ts|meta/__tests__/verifyRunnerBundle\.test\.ts|src/backend/shortcuts/__tests__/shortcutsExistsFallback\.test\.ts|src/backend/__tests__/utils\.test\.ts|\.planning/)'; then
  echo "FAIL: a file outside the five test files and .planning/ was modified"; exit 1
fi

# THE ANTI-DELETION CHECK: full counts on this Mac, nothing skipped.
npx jest --runInBand --silent "$SEF" 2>&1 | tee /tmp/o95-sef.txt | grep -E '^Tests:'
grep -qE '^Tests: +6 passed, 6 total$' /tmp/o95-sef.txt || { echo "FAIL: shortcutsExistsFallback != 6/6"; exit 1; }

npx jest --runInBand --silent "$UT" 2>&1 | tee /tmp/o95-ut.txt | grep -E '^Tests:'
grep -qE '^Tests: +22 passed, 22 total$' /tmp/o95-ut.txt || { echo "FAIL: utils != 22/22"; exit 1; }

npx jest --runInBand --silent "$VRB" 2>&1 | tee /tmp/o95-vrb.txt | grep -E '^Tests:'
grep -qE '^Tests: +36 passed, 36 total$' /tmp/o95-vrb.txt || { echo "FAIL: verifyRunnerBundle != 36/36"; exit 1; }

pnpm codecheck
pnpm lint
npx prettier --check "$VRB" "$SEF" "$UT"
  </automated>
  </verify>
  <done>
Three host-dependent expectations are now explicit and two-armed; no test in any of these three files
is skipped on any host. `summary.ok`, `machoCount`, and both `toBeTruthy()` path claims remain
unconditional, so the platform-independent coverage the todo wanted kept on Linux is kept. The
`utils` fix is arch-based, was RED-proven locally by first forcing `setArch('x64')` (the verbatim
failure message is recorded), and the temporary reproduction line is gone from the G-1 block while
the legitimate `setArch('x64')` at `:228` survives. `lintTranslations.test.ts` byte-identical; no
production source, workflow or jest config touched. All three suites at their HEAD counts. `codecheck`
clean; `pnpm lint` exits 0 with both counts equal to the step-0 baseline and neither ceiling raised.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the parent todo with the per-suite decisions recorded, and carry the one unmeasurable claim forward</name>
  <files>.planning/todos/pending/2026-09-21-confirm-the-five-guarded-suites-run-green-on-the-linux-ci-runner.md, .planning/todos/completed/2026-09-15-darwin-asserting-suites-fail-on-the-linux-ci-runner.md</files>
  <action>
**3a — file the follow-up FIRST**, so the closing note can cross-reference it by filename.

Create `.planning/todos/pending/2026-09-21-confirm-the-five-guarded-suites-run-green-on-the-linux-ci-runner.md`.

Frontmatter order per CLAUDE.md — `created` / `title` / `area`, then `severity:` then `platform:`
then `ready:` in exactly that order, all three values bare, lowercase, unquoted, then `source:` and
`files:`. Use `created: 2026-09-21T00:00:00.000Z`, `area: test`, `severity: minor`,
`platform: linux`, `ready: blocked`, `source: quick-260921-o95`.

`ready: blocked` is the honest value and the body must justify it: the confirmation needs the Linux
CI runner, which is hardware not to hand, and it only fires on `pull_request`. It is not `code`
(nothing to edit), not `live-gate` (no live app run on this Mac can produce it), not `human` (no
decision pending).

Body must record:
- What to check on the next `pull_request` run: that `shortcutsExistsFallback`, `shortcutsFlows`,
  `nativeImageShim`, `utils` and `verifyRunnerBundle` are all green, and — separately — what the
  skip counts are for the two gated suites (expected 4 and 9 skipped on Linux).
- **The specific reason this is not a formality:** both sips suites have only ever failed at IMPORT
  on Linux, so 27 `shortcutsFlows` tests and 3 `nativeImageShim` tests will execute there for the
  first time. New, genuine Linux findings are possible and would not be regressions from this task.
- That the `utils` x64 arm was never executed on an x64 host either; the evidence it is fixed is the
  local RED→GREEN reproduction with `process.arch` forced (record which), plus the fix being
  arch-pinned rather than host-derived.
- The expected macOS-side counts (6 / 31 / 12 / 22 / 36, zero skipped) as the comparison baseline.
- That the sixth failure in that same CI run, `lintTranslations`, is a separate closed defect and is
  not evidence about this work either way.

**3b — close the parent.** Write a closing note at the TOP of the body of
`.planning/todos/pending/2026-09-15-darwin-asserting-suites-fail-on-the-linux-ci-runner.md`
(immediately after the frontmatter, before the `## Problem` section), then move the file.

The note must record:
- Actioned by quick task `260921-o95`, with the per-suite remedy table actually applied — and note
  that it is grouped by CAUSE, which is what the todo's own Verification section demanded:

  | suite | cause | remedy applied |
  | --- | --- | --- |
  | `nativeImageShim` | `/usr/bin/sips` ENOENT at import | module-scope darwin gate; 3 converter-free tests hoisted to an ungated describe |
  | `shortcutsFlows` | `/usr/bin/sips` ENOENT at import | module-scope darwin gate on the fixture + Describe 5 only; 27 of 31 tests stay ungated |
  | `verifyRunnerBundle` | `codesign` absent → `unknown:…` | parameterised; nothing skipped; `ok stays true` still asserted on Linux |
  | `shortcutsExistsFallback` | darwin path collapse | parameterised; nothing skipped; non-empty/no-throw still asserted on Linux |
  | `utils` | **not a platform defect** — x64 host | `process.arch` pinned to a non-x64 value; no platform guard |

- **The correction, stated plainly:** the todo grouped by symptom and inferred "darwin" for all five.
  `utils.test.ts` is not darwin-asserting, it is non-x64-asserting — it would fail identically on an
  x64 Mac and pass on arm64 Linux. A `process.platform` guard there would have been a green check
  proving nothing on every Intel Mac and would have stopped testing box64 on arm64 Linux.
- That the todo's option 3 (fix the CI environment) was refuted from the log for the one suite it was
  proposed for: the runner binaries are present and readable; `codesign` itself is absent, and it
  cannot be installed on Linux. No workflow or CI-environment file was changed.
- **What was NOT measured:** no Linux run of anything; no x64 run of the `utils` case; and the 30
  newly-unblocked tests in the two sips suites have never executed on Linux. Name the new todo by
  filename as the carrier for that confirmation.
- The macOS counts before and after (6 / 31 / 12 / 22 / 36, unchanged, zero skipped) as the evidence
  that no coverage was discarded.
- That `lintTranslations` was deliberately untouched.

**Move mechanics.** Do NOT use `git mv` — it has twice in this repo committed HEAD content and
silently dropped unstaged edits, once undetected for seven days. Sequence: (1) edit the file in place
in `pending/`; (2) plain `mv` to `.planning/todos/completed/`, keeping the filename; (3)
`git add -A .planning/todos/`; (4) verify the CONTENT AT THE DESTINATION PATH. Do not infer success
from the move's exit code.
  </action>
  <verify>
  <automated>
cd /Users/graysonmitchell/Projects/GameLib

NEW=.planning/todos/pending/2026-09-21-confirm-the-five-guarded-suites-run-green-on-the-linux-ci-runner.md
SRC=.planning/todos/pending/2026-09-15-darwin-asserting-suites-fail-on-the-linux-ci-runner.md
DST=.planning/todos/completed/2026-09-15-darwin-asserting-suites-fail-on-the-linux-ci-runner.md

# Frontmatter ONLY -- the body discusses `ready: blocked` in prose, and an
# unscoped grep would be satisfied by that prose.
fm() { awk '/^---$/{n++; next} n==1' "$1"; }

test -f "$NEW" || { echo "MISSING: $NEW"; exit 1; }
fm "$NEW" | grep -qx 'severity: minor'   || { echo "bad severity"; exit 1; }
fm "$NEW" | grep -qx 'platform: linux'   || { echo "bad platform"; exit 1; }
fm "$NEW" | grep -qx 'ready: blocked'    || { echo "bad ready"; exit 1; }
ORDER=$(fm "$NEW" | grep -E '^(severity|platform|ready):' | cut -d: -f1 | tr '\n' ' ')
[ "$ORDER" = "severity platform ready " ] || { echo "bad key order: [$ORDER]"; exit 1; }

# The follow-up carries the two facts that make it more than a formality.
for t in 'shortcutsFlows' 'nativeImageShim' 'verifyRunnerBundle' 'first time'; do
  grep -qF -- "$t" "$NEW" || { echo "follow-up missing: $t"; exit 1; }
done

# The parent moved, and the DESTINATION content carries the decisions.
test ! -e "$SRC" || { echo "parent still in pending/"; exit 1; }
test -f "$DST"   || { echo "parent not in completed/"; exit 1; }
for t in '260921-o95' 'process.arch' 'codesign' \
         '2026-09-21-confirm-the-five-guarded-suites-run-green-on-the-linux-ci-runner.md'; do
  grep -qF -- "$t" "$DST" || { echo "closing note missing: $t"; exit 1; }
done
# The original body survived the move.
grep -qF '## Problem' "$DST" || { echo "parent body was truncated by the move"; exit 1; }
# CLAUDE.md: never frame findings against Heroic.
if grep -niE 'upstream|heroic' "$NEW"; then echo "follow-up frames this against Heroic"; exit 1; fi

git add -A .planning/todos/
pnpm planning-gates
  </automated>
  </verify>
  <done>
The follow-up todo exists with valid, correctly-ordered triage frontmatter and records both what to
check on the next `pull_request` run and why it is not a formality. The parent is absent from
`pending/`, present in `completed/` with its full original body plus a closing note — verified by
reading the destination path on disk after the move, not inferred from the move's exit code — that
carries the per-suite cause→remedy table, the arch correction, the refutation of option 3, and an
explicit list of what was not measured. `pnpm planning-gates` green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Test-file edits and planning-document writes only. No production code path changes, no new dependency, no network call, no filesystem path derived from user input. The security-relevant surfaces these suites pin are unchanged; only where they are asserted moves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-o95-01 | Repudiation | all five suites | mitigate | A guard that skips everywhere is deletion with extra steps and would leave a green check proving nothing. Every task's verify asserts the EXACT macOS test count (`Tests: N passed, N total`, no `skipped` token), and Task 1 additionally measures the forced non-darwin arm's skip counts so a silent drop to zero is impossible in either direction. |
| T-o95-02 | Tampering | `shortcutsFlows.test.ts` Describe 5 | accept | Gating Describe 5 removes the `GAMELIB_SHELL_EXE` pin and the `run.sh` shell-injection proof (T-34.5-65/66, T-34.5-C6-17) on non-darwin hosts. Accepted: `generateMacOsApp`/`run.sh` is darwin-only production code (`shortcuts.ts:172-175, 181`), so the injection surface does not exist on Linux, and the send-kind pin's invoke-kind counterpart (Describe 6, `addToSteam`) stays ungated and still asserts the same underlying property on every host. |
| T-o95-03 | Tampering | `utils.test.ts` | mitigate | A `process.platform` guard here would silently stop exercising the box64 x64-fallback on arm64 Linux — the exact platform the documented case is about — while appearing to fix the failure. Task 2's verify fails if `process.platform === 'darwin'` appears anywhere in the file and requires the arch pin plus the `afterEach` restore. |
| T-o95-04 | Tampering | `verifyRunnerBundle.test.ts`, `shortcutsExistsFallback.test.ts` | mitigate | Scoping too widely would discard the platform-independent claims the todo explicitly wants green on Linux. Verify asserts these two files contain no skip of any kind and that `summary.ok`, `machoCount` and both `toBeTruthy()` assertions remain unconditional. |
| T-o95-05 | Tampering | `process.platform` / `process.arch` overrides | mitigate | A test that fakes `process.platform`/`process.arch` and does not restore it contaminates every later suite in the same jest worker. The hoisted `nativeImageShim` describe gets its own capture/restore hooks, and the `utils` G-1 block gets `afterEach(() => setArch(originalArch))`. |
| T-o95-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this task. No legitimacy gate required. |
</threat_model>

<verification>
Run from the repo root after all three tasks. **Run these; do not assert them** — a plan asserting a
gate's colour instead of measuring it has produced four false REDs in this repo.

```
npx jest --runInBand --silent \
  src/backend/shortcuts/__tests__/shortcutsExistsFallback.test.ts \
  src/backend/sidecar/__tests__/shortcutsFlows.test.ts \
  src/backend/sidecar/__tests__/nativeImageShim.test.ts \
  src/backend/__tests__/utils.test.ts \
  meta/__tests__/verifyRunnerBundle.test.ts
# expect: Test Suites: 5 passed, 5 total / Tests: 107 passed, 107 total  (6+31+12+22+36)
# the word "skipped" must NOT appear

pnpm codecheck
pnpm lint
pnpm planning-gates
git diff --name-only    # five test files + .planning/ paths, nothing else
git diff --quiet -- meta/__tests__/lintTranslations.test.ts && echo "lintTranslations untouched"
```

`pnpm lint` must exit 0 with both warning counts equal to the Task 1 step-0 baseline. Ceilings are
`SRC_CEILING = 1124` / `TESTS_CEILING = 638` in `meta/lintScoped.cjs`; the tests ceiling has zero
slack. Neither may be raised, and no unrelated warning may be "fixed" to buy headroom.

The full Backend project is deliberately NOT run as a gate here: it is red at HEAD for unrelated,
ledgered reasons. If you run it anyway, compare against the step-0 baseline and attribute nothing to
this task that was already red.

**Three claims in this plan rest on reading, not running, and the SUMMARY must say so:**
1. **No Linux run of anything.** Every Linux-side expectation (`/^unknown:/`, `not.toEqual`, the two
   suites importing cleanly) is derived from the 2026-09-15 CI log's verbatim output plus the
   production source, and from the forced-non-darwin local arm in Task 1c — which does not reproduce
   Linux path layout.
2. **No x64 run of the `utils` case.** This Mac is arm64. The evidence is the local RED→GREEN
   reproduction with `process.arch` forced to `x64` and then to `arm64`, plus the fix being
   arch-pinned rather than host-derived.
3. **30 tests have never executed on Linux at all** — 27 in `shortcutsFlows`, 3 in `nativeImageShim`.
   Both suites have only ever failed at import there. The next CI run is their first measurement.
</verification>

<success_criteria>
- All five suites run their full HEAD counts on this Mac with zero skipped: 6 / 31 / 12 / 22 / 36
  (107 total).
- The two `sips` suites gate at module scope on a host constant captured before any
  `overrideProcessPlatform` call, and the forced non-darwin arm was measured at 9 skipped / 3 passed
  and 4 skipped / 27 passed and then reverted.
- `verifyRunnerBundle.test.ts`, `shortcutsExistsFallback.test.ts` and `utils.test.ts` contain no skip
  of any kind; their platform-independent claims are still asserted unconditionally.
- `utils.test.ts` contains no `process.platform` guard; its fix pins `process.arch` and restores it in
  `afterEach`, and the legitimate `setArch('x64')` at `:228` survives.
- `meta/__tests__/lintTranslations.test.ts` is byte-identical; no production source, CI workflow or
  jest config is modified.
- `codecheck`, `lint` (both counts unmoved, no ceiling raised), prettier on the five files, and
  `planning-gates` are green.
- The parent todo is in `completed/` with a closing note verified on disk after the move, carrying the
  per-suite cause→remedy table, the arch correction, the refutation of the CI-environment option, and
  an explicit list of what was not measured; the follow-up todo carries the Linux confirmation with
  valid triage frontmatter.
</success_criteria>

<output>
Create `.planning/quick/260921-o95-guard-or-parameterise-the-five-darwin-as/260921-o95-SUMMARY.md` when done.

Record in it: the five suites' `Tests:` lines before and after, side by side; the two verbatim
`Tests:` lines from the forced non-darwin arm in Task 1c; the verbatim failure message from the
`setArch('x64')` reproduction in Task 2c; the `codesign` PATH-stripped probe output if taken; the
`pnpm lint` production and tests warning counts before and after; and an explicit, unhedged statement
of the three things that were NOT measured (no Linux run, no x64 run, 30 tests executing on Linux for
the first time on the next PR). Do not write that CI is green — that confirmation lives in the new
pending todo.
</output>
