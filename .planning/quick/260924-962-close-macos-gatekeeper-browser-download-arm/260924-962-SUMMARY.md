---
status: complete
phase: quick-260924-962
plan: 01
---

# Quick task 260924-962: record the 2026-09-24 Gatekeeper live-gate

## What this recorded

The live gate for this quick task had already been run by the orchestrator on 2026-09-24, on the
operator's Mac. This task's entire job was transcription and frontmatter enactment against
`.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md` — no artifact
was downloaded, no release published, no dmg mounted, no app launched, and no helper binary was
exec'd during this session.

## Verification bullet status

Of the todo's five original Verification bullets:

1. `codesign -dv --verbose=4` (Developer ID authority, not adhoc) — **satisfied**, on the 2026-09-23
   session, reconfirmed on 2026-09-24's quarantined copy.
2. `spctl -a -vvv -t install` (`accepted` / `source=Notarized Developer ID`) — **satisfied**, same
   basis.
3. `xcrun stapler validate` (ticket stapled) — **satisfied**, same basis.
4. Browser download on a machine that has never built the app, confirming no Gatekeeper
   interstitial — **run, but its wording is FALSIFIED while its intent is CONFIRMED**. A real
   Safari download (`com.apple.quarantine: 0083;6ab41d0e;Safari;C42D...`) of the byte-identical
   published-release artifact (sha256 `c74717b5...669c`) DID show an interstitial on first launch —
   but it was the benign notarized-app confirmation ("Apple checked it for malicious software and
   none was detected", `Open` as the default button), not the blocking form that requires System
   Settings -> "Open Anyway". So "no interstitial at all" is false; "no Gatekeeper block" is true.
   The substitute route does **not** reproduce two of the bullet's clauses: the recorded download
   origin is localhost (`kMDItemWhereFroms` = `http://127.0.0.1:8931/...`), not github.com, because
   the release is still an unpublished draft; and the test machine is the one that **builds** the
   app, not a machine that has never built it — `/usr/bin/syspolicy_check distribution`'s
   cache-independent pass ("App passed all pre-distribution checks and is ready for distribution")
   is named in the todo as the mitigation for that second gap, not a substitute for it.
5. The install-N / update-to-N+1 no-re-prompt Keychain claim — still untested; unchanged by this
   session.

## Frontmatter enacted (both of item 7's deferred proposals)

- `severity: major` -> `minor` (Proposal 1 — the feature is measured working end to end; only a
  credentialed errand with no known defect remains)
- `ready: live-gate` -> `human` (everything gate-testable without credentials has now run)
- `needs: release-run-then-browser-download-verify` -> `credentialed-in-app-store-actions-on-a-signed-build`
- `title:` restated to describe the now-verified world (Proposal 2)
- `status: OPEN` — unchanged, and the todo was **not** moved to `completed/`

## What remains: residual (c-iii)

Residual (c) — carried forward from the 2026-09-23 section — was split three ways this session:

- **(c-i)** bundled helpers (`legendary`, `gogdl`, `nile`, `comet`) executing at all under the
  hardened runtime from the signed/notarized bundle — **CLOSED GREEN**, all `rc=0` under an
  isolated fake HOME.
- **(c-ii)** `steam-bridge-helper`'s `dlopen` of Valve's `libsteam_api.dylib` under its
  `disable-library-validation` entitlement — **CLOSED GREEN** under a declared real-profile arm.
- **(c-iii)** — the actual Epic login, Amazon library refresh, and GOG action, which need real
  credentials and a person at the keyboard — is now the **entire remaining residue** of this todo.

A side observation (`steam-bridge-helper --help` blocking past 120s) surfaced during (c-ii) and was
parked, by filename, to the existing pending todo
`2026-09-23-steam-bridge-helper-never-spawned-by-the-sidecar-only-direct-exec-proven.md` rather than
chased here.

## Verification of this session's own work

- `git diff --numstat` after Task 1 alone showed 0 deletions, confirming every predecessor STATUS
  section, `## Problem`, `## Why it matters`, `## Direction` and `## Verification` are byte-identical
  to HEAD `3ee65a624`.
- All 20 required verbatim strings (sha256, quarantine attribute, `syspolicy_check` output, dialog
  text, pid values, `c-i`/`c-ii`/`c-iii`, `Robert Speedwagon`, `libsteam_api.dylib`, the parked
  todo's filename, `Proposal 1`/`Proposal 2`, etc.) are present in the new section.
- Task 2's frontmatter checks confirmed the four locked values in the correct key order
  (`severity` -> `platform` -> `ready`), the old values gone from frontmatter position, the title
  restated on one line (not a block scalar), and the file still at its original `pending/` path with
  both of its two other inbound references (three lines) intact.
- `git diff --name-only` after both tasks returns exactly one path:
  `.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md`.
- `npx prettier --check` on that path returned rc=0 — but this is **reported honestly as vacuous**,
  not as "formatting verified": `.prettierignore` line 29 is a bare `.planning` entry, so prettier
  inspected zero files here (probed at HEAD `3ee65a624`: `--list-different` prints nothing and exits
  0, while `--ignore-path /dev/null --list-different` on the same path prints it and exits 1).
- `pnpm planning-gates` reported **12/12** both after Task 1 and after Task 2, matching the baseline
  measured at HEAD `3ee65a624` when the plan was written.
- No `gh release`, `hdiutil`, `spctl`, `codesign`, or `open -a` command was run during this session.

## Self-Check

- FOUND: `.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md`
  (modified, still at its original path)
- FOUND: commit `fe288c404` in `git log --oneline`
- Exactly one tracked file changed by this quick task, confirmed by `git diff --stat` on the parent
  commit.

## Self-Check: PASSED
