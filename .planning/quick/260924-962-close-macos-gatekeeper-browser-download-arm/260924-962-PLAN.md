---
phase: quick-260924-962
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md
autonomous: true
requirements:
  - 962-01
  - 962-02
  - 962-03
  - 962-04
  - 962-05
  - 962-06
must_haves:
  truths:
    - 'The 2026-09-24 live-gate results are recorded in the todo as a NEW `## STATUS 2026-09-24 (quick-260924-962)` section, and the three predecessor STATUS sections plus `## Problem`, `## Why it matters`, `## Direction` and `## Verification` are byte-identical to HEAD after Task 1.'
    - 'The new section states the HEADLINE honestly: the fourth Verification bullet as WORDED is falsified (an interstitial DID appear) while the thing it guarded against is CONFIRMED ABSENT (it was the benign notarized confirmation, not the block).'
    - 'The new section states the two things this run does NOT prove — localhost origin on a still-DRAFT release, and a build machine rather than a machine that has never built the app — and names `syspolicy_check distribution` as the cache-independent mitigation for the second.'
    - 'The `## Why it matters` consequence 1 claim about System Settings -> "Open Anyway" is corrected FROM INSIDE the new section, by name, and is NOT edited in place.'
    - 'Residual (c) is SPLIT into (c-i) helper exec under hardened runtime, (c-ii) steam-bridge-helper dlopen of Valve''s dylib, and (c-iii) the credentialed round-trips — with (c-i) and (c-ii) recorded as CLOSED GREEN and (c-iii) named as the entire remaining residue.'
    - 'The near-miss is recorded: the frontmost pid was an ORPHAN from the previous session, and the clean re-launch measured shell pid 98884 == frontmost.'
    - 'Frontmatter reads `severity: minor`, `platform: macos`, `ready: human` IN THAT ORDER, bare and lowercase, with `needs: credentialed-in-app-store-actions-on-a-signed-build` and `status: OPEN`.'
    - 'The title is restated to the exact locked string; the FILENAME is unchanged, so its inbound references from two other pending todos (three lines in total) still resolve.'
    - 'The todo remains in `todos/pending/` and does NOT appear in `todos/completed/`.'
    - 'Exactly ONE tracked file is modified. Nothing under `src/`, `meta/`, `src-tauri/` or `.github/` changes, and no live gate, download, app launch or dmg mount is re-run.'
  artifacts:
    - path: '.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md'
      provides: 'The macOS signing todo, still OPEN, now recording the 2026-09-24 quarantined-Gatekeeper measurement and carrying only the credentialed residue'
      contains: '## STATUS 2026-09-24 (quick-260924-962)'
  key_links:
    - from: 'the new STATUS 2026-09-24 section'
      to: '`## Why it matters` consequence 1'
      via: 'an explicit by-name correction rather than an in-place edit'
      pattern: 'consequence 1'
    - from: 'the new STATUS 2026-09-24 section'
      to: '`## STATUS 2026-09-23` item 7 Proposals 1 and 2'
      via: 'a statement that both are now ENACTED in frontmatter'
      pattern: 'Proposal 1'
    - from: 'the new STATUS 2026-09-24 section'
      to: '2026-09-23-steam-bridge-helper-never-spawned-by-the-sidecar-only-direct-exec-proven.md'
      via: 'the parked `--help` blocking side observation'
      pattern: 'steam-bridge-helper-never-spawned-by-the-sidecar'
---

<objective>
The live gate has ALREADY BEEN RUN, on 2026-09-24, on the operator's Mac. This plan writes what it
measured into
`.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md` and changes
that file's frontmatter to match the new world.

Purpose: the todo's fourth Verification bullet — the browser-download arm — has been the single
outstanding item since 2026-09-23, and `## STATUS 2026-09-23` item 7 left two changes standing as
**PROPOSALS AWAITING THE OPERATOR**. The operator has now decided both. Recording the result and
enacting the proposals is the whole job.

Output: **ONE file changed. DOCUMENTATION ONLY.** No code, no workflow, no gate, no STATE.md.

**DO NOT RE-RUN THE GATE.** No step in this plan downloads an artifact, publishes a release, mounts
a dmg, launches the app, or execs a helper binary. Every number below was measured on 2026-09-24 and
is reproduced verbatim in `<measured_evidence>` for the executor to COPY, not paraphrase and not
re-derive. If you find yourself reaching for `gh`, `hdiutil`, `spctl`, `codesign` or `open`, stop —
you have misread the task.

**No `<threat_model>` section.** Stated rather than omitted silently: this plan edits one Markdown
file under `.planning/`, installs no package, adds no dependency, crosses no trust boundary and
touches no executable path. The STRIDE register would be empty, and an empty register is a green
check proving nothing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md
@CLAUDE.md

Read the todo **in full** before editing. It already carries three STATUS sections in a deliberate
shape: each records what was measured on its own date and leaves its predecessors intact, and
`## STATUS 2026-09-23` says so in its own first paragraph. The new section must adopt the same
shape. Correcting a predecessor in place would destroy the one property this file has been
maintaining for three weeks.
</context>

<planning_measurements>

Five things were measured at HEAD `3ee65a624` while writing this plan, so the executor does not
re-derive them and a later reader can tell measurement from assumption.

1. **`pnpm planning-gates` is 12/12 at `3ee65a624`.** That is the baseline the verify block
   ratchets against. Recorded because this repo's `a-plans-verify-blocks-rot-against-its-own-baseline-sha`
   lesson is that a plan asserting a gate count against an unstated baseline produces false reds.

2. **The file currently carries exactly 3 `## STATUS ` headings** (lines 18, 57, 183) and its
   `## Related` section starts at line 257. `## STATUS 2026-09-23` was INSERTED above `## Related`,
   not appended at EOF. The new section goes in the same place: after the last line of
   `## STATUS 2026-09-23` and before `## Related`.

3. **The FILENAME does not change, only the title — and that matters.** Measured at this HEAD:
   **two** other pending todos reference this file by name, across **three** lines — one of them in
   a `source:` with a full path
   (`2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md:11`, plus `:81`, plus
   `2026-09-23-steam-bridge-helper-never-spawned-by-the-sidecar-only-direct-exec-proven.md:85`).
   Because this plan changes no filename and performs no `git mv`, all three lines keep resolving
   and neither file may be touched. (Do not expect the count of FILES and the count of LINES to
   agree: `grep -rl` over `todos/pending/` returns 2, `grep -rn` returns 3.)

4. **The mandated prettier check is VACUOUS on this path, and was PROBED, not assumed.**
   `.prettierignore` line 29 is a bare `.planning` entry. Measured at this HEAD:

   ```
   npx prettier --list-different <path>                       -> no output, rc=0
   npx prettier --ignore-path /dev/null --list-different <path> -> prints the path, rc=1
   ```

   So prettier inspects NOTHING here and exits 0 having checked zero files
   (memory `prettier-check-over-a-planning-path-is-vacuous`). CLAUDE.md mandates the check in every
   task's `<verify>`, so it is present below — but **its green result is evidence of nothing** and
   must not be cited as if it were.

5. **TRAP, measured: `prettier --write` on this file would reflow 26 lines of HISTORY.** The
   probe above shows the file is already non-conforming to prettier's markdown style; a hypothetical
   reformat changes `*working*` -> `_working_` on line 27 and re-pads the `## Problem` secrets table.
   **Do not run `prettier --write` on this file, and do not "fix" the rc=1 from the bypass probe.**
   Those 26 lines are inside the frozen predecessor sections, and Task 1's zero-deletions gate would
   convict the reformat — correctly.

</planning_measurements>

<measured_evidence>

**Reproduce these strings VERBATIM. Do not paraphrase, do not round, do not re-derive.** Everything
here was measured on 2026-09-24 on the operator's Mac (Darwin 25.6.0, arm64).

**RUN CONTEXT.** Artifact `GameLib_0.7.0_aarch64.dmg` from draft release `378785323`, asset id
`583428514`, sha256 `c74717b59421119eaacce55c51ff153222c9e03296f87d817ed422423dba669c`,
97083599 bytes — **byte-identical to the artifact `## STATUS 2026-09-23` measured**.

**FINDING 1 — the bullet could NOT be run as literally written.** The asset lives on an UNPUBLISHED
DRAFT release. Its `browser_download_url` is
`https://github.com/grayson-mitchell/GameLib/releases/download/untagged-15f75eb2dbc69bc57563/GameLib_0.7.0_aarch64.dmg`.
Opened in Safari, GitHub rendered its 404 page ("This is not the web page you are looking for") and
the nav bar showed "Sign in" / "Sign up" — **Safari is not authenticated to GitHub**. Anonymous
`curl` on the same URL returns HTTP 404. So the bullet is unrunnable on two counts at once: the
release is not published, and this browser has no GitHub session. **Publishing the `v0.7.0` draft to
satisfy the bullet was REJECTED as out of scope** — it is a public, hard-to-reverse act, and there is
an open todo (`2026-09-17-packaged-app-renders-blank-on-roughly-one-launch-in-four.md`) against this
very build.

**FINDING 2 — the substitute route, and exactly what it does and does not reproduce.** The
byte-identical artifact was fetched with an authenticated API call, served from `127.0.0.1:8931`,
and downloaded THROUGH SAFARI. **Negative control first:** the API-fetched copy carried
`com.apple.provenance` and NO `com.apple.quarantine` — reproducing the exact trap this todo warned
about and that the 2026-09-23 verification fell into. The Safari-downloaded copy carried a real
browser-set attribute:

```
com.apple.quarantine: 0083;6ab41d0e;Safari;C42D10F2-0A4B-4E85-8380-B96FD266E6FD
```

and sha256 `c74717b5...669c`, identical to the published asset. **What this does NOT reproduce,
stated plainly:** the recorded origin is localhost, not github.com (`kMDItemWhereFroms` reads
`http://127.0.0.1:8931/...`), and the machine is the one that BUILDS the app, not a machine that has
never built it. Gatekeeper's verdict does not consult the origin URL, but the "machine that has
never built the app" clause of the bullet is NOT satisfied and is not claimed to be.

**FINDING 3 — quarantine propagation.** The `.app` inside the mounted dmg carries no quarantine
xattr of its own; quarantine rides on the dmg. Copied out with `ditto`, the `.app` gained
`0283;00000000;;C42D10F2-0A4B-4E85-8380-B96FD266E6FD` — **same download UUID** as the Safari
download, i.e. genuine propagation.

**FINDING 4 — assessments on the QUARANTINED copy (not a local build).**

| instrument | result |
| --- | --- |
| `spctl -a -vvv -t exec` | `accepted`, `source=Notarized Developer ID`, `origin=Developer ID Application: grayson mitchell (S7U223QWXJ)`, `SPCTL_RC=0` |
| `/usr/bin/syspolicy_check distribution` | `App passed all pre-distribution checks and is ready for distribution.` — **cache-independent**, which answers the "machine that has already built it" contamination worry directly |
| `xcrun stapler validate` | `The validate action worked!` |
| `codesign -dv --verbose=4` | `Identifier=com.gamelib.shell`; `CodeDirectory v=20500 size=26621 flags=0x10000(runtime) hashes=821+7 location=embedded`; `Authority=Developer ID Application: grayson mitchell (S7U223QWXJ)`; `Notarization Ticket=stapled`; `TeamIdentifier=S7U223QWXJ` |

**FINDING 5 — THE HEADLINE. The bullet's WORDING is falsified; its INTENT is confirmed.** First
launch of the quarantined app DID show an interstitial. Verbatim:

```
"GameLib" is an app downloaded from the internet. Are you sure you want to open it?
Safari downloaded this file today at 6:40 AM. Apple checked it for malicious software
and none was detected.
[Cancel]  [Open]      <- Open is the default (highlighted) button
```

This is the **BENIGN notarized-app confirmation, NOT a block.** The blocking form ("... cannot be
opened because Apple could not verify it is free of malicious software", which offers only Move to
Trash / Done and requires System Settings -> Privacy & Security -> "Open Anyway") **did NOT appear**.
So the bullet as worded ("confirm it opens with **no** Gatekeeper interstitial") is FALSE — there is
one — while the thing it was actually guarding against is CONFIRMED ABSENT. Log corroboration:
`SecTranslocateCreateSecureDirectoryForURL` created an App Translocation copy; `runningboardd`
tracked `app<application.com.gamelib.shell...>`; **no denial was logged**.

**FINDING 6 — a NEAR-MISS, same shape as the 2026-09-23 curl near-miss.** After clicking Open a
GameLib window was frontmost and fully rendered, and it would have been easy to record "it opens".
It was MEASURED instead: the frontmost pid was **82738** — an ORPHANED GameLib instance left running
since `Wed Sep 23 21:36:16` by the PREVIOUS session (scratchpad path
`.../c2d06025-.../scratchpad/verify/GameLib.app`). The newly launched translocated copy had hit
GameLib's single-instance guard, handed focus to the orphan, and exited. **The screenshot that
"proved" the launch was a picture of a DIFFERENT PROCESS.** The orphan (shell 82738 + sidecar 82751)
was terminated; an AppleScript `quit` did not take it down within 15s, but `kill -TERM` on the shell
removed BOTH shell and sidecar, confirming the process-group teardown works. Re-launched clean with
no other instance running: shell pid **98884** + sidecar pid **98891**, both under
`/private/var/folders/.../AppTranslocation/B606428A-F247-4635-98DC-EC96D166F2D2/d/GameLib.app`.
Frontmost pid was then confirmed **== 98884**. The UI rendered fully (Library tab, `All Games 387`,
artwork and per-store badges present — not blank). No crash reports. **Incidental, previously
unmeasured:** the app ran correctly APP-TRANSLOCATED, which is the real path for any user who
launches from Downloads rather than dragging to `/Applications`. No second Gatekeeper dialog on the
second launch; the quarantine flags had advanced `0283 -> 02c3`, i.e. the approval is recorded per
download UUID.

**FINDING 7 — residual (c) is now SPLIT, and its larger half is CLOSED GREEN.** `## STATUS
2026-09-23` item 5 silently conflated two very different risks. Separated and measured:

- **(c-i) — can the bundled helpers EXEC AT ALL under the hardened runtime, from the signed and
  notarized bundle?** Needs NO credentials, and was the real risk: same failure class as the
  `allow-jit` gap quick-260914-vbw caught, and PyInstaller bundles are a classic library-validation
  casualty. MEASURED, all from the mounted published dmg, each run under an isolated fake HOME
  (`mkdtemp` 0700, all eight containment variables set per CLAUDE.md's two-profile rule; profile
  shredded afterwards):

  ```
  legendary --version  -> rc=0   legendary version "0.21.0", codename "Lowlife"
  gogdl --version      -> rc=0   1.3.0
  nile --version       -> rc=0   1.2.0 Robert Speedwagon
  comet --help         -> rc=0   Usage: comet [OPTIONS] --username <USERNAME> [COMMAND]
  ```

  Signing census on those helpers: all carry
  `Authority=Developer ID Application: grayson mitchell (S7U223QWXJ)` and `flags=0x10000(runtime)`;
  `legendary`, `gogdl`, `nile` and `comet` carry NO entitlements; only `steam-bridge-helper` carries
  `com.apple.security.cs.disable-library-validation`. No AMFI, library-validation or code-signature
  denials were logged for any of them.
- **(c-ii) — `steam-bridge-helper`, the one that must `dlopen` VALVE's `libsteam_api.dylib`** — a
  dylib signed by Valve, not by us, which is precisely what its `disable-library-validation`
  entitlement exists for. Under the fake HOME it died instantly and correctly with
  `FATAL dlopen(...libsteam_api.dylib...) (no such file)` — no Steam in the fake profile. Under a
  **DECLARED real-profile arm**, with Valve's dylib present, it did NOT hit that FATAL and stayed
  alive past 120s before being terminated, and no library-validation denial was logged. So the
  entitlement works and the dylib loads under hardened runtime. **SIDE OBSERVATION, not chased
  here:** `steam-bridge-helper --help` BLOCKING for >120s instead of printing help and exiting is
  odd on its face; it is left to the existing pending todo
  `2026-09-23-steam-bridge-helper-never-spawned-by-the-sidecar-only-direct-exec-proven.md`.
- **(c-iii) — WHAT GENUINELY REMAINS:** only the AUTHENTICATED round-trips — an actual Epic login,
  an actual Amazon library refresh, an actual GOG action — which need real credentials and a person
  at the keyboard. **This is the entire residue of this todo.**

**CLEANUP DONE.** App quit, localhost server stopped, dmg detached, fake profile shredded, all
screenshots and the Gatekeeper log deleted, the downloaded dmg removed from `~/Downloads`. The
scratchpad is empty. `/Applications/GameLib.app` was deliberately NOT touched (still the Sep 1
build) — the test app was installed to the scratchpad instead.

</measured_evidence>

<locked_decisions>

Decided by the operator. **Apply them; do not re-litigate them.**

| # | change | locked value |
| --- | --- | --- |
| D-01 | `severity` | `major` -> `minor` |
| D-02 | `ready` | `live-gate` -> `human` |
| D-03 | `needs` | `release-run-then-browser-download-verify` -> `credentialed-in-app-store-actions-on-a-signed-build` |
| D-04 | `title` | restated (exact string in Task 2) |
| D-05 | `status` | **stays `OPEN`.** The todo is NOT closed and must NOT be moved to `completed/`. |
| D-06 | predecessors | append a new STATUS section; do NOT revise `## STATUS 2026-09-14`, `## STATUS 2026-09-17`, `## STATUS 2026-09-23`, `## Problem`, `## Direction` or `## Verification`. **ONE EXCEPTION**, handled in Task 1: `## Why it matters` consequence 1 is corrected FROM INSIDE the new section, by name, never in place. |

**Justification to record for D-01** (write it into the new section, do not invent your own):
CLAUDE.md defines `major` as "a feature is broken or a measurement is silently contaminated". The
feature — a signed, notarized, stapled macOS build that clears Gatekeeper on a real quarantined
download — is now MEASURED WORKING end to end, and the helper binaries execute under the hardened
runtime. What is left is one credentialed errand with no known defect behind it, which is
CLAUDE.md's `minor`: "polish, rough edge, or a latent trap with no live consequence". This enacts
the todo's own **Proposal 1**.

**Justification for D-02:** everything desk-testable and everything gate-testable without
credentials has now been run. The only remaining arm needs credentials and a person — CLAUDE.md's
`human`.

**D-04 and Proposal 2:** restating the title enacts the todo's own **Proposal 2**. Say so in the new
section, so a reader of the todo — not only a reader of this plan — can see that item 7's two
proposals were answered.

</locked_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Append the STATUS 2026-09-24 section — evidence BEFORE conclusion</name>
  <files>.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md</files>
  <action>
**Ordering is load-bearing: this task runs BEFORE the frontmatter change.** If Task 2 ran first and
this one aborted, the file would claim `minor` / `human` with no evidence in it for why — a triage
label with nothing behind it is worse than the stale label it replaced. Do not reorder.

Insert a new `## STATUS 2026-09-24 (quick-260924-962)` section **after the last line of
`## STATUS 2026-09-23 (quick-260923-uvt)` and immediately before `## Related`** (measured at
`3ee65a624`: `## Related` is line 257). This is an INSERTION with **zero deleted and zero modified
lines** — the verify block gates on exactly that.

Open with one sentence adopting the established convention, mirroring how `## STATUS 2026-09-23`
opens: this section does not revise its predecessors; each records what was measured on its own date
and is left intact.

Then cover, drawing every string VERBATIM from `<measured_evidence>` above:

1. **Run context** — date, machine, artifact, sha256, byte count, and that it is byte-identical to
   the artifact 2026-09-23 measured. Use the full 64-char sha at least once.
2. **The fourth Verification bullet could not be run as literally written** (FINDING 1) — the draft
   release, the 404 in Safari, the unauthenticated browser, and that **publishing the draft was
   REJECTED as out of scope**, naming the open blank-render todo as the reason.
3. **The substitute route** (FINDING 2) — including the NEGATIVE CONTROL, which is the part that
   makes the result trustworthy: the API-fetched copy had no `com.apple.quarantine`, reproducing the
   exact trap `## STATUS 2026-09-23` item 4 fell into. Quote the Safari quarantine attribute
   verbatim.
4. **WHAT THIS RUN DOES NOT PROVE** — give this its own clearly-marked paragraph or subheading, in
   your own words but covering both: **(a)** the recorded origin is localhost and the release is
   still a DRAFT, not published; **(b)** the machine is the BUILD machine, so the bullet's "machine
   that has never built the app" clause is UNMET and is not claimed. For (b), name
   `/usr/bin/syspolicy_check distribution` as the cache-independent instrument that mitigates that
   specific contamination worry — a mitigation, not a substitute.
5. **Quarantine propagation** (FINDING 3) — same download UUID, i.e. genuine.
6. **Assessments on the quarantined copy** (FINDING 4) — reproduce as a table with verbatim strings.
7. **THE HEADLINE** (FINDING 5) — quote the dialog verbatim in a fenced block. State in as many
   words that **the bullet as WORDED is falsified and its INTENT is confirmed**: an interstitial
   DID appear, it was the benign notarized confirmation with **Open as the default button**, and the
   blocking form did not. Do not soften this into "it opened fine"; do not overstate it into "the
   check failed".
8. **The correction to `## Why it matters` consequence 1** — this is the ONE EXCEPTION to D-06.
   Name consequence 1 explicitly and state that its claim ("the user must go to System Settings ->
   Privacy & Security -> 'Open Anyway'") is **no longer true for the signed, notarized artifact**:
   one click on the default button suffices. **Do NOT edit consequence 1 in place** — the file keeps
   its history-preserving shape by correcting forward, not by rewriting backward. While you are
   there, note that consequence 1's `[ASSUMED — the exact modern-macOS bypass UX should be confirmed
   on hardware...]` marker is now DISCHARGED by this measurement.
9. **The near-miss** (FINDING 6) — the orphan pid 82738, the single-instance guard handing off
   focus, that the screenshot pictured a DIFFERENT PROCESS, the clean re-launch at 98884/98891 with
   frontmost confirmed equal, `All Games 387` rendering, and the incidental App Translocation
   result. Record it for the same reason item 4 of the 2026-09-23 section recorded the curl trap:
   the near-miss is the evidence that the result was measured rather than assumed.
10. **Residual (c) is SPLIT** (FINDING 7) — (c-i) and (c-ii) CLOSED GREEN with the verbatim
    `rc=0` lines and the signing census; (c-iii) named as the entire remaining residue. Park the
    `steam-bridge-helper --help` >120s side observation to
    `2026-09-23-steam-bridge-helper-never-spawned-by-the-sidecar-only-direct-exec-proven.md` by
    filename, and do not chase it here.
11. **Cleanup** — one short paragraph from the CLEANUP DONE block, including that
    `/Applications/GameLib.app` was deliberately left as the Sep 1 build.
12. **Frontmatter changes, forward-referenced** — state that `## STATUS 2026-09-23` item 7's
    **Proposal 1** and **Proposal 2** are both ENACTED by this session, and list the four
    frontmatter changes (D-01..D-04) with the D-01 justification quoted from `<locked_decisions>`.
    State that `status:` stays `OPEN` and why: (c-iii) is genuinely outstanding.

**Writing constraints.**

- Match the surrounding house style: hard-wrapped prose at roughly 100 columns, `**bold**` for the
  load-bearing claim of each item, backticked literals, tables for verbatim instrument output.
- **Never write a frontmatter-shaped line at column 0** inside the body — e.g. a bare line reading
  `severity: minor`. Always inline it in backticks (`` `severity: minor` ``) or behind a list
  marker. A bare one would be indistinguishable from frontmatter to a naive grep and would poison
  Task 2's verify greps, which are anchored with `^...$`.
- **Do NOT run `prettier --write`** on this file. Planning measurement 5 shows it would reflow 26
  lines of frozen history; the zero-deletions gate below would convict that, correctly.
- Emit no tool-envelope tags. `.planning/planning-envelope-tag-gate.py` convicts a trailing orphan
  closing `content`/`invoke` tag, and this file lives under its corpus.
  </action>
  <verify>
    <automated>
P=.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md

# 1. PURE INSERTION. Deletions column of numstat MUST be 0 -- this is the gate that
#    proves D-06: no predecessor STATUS section, and no Problem/Direction/Verification
#    line, was revised. Measured baseline: HEAD 3ee65a624.
#    TRAP: on an UNMODIFIED file numstat prints NOTHING, so a bare `= "0"` compare would
#    fail with a misleading message. Distinguish the two cases explicitly.
INS="$(git diff --numstat -- "$P" | awk '{print $1}')"
DEL="$(git diff --numstat -- "$P" | awk '{print $2}')"
test -n "$DEL" || { echo "FAIL: file is UNMODIFIED -- Task 1 wrote nothing"; exit 1; }
test "$DEL" = "0"  || { echo "FAIL: $DEL line(s) DELETED -- a predecessor section was revised, or prettier --write was run"; exit 1; }
test "$INS" -gt 0  || { echo "FAIL: zero insertions"; exit 1; }

# 2. Exactly one new STATUS heading; 3 before -> 4 after.
test "$(grep -c '^## STATUS ' "$P")" = "4"
test "$(grep -c '^## STATUS 2026-09-24 (quick-260924-962)' "$P")" = "1"

# 3. The new section sits ABOVE ## Related, not at EOF.
test "$(grep -n '^## STATUS 2026-09-24' "$P" | cut -d: -f1)" -lt "$(grep -n '^## Related$' "$P" | cut -d: -f1)"

# 4. Required content. Comment lines stripped per CLAUDE.md grep hygiene (no '#' lines
#    exist in this markdown body, but the filter is applied anyway so the idiom is right).
for s in 'c74717b59421119eaacce55c51ff153222c9e03296f87d817ed422423dba669c' \
         '0083;6ab41d0e;Safari;C42D10F2-0A4B-4E85-8380-B96FD266E6FD' \
         'syspolicy_check' \
         'App passed all pre-distribution checks' \
         'Apple checked it for malicious software' \
         'consequence 1' \
         'Open Anyway' \
         '82738' '98884' 'All Games 387' \
         'c-i' 'c-ii' 'c-iii' \
         'Robert Speedwagon' \
         'libsteam_api.dylib' \
         'steam-bridge-helper-never-spawned-by-the-sidecar' \
         'Proposal 1' 'Proposal 2' ; do
  grep -v '^#' "$P" | grep -qF "$s" || { echo "MISSING: $s"; exit 1; }
done

# 5. NO bare frontmatter-shaped line was introduced into the body (would poison Task 2).
test "$(grep -c '^severity: ' "$P")" = "1"
test "$(grep -c '^ready: ' "$P")" = "1"

# 6. Only this file changed, and nothing outside .planning/.
test "$(git diff --name-only | grep -cv '^\.planning/')" = "0"
test "$(git diff --name-only -- src meta src-tauri .github | wc -l | tr -d ' ')" = "0"

# 7. MANDATED BY CLAUDE.md, AND VACUOUS ON THIS PATH. .prettierignore line 29 is a bare
#    `.planning` entry; measured at 3ee65a624 this command inspects ZERO files and exits 0.
#    It is run because the convention requires it. Its green result is evidence of NOTHING
#    and must not be reported as if the file were checked.
npx prettier --check .planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md

pnpm planning-gates   # MUST report 12/12; baseline 12/12 at HEAD 3ee65a624
    </automated>
  </verify>
  <done>
The todo carries a fourth STATUS section recording the 2026-09-24 measurement; `git diff --numstat`
shows **0 deletions**, proving every predecessor section is byte-intact; the section states the
headline honestly (wording falsified, intent confirmed), names both things the run does not prove,
corrects `## Why it matters` consequence 1 from inside itself without editing it, splits residual
(c) three ways with (c-i)/(c-ii) green, and records the orphan-pid near-miss. `pnpm planning-gates`
is 12/12.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enact Proposals 1 and 2 in frontmatter — severity, ready, needs, title</name>
  <files>.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md</files>
  <action>
Change exactly four frontmatter keys. Touch no other line of the file — Task 1's section is already
written and must not be re-edited.

**D-01/D-02 — lines 5 and 7.** `severity: major` -> `severity: minor`; `ready: live-gate` ->
`ready: human`. Leave `platform: macos` (line 6) exactly as it is. **The key ORDER must remain
`severity`, then `platform`, then `ready`**, per CLAUDE.md. Values **bare, lowercase, exact** —
`severity: minor`, never `severity: "minor"`, never `severity: Minor`.

**D-03 — line 8.** `needs: release-run-then-browser-download-verify` ->
`needs: credentialed-in-app-store-actions-on-a-signed-build`. The old value is satisfied and must
not keep implying otherwise. (The old string survives elsewhere in `.planning/` — in `STATE.md`, in
two quick-task summaries, in a completed todo, and inside this file's own frozen
`## STATUS 2026-09-23` item 3. **All of those are HISTORY. Leave every one of them byte-unchanged.**)

**D-04 — line 3.** Replace the title with EXACTLY this string, on ONE line, single-quoted:

```
title: 'macOS signing and notarization are VERIFIED end-to-end on a published artifact including the quarantined Gatekeeper path — only the credentialed in-app store actions (Epic/Amazon/GOG login) on a signed build remain unexercised'
```

Keep it on a single line. Do not wrap it, do not convert it to a `|` or `>` block scalar, do not
re-quote it — this repo has a recorded lesson about YAML scalars being silently truncated or
reshaped when a long narrative value is wrapped.

**D-05 — do NOT touch `status: OPEN`, and do NOT `git mv` the file to `completed/`.** The filename
does not change; two other pending todos reference it by name across three lines, and all three
references must keep resolving.

Change nothing else. `created`, `area`, `found_by`, `source` and `files` stay as they are.
  </action>
  <verify>
    <automated>
P=.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md

# 1. The four locked values, anchored, bare and lowercase.
test "$(grep -c '^severity: minor$' "$P")" = "1"
test "$(grep -c '^platform: macos$' "$P")" = "1"
test "$(grep -c '^ready: human$' "$P")" = "1"
test "$(grep -c '^needs: credentialed-in-app-store-actions-on-a-signed-build$' "$P")" = "1"
test "$(grep -c '^status: OPEN$' "$P")" = "1"

# 2. The OLD values are gone from FRONTMATTER position (column 0). They may still appear
#    inside the frozen STATUS 2026-09-23 prose, which is correct and must not be swept.
test "$(grep -c '^severity: major$' "$P")" = "0"
test "$(grep -c '^ready: live-gate$' "$P")" = "0"
test "$(grep -c '^needs: release-run-then-browser-download-verify$' "$P")" = "0"

# 3. KEY ORDER severity -> platform -> ready (CLAUDE.md gate rule). Three consecutive lines.
test "$(grep -n '^severity: \|^platform: \|^ready: ' "$P" | cut -d: -f1 | tr '\n' ',')" = "5,6,7,"

# 4. Title restated, on ONE line, not a block scalar.
grep -q "^title: 'macOS signing and notarization are VERIFIED end-to-end on a published artifact including the quarantined Gatekeeper path — only the credentialed in-app store actions (Epic/Amazon/GOG login) on a signed build remain unexercised'$" "$P"
test "$(grep -c '^title: |' "$P")" = "0"

# 5. Still OPEN and still in pending/. Filename unchanged, so its inbound references hold.
#    MEASURED at 3ee65a624: 2 FILES, 3 LINES. Both numbers are pinned so a later reader does not
#    treat the disagreement as a discrepancy -- `grep -rl` returns 2, `grep -rn` returns 3.
test -f "$P"
test ! -f .planning/todos/completed/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md
test "$(grep -rl '2026-09-04-macos-releases-ship-unsigned-and-unnotarized' .planning/todos/pending/ | wc -l | tr -d ' ')" = "2"
test "$(grep -rn '2026-09-04-macos-releases-ship-unsigned-and-unnotarized' .planning/todos/pending/ | wc -l | tr -d ' ')" = "3"

# 6. Exactly ONE tracked file modified by the whole quick task.
test "$(git diff --name-only | wc -l | tr -d ' ')" = "1"
test "$(git diff --name-only)" = "$P"

# 7. MANDATED BY CLAUDE.md, AND VACUOUS ON THIS PATH (.prettierignore bare `.planning`).
#    Run because the convention requires it; a green result proves nothing here.
npx prettier --check .planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md

pnpm planning-gates   # MUST report 12/12, including todo-frontmatter-gate.py
    </automated>
  </verify>
  <done>
Frontmatter reads `severity: minor` / `platform: macos` / `ready: human` on lines 5-7 in that order,
`needs: credentialed-in-app-store-actions-on-a-signed-build`, `status: OPEN`, and the exact locked
title on one line. The file is still at its original path in `todos/pending/`. `git diff
--name-only` returns exactly one path. `pnpm planning-gates` is 12/12.
  </done>
</task>

</tasks>

<verification>

**Whole-task gates, run once after both tasks.**

1. **`pnpm planning-gates` reports 12/12.** Baseline measured 12/12 at HEAD `3ee65a624` while this
   plan was written. `todo-frontmatter-gate.py` is the one that would convict a bad `severity:` /
   `platform:` / `ready:` value, and `planning-envelope-tag-gate.py` the one that would convict a
   stray envelope tag.
2. **Exactly one tracked file modified.** `git diff --name-only` returns the todo path and nothing
   else. No `STATE.md` write is part of this plan — recording the quick task in STATE.md is the
   orchestrator's Step 8, and this repo has a recorded lesson about a quick task shipping and never
   being recorded, so **do not let this plan's silence on STATE.md be read as "no recording is
   owed"**.
3. **No re-run of the gate.** No artifact was downloaded, no release published, no dmg mounted, no
   app launched, no helper exec'd. If a shell history for this session contains `gh release`,
   `hdiutil`, `spctl`, `codesign` or `open -a`, the plan was executed wrong.
4. **The prettier check is reported honestly.** It is present in both tasks because CLAUDE.md
   mandates it. It inspects zero files on this path (probed at `3ee65a624`: `--list-different`
   prints nothing and exits 0; the same command with `--ignore-path /dev/null` prints the path and
   exits 1). **Report it as run-and-vacuous, never as "formatting verified".**

</verification>

<success_criteria>

- The todo carries four STATUS sections; `git diff --numstat` after Task 1 showed **0 deletions**,
  so `## STATUS 2026-09-14`, `## STATUS 2026-09-17`, `## STATUS 2026-09-23`, `## Problem`,
  `## Why it matters`, `## Direction` and `## Verification` are byte-identical to `3ee65a624`.
- The new section records the headline without softening or overstating it: **an interstitial DID
  appear**, it was the benign notarized confirmation with Open as the default button, and the
  blocking form did not appear — so the bullet's wording is falsified and its intent confirmed.
- The new section names both things the run does not prove (localhost origin on a still-DRAFT
  release; the build machine, not a never-built machine) and names `syspolicy_check distribution` as
  the cache-independent mitigation for the second.
- `## Why it matters` consequence 1 is corrected by name FROM INSIDE the new section and is NOT
  edited in place; its `[ASSUMED]` marker is noted as discharged.
- Residual (c) is split three ways: (c-i) and (c-ii) CLOSED GREEN with verbatim evidence,
  (c-iii) — the credentialed round-trips — named as the entire remaining residue.
- Frontmatter: `severity: minor`, `platform: macos`, `ready: human` in that order;
  `needs: credentialed-in-app-store-actions-on-a-signed-build`; `status: OPEN`; the exact locked
  title on one line.
- The todo is still in `todos/pending/` under its original filename; its inbound references from
  two other pending todos (three lines) still resolve.
- `pnpm planning-gates` is 12/12. Exactly one tracked file changed. Nothing under `src/`, `meta/`,
  `src-tauri/` or `.github/` changed.

</success_criteria>

<output>
Create `.planning/quick/260924-962-close-macos-gatekeeper-browser-download-arm/SUMMARY.md` when done.

The summary must state, in its own words: which of the todo's five Verification bullets are now
satisfied and on what evidence; that the fourth bullet's WORDING was falsified while its INTENT was
confirmed; the two clauses the substitute route does not reproduce; and that (c-iii) — the
credentialed Epic/Amazon/GOG round-trips — is the entire remaining residue of the todo.
</output>
