---
phase: quick-260818-v81
plan: 01
subsystem: infra
tags: [steam, macos, mach-o, chmod, depot, resume, reconciliation]

# Dependency graph
requires:
  - phase: 23-08
    provides: applyMachOExecutableFallback (Mach-O magic-byte content-gated chmod compensator) and the fresh-download call site that already used it correctly
provides:
  - healReconciledFileModes now applies applyMachOExecutableFallback unconditionally to every non-directory/non-symlink reconciled file, closing the second half of G-23-02
  - library.ts's buildResumeFinalizeOpts (startup-resume caller) inherits the fix with zero edits, since it shares this exported function
affects: [phase-23, steam-depot, macos-launch]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Shared-function fix: a single exported function (healReconciledFileModes) has two callers (downloadDepotFiles' post-reconciliation heal, library.ts's startup-resume path) — fixing the function fixes both without touching the second caller"

key-files:
  created: []
  modified:
    - src/backend/storeManagers/steam/depot.ts
    - src/backend/storeManagers/steam/__tests__/depot.test.ts

key-decisions:
  - "Restructured healReconciledFileModes's compound early-continue guard (jobFiles.has(file) || !file.flags) into two separate checks so a flagless manifest entry falls through to the Mach-O fallback instead of being skipped entirely — mirrors the fresh-download call site's shape exactly"
  - "Made the Directory(64)/Symlink(512) skip undefined-safe (file.flags !== undefined && ...) rather than relying on `undefined & 64` coercing to 0, since file.flags is now reachable as undefined at that point"
  - "applyMachOExecutableFallback runs unconditionally after the (now-conditional) applyEDepotFileModes call, never wrapped in try/catch and never allowed to influence allModesHealed/failures — the fallback already swallows and logs its own errors"

requirements-completed: [G-23-02]

# Metrics
duration: 28min
completed: 2026-08-18
---

# Quick Task 260818-v81: Close G-23-02's Reconciled-Heal Gap Summary

**`healReconciledFileModes`'s `!file.flags` early-continue was itself the surviving half of G-23-02 — restructured the guard so every reconciled flagless file (the HUMANKIND shape: flagBearing=140/18,949, Directory-only) now reaches the Mach-O executable fallback, the same compensator the fresh-download path already had.**

## Performance

- **Duration:** ~28 min
- **Started:** 2026-08-18T10:34:30Z (first task commit)
- **Completed:** 2026-08-18T10:47:50Z (regression sweep gates all green)
- **Tasks:** 3 (RED, GREEN, regression sweep)
- **Files modified:** 2

## Accomplishments
- Proved the gap was real: Task 1's RED run split exactly 1 failing / 2 passing — the flagless-reconciled-Mach-O-EXECUTE case failed against unmodified `depot.ts` (file stayed at its deliberately-wrong 0o644 starting mode), while the content-gating declines (plain text, Mach-O BUNDLE) passed vacuously (nothing ran) for the wrong reason.
- Closed the gap: `healReconciledFileModes` now calls `applyMachOExecutableFallback` unconditionally for every reconciled file that isn't a Directory/Symlink entry, in the same order as the fresh-download call site (EDepotFileFlag modes first, then the Mach-O fallback).
- Verified both callers benefit without editing the second one — `library.ts` never appears in `git diff --stat`.
- Verified no regression: the full `depot.test.ts` suite (127 tests, including the pre-existing WR-01 directory-traversable-bit pin and the `flags: 32` mode-heal pin) stays green, plus `library.test.ts` and `depotPrimitives.test.ts`.
- Confirmed `pnpm codecheck` (tsc --noEmit) and `pnpm lint` (eslint) both clean on the two changed files — 0 errors (121 pre-existing warnings, none newly introduced by this diff).

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — pin the reconciled-heal Mach-O gap with three failing tests** - `e0c1eb675` (test)
2. **Task 2: GREEN — restructure healReconciledFileModes's guard and wire in the fallback** - `343b74518` (fix)
3. **Task 3: Regression sweep + no-collateral gates** - no code changes (verification-only task; see gate output below)

**Plan metadata:** committed separately by the orchestrator (this SUMMARY, STATE.md, ROADMAP.md are not committed by the executor per this run's constraints).

## Files Created/Modified
- `src/backend/storeManagers/steam/depot.ts` - `healReconciledFileModes`'s per-file loop restructured: `jobFiles.has(file)` stays an unconditional early-continue; the Directory/Symlink skip is now undefined-safe; `applyEDepotFileModes` runs only when `file.flags` is truthy; `applyMachOExecutableFallback` runs unconditionally for every remaining file, after the (optional) mode-flag branch. Doc comment updated to record the G-23-02 root cause and the ~140→~18.5k widened-loop cost tradeoff for a HUMANKIND-shaped title.
- `src/backend/storeManagers/steam/__tests__/depot.test.ts` - New nested `describe('reconciled (skipped-download) files — G-23-02 heal path')` inside the existing Mach-O fallback describe block: three tests (the gap pin, plain-text decline, Mach-O BUNDLE decline), reusing the existing `buildThinMachOHeader` header builder and the established reconciled-file test recipe (pre-written file at a deliberately wrong 0o644 mode, `fetchChunk` mocked to reject, sha matched so the reconciler treats it as already-complete).

## Decisions Made
- See `key-decisions` in frontmatter. No decisions required user input — the `<critical_finding>` in the plan's context already specified the exact restructure shape, matched byte-for-byte against the fresh-download call site.

## Deviations from Plan

None - plan executed exactly as written. The plan's own `<critical_finding>` correctly anticipated that a naive "append a call" fix would sit in dead code; the guard restructure it prescribed is exactly what Task 2 implemented.

## Issues Encountered

**`depot.finalize.test.ts` OOM/timeout during the Task 3 regression sweep, confirmed pre-existing and unrelated to this plan's diff.** Running the four-suite batch (`depot.test.ts`, `library.test.ts`, `depot.finalize.test.ts`, `depotPrimitives.test.ts`) together hit a jest-worker `JavaScript heap out of memory` crash partway through `depot.finalize.test.ts` (`depot.test.ts`, `library.test.ts`, `depotPrimitives.test.ts` all passed: 396/396 tests green). Running `depot.finalize.test.ts` in isolation with `NODE_OPTIONS="--max-old-space-size=8192" --runInBand` avoided the OOM but surfaced 2 failing / 2 passing (both failures: a real `setInterval`-driven progress-heartbeat timer firing after test teardown, then a 5000ms test timeout on Test E). To rule out this plan's diff as the cause, `depot.ts` was checked out to its pre-fix commit (`6913442b1`) via `git checkout 6913442b1 -- src/backend/storeManagers/steam/depot.ts` (a scoped, sanctioned single-file checkout — never `git stash`/`git clean`/`git reset --hard`), the same isolated run was repeated, and it reproduced the **identical** 2 failed / 2 passing split with the same test names and the same `emitProgress`/`Timeout._onTimeout` stack shape (only line numbers shifted by this plan's +44 net lines). The pre-fix `depot.ts` was then restored via `git checkout HEAD -- src/backend/storeManagers/steam/depot.ts` and `depot.test.ts` re-run to confirm the fix was intact (127/127 green). This confirms the `depot.finalize.test.ts` flake is a pre-existing environment/timer-cleanup issue, out of this plan's scope per the deviation rules' scope boundary ("only auto-fix issues DIRECTLY caused by the current task's changes") — not fixed here.

Also note (transient, not a code issue): the repo's `.husky/post-checkout` hook fires on any `git checkout -- <file>` and attempts to download packaged helper binaries, failing deterministically on a placeholder macOS asset digest (`legendary_macOS_x86_64_onedir.tar.gz` / `PENDING-CI-PUBLISH`, per the known "GameLib worktrees are HARD-BLOCKED" finding). The hook's failure did not prevent the file-level checkout from succeeding in either direction; both restores were verified with `git status --short` / `grep -n '!file.flags'` before proceeding.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Code-level fix for G-23-02 is complete and unit-verified. Per the plan's `<success_criteria>`, this plan does NOT claim live-hardware confirmation that a real HUMANKIND resume now launches on macOS — that requires an operator-run install and belongs to Phase 23's own wave-10 hardware gate (tracked separately in project memory as still-open: "HUMANKIND end-to-end throughput still unconfirmed"). The pre-existing `depot.finalize.test.ts` flake documented above is unrelated to this fix and remains open for a future task to investigate (real-timer cleanup between tests, likely in the progress-heartbeat `setInterval` teardown).

---
*Phase: quick-260818-v81*
*Completed: 2026-08-18*

## Self-Check: PASSED

- FOUND: src/backend/storeManagers/steam/depot.ts
- FOUND: src/backend/storeManagers/steam/__tests__/depot.test.ts
- FOUND: .planning/quick/260818-v81-fix-g-23-02-gap-healreconciledfilemodes-/260818-v81-SUMMARY.md
- FOUND commit: e0c1eb675 (Task 1)
- FOUND commit: 343b74518 (Task 2)
