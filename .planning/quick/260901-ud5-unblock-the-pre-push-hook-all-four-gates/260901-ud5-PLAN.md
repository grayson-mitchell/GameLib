---
id: 260901-ud5
title: "Unblock the .husky/pre-push hook — get all four gates green"
date: 2026-09-01
mode: quick
status: in-progress
---

# Quick Task 260901-ud5 — Unblock the pre-push hook

## Measured baseline (HEAD `f04dcbb66`, clean tree, all four legs run in place)

`.husky/pre-push` = `pnpm codecheck && pnpm lint && pnpm prettier && pnpm i18n --fail-on-update`

| leg | command | result |
|---|---|---|
| 1 | `tsc --noEmit` | **PASS** |
| 2 | `eslint --cache .` | **FAIL** — 12 errors, 4191 warnings |
| 3 | `prettier --check .` | **FAIL** — 46 files |
| 4 | `i18next --silent --fail-on-update` | **FAIL** — 78 added `en/` keys |

Both stored records were stale: `prettier-gate-is-red-repo-wide` says lint is GREEN
(it is not — 12 errors) and prettier is 3 files (it is 46); the 2026-08-03 todo says
280 files.

## Task 1 — clear the 12 eslint errors

**files:** `meta/__tests__/cleanDist.test.ts`, `meta/__tests__/pruneStaleHelperBinaries.test.ts`,
`meta/checkBuildBinMirror.ts`, `meta/pruneStaleHelperBinaries.ts`,
`src/backend/__tests__/packagingConfig.test.ts`,
`src/backend/sidecar/__tests__/appShellFlows.test.ts`,
`src/backend/sidecar/__tests__/steamAuthFlows.test.ts`,
`src/backend/sidecar/installedJsonWatcher.ts`, `src/backend/utils.ts`,
`src/frontend/screens/WebView/index.tsx`,
`src/frontend/state/__tests__/GlobalStateSleepAssertionClassification.test.ts`

**action:** Each error triaged individually — none is a blanket suppression.

1. `cleanDist.test.ts:21` — drop the `loadYaml` import. Line 350 of the same file records
   that Phase 35 Plan 14 REMOVED the `electron-builder.yml` artifactName pin (the file was
   deleted by the Electron cutover); the import is that removal's leftover. Trim the now-false
   docstring paragraph that still claims the YAML is parsed structurally.
2. `pruneStaleHelperBinaries.test.ts:18` — drop unused `readdirSync`.
3. `checkBuildBinMirror.ts:38` — drop unused `readdirSync`.
4. `pruneStaleHelperBinaries.ts:46` — drop unused `readlinkSync`.
5. `packagingConfig.test.ts:79,83` — `parseElectronBuilder` / `loadStrippedElectronBuilder`
   are dead. **`electron-builder.yml` does not exist** (verified: `ls` → No such file).
   Deleting only the two flagged functions orphans `loadElectronBuilderRaw`,
   `ELECTRON_BUILDER_PATH`, `ElectronBuilderConfig`, and the `loadYaml` / `stripHashComments`
   imports, which would re-fire the same rule — so the whole dead electron-builder chain goes,
   plus the docstring paragraph describing it. `readFileSync` and `join` STAY (used by the
   Tauri half). `stripJsComments` stays.
6. `appShellFlows.test.ts:1079` — `require('../sidecarRpc')` has no disable comment while the
   two `require()`s bracketing it (1071, 1087) do. Add the same
   `// eslint-disable-next-line @typescript-eslint/no-require-imports`.
7. `steamAuthFlows.test.ts:152` — delete the unused `MAIN_TS_PATH` const. `join` stays
   (used at 920/958/1009/1067).
8. `installedJsonWatcher.ts:106` — `await-thenable`. eslint is right about production:
   `LegendaryLibraryManager.refreshInstalled()` is **synchronous** (library.ts:131, no
   `async`). But the `await` is load-bearing for the test contract —
   `installedJsonWatcher.test.ts:365` does `refreshInstalledMock.mockRejectedValueOnce(...)`
   and :353 asserts production adds no catch. Dropping the `await` would let
   `sendFrontendMessage('refreshLibrary')` fire on a failed rebuild and turn the rejection
   into an unhandled one. So: KEEP the await, add a disable naming the exact rule
   `@typescript-eslint/await-thenable` with that rationale.
9. `utils.ts:22` — drop the unused `BrowserWindow` value import. The type import at line 61
   (`BrowserWindowType` from `backend/platform/types`, D-35-13-03) is a different symbol and stays.
10. `WebView/index.tsx:67` — `setAmazonLoginData` is dead: commit `40dcd9ac1` (35-17) deleted
    the effect body that called it, and its replacement comment names
    `useTauriOAuthLogin.ts`'s `getAmazonLoginData()` as the sole remaining owner. Drop the
    setter from the destructure only — behaviour-identical. `amazonLoginData` is now
    permanently `null` at 6 read sites; **file a todo**, do not fix here.
11. `GlobalStateSleepAssertionClassification.test.ts:132` — the disable above it names
    `no-new-func`, but the rule that fires is `@typescript-eslint/no-implied-eval`. Textbook
    [[eslint-disable-naming-a-dead-rule-suppresses-nothing]]. Add the real rule name.

**verify:** `npx eslint --cache . -f json` filtered on `severity === 2` returns `[]`.
Plain output interleaves 4191 warnings and cannot be read for this.
**done:** `pnpm lint` exits 0.

## Task 2 — one pure-formatting prettier commit

**files:** `.prettierignore` + the 46 flagged files

**action:** Two config decisions settled BEFORE the sweep, per the standing landmine note:
- `public/bin/arm64/darwin/legendary/_internal/legendary_gl-0.21.0.dist-info/direct_url.json`
  is downloaded helper-binary output, not source — `.prettierignore` gets `public/bin`, it is
  not formatted. `download-helper-binaries` is tag-idempotent and would reintroduce the drift
  on every re-download otherwise.
- `src/preload/.prettierrc`'s `printWidth: 120` override: **leave it as-is for this sweep.**
  No `src/preload/` non-test file is in the 46, so the sweep does not widen its blast radius,
  and retiring the override is a separate ~14-file churn decision.
Then `npx prettier --write` on exactly the remaining flagged files. **No behavioural change** —
verified by `git diff --ignore-all-space` being empty of substantive edits.

**verify:** `npx prettier --check .` exits 0; `pnpm codecheck` still passes.
**done:** committed as one formatting-only commit.

## Task 3 — i18n drift (78 keys)

**files:** `i18next-parser.config.js`, `public/locales/en/gamelib.json`, the `t()` call sites,
`src/frontend/screens/Game/GameSubMenu/repairFailure.ts`

**action:** Follow the plan already written in
`.planning/todos/pending/2026-09-01-pnpm-i18n-reports-78-uncommitted-en-keys-4-of-them-test-sentinels.md`
(inventory: `.planning/quick/260901-n60-.../260901-n60-TRIAGE.md`). Buckets:
- **T (4)** — exclude `__tests__` / `*.test.*` from the parser `input` glob.
- **R (67)** — re-namespace fork-authored strings into `gamelib.json`, prefix call sites `gamelib:`.
- **P (6)** — decide the three `{{count}}` plural pairs on the merits (possible live bug —
  `{{count}}` is a reserved interpolation name).
- **E (1)** — give `box.repair.error` a literal default.

**Hard constraint:** the two gates guarding `public/locales/` contradict each other on this
drift. `meta/i18nCatalogChurnGuard.ts` (D-05) forbids `pnpm i18n` changing any catalog except
`gamelib.json`/`gamelib.mt.json`. Committing the parser output as-is goes green on pre-push and
violates D-05. End state: `pnpm i18n` writes **only** `gamelib.json`.

**verify:** `pnpm i18n --fail-on-update` exits 0 AND the D-05 churn guard passes.
**done:** both green.

## Task 4 — prove the hook

**action:** A real `git push` with hooks enabled, no `--no-verify`.
**done:** push succeeds through all four legs.

## Notes

- `pnpm i18n --fail-on-update` **writes nothing** (verified again this run: `git status
  --porcelain public/locales` empty after). Only bare `pnpm i18n` writes.
- Never `git checkout -- <file>` here; `.husky/post-checkout` fires and fails. Use `cp`.
