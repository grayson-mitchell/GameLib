---
phase: quick-260922-8kv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/screens/Settings/sections/LogSettings/index.css
  - .planning/todos/pending/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md
  - .planning/todos/completed/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md
autonomous: true
requirements: [QUICK-260922-8kv]

must_haves:
  truths:
    - "The `dialog .logs-wrapper` block no longer exists in LogSettings/index.css."
    - "No other rule in LogSettings/index.css was removed or altered — the file is 106 lines and prettier-clean."
    - "Every class the deleted block named (.log-buttongroup, .setting.log-box, .logs-wrapper) still has its live rule in the same file."
    - "The pending todo is closed into completed/ with a resolution note naming the decision and the CSS commit sha."
    - "The resolution edit survives the git move — the staged blob carries it, not HEAD's content."
  artifacts:
    - path: "src/frontend/screens/Settings/sections/LogSettings/index.css"
      provides: "LogSettings styling, minus the dead dialog-scoped block"
      contains: ".logs-wrapper"
    - path: ".planning/todos/completed/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md"
      provides: "Closed todo with resolution record"
      contains: "RESOLVED"
  key_links:
    - from: "src/frontend/screens/Settings/sections/LogSettings/index.tsx"
      to: "src/frontend/screens/Settings/sections/LogSettings/index.css"
      via: "className logs-wrapper / log-buttongroup / setting log-box"
      pattern: "\\.logs-wrapper \\{"
---

<objective>
Delete the dead `dialog .logs-wrapper` block at `src/frontend/screens/Settings/sections/LogSettings/index.css:52`, and close the pending todo that filed it.

Purpose: the block's leading `dialog` is a bare **element** selector. This app has no `<dialog>` element in its tree — its dialogs are MUI `Dialog`s whose Paper is a `div` — so the rule is injected into the page and matches nothing. It is a latent trap: it LOOKS live, so a future reader may edit it expecting an effect and get none, or repoint it and silently ship an unreviewed UX change.

Output: an 11-declaration deletion (12 lines including the trailing blank), and the todo moved to `completed/` with a resolution record.
</objective>

<decision_record>
**The operator has already decided this. Do not re-open it, do not re-litigate it, do not offer alternatives.**

**DELETE the entire block.** Do not retarget it.

The block does three things, and only one of them is a height:

1. `height: 15em` — shrinks the log box inside a dialog (240px vs the default `25em`/400px)
2. `.log-buttongroup { display: none }` — **hides the log-picker UI entirely**
3. `grid-template-columns: 1fr` + `.setting.log-box { grid-column: 1 }` — single-column collapse to suit (2)

Item 2 is an unreviewed UX change that has never been in front of a user. Retargeting the selector at `.settingsDialogContent` would silently ship it. This repo has already rejected exactly this revive-unreviewed-dead-styling move **at this same component**: see `src/frontend/components/UI/Dialog/components/Dialog.tsx` (~lines 109-120), where an `.Dialog__element` `maxWidth`/`paddingTop` pair was deliberately DROPPED rather than realized, for this reason. This task follows that precedent.

**OUT OF SCOPE — do not do any of these:**
- Retargeting the selector to `.settingsDialogContent`, `.MuiDialogContent-root`, or anything else
- Preserving item 1 (`height: 15em`) at a new selector
- Any change to `Dialog.tsx` — including its `:has(.logs-wrapper)` selectors at lines 56 and 59
- Any change to the log-picker UI or `.log-buttongroup`
- Any code review, verification, or UAT step (this run has no `--validate`)
</decision_record>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/todos/pending/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md
@src/frontend/screens/Settings/sections/LogSettings/index.css

## Measured baseline (taken 2026-09-21 at planning time — re-measure before trusting)

`src/frontend/screens/Settings/sections/LogSettings/index.css` is **118 lines** and currently
prettier-clean (`pnpm exec prettier --check <file>` exits 0).

The dead block occupies **lines 52-62**, with line 51 and line 63 both blank:

```
50|}
51|
52|dialog .logs-wrapper {
53|  grid-template-columns: 1fr;
54|  flex-grow: 1;
55|  height: 15em;
56|  .log-buttongroup {
57|    display: none;
58|  }
59|  .setting.log-box {
60|    grid-column: 1;
61|  }
62|}
63|
64|.log-buttons.log-choosen,
```

**Deleting lines 52-63** (the block plus ONE trailing blank, not two) yields a 106-line file that is
still prettier-clean and still has exactly one blank line between `}` at 50 and
`.log-buttons.log-choosen,`. This was simulated at planning time with `sed '52,63d'` and both facts
verified. Deleting only 52-62 would leave two consecutive blank lines.

## Who references the named classes, and why deletion is a no-op for each

| Reference | What styles it after deletion | Affected? |
|---|---|---|
| `LogSettings/index.tsx:198` `classNames('logs-wrapper', ...)` | `.logs-wrapper` at css:5, `.logs-wrapper.game-log` at css:38/47 | No |
| `LogSettings/index.tsx:203` `<span className="log-buttongroup">` | nested `.log-buttongroup` at css:13, inside `.logs-wrapper` | No — the deleted copy at css:56 only ever hid it under a `<dialog>` ancestor that does not exist |
| `LogSettings/index.tsx:43` and `:226` `<span className="setting log-box">` | nested `.setting.log-box` at css:26 | No |
| `Dialog.tsx:56` and `:59` `:has(.logs-wrapper)` | JS-side `sx` on StyledPaper, independent of this stylesheet | No — and out of scope |
| `ProgressDialog/index.tsx:68` `"progressDialog log-box"` | `ProgressDialog/index.css:6`, `themes.scss:457` | No — a different compound; `.setting.log-box` never matched it |

The target block is also the **only** bare `dialog` element selector anywhere under `src/` in any
`.css`/`.scss` file. After deletion that population is zero.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead block and commit the CSS change</name>
  <files>src/frontend/screens/Settings/sections/LogSettings/index.css</files>
  <action>
Re-measure first, then delete. The line numbers above are from planning time; confirm them against
the working tree before cutting.

1. Confirm the block is still at lines 52-62 and that 51/63 are blank:
   `awk 'NR>=50 && NR<=64 {printf "%d|%s|\n", NR, $0}' src/frontend/screens/Settings/sections/LogSettings/index.css`
   If the numbers have moved, locate the block by its opening line `dialog .logs-wrapper {` and its
   matching closing `}` and adjust — delete the block plus exactly one adjacent blank line.

2. Delete the block and the single trailing blank line (lines 52-63 at the measured baseline). Use
   the Edit tool on the exact block text, or `sed -i '' '52,63d' <file>` if the numbers confirmed.
   Do not touch anything else in the file.

Leave no comment tombstone in the CSS. The record of why this was removed lives in the closed todo
and the commit message, not in a stylesheet comment — a comment naming the deleted selector would
also make a future `grep dialog .logs-wrapper` census confidently wrong.

3. Commit the CSS change alone (do not stage the todo yet — Task 2 needs this commit's sha):
   `git add src/frontend/screens/Settings/sections/LogSettings/index.css`
   `git commit` with subject
   `fix(quick-260922-8kv): delete the dead dialog .logs-wrapper block in LogSettings`
   and a body recording: the selector is a bare `dialog` element selector, this app has no
   `<dialog>` element (MUI Paper is a `div`, confirmed live under WKWebView 2026-09-21), and the
   block was DELETED rather than retargeted because item 2 (`.log-buttongroup { display: none }`)
   is an unreviewed UX change — following the `.Dialog__element` precedent recorded at
   `Dialog.tsx:109-120`.
   End the message with the attribution line this session was given.
  </action>
  <verify>
    <automated>
# Run each check unpiped and read ITS exit code. Do NOT pipe any of these to `tail`/`head`
# — this repo has a measured trap where piping masks the real exit status.
set -e
F=src/frontend/screens/Settings/sections/LogSettings/index.css

# 1. The target selector is gone, repo-wide (expect ZERO hits; grep exits 1, hence the `!`)
! grep -rn 'dialog \.logs-wrapper' src/
! grep -rn '^dialog[[:space:]]' --include='*.css' --include='*.scss' src/

# 2. No collateral removal — the file is exactly 106 lines
test "$(wc -l < $F)" -eq 106

# 3. Each class the deleted block named still has EXACTLY ONE live rule in this file
test "$(grep -c 'log-buttongroup' $F)" -eq 1        # was 2 (css:13 live + css:56 dead)
test "$(grep -c '\.setting\.log-box' $F)" -eq 1     # was 2 (css:26 live + css:59 dead)
test "$(grep -c 'logs-wrapper' $F)" -eq 3           # was 4 (css:5, 38, 47 live + css:52 dead)

# 4. The three live rules are the ones that survived, by content not by count
grep -q '^\.logs-wrapper {' $F
grep -q '^\.logs-wrapper\.game-log {' $F
grep -q 'height: 25em;' $F                          # the DEFAULT height, not the dead 15em
! grep -q 'height: 15em;' $F

# 5. Formatting is canonical (this is the only gate that actually reads CSS)
pnpm exec prettier --check $F

# 6. Typecheck passes (CSS-only change, so this must be unchanged-green, not merely non-worse)
pnpm codecheck

echo "TASK 1 VERIFY OK"
    </automated>
  </verify>
  <done>
`dialog .logs-wrapper` returns zero hits repo-wide; no bare `dialog` element selector remains in any
`.css`/`.scss` under `src/`; the file is 106 lines and prettier-clean; `.log-buttongroup`,
`.setting.log-box` and `.logs-wrapper` each retain their live rule(s); `pnpm codecheck` exits 0; the
CSS change is committed and its sha is recorded for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close the todo into completed/ with a resolution record</name>
  <files>.planning/todos/pending/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md, .planning/todos/completed/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md</files>
  <action>
**MOVE FIRST, EDIT SECOND. This ordering is load-bearing.** `git mv` commits the file's HEAD content
and silently drops unstaged edits — that trap has bitten this repo twice, the second time running 7
days undetected. If you edit the file in `pending/` and then `git mv` it, your resolution note is
gone and nothing turns red.

1. Prove the file is clean against HEAD before moving:
   `git diff --quiet -- .planning/todos/pending/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md`
   (must exit 0). If it exits 1, stop — something has already edited it, and moving would lose that.

2. Move it, unedited:
   `git mv .planning/todos/pending/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md .planning/todos/completed/`

3. NOW edit the file **at its new path under `completed/`**:

   Frontmatter — prefix the existing `title:` with `RESOLVED — ` (keep it a single quoted scalar on
   one line), and add these two keys after `platform:`, matching the sibling todo's shape:
   ```
   resolved: 2026-09-21
   resolved_by: "quick-260922-8kv, commit <TASK 1 SHA>"
   ```
   Leave `severity`, `platform`, `ready` and `files` as they are — do not retune them. Per CLAUDE.md
   the todo-frontmatter gate's scope is `pending/` only; `completed/` is deliberately exempt, so
   these keys are preserved for the record, not for the gate.

   Body — insert a `## RESOLVED 2026-09-21 — the block was DELETED, not retargeted` section
   immediately under the H1, before `## Measured facts`. It must record:
   - **The decision and who took it:** the operator chose deletion over retargeting.
   - **Why deletion and not a retarget**, in the todo's own terms: the block does three things and
     only item 1 is a height; item 2 (`.log-buttongroup { display: none }`) is an unreviewed UX
     change that has never been in front of a user, so retargeting at `.settingsDialogContent`
     would silently ship it. Cite the binding precedent at `Dialog.tsx:109-120` where an
     `.Dialog__element` sizing pair was deliberately DROPPED rather than realized for the same
     reason.
   - **What actually shipped:** 12 lines removed from `LogSettings/index.css` (the 11-line block
     plus one blank), file 118 → 106 lines. Zero user-visible effect, because the rule never matched
     anything — this is the deletion of an already-inert rule, not a styling change.
   - **What was explicitly NOT done:** no retarget, no preservation of `height: 15em` at a new
     selector, no change to `Dialog.tsx`, no change to the log-picker UI.
   - **The gate results measured at close** (fill in the real numbers from this run, do not copy
     the sibling's).
   - The Task 1 commit sha.

   Two authoring cautions, both from CLAUDE.md / this repo's measured gate traps:
   - Do not leave a stray bare closing envelope-style tag alone on a line in a trailing run — the
     12th planning gate (`.planning/planning-envelope-tag-gate.py`) convicts exactly that shape.
   - Do not spell joined angle-bracket tag literals in the prose; if you must name one, split it.

4. Stage the edited file and **prove the staged blob carries the edit** (this is the check that
   catches the `git mv` trap if the ordering slipped):
   `git add .planning/todos/completed/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md`

5. Run the planning gates unpiped, then commit the todo move:
   subject `docs(quick-260922-8kv): close the dead dialog .logs-wrapper todo on the delete decision`
   End the message with the attribution line this session was given.
   Do not write any claim about push state into the commit message.
  </action>
  <verify>
    <automated>
# Unpiped, exit codes read directly.
set -e
OLD=.planning/todos/pending/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md
NEW=.planning/todos/completed/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md

# 1. Moved: gone from pending/, present in completed/
test ! -e "$OLD"
test -f "$NEW"

# 2. THE GIT-MV TRAP CHECK — the STAGED blob must carry the resolution edit,
#    not HEAD's pre-edit content. `git show :<path>` reads the index, not the worktree.
git show ":$NEW" > /tmp/staged-todo.md
grep -q 'RESOLVED' /tmp/staged-todo.md
grep -q '^resolved: 2026-09-21' /tmp/staged-todo.md
grep -q '^resolved_by:' /tmp/staged-todo.md
grep -q 'quick-260922-8kv' /tmp/staged-todo.md

# 3. The resolution names the Task 1 commit sha (substitute the real sha)
grep -q '<TASK 1 SHA>' /tmp/staged-todo.md

# 4. Frontmatter still parses as a single YAML block with its triage keys intact
grep -q '^severity: minor$' "$NEW"
grep -q '^platform: any$' "$NEW"

# 5. All twelve planning gates pass. MINIMUM_EXPECTED_GATES is 12 as of quick-260922-7pv.
pnpm planning-gates

echo "TASK 2 VERIFY OK"
    </automated>
  </verify>
  <done>
The todo file exists only under `completed/`; `git show :<path>` (the staged blob, not the worktree)
contains `RESOLVED`, `resolved: 2026-09-21`, `resolved_by:` and the Task 1 sha — proving the edit
survived the move; the frontmatter triage keys are intact; `pnpm planning-gates` reports 12/12 and
exits 0; the move is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed. This plan deletes eleven inert CSS declarations and moves a markdown file. No new
dependency is installed, no package manager runs, no network call is made, no user input is parsed,
and no runtime code path changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8kv-01 | Tampering | `LogSettings/index.css` — collateral deletion of a live rule while cutting the dead block | mitigate | Task 1 verify pins the post-state by content AND by count: file is exactly 106 lines, `.logs-wrapper`/`.log-buttongroup`/`.setting.log-box` each retain their live rule, `height: 25em` present and `height: 15em` absent |
| T-8kv-02 | Tampering | git index — `git mv` writing HEAD content and silently discarding the resolution note | mitigate | Move-before-edit ordering is mandated in Task 1's action, and Task 2 verify reads the **staged blob** via `git show :<path>` rather than the worktree, so a slipped ordering turns red |
| T-8kv-03 | Repudiation | closed todo carrying no link to what shipped | mitigate | `resolved_by` and the body both name the Task 1 commit sha; Task 2 verify greps for it |
| T-8kv-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task exists in this plan; no `package.json` change |
</threat_model>

<verification>
Run unpiped; read each command's own exit status. Never pipe a gate to `tail` or `head` — this repo
has a measured incident where doing so returned `tail`'s exit code and produced three wrong
conclusions from one habit.

1. `! grep -rn 'dialog \.logs-wrapper' src/` — zero hits repo-wide
2. `! grep -rn '^dialog[[:space:]]' --include='*.css' --include='*.scss' src/` — no bare `dialog` element selector survives anywhere
3. `test "$(wc -l < src/frontend/screens/Settings/sections/LogSettings/index.css)" -eq 106`
4. `pnpm exec prettier --check src/frontend/screens/Settings/sections/LogSettings/index.css` exits 0
5. `pnpm codecheck` exits 0
6. `pnpm planning-gates` exits 0, reporting 12/12
7. `git show :.planning/todos/completed/2026-09-21-dialog-element-selector-in-logsettings-css-is-dead.md` contains the resolution text

`pnpm lint` is not required here: `lintScoped.cjs` runs ESLint over `.ts`/`.tsx` only and cannot see
a CSS edit. Run it if the working tree carries unrelated TS changes; otherwise it measures nothing
about this work, and reporting it green would be a check proving nothing.
</verification>

<success_criteria>
- The `dialog .logs-wrapper` block is gone from `LogSettings/index.css` and nothing else in that file changed (106 lines, prettier-clean).
- `.log-buttongroup`, `.setting.log-box` and `.logs-wrapper` each keep their live rule; the log picker still renders and the log box keeps its default `25em` height.
- No retarget was attempted, `Dialog.tsx` is untouched, and `git status` shows exactly the two intended paths.
- The pending todo is closed into `completed/` with a resolution note that records the delete decision, its rationale, what was deliberately not done, and the CSS commit sha — and the staged blob proves the note survived the move.
- `pnpm codecheck` and `pnpm planning-gates` both exit 0.
</success_criteria>

<output>
Create `.planning/quick/260922-8kv-delete-dead-dialog-logs-wrapper-css-rule/260922-8kv-SUMMARY.md` when done.

The summary must record the measured before/after numbers (118 → 106 lines; `log-buttongroup` 2 → 1;
`.setting.log-box` 2 → 1; `logs-wrapper` 4 → 3 in that file; bare `dialog` element selectors repo-wide
1 → 0), the exact gate exit codes observed, and both commit shas. If any measured number differs from
this plan's baseline, say so explicitly rather than restating the plan's figure — this plan's numbers
were taken at planning time and can rot against the tree you execute on.
</output>
