---
task: 260905-c40
description: Close out Phase 40 — fix the CI-red D-35-29-01 census pin, queue the open items, write the closure records
created: 2026-09-05
type: quick
---

# 260905-c40 — Close out Phase 40

## Why this is not just bookkeeping

Phase 40 arrived at closeout looking finished: 11/11 plans with committed SUMMARYs, live gate
PASS 3/3 on real macOS hardware, and a verification whose gaps had been closed. Its own
`40-VERIFICATION.md`, however, carried an explicit self-limitation:

> A full `pnpm test:ci` was NOT run. The suites this work could plausibly disturb were run
> individually and are green, but the whole-suite run that CI performs has not been reproduced
> locally.

Closing the phase without running it would have shipped a red `.github/workflows/test.yml` under
a green-looking record. So the first task is to run it.

## Tasks

1. **Re-measure every gate at HEAD** rather than reading the verification's numbers — five commits
   landed after its last measurement.
2. **Run the full `pnpm test:ci`.** If red, diagnose ownership before fixing: red-at-HEAD is not
   the same as caused-by-this-phase. Hold the commit constant and vary the tree (hookless worktree
   at `8ac3a8c12`, the last commit before Phase 40's first).
3. **Fix whatever Phase 40 broke**, at source, without weakening the gate that caught it.
4. **Queue every open item into `.planning/todos/pending/`.** Phase 40's verification status is
   `gaps_closed_partially`, and `audit-uat` parses VERIFICATION files only at `human_needed` or
   `gaps_found` (`~/.claude/get-shit-done/bin/lib/uat.cjs:58`) — so the phase is invisible to
   cross-phase audit and a "recorded in the verification" item is a lost item.
5. **Write the closure records** — ROADMAP.md phase heading, the 11 plan checkboxes, a closure
   record, and STATE.md's Current Position, frontmatter and quick-task row.

## Constraints carried in

- **Do not raise the lint ratchet.** REQ-39-01 established it as monotonic-downward.
- **Do not weaken a gate to reach green.** If a gate convicts correct code, fix what it *matches*,
  and prove the relaxed form still convicts the thing it exists to catch.
- **Do not record a number that was not measured in this task.**
