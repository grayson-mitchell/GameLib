---
phase: quick-260902-qs4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - meta/hardcodedStringGate.ts
  - meta/i18nForkTouchedFiles.json
  - meta/__tests__/genI18nGateScope.test.ts
  - .planning/quick/260902-qs4-fix-the-two-red-meta-i18n-gates-a-17-art/260902-qs4-PLAN.md
  - .planning/quick/260902-qs4-fix-the-two-red-meta-i18n-gates-a-17-art/260902-qs4-SUMMARY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The Pattern 3 exemption again covers repairFailure.ts's documented English fallback, WITHOUT reverting 260901-ud5's Bucket E literal-default fix"
    - "The widened exemption is proven non-vacuous: change the t() default's text so it no longer matches the initialiser and the violation returns"
    - "No file other than repairFailure.ts changes its violation set -- the exemption widens by exactly one case"
    - "i18nGateAllowlist.json is NOT touched -- a false positive must not be parked in a debt register"
    - "meta/i18nGateScope.json is byte-identical -- the BLOCKING gate's scope is not widened, and its hand-curated provenance marker is intact"
    - "A-17's committed artifact equals the live git derivation again (204 -> 207), with every literal count pin re-baselined in the SAME change"
    - "The A-03 ratchet still holds exactly: unscanned = forkTouched - scope goes 43 -> 46"
  artifacts:
    - path: "meta/hardcodedStringGate.ts"
      provides: "Pattern 3 recognition of a literal reassigned from a t() call carrying that same literal as its default"
    - path: "meta/i18nForkTouchedFiles.json"
      provides: "fork-touched artifact matching the live derivation at 207 entries"
    - path: "meta/__tests__/genI18nGateScope.test.ts"
      provides: "re-baselined count pins and declared-debt register"
  key_links:
    - "meta/hardcodedStringGate.ts :: isAssignedThenPassedToT (Pattern 3)"
    - "meta/__tests__/genI18nGateScope.test.ts :: DECLARED_UNSCANNED_DEBT, A0/A2/A3/A4 pins"
    - "src/frontend/screens/Game/GameSubMenu/repairFailure.ts:135 :: the flagged fallback literal"
---

<objective>
Two red Meta suites, two unrelated causes, both measured.

**hardcodedStringGate (2 failures, one cause).** The gate flags
`repairFailure.ts:135`'s `let message = 'Repair failed. See the log for details.'`. That
literal is **unchanged** — `git show c2f567064 -- <file>` renders line 135 as pure context.
What changed is the gate's judgement. Pattern 3 (`isAssignedThenPassedToT`, whose doc comment
names `repairFailure.ts` **by name** as the case it exists for) requires the binding assigned
from the literal to have a reference that is a `t()` argument. Quick `260901-ud5`'s Bucket E
rewrote line 145 from `t('box.repair.error', message)` to
`t('box.repair.error', 'Repair failed. See the log for details.')` — the i18next-parser lexer
resolves a default only from a **literal** second argument — which removed the only reference
of `message` that reached `t()`. The idiom's meaning is untouched; only its syntax moved.

**genI18nGateScope A-17 (1 failure).** The committed `meta/i18nForkTouchedFiles.json` (204) is
stale against the live `git diff <baseCommit> HEAD -- src/frontend` by exactly 3 paths, all
from this morning's commit `24cdae047`.

Output: two separate commits.
</objective>

<evidence>
Measured, not assumed:

- `scanScope()` baseline today: **exactly 1** violation, `repairFailure.ts:135`.
- Audit mode `scanScope({ extraFiles: [the 3 A-17 paths] })`:
  `useOpenDialog.ts` → **0** violations; `CustomWineProton.tsx` → **0**;
  `Tools/index.tsx` → **2** (`"Winecfg"` :89, `"Winetricks"` :96 — upstream tool names,
  arguably do-not-translate glossary terms).
- `meta/hardcodedStringGate.ts` has **no** `require.main`/`import.meta` self-exec guard, so
  importing it for the audit cannot rewrite an artifact. Confirmed both JSON artifacts were
  byte-identical after the audit run.
- Scope 161 / forkTouched 204; none of the 3 new paths is in scope, so
  unscanned = 43 → 46.
</evidence>

<execution_context>
@CLAUDE.md
</execution_context>

<task_1>
**Restore the Pattern 3 exemption for the post-Bucket-E syntax.**

files: `meta/hardcodedStringGate.ts`

action: extend `isAssignedThenPassedToT` with ONE additional, narrow recognition — a reference
qualifies when it is the assignment target (LHS of a binary `=`) of a t-alias call whose
default-text (2nd) argument is a string literal whose text **equals the flagged literal's
text**. That is exactly "this literal is the declared English fallback for this very translated
string" — the same claim Pattern 3 already makes. Match the file's helper style
(`isTCallArgument` :915, `isConsoleArgument` :299, `findAssignedBindingNameNode` :1098) and
document it in the Pattern 3 doc block in the same voice, citing `260901-ud5` Bucket E.

verify:
- `scanScope()` total violations **1 → 0**, and no other file's violation set changes
- **non-vacuity (mandatory):** mutate the `t()` default's literal text so it no longer matches
  the initialiser → the violation must RETURN; then restore and re-confirm 0
- `hardcodedStringGate.test.ts` fully green

done: both failing assertions pass because the exemption is correct again, with no allowlist
entry and no change to `repairFailure.ts`.
</task_1>

<task_2>
**Re-baseline the A-17 fork-touched artifact and every pin that guards it — in one change.**

files: `meta/i18nForkTouchedFiles.json`, `meta/__tests__/genI18nGateScope.test.ts`

action:
1. Add the 3 paths to `i18nForkTouchedFiles.json` in sorted position, 204 → 207.
   Leave `baseCommit`/`baseVersion` untouched.
2. Re-baseline every literal count pin 204 → 207: A0's two assertions, A2's title and body,
   A3, A4, and the `161 -> 204` phrasing in titles and in the file's doc comment near :124.
3. Add all 3 paths to `DECLARED_UNSCANNED_DEBT` (43 → 46) so the A-03 ratchet still holds.
4. Record the audit measurement in the doc comment above `DECLARED_UNSCANNED_DEBT`: two of the
   three files measure **zero** violations and are genuine scope candidates rather than debt;
   only `Tools/index.tsx` has real hits. The register must not silently misrepresent two clean
   files as unpaid work. Note the scope widening as a deliberate follow-up for the gate's owner.

verify: whole Meta project green; A-03 unscanned set is exactly the declared 46.

done: A-17 equals the live derivation, and no pin was left behind.
</task_2>

<constraints>
- **Do NOT run `pnpm gen-i18n-gate-scope` or `pnpm gen-i18n-scope:rewrite`.** A regen is
  measured to take this suite from 1 failure to 5. Hand-edit surgically, as `260901-w9e` did
  for the reverse (shrink) case.
- **Leave `meta/i18nGateScope.json` byte-identical at 161.** Do not widen the blocking gate,
  and do not disturb its hand-curated provenance marker (the A5 ratchet asserts it).
- Do NOT add an `i18nGateAllowlist.json` entry — that file is a deferral register for real
  debt, and parking a false positive there misrepresents it as unpaid debt forever.
- Do NOT edit `repairFailure.ts` and do NOT revert Bucket E.
- **Never `git checkout -- <file>`** — it fires `.husky/post-checkout`, which triggers a
  binary download that throws. Restore via `git show HEAD:<path> > <path>` or a `cp` backup
  taken first.
- **Judge by test delta across the whole Meta project**, not by the target assertions. The
  recorded failure mode here is trading 1 failure for 5, which looks like success if you only
  re-run the test you set out to fix.
- Commit Task 1 and Task 2 separately.
</constraints>
