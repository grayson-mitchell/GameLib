---
phase: quick-260816-py8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-REVIEW.md
  - .planning/STATE.md
autonomous: true
requirements: [QT-260816-py8]

must_haves:
  truths:
    - "A reader opening 34.15-REVIEW.md sees findings counts that match reality: 0 critical open, 3 warnings open"
    - "A reader opening 34.15-REVIEW.md sees an explicit, dated disposition on WR-02, WR-03 and WR-04 rather than a bare Fix paragraph"
    - "A reader of WR-02 understands that CR-01's fix did NOT close it, and knows the todo file it now lives in"
    - "A reader of WR-03 and WR-04 knows no dedicated todo file exists for either and where they are cross-referenced"
    - "The STATE.md metrics comment agrees with the completed_phases value it annotates"
    - "STATE.md is otherwise byte-identical to its pre-task state"
  artifacts:
    - path: ".planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-REVIEW.md"
      provides: "Corrected findings counts + three dated Disposition blocks"
      contains: "**Disposition (2026-08-16, phase close)"
    - path: ".planning/STATE.md"
      provides: "Metrics comment that records the true 19 -> 20 sequence"
      contains: "completed_phases 19 -> 20"
  key_links:
    - from: ".planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-REVIEW.md"
      to: ".planning/todos/pending/2026-08-16-steam-platform-signal-two-writers-no-precedence-rule.md"
      via: "WR-02 Disposition block naming the todo path"
      pattern: "2026-08-16-steam-platform-signal-two-writers-no-precedence-rule\\.md"
---

<objective>
Correct three stale bookkeeping artifacts so Phase 34.15 stops reading as unfinished. The phase IS
complete (ROADMAP COMPLETE, VERIFICATION `status: passed` 16/16, UAT `status: passed` 4/2/0,
SECURITY `status: verified` threats_open 0, STATE `completed_phases: 20`) — only the paperwork lags.

Purpose: a future reader (human or agent) currently gets three false signals — a review claiming an
open BLOCKER that shipped a fix, three warnings that look like unnoticed oversights rather than
deliberate deferrals, and a STATE.md comment that flatly contradicts the number directly beneath it.

Output: two documentation files edited. No source behaviour changes.

**DOCUMENTATION ONLY.** No file under `src/`, `meta/` or `public/` may be touched. No test changes.
`.planning/ROADMAP.md` must NOT be modified — its 34.15 entry already says COMPLETE and already
records WR-02/03/04 as deliberately left open.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-REVIEW.md
@.planning/todos/pending/2026-08-16-steam-platform-signal-two-writers-no-precedence-rule.md

**DO NOT read `.planning/STATE.md` in full.** It is 855KB / 6958 lines and has a documented history
of corruption. Read ONLY lines 345-360 with `offset: 345, limit: 16` if you need to see the region.
</context>

<hard_prohibitions>
These are absolute. Violating any one of them is a task failure, not a style issue.

1. **NEVER run `git stash`.** Two data-loss events in this project came from exactly that command.
   If the working tree is dirty with changes you do not recognise, **STOP and report** — do not
   clean up, do not stash, do not checkout, do not reset.
2. **NEVER run `sed -i`, `perl -i`, `awk` redirects, `cat > file`, heredocs, or any whole-file
   rewrite against `.planning/STATE.md`.** The only permitted mutation is a single targeted `Edit`
   tool call whose `old_string` is the exact 3-line comment block named in SPEC-D.
3. **NEVER invoke any `gsd-sdk` `state.*` or `roadmap.*` verb.** Every one of them is known to
   corrupt STATE.md. Hand-apply only.
4. **NEVER modify `.planning/ROADMAP.md`.**
5. **NEVER `git add -A` / `git add .` / `git commit -a`.** Every `git add` must name explicit paths.
6. **NEVER commit `.planning/STATE.md`, any `*-SUMMARY.md`, or any `*-PLAN.md`.** The orchestrator
   owns that commit. Task 2 leaves STATE.md modified-but-uncommitted on purpose.
7. **Do not change any `status:` field to a value that overstates completion.** `34.15-REVIEW.md`
   keeps `status: issues_found` — three warnings are genuinely open, and flipping it would be false.
</hard_prohibitions>

<count_interpretation_decision>
**Decision (made at planning time, must be implemented as stated): the `findings:` counts in
`34.15-REVIEW.md` frontmatter mean OPEN findings, not findings-ever-found.**

Rationale: the correction mandates `critical: 0` and `warning: 3`. Those are unambiguously OPEN
counts — 1 critical and 4 warnings were *found*. For `total` to mean anything other than OPEN would
make the four keys mutually inconsistent (`0 + 3 + 0 != 5`). Therefore **`total` becomes 3**, and the
historical found-counts (1 / 4 / 0 / 5) are preserved in a YAML comment on the block so the record is
not erased and the meaning cannot go stale again.

No tooling consumes this frontmatter — verified: no schema files exist under
`$HOME/.claude/get-shit-done/schemas/` and no `get-shit-done` source reads a `findings:` key. YAML
comments are therefore safe to add.
</count_interpretation_decision>

<content_specs>
The literal text to write. Reproduce **verbatim**, including blank lines, backticks and em-dashes.
Do not paraphrase, do not re-wrap, do not "improve" the wording.

---

**SPEC-A** — replacement for the `findings:` block in `34.15-REVIEW.md` frontmatter (currently lines
43-48). Anchor the `Edit` on the exact existing six lines from `findings:` through
`status: issues_found` inclusive; `findings:` occurs exactly once in the file.

    findings:
      # These counts are OPEN findings, NOT findings-ever-found. Corrected 2026-08-16 at phase close.
      # Originally FOUND: critical 1, warning 4, info 0, total 5. CR-01 (the critical) was fixed in
      # 77f094bfd and WR-01 in dabd1ccc4 -- both carry **Resolution** blocks in the body below.
      # WR-02 / WR-03 / WR-04 remain OPEN BY DECISION (see their **Disposition** blocks), which is
      # why status stays issues_found rather than flipping to a clean status.
      critical: 0   # open; 1 found, CR-01 fixed in 77f094bfd
      warning: 3    # open (WR-02, WR-03, WR-04); 4 found, WR-01 fixed in dabd1ccc4
      info: 0
      total: 3      # open; 5 found
    status: issues_found

---

**SPEC-B1** — WR-02 disposition. Insert as a new paragraph AFTER the WR-02 `**Fix:**` paragraph
(which ends `...direction the team decides is more authoritative.`) and BEFORE the `### WR-03:`
heading. Separate it from both neighbours with one blank line.

    **Disposition (2026-08-16, phase close): DEFERRED — left open BY DECISION, not fixed.** Judged out
    of Phase 34.15's scope and explicitly assessed by `34.15-VERIFICATION.md` as **non-blocking** for
    that phase's goal: it does not violate the three-valued platform contract (`undefined` = never
    captured, `false` = confirmed absent, `true` = present) and it does not reopen either of the two
    ROADMAP defects 34.15 closed. The phase verified `passed` 16/16 with this open. It is now tracked
    as `.planning/todos/pending/2026-08-16-steam-platform-signal-two-writers-no-precedence-rule.md`,
    which additionally records live D-16 UAT-gate evidence that this review did not have: on a real
    378-game library the two writers were observed to be **complementary rather than conflicting**
    (bulk PICS resolved 363 of 378; 14 of the 15 it skipped were then filled by the `appdetails`
    path), and `appdetails` is being **403-throttled in bulk**, which suppresses how often that writer
    runs at all — both of which currently *mask* the conflict rather than removing it.

    **CR-01's fix did NOT close this finding.** CR-01 was the *symptom* — a stale seed reaching the
    `SteamDialog` glyph row — and was fixed downstream in `77f094bfd`. The two-writer disagreement
    described here is the *cause*, and it is still present in `mergePlatformCapture` and `games.ts`.

---

**SPEC-B2** — WR-03 disposition. Insert as a new paragraph AFTER the WR-03 `**Fix:**` paragraph
(which ends `...instead of a silent/crashing rejection.`) and BEFORE the `### WR-04:` heading.
Separate it from both neighbours with one blank line.

    **Disposition (2026-08-16, phase close): ACCEPTED as a known gap — left open BY DECISION, not
    fixed.** Plan 09 closed the synchronous-throw half at the `captureOwnedAppPlatforms` call site
    (`9d03d2cb2`); the shape this finding actually names — the "all four exit paths emit
    `steamSyncStatus`" guarantee resting on an unenforced cross-module never-throws *convention*
    rather than a structural `try`/`catch` — remains as described. It was judged non-blocking because
    the contract genuinely holds and is well-tested for the code as it stands; what is deferred is
    making it structurally guaranteed rather than true-by-current-implementation. **No dedicated todo
    file exists for this finding.** It is recorded here, and cross-referenced under `## Related, also
    open from the same review` in
    `.planning/todos/pending/2026-08-16-steam-platform-signal-two-writers-no-precedence-rule.md`.

---

**SPEC-B3** — WR-04 disposition. Insert as a new paragraph AFTER the WR-04 `**Fix:**` paragraph
(which ends ``...regardless of `steamLibraryCount`.``) and BEFORE the `---` horizontal rule that
precedes the `_Reviewed: ..._` footer. Separate it from both neighbours with one blank line. **The
`_Reviewed:` / `_Reviewer:` / `_Depth:` footer must remain the last three lines of the file.**

    **Disposition (2026-08-16, phase close): ACCEPTED as a known gap — left open BY DECISION, not
    fixed.** The D-16 UAT gate measured the un-fed-back retry window at **~36 seconds** against an
    unreachable Steam CM, which makes this materially more user-visible than this finding assumed. It
    was still judged non-blocking for 34.15's goal — the failure notice renders and its
    `runner=steam`-scoped retry works; only the in-flight feedback between click and the next terminal
    `steamSyncStatus` emission is missing. **No dedicated todo file exists for this finding.** It is
    recorded here, and cross-referenced under `## Related, also open from the same review` in
    `.planning/todos/pending/2026-08-16-steam-platform-signal-two-writers-no-precedence-rule.md`.

---

**SPEC-D** — replacement for the STATE.md comment. The `old_string` for the `Edit` call is exactly
these three lines (each with its leading two spaces), which occur exactly once in the file
(confirmed: `grep -n 'completed_phases 19 UNCHANGED' .planning/STATE.md` -> line 350 only):

      # completed_phases 19 UNCHANGED: Phase 34.15 is NOT complete. Its D-16 BLOCKING human UAT gate
      # (34.15-UAT.md, status: human_needed) is undischarged, so phase verification was deliberately
      # NOT run and the phase was NOT closed. Do not let 9/9 plans done read as phase complete.

The `new_string` is exactly these eight lines (leading two spaces, `#` prefix, wrapped to match the
~98-column width of the neighbouring comment lines in this block):

      # completed_phases 19 -> 20: when this block was first written the value was HELD at 19 because
      # Phase 34.15's D-16 BLOCKING human UAT gate (34.15-UAT.md) was still status: human_needed, so
      # phase verification was deliberately NOT run and the phase was NOT closed -- 9/9 plans done
      # must never read as phase complete on its own. That gate was subsequently DISCHARGED on
      # 2026-08-16: 34.15-UAT.md went status: passed (4 pass / 2 n-a / 0 fail, both runtimes) and
      # 34.15-VERIFICATION.md went status: passed 16/16, so the phase closed and the value moved
      # 19 -> 20, which is what is recorded below. Comment corrected 2026-08-16 (quick task
      # 260816-py8) -- as written it contradicted the value it annotates.
</content_specs>

<tasks>

<task type="auto">
  <name>Task 1: Correct 34.15-REVIEW.md counts and record the WR-02/03/04 dispositions</name>
  <files>.planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-REVIEW.md</files>
  <action>
Covers corrections (1) and (2). Both land in one file and one commit so the review is never committed
in a half-corrected state.

Before touching anything, run `git status --porcelain` from the repo root. It must be empty. If it is
not, and you do not recognise every listed change as your own, **STOP and report** — do not stash, do
not clean, do not proceed.

Make four `Edit` calls against
`.planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-REVIEW.md`:

1. Replace the frontmatter `findings:` block (six lines, `findings:` through `status: issues_found`)
   with SPEC-A verbatim. `status: issues_found` is deliberately unchanged and is present in both the
   old and new string purely as an anchor — do not alter it.
2. Insert SPEC-B1 verbatim after WR-02's `**Fix:**` paragraph, before the `### WR-03:` heading.
3. Insert SPEC-B2 verbatim after WR-03's `**Fix:**` paragraph, before the `### WR-04:` heading.
4. Insert SPEC-B3 verbatim after WR-04's `**Fix:**` paragraph, before the `---` rule that precedes
   the `_Reviewed: ..._` footer.

Anchor each insertion `Edit` on the trailing sentence of the preceding `**Fix:**` paragraph plus the
following heading (or rule), so the anchor is unambiguous. The three sentence tails are, in order:
`direction the team decides is more authoritative.` / `instead of a silent/crashing rejection.` /
``regardless of `steamLibraryCount`.``

Do not renumber, re-order, re-word or delete any existing finding. Do not touch CR-01's or WR-01's
existing `**Resolution**` blocks — the new blocks are labelled `**Disposition**` specifically so a
reader can tell a deferral from a fix at a glance.

Then stage and commit **this file only**:
`git add .planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-REVIEW.md`
followed by a commit. Never `git add -A`. Suggested message:
`docs(34.15): record actual disposition of review findings -- CR-01/WR-01 fixed, WR-02/03/04 deferred`
  </action>
  <verify>
    <automated>
cd /Users/graysonmitchell/Projects/GameLib || exit 1
R=".planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-REVIEW.md"
FAIL=0
echo "== new counts present (expect 1 1 1 1) =="
grep -c '^  critical: 0 ' "$R"; grep -c '^  warning: 3 ' "$R"
grep -c '^  total: 3 ' "$R"; grep -c '^status: issues_found$' "$R"
echo "== stale counts ABSENT (expect 0) =="
grep -cE '^  (critical: 1|warning: 4|total: 5)$' "$R" || true
echo "== disposition blocks (expect 3) =="
grep -c '^\*\*Disposition (2026-08-16, phase close)' "$R"
echo "== existing Resolution blocks untouched (expect 2) =="
grep -c '^\*\*Resolution (2026-08-16, gap-closure round' "$R"
echo "== WR-02 states CR-01 did not close it (expect 1) =="
grep -c "CR-01's fix did NOT close this finding" "$R"
echo "== WR-02 names the todo (expect 1) =="
grep -c 'todos/pending/2026-08-16-steam-platform-signal-two-writers-no-precedence-rule.md' "$R"
echo "== WR-03+WR-04 state no dedicated todo (expect 2) =="
grep -c 'No dedicated todo file exists for this finding' "$R"
echo "== footer still last 3 lines (expect _Depth: standard_) =="
tail -n 1 "$R"
echo "== commit touched exactly 1 file, and it is the review =="
git show --stat --format= HEAD
echo "== nothing under src/ meta/ public/ or ROADMAP in the commit (expect 0) =="
git show --name-only --format= HEAD | grep -cE '^(src|meta|public)/|ROADMAP\.md$' || true
    </automated>
  </verify>
  <done>
`findings:` reads `critical: 0` / `warning: 3` / `info: 0` / `total: 3` with the explanatory YAML
comment block; `status: issues_found` unchanged; the two stale-count greps return 0; exactly three
`**Disposition (2026-08-16, phase close)` blocks exist; the two pre-existing `**Resolution**` blocks
still exist; WR-02 explicitly denies that CR-01's fix closed it and names the todo path; WR-03 and
WR-04 each say no dedicated todo exists; the `_Depth: standard_` footer is still the final line; the
commit contains exactly one file and it is the review, with zero matches under `src/`, `meta/`,
`public/` or `ROADMAP.md`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace the contradictory STATE.md metrics comment (edit only, DO NOT commit)</name>
  <files>.planning/STATE.md</files>
  <action>
Covers correction (3). `.planning/STATE.md` is 855KB / 6958 lines and has a documented history of
corruption from whole-file rewrites and from `gsd-sdk state.*` verbs. Treat it as hazardous.

**Step 1 — snapshot before any mutation:**
`cp .planning/STATE.md "${TMPDIR:-/tmp}/STATE.md.260816-py8.bak"` and confirm the copy exists with a
matching `wc -l` (6958).

**Step 2 — read only the region.** If you need to see the text, use the `Read` tool with
`offset: 345, limit: 16`. Do not read the whole file.

**Step 3 — one targeted `Edit` call.** `old_string` = the exact three comment lines from SPEC-D
(including their leading two spaces). `new_string` = the exact eight comment lines from SPEC-D.
Nothing else. No `sed -i`, no `perl -i`, no heredoc, no rewrite, no `gsd-sdk` `state.*`/`roadmap.*`
verb. The `total_phases: 26` / `completed_phases: 20` / `total_plans: 344` / `completed_plans: 339` /
`percent: 98` values immediately below the comment are already correct — do not touch them.

**Step 4 — audit the diff.** Run `git diff --numstat -- .planning/STATE.md` and
`git diff -U0 -- .planning/STATE.md`. The diff must be a **single hunk** removing exactly 3 lines and
adding exactly 8, all of them comment lines starting with `  #`. If the diff shows more than one
hunk, or touches any non-comment line, or any other file changed, **restore immediately** with
`cp "${TMPDIR:-/tmp}/STATE.md.260816-py8.bak" .planning/STATE.md` and report the failure. Never use
`git checkout`, `git restore`, `git reset` or `git stash` to recover — restore from the snapshot.

**Step 5 — do NOT commit.** Leave `.planning/STATE.md` modified in the working tree. The orchestrator
owns the docs commit. Report in the SUMMARY that STATE.md is intentionally left uncommitted.
  </action>
  <verify>
    <automated>
cd /Users/graysonmitchell/Projects/GameLib || exit 1
S=".planning/STATE.md"
echo "== stale text GONE (expect 0 and 0) =="
grep -c 'completed_phases 19 UNCHANGED' "$S" || true
grep -c 'Do not let 9/9 plans done read as phase complete' "$S" || true
echo "== new text PRESENT (expect 1 and 1) =="
grep -c 'completed_phases 19 -> 20: when this block was first written' "$S"
grep -c 'Comment corrected 2026-08-16 (quick task' "$S"
echo "== annotated value still 20 (expect 1) =="
grep -c '^  completed_phases: 20$' "$S"
echo "== sibling metrics untouched (expect 1 each) =="
grep -c '^  total_phases: 26$' "$S"; grep -c '^  total_plans: 344$' "$S"
grep -c '^  completed_plans: 339$' "$S"; grep -c '^  percent: 98$' "$S"
echo "== EXACTLY ONE diff hunk (expect 1) =="
git diff -U0 -- "$S" | grep -c '^@@'
echo "== diff is -3/+8 on STATE.md only (expect: 8<TAB>3<TAB>.planning/STATE.md) =="
git diff --numstat -- "$S"
echo "== every added/removed line is a comment (expect 0 non-comment) =="
git diff -U0 -- "$S" | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -cv '^[+-]  #' || true
echo "== line count 6958 -> 6963 =="
wc -l < "$S"
echo "== STATE.md is dirty and UNCOMMITTED (expect a line starting ' M') =="
git status --porcelain -- "$S"
echo "== no source/meta/public/ROADMAP file is dirty (expect 0) =="
git status --porcelain | grep -cE ' (src|meta|public)/| ROADMAP\.md$' || true
echo "== snapshot exists =="
ls -l "${TMPDIR:-/tmp}/STATE.md.260816-py8.bak"
    </automated>
  </verify>
  <done>
Both stale-text greps return 0; both new-text greps return 1; `completed_phases: 20` and the four
sibling metric lines are intact; `git diff -U0` shows exactly one hunk; `git diff --numstat` shows
`8	3	.planning/STATE.md`; zero added-or-removed lines fail the `  #` comment prefix test; the file
is 6963 lines; `git status --porcelain` shows STATE.md as modified-but-uncommitted; no file under
`src/`, `meta/`, `public/` or `ROADMAP.md` is dirty; the snapshot file exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> `.planning/STATE.md` | An 855KB append-only ledger with a documented corruption history; a bad write destroys project bookkeeping that is not reconstructible from source |
| agent -> git working tree | A concurrent session's uncommitted work can be destroyed by cleanup commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-py8-01 | Tampering | `.planning/STATE.md` | mitigate | `cp` snapshot before edit; single targeted `Edit` on an exact 3-line anchor; `git diff --numstat` + single-hunk + comment-prefix assertions after; restore from snapshot on any deviation. `sed -i`, heredocs, whole-file rewrites and all `gsd-sdk state.*`/`roadmap.*` verbs are prohibited outright |
| T-py8-02 | Destruction | git working tree | mitigate | `git stash` is prohibited outright (two prior data-loss events). Dirty-tree precondition check in Task 1 halts and reports instead of cleaning. Recovery is via the `cp` snapshot, never `checkout`/`restore`/`reset` |
| T-py8-03 | Repudiation | `34.15-REVIEW.md` counts | mitigate | Historical found-counts (1/4/0/5) preserved in a YAML comment rather than erased, so the correction does not destroy the record of what the review originally found |
| T-py8-04 | Tampering | source tree | mitigate | Path-scoped `git add` only (`git add -A` prohibited); post-commit `git show --name-only` asserts zero matches under `src/`, `meta/`, `public/` or `ROADMAP.md` |
| T-py8-SC | Tampering | package installs | accept | No package-manager install occurs in this plan — documentation edits only |
</threat_model>

<verification>
Run from the repo root after both tasks:

1. `git log --oneline -1` -> exactly one new commit, touching only `34.15-REVIEW.md`.
2. `git status --porcelain` -> shows `.planning/STATE.md` modified (plus the SUMMARY/PLAN files),
   and **nothing** under `src/`, `meta/`, `public/`, and **not** `.planning/ROADMAP.md`.
3. `git diff -- .planning/ROADMAP.md` -> empty.
4. `grep -c '^\*\*Disposition (2026-08-16, phase close)' .planning/phases/34.15-*/34.15-REVIEW.md`
   -> `3`.
5. `grep -c 'completed_phases 19 UNCHANGED' .planning/STATE.md` -> `0`.
6. `grep -c '^  completed_phases: 20$' .planning/STATE.md` -> `1`.
</verification>

<success_criteria>
- `34.15-REVIEW.md` frontmatter reads `critical: 0`, `warning: 3`, `info: 0`, `total: 3`, with the
  YAML comment block declaring these are OPEN counts and preserving the original found-counts.
- `status: issues_found` is unchanged in `34.15-REVIEW.md`.
- WR-02, WR-03 and WR-04 each end with a dated `**Disposition (2026-08-16, phase close)` block
  distinguishable at a glance from CR-01's and WR-01's `**Resolution**` blocks.
- WR-02's block names its todo file and explicitly states that CR-01's fix did not close it.
- WR-03's and WR-04's blocks each state that no dedicated todo file exists and point at the
  cross-reference inside the WR-02 todo. No todo filename is invented for either.
- The `_Reviewed:` / `_Reviewer:` / `_Depth:` footer remains the last three lines of the review.
- `.planning/STATE.md`'s metrics comment records the true 19 -> 20 sequence and no longer contradicts
  `completed_phases: 20`; the diff is a single hunk of comment-only lines (-3/+8) and nothing else in
  that 6958-line file moved.
- `.planning/ROADMAP.md` is untouched. No file under `src/`, `meta/` or `public/` is touched.
- `git stash` was never run.
</success_criteria>

<output>
Create `.planning/quick/260816-py8-phase-34-15-bookkeeping-corrections/260816-py8-SUMMARY.md` when
done. Record explicitly: (a) the `total: 5 -> 3` interpretation decision and why, (b) that STATE.md
is intentionally left modified-but-uncommitted for the orchestrator, (c) the `git diff --numstat`
output for STATE.md as evidence the blast radius was 3 lines removed / 8 added.
</output>
