---
quick_id: 260902-9el
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md
  - .planning/todos/pending/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md
  - .planning/todos/completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md
  - .planning/todos/completed/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md
  - .planning/quick/260902-9el-close-two-stale-epic-logout-todos-on-pha/260902-9el-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Neither Epic-logout todo remains in .planning/todos/pending/"
    - "Both todos carry a dated 2026-09-02 closure section citing b5b3464bd, bea07cd17, REQ-35-07 and REQUIREMENTS.md:429/:1143"
    - "Todo 1's frontmatter no longer asserts the stale 34.6-prerequisite blocked_by"
    - "Both closure sections name Phase 38 as the owner of off-macOS Epic logout"
    - "Both closure sections state that D-35-19-15 SURVIVES OPEN and does not re-block these todos"
    - "The commit touches only the four todo paths and the quick-task directory — not ROADMAP.md, not STATE.md"
  artifacts:
    - path: ".planning/todos/completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md"
      provides: "Closed todo 1 with corrected blocked_by and dated closure record"
    - path: ".planning/todos/completed/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md"
      provides: "Closed todo 2 with dated closure record and answered open question"
    - path: ".planning/quick/260902-9el-close-two-stale-epic-logout-todos-on-pha/260902-9el-SUMMARY.md"
      provides: "Quick-task summary"
  key_links:
    - from: "completed/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md"
      to: "commit b5b3464bd"
      via: "root-cause citation"
      pattern: "b5b3464bd"
    - from: "completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md"
      to: "todo 2's closure"
      via: "successor-defect resolution citation"
      pattern: "2026-08-24-epic-logout-reports-clearing"
---

<objective>
Close two stale pending todos about Epic logout's cookie clear, on evidence that ALREADY EXISTS.

Purpose: Todo 1 (`2026-08-23`) stayed pending for exactly one stated reason — its successor defect
was unresolved. Todo 2 (`2026-08-24`) IS that successor, and it is fixed and live-verified on a
genuine release artifact. Both are now records-rot: open items asserting conditions that no longer
hold, one of them carrying a false `blocked_by`.

Output: Two todo files moved to `.planning/todos/completed/`, each with a dated closure section
citing the specific evidence, plus a quick-task SUMMARY and one scoped commit.

**THIS IS A RECORDS TASK. NO CODE CHANGES OF ANY KIND.** Nothing under `src/`, `src-tauri/` or
`meta/` may be read-for-modification, edited, created or deleted. No re-investigation, no
re-measurement, no live gestures, no builds, no test runs. Every fact needed is written into this
plan below and has been verified against the repo during planning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md
@.planning/todos/pending/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md
</context>

<concurrent_session_hazard>
**A CONCURRENT SESSION IS WRITING TO THIS REPO RIGHT NOW.** At planning time:

- `.planning/ROADMAP.md` has ~96 uncommitted lines adding Phase 40. **DO NOT EDIT `.planning/ROADMAP.md`. DO NOT COMMIT IT.** Reading it is fine.
- `.planning/STATE.md` has 2 uncommitted lines (the concurrent Phase 40 work plus quick task `260902-8wc`). You MAY append one row to its Quick Tasks Completed table, but **you MUST LEAVE `.planning/STATE.md` UNCOMMITTED** — committing it would absorb the concurrent session's in-flight Phase 40 line and split that record across two authors' commits.
- `.planning/quick/260902-8wc-.../260902-8wc-SUMMARY.md` is untracked and belongs to a prior task. Leave it alone.

**BANNED COMMANDS — do not run any of these under any circumstance:**

| Banned | Why |
|---|---|
| `git stash` / `git stash pop` | Strands the concurrent session's uncommitted work |
| `git reset` (any form) | Same |
| `git checkout -- <file>` | Fires this repo's `post-checkout` hook, which triggers a helper-binary download. If a file must be restored, use `cp` from a copy you made first. |
| `git add -A` / `git add .` / `git commit -a` | Absorbs the concurrent session's files |
| `gsd-sdk query commit` | Recorded to stage the entire tree; it would absorb ROADMAP.md |

Stage only explicitly-named paths. `git mv` is permitted (it stages only the two paths it moves).
</concurrent_session_hazard>

<evidence_base>
Every fact below was verified against the repo during planning. **Cite these; do not re-derive them.**

**REQ-35-07** — *"Logging out clears the embedded browser's persisted state — cookies, localStorage,
IndexedDB and disk cache — and the app does not report success unless a post-clear read confirms it."*

- Marked **Complete** 2026-09-01 by quick task `260901-vuy`, at `REQUIREMENTS.md:429` (status table)
  and `REQUIREMENTS.md:1143` (body, checkbox now `[x]`).
- **BOTH clauses live-proven on a GENUINE RELEASE ARTIFACT** at 22:52–22:54 on 2026-08-31 — Phase 35's
  SIXTH adjudication pass, the first verified against a release build rather than a debug-packaged one
  (`F-5-01`: `tauri build --debug` structurally never executes the SEA sidecar, so the earlier
  identity check had interrogated an inert binary; the chain was closed at the RUNNING PROCESS —
  PID 9781 `gamelib-shell` → PID 9787 **bundled** `gamelib-sidecar`, both sha256-identical to that
  build's outputs).
- Measured result: `epicgames.com before(total=31, matched=8, verdict=SUPPORTED_NONEMPTY) ->
  after(total=23, matched=0)`, `post-clear verification — 0 Epic-owned cookie(s) remain across 5
  domain(s)`, corroborated by an independently-written index-walking jar parse whose BEFORE/AFTER
  record-level set difference is EMPTY IN BOTH DIRECTIONS (23 baseline + 8 login = 31; 31 − 8 = 23).

**Root cause was NOT "the delete lies."** Todo 2 filed this under `[[wry-cookie-delete-lies-about-deleting]]`.
That diagnosis is refuted. `clearEpicCookies` opened a hidden webview on Epic's LIVE login page purely
to obtain a window handle the macOS path never used. Building a WKWebView on an `https` URL IS a
navigation, and Epic/Cloudflare answered it by re-minting `__cf_bm`, `EPIC_DEVICE`, `EPIC_LOGIN_ID`,
`_epicSID` and `_tald` — concurrently with, and for ~1–2s after, the clear loop. The census's
`matched=0` was ACCURATE about an instant that had stopped being true before anyone read it.

- **Fixed by `b5b3464bd`** — *"fix(35): stop the Epic logout re-creating the cookies it deletes."*
  macOS now opens NO window for the cookie step; the deliberately-unresolvable label IS the Rust
  fallback's precondition, so the same Rust code runs with the page load gone.
- **Second clause** (the app reporting success without a confirming read) was a fail-open in the
  post-clear census, closed by **`bea07cd17`** — *"fix(35): close the fail-open post-clear
  verification sweep"* — RED-proved with four mutations against the real file.
- **Todo 2's own open question is ANSWERED by the same root cause.** Its "Not yet determined" section
  asks why the app counted 8 while the jar held 7 in-scope. Answer: the app's delta was measured
  across a window in which cookies were being re-minted. It was not an in-memory-vs-disk divergence.

**Todo 1's discharge condition was SATISFIED on 2026-08-25** by plan `34.6-14` — its own
`## Disposition (2026-08-25, plan 34.6-14)` section (lines 90–115 of the pending file) records the
authenticated session, the UI-driven logout and the paired jar census. It stayed pending for one
stated reason only: *"the confirmed defect is now owned by a separate, newer todo … Closing this todo
would misrepresent an unresolved defect as a resolved concern."* **That reason is now gone.**

**Todo 1's `blocked_by` is STALE (line 7).** It currently reads:
`"nothing external — Phase 34.6 must OBTAIN an authenticated Epic session as part of its live gate.
This is a prerequisite of the gate, not a blocker on it."` — a FUTURE obligation of a phase that has
since closed, and the same file records that obligation being discharged eight days later. This is
the **sixth recurrence** of the `blocked_by`-rot shape in this repo (the file's own body already
calls itself the third). A closed todo carrying a false blocker is precisely the records-rot this
task exists to end, so the field must be CORRECTED, not left as-is and not silently deleted.
</evidence_base>

<carve_outs>
**Both closure sections MUST record these. Neither closure may read as total.**

1. **Off-macOS (Windows/Linux) Epic logout is NOT covered.** All evidence above is macOS-only.
   Already routed to **Phase 38** — ROADMAP's Phase 35 entry reads *"Routed out: … Windows/Linux and
   off-macOS Epic logout → Phase 38"*, ledgered as **`38-W06`** in `38-VERIFICATION.md`'s
   `human_verification` array (`audit-uat` verified moving 29→30 for phase 38, 54→55 total).
   `b5b3464bd` deliberately KEEPS a window off macOS (pointed at `https://gamelib.invalid/`) — which
   is exactly the leg nobody has exercised. `bea07cd17` additionally made an unreadable jar throw, so
   a rejecting read off-macOS surfaces as a user-visible sign-out error. **Name Phase 38 as owner.**

2. **`D-35-19-15`'s sibling-apex seeding remains UNEXERCISED and is unreproducible by construction** —
   `b5b3464bd` removed the hidden window that was the only thing ever seeding those four sibling
   apexes during a logout. **TWO independent adjudication passes (the fourth and the fifth) ruled it
   is `D-35-19-15`'s OWN sub-criterion and NOT a clause of REQ-35-07**, which is why REQ-35-07 could
   close without it; the sixth pass reaffirmed that a third time from the requirement's own text.
   The domain-*suffix* half IS exercised (one `epicgames.com` step sweeps `.epicgames.com`,
   `.www.epicgames.com`, `.ecosec.on.epicgames.com`); only the four sibling *apexes* are not.
   **`D-35-19-15` SURVIVES as an open item with no owning phase.** Do NOT describe it as closed, and
   do NOT let it re-block these todos.
</carve_outs>

<tasks>

<task type="auto">
  <name>Task 1: Close todo 1 — correct the stale blocked_by, append the closure record, move to completed/</name>
  <files>.planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md → .planning/todos/completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md</files>
  <action>
Move first, then edit in place at the new path:

`git mv .planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md .planning/todos/completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md`

**Frontmatter edits (top 8 lines):**
- `status: pending` → `status: CLOSED` (matches the dominant convention in `completed/`; `CLOSED` and `RESOLVED` both occur — use `CLOSED`).
- Add `closed: 2026-09-02` and `closed_by: "quick task 260902-9el"`.
- **Correct `blocked_by` (line 7)** — replace the stale future-obligation string with a discharged
  statement, e.g. `blocked_by: ""` plus a new field
  `blocked_by_history: "STALE AS WRITTEN 2026-08-23 — it named a future obligation of Phase 34.6, which closed; the obligation was discharged 2026-08-25 by plan 34.6-14 (see this file's own Disposition section). Corrected at closure, 2026-09-02, quick 260902-9el. Sixth recorded recurrence of the blocked_by-rot shape."`
  The OLD string must not survive anywhere in the frontmatter block. It MAY be quoted verbatim inside
  the closure prose as historical record — that is the point of recording the rot rather than erasing it.
- Leave `resolves_phase: "34.6"` intact and explain it in prose (see below) rather than rewriting it.

**Append a closure section at the end of the file** (do not rewrite or delete any existing section —
the 2026-08-25 Disposition section is the historical record and stays verbatim). Heading:
`## CLOSURE RECORD — 2026-09-02, quick task 260902-9el`

It must state, in prose, all of:
- The discharge condition was satisfied 2026-08-25 by plan `34.6-14` — cite this file's own
  `## Disposition (2026-08-25, plan 34.6-14)` section above.
- This todo stayed pending for ONE stated reason: its successor defect
  (`2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md`) was unresolved. That
  successor is now FIXED and live-verified, and is closed by this same quick task — so the reason
  is gone. Name the successor file by name.
- The evidence: REQ-35-07 Complete 2026-09-01 (quick `260901-vuy`) at `REQUIREMENTS.md:429` and
  `REQUIREMENTS.md:1143`; both clauses live-proven on a GENUINE RELEASE ARTIFACT at 22:52–22:54 on
  2026-08-31 (Phase 35's sixth adjudication pass, the first against a release build rather than a
  debug-packaged one); commits `b5b3464bd` and `bea07cd17`.
- Why `resolves_phase: "34.6"` did NOT close this and still does not: the file itself warned that
  phase closure is not a live observation. This closes on the OBSERVATION plus the successor's fix,
  not on 34.6 having closed. Say so explicitly so the record cannot later be read as an auto-close.
- The stale `blocked_by` and what it was corrected to, quoting the old value.
- **BOTH carve-outs from `<carve_outs>` above**, each in its own named sub-heading or bolded clause:
  Phase 38 owns off-macOS Epic logout (`38-W06`), and `D-35-19-15` SURVIVES OPEN with no owning phase.
  </action>
  <verify>
    <automated>NEW=.planning/todos/completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md; test ! -e .planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md && test -f "$NEW" && for t in 'CLOSURE RECORD' '260902-9el' 'b5b3464bd' 'bea07cd17' 'REQ-35-07' 'REQUIREMENTS.md:429' 'REQUIREMENTS.md:1143' '260901-vuy' '34.6-14' 'Phase 38' '38-W06' 'D-35-19-15' '2026-08-24-epic-logout-reports-clearing'; do grep -q -- "$t" "$NEW" || { echo "MISSING CITATION: $t"; exit 1; }; done; awk '/^---$/{n++; next} n==1' "$NEW" | grep -q 'nothing external' && { echo 'STALE blocked_by SURVIVES IN FRONTMATTER'; exit 1; }; awk '/^---$/{n++; next} n==1' "$NEW" | grep -q '^status: CLOSED' || { echo 'status not CLOSED'; exit 1; }; grep -q '^## Disposition (2026-08-25, plan 34.6-14)' "$NEW" || { echo 'historical Disposition section was destroyed'; exit 1; }; echo OK</automated>
  </verify>
  <done>Todo 1 lives in `completed/`, `status: CLOSED`, the stale `blocked_by` string is gone from the frontmatter block (the awk gate scopes the check to the frontmatter only, so quoting it in prose is allowed and expected), the 2026-08-25 Disposition section survives verbatim, and the closure section cites every required token including both carve-outs.</done>
</task>

<task type="auto">
  <name>Task 2: Close todo 2 — append the closure record answering its own open question, move to completed/</name>
  <files>.planning/todos/pending/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md → .planning/todos/completed/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md</files>
  <action>
`git mv .planning/todos/pending/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md .planning/todos/completed/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md`

**Frontmatter edits:**
- `status: OPEN` → `status: CLOSED`.
- Add `closed: 2026-09-02` and `closed_by: "quick task 260902-9el"`.
- Leave `title:` verbatim — it is the historical record of the problem as found on 2026-08-24, not a
  current claim. Say so in the closure section (the `completed/` folder already uses this convention,
  e.g. the 786MB bundle todo whose closure text explicitly states the title is kept verbatim).

**Append a closure section at the end** (existing sections stay verbatim). Heading:
`## CLOSURE RECORD — 2026-09-02, quick task 260902-9el`

It must state, in prose, all of:
- **The defect is FIXED and live-verified.** REQ-35-07 marked Complete 2026-09-01 by quick
  `260901-vuy` at `REQUIREMENTS.md:429` and `REQUIREMENTS.md:1143`; BOTH clauses proven on a GENUINE
  RELEASE ARTIFACT at 22:52–22:54 on 2026-08-31, Phase 35's sixth adjudication pass — the first
  verified against a release build rather than a debug-packaged one. Quote the measured result:
  `epicgames.com before(total=31, matched=8, verdict=SUPPORTED_NONEMPTY) -> after(total=23, matched=0)`
  and `post-clear verification — 0 Epic-owned cookie(s) remain across 5 domain(s)`.
- **A named correction of this todo's own diagnosis.** This file filed the defect under
  `[[wry-cookie-delete-lies-about-deleting]]`. That is REFUTED. The real cause: `clearEpicCookies`
  opened a hidden webview on Epic's LIVE login page purely to obtain a window handle macOS never
  used; building a WKWebView on an `https` URL IS a navigation, and Epic/Cloudflare answered it by
  re-minting `__cf_bm`, `EPIC_DEVICE`, `EPIC_LOGIN_ID`, `_epicSID` and `_tald` concurrently with and
  for ~1–2s after the clear loop. **The census's `matched=0` was ACCURATE about an instant that had
  stopped being true before anyone read it.** Fixed by `b5b3464bd` (macOS opens no window for the
  cookie step; the deliberately-unresolvable label IS the Rust fallback's precondition, so the same
  Rust code runs with the page load gone).
- **The second half** — the app reporting success without a confirming read — was a fail-open in the
  post-clear census, closed by `bea07cd17`, RED-proved with four mutations against the real file.
- **This todo's "Not yet determined" open question is ANSWERED**: the 8-reported-vs-7-on-disk mismatch
  has the same root cause — the app's delta was measured across a window in which cookies were being
  re-minted. Explicitly retire the in-memory-vs-disk hypothesis that section recorded.
- **BOTH carve-outs from `<carve_outs>` above**: Phase 38 owns off-macOS Epic logout (`38-W06`) —
  and note that `b5b3464bd` deliberately KEEPS a window off macOS pointed at `https://gamelib.invalid/`,
  the exact leg nobody has exercised, while `bea07cd17` made an unreadable jar throw, so a rejecting
  read off-macOS surfaces as a user-visible sign-out error. And `D-35-19-15` SURVIVES OPEN with no
  owning phase, unreproducible by construction, ruled by two independent adjudication passes to be
  its own sub-criterion and NOT a REQ-35-07 clause.
- A back-reference to the predecessor todo now closed alongside this one:
  `2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md`.
  </action>
  <verify>
    <automated>NEW=.planning/todos/completed/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md; test ! -e .planning/todos/pending/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md && test -f "$NEW" && for t in 'CLOSURE RECORD' '260902-9el' 'b5b3464bd' 'bea07cd17' 'REQ-35-07' 'REQUIREMENTS.md:429' 'REQUIREMENTS.md:1143' '260901-vuy' 'Phase 38' '38-W06' 'D-35-19-15' 'gamelib.invalid' '2026-08-23-epic-logout-cookie-clear'; do grep -q -- "$t" "$NEW" || { echo "MISSING CITATION: $t"; exit 1; }; done; awk '/^---$/{n++; next} n==1' "$NEW" | grep -q '^status: CLOSED' || { echo 'status not CLOSED'; exit 1; }; awk '/^---$/{n++; next} n==1' "$NEW" | grep -q '^status: OPEN' && { echo 'status OPEN survives'; exit 1; }; grep -q '^## Not yet determined' "$NEW" || { echo 'historical Not-yet-determined section was destroyed'; exit 1; }; echo OK</automated>
  </verify>
  <done>Todo 2 lives in `completed/`, `status: CLOSED`, its historical sections survive verbatim, and the closure section cites every required token, names the refuted `wry-cookie-delete-lies-about-deleting` diagnosis, answers the 8-vs-7 question, and records both carve-outs.</done>
</task>

<task type="auto">
  <name>Task 3: Write the SUMMARY, append the STATE row, and commit with an explicitly-scoped path list</name>
  <files>.planning/quick/260902-9el-close-two-stale-epic-logout-todos-on-pha/260902-9el-SUMMARY.md, .planning/STATE.md (edit only — NOT committed)</files>
  <action>
**Step 1 — SUMMARY.** Write `.planning/quick/260902-9el-close-two-stale-epic-logout-todos-on-pha/260902-9el-SUMMARY.md`,
mirroring the frontmatter shape of `.planning/quick/260902-8wc-close-the-stale-opendialog-long-running-/260902-8wc-SUMMARY.md`
(`quick_id`, `title`, `date: 2026-09-02`, `status: complete`, `commits:` list). Body must record:
what was closed and on what evidence; that this was a RECORDS-ONLY task with zero code changes; the
stale-`blocked_by` correction as the sixth recurrence of that shape; and BOTH carve-outs (Phase 38 /
`38-W06` owns off-macOS Epic logout; `D-35-19-15` survives OPEN with no owning phase). Do not let the
summary read as "Epic logout is fully closed" — it is closed **on macOS, on release-artifact evidence**.

**Step 2 — STATE.md.** Append ONE row to the `### Quick Tasks Completed` table (around line 6254),
matching the column count and formatting of the adjacent rows exactly — read the two rows above the
insertion point first and mirror them. **DO NOT COMMIT `.planning/STATE.md`.** It carries the
concurrent session's in-flight Phase 40 line; committing it would split that record. Leaving it dirty
is the deliberate, correct end state for this task and must be stated in the SUMMARY.

**Step 3 — Pre-commit safety check.** Run `git diff --cached --name-only`. It must contain ONLY the
four todo paths staged by the two `git mv` calls. If it lists anything else, **STOP and report** —
the concurrent session has staged work and this task must not commit over it. Do NOT run `git reset`
to fix it; report and halt.

**Step 4 — Commit, explicitly scoped.** Stage only named paths, never `-A` / `.` / `-a`:

`git add .planning/todos/completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md .planning/todos/completed/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md .planning/quick/260902-9el-close-two-stale-epic-logout-todos-on-pha/`

(the two `pending/` deletions are already staged by `git mv`; do not `git add` `pending/` paths, and
do not `git add` `.planning/STATE.md` or `.planning/ROADMAP.md`).

Commit message: `docs(quick-260902-9el): close two stale Epic-logout todos on Phase 35 release-artifact evidence`

Do NOT use `gsd-sdk query commit` — it is recorded to stage the entire tree, which would absorb the
concurrent session's ROADMAP.md.

**Step 5 — Post-commit scope proof.** Run `git show --stat --name-only --no-renames HEAD` (`--no-renames` matters: with rename detection on, a `git mv` plus an append can collapse to a single path and hide half the change) and confirm ROADMAP.md
and STATE.md are absent. Then confirm `.planning/ROADMAP.md` and `.planning/STATE.md` are still dirty
in `git status --porcelain` — proof the concurrent session's work was preserved, not swallowed.
  </action>
  <verify>
    <automated>test -f .planning/quick/260902-9el-close-two-stale-epic-logout-todos-on-pha/260902-9el-SUMMARY.md || { echo 'no SUMMARY'; exit 1; }; F=$(git show --pretty=format: --name-only --no-renames HEAD | grep -v '^$' | sort); echo "$F" | grep -q '^\.planning/ROADMAP\.md$' && { echo 'ROADMAP.md WAS COMMITTED — concurrent session damaged'; exit 1; }; echo "$F" | grep -q '^\.planning/STATE\.md$' && { echo 'STATE.md WAS COMMITTED — splits the concurrent Phase 40 record'; exit 1; }; echo "$F" | grep -q 'src/\|src-tauri/\|meta/' && { echo 'CODE FILE IN A RECORDS-ONLY COMMIT'; exit 1; }; for p in '.planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md' '.planning/todos/pending/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md' '.planning/todos/completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md' '.planning/todos/completed/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md' '.planning/quick/260902-9el-close-two-stale-epic-logout-todos-on-pha/260902-9el-PLAN.md' '.planning/quick/260902-9el-close-two-stale-epic-logout-todos-on-pha/260902-9el-SUMMARY.md'; do echo "$F" | grep -qx "$p" || { echo "EXPECTED PATH ABSENT FROM COMMIT: $p"; exit 1; }; done; git status --porcelain .planning/ROADMAP.md .planning/STATE.md | grep -q . || { echo 'ROADMAP/STATE no longer dirty — the concurrent session lost work'; exit 1; }; echo OK</automated>
  </verify>
  <done>SUMMARY written; one row appended to STATE.md's Quick Tasks Completed table and LEFT UNCOMMITTED; a single commit contains exactly the two renames plus the quick-task PLAN and SUMMARY; ROADMAP.md and STATE.md remain dirty and uncommitted; no file under `src/`, `src-tauri/` or `meta/` appears in the commit.</done>
</task>

</tasks>

<verification>
Run from the repo root after all three tasks:

1. `ls .planning/todos/pending/ | grep -c epic-logout` → **0**
2. `ls .planning/todos/completed/ | grep -c epic-logout` → **2**
3. Frontmatter-scoped staleness gate on todo 1 (prose quotes of the old value are allowed and expected;
   the frontmatter block is what must be clean):
   `awk '/^---$/{n++; next} n==1' .planning/todos/completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md | grep -c 'nothing external'` → **0**
4. Both closure sections cite `b5b3464bd`, `bea07cd17`, `REQ-35-07`, `REQUIREMENTS.md:429`, `REQUIREMENTS.md:1143`, `Phase 38`/`38-W06`, `D-35-19-15`.
5. `git show --stat --no-renames HEAD` names neither `.planning/ROADMAP.md` nor `.planning/STATE.md`, and no path under `src/`, `src-tauri/` or `meta/`.
6. `git status --porcelain .planning/ROADMAP.md .planning/STATE.md` is NON-EMPTY — the concurrent session's work survived.
7. `git diff HEAD~1 HEAD --stat -- src src-tauri meta` is EMPTY — records-only proven, not asserted.
</verification>

<success_criteria>
- Both Epic-logout todos are in `.planning/todos/completed/` with `status: CLOSED`, moved via `git mv`.
- Each carries a dated `## CLOSURE RECORD — 2026-09-02, quick task 260902-9el` section citing the
  specific evidence (commits, dates, REQ id, `file:line`) rather than gesturing at it.
- Todo 1's stale `blocked_by` is corrected in the frontmatter and its old value is preserved as
  quoted history in the closure prose — the rot is recorded, not erased.
- Neither closure reads as total: both name **Phase 38 / `38-W06`** as owner of off-macOS Epic logout,
  and both record that **`D-35-19-15` survives OPEN** with no owning phase and does not re-block them.
- Every pre-existing section of both todos survives verbatim; nothing historical was rewritten.
- Zero code changes. One commit, explicitly scoped. `ROADMAP.md` untouched; `STATE.md` edited but
  left uncommitted.
</success_criteria>

<output>
Create `.planning/quick/260902-9el-close-two-stale-epic-logout-todos-on-pha/260902-9el-SUMMARY.md` when done.
</output>
