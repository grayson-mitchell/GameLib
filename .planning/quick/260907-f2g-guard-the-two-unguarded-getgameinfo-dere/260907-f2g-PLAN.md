---
phase: quick-260907-f2g
plan: '01'
type: execute
wave: 1
depends_on: []
subsystem: steam
autonomous: true
requirements: [QUICK-F2G-01, QUICK-F2G-02, QUICK-F2G-03]
files_modified:
  - src/backend/protocol.ts
  - src/backend/__tests__/protocol.test.ts
  - src/backend/tools/index.ts
  - src/backend/tools/__tests__/runWineCommandOnGameGuard.test.ts
  - .planning/todos/pending/2026-09-07-two-unguarded-getgameinfo-derefs-protocol-findgame-and-runwinecommandongame.md
  - .planning/todos/completed/2026-09-07-two-unguarded-getgameinfo-derefs-protocol-findgame-and-runwinecommandongame.md
user_setup: []

must_haves:
  truths:
    - "A `gamelib://launch?appName=X&runner=steam` deep link for a game whose `getGameInfo()` returns the `{}` sentinel logs `Could not receive game data for X!` and resolves — it does NOT reject with a TypeError."
    - "`findGame()`'s runner-less loop branch still returns a populated GameInfo when a later runner resolves the appName — the D-01 sentinel consumer at protocol.ts:222 is untouched."
    - "`runWineCommandOnGame()` for a bottle-eligible game whose `getGameInfo()` returns the `{}` sentinel returns `{ stdout: '', stderr: '' }`, logs an error, and never calls `runWineCommand`."
    - "`runWineCommandOnGame()` for a game with a real `install.install_path` still reaches `runWineCommand` with that path (the guard is not over-broad)."
    - "Each of the two guards has a test whose named revert turns it RED with a named failure message."
    - "The pending todo file is absent from `.planning/todos/pending/` and present in `.planning/todos/completed/` with `status: RESOLVED` and `resolved_by: quick-260907-f2g`."
  artifacts:
    - path: "src/backend/protocol.ts"
      provides: "findGame()'s explicit-runner branch returns undefined for the `{}` sentinel"
      contains: "app_name"
    - path: "src/backend/tools/index.ts"
      provides: "runWineCommandOnGame() guards install.install_path before dereferencing it"
      contains: "install?.install_path"
    - path: "src/backend/tools/__tests__/runWineCommandOnGameGuard.test.ts"
      provides: "Behavioural RED-proof for the tools/index.ts guard, importing the real module"
    - path: ".planning/todos/completed/2026-09-07-two-unguarded-getgameinfo-derefs-protocol-findgame-and-runwinecommandongame.md"
      provides: "The closed ledger entry with the resolution and both RED-proof recipes"
      contains: "status: RESOLVED"
  key_links:
    - from: "src/backend/protocol.ts findGame() explicit-runner branch"
      to: "handleLaunch()'s `if (!gameInfo)` guard at protocol.ts:116"
      via: "returning undefined instead of a truthy {}"
      pattern: "app_name \\? "
    - from: "src/backend/tools/index.ts runWineCommandOnGame()"
      to: "the adjacent isNative() early-return shape"
      via: "the same `{ stdout: '', stderr: '' }` return plus a logError"
      pattern: "stdout: '', stderr: ''"
    - from: ".planning/todos/completed/2026-09-07-two-unguarded-getgameinfo-derefs-protocol-findgame-and-runwinecommandongame.md"
      to: "quick task 260907-f2g"
      via: "resolved_by frontmatter field"
      pattern: "resolved_by: quick-260907-f2g"
---

<objective>
Guard the two genuinely-unguarded non-title `getGameInfo()` derefs that quick task `260907-e7a`'s
census found and filed as class 6: `protocol.ts:215`'s `findGame()` explicit-runner branch, and
`tools/index.ts:896`'s `runWineCommandOnGame()`.

Purpose: both sites deref a field of the `{}` cross-runner sentinel without a guard, and both turn a
missing-game condition into an unhandled `TypeError` rather than a handled no-op. `protocol.ts`'s
version is worse than a crash — the `{}` is TRUTHY, so it defeats `handleLaunch`'s own
`if (!gameInfo)` guard at L116, and the deep link silently does nothing with no dialog and no
user-facing error.

Output: two call-site guards, each RED-proven by a test with a named revert; the pending todo moved
to `.planning/todos/completed/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/todos/pending/2026-09-07-two-unguarded-getgameinfo-derefs-protocol-findgame-and-runwinecommandongame.md

<locked_decisions>
**D-01 (locked, `260905-luf`; re-confirmed by `260907-e7a`'s census).** `SteamGame.getGameInfo()`
returning `{} as GameInfo` on a double cache miss is a DELIBERATE cross-runner sentinel. FOUR
consumers depend on its falsiness: `gamedetails/dispatch.ts:~82`,
`sidecar/appShellFlowRegistration.ts:~536`, `storeManagers/steam/library.ts:~1257`, and
`protocol.ts:222` (the fourth was surfaced by e7a's census and is NOT named in D-01's own source
comment).

**OUT OF BOUNDS for this plan:**
- Changing the `{}` return itself, or populating a stub `GameInfo` in the double-miss branch.
- Making `getGameInfo()` async (~40 call sites / ~20 files — needs its own phase).
- Any change under `src/backend/storeManagers/steam/`. Nothing there should need to move.

**The trap in Site 1.** `findGame()`'s two branches disagree. The runner-less loop at
`protocol.ts:218-223` guards on `maybeGameInfo.app_name` and IS one of the four legitimate sentinel
consumers — do NOT "fix" it, do NOT rewrite it, do NOT factor the two branches into a shared helper
that changes its behaviour. Only the explicit-runner branch at L215 is defective. The fix makes L215
AGREE with L222 while leaving L222's sentinel reliance exactly as it is.
</locked_decisions>

<measured_facts>
Everything below was MEASURED during planning at HEAD `bc47c9e80`, not assumed. Re-measure any line
number before editing; do not re-derive the reproductions.

**Line numbers at HEAD `bc47c9e80`** (the todo cites `4c0a7f58d`; both files are byte-identical
between the two, so its numbers still hold):
- `src/backend/protocol.ts:215` — `if (runner) return libraryManagerMap[runner].getGame(appName).getGameInfo()`
- `src/backend/tools/index.ts:892` — `if (game.isNative()) {`
- `src/backend/tools/index.ts:896` — `const { folder_name, install } = game.getGameInfo()`
- `src/backend/tools/index.ts:902` — `gameInstallPath: install.install_path,`

**Site 1 reproduces in the EXISTING suite.** `src/backend/__tests__/protocol.test.ts` already mocks
`libraryManagerMap` with all five runners and already defines
`const emptyGameInfoMock = { getGameInfo: () => ({}) }` (L137), assigned to `steam`/`gog`/`nile`/
`sideload` in the outer `beforeEach`. A probe test added inside the outer describe:

    await expect(
      handleProtocol(['gamelib://launch?appName=steam-1&runner=steam'])
    ).resolves.toBeUndefined()

was run and FAILED with, verbatim:

    Received promise rejected instead of resolved
    Rejected to value: [TypeError: Cannot read properties of undefined (reading 'getGame')]

This is the caller-path proof the scope demands: the rejection originates at `protocol.ts:124`
(`libraryManagerMap[gameInfo.runner]` where `runner` is `undefined`), NOT inside `findGame()`.
`handleProtocol` returns `handleLaunch(url)`'s promise, so `await` surfaces the rejection.

**Site 2 IS behaviourally testable — the docstring that says otherwise is WRONG.**
`src/backend/tools/__tests__/dxvkEvidenceLines.test.ts:24-27` asserts in prose that `tools/index.ts`
"reaches Electron transitively and must not be imported under the backend jest project". That was
MEASURED FALSE during planning. A probe file at `src/backend/tools/__tests__/` that imports
`{ runWineCommandOnGame } from '../index'` with exactly two `jest.mock` factories —
`'../../storeManagers'` (supplying `libraryManagerMap`) and `'../../launcher'` (supplying
`runWineCommand`, `setupEnvVars`, `setupWineEnvVars`, `validWine`) — loaded and ran in 0.99s, and
reproduced the defect with, verbatim:

    Rejected to value: [TypeError: Cannot read properties of undefined (reading 'install_path')]

Note `tools/index.ts` reaches `libraryManagerMap` through a LAZY
`await import('../storeManagers')` at L889 — `jest.mock('../../storeManagers', …)` intercepts it
regardless.

**Type facts.** `GameInfo.install` is `Partial<InstalledInfo>` and is a REQUIRED key
(`src/common/types.ts:222`), so `install_path` inside it is already optional. `WineCommandArgs`
declares `gameInstallPath?: string` (`src/common/types.ts:766`) — i.e. an optional-chain-only fix
would TYPE-CHECK and silently pass `undefined` straight through to `runWineCommand`. There is
precedent for the `info.install?.install_path &&` shape at `steam/games.ts:2873`, which lints clean.

**Test-infra facts.**
- Backend jest project `displayName` is exactly `Backend` (case-sensitive; `--selectProjects` fails
  open on a wrong case). Invoke as
  `npx jest --selectProjects Backend --testPathPattern '<path fragment>'`.
- `src/backend/jest.config.js` sets `resetMocks: true`. Set every mock return value inside a test or
  `beforeEach`, never at module scope.
- `dxvkEvidenceLines.test.ts` pins `tools/index.ts` by COLLAPSED-SOURCE markers, never by line
  number, and its only count assertion is
  `countOccurrences(after, 'runWineCommand(') >= 2`. Adding a guard clause removes no occurrence, so
  that gate is unaffected. Run it anyway (Task 2 verify) rather than trusting this paragraph.
</measured_facts>

<interfaces>
From `src/backend/protocol.ts` (current, defective):

    function findGame(appName?: string | null, runner?: Runner): GameInfo | undefined {
      if (!appName) return
      if (runner) return libraryManagerMap[runner].getGame(appName).getGameInfo()   // L215
      for (const runner of RUNNERS.options) {
        const maybeGameInfo = libraryManagerMap[runner].getGame(appName).getGameInfo()
        if (maybeGameInfo.app_name) return maybeGameInfo                            // L222 — DO NOT TOUCH
      }
      return
    }

From `src/backend/tools/index.ts` (current, defective; the `runner === 'steam'` guard at L879-886
and the `isNative()` guard at L892-895 both already return `{ stdout: '', stderr: '' }` after a
`logError(..., LogPrefix.Gog)`):

    const game = libraryManagerMap[runner].getGame(appName)
    if (game.isNative()) { logError(...); return { stdout: '', stderr: '' } }
    const { folder_name, install } = game.getGameInfo()      // L896
    const gameSettings = await game.getSettings()
    return runWineCommand({ gameSettings, installFolderName: folder_name,
                            gameInstallPath: install.install_path, ... })   // L902
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-prove and guard protocol.ts findGame()'s explicit-runner branch</name>
  <files>src/backend/__tests__/protocol.test.ts, src/backend/protocol.ts</files>

  <behavior>
    Extend the EXISTING `src/backend/__tests__/protocol.test.ts` — do not create a new file. Add one
    `describe` block inside the outer `describe('protocol.ts --no-gui behavior', …)` so the outer
    `beforeEach` (which wires `libraryManagerMap`, `getMainWindow`, `dispatchSteamLaunch`) applies.

    - Test 1 (the RED-proof, caller path): an explicit-runner deep link
      `gamelib://launch?appName=steam-1&runner=steam` whose manager returns the `{}` sentinel
      (`emptyGameInfoMock`, already defined at L137) resolves without throwing, calls `logError`
      with `Could not receive game data for steam-1!`, and calls NEITHER `dispatchSteamLaunch` NOR
      `launchEventCallback` NOR `dialog.showMessageBox`.
    - Test 2 (inverse — the guard is not over-broad): the same explicit-runner URL against a
      POPULATED GameInfo (`app_name` set, `is_installed: true`, `runner: 'steam'`) still dispatches
      through `dispatchSteamLaunch` and does NOT hit the `logError`.

    Non-regression for the D-01 loop branch is already carried by the two existing tests
    `'resolves through libraryManagerMap.steam when every other manager is empty'` (L495ff) and
    `'should handle missing game info'` (L264ff). Do not add a third; DO run the whole file.

    `logError` is already in the `jest.mock('../logger', …)` factory but is NOT currently imported
    into the test file — add it to the existing `import { logInfo } from '../logger'` line.
  </behavior>

  <action>
    Step 1 — write Test 1 and Test 2 ONLY (no production change yet). Run the suite as a SEPARATE
    Bash invocation from the write; a `write && npx jest` chain reads a stale file. Confirm Test 1
    FAILS with `Rejected to value: [TypeError: Cannot read properties of undefined (reading
    'getGame')]` and Test 2 PASSES. If Test 1 does not fail with that exact rejection, STOP and
    report — the test is not proving the defect.

    Step 2 — apply the guard at `src/backend/protocol.ts:215`, replacing the single-line
    `if (runner) return …` with a block that reads the info once, returns it only when `app_name` is
    truthy, and otherwise returns `undefined`. Add a short comment naming D-01 and stating WHY:
    returning the sentinel here defeats the caller's own `if (!gameInfo)` at L116, and the fix makes
    this branch agree with the loop branch at L222 whose `app_name` check is a deliberate sentinel
    consumer that must not be altered. Do not touch L218-223. Do not factor the two branches
    together.

    Step 3 — re-run and confirm all tests in the file pass, with a non-zero passed count.

    Step 4 — RED-PROOF RECIPE, to be recorded verbatim in the SUMMARY and the closed todo. Perform
    the revert by HAND-EDITING the line back to
    `if (runner) return libraryManagerMap[runner].getGame(appName).getGameInfo()` — do NOT use
    `git checkout -- <file>` (this repo's post-checkout hook fires a binary download and throws) and
    do NOT use `git stash` (it strands concurrent sessions). Re-run, confirm Test 1 goes red with
    the TypeError above, then hand-edit the guard back and re-run green.

    No new user-facing strings. The `Could not receive game data for …` message is a pre-existing
    developer-facing `logError`, not i18n. Nothing goes in `public/locales/en/gamelib.json`.

    Step 5 — commit. Stage by EXPLICIT PATH only:
    `git add src/backend/__tests__/protocol.test.ts src/backend/protocol.ts`.
    Then `git diff --cached --stat` and confirm it lists exactly those two paths and nothing else —
    the working tree carries untracked `.claude/skills/archify/` and `skills-lock.json` that must
    NEVER be staged. Do NOT use `gsd-sdk query commit` (it absorbs the whole tree), `git add -A`,
    `git commit -a`, or `git commit --only`.
  </action>

  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern 'backend/__tests__/protocol' 2>&1 | tail -20</automated>
    Run must report `Tests:` with a non-zero passed count and zero failures. A run reporting 0 tests
    is a FAILURE, not a pass.
    <automated>git diff --cached --stat</automated>
    Must list exactly `src/backend/__tests__/protocol.test.ts` and `src/backend/protocol.ts`.
  </verify>

  <done>
    `protocol.ts:215`'s branch returns `undefined` for the `{}` sentinel; the whole
    `protocol.test.ts` file is green including both new tests and both pre-existing loop-branch
    tests; the hand-edit revert was executed and observed RED with the named TypeError; one commit
    staged from exactly two explicit paths.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED-prove and guard runWineCommandOnGame(), then close the todo</name>
  <files>src/backend/tools/__tests__/runWineCommandOnGameGuard.test.ts, src/backend/tools/index.ts, .planning/todos/pending/2026-09-07-two-unguarded-getgameinfo-derefs-protocol-findgame-and-runwinecommandongame.md, .planning/todos/completed/2026-09-07-two-unguarded-getgameinfo-derefs-protocol-findgame-and-runwinecommandongame.md</files>

  <behavior>
    New file `src/backend/tools/__tests__/runWineCommandOnGameGuard.test.ts`. No existing test
    exercises `runWineCommandOnGame`'s body: `sidecar/__tests__/wineToolsFlows.test.ts` and
    `sidecar/__tests__/runnerMiscFlows.test.ts` both `jest.mock` it away, and
    `tools/__tests__/dxvkEvidenceLines.test.ts` is a source-text gate that imports nothing from the
    module. So a new file is correct here, not duplication.

    Mock exactly two modules, both before the import of `../index`:
    - `'../../storeManagers'` → `{ libraryManagerMap: { gog: { getGame: (...a) => mockGetGame(...a) } } }`
    - `'../../launcher'` → `{ runWineCommand: (...a) => mockRunWineCommand(...a), setupEnvVars: jest.fn(), setupWineEnvVars: jest.fn(), validWine: jest.fn() }`

    Use `runner: 'gog'` throughout, NOT `'steam'` — `runWineCommandOnGame` returns early for
    `'steam'` at L879 before reaching either the `isNative()` check or the deref, so a Steam runner
    would make the test vacuous. The `isNative() === false` bottled-macOS-Steam scenario the todo
    describes is the REACHABILITY argument; `'gog'` is the vehicle that exercises the same lines.

    Because `resetMocks: true` is set, assign every `mockReturnValue` inside `beforeEach` or the
    test body.

    - Test 1 (the RED-proof): a game with `isNative: () => false` and `getGameInfo: () => ({})`
      RESOLVES to exactly `{ stdout: '', stderr: '' }`, calls `logError` (spy on it, or assert via a
      `jest.mock('backend/logger', …)` factory preserving `LogPrefix`), and `runWineCommand` is
      never called.
    - Test 2 (inverse — the guard is not over-broad): a game with `isNative: () => false` and
      `getGameInfo: () => ({ app_name: 'g', folder_name: 'Game Folder', install: { install_path: '/games/g' } })`
      DOES call `runWineCommand` exactly once, with `gameInstallPath: '/games/g'` and
      `installFolderName: 'Game Folder'`.
    - Test 3 (adjacent-branch parity): the existing `isNative: () => true` path still returns
      `{ stdout: '', stderr: '' }` and never calls `runWineCommand` — pins that the new guard
      returns the SAME shape as the branch it sits next to.
  </behavior>

  <action>
    Step 1 — write the test file. Run it as a SEPARATE Bash invocation from the write. Confirm
    Test 1 FAILS with `Rejected to value: [TypeError: Cannot read properties of undefined (reading
    'install_path')]`, and Tests 2 and 3 PASS. If Test 1 passes before the fix, the test is not
    proving anything — STOP and report rather than claiming coverage.

    Step 2 — apply the guard in `src/backend/tools/index.ts`. IMPORTANT: the todo's own proposed fix
    for this site is WRONG as written. It says to "optional-chain the way `isGameAvailable()` does",
    but `WineCommandArgs.gameInstallPath` is `?: string`, so an optional chain alone type-checks and
    silently invokes Wine with `gameInstallPath: undefined` — trading a loud TypeError for an
    invisible bad-arg run. The scope's requirement governs: guard AND early-return AND log.

    Insert immediately after the `const { folder_name, install } = game.getGameInfo()` at L896, and
    before the `await game.getSettings()`:
    - a check on `!install?.install_path`;
    - a `logError` naming the runner and appName and saying the GameInfo carries no resolved install
      path (developer-facing, `LogPrefix.Gog` to match both adjacent guards);
    - `return { stdout: '', stderr: '' }` — byte-identical shape to L894 and L885.
    Leave `gameInstallPath: install.install_path` at L902 as-is; it is narrowed by the guard.
    Add a one-line comment citing D-01 and this quick task so the next reader does not re-derive why
    a `{}` can arrive here.

    Do NOT log the install path itself; log runner + appName only (matches the redaction posture of
    the adjacent `logError`s).

    Step 3 — re-run the new file green, then run `dxvkEvidenceLines.test.ts` to confirm the
    source-text gate on this same file did not regress.

    Step 4 — run `npx tsc --noEmit -p tsconfig.json` (or the repo's `codecheck` script if present)
    scoped as narrowly as the repo allows. Note: a tsc pass does NOT see eslint errors; also run
    lint on exactly the two touched source files.

    Step 5 — RED-PROOF RECIPE, recorded verbatim in the SUMMARY and the closed todo. Hand-edit the
    guard out (delete the inserted block), re-run, confirm Test 1 goes red with the named TypeError,
    hand-edit it back, re-run green. Again: no `git checkout --`, no `git stash`.

    Step 6 — commit the code change. Stage by explicit path:
    `git add src/backend/tools/index.ts src/backend/tools/__tests__/runWineCommandOnGameGuard.test.ts`
    then `git diff --cached --stat` and confirm exactly those two paths appear. Commit.

    Step 7 — close the todo. `git mv` the pending file to `.planning/todos/completed/` keeping the
    filename. Edit its frontmatter to match the convention already used by the files in that
    directory (see
    `.planning/todos/completed/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md`):
    change `status: OPEN` → `status: RESOLVED`, and add `resolved: 2026-09-07` and
    `resolved_by: quick-260907-f2g`. Leave `created`, `title`, `area`, `severity`, `source`,
    `files`, `resolves_phase` as they are.

    Append a `## Resolution` section carrying, at minimum:
    (a) the final shape of each of the two guards, with the file:line each landed at;
    (b) both RED-proof recipes verbatim — the exact revert AND the exact observed failure text;
    (c) the correction to this todo's own Site-2 proposed fix (optional-chain alone is insufficient
        because `gameInstallPath` is optional in `WineCommandArgs`, so the TypeError becomes a silent
        bad-arg Wine invocation);
    (d) the correction to `dxvkEvidenceLines.test.ts:24-27`'s prose claim that `tools/index.ts`
        "must not be imported under the backend jest project" — measured false; two mock factories
        suffice. State plainly that the docstring was NOT edited by this task and remains inaccurate.

    Step 8 — commit the docs. `git add` the two `.planning/todos/...` paths explicitly (git mv means
    both a delete and an add must be staged), `git diff --cached --stat`, verify, commit.
  </action>

  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern 'tools/__tests__' 2>&1 | tail -25</automated>
    Must report a non-zero passed count across both `runWineCommandOnGameGuard.test.ts` and
    `dxvkEvidenceLines.test.ts`, with zero failures. A 0-test run is a FAILURE.
    <automated>test ! -f .planning/todos/pending/2026-09-07-two-unguarded-getgameinfo-derefs-protocol-findgame-and-runwinecommandongame.md && grep -q 'resolved_by: quick-260907-f2g' .planning/todos/completed/2026-09-07-two-unguarded-getgameinfo-derefs-protocol-findgame-and-runwinecommandongame.md && echo TODO_CLOSED</automated>
    <automated>git status --porcelain .planning/todos/</automated>
    Must be empty after the docs commit — the git mv delete AND add are both staged.
  </verify>

  <done>
    `tools/index.ts` returns `{ stdout: '', stderr: '' }` with a `logError` instead of throwing when
    `install.install_path` is absent; the new test file is green with all three tests including the
    inverse case; `dxvkEvidenceLines.test.ts` still green; the hand-edit revert was executed and
    observed RED with the named TypeError; the todo is in `completed/` with `status: RESOLVED` and
    `resolved_by: quick-260907-f2g`, carrying both RED-proof recipes and both corrections; two
    commits, each staged from explicit paths only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS → `gamelib://` protocol handler | `appName` and `runner` are attacker-supplied via a URL any local process (or a web page's protocol handler invocation) can trigger; they reach `libraryManagerMap[runner].getGame(appName)` |
| renderer/sidecar → Wine-tools channels | `runner` + `appName` reach `runWineCommandOnGame` from `tools/ipc_handler.ts:16/35/44`, `sidecar/wineToolsFlowRegistration.ts:376`, `sidecar/runnerMiscFlowRegistration.ts:167/176` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f2g-01 | Denial of Service | `handleLaunch`'s explicit-runner path | mitigate | The current unhandled `TypeError` at `protocol.ts:124` aborts the protocol handler mid-flight for any URL naming a runner that does not own the appName — trivially triggerable from outside the app. Task 1's guard converts it into the handled `logError` path that `findGame`'s loop branch already reaches. This is the plan's primary security effect, not a side effect. |
| T-f2g-02 | Tampering | Task 1's guard MUST NOT widen `findGame`'s accepted input | mitigate | The guard only narrows: it can turn a returned value into `undefined`, never the reverse. `RUNNERS.safeParse` at `protocol.ts:111` and the own-property confused-deputy check documented at `protocol.ts:20-25` are untouched. Verify by diff: no line outside 214-216 changes in `protocol.ts`. |
| T-f2g-03 | Elevation of Privilege | Task 2's guard MUST NOT relax the two guards above it | mitigate | The `runner === 'steam'` early return (L879-886) and the `isNative()` early return (L892-895) both still run BEFORE the new check. Verify by diff: neither guard's lines are touched; the insert lands strictly after L896. |
| T-f2g-04 | Information Disclosure | the two new log lines | mitigate | Log `runner` and `appName` only — never `install_path`, never a prefix path. Matches the redaction posture of the adjacent `logError`s at L881 and L893. |
| T-f2g-05 | Repudiation | a guard-clause test that is green both before and after the fix | mitigate | Each task's Step 1 requires observing the named RED failure BEFORE any production change, and Step 5/4 requires re-observing it via a named hand-edit revert. Precedent: `260905-luf` shipped an inadequate RED-proof on this exact defect family and had to replace it. If a revert does not turn the test red, the executor STOPS and reports rather than claiming coverage. |
| T-f2g-SC | Tampering | npm/pip/cargo installs | N/A | This plan installs NO packages. No `## Package Legitimacy Audit` is required; if any task finds itself wanting a dependency, STOP and report rather than installing. |
</threat_model>

<source_audit>
| Source | Item | Covered by |
|---|---|---|
| GOAL | Guard `protocol.ts`'s `findGame()` explicit-runner branch | Task 1 |
| GOAL | Guard `tools/index.ts`'s `runWineCommandOnGame()` | Task 2 |
| SCOPE | Each fix RED-proven with a NAMED revert and a NAMED expected failure output | Task 1 Steps 1+4; Task 2 Steps 1+5 (both reproductions measured during planning and quoted verbatim in `<measured_facts>`) |
| SCOPE | Site 1's test exercises the CALLER path, not `findGame()` in isolation | Task 1 Test 1 drives `handleProtocol(...)`; the measured rejection originates at `protocol.ts:124`, inside the caller |
| SCOPE | Site 2 returns the same `{stdout:'',stderr:''}` shape as the adjacent `isNative()` branch, and logs | Task 2 Step 2 + Test 3 (adjacent-branch parity pin) |
| SCOPE | Check for existing coverage; extend rather than duplicate; locate test files rather than assume | Measured during planning: `protocol.test.ts` EXISTS and is extended (Task 1). No existing test exercises `runWineCommandOnGame`'s body — the three that name it all mock it away — so a new file is correct (Task 2 `<behavior>` states the evidence) |
| SCOPE | Run only the relevant jest project/path | Both `<verify>` blocks use `--selectProjects Backend --testPathPattern`; no full `pnpm test` anywhere |
| SCOPE | No new user-facing strings; if any, `gamelib.json` never `translation.json` | NONE NEEDED — stated explicitly in Task 1 Step 4. Both new messages are developer-facing `logError` calls, which this codebase does not route through i18n (see the existing `logError`s at `tools/index.ts:881/893` and `protocol.ts:117`) |
| SCOPE | Close the pending todo via git mv, `status: RESOLVED`, `resolved_by` naming 260907-f2g, matching `completed/` frontmatter convention | Task 2 Step 7 (convention read from `.planning/todos/completed/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md`: `status` / `resolved` / `resolved_by`) |
| CONSTRAINT | Single plan file, 1-2 tasks, ~30% context | This file; 2 tasks |
| CONSTRAINT | `.claude/skills/archify/` and `skills-lock.json` never staged; explicit per-path staging + `git diff --cached --stat` before each commit | Task 1 Step 5; Task 2 Steps 6 and 8; both `<verify>` blocks assert on `git diff --cached --stat` / `git status --porcelain` |
| LOCKED | D-01: no change to the `{}` return, no stub GameInfo, no async `getGameInfo()`, nothing under `storeManagers/steam/` | `<locked_decisions>`; neither task's `files_modified` includes any `storeManagers/` path |
| LOCKED | `protocol.ts:222`'s loop-branch sentinel reliance survives untouched | Task 1 `<action>` Step 2 forbids touching L218-223; non-regression carried by two pre-existing tests the task must run |

No unplanned items. Nothing deferred.
</source_audit>

<verification>
1. `npx jest --selectProjects Backend --testPathPattern 'backend/__tests__/protocol'` — green, non-zero test count.
2. `npx jest --selectProjects Backend --testPathPattern 'tools/__tests__'` — green, non-zero test count.
3. Both RED-proof reverts executed by hand-edit and observed RED with their named messages, then restored.
4. `git diff --cached --stat` inspected before EACH of the three commits; `.claude/skills/archify/` and `skills-lock.json` appear in none of them and remain untracked afterwards.
5. `git status --porcelain .planning/STATE.md .planning/ROADMAP.md` is EMPTY — this plan touches neither file by any means.
6. `git diff HEAD~3 --stat` (after all three commits) touches exactly: `src/backend/protocol.ts`, `src/backend/__tests__/protocol.test.ts`, `src/backend/tools/index.ts`, `src/backend/tools/__tests__/runWineCommandOnGameGuard.test.ts`, and the todo rename. Nothing under `src/backend/storeManagers/`.
</verification>

<success_criteria>
- A `gamelib://launch?appName=X&runner=R` link whose manager returns the `{}` sentinel logs `Could not receive game data for X!` and resolves, instead of rejecting with `TypeError: Cannot read properties of undefined (reading 'getGame')`.
- `runWineCommandOnGame` on a bottle-eligible game with no resolved install path returns `{ stdout: '', stderr: '' }` and logs, instead of rejecting with `TypeError: Cannot read properties of undefined (reading 'install_path')`; and still reaches `runWineCommand` with the real path when one exists.
- `protocol.ts:222`'s loop-branch `app_name` sentinel check is byte-identical to HEAD.
- No file under `src/backend/storeManagers/` is modified.
- The todo is in `.planning/todos/completed/` with `status: RESOLVED` and `resolved_by: quick-260907-f2g`, carrying both RED-proof recipes verbatim and both planning-time corrections (the todo's own Site-2 optional-chain proposal, and `dxvkEvidenceLines.test.ts`'s importability claim).
- `.planning/STATE.md` and `.planning/ROADMAP.md` are untouched.
</success_criteria>

<output>
Create `.planning/quick/260907-f2g-guard-the-two-unguarded-getgameinfo-dere/260907-f2g-SUMMARY.md` when done.
</output>
