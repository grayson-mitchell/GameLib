---
phase: quick-260909-k5x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/sidecar/bootstrap.ts
  - src/backend/sidecar/__tests__/gogPresenceBootWire.test.ts
  - src/backend/sidecar/__tests__/testContainment.test.ts
  - .planning/todos/pending/2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md
  - .planning/todos/pending/2026-09-09-deletepresence-never-resets-interval-so-the-keepalive-cannot-re-arm.md
autonomous: true
requirements:
  - TODO-2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms
user_setup: []

must_haves:
  truths:
    - "At sidecar boot, once online, GOG presence is set without the user first launching a GOG game."
    - "The 5-minute presence keep-alive is armed at boot, because arming it is a side effect of the first setPresence() call and `interval` is `undefined` at process start -- this closes BOTH halves of the todo's title."
    - "Block H reads NO settings flag at its own call site: setPresence()'s own `disableGOGPresence || disablePlaytimeSync || !GOGUser.isLoggedIn() || !isOnline()` early-return remains the sole gate, exactly as in the deleted bare `main.ts:477` call."
    - "Neither a synchronous throw nor a rejection out of gogPresence.setPresence() can fail sidecar boot or escape to processGuards' process-wide net."
    - "The presence call runs exactly once per process, even though init() is called many times per jest file."
    - "The D13 OUT-OF-SCOPE note in syncQueuedPlaytimeWhenOnline()'s header no longer describes this item as still-open, and points forward to Block H."
  artifacts:
    - path: "src/backend/sidecar/bootstrap.ts"
      provides: "Block H -- exported setGogPresenceWhenOnline() helper + gogPresenceInitialized-guarded call site in init() before the READY_SENTINEL write; rewritten D13 note"
      contains: "setGogPresenceWhenOnline"
    - path: "src/backend/sidecar/__tests__/gogPresenceBootWire.test.ts"
      provides: "Dedicated suite: init()-driven wiring proof, the no-settings-hoist receipt, and two never-fails-boot cases (rejection + synchronous throw via captured callback)"
      contains: "setGogPresenceWhenOnline"
    - path: "src/backend/sidecar/__tests__/testContainment.test.ts"
      provides: "STRUCTURALLY_CONTAINED_SUITES registration for the new suite -- T-34.2-83 is a set-equality census over readdirSync of this directory"
      contains: "gogPresenceBootWire.test.ts"
    - path: ".planning/todos/completed/2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md"
      provides: "The closed todo, moved with git mv, status: rewritten to RESOLVED"
      contains: "RESOLVED 2026-09-09 by quick-260909-k5x"
    - path: ".planning/todos/pending/2026-09-09-deletepresence-never-resets-interval-so-the-keepalive-cannot-re-arm.md"
      provides: "The separate, NOT-fixed-here deletePresence() interval defect, with the three CI-enforced triage keys"
      contains: "ready: code"
  key_links:
    - from: "src/backend/sidecar/bootstrap.ts"
      to: "src/backend/storeManagers/gog/presence.ts"
      via: "default import gogPresence, called as gogPresence.setPresence() inside a runOnceWhenOnline callback"
      pattern: "gogPresence\\.setPresence"
    - from: "src/backend/sidecar/bootstrap.ts init()"
      to: "setGogPresenceWhenOnline()"
      via: "if (!gogPresenceInitialized) guarded call site, after Block G, before output.write(READY_SENTINEL)"
      pattern: "gogPresenceInitialized"
---

<objective>
Port the boot-time GOG presence call deleted with `src/backend/main.ts` in commit `5643c7583`
(`main.ts:477`, `runOnceWhenOnline(gogPresence.setPresence)`) into
`src/backend/sidecar/bootstrap.ts` as **Block H**, following the shape of Blocks E and G.

Purpose: today GOG presence never goes online while GameLib runs unless the user launches a GOG
game (`launcher.ts:269`/`:289` are the only non-settings call sites). Because the 5-minute
keep-alive `setInterval` is armed *inside* `setPresence` itself, nothing arms it either. Block H
closes both halves.

Output: Block H (import + helper + guard flag + call site), the rewritten D13 note, a dedicated
test suite, its containment-census registration, the todo moved to `completed/`, and one NEW todo
filed for a separate defect found during triage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@src/backend/sidecar/bootstrap.ts
@src/backend/storeManagers/gog/presence.ts
@src/backend/online_monitor.ts
@src/backend/sidecar/__tests__/playtimeQueueBootDrain.test.ts
@.planning/todos/pending/2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md

Project skill (auto-load for Tauri/sidecar work): `Skill("spike-findings-gamelib")` -- read its
Rust/Tauri sidecar section before touching `bootstrap.ts`.

Per CLAUDE.md's graphify rule: run `graphify query "sidecar bootstrap boot blocks gog presence"`
before browsing raw source, and `graphify update .` after the code change lands.
</context>

<interfaces>
<!-- Extracted at planning time. The executor should NOT need to re-derive any of this. -->

`src/backend/storeManagers/gog/presence.ts` -- DEFAULT export only:
```
export default { setCurrentGame, setPresence, deletePresence }
// async function setPresence(): Promise<void>
```
`setPresence()` already: (a) wraps its entire body in `try { ... } catch (e) { logError(...) }`,
so it never rejects today; (b) early-returns on
`disableGOGPresence || disablePlaytimeSync || !GOGUser.isLoggedIn() || !isOnline()`; (c) arms
`interval = setInterval(setPresence, 5 * 60 * 1000)` behind `if (!interval)`.

`src/backend/online_monitor.ts:136-142`:
```
export const runOnceWhenOnline = (callback: () => unknown) => {
  if (isOnline()) { callback() } else { connectivityEmitter.once('online', () => callback()) }
}
```

`src/backend/sidecar/bootstrap.ts` anchors (verified at planning time):
- `:421` `export function reconcileStoreUsersWhenOnline(): void` (Block E)
- `:516-519` the D13 OUT-OF-SCOPE note inside `syncQueuedPlaytimeWhenOnline()`'s doc comment
- `:521` `export function syncQueuedPlaytimeWhenOnline(): void` (Block G)
- `:587` `export function checkRosettaWhenMac(): void` (Block F)
- `:606` `export function init(`
- Guard-flag declarations live in one run at `:168-230` (`playtimeLockClearInitialized`,
  `storeUserReconcileInitialized`, `rosettaCheckInitialized`, `playtimeQueueDrainInitialized`),
  each with its own "why this guard exists" comment, followed by `let i18nReady`.
- Call sites run Block D -> E -> F -> G, then `output.write(\`${READY_SENTINEL}\n\`)`, then
  `deliverStartupProtocolUrl()` as the deliberate last statement.
- `logDebug`, `logInfo`, `logWarning`, `logError`, `LogPrefix` are ALREADY imported -- no new
  logger import is needed.
</interfaces>

<decisions_settled_at_planning_time>

These two questions were opened by the task brief. Both are settled here; the executor must
implement them as decided and encode the reasoning in the doc comment.

**D-K5X-01 -- Placement/ordering: Block H has NO ordering constraint of its own. Only the generic
three.** State this plainly in the header; do NOT manufacture a load-bearing sentence.

Checked and REFUTED, each individually:
- *Not* dependent on Block D (`playtimeSyncQueue.delete('lock')`). `setPresence` never reads
  `playtimeSyncQueue`. Block G's load-bearing constraint is specific to `syncQueuedPlaytime()`'s
  `if (playtimeSyncQueue.has('lock')) return` first statement; Block H has no analogue.
- *Not* dependent on Block E (`reconcileStoreUsersWhenOnline`). The tempting claim -- "Block E
  touches GOG users, so presence must follow it" -- is false. Block E's
  `configStore.delete('userInfo')` arm is Epic-scoped and gated on `!LegendaryUser.isLoggedIn()`;
  its GOG arm only calls `GOGUser.getUserDetails()` when ALREADY logged in, which cannot change
  what `GOGUser.isLoggedIn()` returns. `setPresence` calls `GOGUser.getCredentials()` itself and
  refreshes on its own.
- *Not* dependent on Block F (`i18nReady`). Block H paints no dialog and reads no catalog.
- *Not* dependent on Block G. Both blocks read `disablePlaytimeSync`, but neither writes it and
  neither shares state with the other.

The three GENERIC constraints that DO apply, same as Blocks E/G:
1. after `initLogger()` -- the helper logs, and `heroicLogWriter` is unset before that (standing
   `sidecar-console-and-logger-are-invisible` finding);
2. after `initOnlineMonitor()` -- it calls `runOnceWhenOnline`, the same requirement Blocks B/E/G
   already record;
3. before the `READY_SENTINEL` write -- so presence is at least queued before the frontend can
   drive any RPC, without delaying READY itself.

Appending after Block G satisfies all three and leaves the existing A->B->C->D->E->F->G sequence
byte-identical.

One HONEST, non-load-bearing note to include: when the process boots offline, Blocks E, G and H
each register a `connectivityEmitter.once('online', ...)` listener, so their callbacks fire in
registration order (E, then G, then H). That ordering is deterministic and observable, but nothing
depends on it -- do not dress it up as a constraint.

**D-K5X-02 -- Do NOT hoist a settings read out of the callback. Block H deliberately differs from
Block G's D5, and the difference is port fidelity.**

Block G hoisted `GlobalConfig.get().getSettings()` to helper entry because the DELETED SOURCE
ITSELF read `settings` at `whenReady()` time (`main.ts:470-476` had a literal
`if (!settings.disablePlaytimeSync) { ... } else { logDebug(...) }` wrapper). Block H's deleted
source had no such wrapper: `main.ts:477` was a bare `runOnceWhenOnline(gogPresence.setPresence)`
with zero settings access at the call site. Reproducing that exactly means the gate stays where
the deleted source left it: inside `setPresence`, evaluated at online time.

Two consequences the executor must NOT "improve":
- There is NO skip-log arm in Block H. Block G's verbatim `logDebug` receipt has no counterpart in
  the deleted presence source; inventing one would be a new log line masquerading as a port.
- A user who toggles `disableGOGPresence` between boot and coming online SHOULD see the new value
  honoured. That is not a determinism regression relative to Block G -- it is the deleted source's
  own behaviour, and `presence.ts`'s module-scope `settingChanged` listener already makes live
  toggling the documented model for this specific flag.

The header must state this contrast explicitly (Block G D5 vs Block H) so a later reader does not
read the missing hoist as an oversight and "fix" it.
</decisions_settled_at_planning_time>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Block H to bootstrap.ts and rewrite the now-stale D13 note</name>
  <files>src/backend/sidecar/bootstrap.ts</files>

  <behavior>
    - `setGogPresenceWhenOnline()` is exported and calls `gogPresence.setPresence` exactly once
      when `runOnceWhenOnline` invokes its callback.
    - It calls `setPresence` even when `disableGOGPresence: true` is in `GlobalConfig` -- the
      helper itself reads no settings (D-K5X-02).
    - A rejecting `setPresence()` neither throws out of the helper nor escapes unhandled.
    - A synchronously-throwing `setPresence` invoked from a DEFERRED callback frame neither throws
      nor escapes.
    - `init()` calls it exactly once per process.
  </behavior>

  <action>
Four edits to `src/backend/sidecar/bootstrap.ts`, all following the file's existing house style
(long provenance-bearing doc comments; guard flags at the call site, never inside the helper).

**1a. Import.** Add `import gogPresence from '../storeManagers/gog/presence'` alongside the other
Block-scoped imports (near the Block F `checkRosettaInstall` import at the end of the import run),
with the file's standard bundle-residency comment. The residency claim, verified at planning time
and re-verifiable with `grep -rn "gog/presence" src/backend --include='*.ts'`: `utils.ts:43`
already does `import gogPresence from './storeManagers/gog/presence'`, and `bootstrap.ts` already
imports `checkRosettaInstall` from `'../utils'` for Block F -- so `presence.ts` is ALREADY resident
in the sidecar bundle. `launcher.ts:77` is a second, independent route. This is a second binding
onto an already-resident singleton, NOT a new module edge. Say so, and add the corollary a reader
will want: because ES modules are singletons, this import does NOT cause a second registration of
`presence.ts`'s module-scope `backendEvents.on('settingChanged', ...)` listener.

Sanity note, not an action: this import is unaffected by `electronUntouched.test.ts`'s
by-construction gate, which bans only the Steam token surface bindings (`configStore`,
`TOKEN_STORE_KEY`, `TOKEN_PREFIX`) in this file.

**1b. Guard flag.** Declare `let gogPresenceInitialized = false` at the end of the existing
guard-flag run (after `playtimeQueueDrainInitialized`, before `let i18nReady`), with its own
comment in the established shape: it guards `setGogPresenceWhenOnline()`'s CALL SITE, because
without it each repeated `init()` in a jest file would register another `runOnceWhenOnline(...)`
-- invoking the body again immediately when online, or stacking another `once('online')` listener
when offline. It lives at the call site, not inside the helper, so the new suite can call the
helper directly without owning a guard it cannot reset (same reasoning as
`playtimeQueueDrainInitialized`).

**1c. The helper.** Add `export function setGogPresenceWhenOnline(): void` immediately after
`checkRosettaWhenMac()` (i.e. after `:587`'s function, before `export function init(`).

Structure -- three layers, matching Blocks E/G:
- outer `try { runOnceWhenOnline(() => { ... }) } catch (error) { logWarning(...) }`
- a SYNCHRONOUS arrow callback (not `async`) with its OWN inner `try/catch`
- inside it, `gogPresence.setPresence()` with an explicit
  `.catch((error: unknown) => logWarning(...))`

The two new catch-arm diagnostics take this file's local `[bootstrap] ` prefix and name
`setGogPresenceWhenOnline`. There is no ported log literal in this block, so the verbatim-log house
rule from Blocks E/G does not apply here -- say that, so a reader does not go looking for a
verbatim string that was never in the deleted source.

**LOAD-BEARING CALL SHAPE:** call it as `gogPresence.setPresence()` -- a property access on the
default-export object, resolved at call time. Do NOT destructure (`const { setPresence } =
gogPresence`) and do NOT pass the bare reference `runOnceWhenOnline(gogPresence.setPresence)` as
the deleted source did. Two reasons, both of which belong in the comment: (i) the arrow wrapper is
what makes the inner try/catch and the explicit `.catch` possible at all -- Block E made the same
sync-arrow deviation from its deleted `async` callback for the same reason; (ii) Task 2's
`jest.spyOn(gogPresence, 'setPresence')` can only intercept a call-time property access, so a
destructured binding would make the wiring proof silently measure nothing.

**Be honest in the header about what the guards are and are NOT.** `setPresence()` today wraps its
whole body in `try/catch` and logs+swallows, so it cannot reject as written and the inner guards
are UNREACHABLE against the current implementation. They are defence-in-depth for the CALL SITE --
the shape Blocks E and G established, and insurance against a future `presence.ts` refactor that
lets a rejection out. Do not write a sentence claiming `setPresence` can reject today; that would
be a false rationale, which this file's history treats as worse than a missing one. DO record the
standing `runOnceWhenOnline` argument that makes two layers structurally necessary regardless: when
offline, the callback body runs on a LATER turn, outside the stack frame of any `try` wrapping the
`runOnceWhenOnline(...)` call, so the outer try covers only REGISTRATION.

Also encode D-K5X-01 and D-K5X-02 from the `<decisions_settled_at_planning_time>` block above --
including the explicit refutations (why Blocks D/E/F/G are each NOT a constraint) and the Block-G-D5
vs Block-H contrast. State plainly: "the only constraints are the generic three."

Provenance line, same shape as Blocks E/F/G: deleted with `src/backend/main.ts` in commit
`5643c7583` ("feat(35-14)!: delete the Electron entry points"), old call site `main.ts:477`, todo
`2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md`, quick-260909-k5x.
Include the one-line KEEP-ALIVE note: `interval` is `undefined` at process start, so this first
call is also what arms `presence.ts`'s 5-minute `setInterval` -- Block H closes both halves of the
todo's title, not just the first.

**OUT OF SCOPE note for the header** (mirroring Block G's own D13 candour): `deletePresence()`
calls `clearInterval(interval)` but never resets `interval` to `undefined`, so after any
`deletePresence()` the `if (!interval)` guard stays falsy-blocked and the keep-alive cannot re-arm
within the same process. That is a REAL, SEPARATE defect, filed as its own todo by Task 3 and
deliberately NOT fixed here. Do not touch `presence.ts`.

**1d. Call site.** Append after Block G's call site, before `output.write(\`${READY_SENTINEL}\n\`)`:

  `if (!gogPresenceInitialized) { gogPresenceInitialized = true; setGogPresenceWhenOnline() }`

with a `// Block H -- boot-time GOG presence ...` comment recording: the generic-three placement
(and, explicitly, that there is NO load-bearing ordering constraint here, unlike Block G -- say it
in the negative so a reader does not go hunting for one), and that the helper never fails boot per
its own header so no extra try/catch is needed at the call site, matching Blocks D/E/F/G.

**1e. Rewrite the stale D13 note at `:516-519`.** It currently reads "is a SEPARATE, still-open
todo ... Not ported here, not touched here -- do not import `gogPresence` from this function."
Both clauses go stale the moment 1a-1d land. Rewrite it -- do NOT delete it; the scoping decision
made at Block G's time is still a real record. The replacement must:
  - keep the record that this was deliberately out of Block G's scope at the time it was written;
  - state that the todo was closed by quick-260909-k5x and ported as **Block H**
    (`setGogPresenceWhenOnline()`), naming it so the forward pointer is greppable;
  - CORRECT the now-false import prohibition: the MODULE now imports `gogPresence` at file scope
    (1a), so the surviving statement is narrower -- `syncQueuedPlaytimeWhenOnline()` itself still
    does not use `gogPresence` and is still not its caller; the two blocks are independent
    boot-time side effects with independent call sites in `init()`. Leaving the old absolute "do
    not import" wording would make this comment invert its own file.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/backend/sidecar/bootstrap.ts','utf8'); const b=s.split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n'); const need=[/export function setGogPresenceWhenOnline/,/gogPresence\.setPresence\(\)/,/let gogPresenceInitialized = false/,/if \(!gogPresenceInitialized\)/,/import gogPresence from '\.\.\/storeManagers\/gog\/presence'/]; const miss=need.filter(r=>!r.test(b)); if(miss.length){console.error('MISSING in code (comments stripped):',miss.map(String));process.exit(1);} if(/const \{\s*setPresence\s*\}\s*=\s*gogPresence/.test(b)){console.error('FORBIDDEN: destructured setPresence defeats the Task 2 spy seam');process.exit(1);} const h=b.indexOf('export function setGogPresenceWhenOnline'); const body=b.slice(h,h+900); if(/getSettings\(\)/.test(body)){console.error('FORBIDDEN: settings read inside Block H violates D-K5X-02');process.exit(1);} if(/still-open todo/.test(s)){console.error('STALE: the D13 note still says still-open');process.exit(1);} if(!/Block H/.test(s)){console.error('D13 note does not point forward to Block H');process.exit(1);} const i=b.indexOf('gogPresenceInitialized = true'), r=b.indexOf('READY_SENTINEL}'); if(i<0||r<0||i>r){console.error('Block H call site is not before the READY_SENTINEL write');process.exit(1);} console.log('OK');"</automated>
    <automated>pnpm codecheck</automated>
  </verify>

  <done>
`bootstrap.ts` exports `setGogPresenceWhenOnline()`, `init()` calls it exactly once behind
`gogPresenceInitialized` after Block G and before the `READY_SENTINEL` write, no settings are read
inside Block H, `setPresence` is invoked as a call-time property access, and the `:516-519` D13
note no longer describes the item as still-open. `pnpm codecheck` is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dedicated suite + containment-census registration, with mutation proofs</name>
  <files>src/backend/sidecar/__tests__/gogPresenceBootWire.test.ts, src/backend/sidecar/__tests__/testContainment.test.ts</files>

  <behavior>
    - case 1 (wiring, via `init()`): `init()` calls `gogPresence.setPresence` exactly once.
    - case 2 (direct, enabled): `setGogPresenceWhenOnline()` calls it exactly once.
    - case 3 (D-K5X-02 receipt): with `disableGOGPresence: true` mocked into `GlobalConfig`, the
      helper STILL calls `setPresence` and emits no skip-log -- the gate is inside `setPresence`.
    - case 4 (never-fails-boot, rejection): a rejecting `setPresence` does not throw out of the
      helper and produces a `[bootstrap] setGogPresenceWhenOnline` warning after a microtask flush.
    - case 5 (never-fails-boot, INNER guard): with `runOnceWhenOnline` mocked to CAPTURE rather
      than invoke, a synchronously-throwing `setPresence` invoked from the test's own frame does
      not throw and produces the same warning.
  </behavior>

  <action>
**2a. Create `src/backend/sidecar/__tests__/gogPresenceBootWire.test.ts` by mirroring
`playtimeQueueBootDrain.test.ts`.** Read that file first and copy its structure; do not invent a
harness. Specifically carry over, unchanged in kind:

- `jest.mock('backend/store_backend', () => ({ __esModule: true, default: jest.requireActual('../fileStore').default }))`
- the `axios` factory mock (`head` resolving `{ status: 200 }`, plus `get`/`create`) -- REQUIRED
  because `init()` runs `initOnlineMonitor()`'s `pingSites()`.
- the online_monitor factory mock, spreading `jest.requireActual('../../online_monitor')` and
  overriding `initOnlineMonitor: jest.fn()`, `isOnline: jest.fn(() => true)`, and
  `runOnceWhenOnline: jest.fn((cb: () => unknown) => cb())` (invoke INLINE).
- a `beforeEach` that RE-ARMS `mockedRunOnceWhenOnline.mockImplementation((cb) => cb())`.
  `src/backend/jest.config.js` sets `resetMocks: true`, which strips the implementation off every
  `jest.fn(...)` -- including one supplied at module-factory creation time -- before every single
  test, the first one included. Without the re-arm the whole suite is vacuous. Case 5 overrides
  the implementation locally.

Imports after the mocks: `PassThrough` from `'node:stream'`,
`{ init, setGogPresenceWhenOnline } from '../bootstrap'`, `* as loggerModule from '../../logger'`,
`gogPresence from '../../storeManagers/gog/presence'`, `{ GlobalConfig } from '../../config'`,
`{ runOnceWhenOnline as mockedRunOnceWhenOnline } from '../../online_monitor'`.

Spy shape for every case: `jest.spyOn(gogPresence, 'setPresence')`, `.mockRestore()` at the end of
each test.

**Two bans, both stated in the file's own docstring:**
- NO `expect(runOnceWhenOnline).toHaveBeenCalled()` anywhere. Every assertion must be on an effect
  that only occurs INSIDE the callback (the `setPresence` spy's call count). That is the vacuous
  shape a standing project finding warns about, and it is banned in this file family -- if the
  inline-invoke mock silently stopped invoking, the spy would show zero calls and the suite would
  go RED, which is the point.
- NO reference to the `'node:os'` specifier anywhere in the file.
  `structuralContainment.test.ts`'s gate convicts any backend `.ts` file that mentions `node:os`
  AND either `homedir` or `userInfo` in its comment-stripped source. Containment is already
  structural via `src/backend/jest.setupContainment.ts` (registered as `setupFiles` for the whole
  `Backend` project), so no `jest.mock('os', ...)` is needed or wanted.

Docstring must also record WHY this is a dedicated file rather than an addition to
`bootstrap.test.ts`/`bootstrapWirings.test.ts`: both already call `init()` early in their own
files, so the module-scope `gogPresenceInitialized` guard would already be consumed by the time an
appended describe ran -- a wiring test placed there could only ever measure a consumed guard. The
`jest.resetModules()` + `require('../bootstrap')` alternative is rejected too: it re-evaluates
`installElectronHook`, which has no idempotency guard of its own. Same reasoning
`playtimeLockBootClear.test.ts` and `bootstrapUserReconcile.test.ts` record for their own
existence.

Cases 1-5 exactly as listed in `<behavior>`. Case 1 drives `init(new PassThrough(), new PassThrough())`.
Case 3 uses `jest.spyOn(GlobalConfig, 'get').mockReturnValue({ getSettings: () => ({ disableGOGPresence: true, disablePlaytimeSync: true }) } as unknown as GlobalConfig)`, asserts the
spy WAS called, and asserts no `logDebug` skip-style line was emitted -- with an inline comment
naming D-K5X-02 and stating that this test is what turns RED if someone later "helpfully" hoists a
settings read to helper entry. Case 5 overrides `mockedRunOnceWhenOnline` locally to
`(cb) => { capturedCallback = cb }`, asserts the spy was NOT called during registration, then
`expect(() => capturedCallback?.()).not.toThrow()` -- this is the only shape that can exercise the
inner guard, because an inline-invoke mock lets the OUTER try catch a synchronous throw and would
pass even with the inner guard deleted.

**2b. Register the new suite.** `testContainment.test.ts`'s `T-34.2-83` (near `:1091`) is a
SET-EQUALITY census: it `readdirSync`s this directory and requires every `*.test.ts` to appear in
exactly one of `IN_SCOPE_SUITES` (4 entries, gated at that length, and each required to carry a
`jest.mock('../pathShim', ...)` this suite has no reason to make) or
`STRUCTURALLY_CONTAINED_SUITES` (declared at `:876`, alphabetically sorted, 61 entries today).
Insert `'gogPresenceBootWire.test.ts'` alphabetically into `STRUCTURALLY_CONTAINED_SUITES`.
Skipping this turns the census RED with `unclassified: ['gogPresenceBootWire.test.ts']`.

**2c. Mutation proofs -- required, and each must be RECORDED in the SUMMARY with the observed
red/green.** Apply each mutation, run the suite, confirm the named case goes RED, revert:
  1. Delete the Block H call site from `init()` -> case 1 RED.
  2. Delete the INNER `try/catch` from the callback -> case 5 RED (and case 1 still green, which is
     the whole reason case 5 exists).
  3. Hoist a `GlobalConfig.get().getSettings()` read with an early return on `disableGOGPresence`
     to helper entry -> case 3 RED.
  4. Destructure `const { setPresence } = gogPresence` at import and call the bare binding ->
     cases 1/2/3 RED (proves the spy seam is real, not decorative).
  </action>

  <verify>
    <automated>npx jest --selectProjects Backend --runTestsByPath src/backend/sidecar/__tests__/gogPresenceBootWire.test.ts src/backend/sidecar/__tests__/testContainment.test.ts src/backend/sidecar/__tests__/structuralContainment.test.ts src/backend/sidecar/__tests__/bootstrap.test.ts src/backend/sidecar/__tests__/bootstrapWirings.test.ts src/backend/sidecar/__tests__/bootstrapUserReconcile.test.ts src/backend/sidecar/__tests__/playtimeQueueBootDrain.test.ts src/backend/sidecar/__tests__/electronUntouched.test.ts 2>&1 | tail -30</automated>
    <automated>npx jest --selectProjects Backend --runTestsByPath src/backend/sidecar/__tests__/gogPresenceBootWire.test.ts 2>&1 | grep -E "Tests:" | grep -qE "5 passed" && echo "ANTI-VACUITY OK: 5 cases ran" || (echo "ANTI-VACUITY FAIL: the suite did not run 5 passing cases"; exit 1)</automated>
    <automated>npx eslint src/backend/sidecar/bootstrap.ts src/backend/sidecar/__tests__/gogPresenceBootWire.test.ts src/backend/sidecar/__tests__/testContainment.test.ts 2>&1 | tee /dev/stderr | grep -c " error " | { read n; [ "$n" -eq 0 ] && echo "ESLINT: 0 errors" || (echo "ESLINT: $n errors"; exit 1); }</automated>
    <automated>npx prettier --check src/backend/sidecar/bootstrap.ts src/backend/sidecar/__tests__/gogPresenceBootWire.test.ts src/backend/sidecar/__tests__/testContainment.test.ts</automated>
  </verify>

  <done>
All eight named suites pass. The new suite runs exactly 5 cases, is registered in
`STRUCTURALLY_CONTAINED_SUITES`, contains no `expect(runOnceWhenOnline).toHaveBeenCalled()` and no
`node:os` reference. All four mutations were applied, observed RED on the named case, and
reverted -- results recorded in the SUMMARY. eslint reports 0 errors on the three touched files and
prettier is clean on them.

**Anti-regression bar, stated honestly:** the Backend project and repo-wide lint are known RED at
HEAD for unrelated reasons (standing findings `backend-suite-and-lint-are-red-at-head` and
`test-ci-red-from-leaked-store-embed-timer`). A fully green `pnpm test:ci` is NOT the bar and must
not be claimed. The bar is: the eight named suites pass, and the Backend project's failure count
does not increase versus a baseline `npx jest --selectProjects Backend --runInBand --silent` run
captured at HEAD before Task 1. Capture that baseline number first and quote both numbers in the
SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the todo, file the separate deletePresence() interval defect</name>
  <files>.planning/todos/pending/2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md, .planning/todos/pending/2026-09-09-deletepresence-never-resets-interval-so-the-keepalive-cannot-re-arm.md</files>

  <action>
**3a. Move and close the todo.** Use `git mv` (the established convention -- see `7aa263c6e`,
`e226e4b81`):

```
git mv .planning/todos/pending/2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md \
       .planning/todos/completed/2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md
```

Then rewrite its frontmatter, matching the shape of
`.planning/todos/completed/2026-09-06-queued-gog-playtime-never-drains-at-boot.md`:
- `status:` becomes a single quoted prose paragraph beginning
  `"RESOLVED 2026-09-09 by quick-260909-k5x -- ..."`. It must name: Block H
  (`setGogPresenceWhenOnline()` in `bootstrap.ts`); that it has NO load-bearing ordering
  constraint, only the generic three (D-K5X-01), stated as the deliberate finding it is rather
  than omitted; that NO settings read was hoisted to the call site, unlike Block G's D5, on port-
  fidelity grounds because `main.ts:477` was a bare call (D-K5X-02); the three guard layers; that
  BOTH halves of the title are closed, because `interval` is `undefined` at process start so the
  boot call is also what arms the 5-minute keep-alive; the new suite name and its 5 cases plus the
  4 mutations; and the NOT-in-scope `deletePresence()` interval defect with a pointer to the new
  todo filename filed in 3b.
- Add `resolved_by: quick-260909-k5x`.
- Keep `resolves_phase: null`, `severity: medium`, `platform: any`, `ready: code` as-is.
- If the `files:` line numbers have drifted, NOTE the drift in `status:` rather than silently
  correcting an untouched call site (the convention `7aa263c6e` set).
- Leave the body prose below the frontmatter unchanged. Its bundle-level evidence
  ("`setPresence` call sites in the bundle ... **No startup call.**") is a record of what was
  measured on 2026-09-06 and must not be retrofitted.

**3b. File the NEW todo** at
`.planning/todos/pending/2026-09-09-deletepresence-never-resets-interval-so-the-keepalive-cannot-re-arm.md`.

CI-enforced frontmatter (CLAUDE.md "Todo triage frontmatter"): `severity:`, then `platform:`, then
`ready:`, in that order, bare lowercase unquoted values. `pnpm planning-gates` scans `pending/`
only and turns red on omission.

```yaml
---
created: 2026-09-09
title: "deletePresence() clears the keep-alive interval but never resets `interval`, so setPresence()'s `if (!interval)` guard stays falsy-blocked and the 5-minute keep-alive can never re-arm in the same process"
area: gog-presence
severity: medium
platform: any
ready: code
source: "quick-260909-k5x triage -- found while porting the boot-time presence call as Block H; deliberately NOT fixed there to keep that task atomic"
files:
  - src/backend/storeManagers/gog/presence.ts (deletePresence's clearInterval, ~:82; setPresence's `if (!interval)` arm, ~:38-40)
resolves_phase: null
---
```

Body must record, plainly:
- The mechanism: `let interval: NodeJS.Timeout` is module scope. `deletePresence()` calls
  `clearInterval(interval)` but leaves `interval` holding the now-dead Timeout object, which is
  truthy. `setPresence()`'s `if (!interval)` is therefore false forever after, so no replacement
  interval is ever created.
- The reachable path: `presence.ts`'s module-scope `settingChanged` listener calls
  `deletePresence(true)` when `disableGOGPresence` flips true, and `setPresence()` when it flips
  back to false. After one off->on cycle, presence is posted once and then never refreshed for the
  remaining life of the process. `launcher.ts:289`'s game-stop path is a second route in.
- Blast radius and workaround: bounded (presence goes stale rather than wrong), workaround is an
  app restart. That is why this is `medium`, not `major`.
- The likely fix, NOT applied here: null out `interval` after `clearInterval` (the declaration
  would need to become `let interval: NodeJS.Timeout | undefined`), and check whether
  `deletePresence`'s own early-return guards mean `clearInterval` is itself sometimes skipped when
  it should not be.
- An explicit note that quick-260909-k5x's Block H is unaffected: at process start `interval` is
  genuinely `undefined`, so the boot call DOES arm the keep-alive. This defect only bites after a
  subsequent `deletePresence()`.
- The scope boundary: `presence.ts` was deliberately not refactored by quick-260909-k5x.

**3c.** Run `graphify update .` per CLAUDE.md, and note in the SUMMARY that
`graphify-out/graph.html` is deleted by `graphify update` (standing finding) if that occurs.
  </action>

  <verify>
    <automated>test -f .planning/todos/completed/2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md && test ! -e .planning/todos/pending/2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md && grep -q "RESOLVED 2026-09-09 by quick-260909-k5x" .planning/todos/completed/2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md && grep -q "resolved_by: quick-260909-k5x" .planning/todos/completed/2026-09-06-gog-presence-never-set-at-startup-and-its-keepalive-never-arms.md && echo "CLOSE OK"</automated>
    <automated>F=.planning/todos/pending/2026-09-09-deletepresence-never-resets-interval-so-the-keepalive-cannot-re-arm.md; node -e "const l=require('fs').readFileSync(process.argv[1],'utf8').split('\n'); const i=n=>l.indexOf(n); const s=i('severity: medium'),p=i('platform: any'),r=i('ready: code'); if(s<0||p<0||r<0){console.error('missing a bare lowercase triage key: severity/platform/ready =',s,p,r);process.exit(1);} if(!(p===s+1&&r===p+1)){console.error('triage keys present but not adjacent in order severity,platform,ready:',s,p,r);process.exit(1);} console.log('TRIAGE FRONTMATTER OK');" "$F"</automated>
    <automated>pnpm planning-gates</automated>
    <automated>git status --porcelain .planning/todos/ | grep -qE "^R|^A" && echo "git sees the move/add" || (echo "git does not show the rename/add -- was git mv used?"; exit 1)</automated>
  </verify>

  <done>
The closed todo lives in `completed/` with a `RESOLVED 2026-09-09 by quick-260909-k5x` status
paragraph naming D-K5X-01, D-K5X-02, the three guard layers, the keep-alive half, and the new
sibling todo; the new `deletePresence()` interval todo exists in `pending/` with `severity:`,
`platform:`, `ready:` adjacent and in that order with bare lowercase values; `pnpm planning-gates`
passes; the graph is updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar -> `presence.gog.com` | Outbound authenticated POST carrying a GOG bearer token, now fired at boot instead of only after a game launch. |
| GOG settings (`disableGOGPresence`, `disablePlaytimeSync`) -> presence posting | The user's opt-out gate. Block H must not weaken or bypass it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-K5X-01 | Information disclosure | `setGogPresenceWhenOnline` -> `setPresence` | mitigate | The opt-out gate is NOT moved or duplicated. `setPresence`'s own `disableGOGPresence \|\| disablePlaytimeSync \|\| !GOGUser.isLoggedIn() \|\| !isOnline()` early-return remains the sole gate, evaluated at online time (D-K5X-02). Task 2 case 3 pins that Block H itself reads no settings, so a later refactor cannot accidentally split the gate across two places and leave one arm stale. |
| T-K5X-02 | Information disclosure | new `logWarning` catch arms | mitigate | Both diagnostics interpolate `String(error)` only. Do NOT log the payload, the `credentials.user_id`, or the bearer token. `setPresence`'s own `logError` already handles the network-error case internally. |
| T-K5X-03 | Denial of service | boot path | mitigate | Three layers (outer try around registration, inner try around the deferred callback body, explicit `.catch` on the floated promise) mean no throw or rejection on this path can abort sidecar boot. Task 2 cases 4 and 5 prove both arms, and mutation 2 proves case 5 is load-bearing rather than decorative. |
| T-K5X-04 | Denial of service | repeated `init()` | mitigate | `gogPresenceInitialized` at the call site prevents stacked `once('online')` listeners across the many `init()` calls jest makes per file, which would otherwise fan out into N presence POSTs per connectivity event. |
| T-K5X-05 | Tampering | supply chain | accept | Zero package installs in this task. `presence.ts` is already resident in the sidecar bundle via `utils.ts:43` and `launcher.ts:77`; the new import is a second binding onto an already-resident singleton, not a new dependency edge. No `npm`/`pip`/`cargo` install occurs, so no legitimacy audit is required. |
</threat_model>

<verification>
1. `pnpm codecheck` clean.
2. The Task 1 grep gate passes (comment-stripped, so this file's own prose cannot self-satisfy it).
3. The eight named suites pass; the new suite runs exactly 5 cases.
4. All four mutations observed RED on their named case and reverted -- recorded in the SUMMARY.
5. eslint reports 0 errors and prettier is clean on the three touched source files.
6. `pnpm planning-gates` passes.
7. Backend project failure count did not increase versus the pre-Task-1 baseline (both numbers
   quoted in the SUMMARY).
</verification>

<success_criteria>
- GOG presence is set at sidecar boot once online, and the 5-minute keep-alive is armed by that
  same call -- both halves of the todo's title closed.
- Block H is documented with honest constraints: the generic three named, the four candidate
  load-bearing constraints explicitly refuted, and no invented one.
- D-K5X-02 is implemented (no hoisted settings read, no invented skip-log) and pinned by a test
  that goes RED if hoisted.
- The `:516-519` D13 note is rewritten -- still records Block G's scoping decision, no longer
  claims the item is still-open, no longer carries the now-false absolute "do not import
  `gogPresence`" prohibition.
- `presence.ts` is untouched; its `interval` defect is filed as a separate `pending/` todo with the
  three CI-enforced triage keys in order.
- The closed todo is in `completed/` via `git mv` with a RESOLVED status paragraph and
  `resolved_by:`.
</success_criteria>

<output>
Create `.planning/quick/260909-k5x-port-boot-time-gog-presence-call-into-si/260909-k5x-SUMMARY.md`
when done. It must record: the two settled decisions and their rationale, the four mutation results
(each with the case that went RED), the pre/post Backend failure counts, and the new todo's
filename.
</output>
