---
quick_id: 260816-i8a
description: Close three stale Steam todos and shrink the GPTK todo to its one remaining gap
date: 2026-08-16
mode: quick
tasks: 2
---

# Quick Task 260816-i8a — Steam todo-ledger reconciliation

## Why

A review of all 8 `area: steam` todos in `.planning/todos/pending/` (prompted by "do the
todos 34.14 created") checked each todo's claims against current source. Three turned out
to be stale — the work landed, or landed differently, and nobody closed the ledger entry. A
fourth shrank from a multi-part defect to a single missing guard.

This is **ledger housekeeping only**. No source file is modified by this task.

**Why it matters beyond tidiness:** a stale todo filed as `priority: high` and
`blocks: <phase>` is actively misleading input to future planning. `steam-getproductinfo-appinfo-dump`
claims to block a phase that shipped, and asks for a parser whose premise the codebase
already disproved.

## Scope boundary

- MOVE 3 files `pending/` → `completed/`, each with an appended `## Resolution 2026-08-16`
  section citing the source evidence.
- APPEND an update section to 1 file that STAYS in `pending/`.
- Do NOT touch `src/`. Do NOT close the four todos verified as still-valid
  (`2026-08-16-steam-sync-does-not-capture-platforms`, `2026-08-16-steam-syncing-spinner-has-no-failure-state`,
  `2026-08-16-absent-is-mac-native`, `2026-08-15-32bit-mac-steam-titles-orphan`).

## Tasks

### Task 1 — Close the three stale todos

**Files:**
- `.planning/todos/pending/steam-getproductinfo-appinfo-dump.md` → `completed/`
- `.planning/todos/pending/steam-startup-download-resume-autoopens-crossover.md` → `completed/`
- `.planning/todos/pending/2026-07-18-productionize-macos-native-steam-bridge-out-of-process-steam.md` → `completed/`

**Action:** `git mv` each to `.planning/todos/completed/`, then append a
`## Resolution 2026-08-16` section recording the verdict AND the source evidence it rests on.
Each resolution must name file:line evidence so a future reader can re-check the call rather
than trust it.

Verdicts:

1. **`steam-getproductinfo-appinfo-dump`** — STALE, premise inverted. The dump happened and
   the answer was that `osarch` is NOT a usable mac-arch signal. Evidence: `library.ts:1155-1162`
   ("Steam's manual osarch metadata proved absent/unreliable on every macOS launch entry
   (18-01 finding, retired)"); shipped detector is `machOArchsOf` (`library.ts:1173`, `lipo -archs`),
   the only detector that may assert `mac_arch === '32'`; captured fixtures at
   `src/backend/storeManagers/steam/__tests__/fixtures/appinfo-64bit.json`. Record the
   near-miss: `depot/select.ts:195-198` DOES read `osarch`, but for depot selection — a
   different purpose, not the mac-arch parser this todo wanted.

2. **`steam-startup-download-resume-autoopens-crossover`** — STALE, fixed more completely
   than requested. The todo asked for a notify/confirm gate before auto-resume; auto-resume
   was removed outright. Evidence: `library.ts:536-584` sets `install.steamResumePending: true`,
   pushes to the frontend, fires `steam.resumeAvailable.notify`, and logs
   "surfacing as resumable, NOT auto-resuming". Actual resume moved to
   `resumeInterruptedSteamInstall()`, reachable only from an explicit user Install click.

3. **`2026-07-18-productionize-macos-native-steam-bridge`** — SUPERSEDED by Phase 24, whose
   ROADMAP entry cites this todo file by name as its own source. 16/17 plans shipped. Map the
   todo's own numbered list to what closed it: item 1 (C++ vtable ABI, the todo's "right next
   frontier spike") → spike 006 + plan 24-01; item 4 (persistent channel) → 24-02; item 5
   (packaging / `zig cc` PE shim) → 24-07; routing → 24-08. Implementation at
   `src/backend/storeManagers/steam/bridge/` and `native/steam-bridge/`. Residual is
   interface/API-breadth coverage, tracked separately as an already-deferred item — say so in
   the resolution rather than leaving this todo open to carry it.

**Verify:** all three files exist under `completed/` and are gone from `pending/`; each has a
`## Resolution 2026-08-16` section; `git status` shows renames, not delete+add.

**Done:** `ls .planning/todos/pending/ | grep -c steam` drops from 8 to 5.

### Task 2 — Shrink the GPTK broken-bottle todo

**File:** `.planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md` (STAYS pending)

**Action:** Append `## Update 2026-08-16 — mostly closed by Phase 34.13`. Since the todo's own
2026-08-14 note, three things landed that it does not know about:

- `getSteamBottleSettings()` self-heals a non-CrossOver engine to CrossOver **and re-persists
  the correction** (`bottle.ts:310-336`, 34.13 review A-21)
- `resolveSubmittedBottleEngine` filters at the wizard's SUBMIT boundary
  (`steamBottleDefaults.ts:164-172`, review C-03)
- `persistBottleWineVersion` logs a warning on a non-CrossOver write (`bottle.ts:371-377`)

The only thing still open is the `provisionBottle` mirror of `provisionBridgeBottle`'s D-08
guard: `provisionBottle` persists `opts.wineVersion` unchecked at `bottle.ts:702-704`, where
its sibling rejects at `bottle.ts:1166-1175`. `steamBottleDefaults.ts:157-162` names this exact
gap in a KNOWN REMAINING GAP comment.

**Downgrade the impact assessment** and say why: because the self-heal corrects the value
whenever CrossOver is on disk — and Steam bottling cannot function when it is not — this is now
defense-in-depth, not a live failure path. Option (b) (prefix-based GPTK provisioning) remains
out of scope.

**Verify:** file still in `pending/`; new section present; original content unmodified above it.

**Done:** the todo reads as a one-guard task, not the multi-part defect it was filed as.

## Non-goals

- No `src/` changes. The `provisionBottle` guard itself is NOT implemented here.
- No changes to the four still-valid Steam todos.
- No ROADMAP.md changes (quick tasks record in STATE.md only).
