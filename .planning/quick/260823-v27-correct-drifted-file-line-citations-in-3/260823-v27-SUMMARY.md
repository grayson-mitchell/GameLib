---
quick_id: 260823-v27
description: Correct drifted file:line citations in 34.9-REVIEW-CYCLE2.md
completed: 2026-08-23
status: complete
commits:
  - dc51cd62d  # citation amendment + inline [now ...] markers
---

# Quick Task 260823-v27 — Summary

## Outcome

All 21 `file:line` citations in `34.9-REVIEW-CYCLE2.md` were audited against the current tree.
**12 had drifted; 9 were still correct.** All 12 are now annotated in place. Nothing was
overwritten.

## The decision that shaped the fix

**Every drifted number was correct when written.** Verified against `package.json` at the review's
own commit `b5e72d3a2` (2026-08-12) — `:44` really was `release:mac`, `:69` really was
`build-steam-bridge`, and so on. This is drift, not error, so the fix is annotation, not
replacement, matching `34.9-GUARD-PROOF.md`'s AMENDMENT v2 (placed *before* §3, with §3-5 left
unrewritten so the original transcript stays interpretable).

## Two mechanisms, deliberately treated differently

**A. `package.json` scripts shifted down 2-3 lines** (`:44`→`:46`, `:49`→`:51`, `:69`→`:72`,
`:70`→`:73`, `:71`→`:74`, `:72`→`:75`).

For **C2-05 — the one finding still open** — the new numbers are given plainly, because a reader
acting on that finding needs to land on live code.

For **C2-01/C2-02/C2-03 the markers say NAVIGATION ONLY.** Those findings quote the
`esbuild … | node` / `| node -` pipe idiom inline. That idiom no longer exists anywhere — every
script now routes through `node meta/runTs.cjs`, and its removal *is* the fix (all three read
FIXED, proven by `34.9-PIPE-PROOF.md` PASS 36/36). Repointing them unqualified would have asserted
a defect at a line that does not have it — the stale-by-behaviour shape, where a citation is
positionally right and semantically wrong.

**B. `meta/cleanDistMac.*` was RENAMED — a file-level miss, not a line-level one.** Quick task
`260822-hrf` renamed it to `meta/cleanDist.*` (`df7af9f4a`, `ab1ee0448`) while closing ledger item
11. Two citations named a path that no longer exists, and both ranges had moved
(`:208-232`→`cleanDist.test.ts:402-449`, `:234-276`→`:451-497`). **This is the part a
line-numbers-only pass would have missed** — the user asked about line numbers, and the more
serious drift was in the filename.

## Verified

- **Proven purely additive by measurement, not by reading the diff:** stripping the new amendment
  section and every inline `*[now …]*` / `*[renamed …]*` marker reproduces the pre-edit file
  **byte-for-byte**. No original citation, quoted script text, or finding body was lost.
- `node 34.9-REVIEW-SWEEP-CHECK.cjs` → 24/24 mapped, unmapped 0, exit 0.
- `pnpm planning-gates` → 7/7.
- `disposition: partial` and `status: issues_found` untouched. This task fixed navigation, not
  disposition — C2-05 is still the sole outstanding finding and the badge is still correctly yellow.

## Left alone deliberately

- **Frontmatter `files_reviewed_list` still names `meta/cleanDistMac.ts` and
  `meta/__tests__/cleanDistMac.test.ts`.** It records what was reviewed on 2026-08-12, and those
  were the paths that existed. The amendment section says so explicitly.
- **9 citations verified correct and not touched:** `build-base.yml:47-53`/`:48`,
  `build-runners-onedir-macos.yml:107`, `electron-builder.yml:64-79`,
  `verifyRunnerBundle.ts:172-181` and `:459-470`, `preserveRunnerSymlinks.ts:121-138`,
  `preserveRunnerSymlinks.test.ts:30` and `:257-271`. The two `verifyRunnerBundle.ts` ranges
  survived because they sit *inside* `inspectFramework` and `summarise`, whose own openings moved
  (`:150`, `:427`) — the ranges happen to still land in the right function.

## Not done — scope stated, not silently widened

`34.9-REVIEW.md` and cycles 3/4/5 were **not** audited for the same drift, nor was
`deferred-items.md`. Cycle 2 was reviewed on 2026-08-12 and the tree has moved a lot since; the
others are likelier to carry it than not. That is its own task.
