---
phase: quick-260911-s4f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md
  - .planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-12-evidence/
  - .planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-LIVE-GATE.md
  - .planning/STATE.md
autonomous: true
requirements: [REQ-43-19]

must_haves:
  truths:
    - "A reader of 43-LIVE-GATE.md can tell, for every row in the verdict table, which of the three runs it belongs to and whether it is live or superseded"
    - "The GAME header offset is filed as its own todo with the four ruled-out hypotheses recorded, so nobody re-derives them"
    - "The verdict count in the document is the output of the document's own census command run against the edited table, not a predicted delta"
    - "Every file 43-LIVE-GATE.md cites for run 3 exists inside .planning/ and survives the loss of /tmp"
    - "STATE.md's prior stopped_at narrative is byte-for-byte present after the edit"
    - "The quick task appears in STATE.md's Quick Tasks Completed table in quick_id order"
  artifacts:
    - path: ".planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md"
      provides: "The sole remaining Phase 43 blocker, filed with evidence and ruled-out hypotheses"
      contains: "ready: code"
    - path: ".planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-12-evidence/measurements-run3.md"
      provides: "Run-3 source of truth, preserved out of /tmp"
    - path: ".planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-LIVE-GATE.md"
      provides: "Three-run verdict table with a recounted VERDICT line"
      contains: "run 3"
  key_links:
    - from: ".planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-LIVE-GATE.md"
      to: ".planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md"
      via: "named by filename under the VERDICT line as the sole remaining blocker"
      pattern: "humble-keys-game-column-header-label-sits-5px-left-of-row-titles"
    - from: ".planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-LIVE-GATE.md"
      to: "43-12-evidence/"
      via: "run-3 provenance blockquote cites the preserved copies, not /tmp"
      pattern: "43-12-evidence"
---

<objective>
Transcribe REQ-43-19 live gate **run 3** (session `20260911T075450Z`) into
`43-LIVE-GATE.md`, preserve its evidence into `43-12-evidence/`, and file the one finding
run 3 exposed that has no home yet: the Humble Keys `GAME` column header label sits ~5.5 CSS
px left of the row titles.

Purpose: run 3 flipped item 2 FAIL to PASS and re-confirmed items 4 and 6, leaving exactly
one open question on Phase 43. The record has to say which run each number came from, has to
survive `/tmp` being wiped, and has to report a count it actually executed rather than one it
predicted.

Output: one new todo, one new evidence directory, an edited gate document with a recounted
verdict, and two STATE.md updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

Source of truth for every number in this plan — **transcribe, never re-derive**:
`/tmp/gamelib-gate-20260911T075450Z/measurements-run3.md`

The document being edited:
`.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-LIVE-GATE.md`
</context>

<hard_constraints>
These are not preferences. Violating any one of them invalidates the record this plan exists
to produce.

1. **No re-measurement.** Do not build, do not launch the app, do not drive it. GameLib is
   running as PID 70156 and must be left alone. Every number comes out of
   `measurements-run3.md`. If a number you want is not in that file, the answer is "run 3 did
   not measure it" — write that, do not go get it.
2. **Do not change the ruler after seeing the result.** Item 3 is scored against
   header-label-left-edge vs title-left-edge, threshold ±2. Run 3 measures 5.5. That is a
   **FAIL**. Do not re-score it against the friendlier title-to-title agreement (1.0), which
   passes. Runs 1 and 2 both refused this exact temptation and the document records that
   refusal twice; run 3 must be consistent with it.
3. **Never use `gsd-sdk` to write STATE.md.** It corrupts the file. Hand-edit only.
4. **Do not stage the working tree wholesale.** `.claude/skills/archify/` and
   `skills-lock.json` are untracked and are not part of this task. `git add` explicit paths
   only; no `git add -A`, no `gsd-sdk query commit`.
5. **No `|` characters inside any new gate-table cell text.** The census command splits on
   `|` and reads `$(NF-1)`; a stray pipe silently shifts the column it reads.
6. **No block scalars (`|-`, `>-`) in any frontmatter you write.** The SDK's hand-rolled
   parser has no block-scalar support and returns the literal two-character indicator,
   silently discarding the body. Single-line single-quoted scalars, apostrophes doubled
   (`''`), matching the house style already in STATE.md.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Preserve run-3 evidence into 43-12-evidence/, redaction-scan it, and file the GAME header offset todo</name>
  <files>
    .planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-12-evidence/ (new),
    .planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md (new)
  </files>
  <action>
**1a. Inspect every PNG before copying it.** Run 1 shipped a file named `capture-*.png`
that turned out to be a screenshot of VS Code; it was caught only by looking at it. For each
of `capture-1-probe.png`, `capture-2-top.png`, `capture-3-gog.png`, `crop-game-column.png` in
`/tmp/gamelib-gate-20260911T075450Z/`: downscale a throwaway copy into the session scratchpad
with `sips -Z 900 --out <scratchpad>/<name>` (keeps the 4 MB probe capture cheap to view),
then open the downscaled copy with the Read tool and actually look at it. Confirm each one
shows the GameLib window and matches what the gate document will claim it shows:
`capture-1-probe.png` the Library tab at launch, `capture-2-top.png` the near-black-theme key
list top, `capture-3-gog.png` the light-theme Racine/GOG row, `crop-game-column.png` the
annotated GAME-column crop. Record the verdict per file. If any one does not match, stop and
report rather than copying it.

**1b. Copy the eight evidence files** from `/tmp/gamelib-gate-20260911T075450Z/` into
`.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-12-evidence/`
(`43-10-evidence/` and `43-11-evidence/` are taken; `43-12-evidence/` is verified free):
`measurements-run3.md`, `capture-1-probe.png`, `capture-2-top.png`, `capture-3-gog.png`,
`crop-game-column.png`, `p1-precondition.log`, `gamelib-launch-1.log`, `terminal.log`. Do NOT
copy `GameLib.app/`. `/tmp` does not survive reboot and the gate document cites these paths.

**1c. P11 redaction scan** over everything copied in. P11 (`43-LIVE-GATE.md:49`) requires
scanning for key-shaped tokens — Humble redemption codes are dash-separated alphanumeric
groups — before any of this reaches `.planning/`. Scan the two logs and the measurements file
with a dash-separated-group pattern (e.g. `grep -nE '[A-Za-z0-9]{4,}-[A-Za-z0-9]{4,}-[A-Za-z0-9]{4,}'`).
Report every match and classify it: git SHAs, ISO timestamps, session directory names,
hyphenated game titles and CSS token names are benign; anything that looks like a redemption
code is not, and if one appears, redact it in the copy and say so. Report "what matched and
why each match is benign" explicitly — an empty report is not the same as a scan that found
nothing, and the record needs to show the scan ran.

**1d. File the todo** at
`.planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md`.

Frontmatter, following the house shape already used by
`.planning/todos/pending/2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md`
(read it first for the exact key order and quoting), with the three CI-enforced triage keys in
CLAUDE.md's mandated order — `severity:` then `platform:` immediately after, then `ready:`
immediately after that — bare, lowercase, unquoted:
`created: 2026-09-11`; `title:` (double-quoted) naming the ~5 CSS px offset and that the cause
is unestablished; `area: humble-keys-ui`; `status: OPEN`; `severity: minor`; `platform: any`;
`ready: code`; `source:` citing REQ-43-19 item 3, run 3 (`20260911T075450Z`),
`43-12-evidence/measurements-run3.md`, and that it was exposed by `e344f589d` fixing the
larger centring defect; `files:` listing
`src/frontend/screens/Humble/Keys/index.tsx`,
`src/frontend/screens/Humble/Keys/index.css`,
`src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx`;
`resolves_phase: null`.

Body, transcribed from `measurements-run3.md` lines 18-48:

- **Measured.** "Game" header label left edge **335.5**. Title left edges **341.0** (Aksun
  Playtest), **341.0** (Dredge), **340.0** (Fabledom), **340.5** (Persona 5 Royal), **341.0**
  (Warhammer 40,000: Rogue Trader), **341.0** (Settlement Survival). Offset **5.5 CSS px**
  against item 3's ±2 threshold.
- **Masked until `e344f589d`.** Run 1 measured a title-to-title spread of **216.5**; run 3
  measures **1.0**. Fixing the large centring defect exposed a small offset underneath it that
  was previously unmeasurable.
- **Ruled out, with evidence** — state these so a future reader does not redo the work:
  - Not antialiasing: the offset is stable at 4.5-5.0 across ink thresholds 28 / 60 / 100 / 140.
  - Not a systematic header-vs-row offset: TYPE header 220.0 vs row logo 220.0 (0.0); KEY
    header 934.5 vs KEY content 934.0 (0.5). GAME alone diverges.
  - Not markup: the header is three plain `<span>` grid items (`Keys/index.tsx:634-638`); the
    title is a plain `<span>` with no padding (`HumbleKeyRow/index.tsx:729`,
    `.humbleKeyRowTitle` at `Keys/index.css:344-350`).
  - Confirmed visually in `crop-game-column.png` (preserved at `43-12-evidence/`): every title
    sits exactly on the 341.0 rule while the header's "G" crosses left of it.
- **Cause NOT established.** Both elements should resolve to the same grid track. The
  remaining candidates need a DOM read, which a release build cannot provide — devtools are
  unreachable there by P2's own design, a structural finding this gate document already
  records. The next step is therefore a `tauri:dev` run with the inspector, **not** more pixel
  measurement. Say this plainly; the failure mode to prevent is someone spending another
  packaged-build capture session on it.
- **Severity justification.** `minor` is correct on its own terms — a ~5 px cosmetic
  misalignment in one column header, no functional impact, no data at risk. State plainly in
  the same breath that it is nevertheless **the sole remaining blocker on Phase 43 closing**,
  so the severity understates its schedule impact. Both facts, together, in the body.
  </action>
  <verify>
    <automated>
test -d .planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-12-evidence &&
test "$(ls .planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-12-evidence | wc -l | tr -d ' ')" = "8" &&
test ! -s /dev/null; for f in .planning/phases/43-*/43-12-evidence/*; do test -s "$f" || { echo "ZERO-LENGTH: $f"; exit 1; }; done;
grep -c '^severity: minor$' .planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md &&
grep -c '^platform: any$' .planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md &&
grep -c '^ready: code$' .planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md &&
grep -c '335\.5' .planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md
    </automated>
  </verify>
  <done>
`43-12-evidence/` holds 8 non-zero files; every PNG was visually inspected before copying and
the inspection verdict is reported per file; the P11 scan is reported with each match
classified; the todo exists with all three triage keys bare/lowercase/in CLAUDE.md's order,
carries the measured 335.5 / 341.0 figures, the four ruled-out hypotheses, the
"cause not established, next step is `tauri:dev` with the inspector" statement, and the
minor-but-sole-blocker severity justification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Transcribe run 3 into 43-LIVE-GATE.md and recount the verdict from the edited table</name>
  <files>.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-LIVE-GATE.md</files>
  <action>
Read the existing run-1/run-2 layering first — the verdict section starts at `## Verdict` (L323),
the table header is at L347, the census block at L384-L400, the VERDICT line at L419, the
deviations section at L472 with its run-2 subsection at L488, and the contract-defects section
at L523 with its run-2 subsection at L552. Run 3 layers onto run 2 exactly the way run 2
layered onto run 1. Follow that convention; do not invent a new one.

**2a. Retitle the verdict heading** to name all three runs. It MUST still begin with the
literal `## Verdict` — the census command's `awk '/^## Verdict/,/^### Note on item/'` range
depends on that prefix and on `### Note on item` still terminating the range. Do not move
either anchor, and keep every new table row inside that range.

**2b. Add a run-3 provenance blockquote** after the run-2 one, in the same voice. It records:
transcribed by quick task `260911-s4f` from
`/tmp/gamelib-gate-20260911T075450Z/measurements-run3.md`, preserved at
`43-12-evidence/measurements-run3.md`; HEAD `3ccc6e689`, carrying `2c68c17fe` (gift gate),
`c690a117a` (divider + `gogIcon`) and `e344f589d` (alignment); `gamelib-shell` sha256
`50d3c940…c3211`; what run 3 re-scored (items 2, 3, 4, 6) and that every other sub-check's
earlier value stands unrevisited.

**2c. Edit the table.** Run-3 rows carry `3` in the `Run` column. Rows that run 3 supersedes
keep their original measurement text **verbatim** and change only their `Result` cell to
`SUPERSEDED by run 3 (was <original result text>)`, exactly the shape run 2 used.

Supersede and replace:

- **Item 2, run-2 row (L352)** — Result becomes `SUPERSEDED by run 3 (was **FAIL**)`. New
  run-3 row: `"Key" header label left edge **934.5**, KEY content **934.0** on all 7 rows,
  spread **0.5 CSS px**` against ±2 → **PASS**. Note in the cell that this is the same literal
  header-vs-content metric run 2 failed at 151.5 divergence — the metric did not move, the
  layout did.
- **Item 4, run-2 row (L361)** — Result becomes `SUPERSEDED by run 3 (was PASS)`. New run-3
  row: near-black bg `[8,10,11]` seam `[41,47,49]` delta **38**; light bg `[237,239,244]` seam
  `[208,209,215]` delta **30**; 7 separators at 103/103 sampled columns → **PASS**. Record
  that four distinct theme backgrounds have now passed across runs 2 and 3 (deltas 29, 30, 31,
  38).
- **Item 6, run-2 row (L367)** — Result becomes `SUPERSEDED by run 3 (was PASS)`. New run-3
  row: GOG glyph (Racine) `[57,59,64]`; Steam glyphs (Darkest Dungeon, Satellite Reign, Dex)
  each `[57,59,64]`; `--text-secondary #393b41` = `[57,59,65]` — all within ±1 and identical
  to each other → **PASS**. Add that the bboxes independently corroborate the OPEN
  non-square-glyph todo (`2026-09-11-gog-logo-svg-renders-non-square-and-is-malformed.md`):
  GOG 19.0 × 17.5 vs Steam's square 19.0 × 19.0.
- **Item 3** — the two run-1 FAIL rows ("GAME left edge, logo row", spread 84.0; "GAME left
  edge, no-logo row", spread 216.5). `measurements-run3.md:28` pins run 3's title-to-title
  spread of **1.0** directly against run 1's **216.5**, so run 3 does supersede that
  measurement. Mark superseded whichever run-1 item-3 rows run 3's six-title sample actually
  covers, as `SUPERSEDED by run 3 (was **FAIL**)`. **`measurements-run3.md` does not state
  whether its six sampled rows include a generic / no-logo row.** Do not assert coverage the
  source does not evidence: whatever you supersede, the run-3 row's own text must say exactly
  which rows the six-title sample comprised and must flag that the logo/no-logo distinction is
  not recorded in the source. An honest limitation in the cell beats a clean-looking row that
  claims more than was measured.

  New run-3 item-3 row: `"Game" header label left edge **335.5**; six title left edges
  340.0-341.0, title-to-title spread **1.0 CSS px** (run 1: 216.5) — the centring defect
  `e344f589d` targeted is FIXED; header-vs-titles **5.5 CSS px**, EXCEEDS ±2` → **FAIL**. In
  the same cell, state that this is scored against the contract's literal metric and was
  **not** re-scored against title-to-title agreement after seeing the result, and that the
  cause is unestablished and now tracked by the new todo (name it by filename).

**2d. Add run-3 build-record rows** to the same table, matching the run-1/run-2 build-record
row shape:

- Build command, contract §3 verbatim
  (`pnpm exec vite build && pnpm build:sidecar-sea && pnpm build:decompress-worker-dev && pnpm exec tauri build`)
  → **exit 0** → PASS.
- Bundle path recovered: `.app` from the DMG, `bundle/macos/` emptied by tauri's own cleanup
  (P3); sha256 `50d3c940d9aa007325d60b31d85997bc121759d52505eb145c65c5a1b54c3211` identical
  both sides (P4) → PASS.
- `pgrep` before first launch: 0, `p1-precondition.log`; the run-2 instance was quit by the
  orchestrator (P1) → PASS.
- `pgrep` while running: 1, launch 1, exactly one instance; P2 release build with no
  `--debug` → PASS.
- P5 sync: `Humble sync finished: gamekeys=34 fetched=6/6 frozen=28 ok=6 schema_error=0` — the
  same 34 keys as runs 1 and 2, so all three runs are like-for-like → PASS.
- **Alignment declarations present in the embedded renderer bundle.** All three were confirmed
  present in `build/renderer` **before any measurement was taken** → PASS. Give this its own
  row and say why it exists: run 1's verdict was undermined by a build that predated a fix, and
  this pre-measurement check is the countermeasure that closed that hole. This is the row that
  makes run 3's numbers trustworthy; do not fold it into another row.
- `pgrep` after final quit / closing inventory: `measurements-run3.md` evidences neither, and
  the app is still running. Record them as `NOT PERFORMED (run 3)` / `NOT RECORDED (run 3)`
  following run 2's precedent — **do not invent a PASS**.

**2e. Add run-3 declared deviations** as a `### Run-3-scoped deviations (20260911T075450Z,
quick task 260911-s4f)` subsection under the existing deviations section, continuing the
existing numbering (run-2 used 5-7, so run 3 starts at 8):

(a) The app launched to the **Library** tab, not Humble Keys, so the operator navigated
manually; `capture-1-probe.png` documents the launch state.
(b) **"Redeemable keys only" was turned OFF** for the item-3 sample, so the row set would be
large and title lengths varied.
(c) Item 6 again used the **search box** to surface Racine — filtering, not shape manufacture,
so within P7 (which prohibits ownership-override clicks, not filtering). Note it reprises the
same run-2 deviation.
(d) Items 2 and 3 were measured in a **near-black theme** while items 4 and 6 were measured in
a **light theme**, because 2 and 3 are geometry and theme-independent whereas 4 and 6 are
colour. No item is scored across a theme switch.

**2f. Contract defects.** Add a run-3-scoped note under the contract-defects section. Add a
defect entry only if `measurements-run3.md` actually evidences one. If it does not, say so in
one line — "run 3 surfaced no new contract defect" — rather than leaving the section silent,
so the absence is a recorded finding and not an omission.

**2g. Recount the verdict. Execute the census; do not compute a delta.**

Mark the existing run-1+run-2 census block as the historical run-2 census (retitle it, keep
its numbers verbatim) so the document does not carry two live counts. Then run the document's
own command verbatim against the edited file:

    awk '/^## Verdict/,/^### Note on item/' 43-LIVE-GATE.md | grep '^| ' \
      | awk -F'|' '{print $(NF-1)}' | sed 's/^ *//;s/ *$//' | sort | uniq -c | sort -rn

Paste the real output into a new census block. Apply the document's own established exclusion
rules: drop the header row (`Result`), drop **every** `SUPERSEDED …` bucket — run 2's three and
run 3's — and count `PASS (Steam)` as a PASS. Run 2's census got this wrong once by folding
SUPERSEDED rows into the FAIL total; the correction is recorded in the document and you are
expected not to repeat it.

Then write the new `**VERDICT:** …` line from that output. **Do not carry the current
`20 PASS / 3 FAIL across 23 scored` forward and adjust it.** One remaining FAIL (item 3) is the
expectation, and the PASS total will rise by however many run-3 build-record rows you wrote —
which is why it is not predicted here. **If your count disagrees with the expectation, your
count is the answer**; say so explicitly in the text, as run 2 did.

State plainly whether Phase 43 closes. Directly under the verdict, name the sole remaining
blocker — item 3's 5.5 px `GAME` header offset — and link the new todo **by filename**
(`2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md`). Also note
that the three FAILs the previous verdict attributed to `.App { text-align: center }`
(`App.css:24`) are resolved by `e344f589d`, so that root cause no longer blocks anything, and
the surviving FAIL is a different and unexplained defect.
  </action>
  <verify>
    <automated>
cd .planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit &&
grep -c '^## Verdict' 43-LIVE-GATE.md &&
grep -c '^### Note on item' 43-LIVE-GATE.md &&
grep -c 'SUPERSEDED by run 3' 43-LIVE-GATE.md &&
grep -c 'humble-keys-game-column-header-label-sits-5px-left-of-row-titles' 43-LIVE-GATE.md &&
grep -c '43-12-evidence' 43-LIVE-GATE.md &&
grep -c '^| 3 |' 43-LIVE-GATE.md &&
awk '/^## Verdict/,/^### Note on item/' 43-LIVE-GATE.md | grep '^| ' | awk -F'|' '{print $(NF-1)}' | sed 's/^ *//;s/ *$//' | sort | uniq -c | sort -rn
    </automated>
  </verify>
  <done>
Every run-3 row carries `3` in the Run column; the four re-scored items each have a superseded
predecessor retaining its original measurement and a live run-3 row; the alignment-declarations
pre-check has its own build-record row; four run-3 deviations are declared; the census block
contains the real output of the command above run against the edited file; the VERDICT line
reports that count (not an adjusted 20/3/23); the sole remaining blocker is named and the new
todo is linked by filename.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md (prepend stopped_at, add the quick-task table row), verify, commit</name>
  <files>.planning/STATE.md</files>
  <action>
**3a. PREPEND the `stopped_at` narrative** (`.planning/STATE.md:6`). Prior value preserved
**verbatim** — append it after your new text behind a ` PRIOR: ` marker, exactly as the
existing field already chains its own history. A prior `stopped_at` was once silently replaced
wholesale, discarding ~29,700 characters that nothing detected; prepending is the standing
countermeasure.

Keep it a **single-line single-quoted YAML scalar**. Apostrophes inside doubled (`''`). No
block scalar — the SDK parser cannot read them and would return the bare indicator, discarding
everything. No raw `"` and no backslash escapes.

New text covers: quick task `260911-s4f` transcribed REQ-43-19 live gate RUN 3 (session
`20260911T075450Z`, HEAD `3ccc6e689`, `gamelib-shell` sha256 `50d3c940…c3211`, evidence
preserved at `43-12-evidence/`); alignment declarations confirmed in the embedded renderer
bundle before measuring, closing the hole run 1 fell into; item 2 FAIL→PASS (header 934.5 vs
content 934.0, spread 0.5, down from 151.5 divergence); items 4 and 6 re-confirmed PASS on new
backgrounds (deltas 38 near-black / 30 light — four theme backgrounds now passed; GOG and three
Steam glyphs all `[57,59,64]` against `--text-secondary [57,59,65]`); item 3 FAIL→FAIL for a
different and much smaller reason — title-to-title spread now 1.0 (was 216.5), so `e344f589d`
fixed the centring defect, but header-vs-titles 5.5 still exceeds ±2 and was deliberately NOT
re-scored against the friendlier metric; cause unestablished, needs a `tauri:dev` DOM read not
more pixels; new todo filed; the verdict was recounted by executing the document's own census
against the edited table rather than adjusting the previous 20/3/23; and whether Phase 43
closes (state the executed count).

**3b. Add a row to the Quick Tasks Completed table.** The table heading is at
`.planning/STATE.md:5548`, its column header `| # | Description | Date | Status | Directory |`
at L5550. Rows are ordered by quick_id: the current tail runs `…nyq`, `p6s`, `qds`, `r8u`
(L5836). **Check the tail yourself rather than trusting these line numbers** — the two most
recent quick tasks each missed this table (one updated `stopped_at` only, the other scoped
STATE.md out entirely), so the tail may have moved. `s4f` sorts after `r8u`, so it becomes the
new last row.

Five cells, matching the existing rows' shape: `| 260911-s4f | **<bolded one-line finding>**
<short body> | 2026-09-11 | ✅ | .planning/quick/260911-s4f-file-the-game-header-offset-todo-and-tra |`.
The finding line should lead with what run 3 actually settled: item 2 cleared, and Phase 43
now turns on a single ~5 px header offset whose cause is unknown.

**3c. Verify the edits before committing:**
- The prior `stopped_at` text survives byte-for-byte. Extract the substring after your
  ` PRIOR: ` marker and diff it against a pre-edit snapshot of the old field value; do not
  eyeball it.
- Quoting is balanced: the frontmatter parses. Check with **both** parsers, since they
  disagree — `gsd-sdk query frontmatter.get .planning/STATE.md stopped_at` must return the
  narrative text (not `|-`, not empty, not truncated at the first apostrophe), and a js-yaml
  load of the frontmatter must succeed.
- `pnpm planning-gates` → **10/10**.
- `.planning/` is prettier-ignored (`.prettierignore:29`) — confirm that line before spending
  a run on prettier; do not run prettier over `.planning/`.

**3d. Commit.** Explicit paths only — `.claude/skills/archify/` and `skills-lock.json` are
untracked and unrelated, and must not be absorbed. No `git add -A`, no `gsd-sdk query commit`.
Stage exactly: the new todo, `43-12-evidence/`, `43-LIVE-GATE.md`, `.planning/STATE.md`, and
this plan directory. Message: `docs(quick-260911-s4f): transcribe REQ-43-19 live gate run 3 and
file the GAME header offset todo`.
  </action>
  <verify>
    <automated>
pnpm planning-gates 2>&1 | tail -5 &&
gsd-sdk query frontmatter.get .planning/STATE.md stopped_at | head -c 200 &&
grep -c '^| 260911-s4f |' .planning/STATE.md &&
grep -c 'PRIOR: Completed quick task 260911-qds' .planning/STATE.md &&
sed -n '29p' .prettierignore
    </automated>
  </verify>
  <done>
`stopped_at` leads with the run-3 narrative and still contains the full prior text verbatim
behind a ` PRIOR: ` marker; both parsers read the field as non-empty narrative text; exactly one
`260911-s4f` row exists in the Quick Tasks Completed table, positioned after `260911-r8u`;
`pnpm planning-gates` reports 10/10; the commit contains only the five intended paths and
leaves `.claude/skills/archify/` and `skills-lock.json` untracked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/tmp` session → `.planning/` | Live-session artifacts from a signed-in real Humble account cross into a committed, public-fork-tracked repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information disclosure | `terminal.log`, `gamelib-launch-1.log`, PNG captures copied into `43-12-evidence/` | mitigate | Task 1c runs the P11 dash-separated-group scan over every copied file and classifies each match; Task 1a visually inspects every PNG before copying, since a capture can contain a wizard-revealed code that no text scan would see |
| T-43-02 | Tampering | `.planning/STATE.md` narrative fields | mitigate | Task 3a prepends rather than replaces, and Task 3c diffs the preserved prior text against a pre-edit snapshot instead of eyeballing it |
| T-43-03 | Repudiation | `43-LIVE-GATE.md` verdict count | mitigate | Task 2g executes the document's own census against the edited table and pastes the real output, rather than adjusting the prior count; the plan states in advance that a disagreeing count wins |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — records-only, no build, no dependency change |
</threat_model>

<verification>
- `pnpm planning-gates` → 10/10.
- `.planning/` confirmed prettier-ignored at `.prettierignore:29` before any prettier run.
- The gate document's own census command, executed against the edited file, produces the count
  reported in its VERDICT line.
- No build, no app launch, no re-measurement. PID 70156 untouched.
</verification>

<success_criteria>
- The new todo exists in `.planning/todos/pending/` with `severity: minor`, `platform: any`,
  `ready: code` bare/lowercase/in CLAUDE.md's order, carrying the 335.5 vs 340.0-341.0
  measurements, the four ruled-out hypotheses, and the `tauri:dev`-not-more-pixels next step.
- `43-12-evidence/` holds all 8 run-3 files, non-zero, visually inspected, P11-scanned with
  each match classified in the report.
- `43-LIVE-GATE.md` records three runs unambiguously: every row is attributable to a run,
  superseded rows retain their original measurements under `SUPERSEDED by run 3 (was …)`, and
  run-3 deviations are declared.
- The VERDICT line reports an executed census, states whether Phase 43 closes, names item 3's
  5.5 px offset as the sole remaining blocker, and links the new todo by filename.
- STATE.md's `stopped_at` is prepended with the prior value intact, and the Quick Tasks
  Completed table carries a `260911-s4f` row in quick_id order.
- `pnpm planning-gates` 10/10; one commit, five intended paths, nothing else absorbed.
</success_criteria>

<output>
Create `.planning/quick/260911-s4f-file-the-game-header-offset-todo-and-tra/260911-s4f-SUMMARY.md`
when done. Report the executed census output verbatim in the summary, plus the P11 scan
classification and the per-PNG visual-inspection verdicts.
</output>
