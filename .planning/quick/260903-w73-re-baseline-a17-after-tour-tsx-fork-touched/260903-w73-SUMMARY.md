---
phase: quick-260903-w73
plan: 01
status: complete
date: 2026-09-03
commits:
  - b9d736e13
files_modified:
  - meta/i18nForkTouchedFiles.json
  - meta/__tests__/genI18nGateScope.test.ts
decisions:
  - id: D-01
    decision: "Hand-edit; do not run `pnpm gen-i18n-gate-scope`."
    rationale: "The regen is measured to take this suite from 1 failure to 5 -- the artifact and the count pins guarding it are pinned to each other. Third clean application of the `i18n-fork-pin-regen-cascades` recipe; the cascade did not recur."
  - id: D-02
    decision: "Declare `Tour.tsx` as unscanned debt rather than promoting it into `i18nGateScope.json`."
    rationale: "Not a judgement call: it imports `./Tour.scss`, and .scss-importing files are unparseable by this jsdom-less jest config, so it is not scope-eligible. Also avoids widening a blocking gate as a side effect of a red-suite fix, which is the accidental widening this gate's own comment warns about."
  - id: D-03
    decision: "Leave every historical dated entry byte-identical and append a new one."
    rationale: "Those numbers are a RECORD of what past tasks did, not live pins. Bulk-replacing `209` everywhere would have falsified the log -- exactly the trap the recipe warns about, and the reason the ten live pins were edited by explicit line number."
---

# Quick task: re-baseline A-17 after `Tour.tsx` became fork-touched

## What was wrong

`f8b432b7e` -- the intro.js memoization committed during this morning's blank-tooltip debug -- made
`src/frontend/components/Tour/Tour.tsx` fork-divergent and did not re-baseline
`meta/i18nForkTouchedFiles.json`. A-17 ANTI-ROT compares the artifact against a live
`git diff --name-status <merge-base> HEAD -- src/frontend` and went red.

**It stayed red for three commits.** The commit that caused it, and the two after it, verified with
`pnpm jest --selectProjects Frontend`. The whole Meta project -- which is where every i18n gate
lives -- was never run. A project-scoped run is not evidence about the gates.

## What changed

| | before | after |
| --- | --- | --- |
| `i18nForkTouchedFiles.json` | 209 | **210** |
| `DECLARED_UNSCANNED_DEBT` | 45 | **46** |
| live count pins (A0 x2, A2, A3 x2, A4 x2, 2 doc lines) | `209` | **`210`** |
| `meta/i18nGateScope.json` | 164 | **164 -- untouched** |

Historical dated entries left byte-identical; a new 2026-09-03 entry appended.

## Attribution, before action

The failure named `Tour.tsx`, a file neither open task had touched, so the first question was
whose it was. `git diff --name-only <base> -- Tour.tsx` returns the path (fork-touched live) while
the committed artifact does not list it -- stale artifact, and the commit that made it stale was
mine from earlier in the same session. Worth stating plainly rather than filing as ambient repo
drift.

## Evidence

- Meta: **36 suites / 967 passed**, 1 skipped -- was 1 failed / 35 passed.
- The documented 1-failure-to-5 cascade **did not occur**, which is the point of hand-editing.
- Whole repo: `pnpm test:ci` **377 suites / 7737 tests**, 3 skipped, zero failures.
- `meta/i18nGateScope.json` byte-identical -- no accidental widening.
