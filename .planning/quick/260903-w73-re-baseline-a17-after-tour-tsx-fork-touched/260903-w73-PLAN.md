---
phase: quick-260903-w73
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - meta/i18nForkTouchedFiles.json
  - meta/__tests__/genI18nGateScope.test.ts
must_haves:
  truths:
    - "`meta/i18nForkTouchedFiles.json` equals the live `git diff --name-status <base> HEAD -- src/frontend` derivation, so A-17 ANTI-ROT is green."
    - "`meta/i18nGateScope.json` is byte-identical -- the blocking gate is NOT widened as a side effect."
    - "The historical dated entries in the doc comment are byte-identical; a NEW dated entry records this change."
    - "The measured regen cascade (1 failure -> 5) does not occur: the whole Meta project is green."
---

# Quick task: re-baseline A-17 after `Tour.tsx` became fork-touched

`f8b432b7e` (the intro.js memoization committed during the blank-tooltip debug earlier today) made
`src/frontend/components/Tour/Tour.tsx` fork-divergent without re-baselining
`meta/i18nForkTouchedFiles.json`. A-17 ANTI-ROT compares the committed artifact against the live
git derivation and went red.

That commit ran only the Frontend jest project, so the red Meta suite went unseen for three
commits.

## Constraint

`pnpm gen-i18n-gate-scope` is NOT the fix -- it is measured to take this suite from 1 failure to 5,
because the artifact and the pins guarding it are pinned to each other. Hand-edit.

## Tasks

1. Add `Tour.tsx` to `i18nForkTouchedFiles.json` (209 -> 210), sorted.
2. Update the ten LIVE count pins `209` -> `210`. Leave the historical dated lines alone -- their
   numbers record past changes, not current state.
3. `DECLARED_UNSCANNED_DEBT` 45 -> 46.
4. Append a new dated entry.

## Scope decision

Debt, not scope -- and not a close call: `Tour.tsx` imports `./Tour.scss`, and `.scss`-importing
files are unparseable by this jsdom-less jest config, so it is not scope-eligible at all.
