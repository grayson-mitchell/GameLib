---
phase: quick-260907-ov3
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [REQ-34.4.1-GAP-11, T-34.4.1-114, T-34.4.1-115, D-34.5-ROUTING-3]
files_modified:
  - .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-LIVE-GATE.md
  - .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-35-02-ab-tauri-part1.log
  - .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-34.6-21-gap2.log
  - .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-pre-346-12.log
  - .planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md
  - .planning/todos/completed/2026-08-23-keyring-get-bounded-timeout-unverified-live.md

must_haves:
  truths:
    - "The archived log lines that satisfy the todo's discharge condition are preserved INSIDE the repo, so the evidence cannot rotate away"
    - "Each of the discharge condition's four clauses is scored individually against a quoted log line, with the honest verdict recorded per clause rather than one blanket PASS"
    - "The code-drift claim names its baseline sha explicitly and is proven region-by-region, not asserted"
    - "The negative control is the unwrapped sibling arm in the SAME session, proving the greps distinguish a bounded classified failure from an opaque transport timeout"
    - "The todo is closed with a dated disposition citing the measured figures, and carries the transferable lesson about why it sat open"
    - "An optional fresh reproduction is available but does NOT gate the close, and its bars are never filled without operator-supplied raw output"
    - "Zero production source files change"
  artifacts:
    - path: ".planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-LIVE-GATE.md"
      provides: "Clause-by-clause scoring of the discharge condition against preserved archived evidence, plus the code-drift proof and the optional reproduction recipe"
      contains: "## Discharge-condition scoring"
    - path: ".planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-35-02-ab-tauri-part1.log"
      provides: "The primary specimen — two independent slots timing out at 45.0s with classified errors, plus the 60s unwrapped-sibling negative control, in one session"
    - path: ".planning/todos/completed/2026-08-23-keyring-get-bounded-timeout-unverified-live.md"
      provides: "The closed todo with its dated 2026-09-07 disposition"
  key_links:
    - from: ".planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-LIVE-GATE.md"
      to: ".planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-35-02-ab-tauri-part1.log"
      via: "every scored bar is a runnable grep against the in-repo copy, not against ~/Library/Logs/"
      pattern: "260907-ov3-evidence-35-02-ab-tauri-part1\\.log"
    - from: ".planning/todos/completed/2026-08-23-keyring-get-bounded-timeout-unverified-live.md"
      to: ".planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-LIVE-GATE.md"
      via: "the 2026-09-07 disposition cites the gate file and quotes the measured elapsed"
      pattern: "260907-ov3-LIVE-GATE\\.md"
---

<objective>
Discharge `.planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md` from
**evidence that already exists**, preserve that evidence inside the repo before it rotates away, and
close the todo.

**The premise this task was opened on was wrong.** The todo says the bounded, classified
`keyring_get` timeout has never been watched fire. It has — repeatedly, on this machine, in the app
log archive at `~/Library/Logs/GameLib/`. The todo's own 2026-08-25 disposition even anticipated
this in parentheses: *"the specific timeout event never fired **(or was not reported if it did)**"*.
It fired. It was reported. Nobody re-read the archive.

Purpose: staging a fresh forced timeout would manufacture, at cost and with teardown risk to a live
credential slot, a weaker specimen than the one already sitting on disk — one where the block is
naturally occurring, two independent slots time out in a single session, and the negative control is
the unwrapped sibling arm in that same session. The right move is to score the archive honestly,
preserve it, and close.

Output: `260907-ov3-LIVE-GATE.md` scoring each discharge clause against a quoted line; three
preserved log files; and the todo moved to `completed/` with a dated disposition.

**NO PRODUCTION CODE CHANGES.** Everything the discharge rests on already exists at HEAD `1a6b6985f`
and was read during planning. If the executor believes a code change is required, STOP and report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md
@.planning/quick/260817-d61-defer-the-steam-keyring-read-from-startu/260817-d61-LIVE-GATE.md
@CLAUDE.md

Project skill (read if present): `.claude/skills/spike-findings-gamelib/SKILL.md`

<executor_notes>
- **Do NOT run any `gsd-sdk state.*`, `roadmap.*`, or `phase.complete` verb.** The orchestrator owns
  `.planning/STATE.md` and `.planning/ROADMAP.md`. Those verbs have corrupted both files repeatedly
  on this project. Touch neither file.
- **Commit by EXPLICIT PATH only.** The working tree carries unrelated uncommitted changes from a
  concurrent session (`src/backend/storeManagers/gog/library.ts`, `src/backend/sidecar/bootstrap.ts`,
  `.planning/ROADMAP.md`, `.planning/STATE.md`, `src/backend/sidecar/__tests__/testContainment.test.ts`,
  and others). **Never `git add -A`. Never `git commit -a`. Never `gsd-sdk query commit` without
  `--files`** — that verb absorbs whatever is already staged. Name every path.
- `git add` fails ATOMICALLY on a nonexistent pathspec: if one path is missing, NOTHING stages. After
  every commit verify with `git show --stat --name-only HEAD` that the expected files are in it. A
  `git mv` followed by an `add` of only the destination has silently committed stale content on this
  project before — line-count the committed blob, do not trust the exit code.
- Do not run `git checkout -- <file>` in this repo: the post-checkout hook fires and the binary
  download throws.
- **Task-ordering deviation, deliberate.** The task brief specified (a) prep → (b) operator live run
  → (c) records. That ordering was premised on the live run BEING the discharge. It is not: the
  discharge is archival. The records task therefore runs SECOND and the optional reproduction runs
  LAST, so the close never depends on an operator session that may not happen.
</executor_notes>
</context>

<measured_facts>
Measured by the orchestrator and **independently re-measured by the planner** on this machine at HEAD
`1a6b6985f`. Do not re-derive; do not contradict without re-measuring.

## The evidence

| # | Fact | Source |
|---|------|--------|
| E1 | `~/Library/Logs/GameLib/gamelib.log.35-02-ab-tauri-part1` (36873 B, mtime 2026-08-28 16:00) is the **primary specimen**. It carries, in one session: `L11 (15:48:56) [bootstrap] secret stores: keyring`; `L18 (15:48:56) SidecarKeyringSlotStore(humble-session).getToken(): issuing keyring_get (may prompt) trigger=unspecified`; `L32 (15:49:04) SidecarKeyringSlotStore(steam-refresh-token).getToken(): issuing keyring_get (may prompt) trigger=game-page`; `L41 (15:49:41) ... humble-session ... keyring_get failed: keyring:timeout trigger=unspecified elapsed=45006ms`; `L40/L43 keyring failure memoized slot=<slot> class=timeout ms=120000 trigger=<label>`; `L44 (15:49:49) ... steam-refresh-token ... keyring_get failed: keyring:timeout trigger=game-page elapsed=45004ms`; `L45 Steam: refresh token read failed (timeout) — this is retryable, keeping the signed-in session and NOT clearing any credentials`; `L88/L89 (15:51:41, 15:51:48) SidecarKeyringSlotStore(steam-refresh-token).clearToken(): keyring_delete failed: rustInvoke timed out after 60000ms: keyring_delete`. | direct read |
| E2 | **Wall-clock corroborates `elapsed=` independently.** humble-session: issued 15:48:56, failed 15:49:41 → 45s. steam-refresh-token: issued 15:49:04, failed 15:49:49 → 45s. The log's own timestamps agree with the self-reported `elapsed=` token to the second, on BOTH slots. The measurement is therefore not a single self-report. | E1 timestamps |
| E3 | Exactly **three** archived files carry an `elapsed=`-bearing `keyring:timeout` line: `gamelib.log.35-02-ab-tauri-part1` (2), `gamelib.log.34.6-21-gap2-G2-2-and-G2-3` (1, `18:33:03 ... humble-session ... elapsed=45006ms`, mtime Aug 26), `gamelib.log.pre-346-12-20260823T235034Z` (1, `11:39:19 ... humble-session ... elapsed=45006ms`, mtime Aug 24). | `grep -c 'keyring:timeout.*elapsed='` over `*.log*` |
| E4 | Three further archives carry `keyring:timeout` **without** an `elapsed=` token (`gamelib.log.34.5-g6-gate2`, `gamelib.log.34.5-g6-gate3-sessionA`, `gamelib.log.old.34.5-g6-gate2`). They predate quick-260817-d61, which added the `trigger=`/`elapsed=` tokens. They corroborate that the timeout fires often; they are **not** discharge-bearing, because the discharge condition requires the elapsed measured. | same census |
| E5 | **`rustInvoke timed out after 60000ms: keyring_get` occurs ZERO times across the entire log archive.** | `grep -h 'rustInvoke timed out after 60000ms: keyring_get' *.log* \| wc -l` → `0` |
| E6 | The only two `rustInvoke timed out after 60000ms` hits in the primary specimen are both `keyring_delete` (E1 L88/L89). `keyring_delete` is the **deliberately unwrapped** sibling: the dispatch arm's own comment states writes and deletes keep "their original unbounded synchronous shape". `keyring_delete` is also NOT in `UNBOUNDED_RUST_CHANNELS`, so the TS 60s timer is what fired. | `main.rs:5540-5566`, `sidecarRpc.ts:96-100` |

## The code-drift proof

| # | Fact | Source |
|---|------|--------|
| D1 | The specimen's session commit is `d629d9f30` — `2026-08-28  docs(35-02): item 1 verdict BOTH — corrects an incomplete Electron record`. The sha was resolved and its subject line read, not assumed. | `git log -1 --format='%h %ad %s' d629d9f30` |
| D2 | Between `d629d9f30` and HEAD `1a6b6985f`, these five regions are **byte-identical** (md5 of each region's extracted text matches): `const KEYRING_READ_TIMEOUT`, `fn bounded_keyring_read`, `fn keyring_get_result`, `fn keyring_account`, and the `"keyring_get" => {` dispatch arm. | region md5 comparison via `git show <sha>:src-tauri/src/main.rs` |
| D3 | `keyringTokenStore.ts`'s `fetchToken()` (the whole read/announce/classify/memo/log path, from `private async fetchToken` to `async setToken`) is **byte-identical**: md5 `f2735f76f23406b86e1ebd6cfb110980` at both revisions. | same method |
| D4 | `RUST_INVOKE_TIMEOUT_MS` is `60_000` at both revisions (it moved line 58 → 60; the value did not change). | `git show <rev>:src/backend/sidecar/sidecarRpc.ts \| grep 'RUST_INVOKE_TIMEOUT_MS ='` |
| D5 | **Honest scope of the drift claim.** The three files as WHOLES differ substantially over that range (`main.rs` +5478 lines, `keyringTokenStore.ts` 79 changed, `sidecarRpc.ts` 30 changed). The claim is NOT "these files are unchanged" — it is "the five specific regions the discharge depends on are byte-identical, and the outer bound's value is unchanged." The `keyringTokenStore.ts` churn is in the `isAvailable()` reachability-probe path (quick-260905-jx3 / l8g), which the discharge does not touch. | `git diff --stat d629d9f30 HEAD -- <the three files>` |

## Supporting code facts

| # | Fact | Source |
|---|------|--------|
| F1 | `keyring_account()` allowlists exactly four slots: `steam-refresh-token`, `humble-session`, `humble-csrf`, `steamgrid-api-key`. | `main.rs:1076` |
| F2 | Keychain census (non-prompting, re-run by the planner): `steam-refresh-token` **PRESENT**, `humble-session` absent, `humble-csrf` absent, `steamgrid-api-key` absent. `KEYRING_SERVICE` = `com.gamelib.launcher`. | `security find-generic-password`, `main.rs:1023` |
| F3 | `KEYRING_READ_TIMEOUT` = 45s; `RUST_INVOKE_TIMEOUT_MS` = 60000ms; a Rust `#[cfg(test)]` assertion pins the ordering invariant with a 10s round-trip margin. | `main.rs:2218`, `sidecarRpc.ts:60`, `main.rs:~11960` |
| F4 | `UNBOUNDED_RUST_CHANNELS` = exactly `RUST_DIALOG_OPEN`, `RUST_DIALOG_MESSAGE`, `RUST_DIALOG_SAVE`. `keyring_get` is **not** in it, so the 60s timer IS armed for it — which is what makes E5 a real observation rather than a vacuous one. | `sidecarRpc.ts:96-100,378-388` |
| F5 | On timeout the Rust arm emits a **bare `eprintln!`** (`[shell] keyring keyring_get timed out after 45s ...`) to **stderr only**. It is in NO log file — not `gamelib.log`, not `gamelib-shell.log` (which only carries `shell_diag()` lines). **It is therefore absent from the archive, and the discharge condition does not require it.** Capturing it is the one genuinely new thing an optional fresh run would add. | `main.rs:5549-5566`, `main.rs:8250` |
| F6 | `KEYRING_FAILURE_MEMO_MS` = 120000ms, in-memory per process. E1's L42 `memo hit ... without a second keyring_get` shows it engaging immediately after the timeout. | `keyringTokenStore.ts:60,~365` |
| F7 | `GAMELIB_DEV_SECRET_VAULT === '1'` swaps the keyring for a plaintext dev vault; the receipt is `[bootstrap] secret stores: keyring` vs `dev-vault`. E1's L11 shows `keyring` — the specimen is on the real arm. | `devSecretVault.ts:73,276`, `bootstrap.ts:614-631` |
</measured_facts>

<honest_clause_check>
The discharge condition, verbatim from the todo:

> A live run in which `keyring_get` **actually times out** (or is made to, by an unavailable/blocked
> keyring), producing a **classified** error inside the bound rather than a silent consumption of the
> RPC budget — and the elapsed time measured, not inferred.

Scored clause by clause against the archive. **Do not weaken any bar to fit the evidence. If a clause
is not met by the archived lines, say so and the todo does not close.**

| Clause | Verdict | What meets it |
|--------|---------|---------------|
| **C1** — "`keyring_get` actually times out (or is made to, by an unavailable/blocked keyring)" | **MET** | Two independent slots in one session (E1 L41, L44), plus two further sessions (E3). Naturally occurring — a real blocked Keychain, not a synthetic plant. |
| **C2** — "producing a **classified** error" | **MET, three independently** | (a) the literal `keyring:timeout` string from `bounded_keyring_read`'s Rust-side classification; (b) `keyring failure memoized slot=... class=timeout ms=120000` from `classifyKeyringFailure`, a different function reaching the same verdict; (c) `Steam: refresh token read failed (timeout) — this is retryable...` — a downstream consumer branching correctly on the class instead of clearing credentials. |
| **C3** — "inside the bound rather than a silent consumption of the RPC budget" | **MET, and non-vacuously** | 45004ms and 45006ms are both under `RUST_INVOKE_TIMEOUT_MS` (60000). `rustInvoke timed out after 60000ms: keyring_get` occurs **zero** times archive-wide (E5). And the absence is meaningful because the 60s timer was ARMED — `keyring_get` is not in `UNBOUNDED_RUST_CHANNELS` (F4). |
| **C4** — "the elapsed time **measured, not inferred**" | **MET, twice over** | The `elapsed=45004ms` / `elapsed=45006ms` tokens are sidecar-side `Date.now()` deltas over the whole RPC round trip. Independently, the log's own timestamps agree to the second on both slots (E2), so the figure does not rest on a single self-report. |
| **X1** — not the 34.6 Step-1 "unreported" rider the 2026-08-25 disposition rejected | **DISTINCT** | Different sessions, different files, and reported: three archives, four elapsed-bearing lines (E3). |
| **X2** — not the `keyring:unavailable: ... In dark wake, no UI possible` immediate rejection the 2026-08-25 disposition rejected as the wrong failure mode | **DISTINCT** | The literal classification string is `keyring:timeout`. `keyring:unavailable` is a different branch of `keyring_get_result` and does not appear on these lines. |
| **D** — the evidence applies to today's tree | **MET, with a named baseline** | D1–D5. Baseline sha `d629d9f30`, resolved and read. Five regions byte-identical; outer bound unchanged. The claim is scoped to those regions, not to the files as wholes (D5). |
| **NC** — the greps discriminate; the PASS is not vacuous | **MET, for free, from the same session** | E1 L88/L89: the **unwrapped sibling** `keyring_delete`, same slot, same session, ~2 minutes later, produced `rustInvoke timed out after 60000ms: keyring_delete` — the opaque transport timeout with no named class. Wrapped arm → named class at 45.0s; unwrapped arm → opaque transport error at 60.0s. This is a better negative control than anything that could be staged, because both arms ran against the same blocked Keychain in the same process. |

**Recorded limitations (do not launder these away):**
- macOS-only, dev builds. The archive contains no Windows or Linux observation.
- The Rust-side stderr line (F5) is absent from the archive by construction. The discharge condition
  does not ask for it.
- **Scope, unchanged and load-bearing:** this is the OBSERVABILITY requirement. It is NOT a
  root-cause fix for F-9 and closing it does not close F-9. The todo says so in its own body and the
  disposition must repeat it.
</honest_clause_check>

<tasks>

<task type="auto">
  <name>Task 1: Preserve the evidence in-repo and score every discharge clause against it</name>
  <files>.planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-LIVE-GATE.md, .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-35-02-ab-tauri-part1.log, .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-34.6-21-gap2.log, .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-pre-346-12.log</files>
  <action>
**Step A — preserve the evidence first.** The source logs live in `~/Library/Logs/GameLib/`, outside
the repo, and will be rotated away. Evidence that can vanish is not evidence. Copy the three
`elapsed=`-bearing archives (E3) into the quick-task directory:

```bash
Q=/Users/graysonmitchell/Projects/GameLib/.planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi
L=~/Library/Logs/GameLib
cp "$L/gamelib.log.35-02-ab-tauri-part1"         "$Q/260907-ov3-evidence-35-02-ab-tauri-part1.log"
cp "$L/gamelib.log.34.6-21-gap2-G2-2-and-G2-3"   "$Q/260907-ov3-evidence-34.6-21-gap2.log"
cp "$L/gamelib.log.pre-346-12-20260823T235034Z"  "$Q/260907-ov3-evidence-pre-346-12.log"
```

Record each copy's `md5` and byte size against the source's, in the gate file, so the copy is
provably faithful. Total is ~72 KB — small enough to version.

Do **not** copy the three non-`elapsed=` archives (E4); `gamelib.log.34.5-g6-gate2` alone is 351 KB
and none of them is discharge-bearing. Record them in a census table instead, naming why they do not
qualify (they predate quick-260817-d61, which added the `trigger=`/`elapsed=` tokens), so their
absence is a recorded decision rather than an oversight.

**Step B — write `260907-ov3-LIVE-GATE.md`.** Follow the shape of
`.planning/quick/260817-d61-defer-the-steam-keyring-read-from-startu/260817-d61-LIVE-GATE.md`.
Sections, in order:

1. **Header.** What this discharges (`REQ-34.4.1-GAP-11` / the todo), scoring commit HEAD `1a6b6985f`,
   evidence baseline `d629d9f30`, and the one-sentence correction: *the event this todo calls
   UNVERIFIED had already fired live and been reported; the miss was that nobody re-read the log
   archive.*

2. **## Evidence provenance.** The three copied files with source path, mtime, byte size, md5 of
   source and of copy, and what each contains. Plus the E4 census of the non-qualifying archives.

3. **## Discharge-condition scoring.** The `<honest_clause_check>` table above, copied verbatim,
   with each row's verdict backed by a **runnable grep against the in-repo copy** — never against
   `~/Library/Logs/`, which will rotate. Every command in this section must still work a year from
   now with only the repo. Concretely:

   ```bash
   Q=.planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi
   P="$Q/260907-ov3-evidence-35-02-ab-tauri-part1.log"

   # C1/C2 — a real keyring_get timed out and was CLASSIFIED (expect 2)
   grep -cE 'SidecarKeyringSlotStore\((steam-refresh-token|humble-session)\)\.getToken\(\): keyring_get failed: keyring:timeout trigger=[a-z-]+ elapsed=[0-9]+ms' "$P"

   # C2 (b) — the independent second classifier (expect 2)
   grep -cE 'keyring failure memoized slot=[a-z-]+ class=timeout ms=120000 trigger=' "$P"

   # C2 (c) — a downstream consumer branching on the class, not clearing credentials (expect >= 1)
   grep -cF 'refresh token read failed (timeout) — this is retryable' "$P"

   # C4 — the measured elapsed, extracted (expect 45006 and 45004)
   grep -oE 'keyring:timeout[^\n]*elapsed=[0-9]+ms' "$P" | grep -oE '[0-9]+ms'

   # C4 corroboration — the issuing timestamps, for the wall-clock check (expect 15:48:56, 15:49:04)
   grep -nE 'getToken\(\): issuing keyring_get \(may prompt\) trigger=' "$P"

   # C3 — the 60s RPC budget was NOT consumed by keyring_get (expect 0)
   grep -c 'rustInvoke timed out after 60000ms: keyring_get' "$P"

   # C3 non-vacuity — the 60s timer WAS armed for keyring_get (expect 0 = absent from the list)
   sed -n '96,100p' src/backend/sidecar/sidecarRpc.ts | grep -ci keyring

   # X2 — this is NOT the keyring:unavailable wrong-failure-mode shape (expect 0)
   grep -c 'keyring_get failed: keyring:unavailable' "$P"

   # F7 — the real keyring arm ran, not the dev vault (expect >= 1, then 0)
   grep -cF '[bootstrap] secret stores: keyring'   "$P"
   grep -cF '[bootstrap] secret stores: dev-vault' "$P"

   # NC — the unwrapped sibling arm, same slot, same session: opaque 60s transport timeout (expect 2)
   grep -cF 'clearToken(): keyring_delete failed: rustInvoke timed out after 60000ms: keyring_delete' "$P"
   ```

   Run every one of them and fill in the observed value beside each expectation. **If any command's
   observed value disagrees with its expectation, record the disagreement and score that clause NOT
   MET — do not adjust the expectation.**

4. **## Code drift — the evidence applies to today's tree.** D1–D5 as a table, each row with its
   runnable region-md5 command. State the baseline sha `d629d9f30` explicitly, with its subject line,
   because on this project "pre-existing"/"unchanged" is a claim about a chosen baseline and a sha
   asserted without being read has already been recorded as an error. Include D5's honest narrowing
   verbatim: the files as wholes DID change; the five regions did not.

5. **## Recorded limitations.** The three from `<honest_clause_check>`, unlaundered.

6. **## Optional reproduction on today's binary (NOT required for the close).** The
   `steamgrid-api-key` foreign-ACL recipe from this plan's `<reproduction_recipe>` block, copied
   verbatim including its teardown and its operator hazards. Head this section with a bold sentence:
   *this does not gate the close; its only new contribution is capturing the Rust-side stderr line
   (F5), which is absent from the archive by construction and which the discharge condition does not
   require.* Its bars start at `(not run)` and stay there unless an operator supplies raw output.

7. **## Verdict.** Written from the scoring in section 3.

Commit by explicit path:
```bash
git add .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-PLAN.md \
        .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-LIVE-GATE.md \
        .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-35-02-ab-tauri-part1.log \
        .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-34.6-21-gap2.log \
        .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-evidence-pre-346-12.log
git commit -m "docs(quick-260907-ov3): preserve and score the archived keyring_get timeout evidence"
git show --stat --name-only HEAD
```
  </action>
  <verify>
    <automated>Q=.planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi; P="$Q/260907-ov3-evidence-35-02-ab-tauri-part1.log"; test -f "$P" &amp;&amp; test -f "$Q/260907-ov3-evidence-34.6-21-gap2.log" &amp;&amp; test -f "$Q/260907-ov3-evidence-pre-346-12.log" &amp;&amp; test "$(grep -cE 'keyring_get failed: keyring:timeout trigger=[a-z-]+ elapsed=[0-9]+ms' "$P")" = "2" &amp;&amp; test "$(grep -c 'rustInvoke timed out after 60000ms: keyring_get' "$P")" = "0" &amp;&amp; test "$(grep -cF 'clearToken(): keyring_delete failed: rustInvoke timed out after 60000ms: keyring_delete' "$P")" = "2" &amp;&amp; grep -q '## Discharge-condition scoring' "$Q/260907-ov3-LIVE-GATE.md" &amp;&amp; grep -q 'd629d9f30' "$Q/260907-ov3-LIVE-GATE.md" &amp;&amp; grep -q '45004' "$Q/260907-ov3-LIVE-GATE.md" &amp;&amp; git show --stat --name-only HEAD | grep -q '260907-ov3-evidence-35-02-ab-tauri-part1.log'</automated>
  </verify>
  <done>
The three `elapsed=`-bearing archives are copied into the repo and committed, each with a
source-vs-copy md5 recorded. `260907-ov3-LIVE-GATE.md` scores all four discharge clauses plus X1, X2,
D and NC, every bar backed by a runnable grep **against the in-repo copy**, with the observed value
recorded beside the expectation and any disagreement scored NOT MET. The code-drift section names
baseline `d629d9f30` with its subject line and carries D5's honest narrowing. Zero files outside
`.planning/` are in the commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close the todo with a dated disposition and the transferable lesson</name>
  <files>.planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md, .planning/todos/completed/2026-08-23-keyring-get-bounded-timeout-unverified-live.md</files>
  <action>
**Gate this task on Task 1's scoring.** If any of C1–C4, X1, X2 or D was scored NOT MET, do **not**
close: leave the todo in `pending/`, append a dated `does NOT close` section naming the failing
clause and the observed value that failed it, and stop. Only close if all of them were met.

On close: `git mv` the todo from `.planning/todos/pending/` to `.planning/todos/completed/`, set
frontmatter `status: completed`, and **APPEND** — never rewrite — a new section. The existing
2026-08-25 disposition is load-bearing history and stays exactly as written; it was not wrong when
written (see the lesson below).

```
## Disposition (2026-09-07, quick-260907-ov3) — CLOSES

Discharged from evidence that already existed. Scored clause by clause in
`.planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-LIVE-GATE.md`
against three log archives now preserved inside the repo (they lived in `~/Library/Logs/GameLib/`
and would have rotated away).

**Primary specimen:** `260907-ov3-evidence-35-02-ab-tauri-part1.log`, the 2026-08-28 Phase 35-02 A/B
live session, commit `d629d9f30`.

    (15:49:41) SidecarKeyringSlotStore(humble-session).getToken():
               keyring_get failed: keyring:timeout trigger=unspecified elapsed=45006ms
    (15:49:49) SidecarKeyringSlotStore(steam-refresh-token).getToken():
               keyring_get failed: keyring:timeout trigger=game-page elapsed=45004ms
    (15:49:49) Steam: refresh token read failed (timeout) — this is retryable,
               keeping the signed-in session and NOT clearing any credentials

Two independent slots, one session, a naturally blocked Keychain — not a staged plant.

- **Actually timed out** — yes, twice here and once each in two further sessions (2026-08-24,
  2026-08-26).
- **Classified** — three times independently: the literal `keyring:timeout` from
  `bounded_keyring_read`; `keyring failure memoized slot=... class=timeout ms=120000` from
  `classifyKeyringFailure`, a different function; and a downstream consumer branching on the class to
  keep the session rather than clear credentials. It is NOT the `keyring:unavailable: ... In dark
  wake, no UI possible` immediate rejection the 2026-08-25 disposition rejected as the wrong failure
  mode.
- **Inside the bound, RPC budget not consumed** — 45004ms / 45006ms, both under the 60000ms
  `RUST_INVOKE_TIMEOUT_MS`, and `rustInvoke timed out after 60000ms: keyring_get` occurs ZERO times
  across the whole log archive. The absence is non-vacuous: `keyring_get` is absent from
  `UNBOUNDED_RUST_CHANNELS`, so the 60s timer was armed and would have fired.
- **Elapsed measured, not inferred** — the `elapsed=` token is a sidecar-side round-trip delta, and
  the log's own timestamps corroborate it independently on both slots: issued 15:48:56 → failed
  15:49:41, and issued 15:49:04 → failed 15:49:49. Both exactly 45s.

**Negative control, from the same session and better than anything stageable.** At 15:51:41 and
15:51:48 the same slot's `keyring_delete` — the deliberately UNWRAPPED sibling arm — produced
`rustInvoke timed out after 60000ms: keyring_delete`. Same process, same blocked Keychain, minutes
apart: the wrapped arm yields a named class at 45.0s, the unwrapped arm an opaque transport error at
60.0s. That is precisely the degradation `KEYRING_READ_TIMEOUT` exists to prevent, observed happening
next door.

**The evidence applies to today's tree**, against a named baseline: between `d629d9f30` and
`1a6b6985f`, `const KEYRING_READ_TIMEOUT`, `bounded_keyring_read`, `keyring_get_result`,
`keyring_account`, the `keyring_get` dispatch arm and `keyringTokenStore.ts`'s `fetchToken()` are all
byte-identical, and `RUST_INVOKE_TIMEOUT_MS` is still `60_000`. The files as WHOLES changed
substantially; the claim is scoped to those regions and no further.

**Limitations, recorded not laundered:** macOS-only, dev builds. The Rust-side
`[shell] keyring keyring_get timed out after 45s` line is a bare `eprintln!` to stderr and is in no
log file, so it is absent from the archive by construction — the discharge condition does not ask for
it. An optional forced reproduction that would capture it is written up in the gate file and was NOT
required for this close.

**Scope, unchanged.** This is the OBSERVABILITY requirement only. It is NOT a root-cause fix for F-9
and this does not close F-9.

### Why this sat open for two weeks — the transferable lesson

The park note named the revisit trigger as "the next live login gate anyone runs, Phase 34.6 runs
one". **Gates did run, and the event did fire in them** — an `elapsed=`-bearing `keyring:timeout` line
was already on disk by 2026-08-24, one day after this todo was opened and a day before its
2026-08-25 disposition was written.

The 2026-08-25 disposition was not careless: it read the gate DOCUMENT (`34.6-LIVE-GATE.md`) and
correctly found that neither of the two observations that document recorded was a discharge. Its own
hedge — *"the specific timeout event never fired **(or was not reported if it did)**"* — names the
exact gap it could not see past. **The miss was of source, not of rigour: the gate documents were
re-read, the LOG ARCHIVE was not.** A gate document records what its author looked for; it is not a
census of what the run emitted.

**Rule for the next parked live-observation todo: before staging a fresh run to force an event,
grep the log archive for it.** The discharge condition here was greppable in one command
(`grep -h 'keyring:timeout.*elapsed=' ~/Library/Logs/GameLib/*.log*`) and would have closed this two
weeks earlier at zero cost — and without planting an item in a Keychain slot next to a real
credential.
```

Commit by explicit path — **both** the source and destination paths, since a `git mv` needs both
staged, and this project has already shipped an empty rename commit by staging only one:

```bash
git add .planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md \
        .planning/todos/completed/2026-08-23-keyring-get-bounded-timeout-unverified-live.md \
        .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-LIVE-GATE.md
git commit -m "docs(quick-260907-ov3): close the keyring_get bounded-timeout todo from archived live evidence"
git show --stat --name-only HEAD
# and line-count the committed blob, not just the exit code:
git show HEAD:.planning/todos/completed/2026-08-23-keyring-get-bounded-timeout-unverified-live.md | wc -l
```

**Do not run any `gsd-sdk state.*`, `roadmap.*`, or `phase.complete` verb.** Do not modify
`.planning/STATE.md` or `.planning/ROADMAP.md`.
  </action>
  <verify>
    <automated>T=.planning/todos/completed/2026-08-23-keyring-get-bounded-timeout-unverified-live.md; test -f "$T" &amp;&amp; test ! -f .planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md &amp;&amp; grep -q 'Disposition (2026-09-07, quick-260907-ov3) — CLOSES' "$T" &amp;&amp; grep -q 'status: completed' "$T" &amp;&amp; grep -q '45004ms' "$T" &amp;&amp; grep -q 'd629d9f30' "$T" &amp;&amp; grep -q 'Disposition (2026-08-25, plan 34.6-14)' "$T" &amp;&amp; grep -q 'Why this sat open' "$T" &amp;&amp; test "$(git show HEAD:$T | wc -l | tr -d ' ')" -gt "120" &amp;&amp; test -z "$(git show --name-only --format= HEAD | grep -v '^\.planning/')"</automated>
  </verify>
  <done>
The todo is in `.planning/todos/completed/` with `status: completed`, carrying the dated
2026-09-07 disposition that quotes `elapsed=45004ms`, names baseline `d629d9f30`, records the
negative control and the limitations, repeats the F-9 scope note, and contains the "why this sat
open" lesson. The 2026-08-25 disposition is still present, byte-unchanged. The committed blob's line
count confirms the resolved content landed, not a bare rename. The commit contains only `.planning/`
paths.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3 (OPTIONAL, SECONDARY): forced reproduction on today's binary</name>
  <what-built>
**THIS TASK IS OPTIONAL AND DOES NOT GATE ANYTHING.** The todo closed in Task 2 on archived evidence.
Nothing here can un-close it; a contradictory result would be a NEW finding warranting a new todo,
not a retroactive reversal. Skip freely.

**IT IS ALSO OPERATOR-DRIVEN AND MUST NOT BE SELF-ANSWERED.** The executing agent MUST NOT fabricate,
infer, assume, or narrate any live outcome. Every bar in the gate file's optional section stays
`(not run)` unless the operator pastes raw output. If the operator declines, the gate file records
`not run, declined — the close does not depend on it` and that is a complete and correct outcome.
This project has a recorded incident of auto-mode answering its own human-verify gate; `auto_advance`
does not apply and this gate is never auto-approvable.

**What it would add, honestly: one thing.** The Rust-side `[shell] keyring keyring_get timed out
after 45s (worker thread abandoned, not cancelled ...)` line is a bare `eprintln!` to stderr (F5) —
absent from every log file and therefore from the archive. The discharge condition never asked for
it. Capturing it would corroborate the Rust side directly rather than through the sidecar's classified
error. The "reproduces on today's binary" value is largely pre-paid by D2/D3's byte-identical region
proof.
  </what-built>
  <files>.planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-LIVE-GATE.md, .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-repro-shell.log, .planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-repro-gamelib.log</files>
  <action>
**The executing agent's ONLY action in this task is to present the checkpoint below and then
transcribe what the operator pastes back.** It does not launch the app, does not plant the Keychain
item, does not perform the gesture, and above all does not decide the outcome. There is no autonomous
path through this task.

If the operator responds `SKIP`: write `not run, declined — the close does not depend on it` into the
gate file's optional section, leave every R1-R6 bar at `(not run)`, and finish. That is a complete and
correct outcome, not a deficiency.

If the operator pastes raw output: copy the R1-R6 bars' observed values from that output verbatim into
the gate file, commit `260907-ov3-LIVE-GATE.md` plus any `260907-ov3-repro-*.log` files the operator
produced — by explicit path, and drop from the `git add` any path that does not exist, since `git add`
fails atomically on a missing pathspec. Any bar the pasted output does not cover stays `(not run)`.
Never fill a bar by inference. Do not reopen or amend the todo closed in Task 2; a contradictory
result is a NEW finding warranting a new todo.
  </action>
  <how-to-verify>
Work from the gate file's optional section. Paste raw output back after each step.

**Hazards, all of which apply:**
1. **Never Allow / Always Allow the Keychain dialog** — that adds `gamelib-shell` to the item's ACL
   and destroys the forcing condition. Touch nothing until the `keyring:timeout` line appears in
   `gamelib.log`; then click **Deny** / **Cancel**.
2. **Never use the `steam-refresh-token` slot as the forcing target.** It holds a real credential and
   this project has destroyed that slot before. Perform no Steam gesture during the run either — that
   slot is PRESENT (F2) and its read has historically hung 48.9s / 291s, which would put a second
   blocked read in flight and make the measurement unattributable. The Keychain dialog names no item;
   slot attribution comes only from the `issuing keyring_get` log line.
3. The UI may freeze for up to 45s. Expected, not a crash. Do not force-quit.
4. **120s memo (F6):** a second gesture inside the window logs `memo hit ... without a second
   keyring_get` and measures nothing. Restart the app between observations.
5. `GAMELIB_DEV_SECRET_VAULT` must be unset in the launching shell (F7).
6. Launch `pnpm tauri:dev` — never bare `tauri dev` (stale static bundle) — and **TEE stderr**, or
   the one line this run exists to capture is lost.
7. `pnpm tauri:dev` exits 0 without replacing a running instance. Quit GameLib first; confirm
   `pgrep -f gamelib-shell | wc -l` is `1` after launch (a second instance splits the shell sink).
8. Sweep the orphaned node sidecar and `vite` after each run — dev teardown does not reap them and a
   PATH-based sweep misses `vite`.
9. **A dialog answered before 45s elapses VOIDS the run; it is not a FAIL.** "A keyring timeout races
   Keychain approval" is a recorded interaction and the todo names it: a slow human is not a defect.

**Procedure** (full commands in the gate file's `<reproduction_recipe>` transcription):
- **Step 0.** Quit GameLib. Census all four slots; expect `PRESENT / absent / absent / absent` (F2).
  Confirm `env | grep -c GAMELIB_DEV_SECRET_VAULT` is `0`. Confirm
  `grep 'steamGridDbApiKey' ~/Library/Application\ Support/gamelib/config.json` is the empty string —
  if it is not, **STOP**: the boot-time migration would `keyring_set` over the planted item.
- **Step 1.** Plant the foreign-ACL item (Variant 1).
- **Step 2.** `pnpm tauri:dev 2>&1 | tee <Q>/260907-ov3-repro-shell.log`.
- **Step 3.** Open **Settings → the SteamGridDB API key field** (its mount fires
  `steamgriddb.hasApiKey` → `getApiKey()` → `getToken()` on the `steamgrid-api-key` slot). Fallbacks:
  the **Add Game / Sideload** dialog, or right-click a game → **Edit**. Then wait, touching nothing.
  If no dialog appears and the read returns fast, tear down and retry once with Variant 2
  (`-T /usr/bin/true`); only if that also fails to block is the run INCONCLUSIVE.
- **Step 4.** Quit, Ctrl-C the tee, `cp ~/Library/Logs/GameLib/gamelib.log <Q>/260907-ov3-repro-gamelib.log`,
  sweep orphans.
- **Step 5.** **TEARDOWN, mandatory whatever the outcome:** label guard → delete → re-census. See the
  recipe; the `-a steamgrid-api-key` argument is load-bearing.
- **Step 6.** Paste back: both censuses, the raw output of each optional bar, and whether a dialog
  appeared.
  </how-to-verify>
  <resume-signal>
Type `SKIP` (the close does not depend on this), or paste the Steps 0–6 raw output followed by one of
`REPRODUCED`, `NOT REPRODUCED`, or `VOID`. Do not type an outcome without pasting the output it rests
on.
  </resume-signal>
  <done>
The gate file's optional section records either `not run, declined` with all R1-R6 bars at
`(not run)`, or observed values transcribed verbatim from operator-pasted raw output with any
uncovered bar left at `(not run)`. If the run happened, teardown completed and the four-slot Keychain
census matches F2 (`PRESENT / absent / absent / absent`). The todo closed in Task 2 is untouched
either way.
  </done>
</task>

</tasks>

<reproduction_recipe>
Transcribe this block verbatim into `260907-ov3-LIVE-GATE.md` section 6. It is the optional
reproduction method only — it is **not** the discharge.

## Why the `steamgrid-api-key` slot, and only that slot

An item created without `-A` does not list `gamelib-shell` among its trusted applications, so every
read raises a macOS authorization dialog — and a macOS Keychain authorization dialog **blocks until
answered**. Left unanswered, the read blocks the full 45s bound.

`steamgrid-api-key` is the right target on three grounds: it is allowlisted (F1), so `keyring_account()`
resolves it rather than returning `keyring:unknown-slot`; it is currently **absent** (F2), so planting
and deleting it destroys nothing; and it is low-stakes — a SteamGridDB API key, not a login credential.
`~/Library/Application Support/gamelib/config.json` has `"steamGridDbApiKey": ""`, which is falsy, so
`migrateSteamGridDbApiKey()` early-returns and no boot-time `keyring_set` will overwrite the plant —
**re-check this before planting; it is a precondition, not a permanent property.**

**`steam-refresh-token` MUST NOT be the forcing target.** Real credential, documented history of
destruction, PRESENT, and its own read hangs for minutes.

## Plant — Variant 1 (try first)

```bash
security add-generic-password \
  -s com.gamelib.launcher \
  -a steamgrid-api-key \
  -l gsd-260907-ov3-planted \
  -w 'gsd-260907-ov3-dummy'
```

- **No `-A` and no `-T`** is the load-bearing part: it leaves `gamelib-shell` off the trusted list.
- **No `-U`** is deliberate — the command then FAILS if an item already exists, refusing to overwrite
  anything the census missed.
- `-l gsd-260907-ov3-planted` gives a unique label that teardown asserts before deleting.

## Plant — Variant 2 (escalation, only if Variant 1 does not block)

Tear down first, then re-plant naming exactly one trusted app that is definitively not GameLib:

```bash
security add-generic-password -s com.gamelib.launcher -a steamgrid-api-key \
  -l gsd-260907-ov3-planted -T /usr/bin/true -w 'gsd-260907-ov3-dummy'
```

Only after **both** variants fail to block may the run be scored INCONCLUSIVE.

## Teardown — mandatory whatever the outcome

```bash
# 1. GUARD: confirm the item is OURS. Non-prompting (no -w, no -g).
security find-generic-password -s com.gamelib.launcher -a steamgrid-api-key 2>&1 \
  | grep -c 'gsd-260907-ov3-planted'      # must be >= 1 before proceeding

# 2. Delete. The -a flag is LOAD-BEARING.
security delete-generic-password -s com.gamelib.launcher -a steamgrid-api-key

# 3. Re-census; must match F2 exactly.
for s in steam-refresh-token humble-session humble-csrf steamgrid-api-key; do
  printf '%-22s ' "$s"
  security find-generic-password -s com.gamelib.launcher -a "$s" >/dev/null 2>&1 \
    && echo PRESENT || echo absent
done
```

⚠ **`security delete-generic-password -s com.gamelib.launcher` WITHOUT `-a` deletes the first matching
item for the service, which may be `steam-refresh-token`.** The `-a steamgrid-api-key` is not
optional. Step 1's label guard exists so a mis-targeted delete is caught before it fires.

## Optional-run bars — all start at `(not run)`

| Bar | Assertion | Expected | Result |
|-----|-----------|----------|--------|
| R1 | `grep -cE 'SidecarKeyringSlotStore\(steamgrid-api-key\)\.getToken\(\): issuing keyring_get \(may prompt\) trigger=' repro-gamelib.log` | `>= 1` (slot attribution) | (not run) |
| R2 | `grep -cE 'SidecarKeyringSlotStore\(steamgrid-api-key\)\.getToken\(\): keyring_get failed: keyring:timeout trigger=[a-z]+ elapsed=[0-9]+ms' repro-gamelib.log` | `>= 1` | (not run) |
| R3 | elapsed from R2 | integer in `[45000, 46000]` — **do not widen after the fact** | (not run) |
| R4 | `grep -cF '[shell] keyring keyring_get timed out after 45s' repro-shell.log` | `>= 1` — **the one thing the archive cannot supply** | (not run) |
| R5 | `grep -c 'rustInvoke timed out after 60000ms' repro-gamelib.log repro-shell.log` | `0` | (not run) |
| R6 | post-teardown four-slot census | matches F2: PRESENT / absent / absent / absent | (not run) |
</reproduction_recipe>

<verification>
- Every scored bar in `260907-ov3-LIVE-GATE.md` runs against an **in-repo** evidence copy, not against
  `~/Library/Logs/GameLib/`. Verify by grepping the gate file for `Library/Logs` inside the scoring
  section — it may appear only in the provenance table, never in a scoring command.
- Zero production files changed:
  `git diff --name-only <pre-task-sha>..HEAD | grep -v '^\.planning/' | wc -l` is `0`.
- The Keychain census matches F2 (`PRESENT / absent / absent / absent`). If Task 3 was skipped this is
  trivially true and should be confirmed anyway.
- No `gsd-sdk state.*`, `roadmap.*` or `phase.complete` verb was invoked; neither `.planning/STATE.md`
  nor `.planning/ROADMAP.md` appears in any commit this task made.
- The todo exists in exactly one of `pending/` or `completed/`, never both; its 2026-08-25 disposition
  is byte-unchanged and the 2026-09-07 one is appended below it.
- Task 3's bars are either `(not run)` or backed by pasted operator output. No bar is filled by
  inference.
</verification>

<success_criteria>
1. Three `elapsed=`-bearing log archives preserved in-repo with faithfulness proven by md5, so the
   evidence can no longer rotate away.
2. All four discharge clauses plus X1, X2, D and NC scored individually against quoted lines, each
   with a runnable in-repo grep, and any disagreement recorded as NOT MET rather than accommodated.
3. The code-drift claim proven region-by-region against the explicitly named and read baseline
   `d629d9f30`, with its scope honestly narrowed (regions, not files).
4. The todo closed in `completed/` with a dated disposition quoting the measured figures, recording
   the negative control and the limitations, repeating the F-9 scope note, and carrying the "grep the
   log archive before staging a run to force an event" lesson.
5. The optional reproduction available and fully specified, gating nothing, with its bars honestly at
   `(not run)` unless an operator ran it.
6. Commits touch `.planning/` only, by explicit path, with the committed blob verified by content and
   not by exit code.
</success_criteria>

<output>
Create `.planning/quick/260907-ov3-live-gate-the-keyring-get-bounded-classi/260907-ov3-SUMMARY.md`
when done. It must record: the close verdict and which clause-level bars backed it; the exact
`elapsed=` figures; the baseline sha used for the drift proof; that the negative control was the
unwrapped `keyring_delete` sibling in the same session; whether Task 3 was run or skipped; and the
premise correction — that this todo was dischargeable from the archive on 2026-08-24 and stayed open
because the gate documents were re-read and the log archive was not.
</output>
