---
phase: quick-260922-ok3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - meta/findDeadcode.cjs
  - meta/__tests__/findDeadcode.test.ts
  - .planning/todos/pending/2026-09-22-pre-push-hook-cannot-pass-on-a-windows-checkout.md
  - .planning/todos/completed/2026-09-22-pre-push-hook-cannot-pass-on-a-windows-checkout.md
  - .planning/STATE.md
  - .planning/quick/260922-nx4-suppress-gamelib-nsis-install-time-regis/deferred-items.md
autonomous: true
requirements: [QUICK-260922-ok3]

must_haves:
  truths:
    - "On this Windows checkout, `bash .husky/pre-push` exits 0 with no --no-verify"
    - "`git add --renormalize .` after the .gitattributes change stages ZERO blob changes (repo content unchanged; only working-tree EOL on Windows changes)"
    - "`git ls-files --eol` reports w/lf for every text file except the 6 CRLF *.evidence.txt captures, which stay i/crlf w/crlf byte-exact"
    - "find-deadcode produces the same identity for `\\meta\\x.ts:12 - foo` and `meta/x.ts:12 - foo`, proven by a host-OS-independent jest test that fails when the normalisation is removed"
    - "tauriShellSource.test.ts passes 146/146 on Windows (or the reason it does not is recorded)"
    - "macOS behaviour unchanged: LF tree already, forward-slash paths are a no-op for the normaliser"
  artifacts:
    - path: ".gitattributes"
      provides: "LF pin for text files + -text exemption for CRLF evidence captures"
      contains: "* text=auto eol=lf"
    - path: "meta/findDeadcode.cjs"
      provides: "separator-independent finding path"
    - path: "meta/__tests__/findDeadcode.test.ts"
      provides: "Windows-shaped line regression cases"
  key_links:
    - from: "meta/findDeadcode.cjs parseFinding"
      to: "identityOf / KNOWN_PARSE_ARTIFACTS / diffAgainstBaseline"
      via: "finding.path normalised to forward slashes, leading / stripped"
      pattern: "replace\\(/\\\\\\\\/g, '/'\\)"
---

<objective>
Make `.husky/pre-push` pass on a Windows checkout without `--no-verify`, fixing the two
machine-dependent defects in the spec todo:
1. CRLF working tree (core.autocrlf=true) vs Prettier's LF default → pin LF via `.gitattributes`.
2. `find-deadcode` identities carry Windows backslash paths (`\meta\x.ts`) vs forward-slash baselines → normalise.

Purpose: the upcoming Windows single-instance guard work will be pushed from Windows; the hook must be a real gate there.
Output: `.gitattributes` rule, LF Windows working tree, normalised `findDeadcode.cjs` + regression test, todo closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/todos/pending/2026-09-22-pre-push-hook-cannot-pass-on-a-windows-checkout.md
@.planning/quick/260922-nx4-suppress-gamelib-nsis-install-time-regis/deferred-items.md
@.husky/pre-push
@.gitattributes
@meta/findDeadcode.cjs
@meta/__tests__/findDeadcode.test.ts

<interfaces>
Measured facts (HEAD d17a9d226, Windows 11):
- `.husky/pre-push`: `pnpm codecheck && pnpm lint && pnpm prettier && pnpm i18n --fail-on-update && pnpm find-deadcode`
- `.husky/pre-commit` is effectively empty (`#pnpm lint-fix` commented) — commits are not gated.
- `.gitattributes` today: one line, `pnpm-lock.yaml linguist-generated=true`.
- Prettier config is `.prettierrc.json` (NOT `.prettierrc`): trailingComma none, tabWidth 2, semi false, singleQuote true. No endOfLine → Prettier 3 default "lf". DO NOT add endOfLine (rejected weaker option).
- `.prettierignore` already excludes `.planning`, `.claude`, `graphify-out`, etc.
- Exactly 6 tracked files are committed CRLF, and they are exactly the 6 tracked `*.evidence.txt` files (verified `git ls-files '*.evidence.txt'` returns only these 6, all `i/crlf`):
  .claude/skills/spike-findings-gamelib/sources/005b-bottle-to-host-tcp/bridge_out.evidence.txt
  .claude/skills/spike-findings-gamelib/sources/005c-min-steam_api-shim/shim_out.evidence.txt
  .claude/skills/spike-findings-gamelib/sources/006-cpp-vtable-abi/vtable_out.evidence.txt
  .planning/spikes/005b-bottle-to-host-tcp/bridge_out.evidence.txt
  .planning/spikes/005c-min-steam_api-shim/shim_out.evidence.txt
  .planning/spikes/006-cpp-vtable-abi/vtable_out.evidence.txt
- No tracked .bat/.cmd/.ps1/.nsi/.nsh/.reg files (count 0) → no eol=crlf rule needed.
- `.husky/post-checkout` runs `pnpm i && pnpm download-helper-binaries` — harmless if it fires.

From meta/findDeadcode.cjs:
- `const FINDING_RE = /^(.+):(\d+) - (.+?)( \(used in module\))?$/` (~line 114; comment above claims ts-prune strips cwd and a leading `/` — true on POSIX only).
- `function parseFinding(line)` → `{ path: match[1], line: Number(match[2]), name: match[3], usedInModule: match[4] !== undefined }` (~line 289-300). Throws on unparseable lines.
- `function identityOf(finding) { return \`${finding.path} - ${finding.name}\` }` (~line 322).
- `module.exports = { parseFinding, identityOf, partitionFindings, parseBaseline, diffAgainstBaseline, readIgnorePattern, collectFindings, KNOWN_PARSE_ARTIFACTS, excludeKnownParseArtifacts }`
- KNOWN_PARSE_ARTIFACTS are matched by identity, so they ALSO silently fail to exclude on Windows today — normalising at parse time fixes both.

From meta/__tests__/findDeadcode.test.ts: jest, `const findDeadcode = require('../findDeadcode.cjs')`, `describe('meta/findDeadcode.cjs parseFinding', ...)` with `test(...)` + `expect(finding).toEqual({...})`; `describe('meta/findDeadcode.cjs identityOf', ...)` at ~line 76; CLI describe at ~272 spawns the real gate and asserts exit 0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin LF in .gitattributes, prove renormalize is a no-op, re-materialise the Windows working tree as LF</name>
  <files>.gitattributes</files>
  <action>
Step 0 — baseline measurement BEFORE any change (spec requires it so real misformatting is not hidden by the EOL fix): run `pnpm exec prettier --check . --end-of-line auto` and record the count of files it reports (expected 0 after 96f15ef4a; if >0, list them in the SUMMARY — do NOT reformat them in this task; if they would make the hook fail after the EOL fix, fix only those files with `pnpm exec prettier --write <file>` in a separate `style(quick-260922-ok3): ...` commit and name them). Also record `pnpm exec jest src/backend/__tests__/tauriShellSource.test.ts` result (expected 144/146).

Step 1 — edit `.gitattributes`. Keep the existing `pnpm-lock.yaml linguist-generated=true` line. Add, with a short `#` comment block explaining why (Windows core.autocrlf=true produced a CRLF tree that fails `prettier --check .` and source-text tests; LF everywhere, per todo 2026-09-22):
- `* text=auto eol=lf`
- `*.evidence.txt -text` with a comment that these are raw spike captures committed byte-exact with CRLF and must never be normalised. Using the glob is acceptable because `git ls-files '*.evidence.txt'` matches exactly the 6 CRLF files and nothing else — re-verify that command still returns exactly those 6 before relying on it; if it returns more, list the 6 paths explicitly instead.
Order matters: the `-text` line must come AFTER `* text=auto eol=lf` (later lines win).
Do NOT add `binary` patterns: text=auto already leaves the 222 detected-binary files alone and renormalize must not change them; adding patterns is unnecessary scope.

Step 2 — prove zero blob changes. Run `git add --renormalize .` then `git status --porcelain` and `git diff --cached --stat`. The ONLY staged change allowed is `.gitattributes`. If any other path is staged, STOP: unstage with `git restore --staged .` (index-only, non-destructive), record which paths and their `git ls-files --eol` output, and report — do not commit a repo-content rewrite. Also confirm `git check-attr text eol -- meta/findDeadcode.cjs <one evidence path>` shows `text: auto, eol: lf` for the former and `text: unset` for the latter.

Step 3 — commit only `.gitattributes`: `fix(quick-260922-ok3): pin LF line endings in .gitattributes, exempt CRLF spike evidence`. No --no-verify (pre-commit hook is a no-op anyway).

Step 4 — re-materialise the working tree as LF. Guard first: run `git status --porcelain --untracked-files=no`; if its output is non-empty, ABORT this step and report (never rewrite a dirty tree). Only if empty: run `git rm --cached -r -q .` followed immediately by `git reset --hard HEAD`. This is non-destructive on a verified-clean tree: the first command only empties the index (no working-tree changes), the second rewrites tracked files from HEAD through the new eol=lf filter; untracked and ignored files (node_modules, src-tauri/target, public/bin) are untouched. If the permission system blocks either command, STOP and report — do not substitute other checkout/rm commands to work around it. After the reset, `git status --porcelain` must be empty.

Step 5 — verify: `git ls-files --eol | grep -v 'w/lf' | grep -v '^i/-text' | grep -v 'w/-text' | grep -v 'w/none'` should list only the 6 evidence files (i/crlf w/crlf attr/-text). Spot-check `git ls-files --eol src-tauri/src/main.rs` shows `i/lf w/lf attr/text=auto eol=lf`. Report the grep output verbatim in the SUMMARY.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -z "$(git status --porcelain --untracked-files=no)" && git ls-files --eol src-tauri/src/main.rs | grep -q 'w/lf' && test "$(git ls-files --eol | grep 'w/crlf' | wc -l)" -eq 6 && test "$(git ls-files --eol | grep 'w/crlf' | grep -c 'evidence.txt')" -eq 6</automated>
  </verify>
  <done>.gitattributes committed with `* text=auto eol=lf` + `*.evidence.txt -text`; renormalize staged nothing but .gitattributes; tree clean; every text file w/lf; exactly the 6 evidence files remain w/crlf.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalise ts-prune finding paths to forward slashes in findDeadcode.cjs (TDD, mutation-proven)</name>
  <files>meta/findDeadcode.cjs, meta/__tests__/findDeadcode.test.ts</files>
  <behavior>
    - parseFinding('\\meta\\x.ts:12 - foo') → { path: 'meta/x.ts', line: 12, name: 'foo', usedInModule: false }
    - parseFinding('\\meta\\x.ts:12 - foo (used in module)') → { path: 'meta/x.ts', line: 12, name: 'foo', usedInModule: true }
    - identityOf(parseFinding('\\meta\\x.ts:12 - foo')) === identityOf(parseFinding('meta/x.ts:12 - foo')) === 'meta/x.ts - foo'; same equality for the (used in module) variant
    - a nested Windows path 'src\\frontend\\a\\b.ts:3 - bar' (no leading separator) → path 'src/frontend/a/b.ts'
    - existing POSIX cases unchanged (all current tests still pass); only ONE leading '/' is stripped
  </behavior>
  <action>
RED: in `meta/__tests__/findDeadcode.test.ts` add a new `describe('meta/findDeadcode.cjs Windows path separators', ...)` block (place after the identityOf describe, same style: `test(...)`, `expect(...).toEqual/toBe`). Test inputs are literal strings with escaped backslashes so the test is host-OS independent — do NOT use `path.sep`, `path.win32`, or `process.platform` anywhere in the test. Cover the behaviour bullets above. Add a one-line comment referencing todo 2026-09-22 (ts-prune strips cwd, leaving `\meta\x.ts` on Windows, which every baseline line read as stale). Run `pnpm exec jest meta/__tests__/findDeadcode.test.ts -t "Windows path separators"` — must FAIL. Commit: `test(quick-260922-ok3): add failing Windows-path cases for findDeadcode identity`.

GREEN: in `meta/findDeadcode.cjs`, add a small pure helper `normaliseFindingPath(rawPath)` that replaces every backslash with a forward slash (use a global regex replace of `\\` → `/`, NOT `split(path.sep)` — `path.sep` is `/` on macOS so the unit test would not exercise it host-independently, and ts-prune output on Windows is what matters), then strips exactly one leading `/` if present. Call it inside `parseFinding` for `path` (so identityOf, KNOWN_PARSE_ARTIFACTS exclusion, partitioning and baseline diff all see the normalised path — normalising only in identityOf would leave KNOWN_PARSE_ARTIFACTS matching inconsistent). Update the comment above `FINDING_RE` to say the leading-`/` strip is POSIX-only and that Windows yields `\`-separated paths with a leading `\`, normalised by `normaliseFindingPath`. Export `normaliseFindingPath` in module.exports (keep the existing exports). Run the whole file `pnpm exec jest meta/__tests__/findDeadcode.test.ts` — all pass, including the CLI "exits 0 against the real repo" test (this is the end-to-end Windows proof). Commit: `fix(quick-260922-ok3): normalise ts-prune backslash paths in find-deadcode identities`.

MUTATION PROOF: temporarily make `normaliseFindingPath` return its input unchanged, run the new describe block, confirm it FAILS (record the failing test count), then restore the implementation and confirm green. Do not commit the mutation; confirm `git diff` is empty afterwards. Record the result in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm exec jest meta/__tests__/findDeadcode.test.ts && pnpm find-deadcode</automated>
  </verify>
  <done>New Windows-path tests pass, fail under mutation, all existing findDeadcode tests pass, `pnpm find-deadcode` exits 0 on Windows (unreachable/used-in-module both PASS).</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end hook verification on Windows, close the todo, update nx4 deferred item</name>
  <files>.planning/todos/completed/2026-09-22-pre-push-hook-cannot-pass-on-a-windows-checkout.md, .planning/STATE.md, .planning/quick/260922-nx4-suppress-gamelib-nsis-install-time-regis/deferred-items.md</files>
  <action>
Verify: run each pre-push step individually and record exit codes: `pnpm codecheck`, `pnpm lint`, `pnpm prettier`, `pnpm i18n --fail-on-update`, `pnpm find-deadcode`. Then run `bash .husky/pre-push` and record exit code (must be 0). Run `pnpm exec jest src/backend/__tests__/tauriShellSource.test.ts` — expect 146/146; if not, record exactly which tests fail and why (do NOT edit that test file in this task). If `i18n --fail-on-update` modified any file, report it rather than committing it.

Close the todo: `git mv .planning/todos/pending/2026-09-22-pre-push-hook-cannot-pass-on-a-windows-checkout.md .planning/todos/completed/`. Append a `## Resolution (2026-09-22, quick 260922-ok3)` section: commit hashes from Tasks 1 and 2; the `--end-of-line auto` baseline count from Task 1 Step 0; renormalize staged-blob result (only .gitattributes); the 6 evidence files kept CRLF via `*.evidence.txt -text`; per-step exit codes table (before → after); `.husky/pre-push` exit 0; tauriShellSource before/after counts; mutation-proof result; note that no .prettierrc.json endOfLine change was made; note macOS is unaffected (already LF; forward-slash paths pass through the normaliser unchanged) but has not been re-run on a Mac in this task. Add `status: completed` / `resolved: 2026-09-22` / `resolved_by: quick-260922-ok3` to its frontmatter, preserving existing keys.

STATE.md: remove ONLY this todo's line from the `### Pending Todos` list (around line 5514+) if such a line exists (grep for `pre-push-hook-cannot-pass` / "Pre-push hook cannot pass on a Windows"); if none exists, leave STATE.md untouched and say so. Do NOT edit the Quick Tasks table — the orchestrator owns that row.

nx4 deferred-items.md: append a short `**Resolved by quick 260922-ok3 (2026-09-22):**` paragraph under the tauriShellSource section stating the LF pin fixed it with the measured count (or, if still failing, what was observed). Do not rewrite the existing text.

Commit: `docs(quick-260922-ok3): close windows pre-push todo with measured results`. No --no-verify. Do not push.
  </action>
  <verify>
    <automated>bash .husky/pre-push && pnpm exec jest src/backend/__tests__/tauriShellSource.test.ts && test -f .planning/todos/completed/2026-09-22-pre-push-hook-cannot-pass-on-a-windows-checkout.md && test ! -f .planning/todos/pending/2026-09-22-pre-push-hook-cannot-pass-on-a-windows-checkout.md && ! grep -q 'pre-push-hook-cannot-pass' .planning/STATE.md</automated>
  </verify>
  <done>`.husky/pre-push` exits 0 on Windows; tauriShellSource 146/146 (or reason recorded); todo moved to completed with resolution; STATE.md pending line removed (Quick Tasks table untouched); nx4 deferred item annotated; docs commit made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git attributes → working tree | .gitattributes changes how every tracked file is materialised on checkout |
| ts-prune stdout → findDeadcode | tool output parsed into gate identities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ok3-01 | Tampering | .gitattributes eol rule | mitigate | `git add --renormalize .` must stage only .gitattributes; `*.evidence.txt -text` keeps the 6 CRLF captures byte-exact; abort on any other staged path |
| T-ok3-02 | Tampering / data loss | working-tree re-materialisation | mitigate | `git status --porcelain --untracked-files=no` must be empty before `git rm --cached -r` + `git reset --hard HEAD`; untracked/ignored files untouched; stop if permission-blocked, no workarounds |
| T-ok3-03 | Repudiation (gate bypass) | findDeadcode parse | mitigate | parseFinding still throws on unparseable lines; normalisation only rewrites separators, cannot drop findings; mutation-proven test |
| T-ok3-04 | Elevation | commit hooks | accept | no --no-verify used; no push performed |
</threat_model>

<verification>
- `git ls-files --eol`: all text files w/lf; exactly 6 evidence files i/crlf w/crlf attr/-text.
- `git add --renormalize .` staged nothing beyond .gitattributes.
- `pnpm exec jest meta/__tests__/findDeadcode.test.ts` green; mutation makes new cases red.
- Each pre-push step exit 0; `bash .husky/pre-push` exit 0 on Windows.
- tauriShellSource 146/146.
</verification>

<success_criteria>
The todo's "Done when" is met on Windows: `.husky/pre-push` exits 0 on a clean Windows checkout of main with no --no-verify; macOS unaffected by construction (LF tree, forward-slash no-op). No repo content blobs changed apart from .gitattributes, findDeadcode.cjs, its test, and planning docs.
</success_criteria>

<output>
Create `.planning/quick/260922-ok3-make-the-pre-push-hook-pass-on-a-windows/260922-ok3-SUMMARY.md` when done.
</output>
