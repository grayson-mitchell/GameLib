---
phase: quick-260922-7hg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx
  - .planning/todos/pending/2026-09-20-steam-key-dialog-input-has-no-css-rule-at-all.md
  - .planning/todos/pending/2026-09-21-redeem-steam-key-outcome-copy-classnames-have-no-css-rule.md
autonomous: true
requirements: [QUICK-260922-7HG]
must_haves:
  truths:
    - "The Steam-key input no longer renders with browser defaults: it is the shared TextInputField component, so it resolves against the SHIPPED `.textInputFieldWrapper input[type='text']` rule in src/frontend/components/UI/TextInputField/index.css. No new CSS rule is authored anywhere by this task."
    - "Behaviour is preserved exactly: autoFocus, disabled while busy, the tGamelib('redeemSteamKey.placeholder') placeholder, and the onChange that sets `key` AND clears a prior non-success outcome so the user can retry inline (D-06/D-08)."
    - "The dead sibling classNames redeemSteamKey__success and redeemSteamKey__error are NOT silently removed and NOT given an invented style: they are filed as their own pending todo, because the tone distinction they encode is a design decision, not a provable no-op."
  artifacts:
    - path: "src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx"
      provides: "Steam-key input rendered via the shared themed TextInputField"
      contains: "TextInputField"
    - path: ".planning/todos/completed/2026-09-20-steam-key-dialog-input-has-no-css-rule-at-all.md"
      provides: "The actioned todo, moved out of pending with its resolution recorded"
      contains: "260922-7hg"
    - path: ".planning/todos/pending/2026-09-21-redeem-steam-key-outcome-copy-classnames-have-no-css-rule.md"
      provides: "The out-of-scope sibling defect, filed rather than swept up"
      contains: "ready: human"
  key_links:
    - from: "src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx"
      to: "src/frontend/components/UI/TextInputField/index.css"
      via: "TextInputField renders .textInputFieldWrapper, whose input rule is the style this task inherits"
      pattern: "TextInputField"
---

<objective>
Action pending todo `2026-09-20-steam-key-dialog-input-has-no-css-rule-at-all.md`, whose one
remaining open item is a DESIGN DECISION (`ready: human`), not a measurement.

The measurement is already done and is not being re-run: quick `260921-pec` loaded the app's real
frontend into an off-screen WKWebView and found every painted property of this input
byte-identical to a pristine `<input>` in a stylesheet-free iframe. The input is unstyled. The
question left open was what it SHOULD be.

Decision taken: do not author a new rule. Render the input through `TextInputField`, the shared
themed input already used by EditGameDialog, SideloadDialog, CategoriesManager, WineSelector,
BranchSelector and the Settings sections. That reuses a SHIPPED, reviewed style instead of
inventing one, which is exactly what `260921-nub` declined to do when it removed the dead
`Dialog__input` className.
</objective>

<tasks>

### Task 1: Render the Steam-key input through the shared TextInputField

files: src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx

action: Import `TextInputField` from the `frontend/components/UI` barrel (line 7 of
`src/frontend/components/UI/index.tsx` exports it). Replace the raw `<input>` with it, passing
`htmlId="redeem-steam-key-input"`, `value={key}`, the existing `placeholder`, `autoFocus`,
`disabled={busy}`, and an `onChange` that keeps BOTH existing effects (set the key, clear a prior
non-success outcome). Note the prop-shape difference that is easy to get wrong: TextInputField's
`onChange` takes `(newValue: string)`, NOT a React change event — it drives the input from a
native `'input'` listener so the virtual keyboard works.

Do NOT pass a `label`: the dialog header already names the task, and a label would be a new
user-visible string requiring a `gamelib.json` key and 47 locale pairs. Placeholder-only is what
ships today and is what is preserved.

verify: `pnpm exec tsc --noEmit -p tsconfig.json` clean for this file; no raw `<input` left in the
component.

done: The dialog's text field resolves against `.textInputFieldWrapper input[type='text']`.

### Task 2: File the sibling dead-className defect rather than sweeping it in

files: .planning/todos/pending/2026-09-21-redeem-steam-key-outcome-copy-classnames-have-no-css-rule.md

action: `redeemSteamKey__success` and `redeemSteamKey__error` on the outcome `<p>` have no CSS rule
anywhere either — verified by a whole-repo, case-insensitive sweep of every `.css/.scss/.sass/.less`
file outside `node_modules`, which returns ZERO files mentioning `redeemsteamkey` at all. That is
the same defect class the parent todo names, but it is NOT the same fix: the classNames encode a
tone distinction (success vs error) that is currently invisible, so removing them destroys intent
and colouring them is an unreviewed visual change. File it with `ready: human`.

verify: `python3 .planning/todos/todo-frontmatter-gate.py` passes (severity/platform/ready present,
bare lowercase, in that order).

done: The sibling defect is recorded, not silently inherited or silently dropped.

### Task 3: Close the actioned todo

files: .planning/todos/pending/2026-09-20-steam-key-dialog-input-has-no-css-rule-at-all.md

action: `git mv` it to `.planning/todos/completed/` and append a `## Resolved` section naming this
quick task, the decision taken, and what was explicitly NOT done. Order matters: `git mv` commits
HEAD content and drops unstaged edits, so move FIRST, edit SECOND, and stage the moved path
explicitly.

verify: file absent from `pending/`, present in `completed/`, and `pnpm planning-gates` green.

done: No pending todo claims this input is unstyled once it is styled.

</tasks>
