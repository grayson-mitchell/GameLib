---
phase: quick-260815-qf0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/screens/Library/index.tsx
  - src/frontend/screens/Library/__tests__/filterChipRowPlacement.test.ts
  - src/frontend/screens/Library/components/FilterChipRow/index.scss
autonomous: false
requirements:
  - QF0-01

must_haves:
  truths:
    - "On relaunch with filters persisted, the active-filter chips are visible at the top of the library without scrolling."
    - "With zero active filters the library's top-of-scroll layout is byte-identical to today — no empty row, no new gap."
    - "A future edit that returns FilterChipRow below RecentlyPlayed fails an automated gate."
    - "The chip row's fallback chain for --navbar-active is unchanged, so the themeTokens census still passes."
  artifacts:
    - path: "src/frontend/screens/Library/index.tsx"
      provides: "FilterChipRow rendered immediately after <span id=\"top\" />, above RecentlyPlayed"
      contains: "FilterChipRow"
    - path: "src/frontend/screens/Library/__tests__/filterChipRowPlacement.test.ts"
      provides: "Comment-stripped source gate asserting FilterChipRow precedes RecentlyPlayed, RED-proven against the OLD order"
      min_lines: 60
    - path: "src/frontend/screens/Library/components/FilterChipRow/index.scss"
      provides: "Spacing decision for the hoisted position, recorded with rationale"
      contains: "--filter-active-color"
  key_links:
    - from: "src/frontend/screens/Library/__tests__/filterChipRowPlacement.test.ts"
      to: "src/frontend/screens/Library/index.tsx"
      via: "readFileSync + stripSourceComments + indexOf comparison"
      pattern: "stripSourceComments"
    - from: "src/frontend/screens/Library/index.tsx"
      to: "src/frontend/screens/Library/components/FilterChipRow"
      via: "JSX render inside .listing, first child after <span id=\"top\" />"
      pattern: "<FilterChipRow"
---

<objective>
Hoist `<FilterChipRow />` inside `.listing` so it renders immediately after `<span id="top" />`, above `RecentlyPlayed` — and lock that order with an automated gate.

Purpose: `.listing` is the scrolling container. Today the chip row sits fifth (after `RecentlyPlayed`, the Favourites lane, `LibraryHeader` and `AlphabetFilter`), so on relaunch with filters persisted the chips are below the fold and nothing on screen explains why the library looks short. That is the entire defect. `Skill("sketch-findings-gamelib")` → `references/library-filtering.md` states the governing rule directly: "Do not hide filter state in the panel alone. Without chips the user cannot tell why the [library is short]."

Output: one JSX move, one new source-gate suite, one recorded spacing decision on the chip row's own stylesheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Project skill (governs this surface — read before touching CSS):
`.claude/skills/sketch-findings-gamelib/SKILL.md` and its `references/library-filtering.md`

Source under change:
@src/frontend/screens/Library/index.tsx
@src/frontend/screens/Library/components/FilterChipRow/index.scss

Test idioms to copy (do not modify either file):
@src/frontend/screens/Library/__tests__/libraryHeaderVisibility.test.ts
@src/frontend/screens/Library/__tests__/libraryPipeline.test.ts
</context>

<interfaces>
<!-- Extracted from the codebase. Use these directly — no exploration needed. -->

The `.listing` block today, `src/frontend/screens/Library/index.tsx:959-987` (verbatim, current order):

```
959  <div className="listing">
960    <span id="top" />
961    {showRecentGames && (
962      <RecentlyPlayed ... />
967    )}
969    {showFavourites && !showFavouritesLibrary && ( ...Favourites lane... )}
983    <LibraryHeader list={libraryToShow} totalGames={unfilteredGameCount} />
985    {showAlphabetFilter && <AlphabetFilter />}
987    <FilterChipRow />
```

`FilterChipRow` self-suppression (`components/FilterChipRow/index.tsx:100-110`) — both guards
already exist, do not touch them:
- returns `null` (and `console.error`s) when `__isDefaultLibraryContext`
- returns `null` when `activeFilterCount === 0`  ← this is what keeps the unfiltered layout identical

`RecentlyPlayed` (`components/RecentlyPlayed/index.tsx:94-108`) returns `null` when
`recentGames.length === 0`; otherwise its FIRST rendered element is
`<h5 className="libraryHeader">{t('Recent', 'Played Recently')}</h5>`.

Adjacent CSS facts already gathered (do not re-derive):
| Rule | File:line | Spacing |
|---|---|---|
| `.listing` | `Library/index.css:91` | `display:flex; flex-direction:column; flex-grow:1` — **no padding, no gap** |
| `.libraryHeader` (the "Played Recently" h5) | `Library/index.css:42` | `padding: var(--space-md-fixed); margin: 0; position: sticky; top: 0; z-index: 9` |
| `.alphabet-filter-container` | `AlphabetFilter/index.css:22` | `margin: 8px var(--space-md-fixed)` |
| `.FilterChipRow` | `FilterChipRow/index.scss:31-34` | `display:flex; flex-wrap:wrap; gap: var(--space-md); padding-block: var(--space-2xs)` — **no inline padding** |

Stripper util (`src/backend/testUtils/stripSourceComments.ts`):
```typescript
export function stripSourceComments(source: string): string
export function stripTrailingLineComment(line: string): string
```
`stripSourceComments` removes block comments and whole lines that START with a comment marker;
it does NOT remove trailing `// ...`. Layer `stripTrailingLineComment` per line on top — this is
exactly what `libraryHeaderVisibility.test.ts:232-244` does (`readGated` / `gateSource`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate the render order, RED-prove it against the shipped OLD order, then hoist the chip row</name>
  <files>src/frontend/screens/Library/__tests__/filterChipRowPlacement.test.ts, src/frontend/screens/Library/index.tsx</files>
  <behavior>
    Write `filterChipRowPlacement.test.ts` FIRST, before any edit to `index.tsx`. Specs:

    - Stripper integrity: an inline specimen carrying `<FilterChipRow />` before `<RecentlyPlayed` ONLY inside a `//` line comment and inside a `/* */` block comment must NOT satisfy the ordering predicate; real code on that same specimen must survive stripping (proves the stripper is load-bearing, not deleting everything).
    - Ordering, real source: over comment-stripped `Library/index.tsx`, `indexOf('<FilterChipRow')` is greater than -1, `indexOf('<RecentlyPlayed')` is greater than -1, and the FilterChipRow index is LESS than the RecentlyPlayed index.
    - Known-bad specimen: the SAME predicate function applied to a specimen string that reproduces the OLD order (`<span id="top" />`, then `<RecentlyPlayed ... />`, then `<LibraryHeader ... />`, then `<FilterChipRow />`) must return false. This is the permanent RED proof and it must run the identical helper the real-source spec runs — not a re-implementation.
    - Single mount: `<FilterChipRow` occurs exactly once in the stripped source (a hoist that duplicates rather than moves must fail).
    - `<span id="top" />` still precedes `<FilterChipRow` (the back-to-top anchor stays first).
  </behavior>
  <action>
    Create `src/frontend/screens/Library/__tests__/filterChipRowPlacement.test.ts` following the `libraryHeaderVisibility.test.ts` idiom exactly: `readFileSync` + `stripSourceComments` + per-line `stripTrailingLineComment`, `testEnvironment: 'node'`, no jsdom, no render. There is no jsdom and no react-test-renderer in the Frontend jest project, and `Library/index.tsx`'s import graph is far too large to invoke — a source gate is the established shape for this file, not a fallback.

    Factor the predicate into ONE exported-in-module helper, e.g. `chipRowPrecedesRecentlyPlayed(gatedSource: string): boolean`, and drive BOTH the real-source spec and the known-bad specimen spec through it. A specimen checked by a hand-rebuilt copy of the assertion is a replica that drifts silently; route both through the identical call shape.

    RED PROOF PROTOCOL — perform in this order and record the raw output in the SUMMARY:
    1. With `index.tsx` UNCHANGED (it still carries the old order), run the suite. The real-source ordering spec MUST FAIL and the stripper-integrity and known-bad-specimen specs MUST PASS. Capture that exact failure line. This is a genuine RED against the OLD order — no deliberate breakage and no revert is required, so no `git` mutation of any kind is involved.
    2. `ts-jest` here is TRANSPILE-ONLY (`isolatedModules: true`). A RED proof resting on a TYPE error is VACUOUS and does not count. The failure recorded in step 1 must be an assertion failure on the index comparison, nothing else. If the suite fails to LOAD instead, that is also not a RED proof — fix the load error and re-run.
    3. Only then edit `index.tsx`.

    THE MOVE: in the `.listing` block, delete `<FilterChipRow />` from its current position at line ~987 (between `{showAlphabetFilter && <AlphabetFilter />}` and `{refreshing && ...}`) and re-insert it immediately after `<span id="top" />` on line 960, above the `{showRecentGames && (` block. Move the element only — no wrapper, no new conditional, no prop. `FilterChipRow` already self-suppresses on `activeFilterCount === 0`, so the unfiltered tree is unchanged.

    Add a comment at the new call site recording the KNOWN NUANCE so nobody later "corrects" the placement back: the chips now sit above `RecentlyPlayed`, a lane they only PARTLY govern — `RecentlyPlayed` receives `showHidden` and `onlyInstalled` but not the store or runnability facets. The user was told this and chose the hoist anyway; visibility above the fold is the point. State explicitly that making `RecentlyPlayed` honour the remaining facets is NOT the fix and is out of scope.

    Do NOT touch `FilterZeroResult`, `LibraryHeader`, `AlphabetFilter`, `RecentlyPlayed`, or the facet-group badge work landed by quick task `260815-opt`. Do NOT make the chip row sticky, do NOT add a Games-tab badge, do NOT auto-expand facet groups — all three were explicitly deferred by the user.

    FORBIDDEN, no exceptions: `git stash`, `git clean`, `git checkout`, `git restore <path>` without `--staged`, `git reset --hard`. A concurrent Phase 34.13 session shares this working tree and `.husky/post-checkout` runs a binary download that fails deterministically. If any revert becomes necessary, do it as a direct `Edit` back to recorded text and verify with `diff` against a scratchpad copy taken BEFORE the mutation.
  </action>
  <verify>
    <automated>npx jest src/frontend/screens/Library/__tests__/filterChipRowPlacement.test.ts</automated>
    <automated>npx jest src/frontend/screens/Library/__tests__/libraryPipeline.test.ts src/frontend/screens/Library/__tests__/libraryHeaderVisibility.test.ts src/frontend/screens/Library/__tests__/tier2Portal.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The new suite passes green after the move; the SUMMARY records the pre-move RED output verbatim, showing the ordering spec failing as an ASSERTION failure (not a type or load error) while the stripper-integrity and known-bad-specimen specs passed. `<FilterChipRow` appears exactly once in `index.tsx`, immediately after `<span id="top" />`. The three pre-existing Library suites and `tsc --noEmit` are unchanged and green.</done>
</task>

<task type="auto">
  <name>Task 2: Decide the hoisted row's spacing from the adjacent rules — and record the decision either way</name>
  <files>src/frontend/screens/Library/components/FilterChipRow/index.scss</files>
  <action>
    The chip row's `padding-block: var(--space-2xs)` was tuned for its OLD position, sandwiched between `AlphabetFilter` and the grid. Evaluate whether it still holds as `.listing`'s first visible child. Decide from the adjacent rules already tabulated in `<interfaces>` above — do not re-derive them, and do not assume.

    Two questions, answer both explicitly in the SUMMARY:

    1. INLINE alignment. `.listing` supplies no padding. `.libraryHeader` (the "Played Recently" h5 that will now sit directly beneath the chips) insets its text by `var(--space-md-fixed)`; `.alphabet-filter-container` uses the same `var(--space-md-fixed)` horizontal margin. `.FilterChipRow` declares no inline padding at all, so at the top of the container the chips will start flush against the container edge while the heading below them starts one `--space-md-fixed` in. If you add an inset, use `padding-inline: var(--space-md-fixed)` — the token both neighbours already use. Do not invent a new value and do not introduce a new custom property.

    2. BLOCK separation from the "Played Recently" heading. `.libraryHeader` already carries `padding: var(--space-md-fixed)` on all sides, which supplies separation from above. Judge whether `--space-2xs` above it is still enough. If you increase it, step to an existing token from `src/frontend/styles/_spacing.scss` — never a hard-coded length.

    CONCLUDING THAT NO CHANGE IS NEEDED IS A VALID OUTCOME. If that is your conclusion, say so explicitly in the SUMMARY and give the reason from the rules above. Do not change the file just to have changed something.

    HARD CONSTRAINTS on whatever you do change:
    - Any spacing you add lives on `.FilterChipRow` itself. NEVER on `.listing` and NEVER on `RecentlyPlayed` — those apply when the chips are absent and would open a gap at the top of an unfiltered library, which is precisely the regression this task must not cause.
    - Do NOT touch line 29's `--filter-active-color: var(--navbar-active, var(--accent-overlay, var(--accent)))`. `themeTokens.test.ts:83-86` runs a census asserting every consumer file carries the IDENTICAL chain; disturbing it breaks that gate. No new colour, no new custom property, no new bare `--navbar-active` consumer.
    - Every selector stays nested under the `.FilterChipRow` root. An unscoped selector has already leaked app-wide once in this project (34.10's `.MuiTabs-root` incident).
    - Add a short comment above any declaration you change recording WHY the hoisted position needed it, so the value is not later "cleaned up" back to the pre-hoist tuning.

    The same `git` prohibitions from Task 1 apply verbatim.
  </action>
  <verify>
    <automated>npx jest src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts</automated>
    <automated>npx jest src/frontend/screens/Library/__tests__/filterChipRowPlacement.test.ts</automated>
  </verify>
  <done>The themeTokens census passes unchanged. Either the stylesheet is unmodified with a written justification in the SUMMARY, or the change uses only existing spacing tokens, sits on `.FilterChipRow` alone, carries an explanatory comment, and leaves line 29 byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | This change relocates one JSX element within a single component's render tree and adjusts a colocated stylesheet. No new input is parsed, no IPC channel is added or altered, no persisted value is read or written, no external data reaches the renderer by a new path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QF0-01 | Information disclosure | `FilterChipRow` chip labels, now rendered above the fold | accept | Chip labels are the user's own filter selections (view, collection, store/runnability facet values, search term) and were already rendered in this same window one screen lower. Moving them up the same scroll container exposes nothing to a party who could not already see it. No account identifier, token, or path is rendered. |
| T-QF0-02 | Tampering | none — no package-manager install in this plan | accept | No `npm`/`pnpm`/`pip`/`cargo` install task exists here, so the package-legitimacy gate is not engaged. If any task in execution turns out to need a dependency, STOP and re-plan rather than installing. |
</threat_model>

<verification>
## Automated (must be green before the human gate)

- `npx jest src/frontend/screens/Library/__tests__/filterChipRowPlacement.test.ts` — the new order gate
- `npx jest src/frontend/screens/Library/__tests__/` — no regression in the four pre-existing Library suites
- `npx jest src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts` — the `--navbar-active` census is undisturbed
- `npx tsc --noEmit` — exit 0

## Human gate (BLOCKING) — appearance cannot be proven automatically

There is no CSS engine, no jsdom and no react-test-renderer in this project. Nothing above establishes that the hoisted row LOOKS right; the source gate proves ORDER and nothing else. These observations are OWED and must be recorded in the SUMMARY as **OWED, not passed** — do not mark them satisfied on the strength of a green suite.

Run `pnpm tauri:dev` (NEVER bare `tauri dev` — it serves a stale static bundle):

1. **Chips visible above the fold on relaunch.** Apply two or more filters in the Games library, quit, relaunch. At the top of the library with no scrolling, the chip row is visible and names the active filters, and `Clear all` is present.
2. **No gap when unfiltered.** Click `Clear all` (or relaunch with no filters persisted). The top of the library is byte-identical to the pre-change layout — no empty row, no extra whitespace above "Played Recently" or above the first grid lane.
3. **Adequate separation and alignment.** With filters active, the chips read as their own band, clearly separated from the "Played Recently" heading below, and their left edge relates sensibly to that heading's text.
4. **Chips still work from the new position.** Clicking a chip's x removes that one filter; `Clear all` clears them all; the grid updates.
5. **Theme survival.** Repeat observation 1 in at least `gruvbox_dark` and `dracula` (the two themes that define neither `--text-hover` nor `--navbar-active`) — the chips render with visible text and borders in both.

## Known nuance — RECORD, do not fix

The chips now sit above `RecentlyPlayed`, a lane they only partly govern: `RecentlyPlayed` receives `showHidden` and `onlyInstalled` but NOT the store or runnability facets. The user was informed and chose the hoist anyway. Do not attempt to make `RecentlyPlayed` honour the remaining facets, and do not move the row back on this basis.
</verification>

<success_criteria>
- `<FilterChipRow />` renders exactly once inside `.listing`, immediately after `<span id="top" />` and above the `showRecentGames` block.
- A comment at that call site records why the row sits above a lane it only partly governs.
- `filterChipRowPlacement.test.ts` exists, passes, and its RED against the OLD order is recorded verbatim in the SUMMARY as an assertion failure — not a type error, not a load error.
- The gate's known-bad specimen carrying the old order runs through the IDENTICAL predicate helper as the real-source spec.
- The spacing question is answered explicitly in the SUMMARY — either a token-only change on `.FilterChipRow` with a rationale comment, or a documented no-change.
- `FilterChipRow/index.scss:29`'s `--filter-active-color` chain is byte-identical; `themeTokens.test.ts` is green.
- Nothing in `FilterZeroResult`, `LibraryHeader`, `AlphabetFilter`, `RecentlyPlayed`, `.listing`'s CSS, or the `260815-opt` badge work is modified.
- No sticky row, no nav-tab badge, no facet auto-expand.
- No forbidden `git` command was run; `git stash list` is empty at hand-off.
- The five human-gate observations are recorded in the SUMMARY as OWED.
</success_criteria>

<output>
Create `.planning/quick/260815-qf0-hoist-filterchiprow-above-the-recently-p/260815-qf0-SUMMARY.md` when done.
</output>
