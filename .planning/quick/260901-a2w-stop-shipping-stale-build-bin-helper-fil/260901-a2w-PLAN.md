---
phase: quick-260901-a2w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - meta/pruneStaleHelperBinaries.ts
  - meta/__tests__/pruneStaleHelperBinaries.test.ts
  - meta/checkBuildBinMirror.ts
  - meta/__tests__/viteRendererConfig.test.ts
  - vite.config.ts
  - package.json
  - .planning/quick/260901-a2w-stop-shipping-stale-build-bin-helper-fil/260901-a2w-MEASUREMENTS.md
autonomous: true
requirements: [QT-260901-a2w-01, QT-260901-a2w-02, QT-260901-a2w-03]
user_setup: []

must_haves:
  truths:
    - "After `pnpm exec vite build`, the regular-file set under build/bin is exactly the regular-file set under public/bin — 0 entries on either side of the diff (182 on the build side today)."
    - "After `pnpm exec vite build`, the symlink set under build/bin (relPath -> target) is exactly the symlink set under public/bin (12 links today), so the F-34.9-01 Python.framework layout survives the new prune step."
    - "A build whose prune set is non-empty but whose public/bin is absent, empty or partially populated FAILS LOUDLY with a named reason and deletes nothing."
    - "A build whose prune set is empty (fresh CI checkout, no build/bin) proceeds silently — the guard is never reached and CI is not broken."
    - "`pnpm check:build-bin-mirror` exits non-zero when a stale file is re-injected into build/bin, and separately when a build/bin symlink is replaced by a real file."
  artifacts:
    - path: "meta/pruneStaleHelperBinaries.ts"
      provides: "lstat-based mirror walk, prune-set computation, public/bin population guard, vite buildStart plugin"
      exports: ["collectEntries", "computePruneSet", "assessPublicBin", "pruneStaleHelperBinaries", "pruneStaleHelperBinariesPlugin"]
    - path: "meta/__tests__/pruneStaleHelperBinaries.test.ts"
      provides: "regression suite incl. the FAILING direction of the guard"
      min_lines: 120
    - path: "meta/checkBuildBinMirror.ts"
      provides: "standalone build/bin vs public/bin mirror gate (files + symlinks + apparent bytes)"
    - path: ".planning/quick/260901-a2w-stop-shipping-stale-build-bin-helper-fil/260901-a2w-MEASUREMENTS.md"
      provides: "before/after numbers with the subtracted pair named explicitly"
  key_links:
    - from: "vite.config.ts"
      to: "meta/pruneStaleHelperBinaries.ts"
      via: "pruneStaleHelperBinariesPlugin() in the plugins array"
      pattern: "pruneStaleHelperBinariesPlugin\\(\\)"
    - from: "meta/pruneStaleHelperBinaries.ts"
      to: "meta/releaseTags.ts + meta/runnersOnedirDigests.json"
      via: "tag + darwin-layout-marker freshness component of assessPublicBin"
      pattern: "RELEASE_TAGS|runnersOnedirDigests"
    - from: "package.json"
      to: "meta/checkBuildBinMirror.ts"
      via: "check:build-bin-mirror script"
      pattern: "check:build-bin-mirror"
---

<objective>
`build/bin` ships 182 files that no longer exist in `public/bin` — 46.64 MiB of a
superseded Python 3.14 helper release (62 under `legendary`, 60 under `nile`, 60 under
`gogdl`, all beneath `arm64/darwin/*/_internal/`). Cause: `vite.config.ts` sets
`build.emptyOutDir: false` (correctly — `build/` also holds `main/`, `locales/`,
`preload/`, `sidecar-prep.blob` and the icon set), and vite's implicit publicDir copy only
ADDS. `meta/downloadHelperBinaries.ts` is tag-idempotent and cleans only `public/bin`.
Nothing in the pipeline has ever deleted from `build/bin`.

Purpose: stop shipping ~47 MB of dead helper payload, and make its return detectable
rather than silent.
Output: a mirror-prune vite plugin that runs at `buildStart` (strictly before the copy,
strictly before `preserveRunnerSymlinksPlugin`'s `closeBundle`), a hard guard that refuses
to prune against an unpopulated `public/bin`, a standalone mirror gate, and a measurements
record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@vite.config.ts
@meta/preserveRunnerSymlinks.ts
@meta/__tests__/viteRendererConfig.test.ts
@meta/releaseTags.ts
@.planning/quick/260901-8rm-shrink-tauri-macos-bundle-narrow-bundle-/260901-8rm-MEASUREMENTS.md

<measured_baseline>
Measured on this checkout, 2026-09-01, before any change:

  only-in-build regular files ....... 182   (all under arm64/darwin/{legendary,gogdl,nile}/_internal/)
  only-in-public regular files ......   0
  symlinks, build/bin ...............  12   identical relPath+target to public/bin
  symlinks, public/bin ..............  12
  build/bin  apparent bytes ......... 366,874,673
  public/bin apparent bytes ......... 317,967,812
  delta ............................. 48,906,861 B = 46.64 MiB   <- THE NUMBER

  public/bin/.release_tags .......... present, includes __darwin_layout
  public/bin/arm64/darwin/legendary . 110 files, exec bit set
  public/bin/arm64/darwin/nile ...... 102 files, exec bit set
  public/bin/arm64/darwin/gogdl .....  61 files, exec bit set

172 of the 182 stale entries carry a `3.14`/`-314-` marker; the remaining 10 are
`liblzma.5.dylib`, `libmpdec.4.dylib`, `libzstd.1.dylib`, `libsqlite3.dylib` — the 3.14
runtime's shared libraries. They are ghosts of a superseded onedir release.
</measured_baseline>

<the_du_trap>
DO NOT build any gate on `du -sk`. `du -sk build/bin` minus `du -sk public/bin` is
56,092 KiB today, but only 48,176 KiB of that is `arm64/darwin`. The other 7,916 KiB is
APFS block-allocation difference on four trees whose file lists AND apparent byte totals
are byte-identical (verified per-tree):

  x64/win32    du +1,028 KiB   apparent bytes IDENTICAL
  arm64/win32  du +2,176 KiB   apparent bytes IDENTICAL
  x64/linux    du +2,008 KiB   apparent bytes IDENTICAL
  arm64/linux  du +2,704 KiB   apparent bytes IDENTICAL

A `du`-based gate would therefore show a ~7.9 MiB residual after a PERFECT fix, and the
next reader would invent a cause for it. That is exactly the failure the immediately
preceding task recorded in `260901-8rm-MEASUREMENTS.md`'s RETRACTION: a size claim
computed from the WRONG PAIR, followed by two successive fabricated explanations for the
resulting phantom gap. Sum `stat -f %z` over `-type f` instead.
</the_du_trap>

<interfaces>
Contracts the executor needs, extracted from the codebase — no exploration required.

From `meta/preserveRunnerSymlinks.ts` (MUST keep working — F-34.9-01):
```typescript
export function collectSymlinks(rootDir: string): SymlinkRecord[]      // recurses ONLY on dirent.isDirectory() (lstat semantics)
export function restoreSymlinks(sourceDir: string, destDir: string): {
  restored: SymlinkRecord[]; skipped: SymlinkRecord[]; rejected: SymlinkRecord[]
}
export function preserveRunnerSymlinksPlugin(options?: {
  sourceDir?: string; destDir?: string
}): Plugin   // name 'gamelib-preserve-runner-symlinks', apply:'build', enforce:'post', hook: closeBundle
```
Its defaults are `join(__dirname,'..','public')` and `join(__dirname,'..','build')`, i.e.
whole-tree, not `bin/`-scoped.

From `meta/releaseTags.ts`: `RELEASE_TAGS` — a side-effect-free module, safe to import.

From `meta/__tests__/viteRendererConfig.test.ts`:
```typescript
const pluginNames = (config: UserConfig): string[]   // flattens nested plugin arrays, returns .name strings
const resolveConfigFor = (mode: 'production' | 'development'): UserConfig
```

`meta/verifyRunnerBundle.ts` holds `const FILE_COUNT_FLOOR = 20` (NOT exported) with the
rationale "a onedir tree holds ~100+ files; a repackaged onefile would hold a handful".
</interfaces>

<hard_constraints>
1. DO NOT set `build.emptyOutDir: true`. `build/` still holds `main/`, `preload/`,
   `locales/`, `sidecar-prep.blob`, the icon set and `webviewPreload.js`, written by other
   steps. `meta/__tests__/viteRendererConfig.test.ts` already pins `emptyOutDir === false`;
   that pin stays.
2. DO NOT `rm -rf build/bin`. Per the recorded
   `download-helper-binaries-is-tag-idempotent-not-presence-idempotent` finding, an `rm -rf`
   of a helper tree is NOT undone by re-running `pnpm download-helper-binaries` — that
   script decides from a TAG, not from what is on disk. Delete only entries absent from
   `public/bin`.
3. DO NOT import `meta/downloadHelperBinaries.ts` from anything that runs under vite.
   That module calls `main()` at module scope unless `process.env.JEST_WORKER_ID` is set —
   a vite build does not set it, so importing it would START A REAL DOWNLOAD mid-build.
   Recompute the darwin layout marker locally from `meta/runnersOnedirDigests.json`
   instead, and pin the local computation against `darwinLayoutMarker()` in a TEST (where
   `JEST_WORKER_ID` makes the import safe).
4. DO NOT touch the repo-wide red prettier / i18n husky gates. Push with `--no-verify`
   if they block.
</hard_constraints>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `public/bin` -> new delete logic | An externally-sourced, git-ignored vendored tree drives which paths get DELETED from `build/bin`. Its filenames and symlink targets are untrusted input (same input class as WR-01 / T-34.9G-03). |
| build pipeline -> developer disk | A build step now performs recursive deletes. Its blast radius must be provably confined to `build/bin`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-a2w-01 | Tampering | `pruneStaleHelperBinaries` delete path | mitigate | Every delete target is resolved through the `resolveDestPath` containment idiom already in `meta/preserveRunnerSymlinks.ts:91` — `resolve(root, relPath)` must equal `resolve(root)` or start with `resolve(root) + sep`, else THROW. Relative paths are built by the walk itself, never accepted from outside. |
| T-a2w-02 | Denial of Service | prune over an unpopulated `public/bin` | mitigate | `assessPublicBin` guard (Task 1). Non-empty prune set + failing guard = THROW before any delete. Data loss here is unrecoverable-in-practice because `download-helper-binaries` is tag-idempotent. |
| T-a2w-03 | Elevation of Privilege | symlink-following delete | mitigate | The walk recurses ONLY on `dirent.isDirectory()` (lstat semantics, mirroring `collectSymlinks`), so a symlink-to-directory is never traversed; `rmSync` itself lstats, so unlinking a symlink never follows it. A build/bin entry whose public counterpart differs IN KIND is deleted and left for the copy + restore to recreate. |
| T-a2w-04 | Information Disclosure | prune log output | accept | Logs relative paths under `build/bin` only. No secrets, no absolute home paths beyond what vite already prints. |
| T-a2w-SC | Tampering | npm/pip/cargo installs | n/a — mitigate by exclusion | This plan installs NO packages. Every import is already in the tree (`node:fs`, `node:path`, `node:crypto`, `vite`). No Package Legitimacy Gate is required because there is nothing to audit; if an executor finds itself typing `pnpm add`, STOP and re-scope. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror-prune module with a fail-loud public/bin guard</name>
  <files>meta/pruneStaleHelperBinaries.ts, meta/__tests__/pruneStaleHelperBinaries.test.ts</files>

  <behavior>
Write these tests FIRST, against temp trees built with `mkdtempSync`. Every case builds
both a fake `build/bin` and a fake `public/bin` and calls the exported functions directly —
no vite, no network, no real `public/bin`.

`collectEntries(root)` -> `Map<relPath, 'file'|'dir'|'symlink'>`:
  - T1  returns an empty Map for a non-existent root (never throws)
  - T2  classifies a symlink-to-directory as `'symlink'`, NOT `'dir'`, and does NOT emit
        any entry for paths beneath it (the recursion-through-a-symlink guard)
  - T3  uses '/' separators in relPaths on every platform

`computePruneSet(buildBinDir, publicBinDir)` -> `string[]` of TOP-MOST prunable relPaths:
  - T4  a file present in build but absent in public IS in the set
  - T5  a file present in both is NOT in the set
  - T6  a symlink present in both with the SAME target is NOT in the set
  - T7  a kind mismatch (build has a real directory where public has a symlink) puts the
        build path in the set — and its children are NOT separately listed (minimality)
  - T8  a whole directory absent from public is emitted ONCE, not once per descendant
  - T9  an entry present in public but absent in build produces NOTHING (this function
        never reports the reverse direction)

`assessPublicBin(publicBinDir)` -> `{ ok: boolean; reasons: string[] }`. `ok` is true only
when ALL of:
  P1  `<publicBinDir>/.release_tags` exists, parses as JSON, every key of `RELEASE_TAGS`
      matches its stored value, and stored `__darwin_layout` equals the locally recomputed
      marker (sha256 over `JSON.stringify({layout, digests})` with digest keys SORTED,
      read from `meta/runnersOnedirDigests.json`).
  P2  for each of `legendary`, `gogdl`, `nile`: `<publicBinDir>/arm64/darwin/{r}/{r}` is a
      regular file with an exec bit set, AND `<publicBinDir>/arm64/darwin/{r}` contains at
      least 21 regular files -- i.e. MORE THAN 20. `meta/verifyRunnerBundle.ts:445`
      fails on `fileCount <= FILE_COUNT_FLOOR`, so the floor is EXCLUSIVE; re-declare it as
      `> 20`, not `>= 20` (the `FILE_COUNT_FLOOR` value from
      `meta/verifyRunnerBundle.ts`, re-declared locally with a comment naming its source
      and its rationale: a repackaged onefile would hold a handful).
  - T10 fully-populated fixture -> `ok: true`, `reasons: []`
  - T11 missing `.release_tags` -> `ok: false`, a reason naming `.release_tags`
  - T12 `__darwin_layout` mutated by one char -> `ok: false`, a reason naming the layout marker
  - T13 THE PARTIAL CASE — `nile` present and complete, `gogdl` present but holding only 3
        files -> `ok: false`, a reason naming `gogdl` and the file-count floor. This is the
        case that actually loses data; a bare `existsSync` would pass it.
  - T14 a runner binary present but with no exec bit -> `ok: false`

`pruneStaleHelperBinaries(buildBinDir, publicBinDir)` -> `{ pruned: string[]; bytesFreed: number; guardEvaluated: boolean }`:
  - T15 EMPTY PRUNE SET IS A SILENT NO-OP: identical trees -> `pruned: []`,
        `guardEvaluated: false`, no throw — EVEN WHEN `publicBinDir` DOES NOT EXIST and
        `buildBinDir` does not exist either. (This is what keeps a fresh CI checkout green;
        `.github/workflows/release-tauri.yml` runs `pnpm exec vite build` and CI has no
        pre-existing `build/bin`.)
  - T16 NON-EMPTY PRUNE SET + FAILING GUARD THROWS AND DELETES NOTHING: populated build,
        empty public -> throws; assert every build file still exists afterward. The message
        contains the refused entry count and every `reasons[]` string.
  - T17 the same for the PARTIAL public fixture from T13.
  - T18 non-empty prune set + passing guard -> the stale entries are gone, the shared
        entries and the shared symlinks are untouched (assert `lstatSync().isSymbolicLink()`
        and `readlinkSync()` target on a surviving link), and `bytesFreed` equals the summed
        apparent size of the deleted regular files.
  - T19 idempotence: a second call over the just-pruned tree returns `pruned: []` and does
        not throw.
  - T20 containment: a crafted relPath cannot escape. Assert `resolveDestPath`-style
        rejection by unit-testing containment directly with `'../../etc'`. Use the ALREADY-EXPORTED
        `resolveDestPath` from `meta/preserveRunnerSymlinks.ts:91` -- import and reuse it, do NOT
        reimplement it and do NOT add a new containment export to this module.
  - T21 the locally-recomputed darwin layout marker EQUALS `darwinLayoutMarker()` imported
        from `meta/downloadHelperBinaries.ts`. This import is safe HERE ONLY because jest
        sets `JEST_WORKER_ID`, which is precisely what suppresses that module's `main()`.
        Add a comment saying so, so nobody copies the import into build-time code.
  </behavior>

  <action>
Create `meta/pruneStaleHelperBinaries.ts` implementing exactly the contracts above, then
`pruneStaleHelperBinariesPlugin(options?: { buildBinDir?: string; publicBinDir?: string }): Plugin`:

  - `name: 'gamelib-prune-stale-helper-binaries'`
  - `apply: 'build'`, `enforce: 'pre'`
  - hook: `buildStart` — and NO `closeBundle` hook. `buildStart` fires at rollup input
    resolution, strictly before vite's publicDir copy (which happens in the write phase),
    which is in turn strictly before `preserveRunnerSymlinksPlugin`'s `closeBundle`. The
    two plugins therefore share no hook and their order is a property of the vite build
    lifecycle, not of array position. Say this in a comment — it is the F4 safety argument.
  - defaults: `join(__dirname, '..', 'build', 'bin')` and `join(__dirname, '..', 'public', 'bin')`.
    Note in a comment that these are `bin/`-SCOPED, unlike `preserveRunnerSymlinksPlugin`'s
    whole-tree defaults, because `build/` legitimately holds outputs with no `public/`
    counterpart (`main/`, `preload/`, `assets/`, `sidecar-prep.blob`).
  - logs one line: `[prune-stale-helper-binaries] pruned N entr(y|ies), F bytes freed` —
    or `nothing to prune` when the set is empty. Lets the throw propagate untouched.

Write a module docblock recording: the 182/46.64 MiB baseline; that `emptyOutDir` must stay
false and why; that `rm -rf build/bin` is unsafe because `download-helper-binaries` is
tag-idempotent, naming the guard as the mitigation; and the `JEST_WORKER_ID` import hazard
from constraint 3.
  </action>

  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && rm -f /tmp/a2w-prune.json && pnpm exec jest --selectProjects Meta meta/__tests__/pruneStaleHelperBinaries.test.ts --json --outputFile=/tmp/a2w-prune.json >/dev/null 2>&1; node -e "let r;try{r=require('/tmp/a2w-prune.json')}catch(e){console.error('FAIL: no jest json report written');process.exit(1)};if(!r.success||r.numTotalTests<21){console.error('FAIL success='+r.success+' tests='+r.numTotalTests+' (expected >=21 passing)');process.exit(1)};console.log('PASS tests='+r.numTotalTests)"</automated>
  </verify>

  <done>21+ tests pass. The suite exercises the guard's FAILING direction (T16, T17) and
  asserts that nothing was deleted on that path — not merely that a throw occurred. Nothing
  outside `meta/` is touched yet; `vite.config.ts` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the plugin into vite, add the standalone mirror gate</name>
  <files>vite.config.ts, meta/__tests__/viteRendererConfig.test.ts, meta/checkBuildBinMirror.ts, package.json</files>

  <action>
1. `vite.config.ts` — import `pruneStaleHelperBinariesPlugin` from
   `./meta/pruneStaleHelperBinaries` and add `pruneStaleHelperBinariesPlugin()` to the
   `plugins` array immediately BEFORE `preserveRunnerSymlinksPlugin()`. Add a comment block
   in the same register as the existing F-34.9-01 comment at line 118: state that
   `emptyOutDir` stays false (line 107) so vite's publicDir copy only ever ADDS, that this
   plugin supplies the missing subtraction as a MIRROR-PRUNE rather than an `rm -rf`, that
   it runs at `buildStart` and so cannot race the symlink restore at `closeBundle`, and
   that it refuses to run at all when `public/bin` is not fully populated. Do NOT touch
   `emptyOutDir`, `outDir`, `base`, `envPrefix` or any other key.

2. `meta/__tests__/viteRendererConfig.test.ts` — add tests inside the existing
   `describe.each(['production','development'])` block, reusing the file's `pluginNames`
   helper:
   - `pluginNames(config)` contains `'gamelib-prune-stale-helper-binaries'`.
   - ORDERING, asserted by HOOK IDENTITY not array index: locate both plugin objects in the
     flattened plugin list by `.name`; assert the prune plugin exposes `buildStart` and does
     NOT expose `closeBundle`, and that the symlink plugin exposes `closeBundle`. This is
     the assertion that actually protects F4 — an array reorder cannot make it vacuous, and
     it would fail if someone "simplified" the prune into `closeBundle`.
   - `config.build?.emptyOutDir` is still `false` (the existing pin — leave it, and add a
     comment cross-referencing this task so a future reader does not "fix" the stale-file
     problem by flipping it).

3. Create `meta/checkBuildBinMirror.ts` — a standalone CLI importing `collectEntries` from
   `meta/pruneStaleHelperBinaries`. It compares `build/bin` against `public/bin` and exits
   NON-ZERO unless ALL of:
   - a. zero entries present in build but absent-or-kind-mismatched in public
   - b. zero entries present in public but absent-or-kind-mismatched in build
   - c. symlink maps equal in BOTH directions, comparing relPath AND `readlinkSync` target
     (F6's `find -type f` diff is blind to symlinks by construction; this is the separate
     symlink assertion)
   - d. summed apparent bytes (`statSync().size` over regular files, via `lstat`-guarded
     walk) equal on both sides
   - e. ANTI-VACUITY: `public/bin` holds at least 1 regular file and at least 1 symlink is
     present on at least one side, or `public/bin` holds at least 1 regular file and both
     sides hold 0 symlinks. Exit non-zero with `refusing to certify a mirror over an empty
     public/bin` when `public/bin` has 0 regular files — otherwise a wiped checkout would
     report a trivially passing mirror. State this rationale in a comment.
   On success print one line with both file counts, both symlink counts, both byte totals
   and the delta. On failure print every offending relPath (capped at 40 with an
   `...and N more` suffix) and exit 1. No `--json` needed.
   No `main()`-under-import hazard: this file is CLI-only and imported by nothing.

4. `package.json` — add `"check:build-bin-mirror": "node meta/runTs.cjs --bundle --platform=node --target=node22 meta/checkBuildBinMirror.ts"` to `scripts`, placed next to the
   other `verify:` / `check:` entries. Match the surrounding style exactly.
  </action>

  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && pnpm exec tsc --noEmit && pnpm exec jest --selectProjects Meta meta/__tests__/viteRendererConfig.test.ts --json --outputFile=/tmp/a2w-vite.json >/dev/null 2>&1; node -e "let r;try{r=require('/tmp/a2w-vite.json')}catch(e){console.error('FAIL: no jest json report');process.exit(1)};if(!r.success){console.error('FAIL vite config suite');process.exit(1)};console.log('PASS tests='+r.numTotalTests)" && node -e "const s=require('/Users/graysonmitchell/Projects/GameLib/package.json').scripts;if(!s['check:build-bin-mirror']){console.error('FAIL: script missing');process.exit(1)};console.log('PASS script present')"</automated>
  </verify>

  <done>`tsc --noEmit` clean. The vite config suite passes with the two new assertions.
  `pnpm check:build-bin-mirror` is runnable and, on the UNFIXED tree, reports 182
  build-only entries and exits 1.</done>
</task>

<task type="auto">
  <name>Task 3: Live gate, mutation proof, and the measurements record</name>
  <files>.planning/quick/260901-a2w-stop-shipping-stale-build-bin-helper-fil/260901-a2w-MEASUREMENTS.md</files>

  <action>
Run the gate for real. Do NOT run a Tauri build — `pnpm exec vite build` is sufficient and
cheap, per F6.

STEP A — capture BEFORE (the tree is currently unfixed-output; if a build already ran under
Task 2, re-derive rather than trusting the baseline table):
  `pnpm check:build-bin-mirror` -> expect exit 1, 182 build-only entries.
  Record: build/bin apparent bytes, public/bin apparent bytes, delta, symlink counts.

STEP B — `pnpm exec vite build` (redirect to a log; on non-zero exit, print the last 40
lines and stop — do not proceed over a failed build).

STEP C — `pnpm check:build-bin-mirror` -> MUST exit 0. Record the after numbers.

STEP D — MUTATION PROOF. The mirror gate passes trivially on a correct tree, so prove both
of its directions can FAIL. This repo's standing lesson is that a gate whose failing
direction was never exercised proves nothing.
  D1. Re-inject one stale file:
      `mkdir -p build/bin/arm64/darwin/nile/_internal && : > build/bin/arm64/darwin/nile/_internal/libzstd.1.dylib`
      -> `pnpm check:build-bin-mirror` MUST exit 1 naming that path. Then
      `rm build/bin/arm64/darwin/nile/_internal/libzstd.1.dylib`.
  D2. Break one symlink (this is the F4 direction that F6's `-type f` diff CANNOT see):
      pick a real link from `find build/bin -type l | head -1`, record its `readlink`
      target, `rm` it and `touch` a regular file in its place -> `pnpm check:build-bin-mirror`
      MUST exit 1 naming a symlink mismatch. Restore with `rm` + `ln -s <target>`, then
      re-run the gate and confirm exit 0.
  D3. Prove the SAFETY guard fires in the field, not only in unit tests: with a populated
      `build/bin`, run the plugin's prune against a temp EMPTY directory standing in for
      `public/bin` and confirm it throws and deletes nothing —
      `node meta/runTs.cjs --bundle --platform=node --target=node22 -e` is not available, so
      do this with a throwaway script under the scratchpad that imports
      `pruneStaleHelperBinaries` and calls it with `(<repo>/build/bin, <empty tmpdir>)`
      inside a try/catch, then asserts `find build/bin -type f | wc -l` is unchanged
      before and after. Delete the throwaway script afterward.

STEP E — write `260901-a2w-MEASUREMENTS.md` containing:
  - a before/after table
  - a section headed `THE PAIR THIS GATE SUBTRACTS`, stating verbatim: the saving is
    `Σ apparent bytes of regular files under build/bin` BEFORE minus the SAME quantity
    AFTER, on the SAME checkout, with the SAME `public/bin` contents, and with no
    `pnpm download-helper-binaries` run in between. Expected value 48,906,861 B (46.64 MiB).
    The post-fix invariant is that `build/bin` and `public/bin` apparent-byte totals are
    EQUAL (delta 0).
  - a subsection `NOT THIS PAIR`, listing the two rejected subtractions and why, with the
    per-tree `du`-vs-apparent table from `<the_du_trap>` above reproduced as evidence:
      (i)  `du -sk build/bin - du -sk public/bin` (= 56,092 KiB) — 7,916 KiB of it is APFS
           block allocation on four trees that are byte-identical; a `du` gate leaves a
           phantom 7.9 MiB residual after a perfect fix. This is the `260901-8rm`
           RETRACTION's exact failure mode.
      (ii) any DMG or installed-`.app` delta — NOT MEASURED HERE. The Tauri bundle copy
           adds its own ~45 MiB of symlink dereferencing on the darwin trees (see the 8rm
           retraction); that inflation is pre-existing and this task neither causes nor
           removes it.
  - a `PREDICTION (unmeasured)` line, explicitly labelled as such: because all 182 stale
    entries are real `.so`/`.dylib` files copied 1:1 by the bundler (not symlinks), the
    shipped saving should be approximately the same 48.9 MB — but no release build was made
    in this task and that figure is NOT a measurement.
  - the D1/D2/D3 mutation-proof results, each with the exact command and observed exit code.
  </action>

  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && pnpm exec vite build > /tmp/a2w-vite-build.log 2>&1 || { echo "VITE BUILD FAILED:"; tail -40 /tmp/a2w-vite-build.log; exit 1; } && pnpm check:build-bin-mirror && diff <(cd build/bin && find . -type f | sort) <(cd public/bin && find . -type f | sort) && diff <(cd build/bin && find . -type l -exec sh -c 'echo "$1 -> $(readlink "$1")"' _ {} \; | sort) <(cd public/bin && find . -type l -exec sh -c 'echo "$1 -> $(readlink "$1")"' _ {} \; | sort) && test "$(find build/bin -type l | wc -l | tr -d ' ')" -eq 12 && echo "GATE PASS: 0 file diff, 0 symlink diff, 12 symlinks intact"</automated>
  </verify>

  <done>`pnpm exec vite build` succeeds; `pnpm check:build-bin-mirror` exits 0; the F6 file
  diff returns 0 lines (was 182); the symlink diff returns 0 lines and 12 symlinks survive;
  `grep -c 'preserve-runner-symlinks. restored 12' /tmp/a2w-vite-build.log` is 1 (the
  symlink plugin still ran and still restored all 12 — proving the prune did not starve it).
  MEASUREMENTS.md exists, names the subtracted pair, names both rejected pairs, and records
  three mutation proofs with observed exit codes.</done>
</task>

</tasks>

<verification>
Run in order, from the repo root:

1. `pnpm exec tsc --noEmit` — clean.
2. `pnpm exec jest --selectProjects Meta meta/__tests__/pruneStaleHelperBinaries.test.ts` — 21+ pass.
3. `pnpm exec jest --selectProjects Meta meta/__tests__/viteRendererConfig.test.ts` — pass, including the hook-identity ordering assertion.
4. `pnpm exec jest --selectProjects Meta meta/__tests__/preserveRunnerSymlinks.test.ts` — UNCHANGED and still passing (regression check on F4's owner).
5. `pnpm exec vite build` — exit 0, log contains `restored 12 symlink(s), skipped 0, rejected 0`.
6. `pnpm check:build-bin-mirror` — exit 0.
7. `diff <(cd build/bin && find . -type f | sort) <(cd public/bin && find . -type f | sort)` — 0 lines (was 182).
8. Mutation proofs D1/D2/D3 from Task 3 — each observed to FAIL the gate, then restored.

Guard rails that must NOT have moved:
- `vite.config.ts` `build.emptyOutDir` is still `false`.
- `preserveRunnerSymlinksPlugin()` is still in the plugins array and still uses `closeBundle`.
- No new entry in `package.json` `dependencies` or `devDependencies`.
  Check: `git diff -- package.json | grep -E '^\+' | grep -vE 'check:build-bin-mirror|^\+\+\+'` prints nothing.
</verification>

<success_criteria>
- After `pnpm exec vite build`, `build/bin` and `public/bin` have identical regular-file
  sets (0 vs 182 before), identical symlink sets (12 links, relPath AND target), and equal
  apparent-byte totals (delta 0 vs 48,906,861 B before).
- A prune against an absent, empty or partially-populated `public/bin` THROWS and deletes
  nothing — proven in unit tests (both the empty and the partial fixture) and once against
  the live tree.
- A prune with an empty prune set is a silent no-op that never evaluates the guard, so
  `.github/workflows/release-tauri.yml`'s `pnpm exec vite build` on a fresh checkout is
  unaffected.
- `pnpm check:build-bin-mirror` fails on a re-injected stale file AND on a replaced
  symlink — both directions observed, not assumed.
- `build.emptyOutDir` is still `false`; no package was installed.
- MEASUREMENTS.md names the subtracted pair, names the `du` pair and the DMG pair as
  explicitly rejected with reasons, and labels the shipped-saving figure as an unmeasured
  prediction.
</success_criteria>

<output>
Create `.planning/quick/260901-a2w-stop-shipping-stale-build-bin-helper-fil/260901-a2w-SUMMARY.md` when done.
</output>
