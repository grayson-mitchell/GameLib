---
phase: quick-260926-ky9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-UAT.md
  - .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md
  - .planning/todos/pending/2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md
  - CLAUDE.md
autonomous: true
requirements: [QUICK-260926-ky9]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low # estimate-calibration: factor 1, sample_count 0, applied false

must_haves:
  truths:
    - "`node ~/.claude/gsd-core/bin/gsd-tools.cjs audit-uat --raw` reports `summary.parse_gap_files: 0`; `34.6-UAT.md` contributes no outstanding item and no parse gap; `32-HUMAN-UAT.md` item 1 appears as a `pending` item with no `parse_gap`"
    - "Against baseline `b97eca705`, `34.6-UAT.md` differs on exactly lines 73, 77, 83, 95, 122, each being `result: ` + `pass — ` + the original value byte-for-byte; its two `expected: |` blocks are untouched. `32-HUMAN-UAT.md` differs on exactly line 20: the `### ` heading became `**<same text>**`"
    - "CLAUDE.md's UAT section records that 260926-ky9 closed both parse gaps, with the measured `parse_gap_files: 0`, keeps both causes as worked examples, and no longer states the pre-fix `parse_gap_files: 2` figure in the present tense"
    - "The pending todo's title no longer says a gate and CI hold the suppression defect; every claim in the new title holds when checked live (gsd-core 1.14.0; gate file gone; 34.3/34.5 block-scalar files read; item D narrowed to `23.2-HUMAN-UAT.md`; milestone still v0.8)"
    - "The todo's existing body is unedited (the old body is an exact suffix of the new one); only a short dated note is prepended; every frontmatter key other than `title` is byte-identical and `todo-frontmatter-gate.py` passes"
    - "CLAUDE.md's formatter section cites `~/.claude/gsd-core/templates/phase-prompt.md` and `~/.claude/gsd-core/bin/lib/template.cjs`, states they do not carry the reminder (measured 0), keeps the outside-this-repo/unversioned/overwritten caveat, and names itself the only copy; the UAT section's cross-reference names the plan template"
    - "`pnpm planning-gates` prints `12/12 planning gates passed.`; `npx prettier --check CLAUDE.md` passes; three commits each touch exactly their named paths; `.gsd/` and the three root tarballs/scripts stay untracked; no file under `~/.claude/` is modified"
  artifacts:
    - path: ".planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-UAT.md"
      provides: "Five result values that gsd-core's RESULT_LINE_RE reads as pass"
      contains: "result: pass — **PASS**"
    - path: ".planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md"
      provides: "Item 1's block no longer cut short by an intervening heading"
      contains: "**CORRECTION 2026-08-22"
    - path: ".planning/todos/pending/2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md"
      provides: "Title that is true under gsd-core 1.14.0, plus a dated governing note"
      contains: "260926-ky9"
    - path: "CLAUDE.md"
      provides: "UAT section records the closure; formatter section cites real gsd-core paths honestly"
      contains: "gsd-core/templates/phase-prompt.md"
  key_links:
    - from: "34.6-UAT.md result lines"
      to: "~/.claude/gsd-core/bin/lib/uat.cjs RESULT_LINE_RE (line 1387) and UAT_PASS_RESULTS (line 722)"
      via: "a leading bare word `pass` is captured by `(\\w+)` and matches the pass set, so the heading is skipped without counting toward headingsSeen"
      pattern: "result: pass — "
    - from: "32-HUMAN-UAT.md item 1 heading (line 16)"
      to: "item 1's `result:` line (about line 81)"
      via: "parseUatItemsWithStats slices each `### N.` heading to the next heading of any level; no heading may sit between them"
      pattern: "^result: \\[pending"
    - from: "CLAUDE.md UAT section, `Upstream files.` paragraph"
      to: "CLAUDE.md formatter section, last paragraph"
      via: "'the same caveat the formatter section below records for the plan template' must name what that section actually records"
      pattern: "records for the plan template."
    - from: "todo title `PARKED pending the v0.8 milestone advancing`"
      to: ".planning/STATE.md frontmatter `milestone:`"
      via: "the unpark trigger recorded in the todo's PARKED section, item 5"
      pattern: "milestone: v0.8"
---

<objective>
Close the three follow-ups that quick task 260926-kkt's SUMMARY left after the move from
`get-shit-done-cc` 1.42.3 to `@opengsd/gsd-core` 1.14.0. Each is a planning record or a CLAUDE.md
paragraph that is now false, or a UAT file gsd-core cannot fully read.

1. Close the two live `audit-uat` parse gaps (`34.6-UAT.md` and `32-HUMAN-UAT.md`) using the fixes
   the operator approved, then record the closure in CLAUDE.md's UAT section.
2. Correct the stale title of the pending todo
   `2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md`, and prepend a short dated
   note, since the body's governing section repeats the same false claim.
3. Correct CLAUDE.md's formatter section, which claims the upstream plan template carries a
   prettier reminder. It cites a path that no longer exists, and the gsd-core copies carry no
   such reminder.

Purpose: stop CLAUDE.md and the todo from making claims that live measurement contradicts, and get
`audit-uat` back to a clean audit (`parse_gap_files: 0`).
Output: 2 UAT files edited on exact lines, 1 todo retitled with a prepended note, and 2 CLAUDE.md
paragraphs plus 1 cross-reference line corrected. One commit per task, three commits in total.

**Task order differs from the operator's list.** The parse-gap fix goes first (as the tracer)
because it is the one task that runs end to end: file on disk, then the gsd-core parser, then the
`audit-uat` JSON, then the CLAUDE.md record. It is proved by the real tool. The other two tasks do
not depend on it or on each other, except that tasks 1 and 3 both edit CLAUDE.md, so they run in
order.

**Planner measurement that corrects the brief. The executor must re-measure it and must not copy
it.** The brief and the 260926-kkt SUMMARY both describe item D as "18-UAT.md and
23.2-HUMAN-UAT.md are milestone-hidden, since both are absent from audit-uat". Measured at plan
time (2026-09-26, HEAD `b97eca705`), that is only half right:

- gsd-core's `listMilestonePhaseDirs` includes phases 17 and 18 and excludes only 08.1, 23.1,
  23.2 and 999.1.
- `18-UAT.md` is inside the window. It parses with `headingsSeen: 0` and 0 outstanding items,
  because all five of its results are `pass`. It is missing from `audit-uat` because it has
  nothing outstanding, not because a filter hides it.
- `23.2-HUMAN-UAT.md` parses to 1 item and is missing only because its phase directory is
  excluded.

So item D is still open, but only for `23.2-HUMAN-UAT.md`. Absence from `audit-uat` alone cannot
tell these two cases apart. That is why CHECK-CLAIMS below tests window membership and parser
output directly.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260926-kkt-retire-the-uat-visibility-gate-after-the/260926-kkt-SUMMARY.md

Read only the ranges named in each task's read_first. Do not read these files whole: CLAUDE.md
(~420 lines), `.planning/STATE.md` (1000+ lines), and the todo (521 lines).

Baseline for every diff check: `b97eca705`, which is HEAD at plan time. None of the four target
files has changed since then.

Measured at plan time, so the executor knows what "normal" looks like. Re-measure all of it; do
not copy it:

- `audit-uat --raw` before the fix: `total_items: 418`, `total_files: 56`, `parse_gap_files: 2`.
  `34.6-UAT.md` has 0 items, `parse_gap: true`, `unparsed_blocks: 5`. `32-HUMAN-UAT.md` has
  items `[[2,pending],[null,unknown],[null,unknown]]`, `parse_gap: true`, `unparsed_blocks: 1`.
- A simulated fix on scratch copies, run through `parseUatItemsWithStats`: `34.6-UAT.md` gives
  `headingsSeen 0` and items `[]`. Because the file then has nothing outstanding and nothing
  unread, `cmdAuditUat` leaves it out of `results` entirely (uat.cjs:183). `32-HUMAN-UAT.md` gives
  `headingsSeen 0` and items `[[1,pending],[2,pending],[null,unknown],[null,unknown]]`.
- Predicted totals after the fix: `parse_gap_files: 0`, `total_items: 419`, `total_files: 55`.
  Record whatever the live run actually shows.
- `~/.claude/gsd-core/VERSION` = `1.14.0`. `grep -ci 'prettier\|formatter'` returns 0 in both
  `~/.claude/gsd-core/templates/phase-prompt.md` and `~/.claude/gsd-core/bin/lib/template.cjs`.
  `~/.claude/get-shit-done/` does not exist. The CLAUDE.md paragraph making the template claim
  was added in `38bcad5b1` (2026-09-23).
- Baselines at `b97eca705`: `pnpm planning-gates` gives `12/12 planning gates passed.`;
  `python3 .planning/todos/todo-frontmatter-gate.py` gives `OK: 20 pending todo(s)`;
  `npx prettier --check CLAUDE.md` passes.

Named checks. Each one was tested at plan time against both a simulated correct edit (exit 0)
and the unedited tree (exit 1). Run them from the repo root exactly as written:

- CHECK-UAT: diff shape of the two UAT files against the baseline (Task 1 verify).
- CHECK-AUDIT: the live `audit-uat` post-condition (Task 1 verify).
- CHECK-CLAUDE-T1: CLAUDE.md changed only inside the UAT section's two named paragraphs (Task 1
  verify). It is valid only before Task 3 runs.
- CHECK-CLAIMS: every factual claim in the new todo title, measured live (Task 2 verify).
- CHECK-TODO: title and note shape, frontmatter and body preservation (Task 2 verify).
- CHECK-CLAUDE-FINAL: all CLAUDE.md regions, final state (Task 3 verify and final verification).

Standing rules for every task:
- Stage explicit paths only, with `git add <path>`. Never `git add -A`, `.` or `-u`. Never stage
  `.gsd/`, `gamelib-claude-state.tgz`, `gamelib-linux-setup.sh` or `gsd-1.42.3-mac.tgz`.
- Git has no identity configured on this machine. Commit with
  `git -c user.name="Grayson Mitchell" -c user.email="grayson.mitchell@gmail.com" commit`. Use a
  message of the form `docs(quick-260926-ky9): ...` and end it with the trailer line
  `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.
- Work in the main tree, not a worktree. Do not edit `.planning/STATE.md`, and never run any
  `gsd-sdk`/`gsd-tools` `state.*` write verb. The orchestrator's closing docs commit records this
  quick task.
- Do not modify anything under `~/.claude/`. That includes re-adding the prettier reminder to the
  gsd-core templates, which is out of scope.
- `.prettierignore` ignores `.planning`, so prettier over `.planning/` paths proves nothing. The
  formatter check here is `npx prettier --check CLAUDE.md`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Close the 34.6 and 32 UAT parse gaps end-to-end and record the closure in CLAUDE.md</name>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-UAT.md, .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md, CLAUDE.md</files>
  <precondition>`git diff --quiet b97eca705 -- CLAUDE.md .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-UAT.md .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md .planning/todos/pending/2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md && git diff --cached --quiet` exits 0, so all four targets still match the baseline and nothing is staged.</precondition>
  <read_first>
    - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-UAT.md lines 1-12 (frontmatter `status: complete`), 69-125 (the five tests), 190-240 (`## Gaps`, G-34.6-UAT-01/-02/-03)
    - .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md lines 14-84 (item 1 heading at 16, the CORRECTION heading at 20, `expected:` at ~74, `result:` at ~81)
    - CLAUDE.md lines 281-337 (the whole UAT item shape section)
    - ~/.claude/gsd-core/bin/lib/uat.cjs lines 1380-1400 (RESULT_LINE_RE) and 722 (UAT_PASS_RESULTS)
  </read_first>
  <action>
Step 1, the baseline. Run `node ~/.claude/gsd-core/bin/gsd-tools.cjs audit-uat --raw` and write
down `summary.total_items`, `summary.total_files` and `summary.parse_gap_files`, plus the
`34.6-UAT.md` and `32-HUMAN-UAT.md` entries. Expect `parse_gap_files: 2`.

Step 2, 34.6-UAT.md, operator-approved fix. On each of the five column-0 `result:` lines (73, 77,
83, 95, 122), insert the status word `pass` between `result: ` and the existing value. The
inserted text is the word pass, a space, an em dash (U+2014) and a space. Every existing character
after the insertion stays exactly as it was, bold markup included. Line 73, for example, becomes
`result: pass — **PASS** (2026-08-26 19:26). ...`.

Why pass is honest for all five: the frontmatter says `status: complete`, and the issues found
behind tests 3 and 4 are already routed to `## Gaps` entries G-34.6-UAT-01 and G-34.6-UAT-02. So
every test passed on what it asked.

Do not touch anything else in the file. That includes both `expected: |` blocks in
`## Current Test` (about lines 39 and 57); CLAUDE.md says they stay.

Step 3, 32-HUMAN-UAT.md, operator-approved fix. First confirm that line 20 is the only line
containing `#` between item 1's heading (line 16) and item 1's `result:` line (about 81). Plan
time found exactly one. If you find another heading in that range, stop and report it rather
than editing it. Then demote line 20 from a `### ` heading to a bold paragraph line: remove the
leading `### ` and wrap the rest in `**` on both sides. The text in between stays byte-identical.
The result is `**CORRECTION 2026-08-22 — both gates are dead, and one was already dead when this
was written**`.

Step 4, the end-to-end proof. Run CHECK-UAT and CHECK-AUDIT (see verify) and record the new
`total_items`, `total_files` and `parse_gap_files`. These are the numbers Step 5 writes into
CLAUDE.md. If `parse_gap_files` is not 0, stop and diagnose before you touch CLAUDE.md.

Step 5, CLAUDE.md. Keep the edit minimal and inside the UAT section only.

(a) In the paragraph that opens `**What changed, and when.**`, change the one word `reports` in
"Live `audit-uat --raw` reports 418" to `reported`. The 418/56/2 figures stay: they are the dated
pre-fix measurement used to compare against 1.42.3, and after this task they are no longer the
live state.

(b) In the paragraph that opens `**What still goes unread, and it is now LOUD.**`, keep both
causes as the worked examples. Put into past tense the verbs that describe the two files'
pre-fix state: "opens with bolded prose" becomes "opened", "sits between" becomes "sat", "ends at
that heading" becomes "ended", and "ever reaches" becomes "reached". Leave the fixture-probe
clause in present tense, because it describes parser behaviour that is still true. Keep the
sentence saying an audit with `parse_gap_files` above 0 is not a clean audit.

Then replace the paragraph's final sentence, the one saying no file was edited and both gaps are
still open, with a record of the closure. It must say three things:

- quick task 260926-ky9 closed both;
- each of `34.6-UAT.md`'s five values gained a leading bare `pass — ` status word with every
  following character kept, because all five were passes on what the test asked, and the issues
  behind tests 3 and 4 live in its `## Gaps` entries;
- `32-HUMAN-UAT.md`'s `### CORRECTION` heading became a bold paragraph line.

End it with the measured post-fix state, written literally as `parse_gap_files: 0` in backticks,
followed by the Step-4 totals (outstanding items across files).

Keep lines at roughly the section's existing width. Change nothing else in CLAUDE.md. Task 3
owns the formatter section and the cross-reference.

Step 6. Run the verify block. Then stage exactly the three paths and commit with the message
`docs(quick-260926-ky9): close the 34.6 and 32 UAT parse gaps under gsd-core`, using the identity
flags and trailer from context. Confirm with `git show --stat --format= HEAD` that exactly these
three paths are listed.
  </action>
  <verify>
    <automated>cd /home/graysonmitchell/GameLib && python3 -c '
import subprocess
def pair(p):
    o = subprocess.run(["git","show","b97eca705:"+p],capture_output=True,text=True,check=True).stdout.split("\n")
    return o, open(p).read().split("\n")
A = ".planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-UAT.md"
B = ".planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md"
o, n = pair(A); assert len(o) == len(n), "34.6 line count changed"
d = [i+1 for i,(x,y) in enumerate(zip(o,n)) if x != y]; assert d == [73,77,83,95,122], d
for i in d: assert o[i-1].startswith("result: **") and n[i-1] == "result: pass — " + o[i-1][len("result: "):], i
assert sum(1 for l in n if l.startswith("expected: |")) == 2, "expected block scalars touched"
o, n = pair(B); assert len(o) == len(n), "32 line count changed"
d = [i+1 for i,(x,y) in enumerate(zip(o,n)) if x != y]; assert d == [20], d
assert o[19].startswith("### CORRECTION 2026-08-22") and n[19] == "**" + o[19][4:] + "**", n[19]
print("CHECK-UAT OK")
' && node ~/.claude/gsd-core/bin/gsd-tools.cjs audit-uat --raw | node -e '
const j = JSON.parse(require("fs").readFileSync(0, "utf8"));
const s = j.summary;
const a = j.results.find(r => r.file === "34.6-UAT.md");
const b = j.results.find(r => r.file === "32-HUMAN-UAT.md");
const errs = [];
if (s.parse_gap_files !== 0) errs.push("parse_gap_files=" + s.parse_gap_files);
if (a && (a.items.length > 0 || a.parse_gap)) errs.push("34.6-UAT.md still reported");
if (!b || b.parse_gap || !b.items.some(i => i.test === 1 && i.result === "pending")) errs.push("32-HUMAN-UAT item 1 not a clean pending item");
if (errs.length) { console.error("FAIL: " + errs.join("; ")); process.exit(1); }
console.log("CHECK-AUDIT OK " + JSON.stringify({ total_items: s.total_items, total_files: s.total_files, parse_gap_files: s.parse_gap_files }));
' && python3 -c '
import subprocess
o = subprocess.run(["git","show","b97eca705:CLAUDE.md"],capture_output=True,text=True,check=True).stdout
n = open("CLAUDE.md").read()
s, e = "**What changed, and when.**", "**What is enforced, honestly.**"
assert n.count(s) == 1 and n.count(e) == 1, "section anchors missing or duplicated"
assert o.split(s)[0] == n.split(s)[0], "CLAUDE.md changed before the What-changed paragraph"
assert o.split(e)[1] == n.split(e)[1], "CLAUDE.md changed after the What-still-goes-unread paragraph"
r = n.split(s)[1].split(e)[0]
for need in ("260926-ky9", "`parse_gap_files: 0`", "reported 418"):
    assert need in r, "missing in UAT region: " + need
print("CHECK-CLAUDE-T1 OK")
' && npx prettier --check CLAUDE.md && pnpm -s planning-gates 2>&1 | tail -1 | grep -qx '12/12 planning gates passed.' && echo "planning-gates 12/12 OK"</automated>
  </verify>
  <done>
- CHECK-UAT, CHECK-AUDIT and CHECK-CLAUDE-T1 all print OK.
- `audit-uat --raw` shows `parse_gap_files: 0`, and `32-HUMAN-UAT.md` item 1 is `pending`.
- Prettier and `12/12 planning gates passed.` are green.
- One commit, touching exactly the two UAT files and CLAUDE.md.
- The measured before and after totals are written down for the SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retitle the audit-uat body block-scalar todo to what is true under gsd-core, with a short dated note</name>
  <files>.planning/todos/pending/2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md</files>
  <read_first>
    - that todo, lines 1-24 (frontmatter and the head of the governing `## PARKED 2026-09-18` section), 84-108 (PARKED items 5-6: the unpark trigger, the `getMilestonePhaseFilter` breadcrumb, and item 6's present-tense gate claim), 158-165 (item D as defined)
    - CLAUDE.md's todo-frontmatter convention (the `### Todo triage frontmatter (enforced by CI)` section): bare lowercase vocabulary
    - .planning/quick/260926-kkt-retire-the-uat-visibility-gate-after-the/260926-kkt-SUMMARY.md, Follow-ups item 1
  </read_first>
  <action>
Step 1, measure every claim before you write it. Run CHECK-CLAIMS (the first command in verify)
and keep its printed facts JSON. It checks, live:

- `~/.claude/gsd-core/VERSION` is 1.14.0.
- `.planning/uat-visibility-gate.py` does not exist. Also run
  `git log --diff-filter=D --format=%h -1 -- .planning/uat-visibility-gate.py` and expect
  `1fda931c3`.
- `34.3-UAT.md` and `34.5-UAT.md` both carry column-0 `expected: |` block scalars, and
  `audit-uat` still reads items from both. This is the proof that the whole-file suppression
  defect does not occur under gsd-core.
- `.planning/STATE.md` still says `milestone: v0.8`.
- Item D re-measured through `listMilestonePhaseDirs` and `parseUatItemsWithStats`. Phases 17
  and 18 are inside the window; 23.2 is not. `17-UAT.md` surfaces. `18-UAT.md` is all-pass: 0
  items, `headingsSeen` 0, absent. `23.2-HUMAN-UAT.md` parses to at least 1 item but is absent.
  Also run `grep -n getMilestonePhaseFilter ~/.claude/gsd-core/bin/lib/phase-locator.cjs` to
  confirm the old name is still called inside `listMilestonePhaseDirs`. That keeps the todo's
  breadcrumb meaningful.

If CHECK-CLAIMS fails, the world differs from plan time. Write the title and note from what you
measured, not from this plan, and record the difference as a deviation in the SUMMARY.

Step 2, the title. Replace only the `title:` value (line 3). It stays a single-line YAML
double-quoted scalar: no double-quote character and no backslash inside it. Backticks and `|` are
fine.

Keep these three historical clauses exactly as they are, because they are records of past
measurements: the 652-field / 51-file clause, the item A CLOSED-by-relocation clause, and the
closing "PARKED pending the v0.8 milestone advancing".

Replace the clause claiming that a gate ledgers the suppression defect and that CI asserts it.
The new clause says the whole-file `expected: |` suppression defect does not occur under
`@opengsd/gsd-core` 1.14.0, and that its gate was retired in 260926-kkt.

Make the item D clause precise: only latent item D remains, narrowed by re-measurement to
`23.2-HUMAN-UAT.md`, which is outside the gsd-core milestone window.

The planner's suggested wording, which you may adjust to match your measurements:
"`audit-uat` reads YAML blocks in document BODIES too — the 652-field body population is MEASURED
(51 files); the whole-file `expected: |` suppression defect does not occur under
`@opengsd/gsd-core` 1.14.0, and its gate was retired in 260926-kkt; item A (27-UAT.md) is CLOSED
by relocation; only latent item D remains, narrowed by re-measurement to `23.2-HUMAN-UAT.md`
(outside the gsd-core milestone window); PARKED pending the v0.8 milestone advancing". The title
must contain the substrings 1.14.0, 260926-kkt, 23.2-HUMAN-UAT.md, item A, item D, PARKED and
v0.8.

Step 3, the dated note. It is needed because PARKED item 6 still says in present tense that item
C's residue is held by the retired gate, which is the same false claim the title made. The file
calls itself append-only, and its newest governing section sits at the top. So insert one new
section directly after the frontmatter's closing `---` and its blank line, before the
`## PARKED 2026-09-18` heading, and follow it with one blank line.

Do not edit, reorder or delete any existing body line.

Heading: `## NOTE 2026-09-26 — gate retired, suppression defect gone under gsd-core, item D
narrowed to 23.2 (quick `260926-ky9`)`, all on one line.

Body: at most about 20 lines, in the file's existing voice. Cover these points:

1. This note governs where it contradicts the sections below, and nothing below was edited.
2. `.planning/uat-visibility-gate.py` was retired in quick 260926-kkt (deleted in `1fda931c3`),
   so every statement below that the gate holds, ledgers or asserts something in
   `pnpm planning-gates` is historical.
3. Under gsd-core 1.14.0, `expected: |` no longer suppresses a file. Give the measured item
   counts that `audit-uat` reads from `34.3-UAT.md` and `34.5-UAT.md`.
4. Item D, re-measured: 17 and 18 are inside the window, 23.2 is not. `18-UAT.md` is absent
   because it is all-pass, not because a filter hides it. This corrects the 260926-kkt SUMMARY's
   follow-up on that point. The SUMMARY itself is history and is not edited. Item D stays open
   for `23.2-HUMAN-UAT.md` only.
5. Not established here: why 23.2 is outside the window, and whether advancing past v0.8 brings
   it in. The unpark trigger in PARKED item 5 is therefore unchanged but unverified under
   gsd-core.
6. `severity`, `platform` and `ready` are unchanged.

Step 4. Leave the frontmatter alone apart from `title`: `severity: minor`, `platform: any`,
`ready: blocked`, `files:` and every other key stay byte-identical. The todo stays in `pending/`.

Run the verify block. Stage only this todo and commit with the message
`docs(quick-260926-ky9): correct the stale title of the audit-uat body block-scalar todo`, using
the identity flags and trailer. Confirm with `git show --stat --format= HEAD` that exactly one
path is listed.
  </action>
  <verify>
    <automated>cd /home/graysonmitchell/GameLib && node -e '
const fs = require("fs"), cp = require("child_process"), G = process.env.HOME + "/.claude/gsd-core/";
const pl = require(G + "bin/lib/phase-locator.cjs"), u = require(G + "bin/lib/uat.cjs");
const j = JSON.parse(cp.execFileSync("node", [G + "bin/gsd-tools.cjs", "audit-uat", "--raw"], { encoding: "utf8" }));
const dirs = pl.listMilestonePhaseDirs(".planning/phases", { cwd: process.cwd() }).value;
const dirOf = (pfx) => dirs.find((d) => d.startsWith(pfx));
const inAudit = (f) => j.results.some((r) => r.file === f);
const parse = (glob) => { const p = cp.execSync("ls " + glob, { encoding: "utf8" }).trim(); return u.parseUatItemsWithStats(fs.readFileSync(p, "utf8")); };
const errs = [], facts = {};
facts.version = fs.readFileSync(G + "VERSION", "utf8").trim(); if (facts.version !== "1.14.0") errs.push("gsd-core is " + facts.version);
if (fs.existsSync(".planning/uat-visibility-gate.py")) errs.push("gate file still exists");
for (const f of ["34.3-UAT.md", "34.5-UAT.md"]) { const r = j.results.find((x) => x.file === f); const p = cp.execSync("ls .planning/phases/*/" + f, { encoding: "utf8" }).trim(); facts[f] = r ? r.items.length : 0; if (!r || !r.items.length || !/^expected: \|/m.test(fs.readFileSync(p, "utf8"))) errs.push(f + " is not a block-scalar file that audit-uat reads"); }
facts.milestone = (fs.readFileSync(".planning/STATE.md", "utf8").match(/^milestone: (\S+)$/m) || [])[1]; if (facts.milestone !== "v0.8") errs.push("milestone is " + facts.milestone);
facts.window = { "17": !!dirOf("17-"), "18": !!dirOf("18-"), "23.2": !!dirOf("23.2-") };
if (!facts.window["17"] || !facts.window["18"] || facts.window["23.2"]) errs.push("milestone window differs from plan-time measurement");
const p17 = parse(".planning/phases/17-*/17-UAT.md"), p18 = parse(".planning/phases/18-*/18-UAT.md"), p232 = parse(".planning/phases/23.2-*/23.2-HUMAN-UAT.md");
facts.f17 = { items: p17.items.length, headingsSeen: p17.headingsSeen, inAudit: inAudit("17-UAT.md") };
facts.f18 = { items: p18.items.length, headingsSeen: p18.headingsSeen, inAudit: inAudit("18-UAT.md") };
facts.f232 = { items: p232.items.length, headingsSeen: p232.headingsSeen, inAudit: inAudit("23.2-HUMAN-UAT.md") };
if (!facts.f17.inAudit) errs.push("17-UAT.md not surfaced");
if (facts.f18.items !== 0 || facts.f18.headingsSeen !== 0 || facts.f18.inAudit) errs.push("18-UAT.md no longer nothing-outstanding");
if (facts.f232.items === 0 || facts.f232.inAudit) errs.push("23.2-HUMAN-UAT.md no longer hidden-with-content");
console.log(JSON.stringify(facts));
if (errs.length) { console.error("FAIL: " + errs.join("; ")); process.exit(1); }
console.log("CHECK-CLAIMS OK");
' && node -e '
const fs = require("fs"), cp = require("child_process"), yaml = require("js-yaml");
const P = ".planning/todos/pending/2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md";
const split = (t) => { const m = t.match(/^---\n([\s\S]*?)\n---\n([\s\S]*)$/); if (!m) throw new Error("no frontmatter"); return { fm: m[1], body: m[2] }; };
const o = split(cp.execFileSync("git", ["show", "b97eca705:" + P], { encoding: "utf8" }));
const n = split(fs.readFileSync(P, "utf8"));
const fo = yaml.load(o.fm), fn = yaml.load(n.fm);
const errs = [];
for (const k of Object.keys(fo)) if (k !== "title" && JSON.stringify(fo[k]) !== JSON.stringify(fn[k])) errs.push("frontmatter key changed: " + k);
if (Object.keys(fn).length !== Object.keys(fo).length) errs.push("frontmatter key set changed");
if (o.fm.split("\n").filter(l => !l.startsWith("title: ")).join("\n") !== n.fm.split("\n").filter(l => !l.startsWith("title: ")).join("\n")) errs.push("frontmatter bytes changed outside title");
const t = String(fn.title);
for (const need of ["1.14.0", "260926-kkt", "23.2-HUMAN-UAT.md", "item A", "item D", "PARKED", "v0.8"]) if (!t.includes(need)) errs.push("title missing: " + need);
for (const gone of ["CI-asserted", "ledgered by a gate"]) if (t.includes(gone)) errs.push("title still claims: " + gone);
if (!n.body.endsWith(o.body)) errs.push("existing body was edited, not prepended to");
const added = n.body.slice(0, n.body.length - o.body.length);
const addedLines = added.split("\n").length;
if (!/^\n## [^\n]*2026-09-26[^\n]*260926-ky9/.test(added)) errs.push("prepended note heading must carry 2026-09-26 and 260926-ky9");
if (addedLines > 30) errs.push("note is not short: " + addedLines + " lines");
if (errs.length) { console.error("FAIL: " + errs.join("; ")); process.exit(1); }
console.log("CHECK-TODO OK (" + addedLines + " prepended lines)");
' && python3 .planning/todos/todo-frontmatter-gate.py | tail -1 && pnpm -s planning-gates 2>&1 | tail -1 | grep -qx '12/12 planning gates passed.' && echo "planning-gates 12/12 OK"</automated>
  </verify>
  <done>
- CHECK-CLAIMS prints its facts JSON and OK.
- CHECK-TODO prints OK: the title carries the required substrings and none of the stale claim;
  the old body is an exact suffix of the new one; the note is at most 30 lines and its heading
  carries 2026-09-26 and 260926-ky9; frontmatter outside `title` is byte-identical.
- `todo-frontmatter-gate.py` reports OK and planning-gates is 12/12.
- One commit, touching exactly the todo.
- Prettier is not applicable here: `.planning` is in `.prettierignore`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct CLAUDE.md's formatter-section template claim for gsd-core, and the UAT cross-reference</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md from the `**What is enforced, honestly.**` paragraph through `<!-- GSD:conventions-end -->` (about lines 323-364 at baseline; Task 1 may have shifted them by a line or two). Grep for the anchors; do not re-read the UAT paragraphs Task 1 already edited.
  </read_first>
  <action>
Step 1, re-measure. Run the same case-insensitive line count for the words prettier or formatter
that this task's verify block runs, once against `~/.claude/gsd-core/templates/phase-prompt.md` and
once against `~/.claude/gsd-core/bin/lib/template.cjs` (expect 0 and 0). Run `ls -d ~/.claude/get-shit-done` (expect: no such directory). Run
`git log -S 'Both halves of the upstream plan template' --format='%h %ad' --date=short -- CLAUDE.md`
(expect `38bcad5b1 2026-09-23`). Do not modify either gsd-core file. Re-adding the reminder there
is out of scope.

Step 2, rewrite the last paragraph of the `### A formatter check belongs in every task's
<verify>` section: the paragraph that begins "Both halves of the upstream plan template". Leave
every paragraph above it in that section unchanged. The new paragraph must:

- say plainly that the gsd-core plan template does not carry this reminder, using the words "does
  not carry" or "do not carry";
- name both real files, `~/.claude/gsd-core/templates/phase-prompt.md` and
  `~/.claude/gsd-core/bin/lib/template.cjs`, with the measured result: on 2026-09-26 a
  case-insensitive grep for prettier or formatter returned 0 in each;
- say that the reminder this paragraph used to cite (added with it in `38bcad5b1`) was a hand
  edit to the pre-migration `get-shit-done-cc` 1.42.3 install's copies, and did not survive the
  move to `@opengsd/gsd-core`. Refer to the old install by package name and version only, not by
  its old filesystem path, because that directory no longer exists;
- keep the caveat in bold, as it is today: those files are outside this repo, unversioned,
  shared by every project on the machine, and a `gsd-core` upgrade will overwrite them. Keep the
  existing back-reference, "the same caveat this file already records for the UAT template",
  which stays true because the UAT section's `Upstream files.` paragraph records it. Add that a
  reminder re-added there would therefore not be durable either;
- end by stating that this section is the only copy of the requirement, using the words "only
  copy".

Step 3, the cross-reference. In the UAT section's `**Upstream files.**` paragraph, the sentence
says the caveat is the one "the formatter section below records for the UAT template". The
formatter section records that caveat for the plan template (`phase-prompt.md` and
`template.cjs`), not the UAT template. After Step 2 that is still what it records. So change the
single line reading "records for the UAT template." to "records for the plan template.". Change
nothing else in that paragraph.

Step 4. Run the verify block. If `npx prettier --check CLAUDE.md` fails, run
`npx prettier --write CLAUDE.md`, then re-run CHECK-CLAUDE-FINAL. It proves nothing outside the
permitted regions moved. Stage only CLAUDE.md and commit with the message
`docs(quick-260926-ky9): correct the formatter section's template-reminder claim for gsd-core`,
using the identity flags and trailer. Confirm with `git show --stat --format= HEAD` that exactly
one path is listed.
  </action>
  <verify>
    <automated>cd /home/graysonmitchell/GameLib && python3 -c '
import subprocess, re
o = subprocess.run(["git","show","b97eca705:CLAUDE.md"],capture_output=True,text=True,check=True).stdout
n = open("CLAUDE.md").read()
s, e, f, z = "**What changed, and when.**", "**What is enforced, honestly.**", "### A formatter check", "<!-- GSD:conventions-end -->"
K = "The verify block is what keeps the file formatted in the first place."
for a in (s, e, f, z, K): assert n.count(a) == 1, "anchor missing or duplicated: " + a
assert o.split(s)[0] == n.split(s)[0], "changed before the UAT What-changed paragraph"
assert o.split(z)[1] == n.split(z)[1], "changed after the conventions region"
r = n.split(s)[1].split(e)[0]
for need in ("260926-ky9", "`parse_gap_files: 0`", "reported 418"):
    assert need in r, "missing in UAT region: " + need
mo, mn = o.split(e)[1].split(f)[0], n.split(e)[1].split(f)[0]
assert mn == mo.replace("records for the UAT template.", "records for the plan template.") and mn != mo, "UAT cross-reference not adjusted exactly"
fo, fn = o.split(f)[1].split(z)[0], n.split(f)[1].split(z)[0]
assert fo.split(K)[0] == fn.split(K)[0], "formatter section changed above its last paragraph"
last = fn.split(K)[1]
for need in ("gsd-core/templates/phase-prompt.md", "gsd-core/bin/lib/template.cjs", "outside this repo", "unversioned", "overwrite", "only copy", "2026-09-26"):
    assert need in last, "missing in formatter paragraph: " + need
assert re.search(r"(?i)do(es)? not carry", last), "formatter paragraph must say the templates do not carry the reminder"
for gone in ("get-shit-done/templates", "now carry a reminder"):
    assert gone not in n, "stale text still present: " + gone
print("CHECK-CLAUDE-FINAL OK")
' && test "$(grep -ci 'prettier\|formatter' ~/.claude/gsd-core/templates/phase-prompt.md)" = 0 && test "$(grep -ci 'prettier\|formatter' ~/.claude/gsd-core/bin/lib/template.cjs)" = 0 && echo "gsd-core templates still carry no reminder (claim true, files untouched)" && npx prettier --check CLAUDE.md && pnpm -s planning-gates 2>&1 | tail -1 | grep -qx '12/12 planning gates passed.' && echo "planning-gates 12/12 OK"</automated>
  </verify>
  <done>
- CHECK-CLAUDE-FINAL prints OK. Outside the UAT section's two Task-1 paragraphs, the one
  cross-reference line and the formatter section's last paragraph, CLAUDE.md is byte-identical
  to `b97eca705`.
- The formatter paragraph names both gsd-core paths, the measured 0, the caveat, and "only copy".
- The gsd-core template files are untouched and still grep to 0.
- Prettier passes and planning-gates is 12/12.
- One commit, touching exactly CLAUDE.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | Commits are public (GameLib is a public fork). Anything staged is published on push. |
| planning records → future readers | CLAUDE.md and the pending todo are read as ground truth by later sessions. A false claim there misdirects future work. |
| repo → `~/.claude/gsd-core` | A tool install outside the repo that this task reads and must not write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ky9-01 | Information disclosure | `git add` in all three tasks | high | mitigate | Untracked `gamelib-claude-state.tgz` (Claude state), `.gsd/`, `gamelib-linux-setup.sh` and `gsd-1.42.3-mac.tgz` sit in the repo root. Every task stages explicit paths only, never `-A`, `.` or `-u`. `git show --stat --format= HEAD` after each commit must list exactly the task's paths. Final verification asserts all four are still untracked. |
| T-ky9-02 | Tampering | append-only history in the todo body and the two UAT files | medium | mitigate | CHECK-TODO requires the old todo body to be an exact suffix of the new one and the frontmatter outside `title` to be byte-identical. CHECK-UAT pins both UAT diffs to exact line numbers and exact transformations against `b97eca705`, so no historical prose can be silently rewritten. |
| T-ky9-03 | Repudiation / integrity | new claims written into CLAUDE.md and the todo title | medium | mitigate | Every factual claim is measured live by CHECK-CLAIMS / CHECK-AUDIT and the Task-3 grep before it is written. The item-D wording comes from window membership plus parser output, not from absence alone. Plan time already caught one false inference that way (`18-UAT.md`). |
| T-ky9-04 | Tampering | `~/.claude/gsd-core/` templates | low | mitigate | Out of scope and must not be modified. Task 3's verify re-asserts that both files still grep to 0, which proves they are untouched and that CLAUDE.md's claim about them is true. |
| T-ky9-05 | Tampering | `.planning/STATE.md` via `state.*` SDK verbs | medium | accept | No task edits STATE.md. The known whole-file corruption defect in those verbs (recorded in STATE.md itself) is avoided by not calling them. The orchestrator owns the closing docs commit. |
</threat_model>

<verification>
Run after Task 3, from `/home/graysonmitchell/GameLib`:

1. Run CHECK-UAT, CHECK-AUDIT (both from Task 1's verify), CHECK-CLAIMS, CHECK-TODO (both from
   Task 2's verify) and CHECK-CLAUDE-FINAL (Task 3's verify). All must print OK.
   CHECK-CLAUDE-T1 is deliberately not re-run: its tail assertion is valid only before Task 3.
2. `node ~/.claude/gsd-core/bin/gsd-tools.cjs audit-uat --raw` shows `summary.parse_gap_files` 0.
3. `pnpm planning-gates` prints `12/12 planning gates passed.`
4. `python3 .planning/todos/todo-frontmatter-gate.py` prints `OK: 20 pending todo(s) ...`.
5. `npx prettier --check CLAUDE.md` passes.
6. `git log --format='%h %s' -3` shows the three `docs(quick-260926-ky9): ...` commits in task
   order, each with the `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>` trailer.
   `git show --stat --format= <each>` lists exactly: {34.6-UAT.md, 32-HUMAN-UAT.md, CLAUDE.md},
   then {the todo}, then {CLAUDE.md}.
7. `git status --porcelain` still lists `?? .gsd/`, `?? gamelib-claude-state.tgz`,
   `?? gamelib-linux-setup.sh` and `?? gsd-1.42.3-mac.tgz`, and nothing else unexpected.
   `git diff --quiet HEAD -- .planning/STATE.md` exits 0.
</verification>

<success_criteria>
- `audit-uat` is a clean audit: `parse_gap_files: 0`. The five 34.6 results read as pass, and
  32-HUMAN-UAT item 1 is visible as pending. The only UAT lines changed are the six named ones.
- The todo's title makes no false claim about a gate or CI, and every clause in it holds when
  checked live. Item D is stated as narrowed to `23.2-HUMAN-UAT.md`. The body history is intact,
  with only a short dated note prepended.
- CLAUDE.md no longer cites a nonexistent template path, no longer claims the gsd-core templates
  carry the reminder, names itself the only copy, and its cross-reference points at the right
  template. The UAT section records the closure with the measured `parse_gap_files: 0`.
- Three atomic commits, all gates green, no untracked file staged, nothing under `~/.claude/`
  touched.
</success_criteria>

<output>
Create `.planning/quick/260926-ky9-close-gsd-core-migration-follow-ups-stal/260926-ky9-SUMMARY.md`
when done. It must record the before and after `audit-uat` summary figures, the CHECK-CLAIMS facts
JSON, the final todo title verbatim, and the three commit hashes. Record any difference between
live measurements and this plan's plan-time figures as a deviation. In particular, restate the
`18-UAT.md` correction to the 260926-kkt follow-up in the SUMMARY's Decisions section.
</output>
