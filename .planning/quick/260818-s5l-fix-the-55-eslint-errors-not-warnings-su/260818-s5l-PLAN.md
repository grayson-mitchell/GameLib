---
phase: quick-260818-s5l
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [LINT-DEBT-01]
files_modified:
  - meta/buildSidecarSea.ts
  - meta/lintTranslations.ts
  - meta/sidecarSeaFsShim.ts
  - meta/__tests__/i18nParserConfig.test.ts
  - meta/__tests__/preserveRunnerSymlinks.test.ts
  - src/backend/__tests__/longRunningChannels.test.ts
  - src/backend/__tests__/tauriShellSource.test.ts
  - src/backend/crossover_index/__tests__/index.test.ts
  - src/backend/downloadmanager/downloadqueue.ts
  - src/backend/humble/dedup.ts
  - src/backend/sidecar/electronStub.ts
  - src/backend/sidecar/handlers.ts
  - src/backend/sidecar/humbleFlowRegistration.ts
  - src/backend/sidecar/steamFlowRegistration.ts
  - src/backend/sidecar/storeWriteHandlers.ts
  - src/backend/sidecar/__tests__/appRootResolution.test.ts
  - src/backend/sidecar/__tests__/appShellFlows.test.ts
  - src/backend/sidecar/__tests__/bootstrap.test.ts
  - src/backend/sidecar/__tests__/devSecretVault.test.ts
  - src/backend/sidecar/__tests__/humbleFlows.test.ts
  - src/backend/sidecar/__tests__/oauthLoginCapture.test.ts
  - src/backend/sidecar/__tests__/runnerMiscFlows.test.ts
  - src/backend/sidecar/__tests__/steamFlows.test.ts
  - src/backend/storeManagers/gog/redist.ts
  - src/backend/storeManagers/steam/__tests__/lzmaNativeBinding.test.ts
  - src/preload/__tests__/framelessRuntime.test.ts

must_haves:
  truths:
    - "`pnpm lint` reports 0 errors (warnings unchanged, still ~3881)"
    - "`pnpm codecheck` exits 0"
    - "Full jest suite is green at the same pass/skip counts as the pre-change baseline"
    - "No new blanket / file-level / rule-broad eslint-disable was introduced; every surviving suppression is a single narrowly-scoped line with a written `--` justification"
    - "No production runtime behaviour changed — the three load-bearing production `require()` call sites still execute a real CJS `require`"
  artifacts:
    - path: ".planning/quick/260818-s5l-fix-the-55-eslint-errors-not-warnings-su/260818-s5l-SUMMARY.md"
      provides: "Per-category disposition record, incl. every retained suppression and why"
      contains: "no-require-imports"
  key_links:
    - from: "eslint.config.mjs"
      to: "the 26 touched files"
      via: "`npx eslint --cache . -f json` error count"
      pattern: "severity.*2"
---

<objective>
Clear all 55 eslint **errors** (severity 2) currently failing `.github/workflows/lint.yml`. Warnings
(~3881) are explicitly OUT OF SCOPE and must not be touched.

Purpose: CI's lint job is a SEPARATE gate from `pnpm codecheck` (which is `tsc --noEmit` only), so
this debt is invisible to the normal verification loop and has been accumulating unnoticed — see
the standing project lesson "a verifier passing `pnpm codecheck` says NOTHING about CI lint".

Output: 26 files with real fixes; zero eslint errors; unchanged runtime behaviour.

**Headline finding from planning — read before touching anything.** This repo is on
`typescript-eslint@^8.34.1`. v8 **removed** `@typescript-eslint/no-var-requires` and folded it into
`@typescript-eslint/no-require-imports`. Roughly two-thirds of the `no-require-imports` errors sit on
lines that ALREADY carry a written, justified
`// eslint-disable-next-line @typescript-eslint/no-var-requires` comment — those suppressions now
name a rule that no longer exists, so they suppress nothing. That is the actual defect. Retargeting a
stale, already-justified suppression to the live rule name is a **real fix**, not new debt. Adding a
*brand-new* suppression where an ES import would have worked is NOT acceptable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@eslint.config.mjs

**Tooling rule (repo-mandated).** `graphify-out/graph.json` exists. Before reading raw source for
orientation, run `graphify query "<question>"` / `graphify explain "<concept>"`. For this plan the
edits are all line-level and every line number is given below, so reading a **narrow range around a
named line** is the expected access pattern — graphify is for the "is this symbol used anywhere
else?" questions in Task 1, not for locating the lines.

**Concurrent-session git safety (standing project rule, violated twice before).** NEVER run
`git stash`, and never `git checkout -- <file>` (this repo's `.husky/post-checkout` hook runs a
download that fails deterministically). Use `git restore` if you must revert. Do not stage or commit
files outside the `files_modified` list.

**Authoritative error list.** The list embedded in the tasks below was captured fresh during planning
from `npx eslint --cache . -f json`. Re-derive it yourself first (Task 1 step 1) — if the count is
not 55, work from YOUR list and note the drift in the SUMMARY.

**Test-suite characteristics.** ~284 suites / ~5900 tests. Parallel runs intermittently trip a
documented cross-test frame-leak flake in unrelated sidecar tests; use `--runInBand` for the full
run. One suite is known to hit a sandbox V8 OOM independently of any change. Before calling any test
failure a regression, confirm it reproduces on the unmodified baseline.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the 31 type-and-deadness errors (unnecessary assertions, unused vars, redundant unions)</name>
  <files>
    meta/buildSidecarSea.ts,
    meta/lintTranslations.ts,
    meta/__tests__/preserveRunnerSymlinks.test.ts,
    src/backend/__tests__/tauriShellSource.test.ts,
    src/backend/crossover_index/__tests__/index.test.ts,
    src/backend/downloadmanager/downloadqueue.ts,
    src/backend/humble/dedup.ts,
    src/backend/sidecar/electronStub.ts,
    src/backend/sidecar/handlers.ts,
    src/backend/sidecar/steamFlowRegistration.ts,
    src/backend/sidecar/storeWriteHandlers.ts,
    src/backend/sidecar/__tests__/devSecretVault.test.ts,
    src/backend/sidecar/__tests__/humbleFlows.test.ts,
    src/backend/sidecar/__tests__/oauthLoginCapture.test.ts,
    src/backend/sidecar/__tests__/runnerMiscFlows.test.ts,
    src/backend/sidecar/__tests__/steamFlows.test.ts,
    src/backend/storeManagers/steam/__tests__/lzmaNativeBinding.test.ts
  </files>
  <action>
Step 1 — re-derive the authoritative list. Run
`npx eslint --cache . -f json -o /tmp/lint.json` then a small node/python one-liner that filters
`severity === 2` and groups by `ruleId`. Record the total. Planning measured 55.

Step 2 — `@typescript-eslint/no-unnecessary-type-assertion` (21 sites). Every one of these is an
assertion the compiler already proves redundant, so deleting it is type-safe by construction. Delete
the assertion (and any now-redundant wrapping parentheses); do not replace it with anything. Sites:
`meta/__tests__/preserveRunnerSymlinks.test.ts:321`, `meta/lintTranslations.ts:42`,
`src/backend/__tests__/tauriShellSource.test.ts:1437`,
`src/backend/crossover_index/__tests__/index.test.ts:29`,
`src/backend/sidecar/__tests__/devSecretVault.test.ts:58`,
`src/backend/sidecar/__tests__/humbleFlows.test.ts:1118`,
`src/backend/sidecar/__tests__/oauthLoginCapture.test.ts:137,550,559,617,719`,
`src/backend/sidecar/__tests__/runnerMiscFlows.test.ts:312,339,367,386`,
`src/backend/sidecar/handlers.ts:302,340`, `src/backend/sidecar/steamFlowRegistration.ts:319`,
`src/backend/sidecar/storeWriteHandlers.ts:123`,
`src/backend/storeManagers/steam/__tests__/lzmaNativeBinding.test.ts:175,196`.
`meta/lintTranslations.ts:42` is the one that needs care: the `as Namespace[]` sits after a
`.filter((ns): ns is Namespace => ...)` type predicate that already narrows it, so remove the
assertion AND the parenthesis pair that only existed to host it.
This rule IS auto-fixable. You may run `npx eslint --fix <those files>` to do the mechanical work,
but ONLY if you then read the complete `git diff` and confirm every single hunk corresponds to a line
on the list above — a bare `--fix` will also apply autofixes for warning-level rules in those files,
and warnings are out of scope. Revert any unrelated hunk.

Step 3 — `@typescript-eslint/no-unused-vars` (7 sites). Decide per site whether the binding is
genuinely dead or load-bearing. Do NOT reflexively rename to `_foo`; that hides the deadness.
  - `src/backend/humble/dedup.ts:4,5,6` — `normalizeTitle`, `titleSimilarity`,
    `isDlcFalsePositiveRisk` imported from `common/matching/titleMatch`; only `fuzzyMatch` is used.
    Delete the three names from the import list. Pure module, no side-effect import concern.
  - `src/backend/downloadmanager/downloadqueue.ts:7` — `Runner` in the `common/types` import list.
    Delete the name.
  - `src/backend/__tests__/tauriShellSource.test.ts:47` — `CARGO_TOML_PATH` is assigned but never
    read, and its comment claims it backs "Plan 03, Test 8 (REQ-34.4.2-10), the `objc2-web-kit`
    feature array assertion". INVESTIGATE BEFORE DELETING: grep the file for `Cargo.toml` and
    `objc2-web-kit`. If that assertion still exists but builds its path inline, point it at the
    constant instead (constant becomes used, error clears, duplication removed). If the assertion is
    GONE, the constant is dead — delete it together with the now-orphaned comment, and record in the
    SUMMARY that a REQ-34.4.2-10 test appears to have been lost, since a silently-vanished
    requirement test is a finding, not a cleanup.
  - `src/backend/sidecar/__tests__/humbleFlows.test.ts:89` (`registerHumbleFlows`) and
    `src/backend/sidecar/__tests__/steamFlows.test.ts:95` (`SteamUser`) — unused named imports in
    test files. Delete the import. THEN run that suite: if it fails, the import was load-bearing for
    module-load side effects, so restore it as a bare side-effect import
    (`import '../humbleFlowRegistration'`) with a one-line comment saying why. Note which of the two
    outcomes happened in the SUMMARY.

Step 4 — `@typescript-eslint/no-redundant-type-constituents` (3 sites).
  - `meta/buildSidecarSea.ts:304` (`buildPostjectArgv`) and `:409` (`buildCodesignArgv`) both declare
    `platform: NodeJS.Platform | string = process.platform`, which collapses to plain `string`.
    Narrow both to `NodeJS.Platform`. This is safe and verified during planning: the sole production
    caller is `injectBlob()` passing `triplePlatform(triple)`, whose declared return type is already
    `NodeJS.Platform` (`meta/buildSidecarSea.ts:470`), and every test caller in
    `meta/__tests__/buildSidecarSea.test.ts` passes a `'darwin'` / `'win32'` / `'linux'` literal.
  - `src/backend/sidecar/electronStub.ts:115` — the `IpcHandler` return type
    `unknown | Promise<unknown>` collapses to `unknown`. Replace with `unknown`. Keep the
    `Promise<unknown>` intent in a short trailing comment so the next reader does not "restore" it.

Do not touch any `no-require-imports`, `no-implied-eval`, or `no-this-alias` site in this task —
those are Tasks 2 and 3.
  </action>
  <verify>
    <automated>npx eslint --cache . -f json -o /tmp/lint-t1.json && node -e "const d=require('/tmp/lint-t1.json');const e=d.flatMap(f=>f.messages.filter(m=>m.severity===2).map(m=>m.ruleId));const left=e.filter(r=>['@typescript-eslint/no-unnecessary-type-assertion','@typescript-eslint/no-unused-vars','@typescript-eslint/no-redundant-type-constituents'].includes(r));console.log('task1 rules remaining:',left.length,'total errors:',e.length);process.exit(left.length===0?0:1)" && pnpm codecheck</automated>
  </verify>
  <done>Zero errors remain for the three Task 1 rules, `tsc --noEmit` exits 0, total error count has dropped from 55 to 24, and the `git diff` contains no hunk that is not traceable to a listed error line.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the 22 no-require-imports errors — convert where ESM works, retarget the stale suppressions where it does not</name>
  <files>
    meta/sidecarSeaFsShim.ts,
    meta/__tests__/i18nParserConfig.test.ts,
    src/backend/sidecar/humbleFlowRegistration.ts,
    src/backend/sidecar/__tests__/appRootResolution.test.ts,
    src/backend/sidecar/__tests__/appShellFlows.test.ts,
    src/backend/sidecar/__tests__/bootstrap.test.ts,
    src/backend/sidecar/__tests__/humbleFlows.test.ts,
    src/backend/storeManagers/gog/redist.ts,
    src/backend/storeManagers/steam/__tests__/lzmaNativeBinding.test.ts
  </files>
  <action>
First, confirm the stale-rule diagnosis rather than trusting this plan: run
`npx eslint --cache . --report-unused-disable-directives 2>&1 | grep -c "no-var-requires"` or simply
confirm `node_modules/typescript-eslint` resolves to v8.x and that
`@typescript-eslint/no-var-requires` is absent from the installed rule set. Record the verdict in the
SUMMARY — it is the whole justification for the disposition below.

**Disposition rule.** For each of the 22 sites, ask: *would a static top-level ES import produce
identical behaviour?*
  - YES → convert to a real ES import. This is the required outcome.
  - NO → the require is the mechanism, not an oversight. Write exactly one
    `// eslint-disable-next-line @typescript-eslint/no-require-imports -- <one-line reason>` on that
    line. Where a stale `no-var-requires` comment already exists with a written justification,
    REPLACE the rule name in place and keep the existing justification prose verbatim. Never widen a
    single-line disable into a block or file-level disable, and never add a new `/* eslint-disable */`
    region. The two existing block disables at
    `src/backend/sidecar/__tests__/bootstrap.test.ts:342`/`:345` may keep their block form (they wrap
    two adjacent lines) but must have their rule name retargeted.

**CONVERT (ESM works — no module-registry games involved):**
  - `src/backend/storeManagers/steam/__tests__/lzmaNativeBinding.test.ts:131,134` —
    `jest.spyOn(require('node:fs'), 'writeFileSync')` / `'rmSync'`. Add a top-level
    `import * as nodeFs from 'node:fs'` and spy on `nodeFs`. These two sit AFTER the
    `jest.isolateModules(...)` block, not inside it, so they are not part of the isolated registry —
    confirm that by reading lines 120-140 before editing. If spying on the ESM namespace object
    fails at runtime under this repo's ts-jest CJS transform, fall back to the suppression path and
    say so explicitly in the SUMMARY.

**SUPPRESS (the require is load-bearing — each already carries written justification):**
  - `meta/sidecarSeaFsShim.ts:32` — the file's own header (lines 25-31) states the reason: CJS
    `require('node:fs')` returns the mutable shared singleton so a property patch is visible to every
    other bundled module, whereas an ESM namespace binding is read-only and esbuild rejects
    reassignment. Retarget only.
  - `src/backend/sidecar/humbleFlowRegistration.ts:165` — guarded runtime `require('node:sea')` inside
    a `try`, deliberately so an unavailable builtin fails closed to PACKAGED. Retarget only.
  - `src/backend/storeManagers/gog/redist.ts:119` — synchronous lazy `require('..')` breaking the
    `gog/redist.ts <-> storeManagers/index.ts` cycle; the enclosing function is sync so `await
    import()` is not available. Retarget only.
  - `meta/__tests__/i18nParserConfig.test.ts:4` — `i18next-parser.config` is a CJS `.js` file loaded
    synchronously at module scope. Retarget only.
  - `src/backend/sidecar/__tests__/appRootResolution.test.ts:225` — inside `jest.isolateModules`.
    Retarget only.
  - `src/backend/sidecar/__tests__/appShellFlows.test.ts:764,772,776` — inside `jest.isolateModules`;
    the whole point is a fresh module registry. NO existing disable — add one per line with the
    reason "inside jest.isolateModules: a static import would resolve against the shared registry".
  - `src/backend/sidecar/__tests__/bootstrap.test.ts:234,241` — the assertion is literally
    `expect(() => require('backend/electron_store')).not.toThrow()`; a static import would move the
    load to module-eval time and destroy the test. NO existing disable — add one per line.
  - `src/backend/sidecar/__tests__/bootstrap.test.ts:236` — retarget the existing line disable.
  - `src/backend/sidecar/__tests__/bootstrap.test.ts:343,344` — retarget the existing block disable's
    rule name (lines 342 and 345).
  - `src/backend/sidecar/__tests__/humbleFlows.test.ts:1017,1021,1034,1038` — follow
    `jest.resetModules()` + `jest.doMock('node:sea', ...)`; a static import cannot pick up the fresh
    mock. Retarget only.
  - `src/backend/sidecar/__tests__/humbleFlows.test.ts:1124` — `require('../../humble/electronStores')`
    beside a `jest.requireActual` whose adjacent comment (lines 1112-1117) records that a plain
    static import previously produced the wrong instance. Retarget only.

Count your dispositions: CONVERT + SUPPRESS must sum to exactly the number of `no-require-imports`
errors your own Step-1 run reported. Any site you cannot place in one bucket with a stated reason is
a finding for the SUMMARY, not a silent suppression.
  </action>
  <verify>
    <automated>npx eslint --cache . -f json -o /tmp/lint-t2.json && node -e "const d=require('/tmp/lint-t2.json');const e=d.flatMap(f=>f.messages.filter(m=>m.severity===2));console.log('require errors:',e.filter(m=>m.ruleId==='@typescript-eslint/no-require-imports').length,'total:',e.length);process.exit(e.filter(m=>m.ruleId==='@typescript-eslint/no-require-imports').length===0?0:1)" && grep -rn "eslint-disable" --include='*.ts' meta src | grep -v '^\s*$' | grep "no-var-requires" | wc -l | grep -qx '0' && npx jest src/backend/sidecar/__tests__/bootstrap.test.ts src/backend/sidecar/__tests__/appShellFlows.test.ts src/backend/sidecar/__tests__/humbleFlows.test.ts src/backend/sidecar/__tests__/appRootResolution.test.ts src/backend/storeManagers/steam/__tests__/lzmaNativeBinding.test.ts meta/__tests__/i18nParserConfig.test.ts --runInBand</automated>
  </verify>
  <done>Zero `no-require-imports` errors; zero remaining references to the removed `no-var-requires` rule name anywhere in `meta/` or `src/`; every touched test suite passes; no new block-level or file-level eslint-disable region exists in the diff.</done>
</task>

<task type="auto">
  <name>Task 3: Fix no-implied-eval and no-this-alias, then run the full three-gate verification</name>
  <files>
    src/backend/__tests__/longRunningChannels.test.ts,
    src/preload/__tests__/framelessRuntime.test.ts
  </files>
  <action>
**`@typescript-eslint/no-this-alias` — `src/preload/__tests__/framelessRuntime.test.ts:104`.** Real
fix, no suppression. `FakeElement.closest()` opens with `let el: FakeElement | null = this`. Restructure
to test `this` directly and start the walk at the parent, preserving exact semantics (a self-match
still returns `this`; the walk still terminates at a null `parentElement`; a no-match still returns
`null`). Do not change `matches()`, `chain()`, or any assertion.

**`@typescript-eslint/no-implied-eval` — `src/backend/__tests__/longRunningChannels.test.ts:678`.**
Read lines 655-690 first. This is `parseMsConstantFromSource`, a test helper that extracts a
timeout constant's arithmetic expression out of real source text and evaluates it. Line 677 already
carries `// eslint-disable-next-line no-new-func -- expr is validated above to be numeric arithmetic
only`, and line 672 enforces that guard with `/^[\d\s*+\-\/().]+$/` before evaluation, throwing
otherwise. The base `no-new-func` rule is already suppressed with justification; the typed rule
`@typescript-eslint/no-implied-eval` simply is not covered by that comment. Extend the SAME
single-line disable to name both rules
(`// eslint-disable-next-line no-new-func, @typescript-eslint/no-implied-eval -- <keep the existing
reason, and add that line 672's regex gate is what makes it safe>`). Do NOT restructure the helper,
do NOT hand-write an arithmetic parser, and do NOT weaken or remove the line-672 validation regex —
that regex is the entire safety property and it must survive this change byte-for-byte.

**Full verification, run in this order.**
  1. `pnpm lint` — must report 0 errors. Warnings will still be present (~3881); capture the warning
     count and compare it against the pre-change baseline. A warning count that MOVED means an
     autofix strayed outside scope in Task 1 — investigate before proceeding.
  2. `pnpm codecheck` — must exit 0.
  3. `pnpm test -- --runInBand` — full suite. Use a generous timeout. Compare pass/fail/skip counts
     against the pre-change baseline. Before calling any failure a regression, re-run that single
     suite on the unmodified baseline (`git stash` is FORBIDDEN — use a scratch copy of the file or
     `git show HEAD:<path>` into a temp file) and record which it is. The known-flaky sidecar
     frame-leak class and the known sandbox V8 OOM in one suite are pre-existing and are not
     regressions.

Write the SUMMARY with a per-category disposition table: rule, sites, how many CONVERTED / DELETED /
NARROWLY-SUPPRESSED, and for every retained suppression the one-line reason. Explicitly record the
`no-var-requires`-removed-in-v8 finding, since it is the root cause of two-thirds of these errors and
will recur the next time this repo's typescript-eslint major is bumped.
  </action>
  <verify>
    <automated>pnpm lint 2>&1 | tee /tmp/lint-final.txt; grep -E "^✖ [0-9]+ problems \([0-9]+ errors" /tmp/lint-final.txt || true; node -e "const t=require('fs').readFileSync('/tmp/lint-final.txt','utf8');const m=t.match(/(\d+) errors?, (\d+) warnings?/);if(!m){console.log('no summary line -> assume clean');process.exit(0)}console.log('errors',m[1],'warnings',m[2]);process.exit(m[1]==='0'?0:1)" && pnpm codecheck && pnpm test -- --runInBand</automated>
  </verify>
  <done>`pnpm lint` reports 0 errors with the warning count unchanged from baseline; `pnpm codecheck` exits 0; the full jest suite matches the baseline pass/skip counts; line 672's validation regex in `longRunningChannels.test.ts` is byte-identical to HEAD; the SUMMARY carries the per-category disposition table and the typescript-eslint-v8 root-cause finding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source text → `Function()` constructor | `parseMsConstantFromSource` evaluates a substring lifted out of real repository source; the only thing standing between that text and arbitrary code execution is the line-672 regex |
| lint suppression → CI gate | a widened suppression silently disables a rule for code far outside the line that needed it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-s5l-01 | Elevation of Privilege | `src/backend/__tests__/longRunningChannels.test.ts` `Function()` eval | mitigate | Task 3 forbids touching the line-672 `/^[\d\s*+\-\/().]+$/` guard and adds a `<done>` criterion asserting it is byte-identical to HEAD. The suppression is extended, the safety property is not |
| T-s5l-02 | Tampering | eslint suppression scope | mitigate | Task 2 forbids new block/file-level disable regions; Task 3's `<done>` requires the warning count to be unchanged from baseline, which detects an autofix or disable that reached beyond its listed line |
| T-s5l-03 | Tampering | production `require()` sites in `sidecarSeaFsShim.ts` / `humbleFlowRegistration.ts` / `redist.ts` | mitigate | Task 2 classifies all three as retarget-only; converting any of them to ESM would break the fs-singleton patch, the fail-closed SEA probe, or the sync circular-dependency break. `must_haves.truths` pins "still execute a real CJS require" |
| T-s5l-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages. No package-legitimacy gate required |
</threat_model>

<verification>
1. `pnpm lint` → 0 errors (baseline: 55).
2. `pnpm lint` warning count unchanged from baseline (baseline: ~3881) — proves no out-of-scope autofix.
3. `pnpm codecheck` → exit 0.
4. `pnpm test -- --runInBand` → baseline pass/skip counts.
5. `grep -rn "no-var-requires" --include='*.ts' meta src` → 0 hits.
6. `git diff --stat` → confined to the 26 files in `files_modified`.
</verification>

<success_criteria>
All 55 eslint errors cleared with real fixes: redundant assertions deleted, dead bindings removed
(not `_`-prefixed), redundant union members narrowed to their real types, the `this` alias
restructured away, mechanically-necessary dynamic requires kept but with their stale `no-var-requires`
suppressions retargeted to the live `no-require-imports` rule name plus their existing justification
prose preserved, and the single validated-input `Function()` call left intact with its suppression
extended to cover the typed rule. Warnings untouched. Runtime behaviour unchanged.
</success_criteria>

<output>
Create `.planning/quick/260818-s5l-fix-the-55-eslint-errors-not-warnings-su/260818-s5l-SUMMARY.md` when done.
</output>

