---
phase: quick-260925-r8j
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md
  - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md
  - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md
  - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md
  - .planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-VERIFICATION.md
  - .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md
  - .planning/todos/pending/2026-09-25-controller-focus-has-no-perceptible-affordance.md
  - .planning/todos/pending/2026-09-25-mouse-highlight-does-not-confer-dom-focus.md
  - src/frontend/helpers/gamepad.ts

must_haves:
  truths:
    - "`gsd-sdk query audit-uat` reports phase 38 with EXACTLY 16 open items (was 24) and a grand total of EXACTLY 41 (was 49)"
    - "All eight discharged ids live in `human_verification_discharged` and NONE remains in `human_verification`"
    - "`38-VERIFICATION.md` still parses and its `status:` still reads `human_needed`"
    - "Every origin phase that owns a discharged item records the outcome on its own receipt"
    - "No `GAMEPAD-ACT` token survives anywhere under `src/` or `meta/`"
    - "The three gamepad jest suites still report 35/35, 2/2 and 2/2"
  artifacts:
    - path: ".planning/todos/pending/2026-09-25-controller-focus-has-no-perceptible-affordance.md"
      provides: "Todo A, with severity/platform/ready in that order"
    - path: ".planning/todos/pending/2026-09-25-mouse-highlight-does-not-confer-dom-focus.md"
      provides: "Todo B, with severity/platform/ready in that order"
  key_links:
    - from: "38-VERIFICATION.md human_verification_discharged"
      to: "origin-phase receipts in 34.1 / 34.10 / 34.11 / 34.13"
      via: "relocation rule (3) walk-back"
      pattern: "outcome:"
---

<objective>
Close out the 2026-09-25 Phase 38 controller sitting. Eight controller items were RUN and
OBSERVED live on Windows 11 with a PowerA Advantage Wired Controller for Nintendo Switch 2, each
with a verbatim `[GAMEPAD-ACT]` log artifact. This plan writes that sitting into the ledger,
walks the receipts back to all four origin phases, files the two defects the sitting surfaced,
and removes the temporary probe that made the sitting machine-evidenced rather than
recollection-based.

Purpose: a discharged item is only worth what its evidence is worth. This sitting produced
genuinely strong evidence for most clauses and genuinely weak evidence for two of them, and the
ledger's value depends on that difference staying visible in the record rather than being
flattened into eight identical PASSes.

Output: eight items moved to `human_verification_discharged`; a dated session block in
`38-HUMAN-UAT.md`; four origin receipts updated; two pending todos; the `[GAMEPAD-ACT]` probe
gone from `src/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md
@.planning/quick/260925-op0-add-temporary-gamepad-act-dispatch-instr/260925-op0-SUMMARY.md
</context>

<measured_baseline>
Every number below was measured at plan time on 2026-09-25, on this tree, and is what the verify
blocks assert against. Re-measure nothing; assert these.

- `gsd-sdk query audit-uat`: phase 38 = **24** open items, phase 34.13 = **7**, grand total = **49**.
  After this plan: phase 38 = **16**, phase 34.13 = **7** (unchanged), grand total = **41**.
  Assert the exact integers, not a direction — a flat count after a move means the array failed to
  parse, and this ledger's own `audit_tool_note` says both failure modes are silent.
- `38-VERIFICATION.md` array sizes today: `human_verification` = 24, `human_verification_discharged`
  = 2, `human_verification_retired` = **10**. After this plan: 16 / 10 / **10** (retired is frozen).
- `pnpm planning-gates`: **13/13 passed**.
- `grep -rn "GAMEPAD-ACT" src/ meta/`: 5 hits, all in `src/frontend/helpers/gamepad.ts`
  (lines 287, 295, 296, 297 — the comment-block header plus the emission). After this plan: **0**.
- `grep -c requestedAction src/frontend/helpers/gamepad.ts`: 3 (lines 182, 296, 297).
  After this plan: **0**.
- The permanent connect line lives at `gamepad.ts:662` (`[GAMEPAD] id=...`). It stays.
- The pre-existing commented-out `console.log` at `gamepad.ts:155` predates the probe and is NOT
  part of it. Leave it byte-identical.
- 34.10 has **no** `human_verification_relocated` key. Verified by grep at plan time. See
  `the_34_10_decision` — that is decided in-plan, not left to the executor.

Every non-trivial verify command below was dry-run at plan time against this unmodified tree and
confirmed to FAIL informatively (naming what is missing) rather than crash. A checker that throws
is indistinguishable from a failed edit.

## TRAP: two of these files are NOT strict YAML. Do not reach for `js-yaml` on them.

Measured at plan time by loading each file's frontmatter through the repo's bundled `js-yaml`:

| File | `js-yaml` verdict |
|---|---|
| `38-VERIFICATION.md` | **REJECTS** — "bad indentation of a mapping entry" |
| `34.13-UAT.md` | **REJECTS** — same |
| `34.1-VERIFICATION.md` | parses |
| `34.10-VERIFICATION.md` | parses |
| `34.11-VERIFICATION.md` | parses |

The cause is unquoted plain scalars containing `: ` — `38-VERIFICATION.md`'s `score:` line opens
`score: N/A — collection phase, no must-haves. 24 relocated items OPEN, 2 discharged (sitting 1,
2026-09-23: ...` and the bare `2026-09-23:` inside it terminates the scalar for a strict parser.
`gsd-sdk query audit-uat` reads these files fine with a lenient front-matter parser, which is why
nothing has ever flagged it.

This matters because the obvious verify — load the YAML, count the arrays — CRASHES on exactly the
two files that most need checking, producing a red that looks like a failed edit but is a failed
tool choice. The verify blocks below therefore use a small line-scanning reader for those two and
`js-yaml` only for the three that parse. Do not "fix" that inconsistency by switching everything to
`js-yaml`, and do NOT reformat either file to make it strict — reformatting `34.13-UAT.md` in
particular would touch the block scalars CLAUDE.md prohibits flattening.

## The prettier check over `.planning/**` is VACUOUS. Run it anyway; do not trust it.

`.prettierignore` lists `.planning` outright (under "Tooling/agent state and planning artifacts,
not shipped source"). Measured at plan time: a deliberately-malformed probe file written to
`.planning/todos/pending/` passed `npx prettier --check` with exit 0 and
"All matched files use Prettier code style!". So for every `.planning/**` path in this plan the
mandated check proves NOTHING. It stays in each verify block because CLAUDE.md requires it and a
green is the correct result for an ignored path — but do not read it as evidence.

**Do NOT try to make it non-vacuous with `--ignore-path`.** That was measured too: six of the
seven existing planning files this plan touches are already prettier-dirty, so forcing a real
check would demand reformatting frozen historical records — including sitting 1's, which this
plan is explicitly forbidden to rewrite.

The ONE load-bearing prettier check in this plan is Task 5's, over
`src/frontend/helpers/gamepad.ts`, which is real source and not ignored.

## Expected-red suites — do not chase

`src/frontend/screens/Game/GamePage/components/__tests__/labelSuiteI18nCensus.test.ts` and
`src/frontend/screens/WebView/__tests__/storeEmbedSingleOpener.test.ts` fail PRE-EXISTING and
unrelated on this tree (the latter is a Windows path-separator assertion). Quick `260925-op0`
recorded both as expected-red the same day. If a broad jest invocation sweeps them in, name them
and move on.
</measured_baseline>

<sitting_record>
The verbatim artifacts. Quote these into the ledger; do not paraphrase them into prose.

## Conditions

Windows 11, `pnpm tauri:dev`, PowerA Advantage Wired Controller for Nintendo Switch 2. Connect
line reproduced byte-identically with prior sittings:

    [GAMEPAD] id="PowerA Advantage Wired Controller for Nintendo Switch 2 (Vendor: 20d6 Product: a720)" mapping="" buttons=17 axes=10

So the raw-HID branch of `checkNintendo` is the code under test.

## COMMIT BOUNDARY — must be recorded

This is NOT a clean single-variable sitting. `38-C01a`'s main clause was observed at `cdf07ee95`.
Every other item was observed at `959e5c01b`, after quick `260925-pga` (accent focus ring
replacing the hairline; ring extended to hover; stale-ring suppression while mousing) landed
mid-sitting. `260925-pga` touches NON-console game cards; `38-C01a`'s main clause was scored in
Console Mode, which that change does not touch.

## Instrument and positive control

The temporary `[GAMEPAD-ACT]` probe from quick `260925-op0`, logging the RESOLVED action
post-switch with `from=` when the switch rewrote it, plus the focused element. Positive control
taken BEFORE any scoring:

    (17:59:20) [GAMEPAD-ACT] action=mainAction ctrl=0 tag=none

Instrument alive; `action=mainAction` with no `from=` proves physical A binds by printed label on
the raw-HID arm.

## 38-C01a — PASS (main clause). Console Mode.

    (18:01:19) [GAMEPAD-ACT] action=padUp ctrl=0 tag=BUTTON cls=consoleCard focused
    (18:01:20) [GAMEPAD-ACT] action=padUp ctrl=0 tag=BUTTON cls=consoleChip
    (18:01:23) [GAMEPAD-ACT] action=padLeft ctrl=0 tag=BUTTON cls=consoleChip
    (18:01:24) [GAMEPAD-ACT] action=padDown ctrl=0 tag=BUTTON cls=consoleQuitButton danger
    (18:01:25) [GAMEPAD-ACT] action=padRight ctrl=0 tag=BUTTON cls=consoleCard focused

All four cardinals dispatched, no `from=` rewrites, focus moved card -> chip -> quit button ->
card, operator confirmed movement and NO WRAP. OVERTURNS sitting 1's FAIL, which observed the
d-pad producing no response at all because the then-current `checkNintendo` read `buttons[12-15]`
while this pad reports the d-pad as a hat axis.

## 38-C01a — cold-start clause: NOT INDEPENDENTLY OBSERVED

Record this honestly; do not claim it. The item's `expected` names "Up/Left from a COLD START with
nothing focused" (WR-02/WR-03). Three attempts failed to arm it as a d-pad-specific observation:

(a) Reloading inside Console Mode auto-focuses a `consoleCard`, so focus was never null — one such
attempt landed on a focused card and pressing A launched Steam appId 251570.
(b) On the library route two padLeft presses two seconds apart both logged `tag=none` while a card
appeared highlighted, but the OPERATOR STATES library-route highlights "do not stick", so that
state is transient and proves nothing.
(c) Home-out/Home-in does NOT clear focus — `(19:04:28) action=padUp ... cls=consoleCard focused`
shows focus already present.

Disposition to encode: the `if (!el) document.querySelector('body')?.focus()` recovery at
`gamepad.ts:248` is SHARED code across `padUp`/`padDown`/`padLeft`/`padRight` and all four
`leftStick*` cases in ONE combined switch case — there is no per-input branch — and `38-C01b` DID
arm it with machine evidence. So C01a's cold-start clause is discharged as COVERED BY THE SHARED
BRANCH VIA 38-C01b, explicitly NOT as a d-pad-specific live observation.

## 38-C01b — PASS, both clauses. Strongest cold-start evidence of the sitting. Console Mode.

    (19:01:57) [GAMEPAD-ACT] action=leftStickUp ctrl=0 tag=none
    (19:01:59) [GAMEPAD-ACT] action=leftStickDown ctrl=0 tag=BUTTON cls=consoleCard focused
    (19:02:01) [GAMEPAD-ACT] action=leftStickLeft ctrl=0 tag=BUTTON cls=consoleCard focused
    (19:02:02) [GAMEPAD-ACT] action=leftStickRight ctrl=0 tag=BUTTON cls=consoleCard focused

Four distinct `leftStick*` actions, 1-2s apart so these are clean presses not a repeat burst, no
`from=` rewrites. The FIRST line reads `tag=none` — `currentElement()` was null, so the `!el`
recovery branch ARMED — and from line two onward the focused element is a real DOM-focused
`consoleCard`, which is the recovery actually happening, recorded by the instrument rather than by
recollection. Operator: "the first up highlighted a game that was near bottom of visible page, from
then on each push moved the highlighted game one position as expected." First live observation of
WR-02/WR-03's fix, which the item records as "fixed unit-only during code review, never observed
live."

## 38-C02 — PASS

Ledger calls this "the single case most likely to be inverted". Dispatch census across the
session: 177 `rightStickDown`, 57 `rightStickUp` (high counts because scroll uses
`SCROLL_REPEAT_DELAY`). Sign confirmed unambiguously — the operator was asked which games became
VISIBLE, not which way things moved, precisely to avoid the ambiguity: pushing DOWN showed "games
later in the list". NOT inverted. Also confirms the `260925-9de` stick-axis fix reading `axes[5]`
for rightY on the raw-HID arm.

## 38-C03 — PASS, with the sitting's best armed-branch proof. Settings, focus on an MUI select.

    (19:15:36) [GAMEPAD-ACT] action=tab from=padDown ctrl=0 tag=DIV cls=MuiSelect-select MuiSelect-outlined MuiInputBase-input MuiOu...
    (19:15:36) [GAMEPAD-ACT] action=shiftTab from=padUp ctrl=0 tag=DIV cls=MuiSelect-select MuiSelect-outlined MuiInputBase-input MuiOu...
    (19:16:10) [GAMEPAD-ACT] action=tab from=padDown ctrl=0 tag=DIV cls=MuiSelect-select MuiSelect-outlined MuiInputBase-input MuiOu...
    (19:16:10) [GAMEPAD-ACT] action=shiftTab from=padUp ctrl=0 tag=DIV cls=MuiSelect-select MuiSelect-outlined MuiInputBase-input MuiOu...

Both rewrites fired, reproduced twice, focused element proven to be `MuiSelect-select`, and the
dropdown did NOT open (which is the whole purpose of the rewrite). The `from=` field IS the
armed-branch proof procedure step 2 requires. Two additional rewrite paths confirmed incidentally,
worth recording as corroboration but NOT as separate items:

    (19:15:40) [GAMEPAD-ACT] action=leftClick from=mainAction ctrl=0 tag=DIV cls=MuiSelect-select ...
    (19:15:57) [GAMEPAD-ACT] action=tab from=back ctrl=0 tag=DIV cls=MuiPaper-root ...

## 38-C04a — PASS. Game detail page, normal library.

    (19:13:22) [GAMEPAD-ACT] action=back ctrl=0 tag=BUTTON cls=button is-success mainBtn

`action=back` with NO `from=` — dispatched unrewritten — and the operator confirms "navigates
back". Closes the defect that made sitting 1 unable to score the item at all: the pad's face
indices arrived as raw HID `[Y, B, A, X]`, so physical B fired `mainAction`. Physical B now binds
to `back` by PRINTED LABEL.

## 38-C05 — PASS. Main library.

    (19:19:55) [GAMEPAD-ACT] action=padUp ctrl=0 tag=A
    (19:19:56) [GAMEPAD-ACT] action=padDown ctrl=0 tag=A     <- x13, held, walking down the grid

`tag=A` confirms focus on library card anchors. Operator: "if I hold down arrow off end of page the
page scrolls." `scrollCardIntoView` works against the post-34.10 scroll container. Record that this
item had been NOT ATTEMPTED across four consecutive live gate runs and was invisible to `audit-uat`
entirely before relocation.

## 38-C06 — PASS, with one honest caveat that MUST be recorded. Tier-2 filter panel.

    (19:25:21) [GAMEPAD-ACT] action=leftStickDown ctrl=0 tag=BUTTON cls=alphabet-filter-button
    (19:25:23) [GAMEPAD-ACT] action=leftStickDown ctrl=0 tag=BUTTON cls=dropdownButton
    (19:26:44) [GAMEPAD-ACT] action=leftStickDown ctrl=0 tag=BUTTON cls=FilterFacetRow FilterFacetRow--zero
    (19:26:45) [GAMEPAD-ACT] action=leftStickDown ctrl=0 tag=BUTTON cls=FilterFacetRow
    (19:26:45) [GAMEPAD-ACT] action=leftStickDown ctrl=0 tag=BUTTON cls=NavItem FilterCollectionList__row

Operator answers: (1) focus reaches INTO the panel — yes; (2) moves WITHIN it — yes; (4) a row below
the panel's visible area IS scrolled into view — yes. CAVEAT for (3), the expanded-group/checkbox
clause: the operator could NOT verify it visually — their words: "very hard as you cant really see
what is highlighted beyond collapsible areas expanding." That clause is discharged on the LOG
EVIDENCE (`FilterFacetRow` entries prove focus landed on facet rows) and NOT on operator
observation. The `result:` field must say so plainly.

## 38-C08 — PASS, stability clause machine-proven. Steam install dialog, uninstalled game.

    (19:32:51) [GAMEPAD-ACT] action=padDown ctrl=0 tag=BUTTON cls=button is-secondary mainBtn
    (19:32:53) [GAMEPAD-ACT] action=padUp ctrl=0 tag=BUTTON cls=dropdownButton button outline
    (19:32:55) [GAMEPAD-ACT] action=padDown ctrl=0 tag=BUTTON cls=button is-secondary mainBtn
    (19:32:57) [GAMEPAD-ACT] action=padUp ctrl=0 tag=BUTTON cls=dropdownButton button outline
    (19:33:02) [GAMEPAD-ACT] action=padDown ctrl=0 tag=BUTTON cls=button is-secondary mainBtn
    (19:33:03) [GAMEPAD-ACT] action=padUp ctrl=0 tag=BUTTON cls=dropdownButton button outline

THREE complete alternations between the primary Install half (`mainBtn`) and the caret
(`dropdownButton button outline`), identical order every pass. Operator confirms all four
sub-questions pass. This is exactly the failure `hasZeroArea` in `tauriGamepadInput.ts:57` could
have caused and did not. Record that this item now stands ALONE — its Electron twin `38-C07` was
retired when Phase 35 removed that build, so this is the only observation of the caret's controller
reachability that will ever exist.
</sitting_record>

<the_c03_specification_correction>
`38-C03`'s current `test:` reads "Gamepad — Tab / Shift+Tab traversal, driven FROM THE
CONTROLLER", which reads as though some button BINDS those actions. A source census confirms NO
layout binds `tab` or `shiftTab` on any pad. They exist ONLY as rewrites inside `checkAction()`'s
switch in `src/frontend/helpers/gamepad.ts`, verified at plan time:

- `mainAction` -> `tab` when `isMuiSlider()` (~:196)
- `back` -> `tab` when `isInMuiPopover()`; `back` -> `shiftTab` when `insideMuiSlider()` (~:227, :231)
- `padDown`/`leftStickDown` -> `tab` when `isMuiSelect()` OR `isMuiDialogCloseButton()` OR
  `insideMuiSlider()` (:257-269)
- `padUp`/`leftStickUp` -> `shiftTab` when `isMuiSelect()` OR `insideMuiSlider()` (:271-280) —
  note the dialog-close-button condition exists ONLY on the down path, not the up path

Sitting 1's note that this item "depends on `buttons[4]/[5]` shoulders" was WRONG — the shoulders
are unbound on every layout. That wrong note lives in the item's own `sitting_1_2026_09_23` field
and must be corrected rather than silently carried forward.

The item still PASSES. This is a SPECIFICATION CORRECTION, not a re-score, and the entry must say
so at discharge time and say why.
</the_c03_specification_correction>

<the_34_10_decision>
DECIDED HERE, not delegated. Do not silently skip 34.10.

**Finding:** `34.10-VERIFICATION.md` has NO `human_verification_relocated` key. It does have
`human_verification: []` and `status: passed`. But it DOES already carry a functioning receipt for
38-C05, under a different key: `deferred[0]`, which records `relocated_to: "Phase 38"`,
`relocated_as: "38-C05"`, `relocated_on: 2026-08-22`, a `relocation_note` and a `platform_gate`.
This is consistent with 38-C05's own `prior_state`, which records that the item was never in
34.10's `human_verification` array and sat in `deferred:` instead.

**Decision: ADD an `outcome:` field to the existing `deferred[0]` entry. Do NOT create a new
`human_verification_relocated` key in 34.10.**

Three reasons, all checkable:

1. Relocation rule (3)'s substance — "the origin phase keeps a receipt naming this phase and the
   item ID" — is ALREADY SATISFIED by `deferred[0]`. The rule names a receipt, not a key name.
   What is actually missing is the OUTCOME, which is what closing-procedure step 4 asks for.
   Adding the field closes the real gap.
2. Creating a second, parallel receipt for one item gives that item TWO records that can drift
   against each other — a worse failure than the one being fixed, and the opposite of what the
   two-way rule exists to buy.
3. 34.11's own receipt carries an explicit warning about adding frontmatter keys to a
   `status: passed` phase's VERIFICATION without understanding how `audit-uat` falls back to
   scraping the document body. 34.10 is `status: passed`, so `audit-uat` never admits it at all;
   there is no upside to touching its key structure and a non-zero downside.

**Record this decision IN 34.10's `deferred[0]` entry itself**, in a short field (suggest
`receipt_key_note:`), so a future reader following relocation rule (3) and grepping for
`human_verification_relocated` in 34.10 finds nothing, comes here, and learns immediately that the
absence is deliberate and where the receipt actually lives. An unexplained absence is exactly the
orphaning shape rule (3) exists to prevent.
</the_34_10_decision>

<tasks>

<task type="auto">
  <name>Task 1: Discharge the eight items in 38-VERIFICATION.md</name>
  <files>.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md</files>
  <action>
MOVE the eight entries `38-C01a`, `38-C01b`, `38-C02`, `38-C03`, `38-C04a`, `38-C05`, `38-C06`,
`38-C08` out of the `human_verification` array and into `human_verification_discharged`. Move the
WHOLE entry — every existing field travels with it unchanged (`test`, `expected`, `why_human`,
`blocked_by`, `platform_gate`, `origin_phase`, `origin_item`, `prior_state`, `scope_note`,
`not_covered_by_*`, `cannot_be_discharged_at_a_keyboard`, `sitting_1_2026_09_23`). Do NOT annotate
in place: the audit counts array membership and ignores any `result:` field, which this file's own
`audit_tool_note` states outright.

Follow the house style already set by `38-S06` and `38-S08` in that array: `id:` first, then
`result:` as a single dense field, then the original fields. Append the eight in the id order
listed above, after `38-S08`.

Each `result:` must open with a PASS verdict naming sitting 3, 2026-09-25, Windows 11, tauri dev
build, and the commit it was observed at, then quote that item's verbatim `[GAMEPAD-ACT]` artifact
from this plan's `sitting_record` section. Quote the log lines; do not paraphrase them into prose.
Every `result:` states the armed-branch proof (procedure step 2) explicitly — for most of these the
proof IS the presence or absence of the `from=` field.

Honesty requirements, each non-negotiable and each checked by the verify block below:

- `38-C01a` at `cdf07ee95`; all others at `959e5c01b`. The `38-C01a` entry must state the COMMIT
  BOUNDARY: quick `260925-pga` landed mid-sitting, it touches NON-console game cards, and C01a's
  main clause was scored in Console Mode which that change does not touch. This is not a clean
  single-variable sitting and the record says so, exactly as `38-S08`'s own `rescore_receipt` did
  for its re-score.
- `38-C01a` must carry a dedicated field (suggest `cold_start_clause:`) recording that the
  cold-start half was NOT independently observed as a d-pad-specific event, listing the three
  failed attempts (a)(b)(c) from the sitting record, and dispositioning it as COVERED BY THE SHARED
  `!el` RECOVERY BRANCH VIA 38-C01b — because `gamepad.ts:248`'s recovery is one combined switch
  case across all four pad directions and all four `leftStick*` cases, with no per-input branch.
  The exact phrase "not a d-pad-specific live observation" must appear.
- `38-C06` must carry the clause-(3) caveat in its `result:`: the expanded-group/checkbox clause
  rests on LOG EVIDENCE (`FilterFacetRow` entries), NOT operator observation, with the operator's
  own words quoted, including the fragment "collapsible areas expanding".
- `38-C08` must record that it now stands ALONE — `38-C07` was retired when Phase 35 removed the
  Electron build, so this is the only observation of the caret's controller reachability that will
  ever exist.
- `38-C05` must record that it had been NOT ATTEMPTED across four consecutive live gate runs and
  was invisible to `audit-uat` entirely before relocation.

`38-C03` additionally gets the SPECIFICATION CORRECTION described in the
`the_c03_specification_correction` section. Rewrite its `test:` and `expected:` to describe the
real mechanism — that `tab`/`shiftTab` are never BOUND on any layout and exist only as
context-sensitive rewrites inside `checkAction()`'s switch — enumerating the four rewrite paths
with their source conditions and line numbers. Add a field (suggest
`text_corrected_at_discharge:`) stating that the text was corrected at discharge time, that the
original wording implied a binding that does not exist, that sitting 1's "depends on
`buttons[4]/[5]` shoulders" note was wrong because the shoulders are unbound on every layout, and
that this is a specification correction and NOT a re-score — the item passes either way. The exact
phrase "specification correction" must appear. Correct the stale claim inside the existing
`sitting_1_2026_09_23` field rather than deleting that field.

Then update the `score:` narrative in the frontmatter, following the running-tally style every
prior change used: 24 -> 16 open, 2 -> 10 discharged, naming this quick id, the date, the eight
ids, and the confirmed-at-the-tool numbers (`audit-uat` moved 24 -> 16 and 49 -> 41, which is the
check that the array still parses — a FLAT count after a removal would mean the edit did not
register). Also amend `sweep_notes.windows_linux_dependency`, whose text still frames the eight
controller items as "all EIGHT dischargeable in a single controller sitting" — a forward-looking
claim that has now happened.

WRITING CONSTRAINT: keep every new value a SINGLE-LINE scalar in the same style as the existing
entries, single-quoted with doubled inner quotes where needed. Do not introduce a block scalar
(`|` or `>`) anywhere in this frontmatter. The file already fails strict YAML parsing (see the
trap note in `measured_baseline`), and the lenient parser `audit-uat` relies on is the only reader
ever exercised against it — a new construct here is an untested construct.

DO NOT touch: `status:` (stays `human_needed` — `gaps_found` is admitted by the audit and then
yields ZERO items, vanishing the whole backlog with nothing turning red). The ORDER of the
surviving `human_verification` entries (it is ARRIVAL order and the audit emits positional integers
with `id:` dropped). `human_verification_retired`, which holds 10 frozen entries including `38-C07`
and `38-C04b`; `38-C04b` was retired by quick `260925-oyr` and is NOT part of this sitting.
  </action>
  <verify>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx gsd-sdk query audit-uat 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const p=j.results.find(r=>r.phase==='38');const n=p?p.items.length:-1;const t=j.summary.total_items;console.log('phase38='+n+' (expect 16, was 24) total='+t+' (expect 41, was 49)');process.exit(n===16&&t===41?0:1)})"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && node -e "const fs=require('fs');const P='.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md';const raw=fs.readFileSync(P,'utf8');const lines=raw.slice(4,raw.indexOf('\n---',4)).split(/\r?\n/);let key=null,cur=null;const g={},blk={};for(const l of lines){if(/^[a-z_]+:/.test(l)){key=l.trim().slice(-1)===':'?l.trim().slice(0,-1):null;if(key)g[key]=g[key]||[];cur=null;continue}const m=l.match(/^  - id:\s*\x22([^\x22]+)\x22/);if(m&&key){g[key].push(m[1]);cur=key+'/'+m[1];blk[cur]='';continue}if(cur)blk[cur]+=l+'\n'}const open=g.human_verification||[],dis=g.human_verification_discharged||[],ret=g.human_verification_retired||[];const eight=['38-C01a','38-C01b','38-C02','38-C03','38-C04a','38-C05','38-C06','38-C08'];let ok=true;for(const i of eight){if(open.includes(i)){console.error('FAIL still in human_verification: '+i);ok=false}if(!dis.includes(i)){console.error('FAIL not in human_verification_discharged: '+i);ok=false}else if(!/result:/.test(blk['human_verification_discharged/'+i]||'')){console.error('FAIL no result field: '+i);ok=false}}if(open.length!==16){console.error('FAIL open='+open.length+' expect 16');ok=false}if(dis.length!==10){console.error('FAIL discharged='+dis.length+' expect 10');ok=false}if(ret.length!==10){console.error('FAIL retired='+ret.length+' expect 10 frozen');ok=false}if(!raw.includes('\nstatus: human_needed\n')){console.error('FAIL status is not human_needed');ok=false}console.log(ok?'OK open='+open.length+' discharged='+dis.length+' retired='+ret.length:'FAILED');process.exit(ok?0:1)"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && node -e "const fs=require('fs');const P='.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md';const raw=fs.readFileSync(P,'utf8');const lines=raw.slice(4,raw.indexOf('\n---',4)).split(/\r?\n/);let key=null,cur=null;const blk={};for(const l of lines){if(/^[a-z_]+:/.test(l)){key=l.trim().slice(-1)===':'?l.trim().slice(0,-1):null;cur=null;continue}const m=l.match(/^  - id:\s*\x22([^\x22]+)\x22/);if(m&&key){cur=key+'/'+m[1];blk[cur]='';continue}if(cur)blk[cur]+=l+'\n'}const G=i=>(blk['human_verification_discharged/'+i]||'').toLowerCase();let ok=true;const need=[['38-C01a','cdf07ee95'],['38-C01a','260925-pga'],['38-C01a','not a d-pad-specific live observation'],['38-C01b','959e5c01b'],['38-C02','959e5c01b'],['38-C03','959e5c01b'],['38-C03','specification correction'],['38-C04a','959e5c01b'],['38-C05','959e5c01b'],['38-C05','not attempted'],['38-C06','959e5c01b'],['38-C06','collapsible areas expanding'],['38-C08','959e5c01b'],['38-C08','38-c07']];for(const pair of need){if(!G(pair[0]).includes(pair[1].toLowerCase())){console.error('FAIL '+pair[0]+' missing required disclosure token: '+pair[1]);ok=false}}console.log(ok?'OK all 14 disclosure tokens present':'FAILED');process.exit(ok?0:1)"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && pnpm planning-gates 2>&1 | tail -3</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx prettier --check ".planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md"</automated>
  </verify>
  <done>
`audit-uat` reports phase 38 at exactly 16 and grand total at exactly 41. All eight ids are in
`human_verification_discharged` with a `result:` field and none is in `human_verification`.
`status:` is still `human_needed`. `human_verification_retired` still holds 10 entries. All
fourteen disclosure tokens are present: both observation commits, the `260925-pga` boundary, the
C01a cold-start disclaimer, the C06 operator quote, C05's never-attempted history, C08's
stands-alone note and C03's correction. 13/13 planning gates pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the sitting-3 session block to 38-HUMAN-UAT.md and reconcile the stale narrative</name>
  <files>.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md</files>
  <action>
Append a dated session block matching the shape of the existing "### Session 1 — 2026-09-23..." and
"## Sitting 2 — 2026-09-23..." blocks: a heading naming date, host, runtime and commit; bold
per-item verdicts; verbatim artifacts; an explicit honest-limits paragraph. Head it "Sitting 3 —
2026-09-25, Windows 11, pnpm tauri:dev" and state the commit boundary in the heading paragraph
rather than burying it.

Content it must carry, all from this plan's `sitting_record` section:

1. Conditions, including the byte-identical connect line, and the note that the raw-HID branch of
   `checkNintendo` is therefore the code under test.
2. The COMMIT BOUNDARY: 38-C01a's main clause at `cdf07ee95`, everything else at `959e5c01b` after
   quick `260925-pga` landed mid-sitting; why that does not contaminate C01a (pga touches
   non-console game cards, C01a's main clause was Console Mode).
3. The instrument and the POSITIVE CONTROL taken before any scoring (the 17:59:20 mainAction line),
   and what it proves.
4. Per-item artifacts and verdicts for all eight, log lines quoted.
5. The C01a cold-start disposition, with all three failed attempts (a)(b)(c) named, and the
   shared-`!el`-branch reasoning that discharges it via C01b rather than as a d-pad observation.
6. The C06 clause-(3) caveat with the operator's own words.
7. An honest-limits paragraph in the style sitting 2 already uses.

Then RECONCILE the stale narrative WITHOUT rewriting history. Sitting 1's record is frozen — do not
delete it, do not edit its measurements, do not restate its FAILs as passes.

- Update the frontmatter: `status:` (currently `not_started`, now false), `updated:`, and
  `sessions:` (currently `[]` despite three sittings; entries added should describe sittings 1, 2
  and 3, not just this one).
- `## Current Test` still reads "[not started — no hardware sitting yet. 6 items seeded, 0
  discharged.]" — both halves now false. The `## Retired` section already flags the "6 items
  seeded" line as stale for an unrelated reason; reconcile it with the real count and point at
  `38-VERIFICATION.md` as authoritative, as the existing blockquote already does.
- `## Scope` still says "38-C01a … 38-C05 need only a game controller ... should be run in one
  sitting". That prediction was borne out; say so and say which sitting did it.
- The Session 1 heading "CONTROLLER LEG NOT RUN AT SITTING 1 — the blocking defect is now FIXED,
  and the leg is RUNNABLE, not yet a result" is now historical. Do not rewrite that paragraph; add
  a dated, clearly-marked forward pointer directly beneath it saying the leg RAN at sitting 3 on
  2026-09-25 and where the result lives.
- The "Disposition of the eight surviving controller items, to be applied at the ledger" list is
  now spent — every bullet's prediction has been tested. Do not delete it; add a resolved-outcome
  line under each bullet with a dated marker. The `38-C04b` bullet is UNCHANGED — it was retired,
  not run, and is not part of this sitting.

Do NOT introduce any numbered item heading with an `expected:` key. This file is swept by
`.planning/uat-visibility-gate.py`; a numbered item carrying a block-scalar `expected:` silently
deletes EVERY item in the file from `audit-uat`, not just its own. The session blocks in this file
are prose and must stay prose.
  </action>
  <verify>
    <automated>cd "C:/Users/grays/Projects/GameLib" && node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md','utf8');let ok=true;for(const n of ['Sitting 3','2026-09-25','cdf07ee95','959e5c01b','260925-pga','17:59:20','19:01:57','19:32:51','collapsible areas expanding']){if(!t.includes(n)){console.error('FAIL missing: '+n);ok=false}}if(t.includes('[not started')){console.error('FAIL Current Test still says not started');ok=false}if(!t.includes('Session 1 — 2026-09-23')){console.error('FAIL sitting 1 record was removed');ok=false}if(!t.includes('CONTROLLER LEG NOT RUN AT SITTING 1')){console.error('FAIL sitting-1 framing paragraph was rewritten away instead of annotated');ok=false}if(/^expected:\s*\|/m.test(t)){console.error('FAIL a block-scalar expected: was introduced');ok=false}console.log(ok?'OK':'FAILED');process.exit(ok?0:1)"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx gsd-sdk query audit-uat 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const p=j.results.find(r=>r.phase==='38');const n=p?p.items.length:-1;console.log('phase38='+n+' total='+j.summary.total_items);process.exit(n===16&&j.summary.total_items===41?0:1)})"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && python3 .planning/uat-visibility-gate.py</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && pnpm planning-gates 2>&1 | tail -3</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx prettier --check ".planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md"</automated>
  </verify>
  <done>
A "Sitting 3 — 2026-09-25" block exists carrying the commit boundary, the positive control, all
eight per-item artifacts, the C01a cold-start disposition and the C06 caveat. The sitting-1 record
and its "CONTROLLER LEG NOT RUN" paragraph both still exist and are annotated rather than
rewritten. `## Current Test` no longer claims "not started". The uat-visibility gate is green and
phase 38 still audits at 16.
  </done>
</task>

<task type="auto">
  <name>Task 3: Walk the receipts back to all four origin phases</name>
  <files>.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md, .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md, .planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-VERIFICATION.md, .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md</files>
  <action>
Relocation rule (3) and Phase 38's closing-procedure step 4 both require the origin to learn the
outcome. Four origins, three shapes.

**34.1** (`34.1-VERIFICATION.md`, `human_verification_relocated`). ONE entry covers all five of
this sitting's 34.1 items: the entry whose `moved_as` reads "38-C01a, 38-C01b, 38-C02, 38-C03,
38-C04a, 38-C04b (SPLIT into six; 38-C04b RETIRED ...)". Add an `outcome:` field to THAT entry
recording: all five survivors (`38-C01a`, `38-C01b`, `38-C02`, `38-C03`, `38-C04a`) DISCHARGED PASS
at sitting 3 on 2026-09-25; `38-C04b` unchanged (retired, not run, nothing observed); the commit
boundary (`cdf07ee95` for C01a's main clause, `959e5c01b` for the rest); that C01a's cold-start
clause is covered via C01b's shared `!el` branch and NOT as a d-pad observation; and that
`38-C03`'s specification text was CORRECTED at discharge. That last point matters here more than
anywhere: this entry's own `cannot_be_discharged_at_a_keyboard` field and its `split_reason` both
discuss C03, and `split_reason` inherits sitting 1's wrong shoulder-button premise. Correct that
premise where it appears in this entry, dated and marked as a correction, without deleting the
historical text.

**34.11** (`34.11-VERIFICATION.md`, `human_verification_relocated`). Single entry,
`moved_as: "38-C06"`. Add an `outcome:` field: DISCHARGED PASS at sitting 3, 2026-09-25, at
`959e5c01b`, with the clause-(3) caveat carried across — focus into and within the panel and the
below-the-fold scroll are operator-confirmed; the expanded-group/checkbox clause rests on log
evidence only. The word "log" must appear in that outcome so the weaker evidence is not invisible
from the origin side. This entry's prose says "the measurement has still never been performed" —
now false; correct it with a dated marker rather than deleting it.

**34.13** (`34.13-UAT.md` FRONTMATTER, `human_verification_relocated`). Find the entry with
`to_item: "38-C08"`, whose `outcome:` currently reads "open — not yet run in phase 38". Replace
that string with the discharge outcome: PASS at sitting 3, 2026-09-25, at `959e5c01b`; three
complete alternations between `mainBtn` and `dropdownButton button outline`; the `hasZeroArea`
filter at `tauriGamepadInput.ts:57` did NOT drop the caret; and that with `38-C07` retired this is
the only observation of the caret's controller reachability that will ever exist. Leave the
`38-C07` entry's retirement outcome byte-identical. The matching body table row reading
"| G-GAMEPAD-CARET | tauri | RELOCATED |" should become "RELOCATED → DISCHARGED PASS 2026-09-25" in
the same style the electron row already uses for its retirement.

BE CAREFUL IN `34.13-UAT.md`. It is a `*-UAT.md` file carrying block-scalar `expected:` values
CLAUDE.md explicitly prohibits flattening, AND it is one of the two files that do NOT parse as
strict YAML (see the trap note in `measured_baseline`). Edit it as text: frontmatter receipt fields
and one body table row only. Do not touch any `expected:` key, do not reflow any block scalar, do
not renumber any numbered item, and do not reach for a YAML round-trip writer on this file — a
round-trip would rewrite every quoted scalar in it.

**34.10** — apply the `the_34_10_decision` section above exactly as written. Add an `outcome:`
field to the EXISTING `deferred[0]` entry (the one with `relocated_as: "38-C05"`), recording
DISCHARGED PASS at sitting 3, 2026-09-25, at `959e5c01b`, with the `tag=A` artifact, the
held-padDown scroll confirmation, and the fact that this item had been NOT ATTEMPTED across four
consecutive live gate runs and was invisible to `audit-uat` before relocation. Also add the
`receipt_key_note:` field explaining WHY no `human_verification_relocated` key is created here and
where the receipt actually lives, so the deliberate absence is discoverable. Do NOT add a
`human_verification_relocated` key and do NOT change 34.10's `status: passed`.
  </action>
  <verify>
    <automated>cd "C:/Users/grays/Projects/GameLib" && node -e "const y=require('js-yaml'),fs=require('fs');const fm=p=>y.load(fs.readFileSync(p,'utf8').match(/^---\n([\s\S]*?)\n---/)[1]);let ok=true;const chk=(lbl,b,ts)=>{for(const t of ts){if(!b.toLowerCase().includes(t.toLowerCase())){console.error('FAIL '+lbl+' missing: '+t);ok=false}}};const a=fm('.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md');const e1=(a.human_verification_relocated||[]).find(x=>/38-C01a/.test(x.moved_as||''));if(!e1||!e1.outcome){console.error('FAIL 34.1 gamepad receipt has no outcome field');ok=false}else chk('34.1',JSON.stringify(e1),['38-C01a','38-C01b','38-C02','38-C03','38-C04a','2026-09-25','959e5c01b','cdf07ee95']);const b=fm('.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-VERIFICATION.md');const e2=(b.human_verification_relocated||[]).find(x=>x.moved_as==='38-C06');if(!e2||!e2.outcome){console.error('FAIL 34.11 receipt has no outcome field');ok=false}else chk('34.11',JSON.stringify(e2),['2026-09-25','959e5c01b','log']);const d=fm('.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md');if(d.human_verification_relocated){console.error('FAIL 34.10 gained a human_verification_relocated key, contrary to the plan decision');ok=false}if(d.status!=='passed'){console.error('FAIL 34.10 status changed');ok=false}const e4=(d.deferred||[]).find(x=>x.relocated_as==='38-C05');if(!e4||!e4.outcome){console.error('FAIL 34.10 deferred[0] has no outcome field');ok=false}else{chk('34.10',JSON.stringify(e4),['2026-09-25','tag=A']);if(!e4.receipt_key_note){console.error('FAIL 34.10 deferred[0] has no receipt_key_note explaining the absent key');ok=false}}console.log(ok?'OK 34.1 / 34.11 / 34.10 receipts updated':'FAILED');process.exit(ok?0:1)"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && node -e "const fs=require('fs');const P='.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md';const t=fs.readFileSync(P,'utf8');const s=t.indexOf('\nhuman_verification_relocated:');const region=t.slice(s,t.indexOf('\n---',s));const chunks=region.split(/\n  - /).slice(1);const c8=chunks.find(c=>c.includes('to_item: \x2238-C08\x22'))||'';const c7=chunks.find(c=>c.includes('to_item: \x2238-C07\x22'))||'';let ok=true;if(!c8){console.error('FAIL 38-C08 receipt chunk not found');ok=false}if(/open — not yet run/.test(c8)){console.error('FAIL 38-C08 outcome still says open');ok=false}for(const tok of ['2026-09-25','959e5c01b','38-C07']){if(!c8.includes(tok)){console.error('FAIL 38-C08 outcome missing: '+tok);ok=false}}if(!/RETIRED 2026-09-23/.test(c7)){console.error('FAIL 38-C07 retirement outcome was altered');ok=false}if(/\| G-GAMEPAD-CARET \| tauri \| RELOCATED \|/.test(t)){console.error('FAIL body table row still reads plain RELOCATED');ok=false}console.log(ok?'OK 34.13 receipt and body row updated':'FAILED');process.exit(ok?0:1)"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && git diff -- .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md | grep -E '^[-+]' | grep -v '^[-+][-+]' | grep -c 'expected:' | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const n=parseInt(s.trim()||'0',10);console.log('expected: lines touched in 34.13-UAT.md = '+n+' (must be 0)');process.exit(n===0?0:1)})"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx gsd-sdk query audit-uat 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const g=p=>{const r=j.results.find(x=>x.phase===p);return r?r.items.length:0};console.log('phase38='+g('38')+' phase34.13='+g('34.13')+' total='+j.summary.total_items);process.exit(g('38')===16&&g('34.13')===7&&j.summary.total_items===41?0:1)})"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && pnpm planning-gates 2>&1 | tail -3</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx prettier --check ".planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md" ".planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md" ".planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-VERIFICATION.md" ".planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md"</automated>
  </verify>
  <done>
All four origins record the outcome. 34.1's combined gamepad receipt names all five discharged ids
and both observation commits; 34.11's names the C06 log-evidence caveat; 34.13's 38-C08 receipt no
longer says "open", its 38-C07 retirement is untouched, and its body row reads DISCHARGED; 34.10's
`deferred[0]` carries both an `outcome:` and a `receipt_key_note:` explaining the
deliberately-absent key. 34.13-UAT.md's `expected:` keys are untouched, 34.13 still audits at 7
items, and 13/13 planning gates pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: File the two todos the sitting surfaced</name>
  <files>.planning/todos/pending/2026-09-25-controller-focus-has-no-perceptible-affordance.md, .planning/todos/pending/2026-09-25-mouse-highlight-does-not-confer-dom-focus.md</files>
  <action>
FIRST, check for duplicates before writing anything. Grep `.planning/todos/pending/` and
`.planning/todos/completed/` for an existing todo covering either finding. Measured at plan time:
14 pending todos, none covering either; the nearest neighbours are
`completed/2026-08-30-library-search-bar-suggestions-are-mouse-dead-until-a-tab-press.md` and
`completed/2026-09-21-library-search-typing-needs-repeated-attempts-before-it-filters-usably.md`,
which are adjacent to Todo B but are about the SEARCH SUGGESTION list, not about mouse highlight
failing to confer DOM focus on library cards. Read the first of those before writing Todo B; if it
turns out to describe the same mechanism, UPDATE it (move back to `pending/`) rather than creating
a duplicate. State the duplicate-check result in the summary either way.

Both files carry the CI-enforced frontmatter keys in this exact order, bare lowercase exact values,
`platform:` immediately after `severity:` and `ready:` immediately after `platform:`. Match the
shape of `.planning/todos/pending/2026-09-24-linux-unix-focus-sentinel-arm-may-not-restore-a-minimized-window.md`
(keys: `created`, `title`, `found_during`, `severity`, `platform`, `ready`, `area`, `files`), and
give each a `## Mechanism` section.

Set `found_during: Phase 38 controller sitting 3 (quick 260925-r8j close-out)` on both.

**Todo A — controller focus has no perceptible affordance.** `severity: major`, `platform: any`,
`ready: code`. The operator completed all eight items while unable to see where focus was; quote
their words, including the fragment "just mash down until focused regained". `major` is the right
rung because the ledger itself got contaminated: 38-C06's clause (3) could not be scored on
observation for exactly this reason, which is the "a measurement is silently contaminated"
definition in CLAUDE.md's severity table — say so in the todo and name `38-C06`, since that is the
strongest available evidence of impact.

Name at least four surfaces:
  - Console Mode `.consoleChip` pills. Machine-confirmed focus at 19:04:30 and 19:08:39, operator
    confirmed NO visible change. Source: `src/frontend/screens/ConsoleMode/index.scss:94-120` — the
    only focus styling on `.consoleChip` is a 1px accent `border-color` behind
    `&:hover, &:focus-visible` with `outline: none` (verified at plan time), i.e. focus and hover
    share one rule and neither is loud.
  - Tier-2 filter panel rows: `FilterFacetRow` and `FilterCollectionList__row`.
  - The game-page split button.

Record the adjacency to quick `260925-pga`, which unified hover and focus onto one accent ring:
making two states LOOK identical while they BEHAVE differently may COMPOUND this rather than fix
it. Frame that as an observation to check, not as a defect claim against `260925-pga`.

**Todo B — mouse highlight does not confer DOM focus.** `severity: medium`, `platform: any`,
`ready: code`. Operator repro: highlight a library card with the mouse, then use the gamepad — the
element that actually moves is the SEARCH FIELD, not the highlighted card, because the visible
highlight is not `document.querySelector(':focus')`. Corroborating machine evidence: two `padLeft`
presses two seconds apart at 18:12:16 and 18:12:18 both logged `tag=none` while a card appeared
highlighted. Cross-reference Todo A — the two are related but distinct: A is "focus is invisible",
B is "what looks focused isn't". Also cross-reference 38-C01a's cold-start disposition, since
attempt (b) there was blocked by exactly this behaviour and the operator's "library-route
highlights do not stick" is the same observation from the other side.
  </action>
  <verify>
    <automated>cd "C:/Users/grays/Projects/GameLib" && node -e "const fs=require('fs');const F=['.planning/todos/pending/2026-09-25-controller-focus-has-no-perceptible-affordance.md','.planning/todos/pending/2026-09-25-mouse-highlight-does-not-confer-dom-focus.md'];const W=[['major','any','code'],['medium','any','code']];let ok=true;F.forEach((f,k)=>{if(!fs.existsSync(f)){console.error('FAIL missing '+f);ok=false;return}const t=fs.readFileSync(f,'utf8');const m=t.match(/^---\n([\s\S]*?)\n---/);if(!m){console.error('FAIL no frontmatter '+f);ok=false;return}const L=m[1].split('\n');const iS=L.findIndex(l=>/^severity:/.test(l)),iP=L.findIndex(l=>/^platform:/.test(l)),iR=L.findIndex(l=>/^ready:/.test(l));if(iS===-1||iP!==iS+1||iR!==iP+1){console.error('FAIL key order severity/platform/ready wrong in '+f+' (indices '+iS+','+iP+','+iR+')');ok=false;return}[iS,iP,iR].forEach((ix,j)=>{const v=(L[ix].split(':')[1]||'').split('#')[0].trim();if(v!==W[k][j]||/[\x22\x27]/.test(v)){console.error('FAIL bad value in '+f+': got '+JSON.stringify(v)+' want bare '+W[k][j]);ok=false}});if(!/## Mechanism/.test(t)){console.error('FAIL no ## Mechanism section in '+f);ok=false}});const a=fs.existsSync(F[0])?fs.readFileSync(F[0],'utf8'):'';const b=fs.existsSync(F[1])?fs.readFileSync(F[1],'utf8'):'';for(const t of ['consoleChip','FilterFacetRow','260925-pga','just mash down','38-C06']){if(!a.includes(t)){console.error('FAIL todo A missing: '+t);ok=false}}for(const t of ['18:12:16','18:12:18','tag=none']){if(!b.includes(t)){console.error('FAIL todo B missing: '+t);ok=false}}console.log(ok?'OK both todos well-formed':'FAILED');process.exit(ok?0:1)"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && python3 .planning/todos/todo-frontmatter-gate.py</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && pnpm planning-gates 2>&1 | tail -3</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx prettier --check ".planning/todos/pending/2026-09-25-controller-focus-has-no-perceptible-affordance.md" ".planning/todos/pending/2026-09-25-mouse-highlight-does-not-confer-dom-focus.md"</automated>
  </verify>
  <done>
Both todos exist in `.planning/todos/pending/` with `severity`/`platform`/`ready` in that order and
bare lowercase exact values (`major`/`any`/`code` and `medium`/`any`/`code`). Both carry a
`## Mechanism` section and the named evidence. The duplicate check was performed and its result is
stated in the summary. `todo-frontmatter-gate.py` passes and 13/13 planning gates are green.
  </done>
</task>

<task type="auto">
  <name>Task 5: Remove the temporary GAMEPAD-ACT probe and prove behaviour neutrality</name>
  <files>src/frontend/helpers/gamepad.ts</files>
  <action>
Remove BOTH halves of the temporary probe added by quick `260925-op0` (commit `cdf07ee95`),
following the removal precedent of quick `260923-qe5` Task 5 and `260925-ms5`.

Half 1 — the emission. Delete the whole block currently at lines 286-310: the eight-line
"TEMPORARY -- diagnostic probe for the Phase 38 controller sitting..." comment AND the
`try { ... window.api?.logInfo?.(line) } catch { ... }` that follows it. It sits between the close
of the `switch (action)` block and the `if (action === 'mainAction')` dispatch chain. Leave exactly
one blank line where it was, so the switch's closing brace and the dispatch chain are separated the
way every other statement pair in this function is.

Half 2 — the capture that exists only to feed it. Delete the
`const requestedAction: ValidGamepadAction = action` declaration at line 182, plus the blank line
that separated it from the "check special cases for the different actions" comment. This constant
has no other reader — measured at plan time, `requestedAction` appears exactly 3 times in the file
(:182 declaration, :296 and :297 inside the emission), so once half 1 is gone it is dead. If
`ValidGamepadAction` becomes an unused import as a result, remove the import too; if it is still
used elsewhere in the file, leave it. Check, do not assume — `pnpm lint` is the arbiter.

Do NOT touch:
  - The permanent `[GAMEPAD] id=...` connect line at `gamepad.ts:662`. It is production logging,
    not part of the probe, and the sitting record depends on it continuing to exist.
  - The pre-existing commented-out `console.log` at `gamepad.ts:155`. It predates the probe by a
    long way and is not part of it. Leave it byte-identical.
  - `src/frontend/helpers/gamepad_layouts/` — nothing there carries the probe.

Then run the residue grep. Scope it to `src/` and `meta/` ONLY: this plan file and the phase-38
planning documents legitimately contain the `GAMEPAD-ACT` token as quoted evidence, and a
repo-wide grep would flag them as residue when they are the whole point.
  </action>
  <verify>
    <automated>cd "C:/Users/grays/Projects/GameLib" && N=$(grep -rn "GAMEPAD-ACT" src/ meta/ | wc -l | tr -d ' ') && echo "GAMEPAD-ACT residue in src/+meta/ = $N (expect 0, was 5)" && test "$N" -eq 0</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && N=$(grep -c "requestedAction" src/frontend/helpers/gamepad.ts || true) && echo "requestedAction in gamepad.ts = $N (expect 0, was 3)" && test "$N" -eq 0</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && N=$(grep -c '\[GAMEPAD\] id=' src/frontend/helpers/gamepad.ts) && M=$(grep -c 'console\.log(`Action: ' src/frontend/helpers/gamepad.ts) && echo "permanent connect line = $N (expect 1); pre-existing commented log = $M (expect 1)" && test "$N" -eq 1 && test "$M" -eq 1</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && git diff --quiet -- src/frontend/helpers/gamepad_layouts/ && echo "gamepad_layouts untouched OK"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && git diff -- src/frontend/helpers/gamepad.ts | grep -E '^[-+]' | grep -v '^[-+][-+]' | wc -l | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const n=parseInt(s.trim(),10);console.log('changed lines in gamepad.ts = '+n+' (expect ~27: 25 for the emission block, 2 for the capture and its blank line; a much larger number means collateral edits)');process.exit(n>=25&&35>n?0:1)})"</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && pnpm codecheck</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && pnpm lint 2>&1 | tail -5</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx jest --selectProjects Frontend nintendoLayout 2>&1 | tail -6</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx jest --selectProjects Frontend gamepadRepeatTiming 2>&1 | tail -6</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx jest --selectProjects Frontend gamepadDisconnect 2>&1 | tail -6</automated>
    <automated>cd "C:/Users/grays/Projects/GameLib" && npx prettier --check src/frontend/helpers/gamepad.ts</automated>
  </verify>
  <done>
Zero `GAMEPAD-ACT` hits under `src/` and `meta/`; zero `requestedAction` in `gamepad.ts`; the
permanent `[GAMEPAD] id=` connect line and the pre-existing commented-out `console.log` both still
present exactly once each; `gamepad_layouts/` untouched; the diff is confined to the two probe
edits. `pnpm codecheck` exits 0, `pnpm lint` reports both ceilings PASS, and the three gamepad
suites report 35/35, 2/2 and 2/2. `npx prettier --check src/frontend/helpers/gamepad.ts` is clean —
this is the one non-vacuous prettier check in the plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning ledger to `gsd-sdk query audit-uat` | A malformed edit silently zeroes the project's entire deferred-hardware backlog with nothing turning red |
| renderer log emission to `gamelib.log` | The probe wrote focused-element class names and test ids into a user-profile log file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-r8j-01 | Tampering | `38-VERIFICATION.md` frontmatter | mitigate | Every task's verify re-runs `audit-uat` and asserts the EXACT expected integers (phase 38 = 16, 34.13 = 7, total = 41), not a direction. A flat or zero count fails the gate. |
| T-r8j-02 | Repudiation | discharged `result:` fields | mitigate | Each `result:` quotes a timestamped `[GAMEPAD-ACT]` line and names its observation commit; Task 1's third verify greps for fourteen specific disclosure tokens, so a paraphrase that drops the C01a cold-start disclaimer, the C06 operator quote, C05's never-attempted history or the C03 correction fails the gate. |
| T-r8j-03 | Tampering | `34.13-UAT.md` block scalars | mitigate | The file is edited as text, not YAML round-tripped (it does not parse strictly anyway), and Task 3's third verify asserts the diff touches ZERO `expected:` lines. |
| T-r8j-04 | Denial of service | `38-HUMAN-UAT.md` | mitigate | A numbered item carrying a block-scalar `expected:` silently deletes EVERY item in the file from `audit-uat`. Task 2 forbids numbered items in the session block, its verify greps for an introduced block scalar, and `.planning/uat-visibility-gate.py` runs behind it. |
| T-r8j-05 | Information disclosure | `[GAMEPAD-ACT]` lines in `gamelib.log` | accept, then close | The probe logged DOM class names and `data-testid`s — UI structure, no credentials, no profile data — beside a permanent production log line. Task 5 removes the emission entirely, so the surface does not persist past this plan. No log shredding is specified because nothing sensitive was captured. |
| T-r8j-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs nothing. No `package.json`, `Cargo.toml` or lockfile is in `files_modified`. No package-legitimacy checkpoint is required. |
</threat_model>

<verification>
Run after all five tasks:

1. `npx gsd-sdk query audit-uat` gives phase 38 = 16, phase 34.13 = 7, grand total = 41.
2. `pnpm planning-gates` gives 13/13.
3. `grep -rn "GAMEPAD-ACT" src/ meta/` produces no output.
4. `pnpm codecheck` exits 0. `pnpm lint` reports both ceilings PASS.
5. `git status --short` lists exactly the nine files in `files_modified` plus this plan's own
   directory, and nothing else.

`labelSuiteI18nCensus.test.ts` and `storeEmbedSingleOpener.test.ts` are expected-red and unrelated.
Do not chase them and do not report them as regressions.
</verification>

<success_criteria>
- Eight items moved (not annotated) into `human_verification_discharged`, each with a `result:`
  quoting its verbatim artifact and naming its observation commit.
- `38-VERIFICATION.md` still parses via `audit-uat`, `status:` still `human_needed`, surviving
  `human_verification` order unchanged, `human_verification_retired` untouched at 10 entries.
- Phase 38's open count dropped by EXACTLY eight: 24 to 16, grand total 49 to 41.
- A "Sitting 3 — 2026-09-25" block exists in `38-HUMAN-UAT.md` carrying the commit boundary, the
  positive control, all eight artifacts, the C01a cold-start disposition and the C06 caveat; the
  sitting-1 record is annotated, not rewritten.
- All four origins record the outcome: 34.1, 34.11 and 34.13 on their
  `human_verification_relocated` receipts; 34.10 on its existing `deferred[0]` entry, with a
  `receipt_key_note:` explaining why no new key was created.
- `38-C03`'s `test:`/`expected:` describe the real rewrite mechanism, with the correction disclosed
  as a correction and the item still PASSING.
- Two pending todos exist with CI-valid frontmatter, after a stated duplicate check.
- No `GAMEPAD-ACT` token survives under `src/` or `meta/`; the permanent connect line and the
  pre-existing commented-out log both survive; `codecheck`, `lint` and the three gamepad suites
  (35/35, 2/2, 2/2) are green.
- The four honesty constraints are all visible in the written record: C01a's cold-start clause is
  NOT a d-pad-specific observation; C06's clause (3) rests on log evidence NOT operator
  observation; the `cdf07ee95`/`959e5c01b` commit boundary is recorded; the C03 text change is
  disclosed as a correction.
</success_criteria>

<output>
Create `.planning/quick/260925-r8j-phase-38-controller-sitting-close-out-di/260925-r8j-SUMMARY.md`
when done. It must state the duplicate-check result from Task 4, the measured before/after
`audit-uat` integers, and the verbatim gate battery results.
</output>
