---
phase: quick-260921-q9v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/index.scss
  - src/frontend/components/UI/Dialog/index.css
  - .planning/todos/pending/2026-09-20-the-bare-dialog-class-is-never-applied-to-any-element.md
autonomous: true
requirements: [QUICK-260921-Q9V]

must_haves:
  truths:
    - "`--dialog-margin-horizontal: 32px` is declared exactly once, in `src/frontend/index.scss`'s `:root` block, alongside `--blur-light`/`--blur-strong`, so it resolves globally and both existing `var()` references become valid at computed-value time."
    - "The bare `.Dialog { padding: 0; text-align: start; }` rule and its `quick-260921-nub` comment are gone from `src/frontend/components/UI/Dialog/index.css`; the file still exists and still carries its two live rules `.log-upload-result` and `.Dialog__footer`, so `src/frontend/components/UI/Dialog/index.ts:1`'s `import './index.css'` stays valid."
    - "`grep -rn \"dialog-margin-horizontal\" src/` returns exactly three hits, in exactly three files: the one declaration in `index.scss` and the two pre-existing `var()` references. Zero hits anywhere under `src/frontend/components/UI/Dialog/`."
    - "`npx jest --selectProjects Frontend` is green, which is the proof `cssTokenSweep.test.ts` still finds the NAME declared after the move (its `ALLOWLIST` is `[]`, so a dropped declaration would turn it red)."
    - "The todo file stays in `.planning/todos/pending/` — it is NOT closed and NOT moved — and records that the remedy shipped and that closure is gated on the orchestrator's live WKWebView re-measurement."
  artifacts:
    - path: "src/frontend/index.scss"
      provides: "The `:root` home for the moved layout constant"
      contains: "--dialog-margin-horizontal: 32px;"
    - path: "src/frontend/components/UI/Dialog/index.css"
      provides: "Only the two live rules; the dead bare-.Dialog rule removed"
      contains: ".Dialog__footer"
    - path: ".planning/todos/pending/2026-09-20-the-bare-dialog-class-is-never-applied-to-any-element.md"
      provides: "Remedy record, left open pending live re-measurement"
      contains: "ready: live-gate"
  key_links:
    - from: "src/frontend/screens/Library/components/InstallModal/index.scss:27"
      to: "src/frontend/index.scss :root"
      via: "var(--dialog-margin-horizontal) now inherited from the document root"
      pattern: "var\\(--dialog-margin-horizontal\\)"
    - from: "src/frontend/components/UI/Winetricks/index.scss:3"
      to: "src/frontend/index.scss :root"
      via: "var(--dialog-margin-horizontal) now inherited from the document root"
      pattern: "var\\(--dialog-margin-horizontal\\)"
---

<objective>
Fix the `ready: code` todo `2026-09-20-the-bare-dialog-class-is-never-applied-to-any-element.md` by
re-homing `--dialog-margin-horizontal` from a selector that matches nothing to the global `:root`
block, and by deleting the `.Dialog` rule that becomes entirely dead once the token leaves it.

Purpose: two shipped surfaces — the InstallModal anticheat banner and the Winetricks install
wrapper — reference a token declared on `.Dialog`, a class **no element in `src/` ever carries**.
Quick `260921-pec` measured the consequence live in a real WKWebView: `.anticheatInfo` renders with
`0px` on all four sides instead of `16px/32px/0px/32px`, and `.installWrapper` renders with `0px`
left/right instead of `32px`. Moving the declaration to `:root` makes both resolve.

Output: one declaration moved, one dead rule deleted, one todo annotated and left open.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/todos/pending/2026-09-20-the-bare-dialog-class-is-never-applied-to-any-element.md
@src/frontend/components/UI/Dialog/index.css
@src/frontend/index.scss
@src/frontend/components/UI/NavShell/__tests__/cssTokenSweep.test.ts

<why_this_remedy_and_not_the_other_two>
The todo's `## Severity and readiness reasoning` leaves the choice open between three options.
**Option 2 — move the declaration to `:root` — is chosen.** Record the rejections so a later reader
does not reopen a settled decision:

- **REJECTED: apply the `Dialog` class to the Paper in `Dialog.tsx`.** This would make the rule
  match — and would therefore activate `padding: 0` and `text-align: start` on all ~25 Dialog
  consumers at once. That is a far wider visual change than the defect being fixed, and it is the
  same "reviving unreviewed styling as a side effect" that `Dialog.tsx:109-120` already records as
  rejected precedent for this exact component ("reviving an unreviewed `min(700px, 85vw)`/
  `paddingTop` pair here would change sizing for all 25 Dialog consumers as an undiscussed side
  effect").
- **REJECTED: repoint the two consumers to a different token or a literal.** Scatters a magic
  number across two files and leaves the orphan declaration behind, unfixed.

`src/frontend/index.scss`'s `:root` block is already the home for exactly this kind of non-themed
global layout constant — `--blur-light: 4px` and `--blur-strong: 16px` live there and are consumed
from other stylesheets (`Login/index.scss:130`, `Login/components/Runner/index.css:11`). That file
is loaded globally via `src/frontend/index.tsx:28`'s `import './index.scss'`.
</why_this_remedy_and_not_the_other_two>

<the_todos_must_NOT_be_deleted_warning_does_not_forbid_this>
The todo says, at line 49-51: *"The declaration must NOT be deleted to 'fix' this, because deleting
it turns the gate red for the two references above (`cssTokenSweep.test.ts`'s `ALLOWLIST` is
`[]`)."*

**That warning is about the DECLARATION, not about the `.Dialog` rule that currently surrounds it,
and this plan honours it.** `cssTokenSweep.test.ts` is a NAME gate (its own documented blind spot
B: "this gate checks NAMES, not SCOPES" — a declaration found anywhere in any tracked file counts).
Moving `--dialog-margin-horizontal: 32px` to `index.scss` keeps the name declared, so the gate stays
green. Deleting the `.Dialog` *selector* from around it removes no declaration at all.

Do not read the todo's warning as forbidding this change. Do not, under any circumstance, "solve"
anything here by editing `cssTokenSweep.test.ts` or its `ALLOWLIST` — see the constraints below.
</the_todos_must_NOT_be_deleted_warning_does_not_forbid_this>

<hard_constraints>
- **Only three files may change**, and they are the three in `files_modified`. No other stylesheet,
  no `.tsx`, no test, no gate, no config.
- **Do NOT edit `src/frontend/components/UI/NavShell/__tests__/cssTokenSweep.test.ts`, do NOT touch
  its `ALLOWLIST`, and do NOT add a new gate.** If that suite goes red, the edit is wrong — fix the
  edit.
- **Do NOT close the todo, and do NOT move it to `.planning/todos/completed/`.** It stays in
  `pending/`. Closure is the orchestrator's call, after a live re-measurement this executor is not
  asked to run (see below).
- **Do NOT run the live WKWebView measurement.** It needs the Vite dev server plus a compiled Swift
  harness; the orchestrator runs it directly after your gates pass. The harness and probe already
  exist at
  `.planning/quick/260921-pec-record-the-live-wkwebview-measurements-i/evidence/wkprobe.swift` and
  `.../evidence/probe_wk.js`. Do not compile, run, or modify them.
- **A concurrent session has been committing to this repo.** Every commit must name explicit
  pathspecs, and you must run `git diff --cached --name-only` and read its output before each
  commit to confirm nothing foreign was swept in. Do not use `git add -A`, do not use `git stash`,
  and do not use the GSD commit helper (it stages the whole tree).
</hard_constraints>

<the_prediction_this_change_must_satisfy>
This is a VISUAL change: it activates styling that has never applied. The prediction is already
written, so the fix cannot be scored on "the edit looks right".

Measured before the fix (quick `260921-pec`, evidence at
`.planning/quick/260921-pec-record-the-live-wkwebview-measurements-i/evidence/wkresults.json`):

| element | as shipped (BEFORE) | under a `.Dialog` ancestor (control = authored intent) |
| --- | --- | --- |
| `.anticheatInfo` | `0px` top/right/bottom/left | `16px` top, `32px` right, `0px` bottom, `32px` left |
| `.installWrapper` | `16px` top/bottom, `0px` left/right | `16px` top/bottom, `32px` left/right |

**Falsifiable prediction, to be re-measured by the orchestrator, not by you:** after this change
`anticheatInfo_asShipped` must read `16px / 32px / 0px / 32px`, and `installWrapper_asShipped` must
read `16px / 32px / 16px / 32px`. The "as shipped" column must become identical to the control
column. Anything else means the remedy is wrong.

(Why `.installWrapper` keeps `16px` block margins: `margin-block: 1rem` at `Winetricks/index.scss:4`
is a separate, later declaration that overrides the shorthand's top/bottom once the shorthand
becomes valid.)
</the_prediction_this_change_must_satisfy>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the declaration to :root and delete the dead bare-.Dialog rule</name>
  <files>src/frontend/index.scss, src/frontend/components/UI/Dialog/index.css</files>
  <action>
Two edits, in two files.

**Edit A — `src/frontend/index.scss`.** The `:root` block is currently lines 5-14 and ends with
`--blur-strong: 16px;`. Add `--dialog-margin-horizontal: 32px;` inside that block, grouped with the
existing effect constants under its own `/* Layout */` group comment, mirroring the existing
`/* Effects */` group at line 11. Keep the value at `32px` exactly — that is the value the two
consumers were authored against and the value quick `260921-pec` measured under its positive
control.

Write a one-line provenance comment recording that the declaration was moved here from
`Dialog/index.css` by quick `260921-q9v` because the `.Dialog` selector it sat on matched no
element, and citing the todo filename.

**TRAP — the provenance comment must NOT spell the token name.** `grep -rn` does not know a comment
from a declaration, and the verify block below asserts the census is exactly THREE hits in exactly
three files. A comment that repeats `--dialog-margin-horizontal` makes that census read four and
turns your own gate red against correct code. Refer to it as "this token" / "the Dialog horizontal
margin constant" instead. (This repo has recorded this exact failure mode twice: "prose satisfies
the gate that names it" and "raw source gate is satisfied by the prose that names it".)

**Edit B — `src/frontend/components/UI/Dialog/index.css`.** Delete the entire first rule, lines 1-12
inclusive: the `.Dialog { … }` selector, its `padding: 0` and `text-align: start` declarations, the
whole `quick-260921-nub` block comment, and the `--dialog-margin-horizontal: 32px;` line it guards.

The `quick-260921-nub` comment goes with the rule, not after it: every claim in it ("kept because …
still reference it by name", "whether it actually resolves at those two sites is a separate, open
question") is answered and stale the moment the declaration moves. Leaving it would make the file
lie about what remains.

`padding: 0` and `text-align: start` are deleted rather than re-homed, deliberately: per the todo's
`## Measured facts`, nothing in `src/` is ever given the bare class `Dialog`, so neither declaration
has ever applied to anything. Re-homing them would be the REJECTED option 1 above — an unreviewed
visual change across ~25 Dialog consumers — smuggled in under a token fix.

**The file itself must survive.** `src/frontend/components/UI/Dialog/index.ts:1` does
`import './index.css'`, and two tests mock that exact specifier
(`.../UploadedLogFilesList/__tests__/deleteButtonReachability.test.tsx:52` and
`.../SteamLogin/__tests__/steamCreateAccountLink.test.tsx:53`). Keep `.log-upload-result` and
`.Dialog__footer` exactly as they are — both are live.
  </action>
  <verify>
    <automated>
# 1. Census: exactly THREE hits, in exactly THREE files, and none under Dialog/.
test "$(grep -rn 'dialog-margin-horizontal' src/ | wc -l | tr -d ' ')" = "3" \
  && grep -rl 'dialog-margin-horizontal' src/ | sort | tr '\n' ' ' \
     | grep -qx 'src/frontend/components/UI/Winetricks/index.scss src/frontend/index.scss src/frontend/screens/Library/components/InstallModal/index.scss ' \
  && test "$(grep -rn 'dialog-margin-horizontal' src/frontend/components/UI/Dialog/ | wc -l | tr -d ' ')" = "0" \
  && echo CENSUS_OK

# 2. The declaration exists, once, in the :root block, with the value intact.
test "$(grep -c -- '--dialog-margin-horizontal: 32px;' src/frontend/index.scss | tr -d ' ')" = "1" && echo DECL_OK

# 3. The bare rule is gone. Anchor on the SPACE-BRACE: '.Dialog' is a SUBSTRING of
#    '.Dialog__footer', which must SURVIVE — a bare '\.Dialog' grep would false-RED here.
test "$(grep -c '^\.Dialog {' src/frontend/components/UI/Dialog/index.css | tr -d ' ')" = "0" \
  && test "$(grep -c '260921-nub' src/frontend/components/UI/Dialog/index.css | tr -d ' ')" = "0" \
  && grep -q '^\.Dialog__footer {' src/frontend/components/UI/Dialog/index.css \
  && grep -q '^\.log-upload-result {' src/frontend/components/UI/Dialog/index.css \
  && echo RULE_DELETED_LIVE_RULES_KEPT

# 4. Typecheck.
pnpm codecheck

# 5. Frontend jest — this is where cssTokenSweep.test.ts lives. Green here is the
#    proof the declaration SURVIVED the move (its ALLOWLIST is [], so a dropped
#    declaration turns it red on the two var() references).
npx jest --selectProjects Frontend

# 6. Lint, asserting the two printed COUNTS numerically — NOT the exit code.
#    Measured at c71b1eb4f (this plan's baseline): production 1119, tests 638.
#    Ceilings are SRC_CEILING = 1124 / TESTS_CEILING = 638, so src has 5 slots of
#    headroom and tests has ZERO. (The dispatch brief said "one slot in src"; that is
#    stale — 1119/1124 was re-measured at this baseline.)
#    THE INVARIANT IS *UNCHANGED*, not the literal numbers: ESLint here lints only
#    .ts/.tsx (see meta/lintScoped.cjs SCOPES), and this task touches only .scss/.css,
#    so neither count can move. If the PRE-EDIT tree already reads something other than
#    1119/638 because the concurrent session landed .ts/.tsx work, record the pre-edit
#    numbers in the SUMMARY and assert post-edit == pre-edit, still within 1124/638.
#    Do not "fix" a drifted baseline by editing a ceiling.
pnpm lint 2>&1 | grep -E '✖ [0-9]+ problems' | tr -d ' ' | tr '\n' '|'
# must print: ✖1119problems(0errors,1119warnings)|✖638problems(0errors,638warnings)|
    </automated>
  </verify>
  <done>
`--dialog-margin-horizontal: 32px` is declared once in `index.scss`'s `:root`; the bare `.Dialog`
rule and its `quick-260921-nub` comment are gone from `Dialog/index.css` while `.log-upload-result`
and `.Dialog__footer` remain; the token census reads exactly three hits across exactly three files
with zero under `Dialog/`; `pnpm codecheck` passes; `npx jest --selectProjects Frontend` is green
including `cssTokenSweep`; `pnpm lint` prints 1119 and 638 (or the recorded, unchanged pre-edit
pair).
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the remedy in the todo, leave it open, and commit with explicit pathspecs</name>
  <files>.planning/todos/pending/2026-09-20-the-bare-dialog-class-is-never-applied-to-any-element.md</files>
  <action>
**Do NOT close or move this todo.** It stays in `.planning/todos/pending/`.

**Frontmatter — flip two values, and only these two.** What remains on this item is a live WKWebView
re-measurement on the operator's Mac; nothing further is desk-ready, so per CLAUDE.md's vocabulary
table:

- `ready: code` → `ready: live-gate`
- `platform: any` → `platform: macos` (the re-measurement needs the compiled Swift/WKWebView harness)

Leave `severity: medium`, `created`, `title`, `area`, `source` and `files` untouched. Both new values
are in the enforced vocabulary; do not widen anything, and do not quote the values —
`.planning/todos/todo-frontmatter-gate.py` matches bare, lowercase and exact.

Add `src/frontend/index.scss` to the frontmatter `files:` list, since the token now lives there.

**Body — replace the stale reasoning, do not just append.** The file currently asserts `ready: code`
in prose at `## Severity and readiness reasoning` ("What remains is a DECISION about where the token
should live … plus the edit itself — desk work"). That sentence is false the moment this ships.
Rewrite that paragraph to say the decision was made and the edit shipped, and that what remains is
the live re-measurement. A file that keeps a stale claim next to a flipped frontmatter key is the
recorded "shipping part of a todo and keeping it open makes its title false" failure mode — do not
leave one behind.

Then replace the `## Not fixed here` section with a `## Remedy shipped 2026-09-21 (quick 260921-q9v)
— NOT YET CLOSED` section recording, in the todo's own words:

1. **What shipped:** the declaration moved from `.Dialog` in `Dialog/index.css` to the `:root` block
   in `src/frontend/index.scss`, next to `--blur-light`/`--blur-strong`; and the now-entirely-dead
   bare `.Dialog` rule (`padding: 0`, `text-align: start`, plus the `quick-260921-nub` comment) was
   deleted. `Dialog/index.css` survives with `.log-upload-result` and `.Dialog__footer`.
2. **Why option 2 and not the other two** — reproduce both rejections from this plan's
   `<why_this_remedy_and_not_the_other_two>` block, including the `Dialog.tsx:109-120` precedent, so
   the settled choice is recorded in the todo itself and not only in a quick-task plan.
3. **Why this does not violate this file's own "must NOT be deleted" warning:** that warning is
   about the DECLARATION, which still exists; `cssTokenSweep.test.ts` is a NAME gate (its blind spot
   B), so moving the name keeps it green. State this explicitly — the warning as written reads like
   a prohibition on this exact change and a future reader will trip on it otherwise.
4. **The gates that were run and what they prove**, with the numbers from Task 1 — and state plainly
   that `npx jest --selectProjects Frontend` going green proves only that the NAME is declared
   somewhere, **not** that anything renders. `cssTokenSweep.test.ts`'s own header says it "can NEVER
   prove that anything renders", and this repo's frontend jest project is `testEnvironment: 'node'`
   with no CSS engine.
5. **Why this stays open:** the falsifiable prediction, written out —
   `anticheatInfo_asShipped` must become `16px / 32px / 0px / 32px` and `installWrapper_asShipped`
   must become `16px / 32px / 16px / 32px`, i.e. the "as shipped" column must become identical to
   the `## Measured live under WKWebView` control column. Name the harness and probe paths
   (`.planning/quick/260921-pec-.../evidence/wkprobe.swift`, `.../evidence/probe_wk.js`) and state
   that the re-measurement is the ORCHESTRATOR's to run and that closure depends on its result.

Do not touch the existing `## Measured live under WKWebView` section, including its contamination
warning about `elementsCarryingBareDialogClass: 2` — it is still accurate and still load-bearing.

**Commits.** Two commits, each with explicit pathspecs and each preceded by reading
`git diff --cached --name-only` to confirm nothing from the concurrent session was swept in:

```
git add src/frontend/index.scss src/frontend/components/UI/Dialog/index.css
git diff --cached --name-only      # read it: must be exactly those two paths
git commit -m "fix(quick-260921-q9v): re-home --dialog-margin-horizontal to :root, delete the dead bare .Dialog rule"

git add .planning/todos/pending/2026-09-20-the-bare-dialog-class-is-never-applied-to-any-element.md
git diff --cached --name-only      # read it: must be exactly that one path
git commit -m "docs(quick-260921-q9v): record the remedy on the bare-.Dialog todo, re-triage to live-gate"
```

End each commit message with:

```
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

Do NOT write any claim about push state into a commit message.
  </action>
  <verify>
    <automated>
TODO=.planning/todos/pending/2026-09-20-the-bare-dialog-class-is-never-applied-to-any-element.md

# Still pending, never moved.
test -f "$TODO" \
  && test ! -e ".planning/todos/completed/$(basename "$TODO")" \
  && echo STILL_PENDING

# Frontmatter: exactly the two intended flips, bare/lowercase/unquoted.
grep -qx 'severity: medium' "$TODO" \
  && grep -qx 'platform: macos' "$TODO" \
  && grep -qx 'ready: live-gate' "$TODO" \
  && echo FRONTMATTER_OK

# The remedy record exists and the stale "desk work" claim is gone.
grep -q 'NOT YET CLOSED' "$TODO" \
  && grep -q 'index.scss' "$TODO" \
  && test "$(grep -c 'Not fixed here' "$TODO" | tr -d ' ')" = "0" \
  && echo REMEDY_RECORDED

# Planning gates (the todo-frontmatter gate lives here).
pnpm planning-gates

# Commit hygiene: nothing foreign in the last two commits, worktree clean.
git log --oneline -2
git show --stat --name-only --format= HEAD~1 HEAD
test -z "$(git status --porcelain)" && echo TREE_CLEAN
    </automated>
  </verify>
  <done>
The todo is still in `pending/`, carries `platform: macos` + `ready: live-gate`, records the shipped
remedy and both rejected options, explains why the "must NOT be deleted" warning is satisfied,
states the falsifiable prediction and that the orchestrator owns the re-measurement, and carries no
surviving "desk work remains" claim. `pnpm planning-gates` is green. Two commits exist, each
touching only its declared pathspecs, each carrying the attribution line, neither asserting push
state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This change is two static stylesheet edits plus a planning-doc edit. No input is parsed, no network is reached, no process is spawned, no credential or profile data is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q9v-01 | Information disclosure | `:root` scope widening | accept | Promoting a layout constant to `:root` exposes a `32px` literal to every stylesheet. There is no secret in a margin value, and the three-hit census in Task 1 bounds the consumer set to the two pre-existing references. |
| T-q9v-02 | Tampering | concurrent session's commits | mitigate | Explicit pathspecs on every `git add`, plus a mandatory `git diff --cached --name-only` read before each commit; no `git add -A`, no `git stash`, no tree-staging commit helper. |
| T-q9v-SC | Tampering | npm/pip/cargo installs | n/a | No package is installed by this plan. No `package.json` change, no lockfile change. |
</threat_model>

<verification>
Run in order, from the repo root:

1. `grep -rn "dialog-margin-horizontal" src/` → exactly 3 hits, in `index.scss`,
   `InstallModal/index.scss`, `Winetricks/index.scss`; zero under `components/UI/Dialog/`.
2. `pnpm codecheck` → exit 0.
3. `npx jest --selectProjects Frontend` → green, `cssTokenSweep` included.
4. `pnpm lint` → prints `1119` and `638` (or the recorded, unchanged pre-edit pair), both within
   `SRC_CEILING = 1124` / `TESTS_CEILING = 638`. Assert the COUNTS, not the exit code.
5. `pnpm planning-gates` → green.
6. `git status --porcelain` → empty; `git show --stat --name-only --format= HEAD~1 HEAD` → exactly
   the three declared files and nothing else.

**NOT run by the executor:** the live WKWebView re-measurement. The orchestrator runs it after the
above pass, using the existing harness at
`.planning/quick/260921-pec-record-the-live-wkwebview-measurements-i/evidence/wkprobe.swift` plus
`.../evidence/probe_wk.js`, and closes the todo only if `anticheatInfo_asShipped` comes back
`16px / 32px / 0px / 32px` and `installWrapper_asShipped` comes back `16px / 32px / 16px / 32px`.
</verification>

<success_criteria>
- [ ] `--dialog-margin-horizontal: 32px` declared exactly once, in `src/frontend/index.scss`'s
      `:root` block; the provenance comment there does NOT spell the token name.
- [ ] `.Dialog { padding: 0; text-align: start; … }` and the `quick-260921-nub` comment deleted from
      `src/frontend/components/UI/Dialog/index.css`; `.log-upload-result` and `.Dialog__footer`
      untouched; the file still exists for `Dialog/index.ts`'s import.
- [ ] Token census: exactly 3 hits across exactly 3 files, 0 under `components/UI/Dialog/`.
- [ ] `pnpm codecheck`, `npx jest --selectProjects Frontend`, `pnpm planning-gates` all green.
- [ ] `pnpm lint` counts assert numerically and are UNCHANGED from the pre-edit tree.
- [ ] `cssTokenSweep.test.ts` unedited; its `ALLOWLIST` still `[]`; no new gate added.
- [ ] Exactly three files changed. No `.tsx`, no other stylesheet, no gate, no config.
- [ ] The todo is still in `pending/`, re-triaged to `platform: macos` / `ready: live-gate`, records
      the remedy and both rejections, and states that closure awaits the orchestrator's live
      re-measurement.
- [ ] Two commits, explicit pathspecs, `git diff --cached --name-only` read before each, attribution
      line present, no push-state claim in any message.
</success_criteria>

<output>
Create `.planning/quick/260921-q9v-move-dialog-margin-horizontal-to-root-an/260921-q9v-SUMMARY.md`
when done. Record: the pre-edit and post-edit lint counts (both scopes), the token census output
verbatim, the final contents of `Dialog/index.css`, and an explicit statement that the live
WKWebView re-measurement was NOT run and that the todo was deliberately left open.
</output>
