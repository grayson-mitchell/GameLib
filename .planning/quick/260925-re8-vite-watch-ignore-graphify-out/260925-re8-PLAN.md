---
phase: quick-260925-re8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260925-re8-vite-watch-ignore-graphify-out/260925-re8-GATE.md
  - vite.config.ts
  - meta/__tests__/viteRendererConfig.test.ts
  - .planning/todos/pending/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md
  - .planning/todos/completed/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md
autonomous: true
requirements: [QUICK-260925-re8]

must_haves:
  truths:
    - "The live gate ran on the CURRENT (post-260924-vat) vite config and produced a recorded verdict of RED, GREEN, or INCONCLUSIVE."
    - "A GREEN verdict is a legitimate terminal outcome: the todo is closed with the negative finding written down and no speculative config edit is made."
    - "A RED verdict produces a fix that names graphify-out specifically, leaves '**/src-tauri/target/**' unwidened, restates none of Vite's defaults, and is re-verified by re-running the gate."
    - "The build/ question the todo raises is decided explicitly and recorded with its reason, either way."
    - "The todo ends in .planning/todos/completed/ carrying the gate's actual outcome, not a silent deletion."
  artifacts:
    - path: ".planning/quick/260925-re8-vite-watch-ignore-graphify-out/260925-re8-GATE.md"
      provides: "The gate verdict plus verbatim evidence"
      contains: "VERDICT:"
    - path: ".planning/todos/completed/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md"
      provides: "Closed todo recording the gate's actual outcome"
  key_links:
    - from: "260925-re8-GATE.md VERDICT line"
      to: "Task 2 execute-or-skip decision"
      via: "explicit branch condition as the first sentence of Task 2's action"
---

<objective>
Action pending todo `2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md`:
the Vite dev-server watcher may still walk `graphify-out/`, so `graphify update .` — an action
CLAUDE.md actively instructs — can EBUSY-kill the dev server on Windows.

**This plan is gate-first and the fix is conditional.** The todo is emphatic that the observed crash
happened on a dev server started at 18:24, whereas `260924-vat`'s fix (`c9775edac`) landed at
22:35:21 — about three minutes before the 22:38 crash. That server ran the **pre-fix** config, so
the crash "is NOT a reproduction against the current config, and must not be recorded as one."

Purpose: establish, on the current config, whether this defect is live — then either fix it (RED) or
close the todo with a recorded negative finding (GREEN). **Both are correct outcomes of this plan.**

Output: a gate record; conditionally a one-entry watcher-ignore addition plus its jest pin; a closed
todo either way.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/todos/pending/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md
@.planning/todos/completed/2026-09-24-tauri-dev-on-windows-crashes-on-vite-ebusy-watching-src-tauri-target.md

<facts_already_established>
Do not re-derive these. Each was confirmed against the tree during planning.

1. **`workflow.use_worktrees` is false** for this project, so you run on the main tree. This is
   load-bearing: `graphify-out/` is gitignored (`.gitignore:63`, ~340 MB, dated subdirs
   `2026-09-22`..`2026-09-25` plus `cache/`, `graph.json`, `graph.html`, `GRAPH_REPORT.md`,
   `manifest.json`). A worktree-isolated run would not have the directory the gate depends on and
   would be silently, confidently GREEN. If you find yourself in a tree without `graphify-out/`, the
   gate is INCONCLUSIVE — say so; do not report GREEN.

2. **`vite.config.ts:93-112`** currently holds, inside `server`, after `port: 5173` /
   `strictPort: true`, a `260924-vat` rationale comment and then exactly:
   `watch: { ignored: ['**/src-tauri/target/**'] }`.
   That comment states two prohibitions with reasons: Vite 6 **APPENDS** this list to its own
   defaults (`.git`/`node_modules`/`test-results`/cacheDir) so do not restate them; and "Only
   target/ is ignored -- it is the only cargo output under src-tauri -- do not widen this to
   `src-tauri/**`". Engage with that reasoning; do not override it silently.

3. **The gate needs no cargo build.** `src-tauri/tauri.conf.json` sets
   `"beforeDevCommand": "pnpm exec vite"`, and the process that died in the observed crash was that
   bare vite dev server. `pnpm tauri:dev` resolves to
   `pnpm build:sidecar && pnpm build:decompress-worker-dev && tauri dev` — an esbuild sidecar bundle
   plus a full cargo compile, none of which bears on a chokidar `fs.watch` failure. Bare
   `pnpm exec vite` is the **same binary, same `vite.config.ts`, same `root: '.'`, same watcher, same
   process that died**. It is the narrow path to an identical signal. Use it; the todo's suggested
   `pnpm tauri:dev` is the slow path to the same observation.

4. **`build/` is gitignored** at `.gitignore:12` (`/build`) and `emptyOutDir: false` (with a MUST-stay
   -false comment at `vite.config.ts:119-125`), which is the basis of the todo's closing question.
   `260924-vat` already ledgered `build/` as "Noted, not actioned ... an observation only".

5. **Existing jest pin**, `meta/__tests__/viteRendererConfig.test.ts:247-273`, inside the
   `describe.each(['production', 'development'])` block: `ignores src-tauri/target in the dev-server
   watcher`; `does not widen the watcher ignore to the whole src-tauri tree` (asserts no entry matches
   `/src-tauri\/\*\*$/`); `does not restate vite defaults in the watcher ignore list` (asserts absence
   of `**/node_modules/**` and `**/.git/**`). Plus a source-text guard at `:333-334` (`keeps the
   260924-vat EBUSY rationale next to the watcher ignore`) asserting the config source contains both
   `260924-vat` and `EBUSY`. The file header's numbered regression list mentions the watcher as item 6
   at `:30-32`.

6. **`.planning/` and `graphify-out/` are both in `.prettierignore`** (bare entries `.planning` and
   `graphify-out`). Measured during planning: `npx prettier --check <an-ignored-path>` matches ZERO
   files and prints "All matched files use Prettier code style!" with **exit 0** — a vacuous green. So
   a prettier check over `.planning/**` artifacts proves nothing and must not be written as though it
   did. CLAUDE.md's formatter convention still binds fully for every **non-ignored** path you write:
   `vite.config.ts` and `meta/__tests__/viteRendererConfig.test.ts`.

7. **`graphify` is on PATH** at `/c/Users/grays/.local/bin/graphify`.

8. **Fake-HOME isolation does not apply here.** CLAUDE.md's two-profile rule governs spawning the
   compiled sidecar / SEA binary, which reads the user profile. This gate spawns only the vite dev
   server and the graphify CLI; neither touches GOG/Epic/Steam session state. Faking `HOME` would
   also not move the repo root, so it would not isolate `graphify-out/` anyway — and an isolated tree
   is exactly the silently-green failure mode fact 1 warns about. Do **not** wrap this gate in
   `createFakeHomeProfile()`.

9. **Scratchpad for all logs** (never the repo):
   `C:\Users\grays\AppData\Local\Temp\claude\C--Users-grays-Projects-GameLib\69d1698d-d502-4f9e-87f6-14053e8b5fe2\scratchpad`
</facts_already_established>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the live gate against the CURRENT config and record a verdict (RED, GREEN, or INCONCLUSIVE)</name>
  <files>.planning/quick/260925-re8-vite-watch-ignore-graphify-out/260925-re8-GATE.md</files>
  <action>
Establish whether the defect is live on the post-`260924-vat` config.

**A GREEN verdict is a legitimate, recordable, terminal outcome — NOT a failed attempt to be retried
until it goes red.** Do not loop. Do not escalate to `pnpm tauri:dev` hoping for a different answer.
Run each arm exactly once, unless a named *confounder* (below) invalidated that arm, in which case
re-run that arm once after clearing the confounder and record that you did.

Do not modify `vite.config.ts` in this task. The point is to observe the config as it stands.

PREFLIGHT — all three must pass, otherwise the verdict is INCONCLUSIVE and you stop:
- `graphify-out/graph.json` exists and is large (tens of MB). If absent you are not on the main tree.
- Port 5173 is free. Because `strictPort: true`, a squatter makes vite exit non-zero with
  `EADDRINUSE` / "Port 5173 is already in use" — a confounder that LOOKS like a failure. Kill the
  squatter, or record INCONCLUSIVE.
- `git status --short vite.config.ts` is clean, so the gate observes committed config.

ARM A — the observed ordering (server up first, then the write). Mirrors the todo: server running,
`graphify update .` rewrites `graphify-out/<today>/graph.json` underneath it.
  1. Start `pnpm exec vite` in the background, stdout+stderr teed to `scratchpad/armA-vite.log`.
  2. Wait for the "ready in Nms" / Local URL line (vite up, initial walk done). **Bound this wait at
     90s**; if no ready line by then, Arm A is INCONCLUSIVE — capture the log tail and move on.
  3. Run `graphify update .` in the foreground to completion. **Bound at 300s**; on timeout, kill it,
     mark Arm A INCONCLUSIVE, and say so.
  4. For 30s after graphify exits, poll `armA-vite.log` for the failure signature; then record whether
     the vite process is still alive and its exit code if not.

ARM B — cold walk into a file held open for writing. Strictly more likely to arm than Arm A, because
the observed stack frame is `NodeFsHandler._addToNodeFs` -> `createFsWatchInstance`: a watcher being
*added* to a file, which is exactly what chokidar's initial directory walk does. Arm A can in
principle produce only a `change` event on an already-watched path and miss that branch entirely.
**Arm B is the decisive arm; a GREEN resting on Arm A alone is weak and must be labelled as such.**
  1. Start `graphify update .` in the background, teed to `scratchpad/armB-graphify.log`.
  2. Once it is demonstrably writing (a few seconds in, or a fresh/growing file is observable under
     `graphify-out/`), start `pnpm exec vite` teed to `scratchpad/armB-vite.log` so its initial walk
     overlaps the write.
  3. **Bound the vite observation at 120s.** Record whether it reached its ready line or died.

FAILURE SIGNATURE (=> RED). Any of these in a vite log, attributed to a path under `graphify-out/`:
  - `Error: EBUSY: resource busy or locked, watch 'C:\...\graphify-out\...'`
  - `errno: -4082`, `syscall: 'watch'`, `code: 'EBUSY'`
  - `Emitted 'error' event on FSWatcher instance`
  - and/or the vite process exiting non-zero with the above in its output.
An `EBUSY` naming a path **outside** `graphify-out/` is a different finding: record it verbatim and do
not attribute it to this todo.

CLEANUP. Kill any background vite before finishing — port 5173 must be free when the task ends. The
logs hold vite/graphify output, not profile data, so `registerCapture()` shredding is not required;
but they stay in the scratchpad and are never committed.

WRITE `260925-re8-GATE.md` containing, in this order:
  - a bare `VERDICT: RED` / `VERDICT: GREEN` / `VERDICT: INCONCLUSIVE` line, on its own line;
  - the exact commands run, with timestamps;
  - per-arm outcome (including which arms were INCONCLUSIVE and why);
  - verbatim log excerpts of anything matching or near-matching the signature — or, if nothing did,
    an explicit "no EBUSY, no FSWatcher error, vite alive at end of arm" statement per arm;
  - **required even on GREEN** — a "what this does and does not establish" paragraph in the todo's own
    register. For GREEN: it establishes that the arms tested did not reproduce on this config, this
    machine, this date. It does NOT prove the path is safe — chokidar is still walking `graphify-out/`,
    and the EBUSY depends on a write/watch race that timing may simply not have hit.
  </action>
  <verify>
    <automated>test -f .planning/quick/260925-re8-vite-watch-ignore-graphify-out/260925-re8-GATE.md; grep -Eq '^VERDICT: (RED|GREEN|INCONCLUSIVE)$' .planning/quick/260925-re8-vite-watch-ignore-graphify-out/260925-re8-GATE.md</automated>
    <automated># Formatter gate, honestly scoped. This task wrote exactly one file, under .planning/, which is
# in .prettierignore (bare entry `.planning`). Measured: prettier --check on an ignored path
# matches ZERO files and exits 0, so running it here would be a green check proving nothing. It is
# deliberately NOT run and NOT claimed as coverage. Assert the exclusion is still real instead, so
# this reasoning stays falsifiable:
grep -qx '.planning' .prettierignore</automated>
  </verify>
  <done>
`260925-re8-GATE.md` exists with a `VERDICT:` line, per-arm evidence, and the
does/does-not-establish paragraph. Port 5173 is free. `vite.config.ts` is unmodified
(`git status --short vite.config.ts` empty). No log files inside the repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: CONDITIONAL — ignore graphify-out in the dev-server watcher, decide build/, and re-run the gate</name>
  <files>vite.config.ts, meta/__tests__/viteRendererConfig.test.ts</files>
  <action>
**BRANCH CONDITION — read first.** Execute this task ONLY IF `260925-re8-GATE.md` records
`VERDICT: RED`.

- If `VERDICT: GREEN` — **SKIP this task entirely.** Change nothing in `vite.config.ts`. Do not apply
  the "obvious one-line fix" anyway; an unobserved entry in that array dilutes the property that makes
  the list auditable (every entry there is a *measured* failure with a named mechanism), which is the
  same instinct that kept `src-tauri/**` narrow in `260924-vat`. Proceed directly to Task 3 and close
  the todo on the negative finding.
- If `VERDICT: INCONCLUSIVE` — **SKIP this task.** Proceed to Task 3 and close the todo by
  **re-scoping** it (see Task 3), not by resolving it. An inconclusive gate is not a licence to guess.

IF RED, do all of the following:

1. **Config edit (`vite.config.ts`).** Add `'**/graphify-out/**'` as a second entry to the existing
   `server.watch.ignored` array. Name the specific generated directory — do NOT reach for a
   wholesale "ignore everything gitignored" rule, and do NOT widen `'**/src-tauri/target/**'`. Restate
   none of Vite's own defaults (fact 2: Vite 6 appends).

2. **Extend the in-situ comment rather than replacing it.** Keep the existing `260924-vat` text
   intact, including the tokens `260924-vat` and `EBUSY` (a source-text jest guard depends on both,
   fact 5) and the two prohibitions. Append a `260925-re8` paragraph stating: `graphify-out/` is the
   **second member of the same defect class**, not a new one — same mechanism (`fs.watch` on a file
   held open for writing, Windows-only), different generated directory; it is ~340 MB the repo itself
   treats as disposable (`.gitignore:63`, "regenerable via `graphify update .`"); and the trigger is
   *instructed*, since CLAUDE.md tells you to run `graphify update .` after modifying code. Record the
   gate verdict and date in that comment so a future reader knows this entry is measured, not
   speculative.

3. **Decide `build/` in the same breath and write the decision down.** Default position: **DECLINE**
   adding `'**/build/**'`. Reasons to record in the comment and the SUMMARY: it has never been
   observed failing; `260924-vat` already ledgered it as observation-only; and keeping the array to
   observed-and-gated entries is what makes it auditable. **Falsifiable override:** if either gate arm
   produced an `EBUSY` naming a path under `build/`, that is an observation — add `'**/build/**'` too,
   with its own rationale line. State which branch you took either way; do not leave it unaddressed.

4. **Extend the jest pin (`meta/__tests__/viteRendererConfig.test.ts`).** Inside the existing
   `describe.each(['production', 'development'])` block, next to the `260924-vat` cases at `:247-273`:
   add `ignores graphify-out in the dev-server watcher` asserting `ignored` contains
   `'**/graphify-out/**'`. Leave the two prohibition cases (`does not widen ... src-tauri tree`,
   `does not restate vite defaults`) unchanged and still passing. Extend the source-text guard at
   `:333-334` so the config source must also contain `260925-re8`, keeping the existing `260924-vat`
   and `EBUSY` assertions. Add `graphify-out` to the header list item 6 at `:30-32` so the file's own
   summary of what it protects stays true.

5. **Re-run the gate to confirm GREEN-after-RED.** Typechecking is not confirmation of a watcher fix.
   Re-run whichever arm(s) went RED in Task 1, with the same preflight and the same bounds, and append
   a `## Re-run after fix` section to `260925-re8-GATE.md` with the new evidence. If the re-run is
   still RED, STOP and report — do not stack further speculative ignore entries.
  </action>
  <verify>
    <automated># SKIPPED-when-GREEN is valid; gate these on the verdict. If RED, the fix and its pin must hold.
if grep -q '^VERDICT: RED$' .planning/quick/260925-re8-vite-watch-ignore-graphify-out/260925-re8-GATE.md; then
  npx jest meta/__tests__/viteRendererConfig.test.ts
  grep -q "graphify-out" vite.config.ts
  grep -q "260925-re8" vite.config.ts
  grep -q "260924-vat" vite.config.ts
  grep -q "EBUSY" vite.config.ts
  grep -q "## Re-run after fix" .planning/quick/260925-re8-vite-watch-ignore-graphify-out/260925-re8-GATE.md
  # The 260924-vat prohibitions must survive: no widening to the whole src-tauri tree.
  test "$(grep -v '^ *//' vite.config.ts | grep -c "src-tauri/\*\*'")" = "0"
else
  git diff --quiet vite.config.ts meta/__tests__/viteRendererConfig.test.ts
fi</automated>
    <automated># CLAUDE.md formatter convention, explicit paths only (never `.`). Both paths are NON-ignored
# source, so this check is real coverage. Skip only if the task was skipped.
if grep -q '^VERDICT: RED$' .planning/quick/260925-re8-vite-watch-ignore-graphify-out/260925-re8-GATE.md; then
  npx prettier --check vite.config.ts meta/__tests__/viteRendererConfig.test.ts
fi</automated>
    <automated>npx tsc -p tsconfig.meta.json --noEmit</automated>
  </verify>
  <done>
Either (GREEN/INCONCLUSIVE) `vite.config.ts` and `meta/__tests__/viteRendererConfig.test.ts` are
byte-unchanged and the skip is stated in the SUMMARY with its reason; or (RED)
`server.watch.ignored` contains `'**/graphify-out/**'`, `'**/src-tauri/target/**'` is unwidened, no
Vite default is restated, the comment carries both `260924-vat` and `260925-re8` rationale, the
`build/` decision is recorded with its reason, the jest pin covers the new entry and passes, and
`260925-re8-GATE.md` has a `## Re-run after fix` section showing GREEN after RED.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the todo into completed/ with the gate's actual outcome recorded</name>
  <files>.planning/todos/pending/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md, .planning/todos/completed/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md</files>
  <action>
Move the todo from `.planning/todos/pending/` to `.planning/todos/completed/` with `git mv` (same
filename), then append a resolution section recording what actually happened. **The todo is never
silently deleted, and a GREEN gate does not mean "nothing to write down" — the negative finding IS
the deliverable in that branch.**

Frontmatter: keep `severity`/`platform`/`ready` as they are. CLAUDE.md's todo-frontmatter gate
(`.planning/todos/todo-frontmatter-gate.py`, run by `pnpm planning-gates`) scopes those three keys to
`pending/` only — `completed/` is deliberately exempt — so the move needs no frontmatter surgery. Add
a `status:` key matching the sibling's convention (`260924-vat` uses `status: RESOLVED`):

- RED branch -> `status: RESOLVED`. Append `## Resolution (quick-260925-re8)` recording: the gate
  reproduced on the current config (which arm, with the verbatim signature); the fix as applied
  (`'**/graphify-out/**'`, the untouched `src-tauri/target` entry, no restated defaults); the `build/`
  decision and its reason; the jest pin cases added; and the post-fix gate re-run result. Also state
  plainly that this is the **second** member of the class whose first member `260924-vat` fixed.
- GREEN branch -> `status: NOT-REPRODUCED`. Append `## Closed on a negative finding
  (quick-260925-re8)` recording: the arms run, their bounds, that nothing matched the EBUSY signature,
  and — carrying forward the todo's own carefulness — that this does NOT establish the path is safe,
  only that the race did not arm in the arms tested. Note explicitly that **no config change was
  made** and why a speculative one was declined. State what is still true from the todo's "What IS
  established" list: `graphify-out/` remains absent from `server.watch.ignored` and is still walked by
  chokidar, and the trigger remains an instructed action. If that residual risk is judged worth
  tracking, say so in one sentence rather than reopening.
- INCONCLUSIVE branch -> do **not** mark resolved. Either leave the file in `pending/` with an
  `## Attempted gate (quick-260925-re8)` section naming the blocker and what would unblock it (keeping
  `ready: live-gate`), or move it to completed with `status: RE-SCOPED` and file the remaining work as
  a fresh pending todo. Pick one, state which, and do not report a resolution you do not have.

Then update `.planning/STATE.md`'s `last_activity` line to one sentence naming quick `260925-re8` and
the gate's verdict. Do not touch `ROADMAP.md`.
  </action>
  <verify>
    <automated># The todo is out of pending/ unless the verdict was INCONCLUSIVE and the pending-arm was chosen.
if grep -q '^VERDICT: INCONCLUSIVE$' .planning/quick/260925-re8-vite-watch-ignore-graphify-out/260925-re8-GATE.md; then
  ls .planning/todos/pending/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md .planning/todos/completed/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md 2>/dev/null | grep -q .
else
  test ! -f .planning/todos/pending/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md
  test -f .planning/todos/completed/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md
  grep -Eq '^status: (RESOLVED|NOT-REPRODUCED|RE-SCOPED)$' .planning/todos/completed/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md
  grep -q '260925-re8' .planning/todos/completed/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md
fi</automated>
    <automated>pnpm planning-gates</automated>
    <automated># Formatter gate, honestly scoped. Every path this task wrote is under .planning/, which is in
# .prettierignore (bare entry `.planning`). Measured: prettier --check on an ignored path matches
# ZERO files and exits 0 -- a vacuous green. It is therefore deliberately NOT run here and NOT
# claimed as coverage. Assert the exclusion instead, so the reasoning stays falsifiable:
grep -qx '.planning' .prettierignore</automated>
  </verify>
  <done>
The todo is closed (or explicitly re-scoped on INCONCLUSIVE) with a `status:` key and a resolution
section naming quick `260925-re8` and the gate's actual verdict. `pnpm planning-gates` passes.
`STATE.md`'s `last_activity` names this quick task and the verdict. `ROADMAP.md` untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tree -> dev-server watcher | chokidar walks `root: '.'`; any large or write-locked generated directory under it is an availability surface for the dev loop |
| scratchpad logs -> repo | gate captures must not be committed; they stay outside the tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-re8-01 | Denial of Service | vite dev-server watcher vs `graphify update .` | mitigate (only if gate RED) | Add `'**/graphify-out/**'` to `server.watch.ignored`; pin with a jest case so removal becomes a deliberate test edit |
| T-re8-02 | Tampering | speculative widening of the ignore list | mitigate | Existing jest cases forbid `src-tauri/**` and restating Vite defaults; Task 2 keeps both passing and declines unobserved entries (`build/`) with a written reason |
| T-re8-03 | Information Disclosure | gate logs in the scratchpad | accept | Logs carry vite/graphify output only, not profile or session data; no sidecar is spawned, so no fake-HOME arm is needed, and logs never enter the repo |
| T-re8-04 | Repudiation | a GREEN gate later mistaken for proof of safety | mitigate | GATE.md and the closed todo must both carry the explicit "does not establish the path is safe" statement |

No package-manager installs in this plan, so no package-legitimacy gate applies.
</threat_model>

<verification>
1. `260925-re8-GATE.md` exists and its `VERDICT:` line matches what the logs actually showed.
2. If GREEN or INCONCLUSIVE: `git diff --stat` shows **no** change to `vite.config.ts` or
   `meta/__tests__/viteRendererConfig.test.ts`.
3. If RED: `npx jest meta/__tests__/viteRendererConfig.test.ts` passes -- including the pre-existing
   non-widening and no-restated-defaults cases -- and GATE.md's re-run section shows GREEN after RED.
4. `npx prettier --check vite.config.ts meta/__tests__/viteRendererConfig.test.ts` (RED branch only;
   nothing else this plan writes sits outside `.prettierignore`).
5. `npx tsc -p tsconfig.meta.json --noEmit` clean.
6. `pnpm planning-gates` passes.
7. Port 5173 is free and no stray vite or graphify process remains.
</verification>

<success_criteria>
- The gate ran against the current committed config and its verdict is recorded with evidence.
- The fix exists if and only if the gate went RED.
- On GREEN, the recorded outcome is a negative finding with its limits stated -- not a silent pass,
  and not a speculative edit.
- `'**/src-tauri/target/**'` is unwidened and no Vite default is restated, in every branch.
- The `build/` question is answered with a reason wherever Task 2 runs.
- The todo lives in `completed/` (or is explicitly re-scoped) carrying the actual outcome.
</success_criteria>

<output>
Create `.planning/quick/260925-re8-vite-watch-ignore-graphify-out/260925-re8-SUMMARY.md` when done.
It must state the verdict, whether Task 2 ran or was skipped and why, and the `build/` decision.
</output>
