---
phase: quick-260924-g7r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/screens/Library/steamLibraryVisibility.ts
  - src/frontend/screens/Library/index.tsx
  - src/frontend/screens/Library/__tests__/steamLibraryVisibility.test.ts
  - src/frontend/screens/Library/__tests__/connectedStoresParity.test.ts
  - .planning/todos/pending/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md
  - .planning/todos/completed/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md
autonomous: true
requirements:
  - T-34.11-12
  - todo/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12

must_haves:
  truths:
    - 'A user with a persisted Steam identity, a `failed` sync, and zero INSTALLED Steam games sees NO Steam row in the Store facet panel (today they see a permanently-0 row).'
    - 'That same user still sees Steam on the Library screen — `SteamSyncNotice` renders in `signedOut`/`failed` mode — so Steam does not silently vanish from the screen.'
    - 'A user with a non-failed sync and an empty Steam library still sees a Steam row, matching every other store.'
    - 'The Store facet gate and the Games grid gate are now the SAME VALUE, not two expressions that happen to read alike.'
    - '`connectedStoresParity.test.ts` still finds five gated stores on both sides (its non-vacuity assertion does not drop to four).'
  artifacts:
    - path: 'src/frontend/screens/Library/steamLibraryVisibility.ts'
      provides: '`resolveSteamVisibility` returning `{ games, storeConnected }`'
      contains: 'export function resolveSteamVisibility'
    - path: 'src/frontend/screens/Library/index.tsx'
      provides: 'one `steamVisibility` memo read by BOTH `connectedStores` and `makeLibrary`'
      contains: 'steamVisibility.storeConnected'
    - path: 'src/frontend/screens/Library/__tests__/steamLibraryVisibility.test.ts'
      provides: 'outcome matrix for `resolveSteamVisibility` + red-proof of the pre-fix predicate'
    - path: 'src/frontend/screens/Library/__tests__/connectedStoresParity.test.ts'
      provides: 'Steam pinned to the resolver-derived value by name at both sites'
  key_links:
    - from: 'src/frontend/screens/Library/index.tsx (connectedStores memo)'
      to: 'steamVisibility.storeConnected'
      via: "if (...) stores.push('steam') — ONE LINE"
      pattern: "if \\(steamVisibility\\.storeConnected\\) stores\\.push\\('steam'\\)"
    - from: 'src/frontend/screens/Library/index.tsx (makeLibrary)'
      to: 'steamVisibility.storeConnected'
      via: 'const showSteam = ... — ONE LINE'
      pattern: 'const showSteam = steamVisibility\\.storeConnected'
    - from: 'resolveSteamVisibility'
      to: 'selectVisibleSteamLibrary'
      via: 'delegation, not reimplementation'
      pattern: 'selectVisibleSteamLibrary\\(input\\)'
---

<objective>
Close the reopened threat T-34.11-12 (Spoofing) for Steam by taking the todo's **option 2**: the
Store facet panel and the Games grid stop deriving Steam's login gate independently and instead read
one resolver output.

Purpose: today `connectedStores` pushes `'steam'` on `steam?.username` alone while the grid runs the
auth-aware `selectVisibleSteamLibrary`. For an expired session with zero installed Steam games those
two disagree, and the panel advertises a Steam row over a grid with no Steam games — the exact
permanently-0 row T-34.11-12 exists to prevent, which already shipped once for Amazon.

Output: `resolveSteamVisibility` in `steamLibraryVisibility.ts`, one `steamVisibility` memo in
`index.tsx` feeding both sites, an OUTCOME-based test matrix with a red-proof, a by-name pin in the
parity gate, and the todo closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/todos/pending/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md
@src/frontend/screens/Library/steamLibraryVisibility.ts
@src/frontend/screens/Library/__tests__/steamLibraryVisibility.test.ts
@src/frontend/screens/Library/__tests__/connectedStoresParity.test.ts

<interfaces>
<!-- Extracted from the codebase. Use these directly; do not go exploring. -->

From `src/frontend/screens/Library/steamLibraryVisibility.ts` (existing, do NOT change):

  export interface SteamVisibilityGame { is_installed: boolean }

  export interface SteamLibraryVisibilityInput<TGame extends SteamVisibilityGame> {
    library: TGame[]
    steamUsername: string | null | undefined
    steamSyncStatus: SteamSyncStatus   // 'idle' | 'syncing' | 'failed'
  }

  export function selectVisibleSteamLibrary<TGame extends SteamVisibilityGame>(
    input: SteamLibraryVisibilityInput<TGame>
  ): TGame[]

From `src/frontend/screens/Library/index.tsx`, line 57 (import to change):

  import { selectVisibleSteamLibrary } from './steamLibraryVisibility'

From `src/frontend/screens/Library/index.tsx`, lines 262-270 (`connectedStores` memo body, VERBATIM):

  const connectedStores: StoreFacetValue[] = useMemo(() => {
    const stores: StoreFacetValue[] = ['sideload']
    if (gog.username) stores.push('gog')
    if (epic.username) stores.push('legendary')
    if (amazon.user_id) stores.push('nile')
    if (zoom.enabled && zoom.username) stores.push('zoom')
    if (steam?.username) stores.push('steam')
    return stores
  }, [gog.username, epic.username, amazon.user_id, zoom.enabled, zoom.username, steam?.username])

From `src/frontend/screens/Library/index.tsx`, lines 636-668 (`makeLibrary`, abridged):

  const makeLibrary = useCallback(() => {
    const showEpic = !!epic.username
    ...
    const showSteam = !!steam?.username
    ...
    const steamLibrary = showSteam
      ? selectVisibleSteamLibrary({ library: steam?.library ?? [], steamUsername: steam?.username, steamSyncStatus })
      : []
    ...
  }, [ ..., steam?.username, steam?.library, steamSyncStatus, sideloadedLibrary ])

From `src/frontend/screens/Library/__tests__/connectedStoresParity.test.ts` (the two readers whose
regexes constrain the source shape — both are LINE-scoped, `.` does not match a newline):

  const pattern = /if\s*\((.+?)\)\s*stores\.push\('([a-z]+)'\)/g      // readConnectedStoreGates
  const pattern = /const\s+show([A-Z][a-zA-Z]*)\s*=\s*(.+)/g          // readMakeLibraryGates
  function normalise(e: string) { return e.replace(/!!/g, '').replace(/\s+/g, '') }

The `useContext(ContextProvider)` destructure at `index.tsx:93-110` already provides `steam` and
`steamSyncStatus`. No new context reads are needed.
</interfaces>

<measured_facts>
<!-- Measured 2026-09-24 during planning. Treat as given; do not re-derive. -->

1. **Baseline is green.** `npx jest --selectProjects Frontend --testPathPattern
   'Library/__tests__/(steamLibraryVisibility|connectedStoresParity)'` → 2 suites, **26 tests**, all
   pass, 0.15s. That is the exact single-file invocation to use; a bare positional path arg after
   `--selectProjects` is IGNORED and silently runs all 172 Frontend suites.

2. **`.planning` is in `.prettierignore` (line: bare `.planning`).** A `npx prettier --check` over
   the todo file therefore exits 0 without checking anything — it is VACUOUS. Task 5 uses the
   `git show :path` content check instead. Do not add a prettier check there and call it coverage.

3. **The `'failed'` session still shows Steam on this screen.** `resolveSteamSyncIndicator`
   (`librarySyncIndicator.ts:96,106`) returns `'signedOut'` on failed+credentialsMissing and
   `'failed'` on failed, and `index.tsx:1185` renders `<SteamSyncNotice>` for any mode but
   `'hidden'`. This is the measured refutation of the todo's UX objection to option 2.

4. **`pnpm lint` TESTS ceiling is 638 with ZERO headroom** (`meta/lintScoped.cjs:59`). One new
   eslint warning in a test file FAILS the gate. SRC ceiling is 1124.
</measured_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `resolveSteamVisibility` on top of the existing selector</name>
  <files>src/frontend/screens/Library/steamLibraryVisibility.ts</files>
  <action>
Add ONE new export. Do not touch `SteamVisibilityGame`, `SteamLibraryVisibilityInput`,
`selectVisibleSteamLibrary`, or the module header — the existing 317-line test file pins them.

Signature:

  export function resolveSteamVisibility&lt;TGame extends SteamVisibilityGame&gt;(
    input: SteamLibraryVisibilityInput&lt;TGame&gt;
  ): { games: TGame[]; storeConnected: boolean }

Body: call `selectVisibleSteamLibrary(input)` once into a local `games` — delegate, never
reimplement or inline its branches. Then `storeConnected` is
`Boolean(input.steamUsername) && (input.steamSyncStatus !== 'failed' || games.length > 0)`.
Return `{ games, storeConnected }`.

Also declare a named return interface (e.g. `SteamVisibility&lt;TGame&gt;`) and export it, so the
`index.tsx` memo and the tests can name the shape.

Document WHY in the module's existing prose density — it is heavily commented and the new export
must match, not undercut, that. The comment must say:

  - `storeConnected` is the **Store-facet half** of threat T-34.11-12 (Spoofing). The panel must not
    advertise a Steam row over a grid with no Steam games.
  - The `'failed'` term is what is NEW. On a provably-failed sync the grid drops the not-installed
    half, so a user with zero INSTALLED Steam games gets a permanently-0 row — the exact shape
    T-34.11-12 exists to prevent, and which already shipped once for Amazon (`connectedStores` read
    `amazon.username` while `makeLibrary` read `amazon.user_id`).
  - On a NON-failed sync the row is kept even when `games` is empty, **deliberately**: that matches
    every other store (a connected account with an empty library shows a 0 row) and such a row is
    not PERMANENTLY 0 — a sync can still fill it.
  - Name the UX consequence and its measured refutation: Steam does not disappear from the Library
    screen when the row goes, because `SteamSyncNotice` renders in `signedOut`/`failed` mode on the
    same screen (`librarySyncIndicator.ts` branches 2-3, rendered at `index.tsx:1185`). The user
    gets a banner naming the real problem instead of a filter row that filters to nothing.

Do NOT place any fenced code block or an `export` of a second predicate; one function, one interface.
  </action>
  <verify>
    <automated>npx prettier --check src/frontend/screens/Library/steamLibraryVisibility.ts</automated>
    <automated>npx jest --selectProjects Frontend --testPathPattern 'Library/__tests__/steamLibraryVisibility' 2>&amp;1 | tail -5   # existing 20 tests still green, untouched</automated>
    <automated>grep -n 'selectVisibleSteamLibrary(input)' src/frontend/screens/Library/steamLibraryVisibility.ts   # proves delegation, not reimplementation</automated>
    <automated>pnpm codecheck</automated>
  </verify>
  <done>`resolveSteamVisibility` is exported, delegates `games` to `selectVisibleSteamLibrary`, computes `storeConnected` with the `'failed'` term, carries the four documented WHYs, and the pre-existing `selectVisibleSteamLibrary` suite is byte-for-byte unchanged and green.</done>
</task>

<task type="auto">
  <name>Task 2: Compute one `steamVisibility` memo in index.tsx and have both sites read it</name>
  <files>src/frontend/screens/Library/index.tsx</files>
  <action>
**a. Import.** Change line 57 to import `resolveSteamVisibility` from `./steamLibraryVisibility`.
Drop `selectVisibleSteamLibrary` from that import ONLY if nothing else in the file uses it —
`grep -c 'selectVisibleSteamLibrary' src/frontend/screens/Library/index.tsx` after the edit must be 0
if you dropped it, or the identifier must still have a live consumer.

**b. New memo.** Insert it ABOVE the `connectedStores` memo (~line 262) and after the
`useContext(ContextProvider)` destructure (ends ~line 110):

  const steamVisibility = useMemo(
    () =&gt; resolveSteamVisibility({
      library: steam?.library ?? [],
      steamUsername: steam?.username,
      steamSyncStatus
    }),
    [steam?.library, steam?.username, steamSyncStatus]
  )

**c. `connectedStores`.** Replace `if (steam?.username) stores.push('steam')` with
`if (steamVisibility.storeConnected) stores.push('steam')` and swap `steam?.username` for
`steamVisibility.storeConnected` in that memo's dependency array.

  ⚠ **THE PUSH MUST STAY ON ONE LINE.** `connectedStoresParity.test.ts`'s `readConnectedStoreGates`
  regex is `/if\s*\((.+?)\)\s*stores\.push\('([a-z]+)'\)/g` and `.` does not match a newline. A
  multi-line form makes `steam` VANISH from the panel-gate map and turns that file's non-vacuity
  assertion (five stores) red. If prettier wants to wrap it, the expression is too long — it is not.

**d. `makeLibrary`.** Replace the Steam pair with:

  const showSteam = steamVisibility.storeConnected
  const steamLibrary = showSteam ? steamVisibility.games : []

  ⚠ `const showSteam = ...` MUST also stay on ONE LINE — `readMakeLibraryGates`'s regex is
  `/const\s+show([A-Z][a-zA-Z]*)\s*=\s*(.+)/g`, equally line-scoped. Keep the `const show&lt;Name&gt; =`
  shape; do not rename or delete the local (the todo's "Do not" section records that deleting
  `showSteam` blinds the parity gate to Steam entirely — measured 2026-09-24).

  Update the `useCallback` dependency array: `steam?.username`, `steam?.library` and
  `steamSyncStatus` are replaced by the single `steamVisibility`. exhaustive-deps must stay
  satisfied — `pnpm lint` is the arbiter, and the SRC ceiling is 1124 with zero padding, so a new
  react-hooks warning fails the gate.

**e. REWRITE the comment block at ~line 648-660** (the one explaining why `showSteam` is retained as
a textual placeholder). It is now FALSE — the parity is no longer textual-only. The replacement must
say: both sites read one resolver output, so `connectedStoresParity.test.ts`'s string match is now a
CONSEQUENCE of a shared value rather than a coincidence, and the OUTCOME invariant itself is proven
in `steamLibraryVisibility.test.ts`. Keep the pointer to the resolver module for the mechanism.

**f. Update the `connectedStores` prose block at ~line 248-259.** It currently names only the Amazon
divergence. Add the Steam one: the panel read `steam?.username` while the grid ran the auth-aware
resolver, so an expired session with zero installed games got a permanently-0 Steam row; it is now
closed by both sites reading `steamVisibility.storeConnected`. Say that the parity gate compares
gate TEXT and so could not see it — which is why the outcome is pinned in the resolver's own suite.
  </action>
  <verify>
    <automated>npx prettier --check src/frontend/screens/Library/index.tsx</automated>
    <automated>grep -c "if (steamVisibility.storeConnected) stores.push('steam')" src/frontend/screens/Library/index.tsx   # must be 1 — one line, not wrapped</automated>
    <automated>grep -c 'const showSteam = steamVisibility.storeConnected' src/frontend/screens/Library/index.tsx   # must be 1</automated>
    <automated>pnpm codecheck</automated>
    <automated>pnpm lint</automated>
  </verify>
  <done>One `steamVisibility` memo exists; `connectedStores` and `makeLibrary` both read `steamVisibility.storeConnected` on single lines; `makeLibrary` deps list `steamVisibility` and no longer lists `steam?.username`/`steam?.library`/`steamSyncStatus`; both prose blocks are rewritten to describe the shared value; tsc and both lint ceilings pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Outcome matrix + red-proof for `resolveSteamVisibility`, and REPAIR the call-site wiring block</name>
  <files>src/frontend/screens/Library/__tests__/steamLibraryVisibility.test.ts</files>
  <behavior>
  - `steamUsername` absent × any sync × any library → `storeConnected === false`, `games === []`
  - `steamUsername` present × `'idle'`/`'syncing'` × any library → `storeConnected === true`, `games` is the whole library (including when it is empty — the deliberate 0-row case)
  - `steamUsername` present × `'failed'` × installed-only or mixed → `storeConnected === true`, `games` is the installed subset
  - `steamUsername` present × `'failed'` × empty or not-installed-only → `storeConnected === false`, `games === []`  ← the defect this closes
  - INVARIANT: for every `'failed'` row where `storeConnected` is true, `games.length > 0`
  </behavior>
  <action>
**⚠ CRITICAL — this task has a mandatory REPAIR half that is easy to miss.** The existing describe
block `'the Games grid call site is wired to this module'` (line ~248, after the
`selectVisibleSteamLibrary` blocks) reads `index.tsx` as source text and **four of its six
assertions go red under Task 2**, because the resolver call moves out of `makeLibrary` and into the
new memo. Measured against the current file, these break:

  1. `expect(makeLibrary).toContain('selectVisibleSteamLibrary(')` — the call is no longer in the region
  2. `expect(librarySource).toContain("import { selectVisibleSteamLibrary } from './steamLibraryVisibility'")` — import renamed
  3. `expect(makeLibrary).toContain('steamSyncStatus')` — moved to the memo
  4. the deps-region check for `'steamSyncStatus,'` — moved to the memo

These stay green and must be KEPT: `expect(makeLibrary).toContain('showSteam')` and
`expect(makeLibrary).not.toMatch(/showSteam\s*\?\s*steam\.library\s*:/)`.

**Repair, do not delete.** Re-point each broken assertion at the new wiring at the SAME strength —
the block exists because "the module is correct" does not prove "the grid calls it", and that
reasoning is unchanged. Use the file's own `functionRegion(librarySource, ...)` helper with the new
start needle `'const steamVisibility = useMemo('` to extract the memo region, then assert the memo
contains `resolveSteamVisibility(`, `steamSyncStatus` and `steam?.library`; assert `librarySource`
contains the new import line; and assert `makeLibrary` contains `steamVisibility.games`. Update the
block's header prose to say the wiring is now memo→two-consumers rather than a direct call inside
`makeLibrary`. `librarySource` is comment-stripped via `stripSourceComments`, so prose edits from
Task 2 cannot affect these matches.

**Do NOT weaken or edit the `selectVisibleSteamLibrary` describe blocks** (the full state matrix, the
reported-symptom block, and the saboteur block, lines 44-233). They stay byte-identical.

**New work — add a `resolveSteamVisibility` describe block** covering the full matrix:
(steamUsername absent | present) × (`'idle'` | `'syncing'` | `'failed'`) × (library empty |
installed-only | not-installed-only | mixed). Assert BOTH `games` and `storeConnected` on every row.
Reuse the existing `INSTALLED_A`/`OWNED_A`/`LIBRARY` fixtures and the `names()` helper; add
installed-only and not-installed-only arrays. Name each row for the user-visible situation, matching
the existing file's convention, not for its input tuple.

**State the load-bearing assertion as its OWN named test** so it cannot be lost inside a table:
*a `'failed'` session with `storeConnected === true` always has non-empty `games`* — i.e. the facet
row is never offered over an empty grid on an expired session. Iterate the `'failed'` rows and
assert the implication.

**Add a RED-PROOF describe.** State in prose that the pre-fix panel predicate was
`Boolean(steamUsername)` ALONE, express it as a local function, and assert that for the
failed + zero-installed fixture it returns `true` while `resolveSteamVisibility(...).games` is `[]` —
so the fixture demonstrably convicts the shipped shape. Also assert the new predicate returns `false`
for that same fixture, so the proof is discriminating rather than always-failing (the file's existing
saboteur block is the idiom to copy).

Watch the lint TESTS ceiling: 638 with zero headroom. One new warning fails `pnpm lint`.
  </action>
  <verify>
    <automated>npx prettier --check src/frontend/screens/Library/__tests__/steamLibraryVisibility.test.ts</automated>
    <automated>npx jest --selectProjects Frontend --testPathPattern 'Library/__tests__/steamLibraryVisibility' 2>&amp;1 | tail -6</automated>
    <automated>npx jest --selectProjects Frontend --testPathPattern 'Library/__tests__/steamLibraryVisibility' -t 'failed' 2>&amp;1 | tail -6   # the failed-session rows actually ran; must NOT report 0 matched</automated>
    <automated>pnpm lint</automated>
  </verify>
  <done>The four assertions broken by Task 2 are repaired against the new wiring (not deleted, not weakened); the `selectVisibleSteamLibrary` blocks are unchanged; a full `resolveSteamVisibility` matrix asserts both fields per row; the failed+connected⇒non-empty invariant is its own named test; the red-proof shows `Boolean(steamUsername)` alone would have returned true over an empty grid; suite green; lint green.</done>
</task>

<task type="auto">
  <name>Task 4: Pin Steam to the resolver-derived value by name in the parity gate</name>
  <files>src/frontend/screens/Library/__tests__/connectedStoresParity.test.ts</files>
  <action>
The generic parity assertions keep working unchanged — after Task 2 both sites carry the literal text
`steamVisibility.storeConnected`, which `normalise()` leaves identical on both sides. Do not modify
`normalise`, `readConnectedStoreGates`, `readMakeLibraryGates`, `parityMismatches`, `UNGATED_STORES`,
`SHOW_LOCAL_TO_STORE`, the non-vacuity test, the `agrees on every gated store` test, or the existing
nile red-proof.

**Add ONE pinning test**, in the same shape and for the same reason as the existing
`'reads Amazon as user_id at BOTH sites, not username'` test: assert
`readConnectedStoreGates(source)['steam']` and `readMakeLibraryGates(source)['steam']` are BOTH
exactly `'steamVisibility.storeConnected'`. Its justification comment must say what "they match"
alone would not catch: a regression of BOTH sites back to a bare `steam?.username` read would still
satisfy the generic parity assertion while reopening the defect, because the two would still agree
textually. Pinning the value by name is what forecloses that direction.

**Extend the file's header prose** to record the SECOND failure mode this file has now seen. It
currently documents only the Amazon case (two gate expressions that differed). The new one:
*a matching PAIR of gate expressions can still be wrong when one site applies an extra filter the
text comparison cannot see.* That was the state after `debug/steam-library-shows-logged-out`:
`showSteam = !!steam?.username` matched the panel's `steam?.username` exactly, while the grid's real
decision was `showSteam && selectVisibleSteamLibrary(...)` — and the second term is invisible to a
string compare, so the gate was green while the invariant it names had got weaker. Say plainly that
this is why the Steam gate is now pinned to the resolver-derived value BY NAME, and that the outcome
half of the invariant lives in `steamLibraryVisibility.test.ts` because this file reads text and
cannot evaluate anything (no jsdom in any project here).
  </action>
  <verify>
    <automated>npx prettier --check src/frontend/screens/Library/__tests__/connectedStoresParity.test.ts</automated>
    <automated>npx jest --selectProjects Frontend --testPathPattern 'Library/__tests__/connectedStoresParity' 2>&amp;1 | tail -6</automated>
    <automated>npx jest --selectProjects Frontend --testPathPattern 'Library/__tests__/(steamLibraryVisibility|connectedStoresParity)' 2>&amp;1 | tail -6   # both suites; test count must EXCEED the 26 baseline measured 2026-09-24</automated>
    <automated>pnpm lint</automated>
  </verify>
  <done>Steam is pinned to `steamVisibility.storeConnected` at both sites by name; the non-vacuity assertion still lists all five gated stores; the header records the extra-filter failure mode; both suites green with more than the 26 baseline tests.</done>
</task>

<task type="auto">
  <name>Task 5: Close the todo — edit first, stage, then `git mv`</name>
  <files>.planning/todos/pending/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md, .planning/todos/completed/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md</files>
  <action>
**⚠ `git mv` TRAP — this repo has hit it three times.** `git mv` commits the HEAD content of the
file and silently drops unstaged edits. Order is mandatory:

  1. EDIT the file in place at its `pending/` path (append the resolution, flip `status: OPEN` to
     `status: RESOLVED`).
  2. `git add` the pending path.
  3. `git mv` it to `.planning/todos/completed/`.
  4. BEFORE committing, verify the staged content is the NEW content:
     `git show :.planning/todos/completed/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md | grep -c 'Option 2 taken'`
     — must be non-zero. If it is 0, the edits were dropped; redo from step 1.

Do NOT add a `npx prettier --check` for this file: `.prettierignore` contains a bare `.planning`
entry, so such a check exits 0 without reading the file. Measured 2026-09-24. The `git show` check
above is the real verification.

The appended `## Resolution` section must record, honestly and without softening:

  - **Option 2 taken.** Both sites now read one `resolveSteamVisibility` output. Name the files.
  - **Option 3 deliberately NOT taken, as a scope decision — not a deferral to a future todo.**
    Evaluating all five stores' gates over a fixture matrix requires extracting `connectedStores`
    and all five `show*` gates out of `index.tsx` into a pure module: a refactor of every store's
    login gate, in a file with no jsdom coverage, disproportionate to one store's divergence. Say
    that plainly; do not write "follow-up" or file a successor.
  - **The todo's own UX objection to option 2 is FALSE, and that is the measured reason option 2 was
    safe to build.** The todo says "Steam vanishes from the facet panel on an expired session, which
    may be worse UX than an empty row — the Manage Accounts tile is the only other place the user
    would see Steam at all." That is wrong: `SteamSyncNotice` renders on the SAME Library screen in
    exactly this state — `resolveSteamSyncIndicator` returns `'signedOut'` (failed +
    credentialsMissing) or `'failed'` (failed) whenever `steam?.username` is truthy
    (`librarySyncIndicator.ts:96,106`), and `index.tsx:1185` renders it for any mode but `'hidden'`.
    Steam does not vanish from the screen when the row goes; the user gets a banner naming the real
    problem instead of a filter row that filters to nothing.
  - **The "live-gate the parent fix first" prerequisite is ALREADY SATISFIED** — the parent session
    `.planning/debug/resolved/steam-library-shows-logged-out.md:220` records
    `## Live gate — PASSED, 2026-09-24`.
  - **What was NOT live-gated by THIS change.** Nobody has observed the expired-session-plus-
    zero-installed state on a real profile — the operator's live gate ran on a profile WITH
    installed Steam games. This change is desk-verified by unit test only. Record that as written;
    do not imply a live observation.
  - The parity gate's second failure mode, one line: a matching pair of gate expressions can still
    be wrong when one site applies an extra filter a text compare cannot see; the outcome invariant
    now lives in `steamLibraryVisibility.test.ts`.

Keep the frontmatter triage keys intact and bare/lowercase (`severity: medium`, `platform: any`,
`ready: code`) — `pnpm planning-gates` checks `pending/` only, but leave them valid regardless.
Leave no stray closing envelope tag (`</output>` etc.) in the body; that is separately gated.
  </action>
  <verify>
    <automated>test ! -f .planning/todos/pending/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md &amp;&amp; test -f .planning/todos/completed/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md</automated>
    <automated>git show :.planning/todos/completed/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md | grep -c 'Option 2 taken'   # must be non-zero — proves git mv did not drop the edits</automated>
    <automated>git show :.planning/todos/completed/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md | grep -c 'SteamSyncNotice'   # the UX refutation survived the move</automated>
    <automated>pnpm planning-gates</automated>
  </verify>
  <done>The todo lives in `completed/` with `status: RESOLVED`; the staged content — verified via `git show :path`, not the working tree — carries the option-2 decision, the option-3 scope refusal, the measured refutation of the UX objection, the satisfied live-gate prerequisite, and the honest statement that the new state was never observed live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted config → UI authorisation | `steam.username` is a boot-seeded persisted value that survives an expired session; using it as a live-session gate is the root of this defect class |
| Store facet panel ↔ Games grid | two independent derivations of "is this store connected" that must not disagree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.11-12 | Spoofing | `connectedStores` memo + `makeLibrary` in `Library/index.tsx` | mitigate | Both sites read one `steamVisibility.storeConnected`; the value, not just the expression text, is shared. Pinned by name in `connectedStoresParity.test.ts` and proven by outcome in `steamLibraryVisibility.test.ts`. |
| T-34.11-12b | Spoofing | `readConnectedStoreGates` / `readMakeLibraryGates` text comparison | accept | A source-text gate cannot evaluate predicates — no jsdom in any jest project here. Accepted and named in the file's header; the OUTCOME half is carried by the resolver's own node-environment suite. |
| T-34.11-12c | Information disclosure | Steam row disappearing on an expired session | accept | `SteamSyncNotice` renders `signedOut`/`failed` on the same screen (measured), so the state stays legible to the user rather than becoming silent. |

No package-manager installs in this plan, so no legitimacy gate applies.
</threat_model>

<verification>
Run from the repo root after all five tasks:

1. `pnpm codecheck` — tsc clean.
2. `pnpm lint` — BOTH ceilings pass (SRC 1124, TESTS 638, zero headroom on each).
3. `npx jest --selectProjects Frontend --testPathPattern 'Library/__tests__/(steamLibraryVisibility|connectedStoresParity)'` — both suites green, test count strictly greater than the 26 measured on 2026-09-24.
4. `npx jest --selectProjects Frontend` — no collateral damage to the other 170 Frontend suites.
5. `npx prettier --check src/frontend/screens/Library/steamLibraryVisibility.ts src/frontend/screens/Library/index.tsx src/frontend/screens/Library/__tests__/steamLibraryVisibility.test.ts src/frontend/screens/Library/__tests__/connectedStoresParity.test.ts`
6. `pnpm planning-gates`.
7. `git show :.planning/todos/completed/2026-09-24-steam-facet-row-can-outlive-its-grid-games-reopening-t-34-11-12.md | grep -c 'Option 2 taken'` — non-zero.

**Deliberate non-verification, to be stated in the summary rather than glossed:** the
expired-session-plus-zero-installed state has never been observed on a real profile. No live gate is
claimed for this change.
</verification>

<success_criteria>
- `resolveSteamVisibility` exists, delegates `games` to the untouched `selectVisibleSteamLibrary`, and gates `storeConnected` on `Boolean(steamUsername) && (syncStatus !== 'failed' || games.length > 0)`.
- `connectedStores` and `makeLibrary` read the SAME memo value, each on a single line that both parity regexes can still parse.
- `connectedStoresParity.test.ts` still reports five gated stores on both sides, and pins `steam` to `steamVisibility.storeConnected` by name.
- The `'failed' && storeConnected ⇒ games.length > 0` invariant is asserted as its own named test, with a red-proof showing the pre-fix predicate would have passed the failed+zero-installed fixture.
- The call-site wiring describe block in `steamLibraryVisibility.test.ts` is repaired to the new wiring at equal strength, and the `selectVisibleSteamLibrary` blocks are unchanged.
- Every written file passes an explicit-path `npx prettier --check`.
- The todo is in `completed/`, and its STAGED content (verified by `git show :path`) carries the resolution.
</success_criteria>

<output>
Create `.planning/quick/260924-g7r-share-one-steam-visibility-predicate-bet/260924-g7r-SUMMARY.md` when done.
</output>
</content>
</invoke>
