---
phase: quick-260817-sw8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-08-16-eight-minute-install-watchdog-makes-long-native-steam-instal.md
  - .planning/debug/humankind-depot-full-stall.md
autonomous: true
requirements: [SW8-01, SW8-02]

must_haves:
  truths:
    - "The 8-min-watchdog todo records the 2026-08-17 Age of Wonders: Planetfall live run as evidence that Gate A's PROPERTY is demonstrated generically."
    - "That same todo still states explicitly that it stays in pending/ because the canonical HUMANKIND run has not happened."
    - "The humankind-depot-full-stall debug session carries a new Evidence entry recording pool[inline=false] engaging live in a real end-to-end install."
    - "That Evidence entry names Age of Wonders: Planetfall (718850) and explicitly says this is NOT HUMANKIND (1124300)."
    - "Neither file's frontmatter changed; the debug session's status stays awaiting_human_verify and its next_action stays awaiting the real HUMANKIND run."
    - "Both edits are pure appends — no existing line in either file is deleted or reworded."
  artifacts:
    - path: ".planning/todos/pending/2026-08-16-eight-minute-install-watchdog-makes-long-native-steam-instal.md"
      provides: "New '### Live evidence — 2026-08-17 (Age of Wonders: Planetfall)' subsection under ## Resolution"
      contains: "Live evidence — 2026-08-17"
    - path: ".planning/debug/humankind-depot-full-stall.md"
      provides: "New timestamped Evidence entry for the 718850 run"
      contains: "718850"
  key_links:
    - from: ".planning/todos/pending/2026-08-16-eight-minute-install-watchdog-makes-long-native-steam-instal.md"
      to: ".planning/quick/260817-dib-make-the-install-watchdog-a-progress-bas/LIVE-GATE.md"
      via: "explicit citation of the gate's HUMANKIND-specific preconditions as the reason the todo stays pending"
      pattern: "LIVE-GATE"
    - from: ".planning/debug/humankind-depot-full-stall.md"
      to: "Current Focus reasoning_checkpoint P/E-core hypothesis"
      via: "the new entry's implication field, framed as supporting data for an already-open hypothesis"
      pattern: "P/E-core"
---

<objective>
Record a single live-hardware install result as evidence in two existing planning documents.

Age of Wonders: Planetfall (appId `718850`, Mac-native, ~17.15 GB) was freshly uninstalled and
reinstalled through GameLib's native Steam depot path on 2026-08-17, on a build carrying BOTH
just-landed fixes: the `TOP_N_FANOUT`/InflightLimiter throughput fix (quick `260817-ihr`) and the
SEA-sidecar decompress-worker packaging fix (quick `260817-pkx`). The run completed in 1029s
(17m09s), never tripped the install watchdog, wrote a clean `StateFlags="4"` manifest, and — for
the first time in a real end-to-end install rather than an isolated self-test — showed the decode
worker pool actually engaging (`inline=false`).

Purpose: this evidence is load-bearing for two open items, and it is only in a log file right now.
It is generically strong (it demonstrates the property both documents care about) but it is NOT
the canonical run either document is pinned to, because it is not HUMANKIND. Recording it with
that distinction stated explicitly prevents a future reader from closing either item on it.

Output: one appended subsection in the watchdog todo, one appended entry in the debug session's
Evidence list. No source code changes. No file moves. No frontmatter changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-16-eight-minute-install-watchdog-makes-long-native-steam-instal.md
@.planning/debug/humankind-depot-full-stall.md

Reference only — do NOT modify:
- `.planning/quick/260817-dib-make-the-install-watchdog-a-progress-bas/LIVE-GATE.md` — Gate A is
  written entirely against appId `1124300`; its precondition 4 binds the run to phase `23-10`
  Task 1's fresh-install precondition, which is why a non-HUMANKIND run cannot close it.
- `.planning/phases/23-steam-full-ownership-install-stateflags-4/23-10-PLAN.md` — Task 1 is the
  other item pinned to a HUMANKIND-specific run.

<facts_to_record>
These are the measured facts. Use them; do not embellish, do not round differently, do not
add throughput claims beyond what is written here.

- Title: Age of Wonders: Planetfall, appId `718850`, Mac-native, ~17.15 GB.
- Date: 2026-08-17. Fresh uninstall then reinstall via GameLib's native Steam depot path.
- Build under test carried both `260817-ihr` (TOP_N_FANOUT / InflightLimiter throughput) and
  `260817-pkx` (SEA-sidecar decompress-worker embedded as a Node SEA asset).
- Source of all readings: `~/Library/Logs/GameLib/gamelib.log`.
- Total duration: 1029s (17m09s) — well past the old 8-minute (480,000ms) ceiling.
- Proof by absence, both greps returned EMPTY:
  `grep -n "Installation of 718850 failed with:"` and
  `grep -n "Aborting in-flight download for 718850"`.
  The watchdog never tripped.
- `.acf` written cleanly: `StateFlags="4"`, `SizeOnDisk="17151298416"` — full-ownership
  manifest, no manual chmod needed.
- `pool[...inline=false]` on every `[Timing] chunk-stream stats` sample from `@15s` through
  completion — decode workers really spawning via worker_threads inside the packaged SEA
  sidecar binary. First real end-to-end confirmation, versus `260817-pkx`'s isolated
  `GAMELIB_SIDECAR_SELFTEST=decompress-pool` self-test.
- New observation: `avgDecodeMs` climbed steadily across the run (1287ms -> 1738ms -> 2692ms)
  while `avgNetMs` stayed flat (~250-283ms). Decode is the dominant cost by the end (~9.5x
  net). The decode queue backed up early (`queued=51` at `@15s`).
- Rough throughput: ~60s/GB. Extrapolated to HUMANKIND's ~37 GB that is roughly ~37 minutes if
  the rate holds, versus the pre-fix ~93-minute projection.
</facts_to_record>

<what_this_does_not_close>
State this explicitly in BOTH files. This run is Age of Wonders: Planetfall (`718850`), NOT
HUMANKIND (`1124300`). Two items stay pinned to a real HUMANKIND run and are NOT closed:

1. Phase 23's `23-10` Task 1 — Gate 2 must re-run CLEAN on HUMANKIND specifically.
2. The `humankind-depot-full-stall` session's own `next_action` — "awaiting human verification:
   a real end-to-end HUMANKIND (or similarly large) native Steam install re-run to confirm the
   original user-facing symptom is resolved."
</what_this_does_not_close>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the live-evidence subsection to the 8-min-watchdog todo</name>
  <files>.planning/todos/pending/2026-08-16-eight-minute-install-watchdog-makes-long-native-steam-instal.md</files>
  <action>
Append a new `### Live evidence — 2026-08-17 (Age of Wonders: Planetfall)` subsection to the END
of the `## Resolution` section — that is, insert it immediately BEFORE the `## Solution (chosen:
option 1)` heading, so it reads as the last thing `## Resolution` says.

Do NOT touch the frontmatter (lines 1-11). Do NOT move the file out of `pending/`. Do NOT reword
or delete any existing line, including the existing paragraph that ends "...which the operator
runs as part of phase 23 wave 10." The new subsection sits after that paragraph and after the
"What shipped" bullet list and its trailing verification paragraph.

Content of the new subsection, in the file's existing prose-plus-fenced-log style:

- One opening sentence naming the run: Age of Wonders: Planetfall, appId `718850`, Mac-native,
  ~17.15 GB, freshly uninstalled and reinstalled 2026-08-17 on a build carrying both
  `260817-ihr` and `260817-pkx`, read from `~/Library/Logs/GameLib/gamelib.log`.
- The measured facts from `<facts_to_record>`: 1029s / 17m09s total, the two EMPTY proof-by-
  absence greps (show them as a fenced `bash` block with the two `grep -n` commands and note
  both returned no output), the clean `StateFlags="4"` / `SizeOnDisk="17151298416"` manifest,
  `pool[...inline=false]` on every stats sample `@15s` through completion, the
  `avgDecodeMs` 1287 -> 1738 -> 2692ms climb against flat `avgNetMs` ~250-283ms with
  `queued=51` at `@15s`, and the ~60s/GB rough throughput with its ~37min HUMANKIND
  extrapolation versus the pre-fix ~93min projection.
- A short paragraph stating the interpretation directly: **Gate A's PROPERTY is now demonstrated
  generically** — a Mac-native install survived well past 8 minutes without tripping, reached
  100%, and wrote a clean `StateFlags=4` manifest. The re-semantified no-progress watchdog
  behaved exactly as `260817-dib` intended on real hardware.
- A closing paragraph, equally direct, stating the limit: this **does not** move the todo to
  `completed/`. It stays in `pending/`. `LIVE-GATE.md`'s Gate A is written entirely against
  appId `1124300`, and its precondition 4 binds that run to phase `23-10` Task 1's fresh-install
  precondition — so only the canonical HUMANKIND run closes this, and it has not happened yet.
  Name both still-open items from `<what_this_does_not_close>`.

Write it so a reader who lands on the file cold cannot mistake this for closure. Do not soften
the limit paragraph with hedging like "essentially closed" or "effectively verified".
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib &amp;&amp; F=.planning/todos/pending/2026-08-16-eight-minute-install-watchdog-makes-long-native-steam-instal.md; test -f "$F" &amp;&amp; grep -q 'Live evidence — 2026-08-17' "$F" &amp;&amp; grep -q '718850' "$F" &amp;&amp; grep -q '1029' "$F" &amp;&amp; grep -q 'StateFlags="4"' "$F" &amp;&amp; grep -q 'inline=false' "$F" &amp;&amp; grep -q '1124300' "$F" &amp;&amp; grep -q '23-10' "$F" &amp;&amp; test "$(git diff --unified=0 -- "$F" | grep -c '^-[^-]')" = "0" &amp;&amp; echo TASK1_OK</automated>
  </verify>
  <done>
The todo contains a new `### Live evidence — 2026-08-17 (Age of Wonders: Planetfall)` subsection
at the end of `## Resolution` carrying every fact from `<facts_to_record>`, an explicit statement
that Gate A's property is demonstrated generically, and an explicit statement that the todo stays
in `pending/` pending the HUMANKIND-specific run. `git diff` shows zero deleted lines — the edit
is a pure append. Frontmatter byte-identical. File still in `pending/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append the supporting Evidence entry to the humankind-depot-full-stall debug session</name>
  <files>.planning/debug/humankind-depot-full-stall.md</files>
  <action>
Append one new entry to the `## Evidence` section. Insert it as the FIRST entry, immediately after
the `## Evidence` heading — that is where this session's most recent block (the `20:10` / `20:25` /
`20:28` / `20:30` entries) already sits, so a newest-first insert matches the file's actual
convention rather than its nominal ordering.

Match the existing entry shape EXACTLY — four keys, same indentation, same `- timestamp:` list
form:

```
- timestamp: <TS>
  checked: "..."
  found: "..."
  implication: "..."
```

For `<TS>`, run `date "+%Y-%m-%dT%H:%M"` and use the result unquoted, in the same bare form as
the siblings (e.g. `2026-08-17T20:30`). It must sort after the existing `20:30` entry; if the
clock reports earlier than that, use `2026-08-17T21:00`.

Field contents:

- `checked:` — the 2026-08-17 live end-to-end install of **Age of Wonders: Planetfall (appId
  `718850`)**, a ~17.15 GB Mac-native title, freshly uninstalled and reinstalled through
  GameLib's native Steam depot path on a build carrying BOTH `260817-ihr` (TOP_N_FANOUT /
  InflightLimiter) and `260817-pkx` (decompressWorker.js embedded as a Node SEA asset). Read
  from `~/Library/Logs/GameLib/gamelib.log`. State up front, in this field, that this is
  **NOT HUMANKIND (`1124300`)**.
- `found:` — the measurements: 1029s (17m09s) to completion; `pool[...inline=false]` on every
  `[Timing] chunk-stream stats` sample from `@15s` through completion; `queued=51` at `@15s`;
  `avgDecodeMs` climbing 1287 -> 1738 -> 2692ms while `avgNetMs` stayed flat at ~250-283ms
  (decode ~9.5x net by the end); both watchdog greps
  (`Installation of 718850 failed with:` and `Aborting in-flight download for 718850`) EMPTY;
  `.acf` written clean with `StateFlags="4"` and `SizeOnDisk="17151298416"`; rough ~60s/GB.
- `implication:` — three points, stated plainly:
  1. This is the FIRST confirmation of the decode pool engaging in a real end-to-end install
     rather than `260817-pkx`'s isolated `GAMELIB_SIDECAR_SELFTEST=decompress-pool` self-test.
     `inline=false` on every sample directly inverts the `inline=true` signature this session
     recorded on all 5 HUMANKIND runs (Evidence `2026-08-17T20:25`), so the SEA companion-file
     root cause confirmed at `2026-08-17T20:28` is fixed on the shipped binary.
  2. The `avgDecodeMs`-climbs-while-`avgNetMs`-stays-flat split is a NEW data point for the
     already-open P/E-core saturation hypothesis (`blind_spots` item 1, and Evidence
     `2026-08-17T20:10`'s mechanism (a): decode-bound would show `avgDecodeMs` high with
     `avgNetMs` flat). With the pool now genuinely spawning, that hypothesis is live again.
     This is NOT a new defect — it is supporting data for an existing open question about
     `DECOMPRESS_POOL_MAX_WORKERS`'s cap on a 4P+6E chip.
  3. Scope limit, stated explicitly: because this is `718850` and not `1124300`, this entry does
     NOT satisfy `next_action`'s awaited HUMANKIND re-run, and does NOT satisfy phase `23-10`
     Task 1. It is supporting evidence only, not a resolution. The ~60s/GB rate extrapolates to
     roughly ~37 minutes for HUMANKIND's ~37 GB versus the pre-fix ~93-minute projection, but
     that is an extrapolation, not a measurement of HUMANKIND.

Hard constraints. Do NOT change the frontmatter at all — `status` stays `awaiting_human_verify`,
`updated:` stays as-is, and the `symptoms`/`CORRECTION_*` block is untouched. Do NOT change
`next_action`, `hypothesis`, `test`, `expecting`, the `## Current Focus` block, `## Resolution`,
or `files_changed`. Do NOT reword or delete any existing Evidence entry. The ONLY change to this
file is the insertion of the new entry.

Keep the entry's YAML-ish values valid for the file's style: wrap each field's value in double
quotes and escape any interior double quotes as `\"` (the `StateFlags=\"4\"` and
`SizeOnDisk=\"17151298416\"` readings need this), or use the file's existing `>` block-scalar
form as the `2026-08-17T19:20` entry does. Do not leave a bare unescaped `"` inside a
double-quoted value.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib &amp;&amp; F=.planning/debug/humankind-depot-full-stall.md; grep -q '718850' "$F" &amp;&amp; grep -q 'inline=false' "$F" &amp;&amp; grep -q 'Planetfall' "$F" &amp;&amp; grep -q '^status: awaiting_human_verify$' "$F" &amp;&amp; test "$(git diff --unified=0 -- "$F" | grep -c '^-[^-]')" = "0" &amp;&amp; test "$(git diff -- "$F" | grep -c '^+- timestamp:')" = "1" &amp;&amp; echo TASK2_OK</automated>
  </verify>
  <done>
`## Evidence` carries exactly one new `- timestamp:/checked:/found:/implication:` entry matching
the existing format, naming Age of Wonders: Planetfall (`718850`) and explicitly disclaiming that
it is HUMANKIND (`1124300`). `git diff` shows zero deleted lines and exactly one added
`- timestamp:` line. Frontmatter `status` still `awaiting_human_verify`; `next_action` unchanged.
  </done>
</task>

</tasks>

<verification>
Run from the repo root after both tasks:

1. Append-only across both files — no existing line was deleted or reworded:
   ```bash
   git diff --unified=0 -- \
     .planning/todos/pending/2026-08-16-eight-minute-install-watchdog-makes-long-native-steam-instal.md \
     .planning/debug/humankind-depot-full-stall.md | grep -c '^-[^-]'
   ```
   MUST print `0`.

2. Debug session frontmatter untouched — `status` and `next_action` are both unchanged:
   ```bash
   git diff -- .planning/debug/humankind-depot-full-stall.md | grep -c '^[+-]next_action:'
   ```
   MUST print `0`, and `grep -c '^status: awaiting_human_verify$'` on the file MUST print `1`.

3. The todo did not move — it is still under `.planning/todos/pending/` and absent from
   `.planning/todos/completed/`:
   ```bash
   ls .planning/todos/completed/ | grep -c 'eight-minute-install-watchdog'
   ```
   MUST print `0`.

4. Both files still say the HUMANKIND run is outstanding. Each file MUST contain `1124300`, and
   the todo MUST still contain the string `pending/`.

5. No source files were touched:
   ```bash
   git status --porcelain -- src/ | wc -l
   ```
   MUST print `0` for files this plan introduced (pre-existing modifications to
   `src/backend/storeManagers/steam/**` from the concurrent branch state are NOT this plan's
   and must be left exactly as they were — do not stage, stash, revert, or commit them).
</verification>

<success_criteria>
- The 8-min-watchdog todo carries a `### Live evidence — 2026-08-17 (Age of Wonders: Planetfall)`
  subsection at the end of `## Resolution` with every fact from `<facts_to_record>`.
- That subsection states both that Gate A's property is demonstrated generically AND that the todo
  stays in `pending/` because the HUMANKIND-specific run (LIVE-GATE precondition 4 / phase `23-10`
  Task 1) has not happened.
- The debug session carries exactly one new Evidence entry, in the file's four-key format, naming
  `718850` and explicitly disclaiming HUMANKIND (`1124300`).
- The debug session's `status` is still `awaiting_human_verify` and `next_action` is byte-identical.
- Both edits are pure appends: `git diff | grep -c '^-[^-]'` is `0` across both files.
- Zero source code changes. Zero file moves.
</success_criteria>

<output>
Create `.planning/quick/260817-sw8-record-age-of-wonders-planetfall-718850-/260817-sw8-SUMMARY.md` when done.
</output>
</content>
</invoke>
