---
phase: quick-260907-e7a
plan: '01'
type: execute
wave: 1
depends_on: []
subsystem: steam
autonomous: true
requirements: [QUICK-E7A-01]
files_modified:
  # Task 1 modifies NO production code.
  # Task 2's file set is BRANCH-DEPENDENT — exactly one of these two shapes:
  #   Branch REACHABLE (a real gap): src/backend/storeManagers/steam/games.ts
  #     (new private helper + both bypass sites) + steam/__tests__/games.test.ts
  #   Branch NOT-REACHABLE (documented non-gap): src/backend/storeManagers/steam/games.ts
  #     (source comments ONLY, at both bypass sites) — no test file change
  - src/backend/storeManagers/steam/games.ts
  - src/backend/storeManagers/steam/__tests__/games.test.ts
  - .planning/todos/pending/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md
  - .planning/todos/completed/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md
user_setup: []

must_haves:
  truths:
    - "Every backend `getGameInfo()` hit in the census is dispositioned into exactly one class, and the class counts sum to the census total."
    - "Every hit in the three non-title classes (guarded / relies-on-sentinel / genuinely unguarded) is listed individually with a file:line."
    - "The raw-Map bypass at uninstall() and uninstallBottleGameDirectly() has a REACHABILITY VERDICT backed by file:line evidence, not an assertion."
    - "If the bypass is not reachable, both bypass sites carry a source comment saying so, so the next reader does not re-open the question."
    - "The todo file lives in .planning/todos/completed/ with status RESOLVED and is absent from .planning/todos/pending/."
    - "The closure records the D-01 sentinel decision, luf's disproof of the todo's own stated symptom, the verdict table, and task 2's decision."
  artifacts:
    - path: ".planning/todos/completed/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md"
      provides: "The closed todo with status RESOLVED, resolution section, and inline verdict table"
      contains: "status: RESOLVED"
    - path: ".planning/quick/260907-e7a-audit-and-close-the-non-title-axis-of-th/260907-e7a-SUMMARY.md"
      provides: "Census command + total, full disposition table, reachability verdict, RED-proof ledger (or explicit 'no production change' statement)"
    - path: "src/backend/storeManagers/steam/games.ts"
      provides: "Either the side-effect-free shared read helper applied at both bypass sites, OR the documented-non-gap comments at both sites"
  key_links:
    - from: ".planning/todos/completed/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md"
      to: "quick task 260907-e7a"
      via: "resolved_by frontmatter field"
      pattern: "resolved_by:.*260907-e7a"
    - from: "src/backend/storeManagers/steam/games.ts"
      to: "the uninstall()/uninstallBottleGameDirectly() bypass rationale"
      via: "source comment at both sites naming this quick task"
      pattern: "260907-e7a"
---

<objective>
Close the NON-TITLE axis of the Steam `getGameInfo()` double-cache-miss todo — the one section
(`## Open question research left unanswered`) that quick tasks `260905-luf` and `260905-mv5` left
open — then close the todo itself.

Purpose: the todo asks whether the double cache miss causes symptoms BEYOND the empty title. luf
and mv5 answered the title axis and locked D-01 (the `{}` return is a deliberate cross-runner
sentinel, not a defect). Nobody has censused the non-title fields. This plan produces that census
with a per-site verdict, decides the one genuinely open design question (the raw-Map bypass at the
two uninstall sites), and retires the ledger entry.

Output: a per-site verdict table, a reachability verdict with file:line evidence for the bypass,
either a RED-proven fix or a documented non-gap, and the todo moved to `.planning/todos/completed/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md
@.planning/quick/260905-luf-steamgame-getgameinfo-returns-on-a-doubl/260905-luf-SUMMARY.md
@.planning/quick/260905-mv5-fix-the-4-sibling-unguarded-getgameinfo-/260905-mv5-SUMMARY.md
@src/backend/storeManagers/steam/games.ts
@src/backend/storeManagers/steam/library.ts
@src/backend/storeManagers/steam/state.ts
</context>

<locked_decisions>
These are LOCKED. Do not re-derive them, do not re-litigate them, do not widen past them.

**D-01 (from 260905-luf, CONFIRMED):** `SteamGame.getGameInfo()` returning `{} as GameInfo` on a
double cache miss is a DELIBERATE cross-runner sentinel. Three consumers depend on its falsiness:
`gamedetails/dispatch.ts:82` (converts `{}` → `null` for the frontend),
`sidecar/appShellFlowRegistration.ts:536` (tray runner resolution skips a runner on falsy
`app_name`), `storeManagers/steam/library.ts:1257` (its own persisted-cache fallback relies on the
falsy check).

**OUT OF BOUNDS — do not do these, in any task:**
- Populating a stub `GameInfo` in the double-miss branch of `getGameInfo()`. Rejected by D-01.
- Making `getGameInfo()` async. ~40 call sites across ~20 files; luf scoped this out as needing its
  own phase. If this plan's census concludes async is the only real fix for something, RECORD that
  conclusion and leave it — do not start it.
- Re-touching the TITLE axis. luf + mv5 closed it (`resolveGameTitle`/`resolveTitleForGame` in
  `src/backend/utils/gameTitle.ts`, plus D-03's return-false guard on both `shortcutsExists`
  handlers). Title-axis hits are dispositioned in the census by CITING those summaries, not by
  re-analysing them.

**A correction this plan must carry forward, not repeat:** luf DISPROVED the todo's own title. The
nameless surface was the OS notification from `downloadqueue.ts`'s `processNotification`, NOT the
install-failure dialog the todo names — `resolveQueueElementTitle` already had `title || appName`
and its control test was GREEN on first run. The todo's title is factually wrong on that point. The
closure in Task 3 must SAY SO rather than restate the todo's framing.
</locked_decisions>

<evidence>
Anchors measured at plan time (2026-09-07, HEAD `4c0a7f58d`). Each is a SNAPSHOT — the task that
uses it must re-run the command and record the observed value. If an observed value disagrees with
the recorded one, STOP and report rather than proceeding on the stale number.

**E-1 — Census total.** `grep -rn "getGameInfo()" src/backend --include='*.ts' | grep -v __tests__ | wc -l`
→ **101**. Bucket split: outside `src/backend/storeManagers/` → **33**; inside `storeManagers` but
not `steam/games.ts` → **53**; therefore `steam/games.ts` itself → **15** (of which exactly **5**
match `this\.getGameInfo()`: L936, L2037, L2350 *(comment)*, L2575 *(comment)*, L2831).

**E-2 — `SteamGame.stop()` does NOT call `getGameInfo()`.** `games.ts:2803` reads
`nativeInstallsInFlight.get(this.appId)` and otherwise `logWarning`s a no-op. The todo names
`stop()` as an inheriting consumer (todo line ~55); that naming is WRONG. Verdict: not-applicable.

**E-3 — The two named non-title sites in `steam/games.ts`:**
- `L936` `getExtraInfo()`: `const info = this.getGameInfo()` then `return info.extra ?? {reqs: [], about: {...}}`.
- `L2831` `isGameAvailable()`: `info?.is_installed && info.install?.install_path && existsSync(...)`.
- `L2037` `await this.fetchMetadataIfNeeded(this.getGameInfo())` — needs its own verdict.

**E-4 — The `library` Map is hydrated SYNCHRONOUSLY from `steamLibraryStore` at two points:**
- `SteamLibraryManager.init()` (`library.ts:711`): `const cached = steamLibraryStore.get('games', [])`
  → `library.clear()` → `cached.forEach(g => library.set(g.app_name, g))` at L717-722. No `await`
  between the read and the sets.
- `refresh()` (`library.ts`): `library.clear()` at **L1090**, repopulated by `library.set(appIdStr, gameInfo)`
  at **L1215**. Measured with `awk 'NR>=1090 && NR<=1220' src/backend/storeManagers/steam/library.ts | grep -c "await "`
  → **0**. Zero awaits in that window means the mid-session rebuild is atomic with respect to the
  event loop: no IPC handler can interleave into the cleared-Map window. RE-MEASURE this.

**E-5 — The bypass sites and their failure mode when the Map misses.**
`uninstall()` at `games.ts:2354` reads `library.get(this.appId)?.install?.install_path`;
`uninstallBottleGameDirectly()` at `games.ts:2578` reads the same. On a Map miss `installPath` is
`undefined` → `resolveInstallRoot(undefined)` → the `root === null` branch at `games.ts:~2386`,
which `logWarning`s and returns
`{stdout: '', stderr: 'Refused to uninstall: install_path does not resolve inside any known root...'}`.
**Note:** this is a LOGGED REFUSAL returning a non-empty `stderr`, not the "silent no-op" the task
framing predicted. Whether that `stderr` reaches the user is a separate question the task must
answer (trace `uninstall`'s ExecResult through `src/backend/utils/uninstaller.ts`).

**E-6 — The asymmetry that makes the bypass worth checking at all.** `getGameInfo()`
(`games.ts:595-610`) reads the Map, falls back to `steamLibraryStore`, and **self-heals the Map on a
store hit** (`library.set(this.appId, cached)`). The bypass sites do not. So ordering matters: any
prior `getGameInfo()` for that appId erases the divergence.

**E-7 — Completed-todo convention.** Frontmatter keeps the original fields and adds
`status: RESOLVED`, `resolved: <date>`, `resolved_by: quick-260907-e7a` (see
`.planning/todos/completed/keyring-read-lazy-at-point-of-use.md`). Body appends a
`## Resolution (2026-09-07, quick task \`260907-e7a\`)` section (same file, L91).

**E-8 — Test invocation.** `jest.config.js` `projects` are PATHS with no `displayName`, so
`--selectProjects` has no name to select and would fail open (see MEMORY:
`jest-selectprojects-is-case-sensitive-and-exits-zero`). Run jest by test-file PATH instead. Never
run a full `pnpm test` for this task — it manufactures load-induced failures.
</evidence>

<tasks>

<task type="auto">
  <name>Task 1: Census the NON-TITLE axis and verdict every hit</name>
  <files>(no production files — output goes into the SUMMARY and is reused by Task 3)</files>
  <action>
Produce a CENSUS, not a sample. A sampling that clears is not a census that convicts.

**Step 1 — run and record the census command verbatim, with its total.**
Run exactly `grep -rn "getGameInfo()" src/backend --include='*.ts' | grep -v __tests__ | wc -l` and
record the observed number. E-1 recorded 101. If the observed total differs, record BOTH numbers and
say why (new call sites since HEAD `4c0a7f58d`) — do not silently adopt the new number as if the
plan had predicted it.

Then run the same grep without `wc -l` and split it into the three buckets from E-1, recording each
bucket's count. The three bucket counts MUST sum to the total. State that sum in the SUMMARY as an
arithmetic line, e.g. `33 + 53 + 15 = 101`.

**Step 2 — disposition EVERY hit into exactly one of six classes.** Report a count per class; the
six class counts must also sum to the census total. The six classes:

1. `not-a-call` — a comment line, a JSDoc line, or a `getGameInfo(): GameInfo {` method definition.
   Count only; no per-row listing needed.
2. `same-runner` — `this.getGameInfo()` inside a NON-Steam store manager (gog / legendary / nile /
   zoom / sideload) reading its own always-populated `GameInfo`. The Steam `{}` sentinel is
   structurally unreachable there. Count only; no per-row listing needed. State the one-line reason
   in the SUMMARY so this class is not mistaken for hand-waving.
3. `title-axis, already dispositioned` — closed by 260905-luf or 260905-mv5. Cite the summary row
   that closed it. Count + citations; no re-analysis.
4. `non-title, guarded` — reads a non-title field off a possibly-`{}` value but has a working guard.
   **LIST EVERY ROW** with `file:line`, the field(s) read, and the guard expression.
5. `non-title, relies-on-sentinel (intentional)` — the falsiness IS the contract (the three D-01
   consumers, plus anything else you find with the same shape). **LIST EVERY ROW.**
6. `non-title, genuinely unguarded` — reads a non-title field with no guard and no sentinel reliance.
   **LIST EVERY ROW**, and for each state the concrete user-visible consequence if the value is `{}`.

**Step 3 — verdict the four sites the task names explicitly**, even if a class assignment already
covers them, because the todo names them by hand and the closure must answer the todo on its own
terms:
- `games.ts:936` `getExtraInfo()` — `info.extra ?? {...}`.
- `games.ts:2831` `isGameAvailable()` — `info?.is_installed && info.install?.install_path && existsSync(...)`.
- `games.ts:2037` — `this.fetchMetadataIfNeeded(this.getGameInfo())`: what does the callee do with `{}`?
- `SteamGame.stop()` (`games.ts:2803`) — E-2 measured that it does not call `getGameInfo()` at all.
  RE-VERIFY that (`sed -n '2803,2825p' src/backend/storeManagers/steam/games.ts`) and record the
  verdict as `not-applicable — the todo names this site in error`.

**Step 4 — widen the field axis.** For classes 4/5/6, confirm you have covered reads of `install`,
`is_installed`, `art_cover`, `extra`, `platforms`, `is_delisted`, `canRunOffline`, `wine_info`,
`folder_name`, `save_folder`. If any of those fields is read off a Steam-reachable `getGameInfo()`
value at a site your census did not surface, add it — and record how it evaded the grep (e.g. the
value was stored in a variable first and read on a later line).

**Do NOT change production code in this task.** The output is the table. If Step 2 class 6 is
non-empty, list what a fix would be per row, but do not write it — report the finding and let the
next task or a follow-up todo take it. If class 6 is EMPTY, say so plainly; that is a legitimate and
expected outcome, and it is the deliverable.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain src/ 2>/dev/null)" &amp;&amp; echo "PASS: task 1 changed no production code" || (echo "FAIL: task 1 must not modify src/"; exit 1)</automated>
  </verify>
  <done>
The SUMMARY contains: the verbatim census command, the observed total, three bucket counts that sum
to the total, six class counts that sum to the total, per-row listings for classes 4/5/6, and an
explicit verdict line for each of the four named sites in Step 3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decide the raw-Map bypass with evidence — fix it or document the non-gap</name>
  <files>src/backend/storeManagers/steam/games.ts, src/backend/storeManagers/steam/__tests__/games.test.ts</files>
  <behavior>
Branch-dependent. Write NO test until Step 2's verdict is reached.

If the verdict is REACHABLE, the tests to write are:
  - Test R1 (the gap): `library` Map EMPTY, `steamLibraryStore` holding a game whose
    `install.install_path` resolves inside a known root → `uninstall()` proceeds to the correct root
    branch instead of the `root === null` refusal.
  - Test R2 (no-regression): Map POPULATED with a path resolving to root X, store holding a STALE
    entry resolving to root Y → the Map entry still wins (the helper must not invert precedence).
  - Test R3: the same Map-empty/store-hit shape at `uninstallBottleGameDirectly()`.
  - Test R4 (side-effect containment): the new helper does NOT fire `fetchMetadataIfNeeded` —
    the entire rationale for the bypass. Assert the metadata-fetch spy has zero calls.

If the verdict is NOT REACHABLE, NO test is written and NO production behaviour changes. The
deliverable is source comments only.
  </behavior>
  <action>
**Step 1 — re-measure E-4, E-5 and E-6 before trusting them.** They are plan-time snapshots.
Record each observed value beside the recorded one. If any disagrees, STOP and report rather than
proceeding.
- E-4a: `sed -n '711,730p' src/backend/storeManagers/steam/library.ts` — confirm `init()` hydrates
  the Map from `steamLibraryStore` with no intervening `await`.
- E-4b: `awk 'NR>=1090 && NR<=1220' src/backend/storeManagers/steam/library.ts | grep -c "await "` —
  recorded 0. Also confirm L1090 is still the `library.clear()` and L1215 still the `library.set`;
  if the line numbers drifted, re-locate them (`grep -n "library.clear()\|library.set(appIdStr"`)
  and widen the awk window to the CURRENT boundaries before counting.
- E-5: confirm both bypass reads (`games.ts:2354`, `games.ts:2578`) and the `root === null` refusal
  branch text.
- E-6: confirm `getGameInfo()` still self-heals the Map on a store hit.

**Step 2 — answer ONE question with file:line evidence:** can a user reach `uninstall()` (or
`uninstallBottleGameDirectly()`) for an owned, installed Steam game while the in-memory `library`
Map has no entry for that appId, but `steamLibraryStore` does?

Enumerate the candidate windows and kill or confirm each:
  (a) **Before `init()` runs.** Does the renderer render an actionable game card — one whose
      uninstall control is enabled — from its own persisted frontend state, before the backend's
      `init()` hydration lands? Trace what feeds the frontend library list and what gates the
      uninstall control. Name the file:line that decides it.
  (b) **The `refresh()` clear→rebuild window** (`library.ts:1090`→`1215`). E-4b measured zero
      `await`s in it. If that holds, the window is not yield-able and no IPC handler can interleave
      — record that as the killing evidence with the re-measured count.
  (c) **A game present in `steamLibraryStore` but dropped from the Map by a completed `refresh()`.**
      Does `refresh()` write the store from the rebuilt Map (keeping them in sync), or can the two
      diverge? Name the write site.
  (d) **Ordering.** Per E-6, any prior `getGameInfo()` for that appId self-heals the Map. Is there a
      path to `uninstall()` that provably has NOT called `getGameInfo()` for that appId first?
      `isGameAvailable()` (`games.ts:2831`) calls `getGameInfo()` and is the availability check
      behind the library UI — establish whether it runs on that path.

**Step 3a — if REACHABLE (a real gap):** add a private, side-effect-free read helper to
`SteamGame` that mirrors the FIRST HALF of `getGameInfo()` — Map read, then `steamLibraryStore`
find — and stops before `fetchMetadataIfNeeded`. Apply it at BOTH bypass sites. Keeping
`getGameInfo()`'s Map self-heal inside the helper is permitted (it is in-memory cache hydration, not
the network fetch the bypass comment objects to) — but if you keep it, SAY SO explicitly in the
helper's JSDoc and in the SUMMARY as a decision, not an accident. Do NOT populate a stub, do NOT go
async, do NOT touch `getGameInfo()`'s own double-miss branch (D-01).

RED-prove each of R1-R4 with a NAMED revert and the EXPECTED failure message. The revert for R1/R3
is: restore the bare `library.get(this.appId)?.install?.install_path` read at that site. The revert
for R2 is a PRECEDENCE SWAP inside the helper (store-then-Map instead of Map-then-store) — the bare
revert will NOT fail R2, because the old code also returns the Map entry when one is present. Do not
claim R2 is covered by the bare revert: a prior task on this exact todo (luf, ledger #11) shipped
one inadequate RED-proof and had to construct a second, precedence-swapping revert. If any of R1-R4
cannot be RED-proven by a targeted revert, say so in the ledger rather than claiming coverage.

**Step 3b — if NOT REACHABLE (the expected and legitimate outcome):** change no behaviour. Add a
source comment at BOTH bypass sites (`games.ts:~2349` and `games.ts:~2574`, alongside the existing
"reads the raw library Map directly" comments) that records:
  - The bypass ALSO forfeits `getGameInfo()`'s `steamLibraryStore` fallback, not just the metadata
    fetch — the existing comments only mention the fetch, so the next reader will re-derive this.
  - Why that forfeit is harmless: the specific file:line evidence from Step 2 that closes each
    window.
  - The tag `260907-e7a` so the comment is greppable back to this decision.
State plainly in the SUMMARY: "documented non-gap, not a fix." Do not manufacture a fix to justify
the task.

**No new user-facing strings.** Both branches produce only source comments and/or log lines, which
are not localised. Confirm with `git diff --stat public/locales/` and expect empty output; if this
task ever DID need a user-facing string it would go in `public/locales/en/gamelib.json`, never
`translation.json`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test "$(grep -v '^[[:space:]]*[*/]' src/backend/storeManagers/steam/games.ts | grep -c 'getGameInfo(): GameInfo')" = 1 &amp;&amp; grep -q "260907-e7a" src/backend/storeManagers/steam/games.ts &amp;&amp; test -z "$(git diff --stat public/locales/)" &amp;&amp; echo "PASS"</automated>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/games.test.ts</automated>
  </verify>
  <done>
A reachability verdict is recorded with file:line evidence for every candidate window (a)-(d). Both
bypass sites carry a `260907-e7a` marker (a comment in the non-gap branch, a helper call plus JSDoc
in the gap branch). `tsc --noEmit` is clean. The steam `games.test.ts` suite is green. In the gap
branch only, R1-R4 exist and each has a RED-proof ledger row naming its revert and observed failure.
`git diff --stat public/locales/` is empty.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the todo</name>
  <files>.planning/todos/pending/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md, .planning/todos/completed/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md</files>
  <action>
Move the todo with `git mv .planning/todos/pending/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md .planning/todos/completed/`, then edit it in place.

**Frontmatter** — keep every existing field, add (per E-7's convention, verified against
`.planning/todos/completed/keyring-read-lazy-at-point-of-use.md`):
```
status: RESOLVED
resolved: 2026-09-07
resolved_by: quick-260907-e7a
```
`status: OPEN` must become `status: RESOLVED` — do not leave both.

**Body** — append `## Resolution (2026-09-07, quick task `260907-e7a`)` recording FOUR things:

(a) **The D-01 sentinel decision made most of this a non-defect.** `{}` on a double cache miss is a
    deliberate cross-runner protocol, not an oversight; name the three consumers that depend on its
    falsiness (`gamedetails/dispatch.ts:82`, `sidecar/appShellFlowRegistration.ts:536`,
    `storeManagers/steam/library.ts:1257`). Cite 260905-luf.

(b) **This todo's own title is factually wrong.** luf disproved it: the nameless surface was the OS
    notification from `downloadqueue.ts`'s `processNotification`, NOT the install-failure dialog the
    title names — `resolveQueueElementTitle` already carried `title || appName` and its control test
    was GREEN on first run. Say this explicitly. Do not restate the todo's framing as if it held.
    Also correct the todo's body: it names `SteamGame.stop()` as an inheriting consumer, and per
    Task 1 Step 3 that site does not call `getGameInfo()` at all.

(c) **The Task 1 verdict table, INLINE.** The full per-row listing for classes 4/5/6 plus the class
    counts and the census total. Not a link to the SUMMARY — inline, so the closed ledger entry is
    self-contained.

(d) **Task 2's decision**, verbatim in substance: the reachability verdict, the file:line evidence
    that produced it, and whether the outcome was a RED-proven fix or a documented non-gap.

Also state what remains explicitly NOT done and why, so nothing is silently dropped: making
`getGameInfo()` async (~40 call sites across ~20 files) was scoped out by luf and stays scoped out;
if the census's class 6 is non-empty and its rows were not fixed here, name them and file a
follow-up todo rather than burying them in this closure.

Match the formatting of the neighbouring completed todos — check two of them before writing.
  </action>
  <verify>
    <automated>test -f .planning/todos/completed/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md &amp;&amp; test ! -f .planning/todos/pending/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md &amp;&amp; F=.planning/todos/completed/2026-08-22-steam-getgameinfo-returns-empty-on-async-cache-miss.md &amp;&amp; grep -q "^status: RESOLVED" "$F" &amp;&amp; grep -q "^resolved_by: quick-260907-e7a" "$F" &amp;&amp; ! grep -q "^status: OPEN" "$F" &amp;&amp; grep -q "260905-luf" "$F" &amp;&amp; grep -q "processNotification" "$F" &amp;&amp; grep -q "dispatch.ts:82" "$F" &amp;&amp; echo "PASS"</automated>
  </verify>
  <done>
The todo file exists only under `.planning/todos/completed/`, carries `status: RESOLVED` /
`resolved: 2026-09-07` / `resolved_by: quick-260907-e7a` with no residual `status: OPEN`, and its
Resolution section contains all four required elements (a)-(d) including the inline verdict table.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → IPC `uninstall` channel | `appName` is renderer-supplied and reaches `SteamGame(appId)`; `uninstallBottleGameDirectly()` derives filesystem DELETE targets from it |
| `steamLibraryStore` (on-disk JSON) → backend | persisted `install.install_path` values are attacker-influenceable if the store file is writable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-e7a-01 | Tampering | Task 2 Step 3a's new read helper feeding `uninstall()`'s delete routing | mitigate | The helper is READ-ONLY (Map read + store find + optional Map self-heal). It must not relax any existing check: `NUMERIC_APP_ID.test(this.appId)` at `games.ts:2566` and the `resolveInstallRoot` containment check at `games.ts:2579` stay exactly as they are and still run AFTER the helper. Verify by diff: neither guard line is touched. |
| T-e7a-02 | Elevation of Privilege | widening the uninstall path's data source from Map-only to Map+persisted-store | mitigate | The store fallback can only ever return a value that `resolveInstallRoot()` still containment-checks before anything is deleted. A store entry with a poisoned `install_path` reaches the same `root === null` refusal an in-Map poisoned entry would. No new delete target is introduced. |
| T-e7a-03 | Information Disclosure | any new log line added at the bypass sites | mitigate | Log `appId` and root verdict only — never a full `install_path`, matching the redaction posture of the existing `logWarning`s at these sites. If the branch is 3b (comments only), no new log line exists at all. |
| T-e7a-04 | Repudiation | the todo closure asserting a verdict without evidence | mitigate | Task 3 element (d) requires the file:line evidence inline in the closed ledger entry, not a bare claim. Task 2 Step 1 requires re-measuring every plan-time snapshot before it is relied on. |
| T-e7a-SC | Tampering | npm/pip/cargo installs | N/A | This plan installs NO packages. No `## Package Legitimacy Audit` is required; if any task finds itself wanting a dependency, STOP and report rather than installing. |
</threat_model>

<source_audit>
| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | Audit and close the NON-TITLE axis of the todo, then close the todo | Tasks 1, 2, 3 |
| REQ | QUICK-E7A-01 | Tasks 1, 2, 3 |
| CONTEXT | Census with per-site verdict (`guarded` / `relies-on-sentinel` / `genuinely unguarded`), recorded grep command and full hit count | Task 1, Steps 1-4 |
| CONTEXT | The four hand-named sites: `games.ts:936`, `games.ts:2831`, `stop()`, plus the field-axis widening list | Task 1, Steps 3-4 |
| CONTEXT | Raw-Map bypass: decide with evidence, fix + RED-proof if reachable, document if not | Task 2, Steps 1-3 |
| CONTEXT | Move todo to `completed/` with `status: RESOLVED`, resolution section, inline verdict table, matching convention | Task 3 |
| CONTEXT | Closure records D-01, luf's disproof, verdict table, Task 2's decision | Task 3, elements (a)-(d) |
| CONTEXT | No new user-facing strings; if any, `gamelib.json` never `translation.json` | Task 2, final action paragraph |
| CONTEXT | Run relevant jest projects, not the full suite | Task 2 `<verify>`, by test-file path per E-8 |

No unplanned items. Nothing deferred.
</source_audit>

<verification>
1. `git status --porcelain .planning/STATE.md .planning/ROADMAP.md` — MUST be empty. This plan and
   its execution write to neither file, by any means, including any `gsd-sdk state.*`/`roadmap.*`
   verb. The orchestrator owns those two files.
2. `npx tsc --noEmit` — clean.
3. `npx jest src/backend/storeManagers/steam/__tests__/games.test.ts` — green. Run as its own
   command, not chained after a file write (MEMORY: `jest-in-the-same-command-as-a-write-reads-stale`).
4. `git diff --stat public/locales/` — empty.
5. Census arithmetic: bucket counts sum to the total AND class counts sum to the total. Both sums
   stated explicitly in the SUMMARY.
6. `grep -rn "getGameInfo()" src/backend --include='*.ts' | grep -v __tests__ | wc -l` re-run at the
   end matches the Task 1 total (no call sites added or removed by this plan).
</verification>

<success_criteria>
- Every `getGameInfo()` census hit is dispositioned; class counts sum to the census total; classes
  4/5/6 are listed row-by-row with `file:line`.
- The bypass reachability question has a verdict backed by file:line evidence for each of the four
  candidate windows (a)-(d) — not an assertion, and not an assumption inherited from this plan's
  snapshots.
- Exactly one of: (i) a RED-proven fix at both bypass sites with a ledger naming each revert and its
  observed failure, or (ii) source comments at both bypass sites recording a documented non-gap.
  Outcome (ii) is a legitimate, expected result and is not a failure of the task.
- The todo is in `.planning/todos/completed/` with `status: RESOLVED` and a four-element resolution
  section, and is gone from `.planning/todos/pending/`.
- `.planning/STATE.md` and `.planning/ROADMAP.md` are untouched.
</success_criteria>

<output>
Create `.planning/quick/260907-e7a-audit-and-close-the-non-title-axis-of-th/260907-e7a-SUMMARY.md`
when done. It must contain: the verbatim census command and total, the bucket and class arithmetic,
the full disposition table, the reachability verdict with its evidence, and either the RED-proof
ledger or the explicit "documented non-gap — no production behaviour changed" statement.
</output>
