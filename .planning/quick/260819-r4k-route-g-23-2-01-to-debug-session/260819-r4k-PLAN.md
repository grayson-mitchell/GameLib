---
quick_id: 260819-r4k
slug: route-g-23-2-01-to-debug-session
description: "Route gap G-23.2-01 (library-vanish) out of 23.2's UAT ledger to the parked debug session, so 23.2's open_gaps is []"
created: 2026-08-19
mode: quick (docs-only, executed inline — no subagents, no worktree)
status: planned
---

# Quick Task 260819-r4k — Route `G-23.2-01` out of 23.2's ledger

## Why

Phase 23.2's four gates are all discharged: `23.2-REVIEW.md` is `clean` (0 critical / 0
warning / 2 info), `23.2-VERIFICATION.md` is `passed` 9/9, `23.2-HUMAN-UAT.md` is
`complete` 3/3 on real hardware, and `23.2-SECURITY.md` is `verified` with
`threats_open: 0`. The single field keeping the phase's ledger non-empty is
`open_gaps: [G-23.2-01]` — a defect that:

- was **not** one of the three declared UAT items (`new_gaps_found_outside_declared_items: 1`),
- is in a different subsystem (renderer-side library state, not depot selection),
- does not affect any of this phase's truths (the entry's own `scope_note` says test 1
  asserts on the written `.acf` and the install-completion path, both of which passed
  before the vanish surfaced), and
- already has a live owner: `.planning/debug/uninstall-game-vanishes.md`, re-parked
  UNREPRODUCED on 2026-08-19 (`6891551b0`) with all instrumentation reverted.

Holding 23.2 open on it misrepresents both the phase and the defect. Route it out; do
not close it.

## Scope fences

- **Docs only.** No source file is touched. No test is run because none is implicated.
- **The defect is NOT being resolved.** `status` moves `failed` → `routed`, never
  `resolved`. Root cause stays OPEN in the debug session, which stays `parked`.
- **Nothing is erased.** The full gap entry — `triage_so_far`, `narrowed_2026_08_19`,
  `architecture_finding`, `census_trap_noted`, `still_unverified`,
  `debug_outcome_2026_08_19`, `recommended_next`, `artifacts` — is retained verbatim in
  place. Only the status and a routing header change.
- **`new_gaps_found_outside_declared_items: 1` stays `1`.** It is a historical count of
  what the UAT run found, not a live-gap counter.
- **Phase closure is NOT in scope.** STATE.md's stale `stopped_at` ("PHASE 23.2 EXECUTED
  — NOT COMPLETE"), `status: verifying`, and the missing ROADMAP phase-completion marker
  are a separate follow-up.

## Tasks

### 1. Route the gap in `23.2-HUMAN-UAT.md`

Files: `.planning/phases/23.2-steam-depot-selection-required-vs-optional-depots-and-skip-a/23.2-HUMAN-UAT.md`

- Frontmatter `open_gaps: [G-23.2-01]` → `open_gaps: []` with an inline comment naming
  where it went and that the entry is retained below.
- Gap entry `status: failed` → `status: routed`, plus `routed_out`, `routed_to` and
  `routed_why` fields directly under it.
- Prepend a `(LATEST)` clause to `notes:` recording the routing, and demote the previous
  `(LATEST)` marker to `PRIOR:` so exactly one clause claims to be current.

Verify: `open_gaps: []` present; the string `status: failed` no longer appears in the
file; every retained sub-field of the gap entry still present.

### 2. Record sole ownership in the debug session

Files: `.planning/debug/uninstall-game-vanishes.md`

- Amend `also_tracked_as:` to state the gap was routed OUT of 23.2's ledger on
  2026-08-19 and that this file is now the sole live record.
- Add a short body section under the re-park note so a reader arriving from either
  direction learns that no phase ledger will surface this any more.

Verify: the file still reads `status: parked` (routing must not read as a resolution).

### 3. Fix the forward pointer in Phase 23's closed ledger

Files: `.planning/phases/23-steam-full-ownership-install-stateflags-4/23-UAT.md`

`23-UAT.md`'s notes tell a reader the defect "is filed on 23.2-HUMAN-UAT.md". After
task 1 that pointer leads to a `routed` stub. Amend it to name the debug session as the
live owner. Phase 23's own `open_gaps: []` and gate verdicts are untouched.

Verify: `git diff` on `23-UAT.md` shows only the pointer sentence changed.

## Known trap this creates, and why it is accepted

`/gsd-audit-uat` keys on recognised status values; `routed` is not one, so this gap will
stop appearing in cross-phase UAT sweeps — that is the intent, but it means
`.planning/debug/uninstall-game-vanishes.md` becomes the **only** surface that will ever
resurface it. That file must stay `parked` (never archived, never closed) until a real
recurrence or a fix. Recorded here and in SUMMARY.md so the trade is deliberate rather
than discovered later.

## Commit discipline

The working tree carries a **concurrent session's** uncommitted edits
(`.planning/STATE.md` decisions row `260819-p2d`, `.planning/phases/34.13-.../34.13-UAT.md`,
and the untracked `.planning/quick/260819-p2d-.../`). Commit by explicit path only. For
`.planning/STATE.md`, stage only this task's own hunk (`git apply --cached` on a filtered
diff) so the concurrent row is not absorbed. Never `git stash`.
