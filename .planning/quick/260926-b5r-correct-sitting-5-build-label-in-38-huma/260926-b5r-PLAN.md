---
phase: quick-260926-b5r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md
autonomous: true
requirements: [QUICK-260926-b5r]

must_haves:
  truths:
    - "No sitting-5 label in 38-HUMAN-UAT.md (frontmatter sessions row, Current Test prose, section heading, Conditions line) claims a `tauri dev` debug build of `59df4c1b6`"
    - "Sitting 5 is recorded as the INSTALLED shell v0.7.0 (built 2026-09-24 07:34 from `5b6201e26`) with the repo's build/main/sidecar.js"
    - "A desk-diff note explains why 38-S02, 38-S14(a) and 38-W06 transfer to HEAD, framed as a desk argument, not a re-run"
    - "The caret paragraph no longer calls the dead caret a regression, and points at the resolved debug session and commit 1f93c5812"
    - "The original mislabel remains visible as marked history (not silently rewritten)"
    - "No item result: or status: line changes"
  artifacts:
    - path: ".planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md"
      provides: "Corrected sitting-5 build label + desk-diff note + corrected caret paragraph"
      contains: "5b6201e26"
  key_links:
    - from: "38-HUMAN-UAT.md Sitting 5 caret paragraph"
      to: ".planning/debug/resolved/mouse-dead-dropdown-disclosure.md"
      via: "path reference"
      pattern: "mouse-dead-dropdown-disclosure"
---

<objective>
Correct the Phase 38 sitting-5 build label in `38-HUMAN-UAT.md` (operator-approved, docs only).
Sitting 5 did not run a `pnpm tauri:dev` debug build of `59df4c1b6`. It ran the INSTALLED shell
`%LOCALAPPDATA%\GameLib\gamelib-shell.exe` (v0.7.0, mtime 2026-09-24 07:34, built from `5b6201e26`,
the last commit before that mtime), whose child sidecar is the repo's `build/main/sidecar.js`
(compile-time path, `resolve_sidecar_entry()`, `main.rs:7823`). Evidence:
`.planning/debug/resolved/mouse-dead-dropdown-disclosure.md` (commit `1f93c5812`).

Purpose: the sitting's record must say which binary produced its scores, and why those scores
still hold on HEAD. That is decision 1 ("re-label or re-run") of the pending todo
`2026-09-26-tauri-dev-silently-hands-off-to-a-stale-installed-build.md`, decided as re-label.
Output: one edited file. No commit (the orchestrator commits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/debug/resolved/mouse-dead-dropdown-disclosure.md
@.planning/todos/pending/2026-09-26-tauri-dev-silently-hands-off-to-a-stale-installed-build.md

Target file: `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md`.
Only these regions change. Read them by offset, not the whole file.

- Line 12, frontmatter `sessions:` row, currently:
  `- "Sitting 5 -- 2026-09-26, Windows 11, tauri dev (debug build) `59df4c1b6` -- 38-S02 PASS, 38-W06 FAIL accepted; 38-S14 sub-case (a) PASS but item stays OPEN; 38-W04 not run (no CI artifact exists); four defects filed"`
- Lines 17-27, the `## Current Test` bracketed prose. Line 21 begins "and Sitting 5 (2026-09-26), which discharged 38-S02 PASS ...".
- Line 482, heading: `## Sitting 5 — 2026-09-26, Windows 11, `tauri dev` (debug build) `59df4c1b6``
- Lines 484-487, the `**Conditions.**` paragraph. It says "`pnpm tauri:dev`, DEBUG build, commit `59df4c1b6`. Say "debug build" explicitly, for the same reason sitting 4 did: it is what keeps `38-W04`/`38-W05` un-runnable."
- Lines 544-553, the `**THE OBSERVATION TRAP OF THIS SITTING ...**` paragraph. Lines 547-549 say "but the caret itself is a **regression of a resolved debug session** (`.planning/debug/resolved/steam-caret-dropdown-dead.md`, fixed in `3a0e62918` ...)", and it ends "... it is filed as its own todo rather than chased here."

Desk diff `5b6201e26..HEAD`. The orchestrator measured it on 2026-09-26, and it is the content of
the new note:
1. Only the embedded frontend and the Rust shell were stale. The sidecar/backend was the repo build.
2. There are no changes to the install dialog, `MainButton`, `GamePage`, or the Steam backend `storeManager`.
3. `contentLightNotice` English copy is unchanged. Only non-English locale files changed.
4. `main.rs` changes are only `9ca63c7d0`/`1fa6e9e93` (Phase 46 WR-01/WR-02 Windows pipe) and `08f24b05d`/`f3972c70f` (260925-uok `gamelib://` HKCU self-heal). No cookie lines are touched, and `legendary/user.ts` is unchanged. So 38-W06's FAIL stands against HEAD: the cookie-removal defect is live on current code.
5. The `downloadmanager/utils.ts` change is the stalled-install message pluralisation only. S02 does not exercise it.
6. The only behavioural difference that hit the sitting is `Dropdown.toggle` (the pre-`3a0e62918` functional updater), which caused the dead caret. It affected no score, because the S14(a) dialog was opened by right-click and it is the same dialog.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-label sitting 5, add the desk-diff transfer note, and correct the caret paragraph in 38-HUMAN-UAT.md</name>
  <files>.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md</files>
  <action>
Optional cheap spot check before writing: `git log --oneline 5b6201e26..HEAD -- src-tauri/src/main.rs src/backend/storeManagers/legendary/user.ts`
should list only the four `main.rs` commits named in context item 4. If it shows anything else,
stop and report it. Do not write a claim the tree contradicts.

Make these edits and no others:

(a) Frontmatter line 12. Replace only the build part of the row with a correct label. The
scores tail, from "-- 38-S02 PASS" onward, stays byte-identical. Example shape: "Sitting 5 --
2026-09-26, Windows 11, INSTALLED shell v0.7.0 (built 2026-09-24 07:34 from `5b6201e26`) with
the repo build/main sidecar; label corrected by quick 260926-b5r -- 38-S02 PASS, ...". YAML trap:
this is a double-quoted YAML scalar. It must contain NO backslash, because `\G` in
`%LOCALAPPDATA%\GameLib` is an invalid YAML escape and breaks the frontmatter. It also must not
contain a double quote. Keep the Windows path out of the frontmatter, or write it with forward
slashes. Do not put `59df4c1b6` in this row.

(b) Current Test prose, around line 21. Change "and Sitting 5 (2026-09-26), which ..." so that it
names the corrected build briefly, for example "and Sitting 5 (2026-09-26, run on a stale installed
shell, not a dev build of HEAD; see the correction in its section), which ...". Keep the bracketed
paragraph's remaining facts unchanged. It must not mention `59df4c1b6`.

(c) Heading, line 482. Change it to something like "## Sitting 5 — 2026-09-26, Windows 11,
installed shell v0.7.0 (`5b6201e26`), label corrected". Keep the prefix "## Sitting 5" exactly,
because line 23 cites the section by that name. It must not contain `59df4c1b6` or "debug build".

(d) Conditions paragraph, lines 484-487. Mark the correction inline and keep the history. Open
with a bold marker such as "**Conditions (corrected 2026-09-26, quick `260926-b5r`).**", then
state the real conditions:
- the installed `%LOCALAPPDATA%\GameLib\gamelib-shell.exe`, v0.7.0, mtime 2026-09-24 07:34, built from `5b6201e26` (the last commit before that mtime)
- its sidecar is the repo's `build/main/sidecar.js`, a compile-time path (`resolve_sidecar_entry()`, `main.rs:7823`)
- the embedded frontend and the Rust shell were therefore stale; the backend and the shared `gamelib.log` looked current
- the single-instance guard makes a concurrent `pnpm tauri:dev` hand focus to that instance and exit

Cite `.planning/debug/resolved/mouse-dead-dropdown-disclosure.md` (commit `1f93c5812`) and the
pending todo `2026-09-26-tauri-dev-silently-hands-off-to-a-stale-installed-build.md`. Then keep the
original claim as quoted history. The token `Originally recorded as` and `59df4c1b6` MUST sit on
the same physical line, because the verify gate keys on that. Example: _Originally recorded as
"`pnpm tauri:dev`, DEBUG build, commit `59df4c1b6`"; that label was wrong._

Also correct the "Say debug build explicitly ... keeps 38-W04/38-W05 un-runnable" sentence. This
sitting's build profile is not established, so do NOT claim release or debug. State that the
38-W04 not-run reason sitting 5 measured (no `v*` tag, so no CI artifact exists) does not depend on
the build profile. Leave the two library paths and the `enableSteamNativeInstall` ON sentence
unchanged.

(e) New paragraph, placed directly after the Conditions paragraph. Give it a bold marker such as
"**Why the scores transfer to HEAD: a desk diff, not a re-run.**" Record context items 1-6: the
diff `5b6201e26..HEAD`, measured at the desk on 2026-09-26. Map each score to its reason:
- 38-S02: the install path, MainButton, GamePage, storeManager and the dialog are unchanged; the `downloadmanager/utils.ts` pluralisation is not exercised
- 38-S14(a): the dialog is unchanged, `contentLightNotice` English copy is unchanged, and the copy discrimination is intact
- 38-W06: no cookie lines in `main.rs` changed and `legendary/user.ts` is unchanged, so the FAIL stands against HEAD and the cookie-removal defect is live on current code
- the one behavioural difference was `Dropdown.toggle`, which scored nothing

End with an explicit honest-limit sentence: nothing was re-run on HEAD, and this is an argument
from the diff that the scores transfer, not an observation.

(f) Observation-trap paragraph, lines 544-553. Do not silently delete the regression claim. Mark
it as corrected, for example by striking the phrase with `~~regression of a resolved debug
session~~` and following it with a bold "**[Corrected 2026-09-26, quick `260926-b5r`: NOT a
regression.]**". The sentence should then say that the stale bundle carried the pre-`3a0e62918`
`Dropdown.toggle()` functional updater, so this was the already-fixed defect. Cite
`.planning/debug/resolved/mouse-dead-dropdown-disclosure.md` (commit `1f93c5812`), and add that the
operator confirmed on 2026-09-26 that a mouse click opens the dropdowns on HEAD under
`pnpm tauri:dev`. Keep the existing sentences about the route not affecting the score and about the
mouse-dead/gamepad-live split. For the "filed as its own todo" clause, add that the todo is now in
`completed/` as not-a-regression. Keep the `steam-caret-dropdown-dead.md` / `3a0e62918` citation,
because it names the fix the stale bundle predates.

Do NOT touch any `result:` or `status:` line, any item block, any `expected: |` block (CLAUDE.md
forbids flattening them), sitting 4's section, the `38-W04` paragraph's line 536 ("Sitting 4
recorded that a debug build ..." is sitting 4's claim; relabelling sitting 4 is not established),
or any other file. In particular, do NOT edit `38-VERIFICATION.md`,
`2026-09-26-webview2-delete-cookie-does-not-remove-epic-cookies.md` or
`debug/resolved/epic-cookie-clear-read-divergence.md`, even though they repeat the old label. They
are out of this task's scope. Report them in the SUMMARY as follow-ups. Keep the prose wrapped near
100 columns, as the surrounding text is.
  </action>
  <verify>
    <automated>F=".planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md"; cd /c/Users/grays/Projects/GameLib && npx prettier --check "$F" && test "$(grep -E '^  - "Sitting 5' "$F" | grep -c '59df4c1b6')" = 0 && test "$(grep -E '^  - "Sitting 5' "$F" | grep -c 'debug build')" = 0 && test "$(grep -E '^## Sitting 5' "$F" | grep -cE '59df4c1b6|debug build')" = 0 && test "$(awk '/^## Sitting 5/{s=1} s' "$F" | grep '59df4c1b6' | grep -vc 'Originally recorded as')" = 0 && test "$(awk 'NR>=15 && NR<=30' "$F" | grep -c '59df4c1b6')" = 0 && grep -q '5b6201e26' "$F" && grep -q 'mouse-dead-dropdown-disclosure' "$F" && grep -q '1f93c5812' "$F" && test "$(git diff -U0 -- "$F" | grep -cE '^[+-][[:space:]]*(result|status):')" = 0 && node -e "const fs=require('fs');const t=fs.readFileSync(process.argv[1],'utf8');const m=t.match(/^---\n([\s\S]*?)\n---/);require('js-yaml').load(m[1]);console.log('frontmatter yaml OK')" "$F" && pnpm planning-gates</automated>
  </verify>
  <done>
- prettier --check passes on the file.
- The frontmatter sitting-5 row, the Current Test prose, the `## Sitting 5` heading and the Conditions lead carry no `59df4c1b6`/debug-build claim. The only remaining `59df4c1b6` in the section sits on an `Originally recorded as` history line.
- The desk-diff note is present with all six points and the explicit "not a re-run" limit.
- The caret paragraph marks the regression claim as corrected and cites `mouse-dead-dropdown-disclosure.md` / `1f93c5812` and the operator's 2026-09-26 mouse confirmation on HEAD.
- The frontmatter parses as YAML.
- `git diff` shows zero changed `result:`/`status:` lines.
- `pnpm planning-gates` shows no NEW failure. The pre-existing, unrelated 260925-uok failure is expected. Confirm that it is the only failure and that it also fails on a clean checkout (`git stash` / re-run / `git stash pop`), or compare its message with the pre-edit run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning doc → audit tooling | `audit-uat` / `uat-visibility-gate.py` / frontmatter parsers read this file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-b5r-01 | Tampering | 38-HUMAN-UAT.md frontmatter | mitigate | No backslash or double quote in the YAML double-quoted row. The js-yaml parse in verify catches a broken scalar. |
| T-b5r-02 | Repudiation | sitting-5 history | mitigate | The original label is kept as marked history (`Originally recorded as`, strikethrough + `[Corrected ...]`) rather than deleted. |
| T-b5r-03 | Tampering | item results | mitigate | The verify gate asserts zero changed `result:`/`status:` lines in `git diff`. |
| T-b5r-04 | Information disclosure | desk-diff note | accept | It holds commit hashes and repo paths only. No session data or cookie values are copied from gamelib.log. |
</threat_model>

<verification>
- The Task 1 automated gate passes (prettier, label greps, history-line gate, result/status diff gate, YAML parse, planning-gates with only the pre-existing 260925-uok failure).
- `git status` shows only the one target file modified, plus this quick directory.
</verification>

<success_criteria>
The sitting-5 record names the binary that actually ran. It explains, as a desk argument, why
38-S02 PASS, 38-S14(a) PASS and 38-W06 FAIL still hold on HEAD, and it no longer calls the dead
caret a regression. The original label stays visible as corrected history, and no score changes.
</success_criteria>

<output>
Create `.planning/quick/260926-b5r-correct-sitting-5-build-label-in-38-huma/260926-b5r-SUMMARY.md`. List the
out-of-scope files that still carry the old `59df4c1b6` debug-build label: `38-VERIFICATION.md`,
`todos/pending/2026-09-26-webview2-delete-cookie-does-not-remove-epic-cookies.md:16`,
`debug/resolved/epic-cookie-clear-read-divergence.md:206-207`, and the historical 260926-a1l
PLAN/SUMMARY, which stay as written.
</output>
