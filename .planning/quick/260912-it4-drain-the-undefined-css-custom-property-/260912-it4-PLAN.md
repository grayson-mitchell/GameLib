---
phase: quick-260912-it4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/components/UI/SearchBar/index.scss
  - src/frontend/screens/Library/components/GameCard/index.css
  - src/frontend/themes.scss
  - src/frontend/screens/DownloadManager/components/DownloadManagerItem/index.css
  - src/frontend/screens/Login/index.scss
  - src/frontend/styles/_buttons.scss
  - src/frontend/components/UI/EditGameDialog/index.css
  - src/frontend/screens/Library/components/InstallModal/SideloadDialog/index.scss
  - src/frontend/screens/Settings/index.css
  - src/frontend/screens/WineManager/index.css
  - src/frontend/screens/WineManager/components/WineItem/index.css
  - src/frontend/components/UI/PopoverComponent/index.scss
  - src/frontend/components/UI/SteamGridDBPicker/index.scss
  - src/frontend/components/UI/Dropdown/index.scss
  - src/frontend/components/UI/NavShell/__tests__/cssTokenSweep.test.ts
  - .planning/quick/260912-it4-drain-the-undefined-css-custom-property-/sweep-census.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "The used-vs-declared sweep reports 0 undefined CSS custom property references, or reports only names on an explicit, commented allowlist"
    - "A jest gate in the Frontend project fails if a new undefined var() reference is introduced"
    - "The gate fails loudly if its allowlist names a reference that no longer exists"
    - "Every site whose rendered font, colour or border changes has its delta stated in the SUMMARY"
  artifacts:
    - path: "src/frontend/components/UI/NavShell/__tests__/cssTokenSweep.test.ts"
      provides: "Source-text gate holding undefined-token count at 0 + rot-detecting allowlist"
      contains: "stripSourceComments"
    - path: ".planning/quick/260912-it4-drain-the-undefined-css-custom-property-/sweep-census.py"
      provides: "The census script, corrected to strip comments before collecting refs"
  key_links:
    - from: "src/frontend/components/UI/NavShell/__tests__/cssTokenSweep.test.ts"
      to: "backend/testUtils/stripSourceComments"
      via: "import"
      pattern: "stripSourceComments"
---

<objective>
Drain the 39 undefined CSS custom property references across 23 names catalogued in
`.planning/todos/pending/2026-09-12-39-references-to-23-css-custom-properties-declared-nowhere.md`,
then install a sweep gate so the count cannot drift back up.

Purpose: `var(--undefined)` is invalid at computed-value time. In a longhand the declaration
silently falls back to `inherit`/`unset`; in a `font:` shorthand it resets family, size, weight,
style AND line-height together. Nothing in this repo catches any of it — `pnpm lint` is eslint on
`.ts`/`.tsx` only, there is no stylelint, and the Frontend jest project runs
`testEnvironment: 'node'` with no CSS engine.

Output: 22 of 23 names resolved (the 23rd is a phantom, see Task 3), a corrected census script,
and `cssTokenSweep.test.ts` holding the line at 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-09-12-39-references-to-23-css-custom-properties-declared-nowhere.md
@.planning/quick/260912-it4-drain-the-undefined-css-custom-property-/sweep-census.py
@src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts

**MANDATORY repo rule:** run `graphify query "<question>"` to orient before grepping or reading
raw source. Only read raw files to modify or debug specific lines.

**Re-run the census, do not re-invent it:**
`python3 .planning/quick/260912-it4-drain-the-undefined-css-custom-property-/sweep-census.py`
Verified UNCHANGED at HEAD `fba412d5a`: **39 references across 23 names**.
</context>

<measured_facts>
These were measured by the planner at `fba412d5a`. Do not re-derive them; do verify any you are
about to depend on.

**The census's own organising principle is wrong.** The todo splits by name family. The split that
actually decides each fix is **whether the undefined name sits behind a declared fallback arm**:

- 6 of the 39 refs are caught (or nearly caught) by a declared arm and render *something* today.
  "Correcting" those changes rendering. See Task 1.
- 32 are bare or their only other arm is also undefined — those genuinely compute to
  `unset`/`inherit` today.
- 1 (`--token`) is not a reference at all. See Task 3.

**Declared-token universe:**
- `src/frontend/styles/_colors.scss` — raw Figma constants, incl. the full `--status-*` set. It
  carries the literal comment "General Colors as on Figma. Do not add new colors here." There is
  **no** `--status-denied` and **no** `--status-sucess`.
- `src/frontend/styles/_typography.scss` — the live type vocabulary: `--primary-font-family`,
  `--secondary-font-family`, weights `--light --regular --medium --semibold --bold --extrabold`,
  sizes `--text-xs --text-sm --text-md --text-lg --text-xl --text-2xl`…`--text-6xl`.
- `src/frontend/themes.scss` — base `body {}` is lines 1-20 only. `--success`/`--danger` and
  friends are declared **per theme**. Measured counts: `--success:` ×12, `--danger:` ×8,
  `--divider:` ×2 (lines 97, 145), `--input-background:` ×11, `--search-bar-border:` ×1
  (line 58, inside `body.midnightMirage` **only**).

**Locked constraints (do not re-litigate):**

1. **`--status-*` are RAW; `--success`/`--danger` are the theme-adaptive ALIASES.** `body.nord-light`
   overrides the alias and leaves the raw token untouched, so `color: var(--status-success)` is
   near-invisible on nord-light while `color: var(--success)` is not. Never resolve a name to a
   `--status-*` raw token in a `color:` position.
2. **Absent ≠ never-defined.** Before declaring any name dead, run `git log -S "--the-token:" --all`.
   The type vocabulary was deleted **on purpose** by upstream `b6f3da757` ("[Tech/Refactor]
   Frontend/design system", #1851, Oct 2022), which introduced `_typography.scss` as the
   replacement. Migrate the stragglers; do not resurrect the vocabulary.
3. **Two traps on the `--font-*` shorthands** (established by parent quick `260912-7o4`):
   the old `--font-*-bold` names meant weight **500** = `--medium`, **not** `--bold`/700 — "bold"
   was a slot label; and the values were never legal `font` shorthands anyway (no size, family not
   last), so **these rules have never applied**. Migrating one is a **rendering change, not a
   restoration**, and needs its delta stated the way `260912-7o4` stated weight 400 → 500.
4. **The custom-theme question is real.** Heroic ships user CSS
   (`screens/Settings/components/CustomCSS.tsx`). Some names may be deliberate extension points a
   user theme supplies — the fix is then to declare a default, not rewrite the call site. Resolve
   with `git log -S`, not by guessing. Typos are exempt: nothing supplies a misspelling.
5. **Measure, do not eyeball.** The current silent-inherit fallback looks plausible in some themes,
   so "it looks fine" is not evidence. State the delta for every site whose render changes.
</measured_facts>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the 3 typos and the 4 fallback-caught references (7 refs, 6 names, 4 files)</name>
  <files>src/frontend/components/UI/SearchBar/index.scss, src/frontend/screens/Library/components/GameCard/index.css, src/frontend/themes.scss, src/frontend/screens/DownloadManager/components/DownloadManagerItem/index.css</files>
  <action>
Apply exactly these six dispositions. Two of them contradict the todo's "the fix is unambiguous —
correct the spelling"; the planner's measurement is the authority here.

**1. `--input-backgroundd` — `SearchBar/index.scss:25`. LIVE DEFECT on 10 of 11 themes.**
Current: `box-shadow: 0px 0px 0px 3px var(--search-bar-border, var(--input-backgroundd));`
`--search-bar-border` is declared **only** inside `body.midnightMirage` (`themes.scss:58`), so on
the other 10 themes the chain falls through to the misspelling, the whole `box-shadow` declaration
is invalid at computed-value time, and there is **no focus ring at all**.
Fix: change the fallback arm to `var(--input-background)` (declared ×11 in themes.scss).
**Delta to state:** on the 10 non-midnightMirage themes the search bar's `:focus-within` ring goes
from ABSENT to a 3px ring in `--input-background`. midnightMirage is unchanged.

**2. `--status-sucess` — `GameCard/index.css:385`. DO NOT correct the spelling.**
Current: `color: var(--status-sucess, var(--success));` — the fallback arm is declared, so this
renders `--success` (the theme-adaptive alias) today. Renaming to `--status-success` would switch
it to the RAW Figma constant, which per constraint 1 `body.nord-light` does not override — making
installed-game labels near-invisible on that theme.
Fix: collapse to `color: var(--success);`. **Delta: none.** State that explicitly, with the
nord-light reasoning, so the next reader does not "fix the typo" back.

**3. `--status-denied` — `themes.scss:16`. Delete the line.**
Current: `--anticheat-unknown: var(--status-denied);`. There is no `--status-denied` in
`_colors.scss`, and `--anticheat-unknown` has **zero consumers**: `Anticheat/index.scss:27-28`
groups `.anticheatInfo.Unknown` with `.Denied` and styles both from `--anticheat-denied`
(lines 29, 31). Confirm with `git grep -n 'anticheat-unknown' -- src` (expect exactly the one
declaration) before deleting.
Fix: delete the whole declaration line. **Delta: none** — a dead token with a broken value.

**4-5. `--warning-hover` ×2 — `GameCard/index.css:492` and `:501`. Dead fallback arm.**
Current at both: `fill: var(--cancel-button-overlay, var(--warning-hover));`
`--cancel-button-overlay` is declared in the base `body {}` block (`themes.scss:8`, as
`var(--status-warning-hover)`), so the fallback arm is unreachable on every theme.
`--warning-hover` is declared nowhere and never was (`git log -S "--warning-hover:" --all` is
empty — verify).
Fix: repoint the dead arm to `var(--status-warning-hover)`, matching what
`--cancel-button-overlay` already resolves to at `themes.scss:8`. Keep the two-arm shape — this
file's neighbours use it as a convention. **Delta: none** (arm unreachable).

**6-7. `--cancel-button-hover` — `DownloadManagerItem/index.css:29`, and
`--install-button-hover` — `:45`.**
Current: `fill: var(--cancel-button-hover, var(--danger-hover));` and
`fill: var(--install-button-hover, var(--accent-overlay));`. In both the primary arm is undefined
and never was declared (`git log -S` empty for both — verify), and the declared fallback is what
renders today.
Fix: collapse each to its fallback — `fill: var(--danger-hover);` and
`fill: var(--accent-overlay);`. **Delta: none.**

Commit as one atomic commit themed "references a fallback does or should catch".
  </action>
  <verify>
    <automated>python3 .planning/quick/260912-it4-drain-the-undefined-css-custom-property-/sweep-census.py | head -1  # expect "32 references across 17 names"</automated>
    <automated>git grep -n -- '--status-sucess\|--input-backgroundd\|--status-denied\|--warning-hover\|--cancel-button-hover\|--install-button-hover' -- src | grep -v __tests__ | wc -l  # expect 0</automated>
  </verify>
  <done>Sweep reports 32 refs / 17 names. All six names gone from `src/` non-test source. SUMMARY states the SearchBar focus-ring delta and records "no delta" with reasoning for the other five.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the 13 retired-typography references (6 names, 6 files)</name>
  <files>src/frontend/screens/Login/index.scss, src/frontend/screens/DownloadManager/components/DownloadManagerItem/index.css, src/frontend/styles/_buttons.scss, src/frontend/components/UI/EditGameDialog/index.css, src/frontend/screens/Library/components/InstallModal/SideloadDialog/index.scss, src/frontend/screens/Settings/index.css, src/frontend/screens/WineManager/index.css</files>
  <action>
All six names were deleted on purpose by upstream `b6f3da757`, which introduced
`src/frontend/styles/_typography.scss` and migrated most — not all — call sites. Do **not**
re-declare any of them. Migrate each straggler to the live vocabulary.

Per constraint 3, every one of these is a **rendering change, not a restoration**. For each site,
state in the SUMMARY what it renders today (the inherited value) versus what it will render.
Establish "today" by reading the enclosing selector's ancestors, not by looking at a screenshot.

**Longhand `font-family:` sites — currently inherit the ancestor family:**
- `--actions-font-family` ×4 → `var(--primary-font-family)`:
  `Login/index.scss:157`, `Login/index.scss:165`,
  `DownloadManagerItem/index.css:117`, `DownloadManagerItem/index.css:140`.
  Note `--primary-font-family` is set at runtime by `GlobalState.tsx:528-542` via `setProperty`,
  so it is user-configurable — that is the correct target, not a hardcoded family.
- `--content-font-family` ×1 → `var(--secondary-font-family)`: `styles/_buttons.scss:157`.

**Longhand `font-size:` sites — currently inherit the ancestor size:**
- `--font-size-sm` ×3 → `var(--text-sm)`:
  `EditGameDialog/index.css:23`, `EditGameDialog/index.css:98`,
  `Library/components/InstallModal/SideloadDialog/index.scss:109`.
  **The todo's `files:` list omits SideloadDialog** — the planner's re-run confirms the third site
  is there. Add that path when closing the todo.

**Shorthand-named tokens — the two traps apply. Expand to longhands, do NOT invent a `font:` shorthand.**
A legal `font` shorthand requires both a size and a trailing family; the retired values supplied
neither correctly, which is why these rules have never applied. Expanding to longhands and
**omitting `font-size`** preserves today's inherited size exactly, so the delta is confined to
family and weight (and style) — the smallest honest change.

- `--font-primary-bold` ×1 — `Login/index.scss:118`, currently `font-family: var(--font-primary-bold);`.
  Replace with `font-family: var(--primary-font-family);` plus `font-weight: var(--medium);`.
  **"bold" was a slot label meaning weight 500 — use `--medium`, never `--bold`/700.**
  **Delta:** family changes from inherited to `--primary-font-family`; weight changes from
  inherited to 500. State both.
- `--font-secondary-regular` ×2 — `Settings/index.css:27`, `WineManager/index.css:186`, both
  `font: var(--font-secondary-regular);`. Replace with
  `font-family: var(--secondary-font-family); font-weight: var(--regular);`.
  **Delta:** today the entire `font` shorthand is invalid, so family, size, weight, style AND
  line-height all reset to inherited. After: family and weight become explicit; size, style and
  line-height continue to inherit unchanged.
- `--font-secondary-regular-italic` ×2 — `Settings/index.css:82`, `Settings/index.css:94`, both
  `font: var(--font-secondary-regular-italic);`. Same as above **plus** `font-style: italic;`.
  **Delta:** as above, and these two sites become italic for the first time — the italic has never
  rendered. Call that out; it is the most visible change in this task.

Commit as one atomic commit themed "migrate the last stragglers off the retired type vocabulary".
  </action>
  <verify>
    <automated>python3 .planning/quick/260912-it4-drain-the-undefined-css-custom-property-/sweep-census.py | head -1  # expect "19 references across 11 names"</automated>
    <automated>git grep -c -- '--actions-font-family\|--content-font-family\|--font-size-sm\|--font-secondary-regular\|--font-primary-bold' -- src | grep -v __tests__ | wc -l  # expect 0</automated>
    <automated>npx prettier --check "src/frontend/**/*.{css,scss}"</automated>
  </verify>
  <done>Sweep reports 19 refs / 11 names. SUMMARY has one delta line per site (13 lines), each naming the inherited "before" and the explicit "after". The two italic sites are flagged as newly-italic.</done>
</task>

<task type="auto">
  <name>Task 3: Triage the 19 colour/surface references and install the sweep gate</name>
  <files>src/frontend/themes.scss, src/frontend/components/UI/PopoverComponent/index.scss, src/frontend/components/UI/SteamGridDBPicker/index.scss, src/frontend/screens/WineManager/index.css, src/frontend/screens/WineManager/components/WineItem/index.css, src/frontend/screens/Settings/index.css, src/frontend/styles/_buttons.scss, src/frontend/screens/DownloadManager/components/DownloadManagerItem/index.css, src/frontend/screens/Library/components/GameCard/index.css, src/frontend/components/UI/Dropdown/index.scss, src/frontend/components/UI/NavShell/__tests__/cssTokenSweep.test.ts, .planning/quick/260912-it4-drain-the-undefined-css-custom-property-/sweep-census.py</files>
  <action>
**Part A — fix the census script first (one name is a phantom).**

`--token` at `PathSelectionBox/index.css:9` is **inside that file's header comment** — the prose
"every colour is a `var(--token, fallback)`" — not a reference. Do **not** edit the comment: it is
a faithful record and retyping an anchor from display copy silently no-ops. Fix the sweep instead:
strip `/* */` and `//` comments from each file before collecting both declarations and references.
Re-run; `--token` should disappear, leaving 18 real refs across 10 names.

**Part B — resolve the 18. Per-name dispositions, all evidence-backed:**

- **`--border-color` ×7** — `PopoverComponent/index.scss:6`, `:8`;
  `SteamGridDBPicker/index.scss:12`; `WineManager/index.css:30`, `:112`, `:206`;
  `WineManager/components/WineItem/index.css:113`. `git log -S "--border-color:" --all` is
  **empty** — never declared, in any revision. But 7 sites across 4 components use it like a real
  shared token, which is exactly constraint 4's "declare a default" case.
  Fix: add **one** declaration to the base `body {}` block of `themes.scss` (lines 1-20):
  `--border-color: var(--divider, var(--neutral-03));`. `--divider` is the repo's established
  divider token but is declared in only 2 of 11 theme blocks (`themes.scss:97`, `:145` — the same
  fact `themeTokens.test.ts:293` already censuses), so the `--neutral-03` fallback (the value
  `themes.scss:97` itself uses) is what makes it universal. Leave all 7 call sites untouched;
  themes can now override.
  **Delta:** 5 of the 7 sites (`border: 1px solid …`, `border-bottom: 1px solid …`,
  `box-shadow: 0 0 0 1px …`) go from **no border rendered at all** to a 1px divider-coloured
  border. State this.
  **Known residual — do not silently fix, do not silently drop:** `WineManager/index.css:206` and
  `WineItem/index.css:113` are `border-bottom: var(--border-color);` — a whole-shorthand position.
  Declaring the token makes the shorthand *valid* but `border-style` still defaults to `none`, so
  those two remain **visually inert**. That is out of this todo's scope (it is a missing
  `1px solid`, not a missing token). Record it in the SUMMARY and spin out a pending todo with
  `severity: minor`, `platform: any`, `ready: code` in that order, bare lowercase.

- **`--background-hover` ×2** — `SteamGridDBPicker/index.scss:58`,
  `DownloadManagerItem/index.css:63`. Retired from `_colors.scss` by `5e9003e39` ("[UI] New Icons
  + new Default Theme + CSS Variables Overhaul", #2384); historic value `rgb(20 20 20 / 47%)`. Do
  **not** re-add to `_colors.scss` — that file says "Do not add new colors here". Resolve to the
  hover-surface token the live siblings use (grep the same files for their other hover rules).
- **`--stop-button` ×2** — `Settings/index.css:160`, `styles/_buttons.scss:79`. Same origin,
  historic value `#252121`. Same treatment.
- **`--background-gradient` ×1** — `DownloadManagerItem/index.css:109`. Same origin.
- **`--bg-primary` ×1** — `WineManager/index.css:111` (`background: var(--bg-primary);`).
  `git log -S` empty. The sibling at `:30` uses `var(--body-background)` — resolve to that.
- **`--error` ×1** — `WineManager/index.css:160` (`color: var(--error);`). `git log -S` empty.
  Resolve to `var(--danger)`, the theme-adaptive alias — **not** `--status-danger` (constraint 1).
- **`--text` ×1** — `Dropdown/index.scss:65` (`color: var(--text);`). Resolve to
  `var(--text-default)` (base `body {}`, `themes.scss:2`).
- **`--text-muted` ×1** — `SteamGridDBPicker/index.scss:104`. Resolve to `var(--text-secondary)`.
- **`--danger-rgb` ×1** — `SteamGridDBPicker/index.scss:35`,
  `background: rgba(var(--danger-rgb), 0.1);`. **This one cannot be a rename**: `rgba()` needs an
  r,g,b triplet and no declared token is one. Prefer
  `background: color-mix(in srgb, var(--danger) 10%, transparent);` — WKWebView supports
  `color-mix` (Safari 16.2+), which is this app's renderer. **Delta:** a 10% danger-tinted error
  panel background appears where none rendered before.
- **`--installing-effect` ×1** — `GameCard/index.css:406`,
  `filter: grayscale(var(--installing-effect));`. This is a **number**, not a colour, and
  `git log -S "--installing-effect:" --all` is empty — the intended value (0? 0.5? 1?) is not
  recoverable, and guessing changes the appearance of every non-installed tile in the library
  grid. **Allowlist this one** with a comment recording exactly that reasoning, and spin out a
  pending todo (`severity: minor`, `platform: any`, `ready: human` — it needs a design decision).

For each resolution, run `git log -S "--the-name:" --all` first and record the result. If any name
turns out to have history that contradicts the disposition above, follow the evidence and say so.

**Part C — the gate.** Create
`src/frontend/components/UI/NavShell/__tests__/cssTokenSweep.test.ts`, modelled on
`themeTokens.test.ts`:
- Reuse its `read()` / `stripSourceComments` idiom. Comment stripping is **load-bearing** here —
  it is what makes `--token` a non-finding.
- Header comment must state, as `themeTokens.test.ts` does, *why this is a source-text gate*: the
  Frontend jest project is `testEnvironment: 'node'` (see `src/frontend/jest.config.js`) — no
  jsdom, no CSS engine — so this proves what the SOURCE says and can never prove what RENDERS.
- Collect declarations from git-tracked `src/**` + `public/**` `.css/.scss/.ts/.tsx/.js/.html`
  plus `setProperty('--x', …)`; collect `var(--x)` refs from `.css`/`.scss`; assert the difference
  is empty **except** for an explicit `ALLOWLIST` array.
- Each allowlist entry carries an inline comment saying why it is a legitimate extension point.
- **Rot detection is mandatory** and is the known failure mode: assert that every allowlisted name
  still appears as an undefined reference. `themeTokens.test.ts:68-99` records the case where a
  guard's own originating site escaped it and the residual survived a green suite for 14 days —
  and warns verbatim that "a gate that fails on untouched code is a gate someone deletes". A stale
  allowlist entry must turn the suite RED, not quietly pass.
- Also record the sweep's known blind spot in the header: a declaration found *anywhere* in any
  file counts as declared, so a token declared under a narrow selector (e.g.
  `--search-bar-border`, declared only inside `body.midnightMirage`) reads as "declared" while
  being unavailable on 10 themes. This gate catches missing names, not missing scopes.

`cssTokenSweep.test.ts` lands in the tests lint bucket — run `pnpm lint:tests` as well as
`pnpm lint:src`.

Commit as one atomic commit themed "resolve the colour/surface tokens and gate the sweep at 0".
  </action>
  <verify>
    <automated>python3 .planning/quick/260912-it4-drain-the-undefined-css-custom-property-/sweep-census.py | head -1  # expect "1 references across 1 names" (--installing-effect) or 0</automated>
    <automated>npx jest --selectProjects Frontend --passWithNoTests -t 'css token sweep'</automated>
    <automated>npx jest --selectProjects Frontend --passWithNoTests src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts</automated>
    <automated>pnpm lint:tests && pnpm lint:src</automated>
    <automated>npx prettier --check "src/frontend/**/*.{css,scss}" "src/frontend/**/__tests__/cssTokenSweep.test.ts"</automated>
  </verify>
  <done>Census reports 0 undefined refs, or exactly the allowlisted survivors. `cssTokenSweep.test.ts` passes and its rot check fails when an allowlist entry is stale (prove this by temporarily adding a bogus name and confirming RED, then remove it). Residual todos for the two inert `border-bottom` sites and `--installing-effect` exist in `.planning/todos/pending/` with `severity`/`platform`/`ready` in that order, bare lowercase.</done>
</task>

</tasks>

<verification>
1. `python3 .planning/quick/260912-it4-drain-the-undefined-css-custom-property-/sweep-census.py`
   → **0 undefined references**, or only names that appear on `cssTokenSweep.test.ts`'s ALLOWLIST
   with a stated reason. This is the plan's primary gate.
2. `npx jest --selectProjects Frontend --passWithNoTests` — Frontend suite green, including the
   pre-existing `themeTokens.test.ts`.
3. **Negative control for the gate:** add a `var(--definitely-not-a-token)` to any `.scss`, confirm
   `cssTokenSweep.test.ts` turns RED, revert. A gate that has never been seen to fail proves
   nothing.
4. **Negative control for the allowlist:** add a bogus name to the ALLOWLIST, confirm the rot check
   turns RED, revert.
5. `pnpm lint:src && pnpm lint:tests` and `npx prettier --check` on every touched file.
6. **Deltas.** The SUMMARY must carry one line per site whose render changes. Expected set:
   the SearchBar focus ring (10 themes), the 13 typography sites, the 5 live `--border-color`
   sites, the SteamGridDBPicker error tint, and whatever the `--background-hover` /
   `--stop-button` / `--background-gradient` / `--bg-primary` / `--error` / `--text` /
   `--text-muted` resolutions turn out to change. "No delta" is a valid line but must carry its
   reasoning (as with `--status-sucess`).
   Per this project's measure-don't-eyeball rule: the current silent-inherit fallback looks
   plausible in some themes, so "it looks fine" is not evidence. Deltas are established by reading
   what the ancestor actually declares, not by looking.

**Todo closure:** on success, move
`.planning/todos/pending/2026-09-12-39-references-to-23-css-custom-properties-declared-nowhere.md`
to `completed/`, noting the SideloadDialog path its `files:` list omitted and the two premise
corrections (`--status-sucess` must not be spelling-corrected; `--token` was never a reference).
</verification>

<success_criteria>
- Sweep reports 0 undefined refs or an allowlist where every entry names a real, still-present
  reference and states why it is a legitimate extension point.
- `cssTokenSweep.test.ts` exists in the Frontend jest project, passes, and has been *seen* to fail
  under both negative controls.
- 22 of the 23 catalogued names are gone from non-test source; the 23rd (`--token`) is gone from
  the census because the census now strips comments.
- Every render-affecting site has a stated delta in the SUMMARY.
- Any residue is a pending todo with `severity`, `platform`, `ready` in that order, bare lowercase.
- Three atomic commits, one per task.
</success_criteria>

<output>
Create `.planning/quick/260912-it4-drain-the-undefined-css-custom-property-/260912-it4-SUMMARY.md` when done.
</output>
