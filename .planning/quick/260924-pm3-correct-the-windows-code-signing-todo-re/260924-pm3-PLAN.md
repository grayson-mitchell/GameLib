---
quick_id: 260924-pm3
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md
autonomous: true
requirements: [DOC-260924-pm3]
must_haves:
  truths:
    - 'A reader of the Windows signing todo learns, before reaching `## Problem`, that the decision is SignPath Foundation and that the todo''s own Direction steps 1 and 2 are obsolete.'
    - 'A reader is told that no option gives instant SmartScreen trust, so the first signed release will still warn.'
    - 'A reader is told the base64-`.p12` credential shape the workflow implements is no longer obtainable, and that `signCommand` is the replacement.'
    - 'A reader is told which parts of `release-tauri.yml` and `tauri.windows.conf.json` were audited and found CORRECT, so they do not re-audit them.'
    - 'A reader is warned that SignPath signs post-build and that this collides with `createUpdaterArtifacts: true`, with the required build/sign/re-sign/upload ordering written down.'
    - 'Every pre-existing section of the todo (Problem, Current behaviour, Related, Carried residual) survives unchanged, and the four existing Verification bullets survive verbatim.'
    - '`pnpm planning-gates` stays green and the file stays prettier-clean.'
  artifacts:
    - path: .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md
      provides: 'Corrected Windows code-signing todo: SignPath decision, four findings, rewritten Direction, added updater verification bullet'
      contains: '## STATUS 2026-09-24 (quick 260924-pm3)'
  key_links:
    - from: 'todo frontmatter `needs:`'
      to: 'the rewritten Direction step 1'
      via: 'both name the SignPath Foundation application as the blocking human step'
      pattern: 'needs: signpath-foundation-application-then-verify'
---

<objective>
Correct `.planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md`
so it records the 2026-09-24 operator decision (SignPath Foundation) and the four findings measured
that day, two of which invalidate its stated Direction.

Purpose: the todo currently tells a future reader to make a decision that has no correct answer
(OV vs EV, where EV's instant-SmartScreen advantage was removed in 2024) and then to perform a step
that is impossible (export a `.p12`, which the CA/Browser Forum's June 2023 HSM requirement
forbids). Left as-is, whoever picks this up spends the first hour discovering that, and the
post-build-signing/updater-signature collision — the part most likely to ship a silently-broken
auto-update — is recorded nowhere at all.

Output: one edited markdown file. No source or config file is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md

Conventions that bind this plan, both from CLAUDE.md:

- **Todo triage frontmatter is CI-enforced** by `.planning/todos/todo-frontmatter-gate.py` over
  `pending/` only. `severity`, `platform` and `ready` are matched bare, lowercase and exact. This
  plan changes none of those three values — but any accidental requoting or recasing of them turns
  CI red, so the gate is in both tasks' verify blocks.
- **A formatter check belongs in every task's `<verify>`**, scoped to the exact paths written,
  never a bare `.`. Measured three times on this repo: the whole green battery (`codecheck`,
  `lint`, jest, `planning-gates`) can pass over an unformatted file.

Measured while planning, so the executor does not have to:

- `npx prettier --check` over the target file passes TODAY. Any failure after editing is the
  executor's own edit, not pre-existing debt.
- `python3` is NOT on PATH on this Windows box; `python` is (3.12.10). The gate script takes no
  arguments — it globs `pending/*.md` itself — so run it as
  `python .planning/todos/todo-frontmatter-gate.py` from the repo root. `pnpm planning-gates`
  invokes it via `python3` and will fail for the wrong reason here.

The file being edited is 113 lines. The formatting precedent to match is the macOS half,
`.planning/todos/pending/2026-09-04-macos-releases-ship-unsigned-and-unnotarized.md`, which stacks
three `## STATUS <date>` sections above `## Problem`; each opens by stating it does not revise the
sections above or below it, and none rewrites history.
</context>

<scope_fence>
DOCUMENT CORRECTION ONLY. Exactly one file may be modified:
`.planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md`.

Do NOT edit `.github/workflows/release-tauri.yml`, `src-tauri/tauri.conf.json`,
`src-tauri/tauri.windows.conf.json`, `promote-updater-feed.yml`, or any other source or config
file. Do NOT create a new todo file. Do NOT purchase, enrol in, or apply for anything. Do NOT
re-research any finding below — all of it was measured read-only on 2026-09-24 and is established
fact.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Retarget `needs:` and insert the STATUS 2026-09-24 section with all four findings</name>
  <files>.planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md</files>
  <action>
Two edits to the one file.

**Edit A — frontmatter.** Change `needs:` from `certificate-purchase-then-verify` to
`signpath-foundation-application-then-verify`. Leave `severity: major`, `platform: windows` and
`ready: human` exactly as they are — bare, lowercase, unquoted; the SignPath application is
identity-bound to the maintainer and cannot be automated, so `ready: human` is still correct. Leave
`created`, `title`, `area`, `found_by`, `source`, `status` and `files` untouched.

**Edit B — insert a new section headed `## STATUS 2026-09-24 (quick 260924-pm3)` immediately ABOVE
the existing `## Problem` heading.** Insert; do not rewrite anything that follows. Open the section
with a sentence stating plainly that it does not revise any section below it — the Problem and
Current behaviour measurements stay true as history of what was believed and measured then — which
is how the macOS half's STATUS sections stack. Then record the decision and four findings below.
Prose may be improved; no finding may be softened or dropped.

**Operator decision, made 2026-09-24:** pursue SignPath Foundation — free OV-level code signing for
qualifying open-source projects, delivered through a managed signing pipeline. The operator is an
individual developer outside the USA and Canada, which independently rules out Azure Artifact
Signing, whose individual tier is limited to the USA and Canada. Fallback if SignPath declines: a
traditional OV certificate from a commercial CA.

**Finding 1 — the todo's own blocking decision is obsolete.** "Why it matters" item 2 says an EV
certificate "gets it immediately" and calls OV-vs-EV "the actual cost decision". Microsoft's
code-signing-options doc (https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options,
ms.date 2026-08-29) states that EV's instant-SmartScreen-bypass "behavior was removed in 2024",
that EV-signed files "now go through the same reputation-building process as OV certificates", and
that paying the EV premium solely to avoid SmartScreen is "no longer justified"; its comparison
table marks OV and EV identically for SmartScreen. So Direction step 1 as written has no correct
answer — the real decision is which ISSUER, not which validation tier. Record the consequence that
governs expectations: NO option buys instant SmartScreen trust. Reputation accrues per consistent
publisher identity across releases, so the FIRST signed release will still warn. Signing is worth
doing for that accrual and for the publisher name appearing in the prompt — not as an immediate fix.

**Finding 2 — Direction step 2 is unachievable as written, and this IS the Windows analogue that
step 4 told us to look for.** Same Microsoft doc: "As of June 2023, the CA/Browser Forum requires
private keys for OV certificates to be stored on a hardware security module (HSM) or hardware
token." So "export as `.p12`/`.pfx`, base64-encode it" is not possible for any certificate issued
today. The entire implemented `WINDOWS_CERTIFICATE` → `Import-PfxCertificate` →
`certificateThumbprint` path (`release-tauri.yml:354-367`, plus the `--config` override at
`:409-430`) is dead on arrival for a 2026 acquisition. State the shape difference from the macOS
half, because it changes how to think about it: macOS was a LATENT DEFECT ARMED BY ENROLMENT
(hardened runtime implied by signing, missing JIT entitlement); this is an IMPLEMENTED PATH THAT NO
LONGER MATCHES HOW CERTIFICATES ARE ISSUED. Then state plainly that Direction step 4's "No workflow
edits are expected" is now measurably FALSE for every remaining option — the same way the macOS
half's "No code changes" was. Tauri v2 documents `bundle.windows.signCommand` for exactly this
case; this repo has never used it.

**Finding 3 — workflow audit: what is actually CORRECT.** Record this so a future reader does not
re-audit it. Verified read-only: the three-branch gate at `release-tauri.yml:409-430` and the
cert-import gate at `:354` both require all three Windows secrets; no branch calls `exit 1`; D-04's
"green on any secret combination" invariant holds. `src-tauri/tauri.windows.conf.json` carries only
`bundle.resources` and declares NO `certificateThumbprint`, so the D-04 anti-pattern guard is
intact — there is no committed-thumbprint analogue to the macOS entitlements gap. State the
conclusion explicitly: the defect is not in what the workflow DOES, it is that the credential SHAPE
it was built for is no longer obtainable.

**Finding 4 — NEW, previously unrecorded: SignPath signs POST-BUILD, which collides with
`createUpdaterArtifacts: true`.** SignPath Foundation's model is submit-artifact / sign / retrieve,
not an in-build `signCommand`. But `src-tauri/tauri.conf.json` sets `createUpdaterArtifacts: true`,
so `tauri build` minisigns the updater artifact over the UNSIGNED installer bytes — and
`.github/workflows/release-tauri.yml` uses `tauri-apps/tauri-action@v1` (`:557`), which BUILDS AND
UPLOADS to the draft release in ONE step, leaving no seam between build and publish for a post-build
signer. Publishing the build's original `.sig` alongside a SignPath-signed installer would ship a
valid-looking updater signature over the wrong bytes, and `promote-updater-feed.yml` would then
promote that `latest.json`. Write down the required ordering, to be designed once the SignPath
account exists: build (no upload) → SignPath signs the installer → DELETE the stale
`*-setup.exe.sig` and ASSERT its absence → re-run `tauri signer sign` over the signed bytes → only
then upload. Flag that this is a RESTRUCTURE of the `tauri-action` step, not a config tweak, and
that it is the part most likely to ship a silently-broken auto-update — a failure no green build
would reveal.
  </action>
  <verify>
    <automated>cd "C:/Users/grays/Projects/GameLib" &amp;&amp; npx prettier --check ".planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md" &amp;&amp; python .planning/todos/todo-frontmatter-gate.py &amp;&amp; grep -qx 'needs: signpath-foundation-application-then-verify' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx 'severity: major' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx 'platform: windows' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx 'ready: human' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx '## STATUS 2026-09-24 (quick 260924-pm3)' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx '## Problem' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx '## Current behaviour' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx '## Carried residual (2026-09-22, quick 260922-txw)' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -q 'createUpdaterArtifacts' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -q 'signCommand' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -q 'removed in 2024' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -q 'hardware security module' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md</automated>
  </verify>
  <done>
`needs:` reads `signpath-foundation-application-then-verify`; the three CI-enforced triage keys are
unchanged and still bare/lowercase; a `## STATUS 2026-09-24 (quick 260924-pm3)` section sits above
`## Problem` carrying the operator decision and all four findings; `## Problem`,
`## Current behaviour` and `## Carried residual (2026-09-22, quick 260922-txw)` all still exist
(proving insert-not-rewrite); prettier and the frontmatter gate are both green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Direction to the SignPath route and add the updater verification bullet</name>
  <files>.planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md</files>
  <action>
Two edits to the same file. Nothing outside the `## Direction` section and the
`## Verification — a green build proves nothing` section may change.

**Edit A — replace the four numbered steps under `## Direction` with three:**

1. Apply to SignPath Foundation at https://signpath.org/apply. Manual review, typically 1-2 weeks,
   possibly with follow-up questions; the application is identity-bound to the maintainer and cannot
   be automated. Eligibility conditions to check first: an OSI-approved open-source licence with no
   commercial dual-licensing for any component (GameLib is `GPL-3.0-only` per `package.json`, public
   repo — plausibly qualifying); no proprietary or non-open-source component; actively maintained;
   already released in the form that should be signed; functionality described on the download page;
   2FA enabled on the GitHub account.
2. On approval, design the build → sign → re-sign → upload ordering from Finding 4 BEFORE wiring any
   credentials. Getting credentials working first and the ordering second is how a broken updater
   ships.
3. Fall back to an OV certificate from a commercial CA — $150-300/yr, USB token or cloud HSM
   required, and still needing `signCommand` rather than the base64-`.p12` path — only if SignPath
   declines.

Then keep a short closing note: whichever route is taken, the three currently-absent secret names
recorded in the Problem section may no longer be the right secret SHAPE. The Problem section's
measurement that they are absent stays true as history; do not reword it.

**Edit B — under `## Verification — a green build proves nothing`, keep the four existing bullets
VERBATIM (`signtool verify /pa /v`, `Get-AuthenticodeSignature`, the no-`::warning::WINDOWS_CERTIFICATE`
skip-line check, and the browser-download/mark-of-the-web bullet) and ADD one more bullet:** after
the first signed release, verify that the published `*-setup.exe.sig` verifies against the SIGNED
installer bytes, and that an actual in-app update from the prior version completes. A signature that
merely exists proves nothing — this is exactly the failure Finding 4 describes.

Leave `## Why it matters`, `## Related` and `## Carried residual (2026-09-22, quick 260922-txw)`
alone. Why it matters item 2 is now contradicted by Finding 1, and that is fine: Finding 1 names it
by reference, in the STATUS section, rather than rewriting it — same convention as the macOS half,
whose superseded Direction claim is struck through and pointed at its STATUS section instead of
being deleted.
  </action>
  <verify>
    <automated>cd "C:/Users/grays/Projects/GameLib" &amp;&amp; npx prettier --check ".planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md" &amp;&amp; python .planning/todos/todo-frontmatter-gate.py &amp;&amp; grep -q 'signpath.org/apply' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -q 'GPL-3.0-only' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -q 'setup.exe.sig' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -q 'signtool verify /pa /v' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -q 'Get-AuthenticodeSignature' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -q 'zone-identifier mark-of-the-web' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx '## Problem' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx '## Current behaviour' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx '## Related' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -qx '## Carried residual (2026-09-22, quick 260922-txw)' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; grep -q '2026-09-22-windows-packaged-build-breaks-on-darwin-runner-symlinks.md' .planning/todos/pending/2026-09-14-windows-releases-ship-unsigned-no-windows-cert-enrolled.md &amp;&amp; test "$(git -C . diff --name-only | wc -l)" -eq 1</automated>
  </verify>
  <done>
`## Direction` carries the three SignPath-route steps plus the secret-shape note; the Verification
section carries its four original bullets plus the new updater bullet; Problem, Current behaviour,
Related and Carried residual (with its darwin-symlinks closing paragraph) all survive; prettier and
the frontmatter gate are green; `git diff --name-only` lists exactly ONE changed file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                     | Description                                                                  |
| ---------------------------- | ---------------------------------------------------------------------------- |
| none crossed by this plan    | Markdown-only edit inside `.planning/`; no code, config, network or credential |

## STRIDE Threat Register

| Threat ID       | Category   | Component                              | Disposition | Mitigation Plan                                                                                                                                                                       |
| --------------- | ---------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-260924-pm3-01 | Repudiation | the todo's historical STATUS/Problem record | mitigate | A rewrite-instead-of-insert would erase measured history. Both tasks grep for `## Problem`, `## Current behaviour`, `## Related` and `## Carried residual (2026-09-22, quick 260922-txw)`, and Task 2 asserts exactly one changed file. |
| T-260924-pm3-02 | Tampering  | CI triage gate over `pending/`           | mitigate | `severity`/`platform`/`ready` must stay bare and lowercase; both tasks assert them with `grep -qx` and run `.planning/todos/todo-frontmatter-gate.py`.                                 |
| T-260924-pm3-03 | Tampering  | release pipeline files                   | mitigate | Scope fence forbids editing `release-tauri.yml`, `tauri.conf.json`, `tauri.windows.conf.json`; Task 2 verifies the diff touches one file only.                                         |
| T-260924-pm3-SC | Tampering  | npm/pip/cargo installs                   | accept      | No package install occurs. `npx prettier` resolves the repo's already-installed devDependency; no new dependency is added.                                                              |
</threat_model>

<verification>
- `npx prettier --check` over the exact edited path passes (never a bare `.` — `src/preload/.prettierrc` sets a different `printWidth`, so directory matters).
- `python .planning/todos/todo-frontmatter-gate.py` passes from the repo root (`python3`, which `pnpm planning-gates` invokes, is not on PATH on this box and will fail for the wrong reason).
- `git diff --name-only` lists exactly one file.
- Read the finished file top to bottom once and confirm the STATUS section reads as an addition to history, not a replacement of it.
</verification>

<success_criteria>
- `needs:` retargeted to `signpath-foundation-application-then-verify`; the three CI-enforced triage keys unchanged.
- A `## STATUS 2026-09-24 (quick 260924-pm3)` section above `## Problem` recording the SignPath decision, the Azure-individual-tier exclusion, the commercial-CA fallback, and all four findings — including the explicit statements that no option gives instant SmartScreen trust and that "No workflow edits are expected" is now measurably false.
- `## Direction` rewritten to the three SignPath-route steps with the ordering-before-credentials warning.
- Verification section: four original bullets verbatim, plus the updater-signature bullet.
- Problem, Current behaviour, Related and Carried residual preserved.
- Prettier and the todo frontmatter gate green; exactly one file changed.
</success_criteria>

<output>
Create `.planning/quick/260924-pm3-correct-the-windows-code-signing-todo-re/260924-pm3-SUMMARY.md` when done.
</output>
