---
phase: quick-260912-d84
plan: 01
subsystem: ui
tags: [css, humble-keys, grid, css-custom-properties, source-census-test]

requires: []
provides:
  - "Font-size-independent shared column-gap on the Humble Keys .humbleKeysColumnHeader, .humbleKeyRow declaration"
  - "Block-scoped source-census guard (with 4 SANITY controls + 1 stripSourceComments integrity test) pinning the fixed token"
  - "Closed todo recording the established cause, the fix, and an explicit outstanding live re-measurement"
affects: [humble-keys-ui, REQ-43-19]

tech-stack:
  added: []
  patterns:
    - "Font-size-independent (fixed) CSS length tokens on selectors shared by elements with different computed font-sizes"

key-files:
  created: []
  modified:
    - src/frontend/screens/Humble/Keys/index.css
    - src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts
    - .planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md (removed, moved to completed/)
    - .planning/todos/completed/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles-unverified-live.md (created)

key-decisions:
  - "Used --space-md-fixed (already the house instrument for this exact class of problem, used elsewhere in this file and 3 other screens) instead of introducing a new token."
  - "Closed the todo with an -unverified-live filename marker rather than leaving it open, per the 4 existing precedents in completed/, because the source-side fix and its regression guard are both landed even though the live measurement is not."

requirements-completed: []

duration: 35min
completed: 2026-09-13
---

# Quick Task 260912-d84: Align the Humble Keys GAME column header Summary

**Fixed a font-size-dependent `em` column-gap on Humble Keys' shared row/header grid declaration by swapping it for a fixed-length token, closing an arithmetic 4.89px header-vs-row divergence — verified by source-census test only, not by a live render.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-09-12T16:20:00Z (approx)
- **Completed:** 2026-09-13
- **Tasks:** 3/3 completed
- **Files modified:** 2 code files (index.css, humbleKeysStylesheet.test.ts) + 1 todo moved

## Accomplishments

- Changed the combined `.humbleKeysColumnHeader, .humbleKeyRow` declaration's `column-gap` from the em-relative `--space-md` to the fixed `--space-md-fixed`, so the header's gap (previously 11.11px, resolved against its own `--text-xs` font-size) now matches the rows' gap (16px) exactly. Rows are pixel-unchanged.
- Added a block-scoped source-census test with four SANITY controls plus one `stripSourceComments` integrity test, and **proved the guard is load-bearing** by reverting the CSS line, observing a genuine assertion failure, and restoring it.
- Closed the originating todo with an honest `-unverified-live` record: cause established, fix shipped and tested, live measurement explicitly still outstanding.

## Task Commits

1. **Task 1: Make the shared column gap font-size-independent** - `7effab193` (fix)
2. **Task 2: Pin the fixed gap with a block-scoped census and its negative controls** - `864fb65b8` (test)
3. **Task 3: Close the todo with an honest unverified-live record** - `16ec08de3` (docs)

_No separate plan-metadata commit — this quick task's SUMMARY/STATE commit is handled by the orchestrator per the execution constraints._

## Files Created/Modified

- `src/frontend/screens/Humble/Keys/index.css` - Combined `.humbleKeysColumnHeader, .humbleKeyRow` selector's `column-gap` changed from `var(--space-md)` to `var(--space-md-fixed)`, with a rationale comment explaining the em-vs-fixed mechanism.
- `src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts` - New describe block `'Humble Keys shared row/column-header gap is font-size-independent (REQ-43-19 item 3, 260912-d84)'` with 1 positive assertion + 3 SANITY controls; 1 new `stripSourceComments` integrity test; updated the pre-existing standalone-block SANITY fixture to match the shipped token.
- `.planning/todos/completed/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles-unverified-live.md` - Todo moved from `pending/`, rewritten with the established cause, the fix, and a `## NOT verified live` section.

## Decisions Made

- Followed the plan's prescribed fix exactly: `--space-md` → `--space-md-fixed` on the one shared declaration, no new token, no per-selector split.
- No architectural changes were needed or made; this stayed a single-line CSS token swap plus its test guard.

## Deviations from Plan

None — plan executed exactly as written. `prettier --write` was run on the test file after adding new tests (its own formatting choices for wrapped lines/quotes), which is normal prettier idempotence, not a content deviation; the file still passes `prettier --check` and all verification steps.

## Load-Bearing Guard Proof (Task 2, required by plan)

Reverted `index.css` line 241 from `column-gap: var(--space-md-fixed);` back to `column-gap: var(--space-md);`, then ran:

```
npx jest --selectProjects Frontend src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts
```

Observed failure (verbatim from jest output):

```
FAIL src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts
  ● Humble Keys shared row/column-header gap is font-size-independent (REQ-43-19 item 3, 260912-d84) › the shared .humbleKeysColumnHeader, .humbleKeyRow declaration's column-gap uses the fixed token, not the em-relative one

    expect(received).toMatch(expected)

    Expected pattern: /column-gap:\s*var\(--space-md-fixed\)/
    Received string:  "
      grid-template-columns: 6.5rem minmax(0, 1fr) 20rem;
      display: grid;···
      column-gap: var(--space-md);
      align-items: start;
    "

      352 |     const match = HUMBLE_KEYS_SHARED_ROW_GEOMETRY_BLOCK.exec(stripped)
      353 |     expect(match).not.toBeNull()
    > 354 |     expect(match?.[1]).toMatch(COLUMN_GAP_FONT_SIZE_INDEPENDENT)
          |                        ^
      355 |     expect(match?.[1]).not.toMatch(COLUMN_GAP_EM_RELATIVE)
      356 |   })

Test Suites: 1 failed, 158 passed, 159 total
Tests:       1 failed, 2508 passed, 2509 total
```

This is a genuine assertion failure (regex mismatch on the block body), not a file-not-found or a zero-match run. Restored `index.css` to `var(--space-md-fixed)` immediately after and re-ran the full Humble Keys suite plus `prettier --check` to confirm the repo returned to the all-green state (`git diff --stat src/frontend/screens/Humble/Keys/index.css` showed no residual diff against the Task 1 commit).

## Honest Verification Statement (required by plan)

**This fix is verified by arithmetic and by source-census test only. It has NOT been verified by a live measurement.** No test in this repository's Frontend jest project can measure a computed CSS width — `testEnvironment: 'node'` provides no jsdom and no CSS engine, so the new tests can only confirm that `column-gap: var(--space-md-fixed)` was written inside the correct declaration's source text. They do not and cannot confirm that this produces a 16px rendered gap, or that the GAME header label visually moves 4.89px to meet the row titles.

**The one live check the next REQ-43-19 run should make:** re-measure the `GAME` column header label's ink left edge against the row titles' ink left edges (same method that previously produced 335.5 vs 341.0), and confirm the offset falls from **5.5 CSS px to within the ±2 threshold**. TYPE (previously 0.0) and KEY (previously 0.5) should also be spot-checked to confirm they remain near-zero, since this fix does not touch either of their tracks.

## Verification Run Log

- `npx jest --selectProjects Frontend src/frontend/screens/Humble/Keys` — 159/159 suites, 2509/2509 tests passed (full Frontend project; the path argument did not narrow the run, but all suites including the two directly relevant ones — `humbleKeysStylesheet.test.ts` and `HumbleKeyRow/__tests__/index.test.tsx` — passed).
- Raw-source census: `grep -c 'grid-template-columns' src/frontend/screens/Humble/Keys/index.css` = 2 (unchanged).
- `pnpm lint` — exit 0, 0 errors, 638 pre-existing warnings repo-wide (none in the two changed files); `production: PASS | tests: PASS` (both ceilings held).
- `npx tsc --noEmit -p tsconfig.json` — clean, no output.
- `npx prettier --check` on both changed source files — clean (after one `--write` pass to match the new test file to house style).
- `pnpm planning-gates` — 10/10 gates passed, including the todo frontmatter gate.

## Known Stubs

None.

## Threat Flags

None. Per the plan's threat model, no trust boundary is crossed by this change — a CSS length token swap, jest assertions over local source text, and a planning-markdown file move.

## Self-Check: PASSED

- `src/frontend/screens/Humble/Keys/index.css` — FOUND, contains `column-gap: var(--space-md-fixed);` exactly once, no `column-gap: var(--space-md);` remains.
- `src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts` — FOUND, contains the new describe block and `--space-md-fixed`.
- `.planning/todos/completed/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles-unverified-live.md` — FOUND.
- `.planning/todos/pending/2026-09-11-humble-keys-game-column-header-label-sits-5px-left-of-row-titles.md` — CONFIRMED ABSENT.
- Commit `7effab193` — FOUND in `git log --oneline`.
- Commit `864fb65b8` — FOUND in `git log --oneline`.
- Commit `16ec08de3` — FOUND in `git log --oneline`.
