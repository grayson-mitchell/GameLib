---
phase: quick-260918-a1a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/todos/pending/2026-09-15-claude-md-gsd-region-source-markers-are-unreliable-and-the-architecture-region-is-false.md
  - .planning/todos/completed/2026-09-15-claude-md-gsd-region-source-markers-are-unreliable-and-the-architecture-region-is-false.md
autonomous: true
requirements: [TODO-260915-CLAUDEMD-MARKERS]
quick_id: 260918-a1a

must_haves:
  truths:
    - "An agent reading CLAUDE.md is no longer told architecture is unmapped with no pointer; it is told a 391-line Humble/Electron-era study exists, where it is, and why its scope is limited."
    - "An agent reading CLAUDE.md learns the GSD regions are hand-maintained and `source:` is provenance, not live generation, before reaching the first region."
    - "Every `source:` marker either resolves to a real repo-relative path or declares it has no source document."
    - "No `source:` marker names a file that does not source its region."
    - "The three CI-relevant conventions (todo-triage vocabulary, two-profile rule, sidecar exit contract) are byte-identical to their pre-edit text."
    - "The governing todo lives in completed/ with status completed and a per-item record of what was done, including why item 4 needed no edit."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected architecture region, nominal-marker note, repo-relative source markers"
      contains: ".planning/research/ARCHITECTURE.md"
    - path: ".planning/todos/completed/2026-09-15-claude-md-gsd-region-source-markers-are-unreliable-and-the-architecture-region-is-false.md"
      provides: "Closed todo with four-item closure record"
      contains: "status: completed"
  key_links:
    - from: "CLAUDE.md architecture region"
      to: ".planning/research/ARCHITECTURE.md"
      via: "explicit repo-relative path in prose and in the source: marker"
      pattern: "\\.planning/research/ARCHITECTURE\\.md"
    - from: "CLAUDE.md skills region marker"
      to: ".claude/skills/"
      via: "source: marker"
      pattern: "source:\\.claude/skills/"
---

<objective>
Fix three things in `CLAUDE.md` that mislead every agent session, then close the governing todo
honestly.

Purpose: `CLAUDE.md:277` currently states "Architecture not yet mapped" while a 391-line
architecture study sits one directory away — misinformation served on every session load. Five
`source:` markers are bare filenames that resolve nowhere predictable, and one of them
(`conventions`) names a file containing none of its region's content.

Output: an edited `CLAUDE.md` whose every changed line is accounted for, and a closed todo in
`.planning/todos/completed/`.

Non-goal: this is a docs change. Do not touch `.planning/spikes/CONVENTIONS.md`, do not add
`archify` to the skills list, do not alter the three conventions in the conventions region.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/todos/pending/2026-09-15-claude-md-gsd-region-source-markers-are-unreliable-and-the-architecture-region-is-false.md

**Measured facts this plan is built on** (orchestrator, 2026-09-18 at `63e140d03`; the todo
predates them and where they disagree, these win):

| fact | measurement |
| --- | --- |
| `.planning/research/ARCHITECTURE.md` is NOT this app's architecture | titled `# Architecture Research — Humble Bundle Integration`; scope "key-management overlay on an existing multi-store ELECTRON launcher"; researched 2026-07-05 |
| It predates the current shell | Rust/Tauri shell landed 2026-07-20 in `83dc57a76` — 15 days later |
| `archify` is third-party | `skills-lock.json` pins `tt-a1i/archify`, sourceType `github`; 192 files / 7.3 MB vendor output. Teaches nothing about GameLib. Its absence from the region is DELIBERATE AND CORRECT |
| Nothing regenerates these regions | `gsd-sdk` has no reference to the markers; `get-shit-done-cc` not installed; conventions hand-written in `c33f98771`, `d7d021a05`, `67ed8767b` |
| `.planning/spikes/CONVENTIONS.md` | 144 lines titled "Spike Conventions" — contains NONE of the conventions region's content |

**Current marker layout in `CLAUDE.md`** (line numbers at `63e140d03`, will shift after Task 1):

```
1:<!-- GSD:project-start source:PROJECT.md -->            103:<!-- GSD:conventions-start source:CONVENTIONS.md -->
18:<!-- GSD:stack-start source:research/STACK.md -->      273:<!-- GSD:architecture-start source:ARCHITECTURE.md -->
281:<!-- GSD:skills-start source:skills/ -->              289:<!-- GSD:workflow-start source:GSD defaults -->
305:<!-- GSD:profile-start -->
```

Seven start/end pairs. `workflow` and `profile` markers are OUT OF SCOPE — do not touch them.

**Two planner decisions, with reasoning, that the executor must implement as written:**

1. **The `conventions` marker becomes `source:hand-maintained`, NOT a path.** The orchestrator's
   item-3 mapping offered `.planning/spikes/CONVENTIONS.md` with an explicit caution. Writing that
   path would commit the exact defect this task exists to fix: a marker naming a file that does not
   source the region. The region has no source document — the three conventions were typed straight
   into `CLAUDE.md` in three named commits. `source:hand-maintained` is the only true value
   available. The new note at the top of the file defines what that value means.

2. **The nominal-semantics note goes ABOVE the first region marker, outside every region.** Inside
   a region it would be one more hand-maintained claim of the kind it is describing, and the
   workflow/profile regions are explicitly out of scope. Above line 1 it is the first thing an
   agent reads, and it sits outside any block a future tool might rewrite.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight controls, then edit CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Run the pre-flight controls FIRST and record their output — they decide how to read the diff later.

(a) Negative control for prettier. The working tree must be clean for `CLAUDE.md` before editing;
confirm with `git status --porcelain -- CLAUDE.md` (expect empty). Then run
`npx prettier --check CLAUDE.md` and RECORD the result. If the baseline file is already
prettier-clean, any prettier change in Task 2 was caused by our lines. If it is already NOT clean,
say so in the report — it means Task 2's prettier run will legitimately rewrap pre-existing lines
and the diff audit must be read with that known.

(b) Check nothing pins CLAUDE.md's content. Run
`grep -rn "CLAUDE\.md" meta/ src/ --include='*.py' --include='*.cjs' --include='*.ts' -l ; true`
(note the `; true` — `grep` exits 1 on zero matches and would break a `&&` chain on this BSD
userland). If any gate or test reads CLAUDE.md, open it and confirm our edits do not trip it BEFORE
editing. Report what you found either way.

(c) Re-measure the architecture doc's scope rather than inheriting the orchestrator's numbers.
Run `grep -ci electron .planning/research/ARCHITECTURE.md ; grep -ci tauri .planning/research/ARCHITECTURE.md ; true`
and use YOUR measured counts in the prose below. If they differ from 13 and 0, use yours and note
the discrepancy in the report.

Now make exactly five edits with the Edit tool. No other line of `CLAUDE.md` may change.

EDIT 1 — insert above line 1, before `<!-- GSD:project-start ... -->`, followed by one blank line:

> **These `<!-- GSD:*-start source:... -->` regions are hand-maintained.** The `source:` marker is
> historical provenance, not live generation. No tool reads or rewrites these regions today —
> `gsd-sdk` contains no reference to the markers and `get-shit-done-cc` is not installed — and the
> conventions below were hand-written straight into this file (`c33f98771`, `d7d021a05`,
> `67ed8767b`). Edit the regions here, directly; content written into a named source file will not
> propagate. A region marked `source:hand-maintained` has no source document at all.

EDIT 2 — the architecture region body. Replace the single line
`Architecture not yet mapped. Follow existing patterns found in the codebase.`
with (substituting your measured counts from step (c)):

No whole-app architecture is mapped. That is the honest state, not a placeholder awaiting a tool.

The repo's one architecture study is `.planning/research/ARCHITECTURE.md` (391 lines, researched
2026-07-05) — read it, but read it in scope. It is titled _Architecture Research — Humble Bundle
Integration_ and describes a key-management overlay on an **Electron** launcher. It predates the
Rust/Tauri shell, which landed 2026-07-20 in `83dc57a76`, by fifteen days: it mentions Electron
{N} times and Tauri {M}. Treat its process, IPC and packaging model as pre-rearchitecture.

For current structure, orient with `graphify query` (see the graphify section at the end of this
file) and follow the patterns already in the codebase.

EDIT 3, 4, 5 — the marker lines. Change ONLY the `source:` value on each; leave the rest of the
comment byte-identical:

- `<!-- GSD:project-start source:PROJECT.md -->` → `source:.planning/PROJECT.md`
- `<!-- GSD:stack-start source:research/STACK.md -->` → `source:.planning/research/STACK.md`
- `<!-- GSD:conventions-start source:CONVENTIONS.md -->` → `source:hand-maintained`
- `<!-- GSD:architecture-start source:ARCHITECTURE.md -->` → `source:.planning/research/ARCHITECTURE.md`
- `<!-- GSD:skills-start source:skills/ -->` → `source:.claude/skills/`

(That is five marker edits; group them however the Edit tool suits.)

DO NOT touch `source:GSD defaults`, the profile region, any `-end` marker, the skills list (item 4
closes with no edit — `archify` is third-party vendor content and stays out), or any line between
`GSD:conventions-start` and `GSD:conventions-end`.

If you need to start over, restore with
`cp /private/tmp/claude-501/-Users-graysonmitchell-Projects-GameLib/de4e5ce0-5c62-42b9-a52b-51d32b1b037d/scratchpad/CLAUDE.md.preedit CLAUDE.md`
(sha1 `3608d7400266ac2e92fc0ebc9ad54b339acb201d`). NEVER `git checkout --` (fires the repo's
post-checkout hook) and NEVER `git stash` (strands concurrent sessions).
  </action>
  <verify>
  <automated>
cd /Users/graysonmitchell/Projects/GameLib
# 1. The three conventions are untouched: extract the region and compare to the pre-edit copy.
PRE=/private/tmp/claude-501/-Users-graysonmitchell-Projects-GameLib/de4e5ce0-5c62-42b9-a52b-51d32b1b037d/scratchpad/CLAUDE.md.preedit
awk '/GSD:conventions-start/,/GSD:conventions-end/' "$PRE"      | tail -n +2 > /tmp/conv.pre
awk '/GSD:conventions-start/,/GSD:conventions-end/' CLAUDE.md   | tail -n +2 > /tmp/conv.post
diff /tmp/conv.pre /tmp/conv.post && echo "CONVENTIONS-BODY-IDENTICAL"
# 2. No bare/legacy markers survive; all five new values present.
grep -n 'source:' CLAUDE.md
# 3. The false sentence is gone.
grep -c 'Architecture not yet mapped' CLAUDE.md ; true   # MUST print 0
  </automated>
  </verify>
  <done>`diff` prints `CONVENTIONS-BODY-IDENTICAL`; the `source:` listing shows exactly `.planning/PROJECT.md`, `.planning/research/STACK.md`, `hand-maintained`, `.planning/research/ARCHITECTURE.md`, `.claude/skills/`, `GSD defaults` (unchanged); `Architecture not yet mapped` count is 0.</done>
</task>

<task type="auto">
  <name>Task 2: Format, audit every changed line, run gates</name>
  <files>CLAUDE.md</files>
  <action>
Snapshot before formatting so prettier's contribution is isolable:
`cp CLAUDE.md "$SCRATCH/CLAUDE.md.postedit-preprettier"` (use the session scratchpad path from
Task 1's restore path; do not use `/tmp` for the snapshot).

Run `npx prettier --write CLAUDE.md`, then `diff "$SCRATCH/CLAUDE.md.postedit-preprettier" CLAUDE.md`.
Every hunk prettier produces must fall inside a line you wrote in Task 1. Prettier rewraps tables
and prose — if it touched a pre-existing line (a stack/conventions table row, an untouched
paragraph), that is a REPORT-not-commit condition per the task constraints. Say so plainly and
stop before committing.

Then audit the full diff against the expected inventory. Exactly SIX lines should be deleted:

1. `<!-- GSD:project-start source:PROJECT.md -->`
2. `<!-- GSD:stack-start source:research/STACK.md -->`
3. `<!-- GSD:conventions-start source:CONVENTIONS.md -->`
4. `<!-- GSD:architecture-start source:ARCHITECTURE.md -->`
5. `<!-- GSD:skills-start source:skills/ -->`
6. `Architecture not yet mapped. Follow existing patterns found in the codebase.`

Any seventh deleted line means the change is wider than intended — investigate and report it
rather than committing it.

Then verify marker pairing (7 pairs) and run `pnpm planning-gates`, reporting the pass count.
Expected 11/11. `planning-gates` runs in CI, so a red here is a red on push.
  </action>
  <verify>
  <automated>
cd /Users/graysonmitchell/Projects/GameLib
# A. Exactly six deletions, and they are the expected six.
git diff -U0 -- CLAUDE.md | grep '^-' | grep -v '^---' | sed 's/^-//' > /tmp/deleted.txt
wc -l < /tmp/deleted.txt          # MUST be 6
cat /tmp/deleted.txt              # MUST match the inventory above, line for line
# B. Marker pairing: 7 starts, 7 ends, same names in the same order.
grep -o 'GSD:[a-z]*-start' CLAUDE.md | sed 's/-start//' > /tmp/starts.txt
grep -o 'GSD:[a-z]*-end'   CLAUDE.md | sed 's/-end//'   > /tmp/ends.txt
wc -l < /tmp/starts.txt ; wc -l < /tmp/ends.txt          # MUST both be 7
diff /tmp/starts.txt /tmp/ends.txt && echo "MARKERS-PAIRED-7"
# C. Prettier is now a no-op on the file (idempotent = formatted).
npx prettier --check CLAUDE.md
# D. Planning gates.
pnpm planning-gates
  </automated>
  </verify>
  <done>Deletion count is exactly 6 and each matches the inventory; `MARKERS-PAIRED-7` printed with 7/7; `prettier --check` reports the file formatted; `pnpm planning-gates` pass count reported (expected 11/11). If prettier rewrapped any pre-existing line, that is reported and NOT committed.</done>
</task>

<task type="auto">
  <name>Task 3: Close the todo with a per-item record, stage explicitly</name>
  <files>.planning/todos/completed/2026-09-15-claude-md-gsd-region-source-markers-are-unreliable-and-the-architecture-region-is-false.md</files>
  <action>
Order matters here. `git mv` stages the HEAD content of the file, so ANY edit made before the move
is dropped from the staged blob (the `R100`/`RM` status is the tell). Move FIRST, edit SECOND,
re-stage the new path THIRD.

1. `git mv .planning/todos/pending/2026-09-15-claude-md-gsd-region-source-markers-are-unreliable-and-the-architecture-region-is-false.md .planning/todos/completed/`
2. In the moved file: set `status: pending` → `status: completed` in the frontmatter. Leave
   `severity`/`platform`/`ready` as they are — the todo-frontmatter gate scopes `pending/` only and
   `completed/` is deliberately exempt, so nothing needs adding.
3. Append a `## Resolution (2026-09-18, quick 260918-a1a)` section recording what happened to EACH
   of the four "What remains" items. Be specific; a closure record that says "done" is worth
   nothing to the next reader:
   - **Item 1 (architecture region):** the todo's own cheapest remedy — point the region at
     `.planning/research/ARCHITECTURE.md` — was measured and REJECTED as a trap. That document is
     Humble-scoped, Electron-era, researched 2026-07-05, and predates the Rust/Tauri shell
     (`83dc57a76`, 2026-07-20) by fifteen days; citing it bare swaps a false sentence for a stale
     one, which is worse because agents trust a cited document more than an admission of ignorance.
     Took the third option: the region now states no whole-app architecture is mapped, names the
     391-line study with its real scope and its Electron/Tauri mention counts, and points at
     `graphify query` for current structure.
   - **Item 2 (marker semantics):** adopted **Nominal**. A note above the first region marker —
     outside every region, so it is the first thing read and cannot be clobbered by a region
     rewrite — states the regions are hand-maintained and `source:` is historical provenance.
   - **Item 3 (repo-relative paths):** four markers now carry real repo-relative paths
     (`.planning/PROJECT.md`, `.planning/research/STACK.md`, `.planning/research/ARCHITECTURE.md`,
     `.claude/skills/`). The fifth — `conventions` — got `source:hand-maintained` instead of a path.
     Record the reasoning: `.planning/spikes/CONVENTIONS.md` is 144 lines of "Spike Conventions" and
     contains none of the region's content, so writing that path would have committed the exact
     defect this todo was filed against. The region genuinely has no source document.
   - **Item 4 (skills list):** **closed with NO EDIT, deliberately.** `.claude/skills/` holds three
     dirs, but `archify` is a third-party skill installed from GitHub — `skills-lock.json` pins
     `tt-a1i/archify`, sourceType `github`, 192 files / 7.3 MB of vendor output. The region is
     headed "Project Skills" and documents skills that teach an agent about THIS project; the two
     listed entries are GameLib-authored findings. `archify` teaches nothing about GameLib, so its
     omission is correct and was confirmed, not overlooked.
   - Note the verification evidence: conventions region byte-identical to pre-edit, exactly six
     deleted lines, 7/7 marker pairs, planning-gates result.

Stage with EXPLICIT pathspecs only. `git add -A` / `git add .` are forbidden here — the working
tree carries unrelated untracked content that must NOT be swept in: `.claude/skills/archify/`,
`skills-lock.json`, `.planning/quick/260912-d84-.../`, the modified
`.planning/todos/completed/2026-09-11-humble-keys-...md`, and the spike run.log / shot-epic-*.png
captures.

Stage exactly: `CLAUDE.md`, both todo paths (old and new), and this plan's directory
`.planning/quick/260918-a1a-claude-md-region-source-markers/`.

Commit. Do NOT push.
  </action>
  <verify>
  <automated>
cd /Users/graysonmitchell/Projects/GameLib
# A. The file moved and says completed.
ls .planning/todos/pending/ | grep -c 'claude-md-gsd-region-source-markers' ; true   # MUST be 0
grep -n '^status:' .planning/todos/completed/2026-09-15-claude-md-gsd-region-source-markers-are-unreliable-and-the-architecture-region-is-false.md
# B. All four items recorded, including the no-edit rationale.
grep -c 'Item 4' .planning/todos/completed/2026-09-15-claude-md-gsd-region-source-markers-are-unreliable-and-the-architecture-region-is-false.md ; true
grep -c 'archify' .planning/todos/completed/2026-09-15-claude-md-gsd-region-source-markers-are-unreliable-and-the-architecture-region-is-false.md ; true
# C. The STAGED blob carries the edits, not HEAD content (the git mv trap).
git diff --cached --name-status
git show ":.planning/todos/completed/2026-09-15-claude-md-gsd-region-source-markers-are-unreliable-and-the-architecture-region-is-false.md" | grep -n '^status:'
# D. Nothing forbidden was staged.
git diff --cached --name-only | grep -E 'archify|skills-lock|260912-d84|2026-09-11-humble-keys|run\.log|shot-epic' ; true   # MUST print nothing
# E. Gates still green after the move.
pnpm planning-gates
  </automated>
  </verify>
  <done>The todo is absent from `pending/`; the file in `completed/` reads `status: completed`; the staged blob (via `git show :path`) also reads `status: completed`, proving the `git mv` trap was avoided; the forbidden-path grep prints nothing; planning-gates reported. Commit created, not pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git index | untracked third-party/vendor content and unrelated in-flight edits sit beside the intended change |
| CLAUDE.md → every agent session | this file is auto-loaded; a false line here propagates into every future session's assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260918-01 | Tampering | conventions region (CI-relevant, load-bearing) | mitigate | Task 1 verify diffs the extracted region against the pre-edit copy and requires byte-identity |
| T-260918-02 | Tampering | prettier rewrapping pre-existing lines | mitigate | pre-edit `--check` negative control + snapshot diff isolating prettier's contribution + exact six-deletion inventory |
| T-260918-03 | Information disclosure | `git add -A` sweeping `.claude/skills/archify/` (7.3 MB vendor), `skills-lock.json`, spike `run.log` and screenshots into a commit | mitigate | explicit pathspecs only; Task 3 verify greps the staged name list for every forbidden path |
| T-260918-04 | Tampering | `git mv` staging HEAD content and silently dropping the status edit | mitigate | move-then-edit-then-restage ordering; verify asserts on `git show :path`, not the worktree file |
| T-260918-SC | Tampering | package-manager installs | n/a | no installs in this plan; `npx prettier` resolves the existing local devDependency |
</threat_model>

<verification>
1. Conventions region byte-identical to `CLAUDE.md.preedit`.
2. Exactly six deleted lines in `git diff -- CLAUDE.md`, matching the named inventory.
3. Seven `GSD:*-start` / `GSD:*-end` pairs, same names in the same order.
4. `npx prettier --check CLAUDE.md` clean.
5. `pnpm planning-gates` pass count reported (expected 11/11).
6. No forbidden path in `git diff --cached --name-only`.
7. `git show :<completed-todo-path>` shows `status: completed` (staged blob, not worktree).
</verification>

<success_criteria>
- `grep -c 'Architecture not yet mapped' CLAUDE.md` returns 0.
- Every `source:` marker in `CLAUDE.md` is either a path that exists in the repo or the literal
  `hand-maintained` or the untouched `GSD defaults`.
- An agent reading `CLAUDE.md` top-down encounters the hand-maintained note before any region.
- The skills region still lists exactly two entries.
- The todo is in `completed/` with `status: completed` and a per-item record naming why item 4
  needed no edit.
- One commit, explicit pathspecs, not pushed.
</success_criteria>

<output>
Report: the prettier negative-control result, the measured Electron/Tauri counts, the six deleted
lines, the marker pair count, the planning-gates pass count, and the commit sha.
Do not push.
</output>
