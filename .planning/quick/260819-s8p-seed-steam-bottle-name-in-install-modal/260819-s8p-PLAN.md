---
quick_id: 260819-s8p
slug: seed-steam-bottle-name-in-install-modal
date: 2026-08-19
type: execute
mode: quick
closes_gap: G-D05-BOTTLENAME (and its already-FAILED sibling G-ROW-1)
phase_ref: 34.13-steam-install-time-wine-bottle-form-gog-parity
autonomous: true
files_modified:
  - src/frontend/screens/Library/components/InstallModal/index.tsx
  - src/frontend/screens/Library/components/InstallModal/__tests__/installModalBottleSeeding.test.ts
  - .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md
must_haves:
  truths:
    - "The Steam install-options wine section's read-only CrossOver bottle-name field arrives prefilled with `GameLibSteam` instead of blank."
    - "The plain non-Steam (`hasWine`) wine path is byte-for-byte unchanged in behaviour — it never receives a seeded bottle name."
    - "The seed is the DEDICATED Steam bottle name, never `globalConfig.wineCrossoverBottle` (the shared GOG/Epic bottle)."
    - "A jest gate exists that goes RED against the pre-fix source, proven in-suite against an inline known-bad specimen."
  artifacts:
    - path: "src/frontend/screens/Library/components/InstallModal/index.tsx"
      provides: "The seed call on the Steam branch"
      contains: "DEFAULT_STEAM_BOTTLE_NAME"
    - path: "src/frontend/screens/Library/components/InstallModal/__tests__/installModalBottleSeeding.test.ts"
      provides: "Non-vacuous source-shape gate + pre-fix specimen control"
  key_links:
    - from: "src/frontend/screens/Library/components/InstallModal/index.tsx"
      to: "src/frontend/screens/Game/GamePage/components/steamBottleDefaults.ts"
      via: "import { DEFAULT_STEAM_BOTTLE_NAME }"
      pattern: "DEFAULT_STEAM_BOTTLE_NAME"
---

<objective>
Close the Phase 34.13 UAT gap `G-D05-BOTTLENAME` (already observed as a FAIL under `G-ROW-1`
on both runtimes): the Steam install-options wine section renders a read-only CrossOver
bottle-name field that is permanently BLANK, because `crossoverBottle` is initialised to `''`
and nothing on the Steam path ever seeds it.

Purpose: D-05 requires that field to read `GameLibSteam`. The field is *correctly* read-only
(D-05 / D-16) — the bug is the missing VALUE, not the read-only-ness.

Output: a one-line seed on the Steam branch of `InstallModal/index.tsx`, a non-vacuous jest
gate, and an append-only ledger note in `34.13-UAT.md`.
</objective>

<already_verified_do_not_re_derive>
Verified against HEAD `1e8a2ec79`. Re-check any line number you actually edit (they drift), but
do NOT re-derive these conclusions:

1. **Defect site.** `InstallModal/index.tsx:127` — `const [crossoverBottle, setCrossoverBottle] =
   useState('')`. That value is handed to `<WineSelector ... runner="steam" bottleNameReadOnly />`
   at ~`:634`. Read-only + never seeded = permanently blank.

2. **THE `## Gaps` RECORD'S `missing:` NOTE IS STALE. DO NOT ACT ON IT.** It asks for the
   constant to be relocated out of the backend and asks whether the `src/common/types.ts` edit
   fence still holds. Both are moot:
   `src/frontend/screens/Game/GamePage/components/steamBottleDefaults.ts:20` ALREADY exports
   `DEFAULT_STEAM_BOTTLE_NAME = 'GameLibSteam'` as a deliberate, doc-commented frontend mirror of
   the backend constant, and it is already guarded by `steamBottleDefaults.test.ts`.
   → **No constant relocation. No `src/common/types.ts` edit. No backend/preload plumbing.**

3. **Pattern to mirror.** `SteamBottleSetup.tsx:213` already does exactly
   `setCrossoverBottle(DEFAULT_STEAM_BOTTLE_NAME)` inside its seeding effect, with a comment
   recording that it must ALWAYS be the dedicated Steam bottle and NEVER
   `globalConfig.wineCrossoverBottle` — seeding the shared GOG/Epic name there was the 17-06 UAT
   bug that would have provisioned a multi-GB Steam client into the user's shared bottle. That
   constraint applies identically here.

4. **The gate to seed on.** `const showWineSelector = isSteamManagedApp ? steamGating.wineSection
   : hasWine` at ~`:510`, followed by an existing
   `useEffect(() => { if (showWineSelector) { ...getAlternativeWine()... } }, [showWineSelector])`
   at ~`:511-541`. That effect is the seed's home. The seed must fire ONLY on the Steam arm.

5. **No jsdom, no react-test-renderer, and `index.tsx` imports `./index.scss` at line 15.**
   No test in this repo can import `InstallModal/index.tsx` or observe it render. A render/
   element-graph assertion is INFEASIBLE for this file. The comment-stripped source gate with an
   inline known-bad specimen is the established, and only available, mechanism — the sibling file
   `__tests__/installModalSource.test.ts` is the idiom to copy (`readIndexStripped()`,
   `countOccurrences`, `excludingLines`, plus a paired "…the same regex DOES match an inline
   known-bad specimen" spec).

6. **Blast radius.** Frontend-only. Both Electron and Tauri serve the same React bundle, so ONE
   fix closes all four rows: `G-ROW-1` (electron + tauri, already FAILED) and `G-D05-BOTTLENAME`
   (electron + tauri, predicted).
</already_verified_do_not_re_derive>

<repo_hazards>
**The working tree is DIRTY with a CONCURRENT session's files** (`.planning/STATE.md`, the
`34.13-UAT.md` you are about to append to, and untracked
`.planning/quick/260819-p2d-uat-3413-bottle-prefill-note/`).

FORBIDDEN, without exception:
- `git stash` (in any form — this exact move has stranded a concurrent session's work in this
  repo TWICE; the trigger both times was wanting a clean tree to compare against). Never pop.
- `git checkout -- <path>`, `git restore`, `git reset --hard` — `.husky/post-checkout` runs a
  download that fails deterministically here, so even a single-file checkout is hazardous.
- `git add -A`, `git add .`, or `gsd-sdk query commit` without an explicit `--files` list
  (`gsd-sdk query commit` stages the ENTIRE tree; it has already absorbed a concurrent session's
  files into an unrelated phase commit).

REQUIRED: run `git status --short` before every commit and stage by EXPLICIT path only.

**ts-jest here is TRANSPILE-ONLY** — type errors do NOT fail tests. Run `pnpm codecheck` (tsc)
separately. `pnpm codecheck` does NOT see lint errors either; lint is a separate CI workflow, so
do not introduce a `react-hooks/exhaustive-deps` violation and assume a green codecheck cleared it.
</repo_hazards>

<context>
@src/frontend/screens/Library/components/InstallModal/index.tsx
@src/frontend/screens/Game/GamePage/components/steamBottleDefaults.ts
@src/frontend/screens/Library/components/InstallModal/__tests__/installModalSource.test.ts
@src/backend/testUtils/stripSourceComments.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed crossoverBottle with DEFAULT_STEAM_BOTTLE_NAME on the Steam branch</name>
  <files>src/frontend/screens/Library/components/InstallModal/index.tsx</files>
  <action>
Add the import of `DEFAULT_STEAM_BOTTLE_NAME` from
`frontend/screens/Game/GamePage/components/steamBottleDefaults` (path-alias form, matching the
file's other `frontend/...` imports). This is a plain value import from an existing module —
do NOT copy the literal `'GameLibSteam'` into this file, and do NOT import from the backend
constants module.

Inside the EXISTING `useEffect` whose body opens `if (showWineSelector) {` (~`:511`), before the
`getWine` declaration, add the Steam-only seed:

- Guard it on `isSteamManagedApp` so the plain non-Steam `hasWine` arm keeps its current
  behaviour exactly. `showWineSelector` alone is NOT a sufficient guard — it is true on the
  non-Steam path too.
- Use the functional-updater form so an already-set value is never clobbered and the effect
  needs no additional dependency on `crossoverBottle`: set it to the current value when the
  current value is truthy, otherwise to `DEFAULT_STEAM_BOTTLE_NAME`. This mirrors the
  `setWineVersion((current) => ...)` updater form already used a few lines below, whose WR-15
  comment records why closing over state here is wrong.
- Add `isSteamManagedApp` to the effect's dependency array (it becomes `[showWineSelector,
  isSteamManagedApp]`). Do NOT suppress `react-hooks/exhaustive-deps` with a disable comment —
  lint runs in a workflow `pnpm codecheck` cannot see.

Add a short comment recording WHY the value is the dedicated Steam bottle name and never
`globalConfig.wineCrossoverBottle` (the shared GOG/Epic bottle), pointing at the 17-06 UAT
finding — mirroring the comment at `SteamBottleSetup.tsx:213`.

DO NOT:
- change or remove `bottleNameReadOnly` (the field is meant to be read-only — D-05/D-16);
- touch `WineSelector` or any of its props other than leaving them as-is;
- touch the non-Steam wine derivation;
- introduce the literal token `crossoverOnly` into non-comment source — the sibling gate
  `installModalSource.test.ts:272` asserts ZERO occurrences of it in the stripped source.
  </action>
  <verify>
    <automated>pnpm jest --selectProjects Frontend src/frontend/screens/Library/components/InstallModal/__tests__ && pnpm codecheck</automated>
  </verify>
  <done>`InstallModal/index.tsx` imports `DEFAULT_STEAM_BOTTLE_NAME` and seeds `crossoverBottle` with it inside the `showWineSelector` effect under an `isSteamManagedApp` guard; the existing InstallModal test suite (including the `crossoverOnly` zero-occurrence gate) stays green; `pnpm codecheck` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add a non-vacuous seeding gate proven RED against the pre-fix shape</name>
  <files>src/frontend/screens/Library/components/InstallModal/__tests__/installModalBottleSeeding.test.ts</files>
  <action>
New test file, following `installModalSource.test.ts` exactly: read
`../index.tsx` with `readFileSync`, pass it through
`stripSourceComments` (from `backend/testUtils/stripSourceComments`), and assert against the
STRIPPED text. Never import `../index` — line 15 is `import './index.scss'` and this project's
Frontend jest runs under `testEnvironment: 'node'` with no jsdom.

Open the file with a docstring stating the VACUITY BOUNDARY in plain terms: this gate proves the
SOURCE encodes the seed, not that the dialog renders a prefilled field. The live proof is owed to
34.13's blocking human UAT gate; a green run here is not a substitute.

Write the matchers as named module-level constants / small functions so each one can be applied
to BOTH the real source and an inline specimen. Required specs:

1. **Seed present.** The stripped source contains a `setCrossoverBottle(` CALL (open-paren form).
   State in a comment why this is the load-bearing discriminator: pre-fix, the only occurrence of
   the identifier outside the `useState` destructure is the JSX prop pass-through
   `setCrossoverBottle={setCrossoverBottle}`, which has NO open paren — so this matcher is red on
   the unseeded `useState('')` version by construction.
2. **Seed uses the dedicated constant.** `DEFAULT_STEAM_BOTTLE_NAME` appears in the stripped
   source, and it is IMPORTED (an `import` statement naming it) rather than redefined locally.
3. **Shared bottle never seeded — the 17-06 regression guard.** ZERO occurrences of
   `wineCrossoverBottle` in the stripped source. Prove non-vacuity: the same matcher applied to an
   inline specimen containing `setCrossoverBottle(globalConfig.wineCrossoverBottle)` MUST match.
4. **Steam-only.** Flatten whitespace and assert the seed sits within an `isSteamManagedApp`
   guard — i.e. locate the `setCrossoverBottle(` call site in the flattened stripped source and
   assert `isSteamManagedApp` appears between the enclosing `if (` and that call. Follow C-12's
   lesson from the sibling suite: flatten whitespace FIRST so prettier reflow cannot break the
   gate. Prove non-vacuity against an inline specimen where the seed sits OUTSIDE any
   `isSteamManagedApp` guard — that specimen MUST fail the same assertion.
5. **Read-only preserved.** Exactly one occurrence of `bottleNameReadOnly` in the stripped source
   (the bug was the missing value, not the read-only-ness — this pins that the fix did not
   "solve" it by making the field editable).
6. **PRE-FIX SPECIMEN CONTROL (mandatory, the whole point of this file).** One spec builds an
   inline specimen reproducing the real pre-fix shape — a `useState('')` line, the JSX
   `setCrossoverBottle={setCrossoverBottle}` prop pass-through, and NO call form — and asserts
   spec 1's matcher does NOT match it. A grep assertion that cannot fail against a known-bad
   input guards nothing; this file must demonstrate its own failure mode in-suite.

Do not assert on line numbers. Do not assert the file's own comment prose (it is stripped, but
also: a self-invalidating header is a ledgered defect class here).
  </action>
  <verify>
    <automated>pnpm jest --selectProjects Frontend src/frontend/screens/Library/components/InstallModal/__tests__/installModalBottleSeeding.test.ts</automated>
  </verify>
  <done>New suite passes against the fixed source. Its pre-fix specimen spec and its two non-vacuity specimens demonstrate, in-suite, that the matchers go red on the known-bad shapes.</done>
</task>

<task type="auto">
  <name>Task 3: Append the fix to 34.13-UAT.md (dispositions UNTOUCHED) and commit by explicit path</name>
  <files>.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md</files>
  <action>
APPEND ONLY, following the precedent set by quick task `260819-p2d`
(`.planning/quick/260819-p2d-uat-3413-bottle-prefill-note/260819-p2d-SUMMARY.md`).

The ledger table's columns are `| Item | Runtime | Disposition | Observation | Blocker |`. Append a
short, clearly-labelled note to the **Observation** cell of the four affected rows — currently
~L141 (`G-ROW-1` electron), ~L142 (`G-ROW-1` tauri), ~L172 (`G-D05-BOTTLENAME` electron),
~L173 (`G-D05-BOTTLENAME` tauri). Re-locate them by `grep -n`, not by trusting these numbers.

Each note must record:
- **FIX LANDED `<short-sha>` (quick `260819-s8p`) — code change, NOT a runtime observation.**
- What changed in one sentence: `crossoverBottle` is now seeded with `DEFAULT_STEAM_BOTTLE_NAME`
  on the Steam branch of `InstallModal/index.tsx`; the field remains read-only by design.
- **A mid-gate integrity warning:** the surface CHANGED under a gate that is mid-run. Any wine
  section observation recorded BEFORE this commit was made against pre-fix code and needs
  re-observation on a rebuilt app. This is the reason the note exists at all.
- For the tauri rows, restate ground rule 2: the seam is runtime-independent React state, which
  is exactly the shape that invites an inferred PASS — an independent observation is still required.

HARD PROHIBITIONS:
- **Do NOT change any row's Disposition.** Every row keeps its existing value (`pending`, or
  `**FAIL**` for the two already-scored `G-ROW-1` rows). Dispositions are BLOCKING and reserved
  for the human developer at the gate. An executor may not perform, simulate, or infer an
  observation.
- **Do NOT mark the `## Gaps` entry `resolved`** or edit its `status: failed`. A fix landing is
  not an observation.
- Do NOT touch the Roll-up counts or the Recording-integrity section.
- Do NOT reflow, reformat, or re-wrap any line you are not appending to.

Verify before committing: each edited row still parses as 5 pipe-delimited cells; `grep -c` the
dispositions and confirm the counts are identical to before your edit.

Then commit. `git status --short` FIRST. Stage by explicit path ONLY — the tree contains a
concurrent session's `.planning/STATE.md`, its untracked `260819-p2d-*/` directory, and its own
pre-existing modifications to this same UAT file. Leave every one of them alone; do not stash,
do not checkout, do not `git add -A`.

    git add src/frontend/screens/Library/components/InstallModal/index.tsx \
            src/frontend/screens/Library/components/InstallModal/__tests__/installModalBottleSeeding.test.ts \
            .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md \
            .planning/quick/260819-s8p-seed-steam-bottle-name-in-install-modal

Commit subject: `fix(quick-260819-s8p): seed Steam install-modal bottle name with GameLibSteam`.
Note that the UAT file will carry the concurrent session's uncommitted changes too — if
`git diff --cached` shows hunks you did not author, unstage the file, and commit the source +
test first, reporting the conflict rather than absorbing someone else's work.
  </action>
  <verify>
    <automated>grep -c 'pending' .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md && git log --oneline -1 && git status --short</automated>
  </verify>
  <done>Four Observation cells carry the fix note with the mid-gate warning; every Disposition and the `## Gaps` `status: failed` are byte-identical to before; the commit contains only this task's files; the concurrent session's `.planning/STATE.md` and `260819-p2d-*/` remain untracked/unstaged and unmodified.</done>
</task>

</tasks>

<verification>
- `pnpm jest --selectProjects Frontend src/frontend/screens/Library/components/InstallModal/__tests__` — all green, including the pre-existing `installModalSource.test.ts` gates.
- `pnpm codecheck` — clean (remember: transpile-only ts-jest means the suite alone proves nothing about types).
- Manual RED proof, performed once and reported in the SUMMARY: temporarily revert the seed line
  in a scratch copy (NOT via `git checkout`/`git stash` — edit and re-edit by hand, or point the
  gate's reader at a scratch file) and confirm the new suite fails. The in-suite specimen specs
  already encode this, but state the observed result explicitly.
- `git status --short` shows the concurrent session's files still present and untouched.
</verification>

<success_criteria>
- The Steam install-options wine section's read-only bottle-name field is sourced from
  `DEFAULT_STEAM_BOTTLE_NAME`, not `''`.
- The non-Steam `hasWine` path is unchanged.
- `wineCrossoverBottle` appears nowhere in `InstallModal/index.tsx`'s stripped source.
- The new gate demonstrates its own RED against the pre-fix shape, in-suite.
- `34.13-UAT.md` gained notes only; zero dispositions changed; `## Gaps` still `status: failed`.
- No stash, no checkout, no whole-tree staging; concurrent work intact.
</success_criteria>

<output>
Write `.planning/quick/260819-s8p-seed-steam-bottle-name-in-install-modal/260819-s8p-SUMMARY.md`
when done. It must state plainly that this closes the gap in CODE ONLY — `G-ROW-1` and
`G-D05-BOTTLENAME` remain owed to 34.13's blocking human UAT gate on both runtimes, and the
previously-scored wine-section rows now need re-observation because the surface changed mid-gate.
</output>
