---
phase: quick-260926-8vk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md
  - .planning/todos/completed/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md
autonomous: true
requirements: [QUICK-260926-8vk]

must_haves:
  truths:
    - "The todo no longer exists under .planning/todos/pending/ and exists under .planning/todos/completed/ with the SAME filename (date prefix kept)"
    - "The completed todo records the 2026-09-25 Windows gate result: both arms run, no EBUSY, vite served HTTP 200 throughout, decision = no change to server.watch.ignored"
    - "The record states its own limits: evidence against, not proof of immunity; only legendary was refreshed"
    - "vite.config.ts and meta/__tests__/viteRendererConfig.test.ts are byte-unchanged"
    - "pnpm planning-gates and prettier --check are green"
  artifacts:
    - path: ".planning/todos/completed/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md"
      provides: "Closed todo carrying the gate result"
      contains: "resolved_by: \"quick-260926-8vk\""
  key_links:
    - from: "completed todo Resolution section"
      to: "the todo's existing 'If it reproduces' section"
      via: "reopen condition prose"
      pattern: "If it reproduces"
---

<objective>
Close the todo `2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md`.
Its live gate was run on the operator's Windows 11 machine on 2026-09-25 and did NOT reproduce.
Record that result in the todo and move it to `completed/`.

Purpose: the todo was filed `ready: live-gate` as a measurement awaiting a gate; the gate is done,
so leaving it pending misstates the backlog.

Output: one moved and extended markdown file. DOCS ONLY. No source changes. In particular, do NOT
touch `vite.config.ts`, `server.watch.ignored`, or `meta/__tests__/viteRendererConfig.test.ts`.
Do NOT rerun the gate. Do NOT edit STATE.md (the orchestrator owns it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/todos/pending/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md

Repo conventions for closing a todo (derived from recent closures, e.g. `356cfd73e`, `c3cc2ef44`):

- The file is moved with `git mv` from `.planning/todos/pending/` to `.planning/todos/completed/`,
  keeping the exact same filename (date prefix included). Git records it as a rename.
- The existing body is kept intact; a `## Resolution ...` section is appended at the end
  (see `.planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md`,
  heading shape `## Resolution (2026-09-25): closed as obsolete — ...`).
- Frontmatter: many recent closures add `resolved: <date>` and `resolved_by: "quick-<id>"` keys.
  The existing `severity:` / `platform:` / `ready:` keys stay (completed/ is exempt from the gate,
  but do not delete them).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the todo to completed/ and append the gate-result Resolution section</name>
  <files>.planning/todos/pending/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md, .planning/todos/completed/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md</files>
  <action>
Step 1 — move first, so git sees a rename: run `git mv` from
`.planning/todos/pending/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md`
to the same filename under `.planning/todos/completed/`.

Step 2 — frontmatter: in the moved file, insert two keys immediately after the existing `found_by:`
line (keep every existing key unchanged, including `severity: minor`, `platform: windows`,
`ready: live-gate`): `resolved: 2026-09-26` and `resolved_by: "quick-260926-8vk"`. Bare values,
matching the style of other completed todos.

Step 3 — leave the whole existing body untouched (including "Read this first", "How to gate it" and
"If it reproduces" — the last is the reopen recipe and must survive verbatim). Append at the end of
the file a new section headed
`## Resolution (2026-09-26): gate run on Windows, did NOT reproduce — closed with no config change`
containing the following facts, in prose and/or a short list (these were measured by the
orchestrator; transcribe them, do not rerun anything):

- When/where: operator's Windows 11 machine, 2026-09-25. vite 6.3.5 started via `pnpm exec vite`
  with the repo's own `vite.config.ts`, and kept running throughout both arms.
- Arm 1 (`build/`): `pnpm build:sidecar` run 10 times back-to-back, each rewriting
  `build/main/sidecar.js` (1.4 MB). No `EBUSY`, no `errno: -4082`, no FSWatcher error event; vite
  still served HTTP 200 afterwards.
- Arm 2 (`public/bin`): a real `legendary` re-download was forced by setting its tag to `FORCE` in
  `public/bin/.release_tags`; the original `.release_tags` was restored byte-identical afterwards
  (note why that restore matters: a win32 run otherwise rewrites the file without the
  `__darwin_layout` key). `pnpm download-helper-binaries` exited 0, streaming a fresh 17.6 MB
  `x64/win32/legendary.exe` plus the linux x64/arm64 and win32 arm64 builds into `public/bin`
  (the darwin onedir build is skipped on win32). No EBUSY; vite still served HTTP 200.
- The vite log contained only its startup banner through both arms.
- Limits of the claim (state them plainly, do not soften): neither writer holds a file open for
  long the way the linker held `gamelib_shell.exe` in `260924-vat`, so this is evidence against
  these paths, not proof of immunity. Only `legendary` was refreshed; `gogdl`, `nile` and `comet`
  were not rewritten.
- Decision: no change to `server.watch.ignored` (and none to the exact-array assertion in
  `meta/__tests__/viteRendererConfig.test.ts`), consistent with the measured-failures-only rule
  recorded above in this file and in `260925-re8`.
- Reopen condition: reopen if an EBUSY naming `build/` or `public/bin` is ever observed; the
  "If it reproduces" section above remains the recipe.
- One-line side observation: stopping the backgrounded shell on Windows left the vite node child
  (listening on 5173) orphaned; it had to be killed by PID. Record it as an observation only —
  do not file a new todo and do not expand on it.

Do not use the words "fixed" or "immune". Do not add any file other than the moved todo.
  </action>
  <verify>
    <automated>test ! -e .planning/todos/pending/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md && grep -c 'resolved_by: "quick-260926-8vk"' .planning/todos/completed/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md && grep -c '^## If it reproduces' .planning/todos/completed/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md && grep -c '^## Resolution (2026-09-26)' .planning/todos/completed/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md && git diff --quiet HEAD -- vite.config.ts meta/__tests__/viteRendererConfig.test.ts && npx prettier --check .planning/todos/completed/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md</automated>
  </verify>
  <done>Todo exists only under completed/ with the same filename; frontmatter carries resolved/resolved_by; Resolution section contains both arms, the limits, the decision, the reopen condition and the orphan note; original body intact; vite.config.ts and its test unchanged; prettier --check passes on the moved file (if it fails, run `npx prettier --write` on that one path only and re-check).</done>
</task>

<task type="auto">
  <name>Task 2: Run planning gates and commit atomically</name>
  <files>.planning/todos/completed/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md</files>
  <action>
Run `pnpm planning-gates` (wraps `meta/runPlanningGates.py`, which includes the todo frontmatter gate
and the UAT visibility gate). It must be green; if a gate fails, fix the todo file (never widen a
gate's vocabulary, per CLAUDE.md). Then stage ONLY the rename (both the pending deletion and the
completed addition — `git mv` already staged them; `git add` the completed path again after the
edit) and commit with message
`docs(quick-260926-8vk): close vite build/ + public/bin watcher todo — gate did not reproduce`
ending with the attribution trailer
`Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`.
Do not stage the pre-existing unrelated working-tree changes (the modified `38-VERIFICATION.md` and
the untracked `260926-8j9` quick dir) — use an explicit pathspec, never `git add -A` / `git add .`.
Do not use `--no-verify`; the pre-commit prettier hook must run. Confirm with `git show --stat -M HEAD`
that git records a rename (pending -> completed) and that no other file is in the commit.
  </action>
  <verify>
    <automated>pnpm planning-gates && npx prettier --check .planning/todos/completed/2026-09-25-vite-dev-watcher-also-walks-build-and-public-bin-both-written-while-serving.md && git show --stat -M --format= HEAD</automated>
  </verify>
  <done>planning-gates exit 0; prettier clean; HEAD is a single docs commit whose only change is the pending -> completed rename of this todo (with its appended section).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Docs-only edit to a planning markdown file; no code, no input handling, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8vk-01 | Information disclosure | Resolution prose | mitigate | Transcribe only the measured facts given (paths, sizes, versions); no usernames, HOME paths, tokens or captured logs go into the record |
| T-8vk-02 | Tampering | Unrelated working-tree changes | mitigate | Explicit-pathspec staging only; `git show --stat HEAD` confirms the commit holds just the rename |
</threat_model>

<verification>
- Todo moved (rename in git), frontmatter extended, Resolution appended, original body intact.
- `vite.config.ts` and `meta/__tests__/viteRendererConfig.test.ts` untouched.
- `npx prettier --check` on the written path and `pnpm planning-gates` both green.
</verification>

<success_criteria>
The backlog no longer lists the vite `build/` / `public/bin` watcher todo as pending, and the closed
file tells a future reader exactly what was measured, what it does not prove, and when to reopen.
</success_criteria>

<output>
Create `.planning/quick/260926-8vk-close-vite-watcher-build-public-bin-todo/260926-8vk-SUMMARY.md` when done
</output>
