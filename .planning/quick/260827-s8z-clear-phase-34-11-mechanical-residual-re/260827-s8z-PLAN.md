---
quick_id: 260827-s8z
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [WR-04, WR-07, WR-17, WR-18, WR-19]
files_modified:
  - src/frontend/screens/Library/index.tsx
  - src/frontend/types.ts
  - src/frontend/screens/Library/LibraryContext.tsx
  - src/frontend/screens/Library/__tests__/libraryPipeline.test.ts
  - src/frontend/components/UI/Header/index.css
  - src/frontend/components/UI/ErrorComponent/index.css
  - src/frontend/screens/Settings/index.css
  - src/frontend/components/UI/NavShell/index.scss
  - src/frontend/screens/Library/__tests__/tier2Portal.test.ts
  - src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts
  - src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts
  - src/frontend/screens/Library/components/FilterChipRow/index.tsx
  - src/frontend/screens/Library/components/FilterZeroResult/index.tsx
  - .planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md
  - .planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md

must_haves:
  truths:
    - "The three legacy filter-state blocks are gone from Library/index.tsx and the app still typechecks"
    - "resolveLabel exists exactly once in the repo, in chipLabels.ts, imported by both components"
    - "Header/index.css declares no rule that is not scoped under .Header"
    - "The hardcoded-string gate still reports 0 violations over 163 scanned files"
    - "34.11-REVIEW-FIX.md and the pending todo both state the same open count, and neither claims WR-18 is closed"
  artifacts:
    - path: "src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts"
      provides: "the single resolveLabel definition"
      contains: "export function resolveLabel"
    - path: ".planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md"
      provides: "updated dispositions with commit-hash evidence"
      contains: "status: partial"
  key_links:
    - from: "src/frontend/screens/Library/components/FilterZeroResult/index.tsx"
      to: "chipLabels.ts"
      via: "import { resolveLabel }"
      pattern: "import.*resolveLabel.*chipLabels"
---

<objective>
Close the mechanical residual review warnings from Phase 34.11 that survive at `HEAD`,
and record an honest disposition for the two that do not survive contact with the current
codebase.

Purpose: the phase's fix ledger has carried 14 open Warnings since 2026-08-25. Three of
them are pure hygiene and can be discharged with grep-justified deletions. Two of them
(WR-17, WR-18) were investigated during planning and **cannot be closed as the review
specifies** — the review's recommended fix is now actively harmful. Recording that
truthfully is the deliverable for those two, not a code change.

Output: three findings FIXED, one reclassified INVALID with evidence, one left OPEN with
measured evidence, and two ledgers that agree with each other.
</objective>

<planner_findings>
Read this before starting. These facts were measured during planning against `HEAD`
(`985a554cc`) with a clean working tree. **Re-verify each one yourself** — the review is
17 days old and the commands are given below. But do not re-litigate the conclusions
without new evidence.

### WR-17 is a FALSE FINDING. Do not "fix" it.

The review claims `meta/i18nGateScope.json`'s `baseCommit` (`b5b5cad3f`, 2026-06-23) is
stale because it "predates every file it lists". That is the **designed** semantics:

- `meta/genI18nGateScope.ts:397-402` reads `baseCommit` from `package.json`'s
  `upstream.baseCommit` — the **upstream Heroic merge-base**, not a GameLib commit.
- The scope is built from `git diff --name-status <baseCommit> HEAD -- src/frontend`
  (`:425`). Every fork-**added** file necessarily does not exist at the base commit.
  The review's own reproduction (`git cat-file -e b5b5cad:.../FilterChipRow/index.tsx`
  fails) is the definition of "fork-added", not evidence of staleness.
- Measured: `scope.baseCommit === packageJson.upstream.baseCommit` and
  `scope.baseVersion === packageJson.upstream.baseVersion`, both exact matches. The
  companion artifact `meta/i18nForkTouchedFiles.json`, regenerated 2026-08-23, carries the
  **same** `baseCommit`. `genI18nGateScope.test.ts:146` pins this equality.

The `generatedBy` half of WR-17 is also already discharged, and inverted: the marker now
honestly reads `"hand-edited ... NOT a pnpm gen-i18n-gate-scope run"`, and that honesty is
**load-bearing**. `isHandCuratedProvenance()` (`genI18nGateScope.ts:286-289`) uses it as a
default-deny veto that prevents the generator from overwriting the blocking gate's scope.
The generator's own header (`:19-28`) records that an unconditional run previously widened
the gate 160 → 178 files and cost real work twice.

**Running `pnpm gen-i18n-gate-scope` as the review recommends would disarm that veto and
widen a blocking gate by ~36 files.** WR-17 is dispositioned **INVALID**, not FIXED.

### WR-18 cannot be closed as specified. Leave it OPEN with the measured numbers.

Adding `facetLabels.ts` and `chipLabels.ts` to `i18nGateScope.json` was measured in audit
mode via `scanScope({ extraFiles })`, which leaves the committed scope untouched:

| | scanned files | violations |
|---|---|---|
| current committed scope | 163 | **0** |
| + the two label files | 165 | **35** (8 in `facetLabels.ts`, 27 in `chipLabels.ts`) |

The gate flags them as `object-property` / `argument` literals because both files hold the
English defaults as **data tables** consumed by `t()` at the call sites — and every one of
those call sites is *already* in scope. The gate's purpose is served; putting the spec
tables in scope produces 35 false positives and turns a green blocking gate red.

There is also a hard pin: `meta/__tests__/genI18nGateScope.test.ts:574` asserts
`expect(scopeSnapshot.files.length).toBe(163)`.

Closing WR-18 properly needs either a spec-table-aware gate heuristic or two
`meta/i18nGateAllowlist.json` entries with `expected` counts — both are design decisions
outside this task. **Record the measurement, leave it OPEN.**

### Consequence for the ledger counts

The task brief predicted "nine warnings remain open". Because WR-18 stays open, the real
figure is **ten open + one invalid**. Task 3 uses the real figure.
</planner_findings>

<context>
@.planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md
</context>

<house_rules>
- No `git stash`, no `git reset`, no `git checkout -- <file>` — concurrent sessions share
  this tree, and `git checkout` fires the post-checkout hook. To revert a file use
  `git show HEAD:<path> > <path>`.
- Do not push. `.husky/pre-push` is red repo-wide. Commit only.
- `pnpm codecheck` (`tsc --noEmit`) says nothing about lint. Run `pnpm exec eslint` on the
  changed files separately.
- Never run a full `pnpm test` — it manufactures a different failure set under load. Run
  jest by test **path**.
- Jest can exit 0 having run nothing. Every jest step below asserts a non-zero test count.
- Commit after each task so Task 3 has real hashes to cite.
</house_rules>

<tasks>

<task type="auto">
  <name>Task 1: WR-04 — delete the dead legacy filter state</name>

  <files>
src/frontend/screens/Library/index.tsx
src/frontend/types.ts
src/frontend/screens/Library/LibraryContext.tsx
src/frontend/screens/Library/__tests__/libraryPipeline.test.ts
  </files>

  <action>
**Step 1 — prove the state is dead before deleting anything.** Run:

`grep -rn "storesFilters\|platformsFilters\|crossoverRatingFilters" src/ meta/`

Every hit must fall into one of five buckets. Classify all of them and write the counts
into the commit message:
  (a) the declaration blocks in `Library/index.tsx` — being deleted;
  (b) the `LibraryContext.Provider` value entries — being deleted;
  (c) `types.ts` `LibraryContextType` fields/setters and the stale comment — being deleted;
  (d) `LibraryContext.tsx` `initialContext` entries — being deleted;
  (e) **KEEP** — `localStorage` *string keys* read by the live D-02 migration path and the
      one-time reset effect, plus comments and test fixtures.

If any hit is a live **read of the React state variable** outside buckets (a)–(d), STOP,
do not delete, and record what reads it in the SUMMARY instead.

At planning time bucket (e) was: `index.tsx` `migrateStoreFacetSelection(storage.getItem('storeFacet'), storage.getItem('storesFilters'))`; `migrateRunnabilityFacetSelection(..., storage.getItem('platformsFilters'))`; the three `storage.removeItem(...)` calls in the `facetOptInMigrated` effect; `filterEngine.test.ts` fixtures; `hardcodedStringGate.test.ts` fixture source; `filterEngine.ts` comments. **All of these stay.**

**Step 2 — delete in `src/frontend/screens/Library/index.tsx`.** Remove the three
contiguous legacy blocks spanning roughly `:129-209`: `initialStoresfilters` +
`useState<StoresFilters>` + `setStoresFilters`; `initialPlatformsfilters` +
`useState<PlatformsFilters>` + `setPlatformsFilters`; `initialCrossoverRatingFilters` +
`useState<CrossoverRatingFilters>` + `setCrossoverRatingFilters`. Also remove the now-stale
"34.11 Plan 04: opt-in ... additive over the legacy ... state above" comment that
introduces the following section, and the six provider-value entries (`storesFilters`,
`platformsFilters`, `crossoverRatingFilters` and their three setters).

Delete the `StoresFilters` / `PlatformsFilters` / `CrossoverRatingFilters` type imports and
any of `epicCategories`, `gogCategories`, `amazonCategories`, `sideloadedCategories`,
`zoomCategories`, `steamCategories`, `Category` that become unreferenced — but check each
by grep against the rest of the file first; some are used elsewhere. Let `eslint` be the
arbiter in Step 5.

The `else` branches being deleted also carried the file's last two **unguarded**
`JSON.parse` calls on `localStorage`. That is a bonus, not a regression — do not
reintroduce them anywhere.

**Step 3 — `src/frontend/types.ts`.** Remove the six `LibraryContextType` members
(`storesFilters`/`platformsFilters`/`crossoverRatingFilters` and the three setters) and the
three-line stale forward-reference comment near `:337-339` that says the fields "stay until
plan 09 retires `LibraryFilters`/`CategoryFilter`" (plan 09 landed; both components are
gone from disk).

**Leave the `StoresFilters`, `PlatformsFilters` and `CrossoverRatingFilters` interface
declarations in place.** They are still cited by name in `filterEngine.ts:41` and `:538` as
the documented legacy shape the two **live** migration helpers consume, and by the
`StoreFacetValue` comment at `types.ts:446`. Deleting them is out of scope for this task.

**Step 4 — `src/frontend/screens/Library/LibraryContext.tsx`.** Remove the matching six
`initialContext` entries so the object still satisfies `LibraryContextType`.

**Step 5 — fix the gate comment that this deletion falsifies.** In
`src/frontend/screens/Library/__tests__/libraryPipeline.test.ts` (~`:100-111`) the
`crossoverRatingTier` test carries a rationale block asserting the state declaration "is
still read by `LibraryFilters` and stays declared until plan 09 retires that component".
Both halves are now false. Rewrite the comment to say the state is **gone** and that the
assertions remain scoped to the derivation/indexing forms (`crossoverRatingTier`,
`crossoverRatingFilters[`) so the gate keeps failing only when the FILTER actually runs
again. Do not change the assertions themselves.
  </action>

  <verify>
    <automated>
cd /Users/graysonmitchell/Projects/GameLib
# 1. No React-state survivors. Comments and localStorage string keys are excluded by hand
#    below; a bare count would self-invalidate on this file's own prose.
grep -rn "storesFilters\|platformsFilters\|crossoverRatingFilters" src/frontend/screens/Library/index.tsx \
  | grep -v "storage.getItem\|storage.removeItem" | grep -v "^\s*[0-9]*:\s*//" > /tmp/wr04-survivors.txt
test ! -s /tmp/wr04-survivors.txt || { echo "FAIL: survivors"; cat /tmp/wr04-survivors.txt; exit 1; }
# 2. The live migration path is intact — these MUST still be present.
grep -q "storage.getItem('storesFilters')" src/frontend/screens/Library/index.tsx || { echo "FAIL: D-02 store migration removed"; exit 1; }
grep -q "storage.getItem('platformsFilters')" src/frontend/screens/Library/index.tsx || { echo "FAIL: D-02 runnability migration removed"; exit 1; }
test "$(grep -c "storage.removeItem('\(stores\|platforms\|crossoverRating\)Filters')" src/frontend/screens/Library/index.tsx)" -eq 3 || { echo "FAIL: facetOptInMigrated reset effect damaged"; exit 1; }
# 3. Types and context agree.
pnpm codecheck
# 4. Lint the changed files (codecheck cannot see lint).
pnpm exec eslint src/frontend/screens/Library/index.tsx src/frontend/types.ts src/frontend/screens/Library/LibraryContext.tsx
# 5. Targeted jest, with a non-zero-test-count assertion.
pnpm exec jest src/frontend/screens/Library/__tests__/libraryPipeline.test.ts \
               src/frontend/screens/Library/__tests__/filterEngine.test.ts \
               src/frontend/screens/Library/__tests__/engineWiring.test.ts \
               src/frontend/screens/Library/__tests__/tier2Portal.test.ts 2>&1 | tee /tmp/wr04-jest.txt
grep -qE "Tests:.*[1-9][0-9]* passed" /tmp/wr04-jest.txt || { echo "FAIL: jest ran zero tests"; exit 1; }
grep -q "failed" /tmp/wr04-jest.txt && { echo "FAIL: jest failures"; exit 1; }
    </automated>
  </verify>

  <done>
`pnpm codecheck` and `eslint` are clean; the four Library suites pass with a non-zero test
count; the D-02 migration reads and the three `removeItem` calls survive; no React-state
reference to the three legacy filters remains in `Library/index.tsx`. Committed as
`refactor(34.11): WR-04 -- delete dead legacy filter state`.
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-19 — descope Header/index.css to .Header</name>

  <files>
src/frontend/components/UI/Header/index.css
src/frontend/components/UI/ErrorComponent/index.css
src/frontend/screens/Settings/index.css
src/frontend/components/UI/NavShell/index.scss
src/frontend/screens/Library/__tests__/tier2Portal.test.ts
src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts
  </files>

  <action>
**Step 1 — prove each rule's real consumer before moving it.** Run and record:

`grep -rn "iconsWrapper" src/frontend --include='*.tsx' --include='*.css' --include='*.scss'`
`grep -rn "refreshIcon" src/frontend --include='*.tsx'`
`grep -rn "@keyframes refreshing" src/frontend`
`grep -rn "animation:.*refreshing" src/frontend`

At planning time these resolved to: `.iconsWrapper` → one consumer,
`Settings/components/CustomWineProton.tsx:61` (`className="iconsWrapper rightButtons
addRemoveSvgButtons"`, whose sibling class `.addRemoveSvgButtons` is already defined in
`src/frontend/screens/Settings/index.css:215`). `.refreshIcon` → one consumer,
`components/UI/ErrorComponent/index.tsx:35`, whose own `index.css:21` already styles
`.button-icon-flex > .refreshIcon`. `@keyframes refreshing` → defined **twice**, in
`Header/index.css:32` and `components/UI/UpdateComponent/index.css:17`, and consumed by
seven stylesheets (LogSettings, Humble/Keys ×2, LibraryHeader, Accessibility, SIDLogin,
UpdateComponent) none of which import Header's copy.

If any of those counts differ now, adjust the destinations accordingly and say so in the
SUMMARY. If `.refreshIcon` or `.iconsWrapper` has gained a **second** consumer, move the
rule to a shared scoped stylesheet instead of into the single consumer.

**Step 2 — move the icon rules out.** Delete `Header/index.css:19-39` in full (the
`/* TODO move following styles to the components which use them */` comment, `.iconsWrapper`,
the `.refreshIcon:hover, .svg-button:focus-visible .refreshIcon` rule, and the duplicate
`@keyframes refreshing`). Then:

- Add `.iconsWrapper { display: flex; align-content: center; justify-content: center; }`
  to `src/frontend/screens/Settings/index.css`, next to `.addRemoveSvgButtons`.
- Add the `.refreshIcon:hover, .svg-button:focus-visible .refreshIcon { animation:
  refreshing 2s infinite; }` rule to `src/frontend/components/UI/ErrorComponent/index.css`,
  next to the existing `.refreshIcon` rule.
- Do **not** re-add `@keyframes refreshing` anywhere. `UpdateComponent/index.css:17` already
  defines it globally and is what the other seven stylesheets have always resolved against.
  Confirm exactly one definition survives repo-wide.

**Step 3 — delete the dead `.Header` background and its cross-file neutraliser.** First
re-prove the premise with `grep -rn "<Header" src/frontend --include='*.tsx'` plus the
existing portal gate in `tier2Portal.test.ts`: `<Header />` must render only through the
tier-2 portal. If it renders anywhere else, SKIP this step entirely, leave `:7` in place,
and record why — the background is then live, not dead.

If the premise holds: delete `background: var(--gradient-body-background,
var(--body-background));` from `Header/index.css`'s `.Header` block, and delete the
`> .Header { background: transparent; }` override in
`src/frontend/components/UI/NavShell/index.scss` (~`:559`) together with the ~25-line
comment block above it that exists only to explain the override.

**Step 4 — repoint the two gates that pin the old arrangement.** Both will go red
otherwise; neither may be simply deleted, because that would leave the change unguarded.

- `src/frontend/screens/Library/__tests__/tier2Portal.test.ts` (~`:105-107`): the test
  `'retains the unrelated iconsWrapper/refreshIcon styling that lives in this file'`
  asserts `headerCss` matches `/@keyframes refreshing/`. **Invert it** into a migration
  gate: assert `headerCss` does NOT match `/@keyframes refreshing/`, does NOT match
  `/\.iconsWrapper/` and does NOT match `/\.refreshIcon/`; and — the non-vacuity half —
  assert the rules landed at their new homes by reading `Settings/index.css` and
  `ErrorComponent/index.css` and matching `.iconsWrapper` and `.refreshIcon:hover`
  respectively. Rename the test to say what it now guards. An absence-only gate would pass
  if the rules were simply lost.
- `src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts` (~`:377-395`, the
  describe block `'Header background is overridden inside the tier-2 portal'`): its
  assertion `/>\s*\.Header\s*{[^}]*background:\s*transparent/` no longer has a target.
  Replace the block with a **two-sided absence** gate: `Header/index.css`'s `.Header` block
  contains no `background:` declaration, AND `NavShell/index.scss` contains no
  `> .Header { background: transparent }` override. Update the block's comment to record
  that 34.11 WR-19 removed the declaration, so the neutraliser and its gate were removed
  with it rather than left orphaned.

Reuse the file's existing `cssBlock`/brace-counting helper for the `.Header` block read
rather than a flat regex over the whole file.
  </action>

  <verify>
    <automated>
cd /Users/graysonmitchell/Projects/GameLib
# 1. Header/index.css declares nothing outside .Header. Strip comments before counting.
grep -v '^\s*/\*' src/frontend/components/UI/Header/index.css | grep -v '^\s*\*' \
  | grep -nE '^\.[A-Za-z-]|^@keyframes' > /tmp/wr19-sel.txt
grep -vqE '^\s*[0-9]+:\.Header' /tmp/wr19-sel.txt && { echo "FAIL: non-.Header selector remains"; cat /tmp/wr19-sel.txt; exit 1; }
# 2. Exactly one @keyframes refreshing survives repo-wide.
test "$(grep -rl '@keyframes refreshing' src/frontend --include='*.css' --include='*.scss' | wc -l | tr -d ' ')" -eq 1 \
  || { echo "FAIL: keyframes count != 1"; grep -rl '@keyframes refreshing' src/frontend; exit 1; }
# 3. The moved rules exist at their new homes (non-vacuity).
grep -q '\.iconsWrapper' src/frontend/screens/Settings/index.css || { echo "FAIL: iconsWrapper not relocated"; exit 1; }
grep -q '\.refreshIcon:hover' src/frontend/components/UI/ErrorComponent/index.css || { echo "FAIL: refreshIcon hover not relocated"; exit 1; }
# 4. Gates.
pnpm exec jest src/frontend/screens/Library/__tests__/tier2Portal.test.ts \
               src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts 2>&1 | tee /tmp/wr19-jest.txt
grep -qE "Tests:.*[1-9][0-9]* passed" /tmp/wr19-jest.txt || { echo "FAIL: jest ran zero tests"; exit 1; }
grep -q "failed" /tmp/wr19-jest.txt && { echo "FAIL: jest failures"; exit 1; }
    </automated>
  </verify>

  <done>
`Header/index.css` contains only `.Header`-scoped rules; exactly one `@keyframes refreshing`
exists in the repo; both moved rules are asserted present at their new homes by the
repointed `tier2Portal` gate; `themeTokens` passes with a two-sided absence gate. Committed
as `refactor(34.11): WR-19 -- scope Header/index.css to .Header`.
  </done>
</task>

<task type="auto">
  <name>Task 3: WR-07 de-dup, then reconcile both ledgers</name>

  <files>
src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts
src/frontend/screens/Library/components/FilterChipRow/index.tsx
src/frontend/screens/Library/components/FilterZeroResult/index.tsx
.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md
.planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md
  </files>

  <action>
**Step 1 — WR-07. Prove the duplication is still verbatim, then collapse it.**

`diff <(sed -n '40,70p' src/frontend/screens/Library/components/FilterChipRow/index.tsx) <(sed -n '34,64p' src/frontend/screens/Library/components/FilterZeroResult/index.tsx)`

At planning time this produced no output — byte-identical. If it now differs, do NOT blind-
merge: reconcile the two versions explicitly, state which behaviour won, and say so in the
SUMMARY.

Move `resolveLabel` **and** the `TFunc` type into
`src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` as named exports,
unchanged. Carry the whole explanatory comment block with it (the one about
`i18next-parser`'s `JavascriptLexer` only resolving string-literal arguments) — that comment
is the reason the four literal `tGamelib(...)` call sites exist, and it must travel with
them. Append one sentence recording that 34.11 WR-07 collapsed two verbatim copies into
this one, so a future reader does not re-split it.

Delete both local copies and import from `./chipLabels` (in `FilterChipRow`) and
`../FilterChipRow/chipLabels` (in `FilterZeroResult`) — match each file's existing import
style for that module. Remove `FilterZeroResult`'s comment that acknowledges the
duplication (~`:28`); it is now false.

`chipLabels.ts` must stay React-free: it imports nothing from `react` or `react-i18next`
before or after this change. `resolveLabel` already takes `t`/`tGamelib` as parameters, so
no import is needed. Verify with grep, below.

⚠ Do **not** add either label file to `meta/i18nGateScope.json` as part of this. The four
literal call sites moving into `chipLabels.ts` does not change the gate's scope, because
`chipLabels.ts` is not in it — see Step 3 and `<planner_findings>`. Any new user-facing
string would go in `gamelib.json`, never `translation.json`; this step introduces none.

**Step 2 — collect the evidence for the ledger.** Run `git log --oneline -3` and capture
the short hashes for Task 1, Task 2 and this task's WR-07 commit. The ledger cites hashes,
not SUMMARY prose.

**Step 3 — update `34.11-REVIEW-FIX.md`.**

Rows, in the existing table format:

- **WR-04** → `**FIXED**` — cite the Task 1 hash. Evidence: the three state blocks, their
  setters, the six `LibraryContextType` members, the `initialContext` entries and the stale
  `types.ts` forward-reference comment are gone; the live D-02 migration reads and the
  `facetOptInMigrated` reset survive; `libraryPipeline.test.ts`'s rationale comment
  rewritten to match.
- **WR-07** → `**FIXED**` — cite this task's hash. Evidence: `resolveLabel` and `TFunc` live
  once in `chipLabels.ts`, imported by both components; `chipLabels.ts` remains React-free.
- **WR-19** → `**FIXED**` — cite the Task 2 hash. Evidence: `Header/index.css` scoped to
  `.Header`; `.iconsWrapper` → `Settings/index.css`, `.refreshIcon` hover →
  `ErrorComponent/index.css`; duplicate `@keyframes refreshing` deleted (one definition
  survives, in `UpdateComponent/index.css`); dead `.Header` background and its NavShell
  neutraliser removed; both gates repointed with non-vacuity assertions.
- **WR-17** → `**INVALID — not a defect**`. Evidence, stated as fact and reproducible:
  `baseCommit` is `package.json`'s `upstream.baseCommit` (the upstream Heroic merge-base) by
  the generator's own contract (`genI18nGateScope.ts:397-402`, `:425`), so it *must* predate
  every fork-added file it lists; `genI18nGateScope.test.ts:146` pins that equality, and
  `i18nForkTouchedFiles.json` (regenerated 2026-08-23) carries the same value. The
  `generatedBy` marker is honest today and is **load-bearing** — `isHandCuratedProvenance()`
  (`:286-289`) uses it as a default-deny veto over the blocking gate's scope. The review's
  recommended `pnpm gen-i18n-gate-scope` run would disarm that veto. Note explicitly that
  the review was written before the generator was hardened, so its advice is stale rather
  than wrong-at-the-time.
- **WR-18** → keep `**OPEN**`, and replace its evidence cell with the measurement:
  `scanScope()` over the committed scope is 163 files / **0** violations; `scanScope({
  extraFiles: [facetLabels.ts, chipLabels.ts] })` is 165 files / **35** violations (8 + 27),
  all `object-property`/`argument` literals from the English default **data tables** whose
  `t()` call sites are already in scope. Note the hard pin at
  `genI18nGateScope.test.ts:574` (`toBe(163)`). Record that closing it needs either a
  spec-table-aware gate heuristic or two `meta/i18nGateAllowlist.json` entries with
  `expected` counts — a design decision, deliberately not taken here.

Frontmatter: `fixed: 12`, `open: 10`, add `invalid: 1`, `total: 23` (12 + 10 + 1 = 23 —
check this arithmetic before writing). `criticals_open: 0` unchanged. **`status:` stays
`partial`** — ten Warnings remain open, so `all_fixed` would be false and would flip the
VS Code folder badge to `complete`. Add a `swept_by` / re-sweep line for quick `260827-s8z`
alongside the existing `260825-vy5` entry rather than overwriting it.

Update the `## Verdict` heading line and the `## Residual` section to the new figures: ten
open Warnings, one invalid. The Residual section currently says "The 14 open Warnings" and
"Two of them (WR-10, WR-11)"; both sentences need the new counts. Leave the
`## Badge attribution` section untouched — its control-row table is still accurate for
`status: partial`.

**Step 4 — update the pending todo.** In
`.planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md`:

- Title/frontmatter: 14 → 10 open. Keep `status: pending` and keep `resolves_phase: "34.11"`.
- Remove `src/frontend/components/UI/Header/index.css` and `src/frontend/types.ts` from the
  `files:` list (nothing open touches them any more). Keep `meta/i18nGateScope.json` — WR-18
  is still open against it.
- Delete the WR-04, WR-07 and WR-19 bullets from the "Provenance / dead code — mechanical"
  group. Rewrite the WR-17/WR-18 bullet: WR-17 closed as INVALID by quick `260827-s8z` (one
  line, pointing at the fix ledger for the reasoning), WR-18 still open with the 163/0 →
  165/35 measurement and the "needs a gate-design decision, not work" framing. If that
  leaves the group holding only WR-18, retitle it to match.
- Add a one-line note at the top of the body recording that quick `260827-s8z` swept this
  list on 2026-08-27 and closed three, so the next reader knows the remaining ten were
  re-confirmed rather than merely inherited.
- Leave the Correctness / UX / Test-quality / D-08 groups byte-identical.

**Step 5 — cross-check the two documents agree.** The open count in the todo's frontmatter,
the todo's title, `34.11-REVIEW-FIX.md`'s `open:` field, its Verdict line and its Residual
prose must all say **ten**. A count that differs between the two files is the exact
"status doc lags the gate run" failure this project has hit before.
  </action>

  <verify>
    <automated>
cd /Users/graysonmitchell/Projects/GameLib
# 1. resolveLabel is defined exactly once, in chipLabels.ts.
test "$(grep -rn 'function resolveLabel' src/frontend | wc -l | tr -d ' ')" -eq 1 || { echo "FAIL: resolveLabel not unique"; grep -rn 'function resolveLabel' src/frontend; exit 1; }
grep -q 'export function resolveLabel' src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts || { echo "FAIL: not exported from chipLabels"; exit 1; }
# 2. Both components import it.
grep -q 'resolveLabel' src/frontend/screens/Library/components/FilterChipRow/index.tsx || { echo "FAIL: FilterChipRow lost it"; exit 1; }
grep -q 'resolveLabel' src/frontend/screens/Library/components/FilterZeroResult/index.tsx || { echo "FAIL: FilterZeroResult lost it"; exit 1; }
# 3. chipLabels.ts stays React-free.
grep -nE "from 'react'|from 'react-i18next'" src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts && { echo "FAIL: chipLabels imported React"; exit 1; }
# 4. Typecheck + lint the changed source.
pnpm codecheck
pnpm exec eslint src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts \
                 src/frontend/screens/Library/components/FilterChipRow/index.tsx \
                 src/frontend/screens/Library/components/FilterZeroResult/index.tsx
# 5. The i18n gate and the scope pins must still be green, and the scope must be UNCHANGED.
git diff --quiet -- meta/i18nGateScope.json || { echo "FAIL: i18nGateScope.json was modified -- see planner findings WR-17/WR-18"; exit 1; }
pnpm lint-translations:gamelib
pnpm exec jest src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx \
               meta/__tests__/genI18nGateScope.test.ts \
               meta/__tests__/hardcodedStringGate.test.ts 2>&1 | tee /tmp/wr07-jest.txt
grep -qE "Tests:.*[1-9][0-9]* passed" /tmp/wr07-jest.txt || { echo "FAIL: jest ran zero tests"; exit 1; }
grep -q "failed" /tmp/wr07-jest.txt && { echo "FAIL: jest failures"; exit 1; }
# 6. The two ledgers agree on the open count, and the badge status is unchanged.
FIX=.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-REVIEW-FIX.md
TODO=.planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md
grep -qE '^\s*open:\s*10\s*$' $FIX || { echo "FAIL: REVIEW-FIX open != 10"; exit 1; }
grep -qE '^\s*fixed:\s*12\s*$' $FIX || { echo "FAIL: REVIEW-FIX fixed != 12"; exit 1; }
grep -qE '^status:\s*partial\s*$' $FIX || { echo "FAIL: status must stay partial"; exit 1; }
grep -qE '^status:\s*pending\s*$' $TODO || { echo "FAIL: todo must stay pending"; exit 1; }
grep -q 'WR-18' $TODO || { echo "FAIL: todo dropped WR-18"; exit 1; }
grep -q 'WR-04' $TODO && { echo "FAIL: todo still lists closed WR-04"; exit 1; }
grep -q 'WR-19' $TODO && { echo "FAIL: todo still lists closed WR-19"; exit 1; }
echo "ALL CHECKS PASSED"
    </automated>
  </verify>

  <done>
`resolveLabel` exists once, exported from a still-React-free `chipLabels.ts` and imported by
both components; `meta/i18nGateScope.json` is byte-unchanged; `pnpm
lint-translations:gamelib`, the gate suites and the chip-row suite pass with a non-zero test
count; `34.11-REVIEW-FIX.md` reads `fixed: 12 / open: 10 / invalid: 1`, `status: partial`,
with WR-04/07/19 FIXED against real commit hashes, WR-17 INVALID with the generator-contract
evidence and WR-18 OPEN with the 163/0 → 165/35 measurement; the todo says ten open and
still says `pending`. Committed as
`refactor(34.11): WR-07 de-dup resolveLabel; reconcile residual ledgers`.
  </done>
</task>

</tasks>

<verification>
1. `pnpm codecheck` clean.
2. `pnpm exec eslint` clean on every changed `.ts`/`.tsx`.
3. `pnpm lint-translations:gamelib` passes.
4. `git diff --quiet -- meta/i18nGateScope.json` — the scope snapshot is untouched.
5. Targeted jest across the seven touched suites, each asserting a non-zero test count.
6. No push. Three commits on `fix/steam-native-install-stability`.
</verification>

<success_criteria>
- WR-04, WR-07, WR-19 are FIXED in `34.11-REVIEW-FIX.md` with commit-hash evidence.
- WR-17 is reclassified INVALID with reproducible generator-contract evidence.
- WR-18 remains OPEN carrying the measured 163/0 → 165/35 numbers and the `toBe(163)` pin.
- `34.11-REVIEW-FIX.md` `status:` is still `partial`; the todo is still `pending`.
- Both ledgers state the same open count (ten).
- The hardcoded-string gate still reports 0 violations over 163 scanned files.
</success_criteria>

<output>
Create `.planning/quick/260827-s8z-clear-phase-34-11-mechanical-residual-re/260827-s8z-SUMMARY.md`
when done. Record: the grep bucket counts from Task 1 Step 1, the consumer counts from
Task 2 Step 1, whether the Task 3 Step 1 `diff` was still empty, and any place where a
planner finding did not reproduce.
</output>
