---
phase: quick-260815-k25
plan: 01
subsystem: ui
tags: [tauri, macos, window-chrome, titlebar, jest]

requires:
  - phase: quick-260815-j24
    provides: "--navbar-height dropped 56px -> 42px, which exposed the AppKit title-paint band"
provides:
  - "NSWindow.setTitleVisibility(Hidden) applied at creation time on the main window via hiddenTitle:true"
  - "OS-level window name (\"GameLib\") preserved for Window menu / Mission Control / Cmd-Tab / VoiceOver"
  - "Two new jest gates (Test A/C) in tauriWindowConfig.test.ts, each with two non-vacuous SANITY fixtures"
affects: [macos-window-chrome, login-window-titles, tauri-window-config-tests]

tech-stack:
  added: []
  patterns:
    - "hiddenTitle is a creation-only Tauri window config knob with no runtime setter -- structurally immune to frameless toggles"
    - "config-JSON-shape gate + two known-bad SANITY fixtures (pre-change shape, serde default) proves non-vacuity"

key-files:
  created: []
  modified:
    - src-tauri/tauri.conf.json
    - src/backend/__tests__/tauriWindowConfig.test.ts

key-decisions:
  - "Used hiddenTitle:true (suppresses only the AppKit paint) instead of blanking title (which would also blank Mission Control/Window-menu/Cmd-Tab/VoiceOver identity) -- the user's explicitly chosen option"
  - "Left titleBarStyle:'Visible' and applyFramelessDecorations untouched; hiddenTitle is orthogonal and applies at creation regardless of frameless state"

patterns-established:
  - "RED-then-GREEN for JSON-shape config gates: assert against real file first (RED against absent key), then edit config, then re-run (GREEN)"

requirements-completed: [QUICK-260815-k25-01, QUICK-260815-k25-02]

duration: 12min
completed: 2026-08-15
---

# Quick Task 260815-k25: Hide native macOS window title (hiddenTitle) Summary

**Added `"hiddenTitle": true` to the Tauri `main` window config so AppKit stops painting "GameLib" over the 42px nav tab strip, while `"title": "GameLib"` keeps the window's OS-level name intact for Mission Control, the Window menu, Cmd-Tab, and VoiceOver.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-15T02:26:00Z (approx, per session start)
- **Completed:** 2026-08-15 (Task 1 code + gates only; Task 2 checkpoint PENDING)
- **Tasks:** 1 of 2 (Task 1 complete; Task 2 is a blocking human-verify checkpoint, not executed)
- **Files modified:** 2

## Accomplishments
- Added `"hiddenTitle": true` immediately after `"titleBarStyle": "Visible"` in `src-tauri/tauri.conf.json`'s `main` window block, leaving `"title": "GameLib"` and `"titleBarStyle": "Visible"` byte-unchanged.
- Extended `src/backend/__tests__/tauriWindowConfig.test.ts` with a new `describe` block containing 4 tests: Test A (`hiddenTitle === true`, `toBe(true)` not `toBeTruthy()`), Test B (SANITY: two known-bad fixtures — pre-change absent-key shape, and the explicit serde-default `false` — both proven to `.toThrow()`), Test C (`title === "GameLib"`, the standing anti-regression for the rejected "blank the title" option), Test D (SANITY: two known-bad fixtures — blanked title, missing title — both proven to `.toThrow()`).
- Widened the `TauriWindowConfig` interface with named `title?: string` and `hiddenTitle?: boolean` fields so the new assertions are type-checked, not just index-signature `unknown`.
- Extended the file's header docstring with F1/F2/F4 (crate + file + line citations) recording that `setTitleBarStyle('overlay')` does NOT hide the title, that `hidden_title` is a creation-only knob with no runtime setter, and the cargo/jest/human division of labour — so a future reader cannot re-derive the wrong `overlay` assumption.
- Verified RED before the config edit: Test A failed (`hiddenTitle` was `undefined` on disk), Test C passed immediately (title was already `"GameLib"` — reported honestly as already-green, not RED, per plan instruction).
- Verified GREEN after the config edit: all Backend suites (149/149), the Preload `framelessRuntime.test.ts` suite (7/7, 126 tests), and `cargo check --manifest-path src-tauri/Cargo.toml` (exit 0) all pass.

## Task Commits

1. **Task 1: Gate hiddenTitle:true and title:"GameLib", then add the key** - `58ec7b2e9` (fix)
   - RED observed pre-commit (Test A failing against on-disk config before the edit); config edit + all gates green folded into a single commit per plan's single-task structure.

**Plan metadata:** not yet committed (orchestrator handles the docs commit per task instructions).

## Files Created/Modified
- `src-tauri/tauri.conf.json` - Added `"hiddenTitle": true` to `app.windows[0]` (label `"main"`), placed immediately after `"titleBarStyle": "Visible"`. `"title": "GameLib"` and `"titleBarStyle": "Visible"` unchanged.
- `src/backend/__tests__/tauriWindowConfig.test.ts` - Widened `TauriWindowConfig` interface (`title?: string`, `hiddenTitle?: boolean`); extended header docstring with F1/F2/F4 citations; added `describe('tauri.conf.json main window title rendering (QUICK-260815-k25)')` with Tests A-D.

## RED/GREEN Gate Log

| Gate | Pre-edit (RED expected) | Post-edit (GREEN expected) |
|------|--------------------------|------------------------------|
| Test A (`hiddenTitle === true`) | **FAILED** — `Expected: true, Received: undefined` (key absent on disk) | **PASSED** |
| Test B (SANITY, non-vacuity for A) | PASSED (both known-bad fixtures throw as designed — sanity tests are not the RED/GREEN target) | PASSED |
| Test C (`title === "GameLib"`) | **PASSED already** — locks an already-true invariant, reported honestly, NOT presented as having been RED | PASSED |
| Test D (SANITY, non-vacuity for C) | PASSED | PASSED |

## Verification Results

**Backend jest** (`npx jest --selectProjects Backend src/backend/__tests__/tauriWindowConfig.test.ts src/backend/__tests__/tauriConf.test.ts src/backend/__tests__/tauriShellSource.test.ts`):
```
Test Suites: 149 passed, 149 total
Tests:       3262 passed, 3262 total
```
No failures.

**Preload jest** (`npx jest --selectProjects Preload src/preload/__tests__/framelessRuntime.test.ts`):
```
Test Suites: 7 passed, 7 total
Tests:       126 passed, 126 total
```
(Note: this run pulled in the full Preload project's 7 suites, consistent with prior sessions' behavior for `--selectProjects`; `framelessRuntime.test.ts` itself is PASS with no failures, and `applyFramelessDecorations` was not collaterally touched.)

**cargo check** (`cargo check --manifest-path src-tauri/Cargo.toml`):
```
Compiling gamelib-shell v0.7.0 (.../src-tauri)
Finished `dev` profile [unoptimized + debuginfo] target(s) in 21.03s
```
Exit 0 — proves `"hiddenTitle"` deserializes correctly under `deny_unknown_fields` (key spelling gate, per F4).

**Done-criteria node check:**
```
$ node -e "const c=require('./src-tauri/tauri.conf.json').app.windows.find(w=>w.label==='main'); console.log(c.hiddenTitle, JSON.stringify(c.title), c.titleBarStyle)"
true "GameLib" Visible
```
Matches exactly.

**git diff --stat:** exactly 2 files changed (`src-tauri/tauri.conf.json`, `src/backend/__tests__/tauriWindowConfig.test.ts`), nothing under `src-tauri/gen/`.

**git status --porcelain src-tauri/gen:** empty (F5 confirmed — no generated artifact needs regenerating).

## Deviations from Plan

None — plan executed exactly as written. No Rule 1-4 auto-fixes were needed; the config edit and test additions matched the plan's `<action>` and `<behavior>` blocks directly.

## Threat Flags

None. This plan's threat register (T-k25-01 through T-k25-04) was already fully addressed by the plan's own design — `hiddenTitle` is orthogonal to `title`, is not cfg-gated at the field level (only at consumption, per F6), and login windows are structurally unreachable from this key (per F7, `WebviewWindowBuilder::new` never reads `WindowConfig`). No new network endpoints, auth paths, file access patterns, or schema changes were introduced.

## Known Stubs

None.

## Human-Verify Checkpoint: PENDING

**Task 2 (Live-verify the title is unpainted but the window is still named GameLib) was NOT executed.** Per this quick task's constraints, the executor must not launch the app or self-approve this checkpoint — it is presented here verbatim for the user (or a resuming session) to carry out.

### What was built

`"hiddenTitle": true` added to the `main` window in `src-tauri/tauri.conf.json`, so macOS applies `NSWindow.setTitleVisibility(Hidden)` at window creation. `"title": "GameLib"` is unchanged, so the window keeps its OS-level name. Two jest gates now pin both halves, each with non-vacuity fixtures.

### How to verify (verbatim from plan)

This change is compiled into the Rust shell and CANNOT be observed by jest, by `cargo check`, or by any source-text gate. It needs a real rebuild.

Launch with `pnpm tauri:dev` — NOT bare `tauri dev`, which serves a STALE static bundle (`frontendDist` is a directory, not a dev server). `pnpm tauri:dev` runs `electron-vite build && pnpm build:sidecar && tauri dev`. Because this touches `tauri.conf.json`, expect a Rust relink, not just a frontend rebuild.

1. Frameless OFF (the shipped default, `framelessWindow: false`): the native macOS title bar is present with its traffic lights, but it must contain NO text. Confirm the word "GameLib" is painted nowhere in the title bar.
2. Frameless ON (Settings -> toggle frameless window; restart if the toggle asks): the title bar is transparent and the webview extends under it. Confirm "GameLib" is NOT painted over the nav row, and that the tab strip at the 42px navbar height is completely unobstructed — no ghost text over or behind the tabs. This is the actual reported symptom.
3. Toggle frameless OFF -> ON -> OFF once more without restarting. The title must stay unpainted in every state — `hiddenTitle` is creation-only and has no runtime setter, so a reappearance here would falsify F2 and must be reported, not worked around.
4. OS-level name retained (this is the half the user cares about and the half a screenshot will not show). With the app focused, check ALL of:
   a. the macOS menu bar's "Window" menu — the entry must read "GameLib";
   b. Mission Control (F3 / Ctrl-Up) — the window's label must read "GameLib";
   c. Cmd-Tab — the app must still be identified as GameLib;
   d. if you can, turn VoiceOver on (Cmd-F5) and Cmd-Tab into the app — the window announcement must include "GameLib". Say so if you skip this one.
5. Login window NOT collaterally changed (F7): open any store login (Settings -> log in to a store, or the Login screen). Its own window must STILL show a title, and that title must still carry the origin/host. If a login window has lost its title, stop — that means the config key leaked into a builder path that F7 says it cannot reach.
6. Nothing else moved: traffic lights still sit in their 78px reserve without overlapping the first tab, and the Downloads ring at the nav-right is still fully visible.

### Resume signal

Type "approved", or describe exactly what is wrong and in WHICH state (frameless ON / OFF / after-toggle) and on WHICH surface (main window / Window menu / Mission Control / Cmd-Tab / VoiceOver / login window).

### Step 4d disposition

Not attempted — checkpoint has not been run at all pending user action.

## Self-Check: PASSED

- FOUND: `src-tauri/tauri.conf.json`
- FOUND: `src/backend/__tests__/tauriWindowConfig.test.ts`
- FOUND: commit `58ec7b2e9` (`git log --oneline --all | grep 58ec7b2e9` → present)
