node_modules
dist
build
coverage
public/locales/
flatpak
flatpak-build
sign/
.github/workflows/build-base.yml
signatures/version1/cla.json
playwright-report
# Playwright run output, same family as playwright-report above.
# Prettier 3.x does NOT read .gitignore, so gitignoring this is not enough --
# the entry has to be here to keep `prettier --check .` off it.
test-results
pnpm-lock.yaml

# Build output — generated, never hand-edited. `prettier --check .` was walking
# src-tauri/target/.fingerprint/*.json (2791 files), which is what made the
# pre-push hook take 11.4 minutes and report thousands of irrelevant diffs.
src-tauri/target
src-tauri/gen

# Tooling/agent state and planning artifacts, not shipped source.
.planning
.claude
graphify-out
